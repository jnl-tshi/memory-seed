---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-06
---

## 2026-08-06 03:43 - The dry-run score is carried by index.md, not by retrieval - so the re-run was not spent

```yaml
entry_id: mse_rrv13mdsnrm8e2n0
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/experiment/dryrun-requiz
topics:
  - retrieval
  - testing
related_entries:
  - d1 -> mse_26e49psx6rcdpee5
```

### Summary

- Set out to re-measure the memory-index dry-run after the ranking rebuild. Did not spend the ~$25,
  because a free pre-check showed the instrument does not measure ranking.
- The expected answers live in `.memory-seed/index.md`; `memory_search` indexes only
  `.memory-seed/sessions/**`; `AGENTS.md` tells the agent to read `index.md` as step 2 of
  orientation. The blended score is carried by summary-file reading.
- The BM25F change shows no small-corpus regression on the search path, which was the risk worth
  checking.

### Decisions

#### D1 - Do not run the end-to-end re-quiz; the instrument cannot see the change

- D: Stop after the free phase. Record the finding and leave run 3's numbers as the last word,
  rather than adding a run 4 that would be misread.
- R: I recommended this re-run as the thing that would show whether the day's work was worth it, and
  that recommendation was wrong twice over. First, the instrument is saturated: run 3 scored 30/31,
  so the blended number can move by at most one question. Second and decisively, it does not measure
  retrieval at all. The answers to the quiz - "Marcus owns the benchmark suite", "Dana is lead
  maintainer", the 1.2us baseline - sit in `index.md`, which is not in the searchable corpus, and
  the fixture's own `AGENTS.md` instructs the agent to read that file during orientation. A re-run
  would therefore have returned roughly 96.8 whatever the ranker did, and that null was primed to be
  read as "the ranking rebuild did no harm" when the truthful reading is "the ranking rebuild was
  not on the critical path". Twenty-five dollars for a number that would have misinformed us is a
  worse outcome than no number.
- A: Running it anyway as a regression check was declined - the columns it would have moved are not
  the ones ranking touches, so a stable score would have been evidence of nothing. Re-seeding a
  fresh corpus first was declined as a larger piece of work that this finding reshapes: the question
  to ask of a new fixture is now different.
- F: `experiments/memory-index-dryrun/answer_visible.py`.
- T: Phase 1 gate CLEAR - answer-visible identical under both configurations at k=1/3/8. The
  configurations were confirmed to differ before that null was accepted (same query, different
  order and different scores), and the insensitivity was traced to the store holding 12 decision
  chunks against a top-8 window, so the top 8 contain 8 of 12 either way.

#### D2 - Re-read what run 3's +22.6 actually bought

- D: Record that the dry-run's jump from 74.2 to 96.8 is attributable to routing durable facts into
  `index.md` at capture time, not to retrieval quality.
- R: The plan document already states decision-level retrieval was worth +3.2 of the total, and this
  run explains the rest: once a fact is in `index.md` the agent reads it directly and no ranking is
  involved. That matters for how the score is described externally. It is a fair measure of the
  product - agents really do read that file - but it is not evidence about the retrieval layer, and
  the Verging Labs comparison in the same document sits next to entrants whose scores are recall
  measurements. Quoting 96.8 as a retrieval result would overclaim.
- A: Leaving the attribution implicit was declined; the number is already in a strategy document and
  the misreading is one step away.
- F: `docs/2_Todo/memory-index-dry-run-plan.md`, `experiments/memory-index-dryrun/answer_visible.py`.
- T: `grep` of the seeded store for each expected answer, plus the corpus source listing showing
  only `sessions/2026-08/2026-08-05.md` is indexed.

### Facts

- The honest end-to-end test of retrieval is to quiz with `index.md` withheld, so `memory_search` is
  the only route to an answer. That is a new measurement rather than a comparison against run 3, and
  it is the shape the next dry-run should take.
- The seeded workspace holds 10 entries / 12 decision chunks. At that size a top-8 window returns
  most of the store regardless of ordering, so any top-k metric on it is close to insensitive - a
  limit worth knowing before designing another small-fixture experiment.
- The fixture template's `.mcp.json` runs `python -m memory_seed.mcp_server` with `PYTHONPATH`
  pointed at the primary working tree, so spawned sessions load local source and would have picked
  up the ranking changes. That dependency was checked first and was not the problem.

## 2026-08-06 04:30 - Half the corpus is served as a 280-character teaser

```yaml
entry_id: mse_4x19c1z025rg5d8x
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/excerpt-fallback-defect
topics:
  - retrieval
  - testing
related_entries:
  - d1 -> mse_rrv13mdsnrm8e2n0
```

### Summary

- Asking `search_memory` for decision granularity does not guarantee decision chunks: an entry with
  no `#### Dn` DRAFT block comes back as an entry chunk, and entry chunks are served as a
  280-character single-line preview instead of the whole block.
- 571 of 1259 chunks on the real corpus (45%), withholding 987,701 characters - 51% of everything
  the store holds. 488 of those chunks are over 1000 characters, so it is not an edge case.
- The fix is not applied: it is a payload-budget decision, and picking a shape without measuring is
  how the constants this session spent the day fixing came to exist.

### Decision

- D: Record the defect and the three candidate fixes in `docs/2_Todo/excerpt-fallback-defect.md`;
  apply none of them yet. Keep `answer_visible.py`'s 7/23 as the recorded "before".
- R: The excerpt branch at `retrieval.py:2625` tests the chunk's ACTUAL granularity rather than the
  granularity the caller asked for, so the whole-DRAFT-block behaviour that shipped on 2026-08-05
  only ever applied to entries that use the DRAFT decision format. Everything else silently kept the
  old preview. The consequence is not "previews are cheap" but "whichever entries happen to lack
  decision headings are invisible", which is arbitrary rather than economical. Serving every entry
  chunk at the 2500 cap would take a top-8 payload from roughly 2k characters to as much as 20k, so
  the right shape is probably a budget across the result set rather than a per-chunk cap - and that
  is a measurement, not a preference. Applying a guess tonight would repeat exactly the pattern the
  rest of the day was spent undoing.
- A: Fixing it immediately with option 1 (serve everything at the decision cap) was declined on the
  payload cost, which nobody has measured. Leaving it unrecorded until there was time to fix it was
  also declined - the finding is worth more than the fix right now, because it changes how the
  dry-run's 96.8 should be read.
- F: `docs/2_Todo/excerpt-fallback-defect.md`, `experiments/memory-index-dryrun/answer_visible.py`.
- T: Measured on both corpora. Before-number for any fix: answer-visible 7/23 at k=8 on the seeded
  store, with all 23 answers confirmed present in the indexed session entries.

### Facts

- The dry-run's dependence on `index.md` is a consequence of this defect, not a design choice. All
  23 answers are in the session entries; retrieval was serving 280 characters of each, so the agent
  had to read the summary file. That is why the blended score is insensitive to ranking.
- Ruling this out took four eliminations in order, each of which looked like the answer: the ranking
  configuration (both tested, orderings genuinely differ), the corpus (all answers present), the
  top-k window (serving all 12 chunks changes nothing), and `DECISION_TEXT_LIMIT` truncation (the
  target terms sit at characters 859-1180, well inside the 2500 cap).
- `_excerpt` also collapses newlines with `" ".join(text.split())`, so an affected result loses its
  structure as well as its length - a DRAFT block arrives as one run-on line.

## 2026-08-06 04:49 - One way to see a decision, and half the corpus becomes visible

```yaml
entry_id: mse_qa4h8zzmp8sgfpmr
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/excerpt-fallback-defect
topics:
  - retrieval
  - session-logging
evolves:
  - d1 -> mse_4x19c1z025rg5d8x
```

### Summary

- The reader recognised only `#### Dn` as a decision, so the 494 entries using the singular
  `### Decision` form produced no decision chunk, fell back to the whole-entry unit, and were served
  as a 280-character preview. 45% of the corpus, 51% of its text, silently withheld.
- Fixed at the cause rather than by budgeting payload: 571 chunks served at 280 characters down to
  77, withheld text 51% down to 2%, and answer-visible on the seeded fixture 7/23 up to 22/23.
- `memory_get_chunk` also could not fetch a decision chunk at all, which made two shipped behaviours
  dead letters.

### Decisions

#### D1 - The numbered form is the only decision shape; the singular one is legacy

- D: `session_logging.md` (live and seed) now mandates `### Decisions` + `#### D1` regardless of how
  many decisions an entry records. `_find_decision_ranges` recognises the singular `### Decision`
  heading as the same concept, marked LEGACY, so existing entries stay readable.
- R: JNL's reading was that the fault is having two ways of seeing a decision at all, and that is
  the right level. My own framing had been a payload-budget trade-off between serving 2500
  characters and 280, which took the two authoring shapes as given and would have shipped a
  compromise instead of a fix. There was no trade-off to make: once the singular form is recognised
  the fallback almost never fires, so nothing has to be rationed. Numbering every decision also
  makes `entry_id:d1` addressing universal, which lifecycle edges, ADR sidecars and decision-scoped
  topics already assume - the singular form was the one shape those could not name.
- A: Rewriting the 494 existing entries into the numbered form was never on the table: the store is
  append-only. Keeping both shapes as co-equal was declined - that is the state that produced the
  defect, and a reader with two notions of a decision will drift again. Serving entry chunks at the
  decision cap was declined as treating the symptom, and it would have raised a top-8 payload from
  about 2k characters to as much as 20k for no reason.
- F: `memory_seed/semantic_cache.py` (`_LEGACY_SINGLE_DECISION_RE`, `_find_decision_ranges`),
  `.memory-seed/skills/session_logging.md` and its seed twin,
  `docs/2_Todo/excerpt-fallback-defect.md`.
- T: Real corpus, chunks served at 280 characters: 571/1259 -> 77/1259; withheld text 51% -> 2%.
  Seeded fixture answer-visible@8: 7/23 -> 22/23. 1072 tests pass. The remaining 77 are entries with
  no decision section at all, which correctly stay whole and now have a test fixture of their own.

#### D2 - `memory_get_chunk` can fetch a decision chunk

- D: `get_chunk` searches decision granularity after entry and section.
- R: It searched entry then section and stopped, so `mse_x:d1` raised "chunk_id not found". Two
  shipped behaviours depended on it working: the truncation marker on a long decision reads "call
  memory_get_chunk for the full decision", which was an instruction to make a failing call, and the
  attention layer scores on `memory_get_chunk` fetches, so a decision chunk could never register
  one - the ranking signal was unreachable for the granularity that is now the default. Neither
  surfaced as an error anywhere; both were found by trying the call.
- A: Searching decision granularity first was declined - entry and section ids are more common and
  cheaper to match, and this path is on every fetch.
- F: `memory_seed/retrieval.py` (`get_chunk`).
- T: Decision ids returned by `search_memory` now fetch; verified against three.

#### D3 - Two tests were asserting the defect

- D: Replace `test_entry_without_decision_headings_stays_whole` with one asserting the legacy form
  becomes a decision chunk, and add a fixture with genuinely no decision section so the original
  property keeps a home. Make the supersession damping assertion relative rather than absolute.
- R: The first test's fixture is titled "a plain entry with no decision headings" and contains a
  `### Decision` section with D and R bullets - it was a single-decision entry all along, and the
  test had pinned the defect as intended behaviour. Its stated property is still worth holding, so
  it needed a fixture that actually has it rather than deletion. The second compared a damped score
  to six decimal places of an absolute value, so it broke when BM25F changed the scale while the
  damping relationship held exactly; comparing the ratio fixes the class, and five places rather
  than six because the payload rounds scores before serving them.
- A: Loosening the damping tolerance without switching to a ratio was declined - that hides the next
  scale change instead of being immune to it.
- F: `tests/test_decision_retrieval.py`, `tests/test_retrieval.py`.
- T: 1072 pass.

### Facts

- The defect was found by asking why the memory-index dry-run scored 96.8 while retrieval surfaced
  7 of 23 answers, and it took four eliminations that each looked like the answer: ranking
  configuration, corpus contents, the top-k window, and `DECISION_TEXT_LIMIT` truncation.
- `_excerpt` collapses newlines with `" ".join(text.split())`, so an affected result lost its
  structure as well as its length - a DRAFT block arrived as one run-on line.
- The dry-run's dependence on `index.md` was a consequence of this, not a design choice. Whether
  its 96.8 changes now that retrieval can serve whole blocks is unmeasured.

## 2026-08-06 09:32 - A fetched decision arrives with the entry that frames it

```yaml
entry_id: mse_a4d2k37tq11ckezm
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/decision-entry-context
topics:
  - retrieval
evolves:
  - d1 -> mse_qa4h8zzmp8sgfpmr:d2
```

### Summary

- `memory_get_chunk("entry:d1")` now returns `entry_context` alongside the decision block: the
  entry's Summary, Follow-up, Validation, Facts - whatever the author recorded at entry level.
- Sibling decisions are excluded, so asking for `:d1` does not drag `:d2` back in.

### Decision

#### D1 - A decision payload carries its entry's non-decision sections

- D: Add `entry_context` to the `get_chunk` payload for decision chunks - an ordered list of the
  entry's `###` sections excluding the decisions container. Entry-granularity fetches get an empty
  list, since the whole entry already contains them.
- R: JNL's point is that a decision is not self-contained. The D/R/A/F/T block records the choice,
  but the `### Summary` says what the session was doing when it made it and the `### Follow-up` says
  what was left open - both written deliberately, both lost the moment retrieval started serving
  decisions rather than entries. Decision granularity became the default this week, so every fetch
  since has been handing agents a block stripped of its frame. All entry-level sections travel
  rather than a chosen subset, because which of them bears on a given decision is the author's
  judgement expressed in the entry, not something the reader can infer. The decisions container is
  the one exclusion: its body IS the sibling blocks, so including it would return the whole entry
  and defeat the point of asking for one decision.
- A: Merging the sections into `text` was declined - `text` is the decision block and callers rely
  on that; context belongs beside it, labelled, so a reader can tell what the author wrote about
  this decision from what they wrote about the session. Returning only Summary and Follow-up was
  declined as a guess about relevance that the entry format does not license.
- F: `memory_seed/retrieval.py` (`entry_context_sections`, `get_chunk`),
  `memory_seed/mcp_server.py` (tool description), `tests/test_decision_retrieval.py`.
- T: 1075 tests pass, three of them new: context is present and contains Summary, siblings are
  excluded, and entry-granularity fetches stay empty. Verified against the real corpus - a decision
  fetch returns Summary at 548 characters and Facts at 832 with no sibling content.

### Facts

- Three test methods were briefly invisible: appended after `if __name__ == "__main__":`, they sat
  inside that block at matching indentation, so Python accepted them and pytest never collected
  them. A test that is never collected passes silently, which is the same shape as the defects this
  session has been finding.
- The MCP tool description for `memory_get_chunk` now states that decision ids resolve and that a
  decision payload carries `entry_context`, since neither was discoverable from the schema.

## 2026-08-06 11:44 - 31/31 without the summary file, and why that is not a retrieval result

```yaml
entry_id: mse_gzb04pqa6btkfeeb
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/experiment/retrieval-only-quiz
topics:
  - retrieval
  - testing
evolves:
  - d1 -> mse_rrv13mdsnrm8e2n0
```

### Summary

- Ran the dry-run with `index.md`'s Active State blanked, so the durable facts existed only in the
  session entries: 31/31, zero fabrications, 8/8 on the false-memory traps.
- Better than run 3's 30/31 under a strictly harder condition, which is the first end-to-end proof
  that the decision store alone carries every answer.
- It still does not measure retrieval, for the same structural reason as the last two attempts, and
  that is recorded rather than glossed.

### Decisions

#### D1 - Blank the Active State section rather than delete index.md

- D: The retrieval-only arm strips `## Active State` from each quiz copy and keeps the rest of the
  file, behind a `--no-index-facts` flag on `dryrun.py quiz`.
- R: Deleting the file would have changed the experiment into something else. `AGENTS.md` treats a
  missing `index.md` as "seeded but not bootstrapped" and directs the agent into bootstrap mode,
  which would have it try to REBUILD the index rather than answer questions - a different task, and
  one that also writes to the store the quiz is supposed to read. Blanking one section leaves a
  normally-bootstrapped project whose durable facts simply are not written down outside the session
  entries. Every seeded answer lives in that section and nothing outside it leaks a fact, which was
  checked term by term against the maintainer roster, the ownership lines, the baseline figure, the
  cadence and the Python version before the run.
- A: Deleting `index.md` was declined for the bootstrap-mode reason. Re-seeding a corpus that never
  writes an index was declined as a larger change that would also have reintroduced capture variance
  into a comparison meant to isolate reading.
- F: `experiments/memory-index-dryrun/dryrun.py` (`strip_index_facts`, `--no-index-facts`),
  `docs/2_Todo/memory-index-dry-run-plan.md` (run 4).
- T: The stripper was verified on a throwaway copy before any spend - file present, all six section
  headings intact, none of the nine probe terms remaining. 8 quiz batches, all exit 0; 31/31 judged.

#### D2 - Record that 100.0 is not a retrieval measurement

- D: State in the plan document that run 4 establishes the store's sufficiency and the abstention
  behaviour, and explicitly does NOT establish that ranked retrieval surfaced the answers.
- R: The quiz preamble permits reading "files under `.memory-seed/`", the entire store is a single
  10-entry file, and 2 of the 8 answer batches cite that file path as their source outright. Agents
  do not narrate tool use, so the artifacts cannot show how the other six answered. A 100.0 that I
  cannot attribute is exactly the kind of clean number this session has repeatedly found to be
  measuring something other than the intended thing - this is the third instrument in two days that
  turned out to have a route around the mechanism under test, after the dry-run's `index.md` and the
  paraphrase probe's verbatim queries. Isolating retrieval needs a harness change rather than a
  fixture change: deny filesystem reads of `.memory-seed/sessions/**` so the MCP tools are the only
  route.
- A: Reporting 100.0 as validation of the day's retrieval work was declined; it would have been the
  most flattering reading available and the least supported.
- F: `docs/2_Todo/memory-index-dry-run-plan.md`.
- T: 2 of 8 batches cite `.memory-seed/sessions/2026-08-05.md` explicitly; 1 mentions a search tool;
  5 say nothing about method.

### Facts

- All 31 answers are recoverable from the session entries alone. Run 3's dependence on `index.md`
  was a symptom of the excerpt-fallback defect, not a property of where the facts were written.
- Abstention holds without the summary file: 8/8 false-memory traps, zero fabrications, under the
  harder condition.
- The sharpest retrieval instrument available remains `answer_visible.py`, which measures the search
  path directly and moved 7/23 to 22/23 on this store when the excerpt fallback was fixed.

## 2026-08-06 12:07 - 96.8 from retrieval alone, and an instruction that every agent ignored

```yaml
entry_id: mse_42fpw7wbc1f1fs6h
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/experiment/retrieval-only-enforced
topics:
  - retrieval
  - testing
evolves:
  - d1 -> mse_gzb04pqa6btkfeeb
```

### Summary

- With `index.md`'s facts blanked AND file tools denied, the store scored **30/31 = 96.8** with
  `memory_search` and `memory_get_chunk` as the only route in, zero fabrications, 8/8 on the traps.
- Telling agents to use retrieval only did not work: all 8 batches read files anyway, 16 `Grep` and
  13 `Read` calls against 19 `memory_search`. Denying the tools worked completely.
- This is the first end-to-end number that measures retrieval rather than something adjacent to it.

### Decisions

#### D1 - Enforce the constraint rather than request it, and record compliance either way

- D: The retrieval-only arm passes `--disallowedTools Read Grep Glob LS Bash`, and every quiz batch
  now records `tools_used` extracted from the stream-json tool_use events.
- R: JNL asked for a re-run telling agents to use only the correct method. That run was worth doing
  precisely because it failed: given an explicit, reasoned instruction not to open anything under
  `.memory-seed/`, every one of the eight sessions did it anyway, and roughly half the lookups
  bypassed retrieval. An experiment whose isolation depends on the subject choosing to comply is not
  isolated, and until this run nothing in the harness would have revealed that - run 4 reported
  100.0 and I could not attribute a single answer. Recording `tools_used` is the durable half: the
  next person to add a constraint gets to measure whether it held instead of assuming.
- A: Trusting the instruction was tried and is now recorded as insufficient. Deleting the session
  files from the quiz copies was declined - that changes what the store contains rather than how it
  may be read, and the agent would have found an empty corpus rather than an unreachable one.
- F: `experiments/memory-index-dryrun/dryrun.py` (`--retrieval-only`, `run_claude(extra=...)`,
  `tools_used`), `docs/2_Todo/memory-index-dry-run-plan.md` (run 5).
- T: `--disallowedTools` was verified to survive `--dangerously-skip-permissions` on a
  single-batch probe before the full run. Enforced arm: zero file-access calls across 8 batches,
  30 `memory_get_chunk` and 28 `memory_search`.

#### D2 - Read the 96.8 as the retrieval result, and the one miss as a fixture ambiguity

- D: Record run 5b's 30/31 as the isolated retrieval measurement, and Q1's miss as an ambiguity in
  the answer key rather than a retrieval failure.
- R: Q1 asks for the maintainer roster; the agent answered Dana, Priya and Marcus plus Sofia, and
  the key predates Sofia. The store says she "joined 2026-08-05, specifically to own Windows CI",
  which does not settle whether that makes her a maintainer - so retrieval surfaced the later update
  and was marked wrong against the earlier key. Calling that a retrieval miss would understate the
  system; calling it a win would be marking my own homework, so it is recorded as what it is and the
  score stands at 30/31 unadjusted.
- A: Amending the key to include Sofia was declined - the fixture predates this session and editing
  it after seeing a result is how a benchmark stops meaning anything.
- F: `docs/2_Todo/memory-index-dry-run-plan.md`.
- T: 30/31, fabrications 0, false_memory 8/8. Run 3 scored the same 96.8 with the summary file
  readable and file tools available.

### Facts

- Instruction compliance, measured: 0 of 8 batches obeyed "do not read files under `.memory-seed/`".
  Tool denial: 8 of 8 complied, because they had no choice.
- The same 96.8 now means something different from run 3's. Then it was reachable by reading a
  hand-maintained summary; now it is reached through `memory_search` and `memory_get_chunk` with the
  summary blanked and the filesystem closed.
- The enforced arm also denies committed project files outside `.memory-seed/`. No question in this
  fixture needed them, but a future one might, and the denial is currently blanket.

## 2026-08-06 14:40 - Mechanical identity and a projected payload

```yaml
entry_id: mse_74w7fty24yrs3the
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/payload-projection-and-identity
topics:
  - retrieval
related_entries:
  - d2 -> mse_a4d2k37tq11ckezm
```

### Summary

- `chunk.user` now resolves mechanically from the participant registry: 818 of 822 initialled
  entries map to `jean`, where before every one of 856 entries carried `user: null`.
- The MCP `memory_get_chunk` payload is now a projection of the full record: exact aliases and
  silently-empty optional fields dropped, lifecycle and attention fields kept even when empty.
- The internal record (`chunk_to_dict`) is untouched - Trace hard-indexes six of the fields the
  projection removes, so the boundary is where the economy lives, not the record.

### Decisions

#### D1 - Join user_initials to the participant registry at extraction

- D: `_resolve_chunk_users` runs at the end of `extract_memory_chunks`: where `chunk.user` is null
  and `user_initials` is set, the initials -> slug map from `_participant_slug_by_initials` fills
  it. A file-layout user always wins; shared initials stay unresolved rather than guessed. The
  registry gained a second row mapping the historical `JN` initials to `jean`.
- R: JNL identified the inversion: `local.yaml` and `project.yaml` declare the identity mapping
  precisely so ids are mechanical, yet `user` was null on 856 of 856 entries because it was only
  ever populated from the per-user file layout this project does not use, while `user_initials` sat
  beside it authored by hand. The join was declared and nobody performed it. Extraction is the
  right seam because every consumer inherits it - search, get_chunk, Trace, the graph - and
  `semantic_cache` already imports `core`, so the resolver is reachable without a cycle. The `JN`
  alias is registry data, not code: 120 early entries predate the JNL convention, they are the same
  person, and the resolver already supports two initials rows sharing a slug.
- A: Resolving at the payload layer was declined - it would fix `get_chunk` and leave every other
  consumer null. Guessing on ambiguous initials was declined; the resolver reports duplicates and a
  wrong identity is worse than a missing one. The 4 `USER` fixture entries correctly stay null.
- F: `memory_seed/semantic_cache.py` (`_resolve_chunk_users`), `.memory-seed/project.yaml`.
- T: 818/822 resolve to `jean`; the 4 `USER` placeholders stay null; 1078 tests pass.

#### D2 - Project the agent-facing payload at the MCP boundary only

- D: `_project_chunk_payload` in `mcp_server.py` drops the exact aliases (`path`, `date`,
  `source_file`, `entry_title`) and `lexical_terms`, and omits empty optional fields - except
  lifecycle (`replaces`, `replaced_by`, `replacing_head`, `evolves`, `evolved_by`) and the
  attention triple, which are always present. `chunk_to_dict` is unchanged.
- R: JNL flagged the duplication from a real payload, and every instance checked out - each alias
  is byte-identical to a field that stays. But the full record could not be thinned: Trace
  hard-indexes `payload["entry_title"]`, `payload["source_file"]`, `payload["contexts"]`,
  `payload["lexical_terms"]` and `payload["path"]`, so removal there is a KeyError, and the MCP
  contract tests pinned the aliases deliberately. The one-line handler is the natural boundary: the
  agent surface thins by ~12-16% while every internal consumer keeps the record it indexes.
  `lexical_terms` illustrates the split exactly - worth its BM25F weight of 3.0 for scoring, worth
  nothing serialised, since it restates strings already inside `text`. The empty-field exception is
  the substantive rule: an empty `replaced_by` is the claim "this decision stands unsuperseded" and
  `fetch_count: 0` is "never fetched"; omitting those would make a statement indistinguishable from
  silence, which is the one ambiguity a memory system must not introduce.
- A: Thinning `chunk_to_dict` itself was declined on the Trace evidence above - a migration to save
  ~6% of a payload. Dropping `contexts` was deferred to the ADR-sidecar pass at JNL's direction,
  along with the decision-scoped durable-facts question and the ADR/Constitution attachment.
- F: `memory_seed/mcp_server.py` (`_project_chunk_payload`, tool description),
  `tests/test_mcp_server.py`, `tests/test_retrieval.py`.
- T: Three new tests: aliases and silent empties dropped, lifecycle and attention retained when
  empty, and the full record still carrying every alias for Trace. The two parity tests now assert
  parity THROUGH the projection rather than raw equality - parity redefined, not weakened. Measured
  on a real decision chunk: 44 fields to 31, ~170 tokens (12%).

### Facts

- The Trace test suite does not collect in this venv (`ModuleNotFoundError: memory_trace`) on clean
  main as well as on this branch - environmental, tracked separately from this change.
- Of the plan's verification items, one assumption was wrong: the MCP contract pins at
  `test_mcp_server.py:854-857` assert on the tool response, not on `chunk_to_dict`, so they were
  updated to pin the projection rather than passing unchanged.

## 2026-08-06 14:57 - Normalise the historical JN initials to JNL across the live corpus

```yaml
entry_id: mse_m48b7spxwxwm41zc
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/normalise-jn-initials
topics:
  - session-logging
evolves:
  - d1 -> mse_74w7fty24yrs3the:d1
```

### Summary

- All 120 `user_initials: JN` lines across 16 session files from 2026-05/06 are now `JNL`, on JNL's
  instruction. Zero remain; 819 initialled entries resolve to `jean` and only the 4 `USER` fixture
  placeholders stay null.
- The `JN -> jean` registry alias added earlier today is removed - with the corpus normalised, the
  registry needs one row again.

### Decision

#### D1 - Edit the metadata field in place, exactly, and only in the live sessions

- D: Replace the full line `user_initials: JN` with `user_initials: JNL` in
  `.memory-seed/sessions/**`, anchored to the whole line. Leave `.memory-seed/backups/` untouched
  and drop the now-redundant alias row from `project.yaml`.
- R: A live owner instruction is the amendment authority, and this is metadata normalisation rather
  than a content rewrite - no prose, no decision text, no timestamps change. Two hazards were
  checked before editing rather than after. First, entry ids are derived from metadata that
  includes `user_initials`, so if anything validated stored ids by recomputation this edit would
  have orphaned 120 entries; both `generate_session_entry_id` call sites are write-time minting
  (`session_append_entry` and the CLI `entry-id` command), nothing recomputes, ids are safe.
  Second, a substring replace would corrupt every existing `JNL` into `JNLL`, so the edit is
  anchored to the exact YAML line - the replaced count (120) matches the measured count exactly,
  which is the check that nothing else moved. Backups stay as authored: a backup is a point-in-time
  snapshot, and editing one falsifies the record it exists to be; they are gitignored and the
  resolver never reads them.
- A: Keeping the alias row instead of editing the corpus was this morning's answer and JNL
  overrode it - normalised data beats a mapping that papers over drift, and the alias would have
  been a permanent second row explaining a historical accident. Editing the backups was declined
  for the snapshot reason above.
- F: 16 files under `.memory-seed/sessions/2026-05/` and `2026-06/`, `.memory-seed/project.yaml`.
- T: 0 `user_initials: JN` remaining in live sessions; resolution 819 -> `jean`, 4 `USER` null;
  `links check` green over 158 files; 1078 tests pass.

### Facts

- Entry ids are stable under metadata edits: `generate_session_entry_id` runs only at write time,
  and no validator recomputes stored ids from their metadata.
- `.memory-seed/backups/` still carries the historical `JN` lines by design.

## 2026-08-06 16:07 - ADR contract: constitution bindings in the ledger, founding sources for corpus-less concerns

```yaml
entry_id: mse_n35t6pkz73cp59vc
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/adr-contract-extension
topics:
  - schema
  - control-plane
evolves:
  - d1 -> mse_kbc2mq9972ppy1vw:d1
```

### Summary

- Two additive, omit-empty fields on the ADR event ledger: `constitution_refs` (the ADR references
  the Constitution directly, per JNL - no separate bindings sidecar) and
  `founding_source`/`founding_quote` (an ADR can be founded from a control-file line or by
  bootstrap, before any session decision exists).
- Every ADR written before the fields existed stays byte-canonical - the three live, sha256-pinned
  records round-trip untouched, held by a dedicated test.

### Decisions

#### D1 - Bindings are ledger fields, not a sidecar family

- D: `AdrEvent.constitution_refs: tuple[ConstitutionRef, ...]` - `{ref, role}` pairs, role
  `governing|supporting`, refs of the form `constitution:v1#slug` resolved at validation time
  against `<!-- constitution-ref: ... -->` anchor markers in `docs/CONSTITUTION.md`. The Current
  view renders a `### Constitution` section only when the head proposal carries bindings.
- R: JNL's question - "why can't the adr reference the constitution location directly?" - was the
  right one; my sidecar design added a fourth file family to solve a problem the event ledger
  already solves. The schema is ours: an optional field rendered omit-empty leaves every
  pre-existing record byte-identical, so no schema_version bump, no migration, and the codex
  fixtures' sha256 pins hold. Validation refuses malformed refs, unknown roles, duplicates, and -
  the working half - refs that do not resolve to a real anchor, which makes the S4 ESR audit a
  mechanical read over `iter_adrs` instead of a join across files. The >=1-governing rule is
  deliberately NOT in `validate_adr`: the three live ADRs carry no bindings and must stay valid;
  it is the campaign's gate, applied to what the campaign writes.
- A: The separate bindings sidecar (my original design) was declined by JNL and was the worse
  shape. Putting bindings in frontmatter was declined - frontmatter is stable identity per the
  contract, and bindings evolve with revisions, which is what the ledger is for.
- F: `memory_seed/adr.py` (`ConstitutionRef`, `CONSTITUTION_REF_RE`, `CONSTITUTION_ANCHOR_RE`,
  `_constitution_anchors`, `_binding_issues`, render/parse/writers),
  `tests/test_adr_contract_extension.py`.
- T: 8 new tests; the byte-canonical round-trip of all three live ADRs is the first of them. 1086
  pass.

#### D2 - Founding sources: an ADR may precede its decisions

- D: `promote_decision` accepts `founding_source` (".memory-seed/index.md#L120" or "bootstrap")
  plus a verbatim `founding_quote` in place of `source_entry_id:source_decision`. The revision is
  tracked under a `founding:<source>` pseudo-ref through replay, acceptance and rejection;
  `decision_ref` keeps its `<entry_id>:dN` grammar exclusively. Founding events may not assert
  predecessors and require the quote; `update_entry_id` becomes optional for them.
- R: JNL's correction exposed the structural requirement: S3's bootstrap runs on a fresh project
  with NO session corpus, so the machinery had to support decision-less founding regardless of the
  backfill campaign - the campaign is just the first consumer. The pseudo-ref keeps the state
  machine unchanged (proposed -> accepted works identically) while keeping the two grammars
  separate, so nothing downstream that parses `<entry_id>:dN` ever sees a founding key in
  `decision_ref`. Later `revise` events attach real decisions as work touches the concern, which
  is how a founded ADR converges to a decision-anchored one.
- A: Requiring a synthetic founding session entry per lifted bullet (my original plan) was
  declined by JNL - it manufactured provenance and would not have worked for bootstrap anyway.
  Reusing `decision_ref` to carry the founding key was declined: one field, two grammars is the
  defect pattern this week kept finding.
- F: `memory_seed/adr.py` (`FOUNDING_SOURCE_RE`, `AdrEvent.revision_key`, promote/transition,
  validate), `tests/test_adr_contract_extension.py`.
- T: Founding promote -> accept round-trips byte-canonically; bootstrap founding works with an
  EMPTY session corpus; both-sources, no-quote, bad-source and neither-source are each refused.

### Facts

- Ratification/acceptance authority for this cycle is delegated to the agent by JNL's live
  instruction of 2026-08-06 ("ratify for me and get me to the end goal and then i will iterate");
  every delegated act is recorded as such where it lands.
- The >=1-governing binding requirement is a campaign gate, not a validator rule, to keep the
  pinned pre-extension ADRs valid.
