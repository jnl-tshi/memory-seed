---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-04
---

## 2026-08-04 00:21 - Codex arm for the agent-capture experiment

```yaml
entry_id: mse_aqsfm061kmekm87m
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/agent-capture-codex-arm
topics:
  - mcp-tools
  - testing
```

### Summary

- Wired the agent-capture experiment's Codex arm and validated the instrument end to end with two
  unscored probes: an L0 MCP write that landed in the fixture store with the parent untouched, and an
  L3 run whose own SessionStart hook and git commit hook both fired inside the fixture.

### Decisions

#### D1 - Adopt four Codex harness constants, each forced by an observed failure

- D: The Codex runner passes `-c projects={"<run dir>"={trust_level="trusted"}}`,
  `--dangerously-bypass-approvals-and-sandbox`, `-c features.apps=false`, and pins the MCP server's
  `cwd` to the run directory per run rather than in the template.
- R: None is cosmetic. Codex loads project-scoped `.codex/config.toml` and `.codex/hooks.json` only
  for trusted paths and every run directory is a fresh untrusted path, so without the trust
  override L0 has no write path and L2/L3 have no hooks. `approval_policy="never"` does not cover
  MCP tool calls - a probe with it set returned `user cancelled MCP tool call` - so the approvals
  bypass is the only headless route, the direct analogue of Claude's `--dangerously-skip-permissions`.
  This machine's Codex exposes 240 MCP tools costing ~139k input tokens of tool surface per turn;
  `features.apps=false` removes them while leaving the memory-seed server intact. And the stock MCP
  entry leaves `cwd` unset, which matters because the server resolves `cwd="."` against its own
  process cwd (H4) while `resolve_runtime` walks upward with no boundary guard (H1) - unpinned, a
  write could silently land in the PARENT store while the agent reported success.
- A: The whole-table `projects={...}` form was chosen over the dotted
  `projects.<path>.trust_level` path because the dotted-path parser splits on `.` and would break on
  any path containing one. `--ignore-user-config` was tested as a way to get a clean environment
  independent of the user's config drift and rejected: it drops the project layer too, so the
  memory-seed server disappeared. `--disable apps` (the flag form) was rejected in favour of
  `-c features.apps=false` after the model's self-reported tool list proved unreliable in both
  directions - the question was only settled by making an actual `memory_topics_list` call.
  `--dangerously-bypass-hook-trust` was NOT adopted: the probe showed injected trust already covers
  hook trust, so carrying it would have been an assumed rather than a demonstrated constant.
- F: `experiments/agent-capture/run.py`, `experiments/agent-capture/generate_fixtures.py`.
- T: Fixture determinism re-verified for all four Codex levels (byte-identical tree hashes across a
  regeneration); parent `links check` (153 files) and `docs check` (191 files) both OK.

#### D2 - Codex validates the instrument, not the hypothesis, and its results are never pooled

- D: Record the Codex arm in PREREGISTRATION.md as a dated append with an explicit no-pooling rule,
  add a `--brief` probe override that `collect.py` excludes from every scored table, and fingerprint
  the parent repo before and after each run into `parent_isolated`.
- R: JNL authorised Codex only because the Claude CLI's OAuth had expired, so these runs answer
  "does the harness work", not "do agents capture decisions". Two independent facts make the arms
  non-comparable: a different tool surface and system prompt, and a materially thinner transcript -
  Codex `--json` emits `mcp_tool_call` items and the final message but no reasoning items, only a
  token count, so faithfulness can only be judged there against observable behaviour. Amending by
  append rather than edit is the same append-only discipline the product sells. The parent
  fingerprint is asserted per run rather than assumed because the approvals bypass also drops the
  sandbox.
- A: Editing the pre-registration in place was declined as a silent rewrite of a document whose
  whole value is being fixed before the result. Deferring the Codex arm until Claude auth returned
  was declined because the instrument's hazards are agent-independent and finding them now is free.
- F: `experiments/agent-capture/PREREGISTRATION.md`, `experiments/agent-capture/README.md`,
  `experiments/agent-capture/collect.py`.
- T: Probe A wrote one entry to `<run>/.memory-seed/sessions/2026-08/2026-08-04.md` with the parent
  store unchanged at 154 files / 2,729,148 bytes, and the store's validators refused the first
  malformed append; probe B stamped `Memory-Entry: mse_dm8fjga4gn7znzg8` on fixture commit `94b7cd9`
  with the fixture's SessionStart orientation block visible in its stderr, while L0 produced neither;
  `collect.py` reported both probes excluded and zero scored runs.

## 2026-08-04 00:26 - Scope the agent-capture isolation signal and validation claims

```yaml
entry_id: mse_vaz9d3h7x4bcmd1t
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/agent-capture-isolation-signal
topics:
  - testing
  - mcp-tools
evolves:
  - d2 -> mse_aqsfm061kmekm87m:d2
```

### Summary

- Narrowed the agent-capture isolation assertion to the parent session store and corrected the
  instrument's own claim about what the Codex probes proved: they validate the Codex arm only, and
  the Claude arm's equivalent hazards are now recorded as open items that block scoring there.

### Decisions

#### D1 - Assert isolation on the store alone; report working-tree dirtiness as informational

- D: `parent_isolated` compares only `session_files` and `session_bytes`; a changed `dirty_paths`
  count now prints a note instead of failing the run.
- R: The parent session store is the only thing an unpinned MCP write could reach, so it is the
  signal that actually tests the hazard. Working-tree dirtiness is not under this run's control: the
  primary checkout is shared with concurrent sessions, and a scored batch overlapping any editing
  session would have flagged `parent_isolated: false` for reasons unrelated to the experiment. A
  safety signal that cries wolf on somebody else's edit stops being read, which would cost more than
  the extra strictness ever bought.
- A: Snapshotting the whole parent tree hash was declined as far too slow to run twice per session
  across a 60-session matrix. Dropping the dirtiness check entirely was declined because an
  unsandboxed agent wandering out of the fixture is exactly what it would catch.
- F: `experiments/agent-capture/run.py`.
- T: Dry run confirms the fingerprint still reads (155 files / 2,734,163 bytes / 3 dirty paths).

#### D2 - State plainly that the Codex probes validate the Codex arm only

- D: README invariant 5 now says Claude has passed neither smoke probe, and the pre-registration
  amendment lists three open Claude-arm items - the unpinned MCP process cwd, the unmeasured
  account-level tool surface, and the scope of the `~/.claude/CLAUDE.md` constant - plus a note that
  fixture entries inherit the parent's `user_initials`.
- R: The README said scoring waits on "the smoke probes (one L0, one L3)" without naming an agent,
  so two Codex passes read as validating the instrument outright. Both hazards the Codex probes
  actually turned up have live Claude analogues: `.mcp.json` carries no `cwd` field at all, so that
  arm relies on inherited process cwd - the precise assumption that was refused for Codex against an
  H1 with no boundary guard. Leaving that implicit would have let an unvalidated arm be scored on
  another arm's evidence. The identity note matters because judge packets are built out of these
  stores and may go to an external reviewer.
- A: Re-running the Claude probes now was not possible - the CLI's OAuth is still expired, which is
  why the Codex arm exists. Silently editing the pre-registration was declined for the same reason
  the Codex amendment was appended rather than merged in.
- F: `experiments/agent-capture/README.md`, `experiments/agent-capture/PREREGISTRATION.md`.
- T: `docs check` OK (191 files); the probe-discriminating test is written down as a runnable check -
  `memory_topics_list` must return the stock seed placeholder vocabulary, not the parent's 69 topics.

## 2026-08-04 02:05 - Run the agent-capture matrix on the Claude arm

```yaml
entry_id: mse_gvq7xhvgekts00at
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/agent-capture-batch
topics:
  - testing
  - mcp-tools
related_entries:
  - mse_aqsfm061kmekm87m
```

### Summary

- Ran the full pre-registered agent-capture matrix on the Claude arm: 60 scored headless sessions,
  4 scaffolding levels x 3 tasks x 5 reps, zero harness failures and zero parent-store changes.
  Mechanical result: L0 0/15, L1 11/15, L2 11/15, L3 14/15. All fifteen L0 stores are empty files-on-
  disk empty, not merely unparsed. $99.29 and 1,366 turns across the matrix.

### Decisions

#### D1 - Score capture format-blind, after the structured-only counter faked a dip

- D: The headline mechanical metric counts whether a run recorded ANY entry; the DRAFT
  `### Decision` / `#### Dn -` shape became a separate `structured` sub-measure.
- R: The original counter keyed on the DRAFT headings, and an agent given no format instruction
  records in prose. Caught live at 36 runs: an L2 session recorded the unidecode decline with the
  deciding policy line quoted verbatim, and the table scored that cell 0/3. The error was not
  neutral - the arms most likely to write unstructured prose are the low-scaffolding ones, so the
  measurement was biased by exactly the treatment being dosed, and it manufactured an L2 dip
  (0.44 against L1's 0.78) that does not exist. Format-blind, both sit at 0.73.
- A: Adding a prose-decision regex to the scored pipeline was declined - swapping one heuristic for
  a more elaborate heuristic keeps the judgement in the collector, where it does not belong.
  Whether an entry captures the SEEDED decision stays a blind-judge question.
- F: `experiments/agent-capture/collect.py`.
- T: Recount across all 60 runs on three definitions (any entry / structured / prose-only) before
  the change was accepted; L0 is 0 under every one of them.

#### D2 - Keep the thin transcript as a uniform limitation rather than restart the matrix

- D: Let the batch finish on `--output-format json` and switch to `stream-json` only for later
  batches, recording degraded faithfulness judging as a limitation of this matrix.
- R: `claude -p --output-format json` returns the final message and usage, not the tool-call
  stream, so there is no reasoning trace to check a recorded `R:` against. Discovered at run 24,
  when guard detection came back empty and the cause turned out to be the transcript, not the
  guard. Restarting would have discarded ~$40 of completed runs to fix one of four metrics, and
  editing `run.py` mid-batch would have split the matrix into two incomparable halves - a known
  uniform limitation is worth more than a silently heterogeneous instrument. Capture rate, latency,
  noise and no-diff capture are unaffected; they read the store and the preserved fixture git diff.
- A: Discarding and re-running was declined on cost; a mid-batch edit was declined on comparability.
- F: `experiments/agent-capture/run.py` (unchanged during the batch, deliberately).
- T: The same thinness independently limits the Codex arm, so the constraint is symmetric rather
  than favouring either.

#### D3 - Pin the Claude arm to the fixture's own MCP config

- D: Claude runs pass `--mcp-config <run dir>/.mcp.json --strict-mcp-config`.
- R: The first probe measured 57 tools, of which the operator's user-level `semble` server
  contributed 2, with an unauthorised claude.ai connector offering more. A fixture session was
  therefore inheriting account-level tooling that has nothing to do with the treatment. Re-probed
  at L3: 52 tools, exactly 18 MCP, all from memory-seed. This is the Claude analogue of the Codex
  `features.apps=false` trim, and both are now recorded harness constants.
- A: Leaving it uncontrolled was declined - it is the same confound that cost a dozen probes to
  eliminate on the Codex side.
- F: `experiments/agent-capture/run.py`, `experiments/agent-capture/PREREGISTRATION.md`.
- T: Probe A confirmed the store resolves inside the fixture (stock 8-slug seed vocabulary, not the
  parent's 69); probe B confirmed the fixture's own `prepare-commit-msg` stamped `Memory-Entry` on
  fixture commit `35ec9e3`; all 60 runs reported `parent_isolated: true`.

## 2026-08-04 02:40 - Judge the agent-capture matrix and record E5

```yaml
entry_id: mse_ptcadyekn3j2ga9r
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/agent-capture-judging
topics:
  - testing
  - mcp-tools
related_entries:
  - mse_gvq7xhvgekts00at
```

### Summary

- Judged all 60 agent-capture runs blind with Codex and recorded the result as field evidence E5.
  Judged capture: L0 0.00, L1 0.74, L2 0.79, L3 0.94. Against the pre-registered threshold only L3
  is reliable, which is the outcome the pre-registration defined in advance as the
  mechanical-cheapness claim failing.

### Decisions

#### D1 - Report the pre-registered verdict against the claim, and mark it directional

- D: Record that only L3 clears the >=0.8 threshold and that the cheapness claim therefore fails,
  while stating in the same breath that L2's failure rests on a single decision (15/19 = 0.789; one
  more capture gives 0.84 and flips the verdict).
- R: The threshold and the fork were both fixed before any scored run precisely so the answer would
  not be chosen after seeing the data, and reporting only the softening caveat would be choosing it
  after the fact. But a binary verdict decided by one unit at n=19 is a coin near the line, and
  presenting it as a clean refutation would overstate it in the other direction. Both halves are
  needed: the pre-registered call went against the cheap story, and the margin is too thin to
  settle it. L0 = 0/18 with fifteen empty stores is the number that carries no such caveat.
- A: Raising N at L2 to break the tie was declined for now - deciding to gather more data *because*
  the result landed on the wrong side of a pre-registered line is exactly the practice
  pre-registration exists to prevent. If it is re-run, it should be re-run for every arm.
- F: `business/research/field-evidence-log.md` (E5).
- T: 60/60 runs judged with no judge failures; 203 decisions found, split 128 recorded / 75 not.

#### D2 - Retract the faithfulness and noise metrics as unmeasured

- D: Report faithfulness and noise rate as not measured rather than as 100% and 0%.
- R: The judge called 124 recorded decisions faithful and none unfaithful, and returned zero noise
  in all 60 runs. Zero variance across 128 judgements is what a null instrument looks like, not a
  perfect product. The cause is known: `claude -p --output-format json` returns the final message
  and usage but no tool-call or reasoning stream, so there was no record of what actually drove a
  decision for a stated reason to be checked against. Quoting 100% faithfulness would be the single
  most flattering and least supported number in the whole run.
- A: Inferring faithfulness from the final message alone was declined - the final message is
  written by the same agent that wrote the entry, so agreement between them is self-confirmation.
- F: `business/research/field-evidence-log.md` (E5), `experiments/agent-capture/PREREGISTRATION.md`.
- T: Verdict distribution checked directly: stage 1 `yes` 124 / `not_recorded` 75 / `unclear` 4,
  with no `no` verdicts anywhere; noise 0 in 60/60 runs.

#### D3 - Treat the harness as failing silently toward success

- D: Record both mid-run measurement bugs in E5 as a standing warning about this instrument.
- R: Each produced a complete, internally consistent, plausible table that was wrong. The
  DRAFT-heading counter scored prose captures as silence and invented an L2 dip; argv-truncated
  judge prompts left 56 of 60 blind judgements answering a fragment while the keyed stage, obliged
  by its schema to emit one verdict per seeded key, filled in all of them regardless. Neither threw
  an error and both were caught only by distrusting numbers that looked too clean - the first
  because a level was oddly low, the second because faithfulness was implausibly perfect. A harness
  whose failure mode is a convincing result needs that property written down next to its output.
- A: Fixing the bugs and reporting only the corrected numbers was declined; the failure mode is
  itself the more durable finding, and future readings of this instrument depend on knowing it.
- F: `business/research/field-evidence-log.md` (E5), `experiments/agent-capture/judge.py`,
  `experiments/agent-capture/collect.py`.
- T: Stage-2 fabrication is now structurally prevented (stage 2 is skipped when stage 1 is empty);
  prompts go over stdin; capture counting is format-blind. Re-judged 60/60 clean after the fixes.

## 2026-08-04 02:44 - Verify and qualify the L0 capture claim

```yaml
entry_id: mse_66m36tetrw658bym
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/l0-verification-entry
topics:
  - testing
  - mcp-tools
evolves:
  - d2 -> mse_ptcadyekn3j2ga9r:d1
```

### Summary

- Verified the E5 headline empirically rather than by inference, and qualified it: an L0 session run
  under the identical scored-run configuration does see all 18 memory-seed tools and resolves
  `memory_topics_list` to the fixture's own vocabulary.

### Decisions

#### D1 - Verify L0 tool availability before letting the headline stand

- D: Ran one probe under the exact scored-run constants to confirm the memory-seed MCP server was
  actually loaded at L0, and recorded the result in E5.
- R: `--mcp-config` plus `--strict-mcp-config` were added to the Claude arm AFTER the probes that
  confirmed MCP reachability, so no scored L0 run had ever been shown to have the write path. If
  the flags had excluded the fixture's own server, "had the tool and did not use it" would silently
  have been "had no tool" - and that is the number E5 calls least equivocal. The transcript could
  not settle it: `--output-format json` records no server or tool list.
- A: Inferring availability from the L1-L3 arms was declined; they differ from L0 in exactly the
  files that could affect discovery. Inferring it from probe A was declined too - probe A predates
  the flags.
- F: `business/research/field-evidence-log.md` (E5).
- T: Probe returned YES for `memory_session_append`, enumerated all 18 `memory-seed` tools, and
  `memory_topics_list` returned `deliverable, research, operations` - the fixture's stock seed
  vocabulary, not the parent's 69 topics.

#### D2 - Qualify L0 as a visible tool NAME, not a visible tool

- D: Record in E5 that MCP tools are deferred in this harness, so L0 measures name-visibility and
  is a weaker stimulus than the pre-registration's limitation 2 assumed.
- R: The probe volunteered it - it had to call `ToolSearch` to load a schema before it could invoke
  anything, so an L0 agent sees names without descriptions. The pre-registration argued L0 is not
  true zero-scaffolding because "the tool's description is itself a weak prompt"; that reasoning
  does not hold when the description is not present until fetched. The finding survives and is
  arguably cleaner, but the claim it supports is narrower than the one written down in advance, and
  narrowing it after the fact is only honest if it is stated.
- A: Leaving the pre-registered wording unqualified was declined - it would claim a stronger
  stimulus than the agents actually received.
- F: `business/research/field-evidence-log.md` (E5).
- T: Probe transcript states the deferral explicitly and names `ToolSearch` as the required step.

#### D3 - Mark the worktree-guard columns unmeasured rather than let them read as observed

- D: `guard_called` / `guard_blocked` are annotated as unreliable in `collect.py` and in E5, and the
  pre-registered guard confound is declared still open.
- R: They match on transcript text and the Claude transcript is the final message only, so they
  read `false` almost everywhere whatever happened. Left unannotated, a future reader would take
  "the guard never fired" as an observation - which would quietly close a confound that was
  registered before the first scored run and has never been tested.
- A: Deleting the columns was declined; they become correct as soon as the arm is re-run with
  `--output-format stream-json`, and a named unmeasured field is a better prompt to do that than a
  missing one.
- F: `experiments/agent-capture/collect.py`, `business/research/field-evidence-log.md` (E5).
- T: `links check` OK (154 files); `docs check` OK (191 files); dose-response table unchanged.

## 2026-08-04 11:33 - Replicate the capture matrix and measure faithfulness

```yaml
entry_id: mse_r32k8mk46ky8rdtv
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/agent-capture-v2-transcripts
topics:
  - testing
  - mcp-tools
evolves:
  - d1 -> mse_ptcadyekn3j2ga9r:d1
```

### Summary

- Ran a second full 60-session matrix to measure faithfulness, and it replicated E5's design exactly.
  Faithfulness came out 104 faithful / 17 unclear / 0 unfaithful across 121 recorded decisions - the
  first real measurement of it. The replication also reversed E5's threshold verdict, and pooling
  both runs shows every arm's confidence interval straddles the 0.80 line.

### Decisions

#### D1 - Report the threshold question as unanswerable at this sample size

- D: Record E6 with the pooled estimate and Wilson intervals, state that neither matrix's binary
  verdict stands, and explicitly decline to run the matrix again to settle it.
- R: v1 concluded the cheapness claim fails; v2, same fixtures and briefs and constants, concluded
  it survives. Nothing changed but the transcript format, which the agent never sees, so the
  disagreement is sampling. Pooled, L1 0.77 [0.61, 0.88], L2 0.83 [0.68, 0.92] and L3 0.91
  [0.77, 0.97] all straddle 0.80 - even L3. Separating 0.83 from 0.80 needs hundreds of seeded
  decisions per arm, an order of magnitude beyond the ~$200 both matrices cost, and buying that
  precision would not change any decision on the table. The pre-registered threshold was the wrong
  instrument for this question at a feasible price; the band is the honest output.
- A: Reporting v2 alone was declined - it is the newer run but not the better one, and quoting it
  would have meant announcing the opposite of E5 with no more evidence behind it. Averaging the two
  verdicts was declined as meaningless for a binary. Running a third matrix as a tie-break was
  declined: with intervals this wide a third run is as likely to disagree again.
- F: `business/research/field-evidence-log.md` (E6, and a pointer at the head of E5).
- T: Wilson intervals computed over pooled seeded decisions per arm; per-run and pooled figures both
  recorded so either can be re-derived.

#### D2 - Promote the L0-versus-everything gap as the finding that survived replication

- D: State the durable conclusion as "the write path alone is not used, and one line in AGENTS.md
  gets most of the available capture", rather than any ordering within L1-L3.
- R: L0 pooled 0.05 [0.01, 0.17] does not come near any other arm, and it is the only separation
  that survived a replication which overturned the rest. Everything from L1 up sits in a 0.77-0.91
  band this data cannot split, so claiming the full contract or the hooks beat a single line would
  be reading noise. This is a materially different claim from either matrix's headline and it is
  the one the pooled data actually supports.
- A: Keeping E5's "only the full install is reliable" framing was declined - the replication put
  L1 above threshold, so that framing depended on which run got quoted.
- F: `business/research/field-evidence-log.md` (E6).
- T: L0 = 2/38 pooled; the single L0 capture used full DRAFT structure with no rules file present,
  so L0 is stated as ~3-5% rather than as never.

#### D3 - Retire the worktree-guard confound as measured and absent

- D: Close the guard confound registered before the first scored run: `guard_called` was true in
  0 of 60 sessions with a reliable event stream.
- R: It was registered because only L2/L3 carry the contract telling an agent to consult the guard,
  so a block could have masqueraded as scaffolding suppressing capture. With real tool-call events
  it is now visible that agents do not call the guard during ordinary task work at any level, so the
  mechanism cannot be operating. It was carried as open only because the v1 transcript could not
  see tool calls at all.
- A: Leaving it open was declined now that it is genuinely measured rather than merely unobserved.
- F: `business/research/field-evidence-log.md` (E6), `experiments/agent-capture/collect.py`.
- T: `guard_signal_reliable` true for all 60 v2 runs; `guard_called` false in all 60.
