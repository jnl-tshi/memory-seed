---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-07
---

## 2026-08-07 00:17 - ADR campaign S1: 34 concerns founded, accepted, and wired into the maps

```yaml
entry_id: mse_7c5hj34hjwkdbhpw
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/experiment/adr-campaign
topics:
  - control-plane
  - schema
  - decision-harvest
evolves:
  - d1 -> mse_n35t6pkz73cp59vc:d2
related_entries:
  - d2 -> mse_czc4yc6h2x1fs8hs:d1
```

### Summary

- 34 concern-level ADRs founded from index.md and policy.md, all accepted under delegated
  ratification; the corpus is now 38 (3 pre-existing untouched, 36 accepted, 2 rejected).
- 35 carry a governing constitution binding; 24 session decisions attached as grounded evidence.
- Index and policy now point at ADRs: 21 index refs (6 bullets fully thinned to descriptive
  pointers), 8 policy rules gaining governing-ADR refs with their text kept inline.
- Full findings in `experiments/adr-campaign/FINDINGS.md`.

### Decisions

#### D1 - Found ADRs from control-file lines, not synthetic session entries

- D: Every campaign ADR is founded on the index/policy line it lifts, carrying that line's verbatim
  quote as grounding, with session decisions attached as supporting evidence where they exist.
  `source: derived` throughout. Two decided-and-rejected concerns were written as `rejected`-status
  ADRs rather than omitted.
- R: The alternative I had planned - authoring a synthetic "founding" session decision per lifted
  bullet - manufactures provenance for a decision that was actually made elsewhere, and would not
  work for S3 at all, since bootstrap runs on a project with no session corpus. Founding on the
  control-file line records what is true: the decision's current home is that line, and the ADR now
  supersedes it as the place the reasoning lives. Recording the two rejections matters because an
  ADR corpus holding only adopted decisions loses precisely the reasoning most likely to be
  re-litigated.
- A: Synthetic founding entries (rejected - false provenance). Skipping concerns with no locatable
  session decision (rejected - would have dropped roughly two thirds of the architecture).
- F: `experiments/adr-campaign/{concerns.json,write_proposed.py,FINDINGS.md}`,
  `.memory-seed/decisions/*.md` (35 new).
- T: `adr check` green after every batch; the three sha256-pinned ADRs byte-unchanged against
  `real-current.json`; 1086 tests pass.

#### D2 - The swarm's two failure modes were brief defects, and the fix was a closed candidate list

- D: Brief v2 separates the topic vocabulary from the constitution-anchor vocabulary, caps quotes
  at 40-120 characters within a single line, and renders VERIFIED candidate decision refs into each
  worker payload so attachments are chosen from a closed list rather than recalled.
- R: Run 1 lost 12 of 34 ADRs and 15 of 17 attachments. Every drop traced to my brief: 6 workers
  put constitution slugs in `topics` because both vocabularies arrived in one payload with the same
  shape, and 6 quotes matched their opening clause exactly then drifted in the tail, which is what
  wrapping does to a long transcription. The attachment failures were invented or truncated entry
  ids - workers recalling refs instead of reading them, which no instruction fixes but a closed
  list removes. Run 2 recovered 12/12 ADRs and 20/20 attachments.
- A: Blaming the model tier and escalating alone (rejected - it would have shipped the same broken
  brief at higher cost).
- F: `experiments/adr-campaign/{worker-brief-v1.md,worker-brief-v2.md,validate_drafts.py}`.
- T: Run 1 22/34 ADRs and 2/17 attachments; run 2 12/12 and 20/20.

#### D3 - Thin the index only where a bullet is not bundling uncovered decisions

- D: Index bullets under 400 characters thin to a descriptive pointer (one-line digest plus ADR
  link); longer bullets keep their text and gain the ADR reference; policy rules always keep their
  text inline and gain only the governing-ADR pointer. Three bullets whose grounding quotes span
  line wraps were left entirely untouched.
- R: Policy is Always Read, so an agent must learn how to behave without chasing links - that is
  why its rules stay inline while the ADR owns the why. For the index, the harvest found that
  single bullets bundle two to four decisions, so thinning a long bullet to one concern's pointer
  would silently delete the decisions no ADR covers. A length threshold is a crude proxy for
  bundling, and it errs toward keeping text. The honest consequence is that the index is only about
  19% thinned; finishing it needs a judgement pass that splits bundled bullets.
- A: Thinning every located bullet (rejected - silent loss). Thinning nothing (rejected - leaves
  two authoritative homes for the same decision, the drift shape this cycle exists to close).
- F: `.memory-seed/index.md`, `.memory-seed/policy.md`.
- T: 6 thinned, 23 refs appended, 3 unresolved and reported; `links check`, `topics check`,
  `adr check`, `docs check` green.

### Facts

- Acceptance and ratification for this cycle were delegated to the agent by JNL's live instruction
  of 2026-08-06; every acceptance event carries that on its reason line, so the ledger reads
  agent-executed-under-owner-instruction rather than owner-reviewed.
- Run 2 changed model tier, brief, and candidate-ref list simultaneously - no haiku-vs-opus claim
  can be drawn from it. My read is the candidate list did most of the work.
- One worker died on a session limit; its 6 concerns were orchestrator-drafted through the
  identical validator (6/6 ADRs, 8/8 attachments).
- Every lifecycle-linked session write from here now pays the mandatory ADR review gate. That is
  the living-document design working, and it is permanent.

### Follow-ups

- Hand pass for the 3 unresolved bullets and the ~19%-thinned index: split bundled bullets so the
  remaining decisions can be pointed at rather than held.
- Re-run brief v2 on haiku if the model-tier question needs settling.

## 2026-08-07 00:43 - ADR lineage map: machine-suggested edges may not move an ADR head

```yaml
entry_id: mse_ex5216t2hn30s5wx
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/adr-lineage-reconcile
topics:
  - lifecycle-edges
  - schema
evolves:
  - d1 -> mse_7c5hj34hjwkdbhpw:d1
```

### Summary

- Built the decision-level lineage map JNL asked for: every ADR member walked forward through
  link-sidecar `evolves`/`replaces` edges to the decision that is current, with `related` edges
  collected as context. 313 edges indexed, 11 unusable after normalisation, 5 ADRs affected.
- Applying the map automatically produced a WRONG head and was reverted before publication. The
  corrected rule - only an authored edge may move a head - yields zero automatic changes and a
  1-item review queue.
- Also fixed a validator defect that would have stranded every founded ADR at its founding
  placeholder forever.

### Decisions

#### D1 - A real decision supersedes a founding head without proving descent

- D: `validate_adr` no longer requires a new head to descend from the current authoritative head
  when that head is a `founding:` pseudo-ref. Descent is still required between two real decisions,
  held by a test asserting an unlinked successor is refused.
- R: A founding ref is a placeholder for "this concern as recorded in the control file", not a node
  in the lineage graph, so no real decision can ever descend from it. The rule as written made the
  convergence the founding mechanism exists for structurally impossible, stranding all 34 campaign
  ADRs at their founding head permanently. Probed before designing: `revise` succeeded and
  `transition` failed with "does not descend from authoritative decision founding:...", locating
  the defect in the acceptance rule rather than the writers.
- A: Landing reconciliations as `proposed` and leaving acceptance broken (rejected - that hands
  over proposals nobody can accept). Making the founding ref a synthetic predecessor (rejected -
  `LINK_ASSERTION_RE` requires real decision refs at both ends, so it would mean loosening the edge
  grammar to patch a status rule).
- F: `memory_seed/adr.py`, `tests/test_adr_contract_extension.py`.
- T: Founding-head convergence accepted end-to-end; descent between two real decisions still
  refused; 31 ADR tests pass.

#### D2 - Only an authored edge may move an ADR head; machine-scored edges go to a review queue

- D: Head changes are applied only when every step of the chain is authored (no `edge_confidence`
  entry). Machine-scored edges are written to `LINEAGE-REVIEW.md` with BOTH decision titles, and
  `reconcile_lineage.py` applies nothing unless the ADR is named with `--approve`. Batch mode was
  removed.
- R: Measured, not assumed. The first pass applied the map wholesale under a confidence floor of
  0.7 and moved `adr_subproject_scoping` onto `ms-0bd3d8b2:d1` - "Use a seeded trigger registry
  instead of only prose triggers" - a decision about skill triggers, not sub-project scoping,
  reached through a single 0.75-scored machine edge. Reverted before publication. Raising the floor
  would not fix it: the same corpus offers 0.9-scored candidates with the same failure mode, and
  the link campaign's own premise is that the swarm suggests while a human decides. An ADR head is
  exactly the kind of decision that reserves. Showing both titles side by side is what exposed the
  error, so the queue leads with them.
- A: A confidence floor alone (rejected - measured to fail at 0.75 and unfixable by raising it).
  Auto-applying and letting review catch it later (rejected - the ledger is append-only, so a wrong
  accepted head is corrected only by more events).
- F: `experiments/adr-campaign/lineage_map.py`, `reconcile_lineage.py`, `lineage-map.json`,
  `LINEAGE-REVIEW.md`.
- T: 4 authored lineage edges touch ADR members and all 4 confirm heads the pinned ADRs already
  carry; 8 machine-scored edges touch members; 0 automatic changes, 1 ADR queued for review.

#### D3 - Stop on fan-out, not on hop count

- D: The walk follows a chain only while a node has exactly one qualifying successor, stopping at
  any branch; bare refs normalise to `<entry>:dN` only where the entry has exactly one decision.
- R: An unrestricted 6-hop walk from `adr_control_file_authority` reached the decision-identity
  mandate, which belongs to a different ADR. Hop limits cap drift without preventing it, whereas a
  branch is the actual signal that the concern split - and choosing a branch is judgement rather
  than mechanics.
- A: A fixed hop limit (rejected - drifts within the limit).
- F: `experiments/adr-campaign/lineage_map.py`.
- T: 313 edges indexed, 11 unusable, 0 ambiguous walks after the fan-out rule.

### Facts

- The map CONFIRMS all three sha256-pinned ADRs: their members' authored chains terminate at the
  heads they already carry. They are mapped and reported, never written.
- The lineage payoff is thin because attachment is thin - only 24 session decisions are attached
  across 34 campaign ADRs, and most are not edge targets. S2's write-time attachment is what will
  make this map dense; the consuming mechanism now exists.
- Composing inference layers bit exactly where `retrieval.py:2280` warns it would: link-swarm edges
  feeding an ADR head decision.

### Follow-ups

- JNL to review `experiments/adr-campaign/LINEAGE-REVIEW.md` and name any ADRs to re-anchor.

## 2026-08-07 01:10 - Topics find ADR decision candidates well, but cannot gate lineage hops

```yaml
entry_id: mse_gm39dmzzs4nbysm1
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/adr-topic-candidates
topics:
  - topic-vocabulary
  - lifecycle-edges
evolves:
  - d1 -> mse_ex5216t2hn30s5wx:d2
```

### Summary

- JNL proposed using decision-level topics to reduce cross-concern lineage drift. Tested against
  the 1 known-bad hop and 3 known-good authored hops: topic agreement fails as a hard gate at every
  granularity, but is the strongest available signal for FINDING attachment candidates.
- Shipped both readings: topic overlap now annotates `LINEAGE-REVIEW.md`, and a new
  `ATTACHMENT-REVIEW.md` ranks candidate decisions for all 20 ADRs that carry none.

### Decisions

#### D1 - Topic agreement annotates the lineage review; it does not gate it

- D: The lineage review shows each hop's shared and divergent topics, flagging `NO OVERLAP - check
  concern drift`. No hop is blocked or admitted on topic evidence.
- R: Measured against the one hop known to be wrong and the three authored hops known to be right.
  Leaf-slug matching blocks the bad hop but also rejects 2 of 3 good ones; area-axis matching
  rejects all 3; root-area matching readmits the bad hop entirely, because `agent-rules` and
  `skill-architecture` both sit under `control-plane`. The cause is structural rather than a tuning
  problem: the activity axis is SUPPOSED to change along a chain - a `bugfix` evolving into a
  `feature-build` is a normal lifecycle - and sibling areas like `session-logging`/`session-fuse`
  are genuinely adjacent, so leaf matching rejects real evolutions while root matching cannot see
  concern boundaries at all. n=5, so this is a pattern rather than a measurement, which is itself
  a reason to annotate rather than gate.
- A: Adopting leaf matching as a gate (rejected - would have blocked 2 of 3 known-good authored
  hops, trading a false positive for two false negatives). Root matching (rejected - fails to stop
  the exact hop that motivated the idea).
- F: `experiments/adr-campaign/lineage_map.py`, `LINEAGE-REVIEW.md`.
- T: 4-case comparison table across leaf/area/root recorded in `topic_candidates.py`; annotation
  correctly marks the bad hop `NO OVERLAP`.

#### D2 - Topic matching is the right tool for finding attachment candidates

- D: `topic_candidates.py` ranks real session decisions by topics shared with each ADR and writes
  `ATTACHMENT-REVIEW.md`. Candidates only - attaching a decision is a `revise` event that moves the
  head, so nothing is applied without being named.
- R: Attachment is the actual bottleneck: 20 ADRs carry no decision at all, and the swarm asked to
  find them invented refs instead. Topic matching recovers the right ones - `adr_draft_format`
  surfaces "Use DRAFT for compact decision records" and `adr_entry_id_scheme` surfaces "80-bit
  generated entry IDs", which is the real decision behind the hallucinated `mse_a4282580`. This is
  recall-oriented work with a human reviewing, which is where topic matching is strong and where a
  false positive costs a glance rather than a wrong authority claim. All 20 unattached ADRs produced
  candidates.
- A: Auto-attaching high-overlap candidates (rejected - same stakes as a lineage move, and the
  same lesson applies).
- F: `experiments/adr-campaign/topic_candidates.py`, `ATTACHMENT-REVIEW.md`.
- T: 20 of 20 unattached ADRs produced ranked candidates.

### Facts

- Topics are read with sidecar precedence, never a union with authored entry topics - JNL corrected
  this mid-pass, and it matters: the first run compared authored topics on one end against sidecar
  topics on the other, which is not a comparison. This is `adr_topic_authority` applied to its own
  consumer.
- Decision-level topic coverage is 1051 of 1292 decisions.

## 2026-08-07 11:43 - Soft ADR context; five attachments; topic hierarchy cannot gate lineage

```yaml
entry_id: mse_8p47vz0pgabqm1va
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/adr-soft-context
topics:
  - lifecycle-edges
  - schema
evolves:
  - d1 -> mse_gm39dmzzs4nbysm1:d1
```

### Summary

- New `context-added` ledger event: attach decisions to a concern as SOFT evidence, with no head
  move and no status change. It is the hard/soft separation JNL asked for, made expressible.
- Five screened attachments applied on JNL's approval: 2 as heads, 3 as soft context.
- Tested JNL's hierarchy-aware chaining proposal against the 5 known hops: every hierarchy level
  admits the known-bad hop, because its endpoints are topical SIBLINGS while the known-good hops
  cross topic areas entirely.

### Decisions

#### D1 - `context-added`: soft attachment that moves nothing

- D: A new event kind carrying `supporting_decisions` plus a Reason, with no `decision_ref` and no
  predecessors, validated to refuse both. It leaves head and status untouched, and its refs stay
  outside `adr_membership`, so they never arm the MCP review gate.
- R: Attachment previously required a `revise` event, which sets a head - so "add context" was
  inexpressible, and two of the five approved attachments could not be written truthfully without
  it. `adr_archive_before_replace`'s decision is an INSTANCE of the policy rather than the policy
  itself, and `adr_community_detection_rejected` / `adr_topic_backfill_rejected` carry status
  `rejected`, where accepting a head would have read as "this revision was accepted" instead of
  "we decided against this". The event also makes the cost model legible: hard attachment
  (decision_ref + predecessors) is what the review gate charges for, soft attachment is free.
- A: Making all five heads (rejected - overclaims, and would silently flip two statuses).
  Attaching only the three that fit the head shape (rejected - drops real evidence rather than
  fixing a schema gap that was already identified).
- F: `memory_seed/adr.py` (`EVENT_TYPES`, `EVENT_RE`, validation branch, `add_context`),
  `tests/test_adr_contract_extension.py`.
- T: Head, status and membership all unchanged after a context write; a `rejected` ADR stays
  rejected; empty evidence, missing reason and unresolvable refs each refused. 1091 tests pass.

#### D2 - Topic hierarchy cannot gate lineage hops, at any level

- D: Chain construction stays gated on authorship (authored edges act, machine-scored edges go to
  review). Topic overlap remains an annotation. Hierarchy-aware variants were measured and not
  adopted.
- R: JNL proposed comparing at parent or grandparent level so adjacent siblings resolve together.
  Measured against the 1 known-bad hop and 3 known-good authored hops, every hierarchy rule -
  same-parent, any-shared-ancestor, same-root - ADMITS the bad hop and blocks 2 or 3 of the good
  ones. The cause is the opposite of what the proposal assumes: the bad hop's endpoints
  (`agent-rules`, `skill-architecture`) are SIBLINGS under `control-plane`, making it the most
  topically coherent hop in the sample, while the good hops genuinely cross areas
  (`session-logging` -> `session-fuse`) or change activity (`bugfix` -> `feature-build`) because
  that is what a real evolution does. Topic proximity measures subject matter; concern identity
  measures which question a decision answers, and two decisions can be maximal topic-neighbours
  while belonging to different concerns. Authorship separates all 4 cases correctly and is already
  the implemented rule.
- A: Adopting same-parent matching (rejected - admits the bad hop, keeps only 10 of 239 edges).
  Adopting any-shared-ancestor (rejected - admits the bad hop for 5 more edges than leaf matching).
- F: measurement only; no code change.
- T: leaf 34 edges/18 components; siblings 10/5; any-ancestor 39/19; same-root 39/19; unfiltered
  239/55 with a 60-node component. Best rule keeps 16% of edges, so segmentation stays a reading
  lens rather than a partition.

### Facts

- Also applied earlier in the same review, on JNL's "reject the first, take the second":
  `adr_merge_branch_primitive` converged onto its own decision (mse_dr5eprnhrctqeeg3:d1) with the
  related integration decision attached; the machine-suggested `adr_subproject_scoping` head move
  was rejected.
- Five attachments applied: heads on `adr_entry_id_scheme` (ms-a4282580:d2) and
  `adr_topic_vocabulary` (mse_25zzy3cmdjgrsf69:d1); soft context on `adr_archive_before_replace`,
  `adr_topic_backfill_rejected` and `adr_community_detection_rejected`.
- Screening validation dropped 7 of 10 worker picks: two quoted the decision's TITLE rather than
  its body, two quoted text that does not exist, one was 27 characters, one cited a ref never
  offered. Re-reading them by hand recovered 4 good picks - the selections were often right when
  the proof was not.
- The topic candidate finder has high recall and modest precision: candidates for all 20 unattached
  ADRs, of which about 5 were worth attaching.

### Follow-ups

- ADR topic granularity is the untested half of JNL's proposal: if ADRs declared narrower topics,
  the attachment candidate finder should get more precise. Worth measuring before the next pass.

## 2026-08-07 11:56 - Link sidecars: a re-visited file is legitimately out of order, so the writer sorts instead of refusing

```yaml
entry_id: mse_9aggjnyaqrmb545d
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/session-links-sidecar-e41dab
```

### Summary

- JNL proposed that link sidecars be filed under the originating decision's month folder and date
  filename, append-only across passes. That is already the as-is design, enforced at five points
  (spec, `apply_link_gap_stubs`, `links check`'s `link-sidecar-date-mismatch` at error severity,
  the fuse's routing and its `link_date` check, plus a test). No delta to close.
- The comparison did surface a live defect the placement rule and the identity rule create together:
  four dates were permanently closed to further scaffolding. Fixed, and the four files repaired.

### Decisions

#### D1 - A non-chronological link sidecar is sorted on write, not refused

- D: `apply_link_gap_stubs` no longer raises `Existing link sidecar blocks are not chronological`.
  It re-sorts the whole block region on write with the same stable sort
  `_write_chronological_link_sidecar_file` already used for the fuse - existing blocks keep their
  relative order within a minute, new blocks land after them, block content is never touched. The
  four corpus files stuck in that state (`2026-07-03`, `-05`, `-12`, `-17`) were repaired as a pure
  permutation, verified by comparing the sorted line multiset before and after.
- R: Two sanctioned rules produce the offending shape between them. A sidecar is filed under its
  SOURCE entry's session date, and a later enrichment pass stamps its blocks with the AUTHORING wall
  clock because block identity is `(entry_id, heading timestamp)`. So a re-visited file is
  legitimately out of order, and refusing it closed that date to every future mechanical pass.
  Measured, not inferred: an audit+apply against a scratchpad copy raised on `2026-07-12` and
  `2026-07-17` while succeeding on `2026-08-04`, and 5-14 unblocked entries remained on each of the
  four dates, so the guard was reachable rather than shadowed by the `pending`-empty early return.
- A: Stamp the source entry's own timestamp in the heading instead, so files sort by construction.
  Rejected, and it was my first proposal until the reasoning was checked: pass two's block for one
  entry would then key identically to pass one's, reintroducing the exact one-block-per-entry wall
  the 2026-07-23 identity fix removed. The wall-clock stamp is load-bearing for multi-pass
  enrichment; the refusal was the part that had to go.
- A: Reordering as a content edit under append-only. It is not - the permutation is byte-exact per
  block, and the fuse compares immutability per `(entry_id, heading timestamp)` on record text, so a
  re-sorted file does not read as modified.
- F: `memory_seed/retrieval.py`, `tests/test_link_audit.py`, `CHANGELOG.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-03.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-05.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-12.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-17.md`
- T: `test_apply_sorts_a_wall_clock_stamped_sidecar_instead_of_refusing` reproduces the shape - two
  wall-clock blocks appended newest-first under an origin-date `link_date` - and asserts the apply
  succeeds, the output is ordered, both published blocks survive verbatim, and `links check` passes.
  Full suite 1089 passed; all four dates now accept a stub apply in a probe copy.

#### D2 - The swarm skill said "the day's link sidecar", which reads as today's

- D: `link_swarm.md` step 5 (live and seed) now says the sidecar for the SOURCE entry's own session
  date, states that a campaign spanning many dates writes to many files, names the failure mode
  (`link-sidecar-date-mismatch`), and specifies the AUTHORING WALL CLOCK for the block heading with
  the identity reason attached. The sidecar spec gains a bullet recording that a wall-clock heading
  inside an origin-date file is deliberate and that any writer meeting one must sort, never refuse.
- R: The line was wrong twice over and in opposite directions - "the day's" would have filed every
  block of a campaign under the day it ran, failing `links check` on every one, while
  `<source entry timestamp>` for the heading is the identity collision D1 rejects. The 2026-07-25
  campaign did the right thing on both counts, but that was judgment rather than instruction, and
  the next campaign had no reason to repeat it.
- F: `.memory-seed/skills/link_swarm.md`, `memory_seed/seed/.memory-seed/skills/link_swarm.md`,
  `docs/3_Spec/lifecycle-edge-linking-sidecars.md`

## 2026-08-07 12:17 - Fix the ADR fuse: reconciliation replayed on decision_ref, skipping founding revisions

```yaml
entry_id: mse_0mp31v67vc3fqns6
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/adr-reconcile-founding
topics:
  - session-fuse
  - schema
evolves:
  - d1 -> mse_8p47vz0pgabqm1va:d1
```

### Summary

- `session merge-branch` refused every branch that appended events to a founded ADR. Cause found
  and fixed: `reconcile_adr_records` keyed its replay on `decision_ref`, which a founding revision
  does not have.
- Regression tests cover both the append case and idempotent self-reconciliation.

### Decision

#### D1 - Reconciliation keys on revision_key, and honours the founding carve-out

- D: `reconcile_adr_records` replays on `event.revision_key` rather than `event.decision_ref`, and
  skips the descent check when the current head is a `founding:` placeholder - the same two rules
  `replay_adr` and `validate_adr` already apply.
- R: A founding revision carries no `decision_ref`, so the old key skipped it entirely: it never
  entered `states`, its acceptance then read as "transition targets non-pending revision", and
  every later acceptance read as "competing acceptance ... replay has no head". Both messages
  appeared verbatim when merging the soft-context branch, while `adr check` was green on the branch
  AND on the merge result - the signature of a guard failing rather than a corpus failing. With 34
  of 38 ADRs founded, this made the fuse refuse essentially every future ADR-touching branch, so it
  is a standing tax on the pipeline rather than a one-off.
- A: Continuing to bypass with documented plain merges (rejected - it normalises stepping around
  the append-only guard, which is the habit the guard exists to prevent).
- F: `memory_seed/adr.py` (`reconcile_adr_records`), `tests/test_adr_contract_extension.py`.
- T: A founded ADR with an appended revision+acceptance now reconciles cleanly and replays to the
  new head; self-reconciliation is idempotent; 36 ADR tests pass.

### Facts

- This was the THIRD copy of the replay rules in `adr.py` - `replay_adr`, `validate_adr` and
  `reconcile_adr_records` each implement them. The founding-source extension updated the first two
  and missed the third, which is exactly the failure mode duplicated logic produces. Worth
  collapsing to one implementation before the next schema change.
- The preceding merge of `claude/feature/adr-soft-context` was landed by plain merge under this
  bug; its merge message records the bypass and what was verified instead.
