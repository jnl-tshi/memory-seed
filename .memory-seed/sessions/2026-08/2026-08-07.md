---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-07
---

## 2026-08-07 00:17 - ADR campaign S1: 34 concerns founded, accepted, and wired into the maps

```yaml
entry_id: mse_7c5hj34hjwkdbhpw
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/experiment/adr-campaign
topics:
  - control-plane
  - schema
  - decision-harvest
evolves:
  - d1 -> mse_n35t6pkz73cp59vc:d2
related_entries:
  - d2 -> mse_czc4yc6h2x1fs8hs:d1
```

### Summary

- 34 concern-level ADRs founded from index.md and policy.md, all accepted under delegated
  ratification; the corpus is now 38 (3 pre-existing untouched, 36 accepted, 2 rejected).
- 35 carry a governing constitution binding; 24 session decisions attached as grounded evidence.
- Index and policy now point at ADRs: 21 index refs (6 bullets fully thinned to descriptive
  pointers), 8 policy rules gaining governing-ADR refs with their text kept inline.
- Full findings in `experiments/adr-campaign/FINDINGS.md`.

### Decisions

#### D1 - Found ADRs from control-file lines, not synthetic session entries

- D: Every campaign ADR is founded on the index/policy line it lifts, carrying that line's verbatim
  quote as grounding, with session decisions attached as supporting evidence where they exist.
  `source: derived` throughout. Two decided-and-rejected concerns were written as `rejected`-status
  ADRs rather than omitted.
- R: The alternative I had planned - authoring a synthetic "founding" session decision per lifted
  bullet - manufactures provenance for a decision that was actually made elsewhere, and would not
  work for S3 at all, since bootstrap runs on a project with no session corpus. Founding on the
  control-file line records what is true: the decision's current home is that line, and the ADR now
  supersedes it as the place the reasoning lives. Recording the two rejections matters because an
  ADR corpus holding only adopted decisions loses precisely the reasoning most likely to be
  re-litigated.
- A: Synthetic founding entries (rejected - false provenance). Skipping concerns with no locatable
  session decision (rejected - would have dropped roughly two thirds of the architecture).
- F: `experiments/adr-campaign/{concerns.json,write_proposed.py,FINDINGS.md}`,
  `.memory-seed/decisions/*.md` (35 new).
- T: `adr check` green after every batch; the three sha256-pinned ADRs byte-unchanged against
  `real-current.json`; 1086 tests pass.

#### D2 - The swarm's two failure modes were brief defects, and the fix was a closed candidate list

- D: Brief v2 separates the topic vocabulary from the constitution-anchor vocabulary, caps quotes
  at 40-120 characters within a single line, and renders VERIFIED candidate decision refs into each
  worker payload so attachments are chosen from a closed list rather than recalled.
- R: Run 1 lost 12 of 34 ADRs and 15 of 17 attachments. Every drop traced to my brief: 6 workers
  put constitution slugs in `topics` because both vocabularies arrived in one payload with the same
  shape, and 6 quotes matched their opening clause exactly then drifted in the tail, which is what
  wrapping does to a long transcription. The attachment failures were invented or truncated entry
  ids - workers recalling refs instead of reading them, which no instruction fixes but a closed
  list removes. Run 2 recovered 12/12 ADRs and 20/20 attachments.
- A: Blaming the model tier and escalating alone (rejected - it would have shipped the same broken
  brief at higher cost).
- F: `experiments/adr-campaign/{worker-brief-v1.md,worker-brief-v2.md,validate_drafts.py}`.
- T: Run 1 22/34 ADRs and 2/17 attachments; run 2 12/12 and 20/20.

#### D3 - Thin the index only where a bullet is not bundling uncovered decisions

- D: Index bullets under 400 characters thin to a descriptive pointer (one-line digest plus ADR
  link); longer bullets keep their text and gain the ADR reference; policy rules always keep their
  text inline and gain only the governing-ADR pointer. Three bullets whose grounding quotes span
  line wraps were left entirely untouched.
- R: Policy is Always Read, so an agent must learn how to behave without chasing links - that is
  why its rules stay inline while the ADR owns the why. For the index, the harvest found that
  single bullets bundle two to four decisions, so thinning a long bullet to one concern's pointer
  would silently delete the decisions no ADR covers. A length threshold is a crude proxy for
  bundling, and it errs toward keeping text. The honest consequence is that the index is only about
  19% thinned; finishing it needs a judgement pass that splits bundled bullets.
- A: Thinning every located bullet (rejected - silent loss). Thinning nothing (rejected - leaves
  two authoritative homes for the same decision, the drift shape this cycle exists to close).
- F: `.memory-seed/index.md`, `.memory-seed/policy.md`.
- T: 6 thinned, 23 refs appended, 3 unresolved and reported; `links check`, `topics check`,
  `adr check`, `docs check` green.

### Facts

- Acceptance and ratification for this cycle were delegated to the agent by JNL's live instruction
  of 2026-08-06; every acceptance event carries that on its reason line, so the ledger reads
  agent-executed-under-owner-instruction rather than owner-reviewed.
- Run 2 changed model tier, brief, and candidate-ref list simultaneously - no haiku-vs-opus claim
  can be drawn from it. My read is the candidate list did most of the work.
- One worker died on a session limit; its 6 concerns were orchestrator-drafted through the
  identical validator (6/6 ADRs, 8/8 attachments).
- Every lifecycle-linked session write from here now pays the mandatory ADR review gate. That is
  the living-document design working, and it is permanent.

### Follow-ups

- Hand pass for the 3 unresolved bullets and the ~19%-thinned index: split bundled bullets so the
  remaining decisions can be pointed at rather than held.
- Re-run brief v2 on haiku if the model-tier question needs settling.

## 2026-08-07 00:43 - ADR lineage map: machine-suggested edges may not move an ADR head

```yaml
entry_id: mse_ex5216t2hn30s5wx
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/adr-lineage-reconcile
topics:
  - lifecycle-edges
  - schema
evolves:
  - d1 -> mse_7c5hj34hjwkdbhpw:d1
```

### Summary

- Built the decision-level lineage map JNL asked for: every ADR member walked forward through
  link-sidecar `evolves`/`replaces` edges to the decision that is current, with `related` edges
  collected as context. 313 edges indexed, 11 unusable after normalisation, 5 ADRs affected.
- Applying the map automatically produced a WRONG head and was reverted before publication. The
  corrected rule - only an authored edge may move a head - yields zero automatic changes and a
  1-item review queue.
- Also fixed a validator defect that would have stranded every founded ADR at its founding
  placeholder forever.

### Decisions

#### D1 - A real decision supersedes a founding head without proving descent

- D: `validate_adr` no longer requires a new head to descend from the current authoritative head
  when that head is a `founding:` pseudo-ref. Descent is still required between two real decisions,
  held by a test asserting an unlinked successor is refused.
- R: A founding ref is a placeholder for "this concern as recorded in the control file", not a node
  in the lineage graph, so no real decision can ever descend from it. The rule as written made the
  convergence the founding mechanism exists for structurally impossible, stranding all 34 campaign
  ADRs at their founding head permanently. Probed before designing: `revise` succeeded and
  `transition` failed with "does not descend from authoritative decision founding:...", locating
  the defect in the acceptance rule rather than the writers.
- A: Landing reconciliations as `proposed` and leaving acceptance broken (rejected - that hands
  over proposals nobody can accept). Making the founding ref a synthetic predecessor (rejected -
  `LINK_ASSERTION_RE` requires real decision refs at both ends, so it would mean loosening the edge
  grammar to patch a status rule).
- F: `memory_seed/adr.py`, `tests/test_adr_contract_extension.py`.
- T: Founding-head convergence accepted end-to-end; descent between two real decisions still
  refused; 31 ADR tests pass.

#### D2 - Only an authored edge may move an ADR head; machine-scored edges go to a review queue

- D: Head changes are applied only when every step of the chain is authored (no `edge_confidence`
  entry). Machine-scored edges are written to `LINEAGE-REVIEW.md` with BOTH decision titles, and
  `reconcile_lineage.py` applies nothing unless the ADR is named with `--approve`. Batch mode was
  removed.
- R: Measured, not assumed. The first pass applied the map wholesale under a confidence floor of
  0.7 and moved `adr_subproject_scoping` onto `ms-0bd3d8b2:d1` - "Use a seeded trigger registry
  instead of only prose triggers" - a decision about skill triggers, not sub-project scoping,
  reached through a single 0.75-scored machine edge. Reverted before publication. Raising the floor
  would not fix it: the same corpus offers 0.9-scored candidates with the same failure mode, and
  the link campaign's own premise is that the swarm suggests while a human decides. An ADR head is
  exactly the kind of decision that reserves. Showing both titles side by side is what exposed the
  error, so the queue leads with them.
- A: A confidence floor alone (rejected - measured to fail at 0.75 and unfixable by raising it).
  Auto-applying and letting review catch it later (rejected - the ledger is append-only, so a wrong
  accepted head is corrected only by more events).
- F: `experiments/adr-campaign/lineage_map.py`, `reconcile_lineage.py`, `lineage-map.json`,
  `LINEAGE-REVIEW.md`.
- T: 4 authored lineage edges touch ADR members and all 4 confirm heads the pinned ADRs already
  carry; 8 machine-scored edges touch members; 0 automatic changes, 1 ADR queued for review.

#### D3 - Stop on fan-out, not on hop count

- D: The walk follows a chain only while a node has exactly one qualifying successor, stopping at
  any branch; bare refs normalise to `<entry>:dN` only where the entry has exactly one decision.
- R: An unrestricted 6-hop walk from `adr_control_file_authority` reached the decision-identity
  mandate, which belongs to a different ADR. Hop limits cap drift without preventing it, whereas a
  branch is the actual signal that the concern split - and choosing a branch is judgement rather
  than mechanics.
- A: A fixed hop limit (rejected - drifts within the limit).
- F: `experiments/adr-campaign/lineage_map.py`.
- T: 313 edges indexed, 11 unusable, 0 ambiguous walks after the fan-out rule.

### Facts

- The map CONFIRMS all three sha256-pinned ADRs: their members' authored chains terminate at the
  heads they already carry. They are mapped and reported, never written.
- The lineage payoff is thin because attachment is thin - only 24 session decisions are attached
  across 34 campaign ADRs, and most are not edge targets. S2's write-time attachment is what will
  make this map dense; the consuming mechanism now exists.
- Composing inference layers bit exactly where `retrieval.py:2280` warns it would: link-swarm edges
  feeding an ADR head decision.

### Follow-ups

- JNL to review `experiments/adr-campaign/LINEAGE-REVIEW.md` and name any ADRs to re-anchor.

## 2026-08-07 01:10 - Topics find ADR decision candidates well, but cannot gate lineage hops

```yaml
entry_id: mse_gm39dmzzs4nbysm1
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/adr-topic-candidates
topics:
  - topic-vocabulary
  - lifecycle-edges
evolves:
  - d1 -> mse_ex5216t2hn30s5wx:d2
```

### Summary

- JNL proposed using decision-level topics to reduce cross-concern lineage drift. Tested against
  the 1 known-bad hop and 3 known-good authored hops: topic agreement fails as a hard gate at every
  granularity, but is the strongest available signal for FINDING attachment candidates.
- Shipped both readings: topic overlap now annotates `LINEAGE-REVIEW.md`, and a new
  `ATTACHMENT-REVIEW.md` ranks candidate decisions for all 20 ADRs that carry none.

### Decisions

#### D1 - Topic agreement annotates the lineage review; it does not gate it

- D: The lineage review shows each hop's shared and divergent topics, flagging `NO OVERLAP - check
  concern drift`. No hop is blocked or admitted on topic evidence.
- R: Measured against the one hop known to be wrong and the three authored hops known to be right.
  Leaf-slug matching blocks the bad hop but also rejects 2 of 3 good ones; area-axis matching
  rejects all 3; root-area matching readmits the bad hop entirely, because `agent-rules` and
  `skill-architecture` both sit under `control-plane`. The cause is structural rather than a tuning
  problem: the activity axis is SUPPOSED to change along a chain - a `bugfix` evolving into a
  `feature-build` is a normal lifecycle - and sibling areas like `session-logging`/`session-fuse`
  are genuinely adjacent, so leaf matching rejects real evolutions while root matching cannot see
  concern boundaries at all. n=5, so this is a pattern rather than a measurement, which is itself
  a reason to annotate rather than gate.
- A: Adopting leaf matching as a gate (rejected - would have blocked 2 of 3 known-good authored
  hops, trading a false positive for two false negatives). Root matching (rejected - fails to stop
  the exact hop that motivated the idea).
- F: `experiments/adr-campaign/lineage_map.py`, `LINEAGE-REVIEW.md`.
- T: 4-case comparison table across leaf/area/root recorded in `topic_candidates.py`; annotation
  correctly marks the bad hop `NO OVERLAP`.

#### D2 - Topic matching is the right tool for finding attachment candidates

- D: `topic_candidates.py` ranks real session decisions by topics shared with each ADR and writes
  `ATTACHMENT-REVIEW.md`. Candidates only - attaching a decision is a `revise` event that moves the
  head, so nothing is applied without being named.
- R: Attachment is the actual bottleneck: 20 ADRs carry no decision at all, and the swarm asked to
  find them invented refs instead. Topic matching recovers the right ones - `adr_draft_format`
  surfaces "Use DRAFT for compact decision records" and `adr_entry_id_scheme` surfaces "80-bit
  generated entry IDs", which is the real decision behind the hallucinated `mse_a4282580`. This is
  recall-oriented work with a human reviewing, which is where topic matching is strong and where a
  false positive costs a glance rather than a wrong authority claim. All 20 unattached ADRs produced
  candidates.
- A: Auto-attaching high-overlap candidates (rejected - same stakes as a lineage move, and the
  same lesson applies).
- F: `experiments/adr-campaign/topic_candidates.py`, `ATTACHMENT-REVIEW.md`.
- T: 20 of 20 unattached ADRs produced ranked candidates.

### Facts

- Topics are read with sidecar precedence, never a union with authored entry topics - JNL corrected
  this mid-pass, and it matters: the first run compared authored topics on one end against sidecar
  topics on the other, which is not a comparison. This is `adr_topic_authority` applied to its own
  consumer.
- Decision-level topic coverage is 1051 of 1292 decisions.

## 2026-08-07 11:43 - Soft ADR context; five attachments; topic hierarchy cannot gate lineage

```yaml
entry_id: mse_8p47vz0pgabqm1va
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/adr-soft-context
topics:
  - lifecycle-edges
  - schema
evolves:
  - d1 -> mse_gm39dmzzs4nbysm1:d1
```

### Summary

- New `context-added` ledger event: attach decisions to a concern as SOFT evidence, with no head
  move and no status change. It is the hard/soft separation JNL asked for, made expressible.
- Five screened attachments applied on JNL's approval: 2 as heads, 3 as soft context.
- Tested JNL's hierarchy-aware chaining proposal against the 5 known hops: every hierarchy level
  admits the known-bad hop, because its endpoints are topical SIBLINGS while the known-good hops
  cross topic areas entirely.

### Decisions

#### D1 - `context-added`: soft attachment that moves nothing

- D: A new event kind carrying `supporting_decisions` plus a Reason, with no `decision_ref` and no
  predecessors, validated to refuse both. It leaves head and status untouched, and its refs stay
  outside `adr_membership`, so they never arm the MCP review gate.
- R: Attachment previously required a `revise` event, which sets a head - so "add context" was
  inexpressible, and two of the five approved attachments could not be written truthfully without
  it. `adr_archive_before_replace`'s decision is an INSTANCE of the policy rather than the policy
  itself, and `adr_community_detection_rejected` / `adr_topic_backfill_rejected` carry status
  `rejected`, where accepting a head would have read as "this revision was accepted" instead of
  "we decided against this". The event also makes the cost model legible: hard attachment
  (decision_ref + predecessors) is what the review gate charges for, soft attachment is free.
- A: Making all five heads (rejected - overclaims, and would silently flip two statuses).
  Attaching only the three that fit the head shape (rejected - drops real evidence rather than
  fixing a schema gap that was already identified).
- F: `memory_seed/adr.py` (`EVENT_TYPES`, `EVENT_RE`, validation branch, `add_context`),
  `tests/test_adr_contract_extension.py`.
- T: Head, status and membership all unchanged after a context write; a `rejected` ADR stays
  rejected; empty evidence, missing reason and unresolvable refs each refused. 1091 tests pass.

#### D2 - Topic hierarchy cannot gate lineage hops, at any level

- D: Chain construction stays gated on authorship (authored edges act, machine-scored edges go to
  review). Topic overlap remains an annotation. Hierarchy-aware variants were measured and not
  adopted.
- R: JNL proposed comparing at parent or grandparent level so adjacent siblings resolve together.
  Measured against the 1 known-bad hop and 3 known-good authored hops, every hierarchy rule -
  same-parent, any-shared-ancestor, same-root - ADMITS the bad hop and blocks 2 or 3 of the good
  ones. The cause is the opposite of what the proposal assumes: the bad hop's endpoints
  (`agent-rules`, `skill-architecture`) are SIBLINGS under `control-plane`, making it the most
  topically coherent hop in the sample, while the good hops genuinely cross areas
  (`session-logging` -> `session-fuse`) or change activity (`bugfix` -> `feature-build`) because
  that is what a real evolution does. Topic proximity measures subject matter; concern identity
  measures which question a decision answers, and two decisions can be maximal topic-neighbours
  while belonging to different concerns. Authorship separates all 4 cases correctly and is already
  the implemented rule.
- A: Adopting same-parent matching (rejected - admits the bad hop, keeps only 10 of 239 edges).
  Adopting any-shared-ancestor (rejected - admits the bad hop for 5 more edges than leaf matching).
- F: measurement only; no code change.
- T: leaf 34 edges/18 components; siblings 10/5; any-ancestor 39/19; same-root 39/19; unfiltered
  239/55 with a 60-node component. Best rule keeps 16% of edges, so segmentation stays a reading
  lens rather than a partition.

### Facts

- Also applied earlier in the same review, on JNL's "reject the first, take the second":
  `adr_merge_branch_primitive` converged onto its own decision (mse_dr5eprnhrctqeeg3:d1) with the
  related integration decision attached; the machine-suggested `adr_subproject_scoping` head move
  was rejected.
- Five attachments applied: heads on `adr_entry_id_scheme` (ms-a4282580:d2) and
  `adr_topic_vocabulary` (mse_25zzy3cmdjgrsf69:d1); soft context on `adr_archive_before_replace`,
  `adr_topic_backfill_rejected` and `adr_community_detection_rejected`.
- Screening validation dropped 7 of 10 worker picks: two quoted the decision's TITLE rather than
  its body, two quoted text that does not exist, one was 27 characters, one cited a ref never
  offered. Re-reading them by hand recovered 4 good picks - the selections were often right when
  the proof was not.
- The topic candidate finder has high recall and modest precision: candidates for all 20 unattached
  ADRs, of which about 5 were worth attaching.

### Follow-ups

- ADR topic granularity is the untested half of JNL's proposal: if ADRs declared narrower topics,
  the attachment candidate finder should get more precise. Worth measuring before the next pass.

## 2026-08-07 11:56 - Link sidecars: a re-visited file is legitimately out of order, so the writer sorts instead of refusing

```yaml
entry_id: mse_9aggjnyaqrmb545d
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/session-links-sidecar-e41dab
```

### Summary

- JNL proposed that link sidecars be filed under the originating decision's month folder and date
  filename, append-only across passes. That is already the as-is design, enforced at five points
  (spec, `apply_link_gap_stubs`, `links check`'s `link-sidecar-date-mismatch` at error severity,
  the fuse's routing and its `link_date` check, plus a test). No delta to close.
- The comparison did surface a live defect the placement rule and the identity rule create together:
  four dates were permanently closed to further scaffolding. Fixed, and the four files repaired.

### Decisions

#### D1 - A non-chronological link sidecar is sorted on write, not refused

- D: `apply_link_gap_stubs` no longer raises `Existing link sidecar blocks are not chronological`.
  It re-sorts the whole block region on write with the same stable sort
  `_write_chronological_link_sidecar_file` already used for the fuse - existing blocks keep their
  relative order within a minute, new blocks land after them, block content is never touched. The
  four corpus files stuck in that state (`2026-07-03`, `-05`, `-12`, `-17`) were repaired as a pure
  permutation, verified by comparing the sorted line multiset before and after.
- R: Two sanctioned rules produce the offending shape between them. A sidecar is filed under its
  SOURCE entry's session date, and a later enrichment pass stamps its blocks with the AUTHORING wall
  clock because block identity is `(entry_id, heading timestamp)`. So a re-visited file is
  legitimately out of order, and refusing it closed that date to every future mechanical pass.
  Measured, not inferred: an audit+apply against a scratchpad copy raised on `2026-07-12` and
  `2026-07-17` while succeeding on `2026-08-04`, and 5-14 unblocked entries remained on each of the
  four dates, so the guard was reachable rather than shadowed by the `pending`-empty early return.
- A: Stamp the source entry's own timestamp in the heading instead, so files sort by construction.
  Rejected, and it was my first proposal until the reasoning was checked: pass two's block for one
  entry would then key identically to pass one's, reintroducing the exact one-block-per-entry wall
  the 2026-07-23 identity fix removed. The wall-clock stamp is load-bearing for multi-pass
  enrichment; the refusal was the part that had to go.
- A: Reordering as a content edit under append-only. It is not - the permutation is byte-exact per
  block, and the fuse compares immutability per `(entry_id, heading timestamp)` on record text, so a
  re-sorted file does not read as modified.
- F: `memory_seed/retrieval.py`, `tests/test_link_audit.py`, `CHANGELOG.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-03.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-05.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-12.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-17.md`
- T: `test_apply_sorts_a_wall_clock_stamped_sidecar_instead_of_refusing` reproduces the shape - two
  wall-clock blocks appended newest-first under an origin-date `link_date` - and asserts the apply
  succeeds, the output is ordered, both published blocks survive verbatim, and `links check` passes.
  Full suite 1089 passed; all four dates now accept a stub apply in a probe copy.

#### D2 - The swarm skill said "the day's link sidecar", which reads as today's

- D: `link_swarm.md` step 5 (live and seed) now says the sidecar for the SOURCE entry's own session
  date, states that a campaign spanning many dates writes to many files, names the failure mode
  (`link-sidecar-date-mismatch`), and specifies the AUTHORING WALL CLOCK for the block heading with
  the identity reason attached. The sidecar spec gains a bullet recording that a wall-clock heading
  inside an origin-date file is deliberate and that any writer meeting one must sort, never refuse.
- R: The line was wrong twice over and in opposite directions - "the day's" would have filed every
  block of a campaign under the day it ran, failing `links check` on every one, while
  `<source entry timestamp>` for the heading is the identity collision D1 rejects. The 2026-07-25
  campaign did the right thing on both counts, but that was judgment rather than instruction, and
  the next campaign had no reason to repeat it.
- F: `.memory-seed/skills/link_swarm.md`, `memory_seed/seed/.memory-seed/skills/link_swarm.md`,
  `docs/3_Spec/lifecycle-edge-linking-sidecars.md`

## 2026-08-07 12:17 - Fix the ADR fuse: reconciliation replayed on decision_ref, skipping founding revisions

```yaml
entry_id: mse_0mp31v67vc3fqns6
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/adr-reconcile-founding
topics:
  - session-fuse
  - schema
evolves:
  - d1 -> mse_8p47vz0pgabqm1va:d1
```

### Summary

- `session merge-branch` refused every branch that appended events to a founded ADR. Cause found
  and fixed: `reconcile_adr_records` keyed its replay on `decision_ref`, which a founding revision
  does not have.
- Regression tests cover both the append case and idempotent self-reconciliation.

### Decision

#### D1 - Reconciliation keys on revision_key, and honours the founding carve-out

- D: `reconcile_adr_records` replays on `event.revision_key` rather than `event.decision_ref`, and
  skips the descent check when the current head is a `founding:` placeholder - the same two rules
  `replay_adr` and `validate_adr` already apply.
- R: A founding revision carries no `decision_ref`, so the old key skipped it entirely: it never
  entered `states`, its acceptance then read as "transition targets non-pending revision", and
  every later acceptance read as "competing acceptance ... replay has no head". Both messages
  appeared verbatim when merging the soft-context branch, while `adr check` was green on the branch
  AND on the merge result - the signature of a guard failing rather than a corpus failing. With 34
  of 38 ADRs founded, this made the fuse refuse essentially every future ADR-touching branch, so it
  is a standing tax on the pipeline rather than a one-off.
- A: Continuing to bypass with documented plain merges (rejected - it normalises stepping around
  the append-only guard, which is the habit the guard exists to prevent).
- F: `memory_seed/adr.py` (`reconcile_adr_records`), `tests/test_adr_contract_extension.py`.
- T: A founded ADR with an appended revision+acceptance now reconciles cleanly and replays to the
  new head; self-reconciliation is idempotent; 36 ADR tests pass.

### Facts

- This was the THIRD copy of the replay rules in `adr.py` - `replay_adr`, `validate_adr` and
  `reconcile_adr_records` each implement them. The founding-source extension updated the first two
  and missed the third, which is exactly the failure mode duplicated logic produces. Worth
  collapsing to one implementation before the next schema change.
- The preceding merge of `claude/feature/adr-soft-context` was landed by plain merge under this
  bug; its merge message records the bypass and what was verified instead.

## 2026-08-07 12:31 - Schema-before-data: a fuse guard that names the cause, and the policy rule

```yaml
entry_id: mse_7r60st50e977pjz7
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/adr-schema-ordering-guard
topics:
  - session-fuse
  - control-plane
evolves:
  - d1 -> mse_0mp31v67vc3fqns6:d1
```

### Summary

- The fuse now detects an ADR event kind the running build cannot parse and says so, instead of
  reporting the ledger as corrupt.
- `policy.md` gains the general rule: land a format change before any data that uses it, as its
  own merge.

### Decision

#### D1 - Name the cause at the guard, and state the ordering rule where agents read it

- D: `unknown_event_kinds()` scans a branch's ADR text for `### <kind> - <stamp>` headings whose
  kind this build does not know, and the fuse checks it BEFORE parsing, failing with "uses ADR
  event kind(s) X that this build does not understand. Land the parser change as its own merge
  first, then merge the ledger data." Policy states the same rule generally, covering sidecar
  fields as well as ADR event kinds.
- R: `EVENT_RE` matches only known kinds, so an unrecognised event is skipped silently - the record
  then renders without it and every downstream message blames the ledger ("Current view is stale",
  "competing acceptance") for what is actually a stale parser. That misdirection cost a full
  diagnosis cycle today. Checking before parsing is what lets the error name the real cause; doing
  it after would already have lost the evidence. The policy line matters independently because the
  guard only fires for ADRs, while the ordering constraint applies to any format the fuse
  validates.
- A: Only documenting the rule (rejected - the failure appears as a confusing error long before
  anyone consults policy). Only adding the guard (rejected - it covers ADRs alone, and the same
  trap exists for sidecar fields).
- F: `memory_seed/adr.py` (`EVENT_HEADING_RE`, `unknown_event_kinds`), `memory_seed/core.py`
  (fuse ADR path), `.memory-seed/policy.md`, `tests/test_adr_contract_extension.py`.
- T: Unknown kinds detected and known ones ignored; Current-view headings never mistaken for
  events; every live ADR parses clean under this build. 1098 tests pass.

### Facts

- `.memory-seed/policy.md` has no seeded twin - the Merge And Branch Safety section is
  project-local and `update` never replaces it - so no seed/live parity sync was needed.

## 2026-08-07 13:14 - Link swarm drains the stub backlog, after fixing a warning that could never be cleared

```yaml
entry_id: mse_10m885gx6bery1ew
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/stub-resolution-by-sibling-block
```

### Summary

- A stub's resolution can only ever arrive as a later sibling block, but `sidecar-unclassified-stub`
  was decided per block - so no verdict could clear one. Fixed before spending the fan-out; the
  count fell 71 -> 67 on the real corpus, because four 2026-07-21 stubs had been answered long ago
  and had been warning falsely ever since.
- 67 haiku workers judged 335 candidate pairs. 131 edges survived mechanical validation and were
  written with per-edge confidence. Open stubs: 67 -> 9, the 9 being sources whose every candidate
  was judged `none`.

### Decisions

#### D1 - A stub is resolved by a later sibling block, not never

- D: `sidecar-unclassified-stub` is now an ENTRY-level question, decided after every block is read.
  A stub survives only when no LATER block for that entry authors an edge or declares
  `edge_status: not_applicable`. Ordering is `(heading timestamp, block index)` - the same
  later-and-more-specific precedence `edge_status` already used - so a resolution written BEFORE a
  stub correctly leaves it standing, because a fresh audit asking again about an already-classified
  entry is a real question.
- R: Append-only forbids editing a stub to record its own answer, so deciding the warning per block
  made it permanent by construction. The advisor flagged the risk and the empirical test settled it:
  a hand-written verdict block in a scratchpad copy left the count at 71. Had the swarm run first,
  its entire output would have been invisible to `links check` and ESR - the run would have looked
  like it changed nothing.
- A: Suppress on ANY resolving block rather than a later one. Rejected: it would silence a genuine
  re-audit of an entry that was classified once and has since grown new candidates.
- F: `memory_seed/core.py`, `tests/test_links_check.py`
- T: `test_a_later_sibling_block_resolves_an_unclassified_stub` (subtested for both an edge-bearing
  and an `edge_status: not_applicable` resolution) and
  `test_a_stub_written_after_a_resolution_still_warns` for the ordering half. Full suite 1100.

#### D2 - Closed answer lists, and every edge lands carrying its confidence

- D: Ran the swarm over all 67 open stubs, one worker per source entry (~5 pairs each), and wrote
  all 131 surviving verdicts with `edge_confidence` and a tier on the corpus's own thresholds
  (`low < 0.70 <= review < 0.90 <= high`, derived from 33 authored pairs with no contradictions).
  Worker payloads address the SOURCE by bare `dN` and every TARGET by a fully-qualified
  `mse_x:dN` - candidate decision bodies carry `ref`, never `ordinal` - so the two vocabularies
  never share a shape, guarded by a build-time assertion.
- R: Two prior measurements decided the shape rather than my estimate. Cycle 3 measured FIVE pairs
  per agent, so the six-sources-per-agent batching I had planned was a 6x extrapolation; one source
  per agent restores the measured shape at the cost of 67 agents. And the closed-list rule came from
  the attachment campaign where workers invented 15 of 17 identifiers they were asked to recall -
  here 4 verdicts out of 157 named a ref outside the legal list. Writing the weak `related` edges
  faded rather than dropping them follows the same reasoning as `edge_status`: a low-confidence
  verdict is evidence someone looked, and deleting it destroys that.
- A: A confidence floor that discards everything below 0.70. Rejected by JNL's call to write all 131
  - the graph and Trail already fade low-confidence edges, so the floor belongs at read time.
- F: `.memory-seed/sessions/links/2026-07/2026-07-21.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-29.md`,
  `.memory-seed/sessions/links/2026-07/2026-07-30.md`,
  `.memory-seed/sessions/links/2026-08/2026-08-04.md`
- T: `links check` integrity OK, 0 errors, and the write was 1067 insertions with 0 deletions across
  9 files - no published block reopened. The 26 mechanical drops were led by 14 quote-grounding
  failures, the guard doing its job. These are wall-clock blocks inside origin-date files, the exact
  shape that used to raise `blocks are not chronological`, so the write validates 105dffb end to end.

## 2026-08-07 13:52 - Closing the stub backlog: a length rule buried real edges, and one verdict was a rule stretched too far

```yaml
entry_id: mse_mw03g9n22yk2z9st
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- Open classification stubs reach 0. Eight carry `edge_status: not_applicable`; the ninth turned
  out to hold three real edges my own validator had buried.
- Hand-audited the three litmus-flagged swarm verdicts: two were false positives of a keyword flag,
  one was a genuine over-reach and is downgraded append-only.

### Decisions

#### D1 - Grounding decides a quote, length never does

- D: The three verdicts on `mse_0v732ccp4tj2p1gf` dropped for quote length (141/141/38 chars against
  a 40-120 window) are written as edges. Their quotes are verbatim spans naming legal refs, so the
  grounding check - the actual hallucination guard - passed on all three. The one other length drop
  in the run (131 chars) is genuinely ungrounded and stays dropped. The remaining eight stubs, whose
  every candidate was judged `none`, carry `edge_status: not_applicable` with a note recording that
  a swarm looked rather than a human.
- R: The length bound came from my own memory note and was doing correctness work it was never
  entitled to do. Worse, it was about to be laundered into a false statement: marking that entry
  "examined and found nothing to link" while three grounded edges sat in the drop log. Checking
  whether all nine stubs were genuinely empty before writing the status is what caught it - the
  distinction between "looked and found nothing" and "found something that failed a formatting gate"
  is exactly what `edge_status` exists to keep straight.
- A: Truncate the over-long quotes to fit the window. Rejected - that is editing evidence to satisfy
  a rule that should not have been a gate.
- F: `.memory-seed/sessions/links/2026-07/2026-07-30.md`,
  `.memory-seed/sessions/links/2026-08/2026-08-03.md`
- T: `links check` integrity OK; open stubs 9 -> 0; 86 insertions and 0 deletions.

#### D2 - Using a field is not completing the decision that created it

- D: `mse_7c5hj34hjwkdbhpw:d1 evolves mse_tyj5wy264y84rhnf:d2` is downgraded to `related` through a
  `retracts:` block plus a re-authored `related_entries`, carrying no `edge_confidence` because a
  hand audit is authored rather than machine-scored. The other two flagged verdicts stand.
- R: Constitution v1.6 D2 accepted the write-time sidecar consolidation proposal and fixed its build
  order; the ADR campaign applied the same declared-provenance convention to a different record
  family. Rule 2 covers implementing what an earlier entry proposed, not using a facility it
  created, and the swarm stretched it. The other two flags fired only because "merge" and "campaign"
  appeared as subject matter rather than as the act rules 5 and 6 describe - a keyword flag cannot
  tell those apart, which is why it flags for human eyes instead of dropping.
- A: Leave it at 0.75 `evolves` and let the confidence fade carry the doubt. Rejected: fading
  expresses uncertainty about strength, not a wrong edge kind.
- F: `.memory-seed/sessions/links/2026-08/2026-08-07.md`
- T: `entry_link_sidecars` now returns `('related', 'd1', 'mse_tyj5wy264y84rhnf', 'd2')` for the pair
  and no `evolves`, so the retraction is applied by the reader, not merely recorded.

## 2026-08-07 15:00 - Retire the per-entry diagram trigger; ADR attachment is what earns a diagram, and the topic envelope leak closes

```yaml
entry_id: mse_n3fz1dmssqf8ekx5
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- JNL resolved the open question: the per-entry diagram trigger list is retired, not kept beside the
  ADR rule. Applied to `session_logging.md` and its seed twin.
- Queue item 1 landed: the logging skill now documents `--decisions-file` first, and the CLI warns
  when the entry-level flags are used. That is the half that actually leaks.

### Decisions

#### D1 - A rule that cannot be enforced is retired, not supplemented

- D: `session_logging.md` (live and seed) no longer lists branch/worktree/merge topology, layout
  migrations, schema flows, concurrency, command lifecycle and retrieval pipelines as positive
  triggers obliging a per-entry diagram. A decision earns a diagram when it is **attached to an
  ADR**; the obligation is to ANSWER (a diagram, or `diagram_status: not_applicable` with a reason),
  never to draw. The retired shapes survive as guidance for what to draw once one is owed.
- R: JNL's call, on evidence rather than taste: the trigger degraded measurably - 0% of entries
  carried a diagram across May-June, 6.8% in July, 2.3% in August - and had no enforcement, because
  the keyword heuristic that would have enforced it flagged 35% of all entries and was rejected.
  Attachment to an ADR is already a deliberate, recorded, human-gated act meaning "this governs a
  standing concern", so it predicts worth-drawing better than an in-the-moment judgement AND is
  mechanically determinable, which is what lets an ESR pass hold it.
- A: Keep both rules, with ADR attachment as an additional obligation. Rejected by JNL - a rule
  nobody follows is worse than no rule, and leaving it in place would have kept the ESR signal
  measuring against a denominator nobody honours.
- F: `.memory-seed/skills/session_logging.md`,
  `memory_seed/seed/.memory-seed/skills/session_logging.md`,
  `docs/2_Todo/adr-attached-decisions-earn-a-diagram-proposal.md`
- T: Seed twins OK in the ESR report; `links check` integrity OK across 163 files.

#### D2 - The logging skill taught the leaking form, so the skill is the fix

- D: `session_logging.md:86` now shows `--decisions-file` as the documented CLI form with an example
  envelope, and states what the legacy flags cost. `session append` warns on
  `--topics`/`--related`/`--replaces`/`--evolves` naming the envelope as the alternative, rather than
  refusing - an existing script keeps working while its author is told what it costs.
- R: The measured cause of decision-keyed attribution falling 92% -> 8% between July and August was
  not agent discipline. MCP has required the envelope since 2026-07-31, so those writes came through
  the CLI - and the canonical logging skill documented `[--topics a,b]` and never mentioned
  `--decisions-file`. A flag nobody is told to use leaks nothing; a skill teaching the wrong form
  leaks continuously.
- A: Refuse the legacy flags outright. Deferred rather than rejected: it is the accepted build
  order's step 3 and belongs with the CLI flip in
  `write-time-topic-envelope-closure-proposal.md`, which is proposed and awaiting JNL. Warning now
  stops the bleeding without breaking a caller mid-flight.
- F: `memory_seed/cli.py`, `.memory-seed/skills/session_logging.md`
- T: Full suite 1110 passed; warning verified firing against a scratch project.

## 2026-08-07 15:18 - Close the topic envelope leak: both axes mandatory on every path, and a vocabulary request that never becomes a topic

```yaml
entry_id: mse_0z5r2qwwnt323eh5
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- JNL accepted `write-time-topic-envelope-closure-proposal.md`; both halves are built. The CLI and
  MCP paths now agree that a decision owes one Area and at least one Activity.
- `proposed_topic` ships as a request, not an attribution: it never resolves, never lands in
  `topics:`, and reaches the vocabulary only through a human editing `topics.yaml`.

### Decisions

#### D1 - Both axes are mandatory in core, so every write path agrees

- D: `_normalise_decision_sidecars` refuses a decision without `topics.area` or without at least one
  `topics.activity`. The MCP JSON schema has required both since 2026-07-31; moving the rule into
  core makes `session append` refuse identically instead of accepting a decision with neither.
- R: The asymmetry WAS the leak. Decision-keyed attribution fell 92% (July) to 8% (August) while
  coverage stayed at 100%, and only 7 of 86 August entries carried a topic sidecar at all - because
  the CLI accepted the entry-level form and `session_logging.md` documented it. A rule enforced on
  one path and not the other is not a rule.
- A: Refuse the legacy `--topics`/`--related`/`--replaces`/`--evolves` flags outright, as the
  proposal's section A wrote it. Softened to a warning naming the cost and the replacement: refusing
  breaks a script still passing them mid-flight for no gain the warning does not deliver, and the
  doc fix already removed the instruction that taught the wrong form. Tightening later is one line,
  and the ESR attribution-gap count is the signal for when it is safe.
- F: `memory_seed/core.py`, `memory_seed/cli.py`, `tests/test_session_append.py`
- T: `test_both_axes_are_mandatory_on_every_decision`; full suite 1113. No existing test wrote a
  decision without both axes, so nothing was grandfathered in.

#### D2 - A vocabulary request rides beside the attribution, never inside it

- D: `topics.proposed_topic` on the decision envelope: slug-shaped, refused if it already resolves,
  and rendered into the topic sidecar under its own `proposed_topics:` key alongside the still
  mandatory real pair. ESR lists open requests with the requesting decision as evidence. It is never
  returned as an attribution and cannot enter `topics.yaml` by being used.
- R: An author needing a slug the vocabulary lacks had two bad options - force-fit an existing slug,
  or be blocked. This gives the need somewhere to go without letting usage mint vocabulary, which is
  the PROPOSE/ASSIGN split `propose_topic_children.py` already enforces and the promotion rule
  `topics.yaml` records: evidence, not usage.
- A: Put the request in the `topics:` list with a marker. Rejected - every topic reader treats that
  list as resolvable vocabulary, so a request sitting in it becomes a topic by being read, which is
  exactly what the split exists to prevent. Its own key makes that impossible rather than merely
  discouraged.
- F: `memory_seed/core.py`, `memory_seed/mcp_server.py`, `memory_seed/esr.py`,
  `tests/test_session_append.py`
- T: `test_a_proposed_topic_is_a_request_never_an_attribution` and
  `test_a_proposed_topic_that_already_resolves_is_refused`; `links check` integrity OK across 163
  files, so a sidecar carrying the new key validates unchanged.

## 2026-08-07 15:36 - Topic sidecars declare their axis, and a vocabulary request must name the axis it is asking for

```yaml
entry_id: mse_0zf7hxmrhn6kxjzt
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- JNL: a sidecar should say which slug is the Area and which the Activity, for assigned topics and
  for requests alike. The nested shape that does this was already readable and simply had no writer.
- `proposed_topic` becomes `{slug, axis}`; a request without an axis is refused.

### Decision

#### D1 - Declare the axis in the record rather than leaving it to a lookup

- D: The write-time topic sidecar now emits the nested form - `topics:` with `area:`/`activity:`
  sub-keys, and `proposed_topics:` in the same shape. `topics.proposed_topic` is an object
  `{slug, axis}` with `axis` required and constrained to `area`/`activity`; ESR prints the axis
  beside each open request.
- R: JNL's point is that a flat list cannot be read on its own terms - you cannot tell the thing
  worked on from the kind of work without opening `topics.yaml`. It matters more for a request,
  which is unanswerable without it: "add `swarm-orchestration`" cannot be ruled on until someone
  says whether it is an Area or an Activity, and the requester is the one who knows. The nested
  shape needed no new contract - `entry_topic_sidecars` has read it since 2026-07-27, returning
  `decision_area`/`decision_activity`, and no writer had ever emitted it.
- A: A trailing `# area` comment on each flat item. Rejected - comments are invisible to every
  parser, so the axis would be documentation rather than data, and `decision_area` would stay empty.
- F: `memory_seed/core.py`, `memory_seed/mcp_server.py`, `memory_seed/esr.py`,
  `tests/test_session_append.py`, `docs/2_Todo/write-time-topic-envelope-closure-proposal.md`
- T: `test_the_sidecar_declares_each_slug_s_axis` asserts the rendered shape AND that
  `entry_topic_sidecars` returns the axis pairs; `test_a_proposed_topic_is_a_request_never_an_
  attribution` asserts the reader never returns a request among `topics` or `decision_topics`;
  `test_a_proposed_topic_needs_an_axis_and_must_not_already_resolve` covers both refusals. Full
  suite 1114. The nested form degrades safely - an axis-blind reader collects the same `- ` lines.

## 2026-08-07 16:16 - Every ADR answered from its decisions, and Leg B says per-decision topics are worth the sweep

```yaml
entry_id: mse_tff97qz8pnsxz5hj
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- Every ADR now carries a diagram answer: 38 of 38, 24 drawn and 14 recorded as
  `not_applicable`. JNL's steer to draw from the DECISIONS rather than the ADR summary flipped
  every re-drafted ADR from declined to drawn.
- Leg B is measured and resolved: per-decision topic attribution carries non-arbitrary
  information, and free inheritance is wrong about half the time a topic query touches a
  multi-decision entry.

### Decisions

#### D1 - Leg B resolved by measurement, and the permutation control is what makes it a finding

- D: Per-decision attribution beats free inheritance. Across 208 entries and 563 decisions,
  inheritance assigns a decision 42.8% topics on average that its own attribution never claimed;
  87% of those entries have decisions that genuinely differ; and 1053 of 2166 decisions a topic
  query returns under inheritance (48%) are false positives. The topic-swarm backfill is worth
  running.
- R: The headline divergence alone proves only that the two models DISAGREE - an attributor that
  scattered an entry's topics at random across its decisions would produce exactly the same
  number. The discriminating test is a within-entry permutation, holding the union, the decision
  count and the slugs-per-decision fixed and varying only the pairing: the real assignment tracks
  the decision's own text better than its shuffled control in 144 of 183 entries (78%, z = 7.8,
  mean delta +0.0254 with 95% CI +0.0204 to +0.0304). A random carve-up wins half the time.
- A: Score the swarm against authored topics, which is what the aborted pilot did. Rejected as the
  thing that made the pilot unusable: if both sides are model judgments, 0.613 is inter-annotator
  agreement rather than accuracy. Leg B deliberately asks a structural question the corpus can
  answer without adjudicating anyone.
- F: `docs/4_Reference/leg-b-per-decision-attribution-measurement.md`,
  `docs/2_Todo/write-time-topic-envelope-closure-proposal.md`
- T: Two read-only scripts over `.memory-seed/`, no model calls. Caveats recorded in the doc:
  this does not resolve Leg A (accuracy), the lexical proxy makes 1.17x a floor rather than an
  estimate, and the 6-decision bucket that breaks the monotone trend is n=3.

#### D2 - Draw ADR diagrams from the decisions, not the ADR summary

- D: All 38 ADRs answered - 24 drawn, 14 `not_applicable` - with 19 drawn diagrams citing 32
  decision refs in `grounded_in`. Where an ADR had attached decisions, the diagram is drawn from
  their D/R/A bodies rather than the ADR's Current view.
- R: JNL's steer, and it changed the outcome rather than merely improving it. A first fan-out
  working from the one-paragraph summary produced 19 drawn / 19 declined; re-drafting 18 of them
  from the decision bodies flipped every one to drawn. `adr_entry_id_scheme` reads as a bare
  format rule in summary and was declined; its decision describes a branch - legacy `ms-` frozen
  forever, new `mse_` generated, both resolving in one corpus - which is worth a picture. The
  briefs asked specifically for ALTERNATIVES, because a rejected branch is real structure that a
  summary always flattens away. This applied far wider than expected: 23 ADRs carry resolvable
  decision refs, not the 6 with a real decision head, because the campaign attached decisions as
  evidence to the `founding:` ones too.
- A: Leave the summary-drawn diagrams. Rejected - six were declines that the decisions show were
  wrong, so the summaries were actively costing coverage.
- F: `.memory-seed/sessions/diagrams/2026-06/2026-06-15.md`,
  `.memory-seed/sessions/diagrams/2026-07/2026-07-26.md`,
  `.memory-seed/sessions/diagrams/2026-08/2026-08-07.md`
- T: `links check` OK across 166 files with zero diagram issues; 82 Mermaid blocks parse through
  the real parser across 14 sidecars; every `grounded_in` ref machine-checked against its own ADR's
  closed list, none fabricated. Replacement was legitimate only because the blocks were
  uncommitted - the replacer refuses any adr_id already in HEAD, so four committed declines were
  left alone.

## 2026-08-07 18:01 - ADR diagrams are a review tick that evolution clears, not a coverage gate

```yaml
entry_id: mse_xzeepd82actv9j87
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- JNL reframed the ADR diagram rule from a coverage mandate to a REVIEW TICK that evolution clears.
  That dissolved the problem a hard gate could not solve, and most of it was already built.

### Decision

#### D1 - A review tick that evolution clears, not a coverage gate

- D: An ADR carries a recorded verdict on whether it deserves a diagram - a diagram, or
  `diagram_status: not_applicable`. Because the answer is filed under the ADR's
  authoritative-decision date, an ADR that later evolves onto a new decision stops matching, and
  `links check` raises `needs-diagram-review` (WARNING) asking for another look. No hard gate; ESR
  reports reviewed / never-reviewed / needs-re-review.
- R: JNL's design, and it is strictly better than the gate I attempted. Framing it as coverage made
  a missing answer a failure, which broke five test fixtures that create ADRs to exercise unrelated
  contract rules and - the real objection - would have failed for any DOWNSTREAM project with ADRs
  the moment it upgraded, over a convention invented that afternoon. Framing it as review STATE
  removes the mandate entirely: an unreviewed ADR is simply unreviewed. The staleness half was
  already built and tested; only the severity and the wording were wrong.
- A: A project-local `require_adr_diagram_answer` flag, which is what I was about to propose.
  Rejected as machinery bolted on to preserve a framing that was itself the problem.
- F: `memory_seed/core.py`, `memory_seed/esr.py`, `tests/test_links_check.py`,
  `.memory-seed/skills/session_logging.md` (live and seed)
- T: `test_adr_diagram_is_filed_under_its_authoritative_decision_not_the_drawing_day` now asserts the
  warning severity AND that `check_session_links` stays ok - a cleared tick must never fail the
  check. Full suite 1114; links check OK across 166 files; ESR reads "ADR diagram review: 38 of 38".
- Note: JNL also corrected my framing that this was a new invention. The ADR governance pipeline
  predates this session (set 2026-08-06, S1 landed 2026-08-07), and S4 is already scoped as an ESR
  audit stage - which is exactly where a periodic diagram review belongs. Verified separately that
  no work in this session touched an ADR record; the six that changed came from another session's
  `context-added` commit, and the sha256-pinned fixture tests pass.

## 2026-08-07 18:31 - The topic campaign that worked was a swarm, not a human; and skills now name their governing ADR

```yaml
entry_id: mse_azhh0f71css624jc
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- `adr_topic_backfill_rejected` says a HUMAN worker judged 1,030 units and wrote 2,013 attributions.
  JNL: that never happened - it was a haiku swarm. The record is corrected and the swarm is recorded
  as authoritative for topic attribution.
- Skills now name a `governing_adr`, audited by ESR, because nothing routed an agent from a skill to
  the ADR that governs it.

### Decisions

#### D1 - The 2026-07-27 topic campaign was a SWARM, and it is authoritative

- D: The successful campaign was 58 batches of 12 entries, one haiku worker per batch, no scoring
  gate - a swarm, not human adjudication. What made it work was DROPPING the agreement gate against
  pre-axes authored tags, not substituting a person for the workers. The swarm is the adopted,
  working method for decision-level topic attribution. `index.md` is corrected; the ADR's Current
  view still misstates it and needs a revision event, which is JNL's to accept.
- R: JNL's correction, and the corpus backs it: `mse_8ckmgajc16fxywt2:d1` records "no agreement gate,
  no scoring against authored tags, and one worker per batch", and its D3 reasons about "the worker
  plainly chose it" and "supplying one would be my judgement wearing the swarm's provenance" - an
  orchestrator over workers. The mislabel entered at `index.md`'s ambiguous "a human/worker still
  decides" and hardened into the ADR's "Manual, curated attribution ... a human worker". Left
  standing it would have blocked exactly the method that succeeded: I read that ADR today and
  concluded the swarm was rejected.
- A: Treat the ADR as authoritative and re-plan the 201 as a human task. Rejected on evidence -
  1,030 units of human adjudication never occurred, so the ADR's stated reason for adopting it is
  not a record of anything.
- F: `.memory-seed/index.md`
- T: ADR revision deliberately NOT written here - promote lands `proposed` and acceptance is JNL's
  gate.

#### D2 - A skill names the ADR that governs it, and ESR audits the link

- D: Skill frontmatter gains optional `governing_adr: <adr_id>`. ESR gains a `Skill governance`
  section reporting skills with no ref and skills whose ref does not resolve. Reported, never
  enforced. `topic_swarm.md`, `link_swarm.md` and `session_logging.md` are routed first (live and
  seed); 24 skills remain unrouted.
- R: JNL asked for this after watching the failure. A skill says what to DO; an ADR says what is
  currently AUTHORITATIVE, and nothing connected them - so `topic_swarm.md`, which documents its own
  pilot abort in convincing detail, reads as complete while a rejection sat elsewhere. An agent that
  reads the skill and acts is not being careless; it has no route. This is the `rule -> ADR ->
  constitution clause` chain the ADR campaign already gave `policy.md`, applied to the other half of
  the control plane.
- A: A generic "search history before acting" instruction. Rejected - one already exists in the
  prompt hook and I read it every turn without it firing, because I had read a thorough file and
  felt informed. Routing beats exhortation.
- F: `memory_seed/esr.py`, `.memory-seed/skills/topic_swarm.md`, `.memory-seed/skills/link_swarm.md`,
  `.memory-seed/skills/session_logging.md`
- T: Full suite 1114; ESR prints the section; seed twins stay in sync.

## 2026-08-07 20:29 - Topic swarm closes the gap to 2, and skills route to their governing ADRs

```yaml
entry_id: mse_hqzcxnkehb7mbcma
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- The topic swarm ran in the adopted unscored shape and closed the attribution gap: 201 -> 2, with
  1185 of 1237 corpus decisions (95%) now carrying decision-keyed area+activity.
- Skill -> governing-ADR routing extended to 12 more skills; 12 remain deliberately unrouted.

### Decisions

#### D1 - The swarm works, and two write-path lessons are now in the brief

- D: Ran 10 batches of <=12 entries, one haiku worker each, no agreement gate. 194 of 201 units
  survived validation, then 5 of the 7 rejects on retry - gap 201 -> 2. Both failures found on the
  way are recorded in `topic_swarm.md` (live and seed): a new block SUPERSEDES the entry's previous
  one wholesale, so pre-existing per-decision attributions must be carried forward; and workers get
  two separately labelled closed lists plus an explicit escape hatch.
- R: The carry-forward defect is the one worth keeping. `entry_topic_sidecars` is most-recent-wins
  per ENTRY, not per decision, so a block naming only this campaign's decisions un-attributed 14
  siblings an earlier campaign had keyed - slugs still on disk, silently unreadable. Nothing reported
  it; it surfaced only because the resulting gap count did not match the arithmetic. Append-only
  saved the data, not the design. 30 attributions were carried forward on the corrected write.
- A: Repair the 2 remaining quote failures by hand. Rejected - a repaired quote is the orchestrator's
  judgement wearing the swarm's provenance, which is the rule the 2026-07-27 campaign already set.
- F: `.memory-seed/skills/topic_swarm.md`, `.memory-seed/sessions/topics/`
- T: 201 units asked, 201 answered; zero alias-for-canonical and zero cap violations, so the split
  vocabulary lists held. 41 of 112 entries got DIFFERENT attributions across their decisions.
  `links check` OK across 172 files.

#### D2 - Route a skill to its ADR only where one genuinely governs it

- D: 12 more skills carry `governing_adr` (agent_collaboration -> worktree_convention,
  release_publishing -> release_topology, subproject_runtime -> subproject_scoping, and so on). 12
  remain unrouted and ESR keeps naming them.
- R: The unrouted 12 are persona and tooling skills - `code_search`, `security_triage`,
  `office_document_editing`, `graphify_analysis` - with no architectural concern in the ADR corpus to
  point at. Inventing a ref to silence the report would defeat what the report is for: an agent
  following a wrong pointer is worse off than one following none. The remainder is also a reading of
  the ADR corpus, which has no concern covering skill architecture itself.
- A: Route all 24 to the nearest-looking ADR. Rejected for the reason above.
- F: `.memory-seed/skills/*.md` (live and seed)
- T: Full suite 1114; seed twins OK; ESR reports 12 routed, 12 not.

## 2026-08-07 21:04 - Lineage chains segment the hairball, and unclaimed chains are ADR candidates

```yaml
entry_id: mse_nbvrzre3258snxcq
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/lineage-chains
topics:
  area:
    - lifecycle-edges:d1
    - lifecycle-edges:d2
    - topic-vocabulary:d3
    - decision-harvest:d4
  activity:
    - feature-build:d1
    - feature-build:d2
    - feature-build:d3
    - process-correction:d4
evolves:
  - d1 -> mse_gm39dmzzs4nbysm1:d1
```

### Summary

- Same-area filtering breaks the 67-decision lineage hairball into 16 readable chains; 14 carry no
  ADR and are candidate concerns. The largest is 11 decisions, all `mcp-tools`.
- Pair-affinity expansion ships as a RECALL widening only, with the two measurements that forbid
  every other use written into the module that implements it.
- The quote-length gate is retired: grounding decides, length is reported.

### Decisions

#### D1 - Read the axis the record declares, not one derived from the vocabulary

- D: `audit_link_topic_join` gains `decision_axes()`, reading `decision_area`/`decision_activity`
  from the topic sidecars; `decision_topics()` unions them and falls back to the flattened channel
  only for records predating the nested form. `proposed_topics` is never read.
- R: The nested form landed today and states the axis on the record, so deriving it by walking
  `topics.yaml` ancestry was a second derivation of something already declared - and a worse one:
  the declared channel resolves 1155 decisions with an area against 1067 flattened. Skipping blank
  ordinals also removed the 34 "attributions at a non-existent ordinal" the audit had been
  reporting, which were entry-level attributions misread as decision-level. `proposed_topics` holds
  vocabulary REQUESTS that never resolve; reading them would make a request into a topic by use,
  which is exactly what its separate key exists to prevent.
- F: `experiments/adr-campaign/audit_link_topic_join.py`
- T: Audit reports 0 non-existent ordinals (was 34), 0 non-canonical slugs leaking, and a spot check
  matches the sidecar's own `area:`/`activity:` declaration.

#### D2 - A chain is same-area; a chain nobody claims is a candidate concern

- D: `lineage_chains.py` emits connected components over lineage edges whose endpoints share a
  declared area, ordered oldest-to-newest, annotated with the ADRs claiming each member, and
  reports the unclaimed ones as ADR candidates with a suggested head. Claim detection counts SOFT
  `context-added` attachments as claims, not just `adr_membership`.
- R: The unfiltered graph's largest component is 67 decisions spanning six unrelated areas - not a
  concern, a hairball. Same-area filtering yields 11 `mcp-tools`, 8 `session-fuse`, 7
  `docs-lifecycle`, each reading as one thing. That result replicated three times as the corpus grew
  231 -> 265 -> 267 edges with topic coverage nearly doubling, which is what makes it a pattern
  rather than a fit. Counting soft attachments matters because reporting an already-read chain as
  unclaimed sends a reviewer to look at nothing.
- A: Filter on leaf topic rather than area. Rejected - it keeps more edges but mixes activity into
  the grouping, and activity is SUPPOSED to change along a chain.
- F: `experiments/adr-campaign/lineage_chains.py`, `LINEAGE-CHAINS.md`
- T: 16 chains of 3+, 14 unclaimed. Independent corroboration: the mechanism surfaced a
  `skill-architecture` chain of 4 with no ADR, and today's `governing_adr` routing concluded by hand
  that "the ADR corpus has no concern covering skill architecture itself" - two methods, same gap.
  Claim detection verified against `adr_merge_branch_primitive`, correctly shown as owning chain 2.

#### D3 - Pair affinity widens recall and may never gate or rank

- D: `pair_affinity.py` builds an area-pair table by lift (n>=3, lift>=2.0, leave-one-out) and
  `topic_candidates.py` uses it to surface extra candidates, each labelled `[pair-expanded]` with
  its lift and n. The two measurements forbidding any other use are in the module docstring.
- R: Both alternative uses were measured and both failed. As a GATE the table scored the known-bad
  hop highest of all (lift 11.30) - from n=1, because that very edge was in the data it learned
  from, which is why leave-one-out and the count floor are load-bearing rather than tidy. As a
  RANKER every topic-derived scorer placed JNL's approved picks at chance (mean rank 3.00 of 5).
  What topics do earn is recall - all 5 approved picks were surfaced from ~1050 topiced decisions -
  so the table widens that surfacing and nothing else. The table is also learned from 229
  machine-suggested edges against 38 authored, so the circularity is contained by bounding the
  blast radius rather than by pretending the input is clean: a wrong pair costs one extra candidate
  a worker reads and discards.
- A: Learn from authored edges only. Rejected on measurement - 38 authored edges yield 1 usable
  pair, so the mechanism would do nothing.
- F: `experiments/adr-campaign/pair_affinity.py`, `topic_candidates.py`
- T: 10 cross-area pairs, several bidirectional, replicating across all three corpus states;
  45 expanded candidates offered across 17 unattached ADRs. Reachability grepped: the table is
  imported only by its own report and the recall report, never by `lineage_map.py` (the review path)
  or `reconcile_lineage.py` (the write path).

#### D4 - Grounding decides a quote; length is reported

- D: `validate_screening.py` no longer drops a pick for quote length. It verifies the substring
  match and reports anything outside a 40-120 advisory window as an outlier.
- R: The bound was mine, from a memory note, and it was doing correctness work it was never
  entitled to do - it buried a valid `adr_entry_id_scheme` pick over 27 characters. The same
  anti-pattern buried three verbatim edges in today's link swarm (141/141/38 chars), where one entry
  was about to be marked "examined and found nothing to link" as a result. A formatting rule that
  can launder a false statement is not a formatting rule.
- F: `experiments/adr-campaign/validate_screening.py`
- T: The 157-char `adr_markdown_substrate` pick is now kept and reported rather than dropped.

### Facts

- The planned M4 check - that the `adr_entry_id_scheme` pick survives - is moot: that ADR was
  attached earlier today, so it no longer appears in the candidate review. The 157-char outlier
  demonstrates the behaviour instead.
- If all 14 unclaimed chains were founded and their members attached, that is 61 diagram answers
  owed under the 2026-08-07 rule. The report prints the number so the cost is chosen, not
  discovered.

### Follow-ups

- Promote the unclaimed-chain report into the ESR standing check once its candidates prove good
  over a few cycles - JNL's sequencing, deliberately not wired in on faith.
- `adr_topic_backfill_rejected` still records a human worker where it was a swarm; its revision is
  JNL's to accept and is untouched here.

## 2026-08-07 23:31 - Two ADRs founded from unclaimed chains, and the diagram answer is per ADR

```yaml
entry_id: mse_7gw3cmhcz2d3n6qp
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/found-chain-adrs
topics:
  area:
    - decision-harvest:d1
    - diagram-view:d2
  activity:
    - feature-build:d1
    - process-correction:d2
evolves:
  - d1 -> mse_nbvrzre3258snxcq:d2
```

### Summary

- `adr_mcp_integration_surface` (11 decisions) and `adr_skill_registry_and_generic_skills` (4)
  founded from the two chains JNL approved. Both land `proposed`; acceptance is JNL's gate.
- Corrected my own miscount: an ADR owes ONE diagram answer, not one per attached decision. The
  rule already worked that way; my report multiplied by member count and the skill's wording
  invited it.

### Decisions

#### D1 - Found two concerns from lineage chains, heads on the newest member

- D: `adr_mcp_integration_surface` heads on `mse_vzsef0fmpsde2jh4:d1` with 8 supporting decisions;
  `adr_skill_registry_and_generic_skills` heads on `mse_542z3qn0azma9mmx:d1` with 3. Members attach
  as `supporting_decisions`, not `predecessors`.
- R: The newest member is what "current" means in this model. Supporting rather than predecessors
  because a predecessor needs a `link:<head>:<kind>:<member>` assertion matching a real edge between
  that exact pair, and these chains connect members to each other rather than all to the head -
  asserting otherwise would invent lineage the sidecars do not record. The MCP chain spans config
  placement, upsert semantics AND the gated write surface, so the title says all three rather than
  pretending the head covers only placement; a narrower title would misdescribe eight of its
  eleven members.
- A: Accept them here under the earlier delegation. Rejected - a head is exactly the claim the
  review gate exists for, and the S1 delegation was scoped to reaching that end state, which we
  passed.
- F: `.memory-seed/decisions/adr_mcp_integration_surface.md`,
  `.memory-seed/decisions/adr_skill_registry_and_generic_skills.md`,
  `experiments/adr-campaign/found_chain_adrs.py`
- T: `adr check` green; corpus 38 -> 40; the chain report now shows both chains as claimed and
  unclaimed falls 14 -> 12, so claim detection sees the new ADRs.

#### D2 - One diagram answer per ADR, and the skill now says so

- D: The chain report's cost line is corrected to one answer per unclaimed chain, and
  `session_logging.md` (live and seed) now reads "An ADR earns a diagram when a decision is
  attached to it - one answer per ADR, not one per attached decision", stating that the block keys
  on `adr_id` and is filed under the head's date.
- R: JNL's instruction was that diagrams belong on top-level decisions only - and the system
  already did exactly that: diagram blocks key on `adr_id`, and ESR counts
  `adrs_without_diagram_answer` over ADR ids. The defect was mine and the skill's. My report
  multiplied answers by member count and published "61 diagram answers owed" when the real figure
  was 14; the skill's opening line, "A decision earns a diagram when it is attached to an ADR",
  reads naturally as per-decision and is what I reasoned from. A cost overstated four-fold argues
  against founding ADRs, which is the opposite of what the evidence supports.
- A: Change the rule to match my reading. Rejected once the code was checked - there was nothing to
  change, only something to say correctly.
- F: `experiments/adr-campaign/lineage_chains.py`, `.memory-seed/skills/session_logging.md`,
  `memory_seed/seed/.memory-seed/skills/session_logging.md`
- T: Report now prints 12 answers for 12 unclaimed chains; live and seed twins carry identical
  wording; full suite 1114, `links check` OK across 172 files.

### Facts

- The MCP chain's head (`Gate the MCP write surface`) is newer than the placement decisions it
  sits with, which is why the ADR is titled for all three threads rather than for placement alone.
  If JNL would rather split it, the chain report has the members to do so.
- 12 unclaimed chains remain, including `docs-lifecycle` (7), `trail` (6) and `hooks` (4).
