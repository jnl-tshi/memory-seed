## 2026-08-31 14:29 - Promote the session-log git-diff trigger fix into a new ADR

```yaml
entry_id: mse_v45yv16xpebmqs8c
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/adr-session-log-trigger
```

### Summary

- Promoted the git-diff session-log trigger fix into its own ADR, since no existing ADR covered
  the concern (entry-format, hook-installation, and ADR-review-gating ADRs all stop short of
  when/why the Stop hook fires). Wired citations into `policy.md` and `index.md`, and refreshed
  `index.md`'s stale description of the hook (it only described the pre-2026-08-29 staleness-only
  mechanism).

### Decisions

#### D1 - Promote a new ADR rather than amend an existing one

- D: Ran `memory-seed adr promote --adr-id adr_session_log_trigger_enforcement` sourced from
  `mse_4e4y2348y7493b37:d1` (the git-diff trigger fix) with `mse_97jrpfbzq4pyr77h:d2` (the
  replication run's root-cause diagnosis) as supporting evidence. Status `proposed` - promotion
  alone doesn't accept it, left for JNL to review the written file before a separate `adr
  transition` call.
- R: An Explore pass over all 58 existing ADRs found none claim this concern -
  `adr_draft_format` governs entry format, `adr_hook_merge_framework` governs hook
  *installation*, `adr_session_decision_authority`/`adr_mcp_decision_envelope_review` govern
  ADR-review process - none extend to the Stop hook's trigger logic. `policy.md` and `index.md`
  both described `session-log-check.py` with no ADR citation, unlike neighboring bullets that do
  cite one. JNL asked for this to be captured in an ADR; promotion (not hand-authoring) is the
  only sanctioned path in this project.
- A: Considered amending `adr_hook_merge_framework` (closest by subject) - rejected, its stated
  scope is installation/upsert mechanics, not internal trigger behavior; stretching it would
  misrepresent both concerns under one ADR.
- F: `.memory-seed/decisions/adr_session_log_trigger_enforcement.md` (new).
- T: `memory-seed adr show adr_session_log_trigger_enforcement` - status `proposed`, decision ref
  resolves; `memory-seed adr check` - "ADR sidecars OK"; `memory-seed links check` - "Session
  memory integrity OK (220 files)", pre-existing warnings only, none touching the new ADR or its
  source decisions.

#### D2 - Mirror the founding decision's area/activity topic pair, defer real axis support

- D: `--topics control-plane,bugfix,session-logging` - the first two positions exactly mirror
  `mse_4e4y2348y7493b37:d1`'s own `area: control-plane, activity: bugfix` tagging; a third tag
  (`session-logging`, an area slug) is appended purely for findability, documented as not carrying
  axis meaning.
- R: JNL: ADR topics should carry at least an area axis "so they can be easily parsed much like
  the decisions that make them up." Checked and confirmed this is a real, corpus-wide gap -
  `AdrRecord.topics` is a flat tuple, `validate_adr` only checks slug validity, never calls
  `topic_index.axis_of()` - zero axis awareness anywhere in `adr.py`, unlike decision-level topics
  which are fully area/activity-structured and validated in `core.py`. JNL's call, given via
  AskUserQuestion: promote this ADR now with a position-based stopgap; treat real axis support
  (schema decision, CLI flags, retrofit-or-not for 58 existing ADRs) as a separate follow-up.
- A: Considered designing/building real area/activity structure for ADR topics in this same pass -
  rejected per JNL's explicit sequencing choice; it's corpus-wide scope (58 existing ADRs, a
  schema-version question ADRs don't have a precedent for) that deserves its own proposal, not
  something to fold into promoting one ADR.
- F: none beyond the ADR's own `--topics` value.
- T: n/a - policy choice, not a code change.

#### D3 - Wire ADR citations into policy.md and index.md, refreshing index.md's stale description

- D: Added `(ADR [adr_session_log_trigger_enforcement](...))`  citations to `policy.md`'s
  `.memory-seed/hooks/*.py` ownership line and to `index.md`'s "Session-log-check escalation"
  bullet. Rewrote that `index.md` bullet in the same edit - it only described the pre-2026-08-29
  staleness-only mechanism (`consecutive_misses`, no mention of the git-diff fingerprint or
  `baseline_diff_fingerprint` field added in `mse_4e4y2348y7493b37`), which is exactly the kind of
  drift `index.md` is supposed to stay ahead of.
- R: Citing the ADR only where the mechanism is described, without also fixing the description
  being wrong, would have left `index.md` internally inconsistent - a reader following the new
  citation would land on an ADR describing a mechanism `index.md` itself doesn't yet mention.
- A: Considered leaving `index.md`'s existing bullet untouched and adding a second, separate bullet
  for the git-diff addition - rejected as unnecessary fragmentation; the existing bullet is already
  the canonical "current state of this hook" entry and updating it in place keeps that true.
- F: `.memory-seed/policy.md`, `.memory-seed/index.md`.
- T: `memory-seed docs check` - 223 files, 16 pre-existing warnings unchanged.

### Follow-up

- ADR-topics-need-an-area-axis is a real, scoped-but-undesigned gap (D2 above). Not filed as a
  `2_Todo/` proposal in this pass - JNL asked only to sequence it after this promotion, not to
  scope it yet.
