## 2026-08-02 01:36 - Move vendor-neutral rules from CLAUDE.md into policy.md

```yaml
entry_id: mse_q0csfqb2darg474m
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
topics:
  - control-plane
  - agent-rules
related_entries:
  - mse_re2dzyar7mcc32rp
```

### Summary

- Moved the two vendor-neutral behavioural rules out of `CLAUDE.md` into `.memory-seed/policy.md` so they survive control-plane updates and reach every agent, leaving only the Claude-specific turn-shape rule behind.

### Decision

- D: Relocate Orientation and Merge And Branch Safety to `policy.md` as new sections, keep Response Length in `CLAUDE.md` with a pointer to where the vendor-neutral rules now live, and move rather than copy so the two files cannot drift.
- R: `policy.md` is listed in `BOOTSTRAP_GENERATED_FILES` in `core.py` and is absent from the seed file set, so `update` generates it once and never replaces it - unlike `CLAUDE.md`, which `core.py` skips only while its `memory-system-version` is at least the package version and therefore archives and overwrites at the next bump. The two relocated rules are also vendor-neutral: Codex, Gemini and Cursor never read `CLAUDE.md`, so rules about verifying repository state and proving a merge landed were invisible to three of the four agents this project supports.
- A: Duplicating the rules in both files was rejected because two copies of a behavioural constraint drift, and the drift is silent. Leaving everything in `CLAUDE.md` was rejected once the replacement mechanism was confirmed in code rather than assumed. The suggested `pip index versions memory-seed` command was replaced in the policy wording with the PyPI JSON API, because `pip index` is an experimental subcommand that is frequently unavailable - the original wording was preserved verbatim while it lived in `CLAUDE.md`, but a durable policy file should carry a command that works.
- F: `.memory-seed/policy.md`, `CLAUDE.md`.
- T: `python -m memory_seed.cli doctor` reported the reusable control plane healthy and bootstrap complete; `links check` passed.

### Follow-up

- A third constraint was added while writing the policy section: prefer `session merge-branch` over a raw `git merge` for branches carrying session entries, since the fuse is what preserves chronology and stamps trailers. This session used that path and the dry-run previewed all nine entries before anything merged, which is the behaviour the rule now names.
- The orphaned worktree directory at `.claude/worktrees/standards-regulatory-research-853c9c` is deregistered from git but could not be deleted from this session - the shell's working directory keeps resetting into it, so removal reports the device as busy. It will delete cleanly once the session ends.
