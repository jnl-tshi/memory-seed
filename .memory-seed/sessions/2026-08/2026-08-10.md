---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-10
---

## 2026-08-10 01:09 - Semantic compression has not earned a sidecar

```yaml
entry_id: mse_tffbcbqgj0yf5yfg
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Built and measured an isolated Stage 1 semantic-compression ablation over 100 stratified Memory Seed decisions, with production changes explicitly out of scope.
- Three independent subagent reviews found and closed metadata leakage, cross-granularity link labels, parser-field bleed, citation errors, and missing provenance.

### Decisions

#### D1 - Compression has not earned a semantic sidecar

- D: Keep the recommendation at INVESTIGATE FURTHER: raw text remains best on the adequately sized retrieval task, the fully scoped relationship task is underpowered, and Stage 2 comprehension plus fidelity must pass non-inferiority gates before any build decision.
- R: After clearing all BM25F metadata, raw MRR is 0.582; core + why + constraint uses 39.1% of raw representation-body context but falls to 0.508 MRR. Structured labels consume more context without improving retrieval. Only 14 fully scoped decision-edge positives remain, so relationship results cannot support a conclusion.
- A: A production meaning sidecar, ontology, and knowledge graph were rejected at this stage because the experiment has not shown they improve comprehension, fidelity, retrieval, or relationship detection.
- F: `experiments/semantic-compression/README.md`, `experiments/semantic-compression/research.md`, `experiments/semantic-compression/experiment-design.md`, `experiments/semantic-compression/benchmark.py`, `experiments/semantic-compression/dataset.json`, `experiments/semantic-compression/metrics.json`, `experiments/semantic-compression/results.md`, `experiments/semantic-compression/recommendation.md`, `tests/test_semantic_compression_benchmark.py`.
- T: 49 targeted benchmark/retrieval tests pass; a second generation is byte-stable; reviewers report no remaining Critical or Important methodology findings.

### Follow-up

- Run the preregistered blinded Stage 2 comprehension and semantic-fidelity evaluation before changing the recommendation.

## 2026-08-10 01:27 - Stage 2 is planned, not preregistered

```yaml
entry_id: mse_p5trg3y08d5f1k1h
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Clarification to `mse_tffbcbqgj0yf5yfg`: Stage 2 is planned, not preregistered. Its eligibility gates are specified, but its prompts, item set, model/version, decoding configuration, randomization seed, and annotation/adjudication materials have not been frozen.

### Validation

- The experiment README, design, metrics, and recommendation consistently report `planned-not-preregistered` after commit `9a880e7`.

## 2026-08-10 06:59 - esr --json carries both ADR queues, and the review queue names its answer paths

```yaml
entry_id: mse_r4r74wzb4y8xnx57
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/esr-json-and-queue-answers
```

### Summary

- Two ESR fixes from the storylines review (R7, R9), implemented by a sonnet subagent against the
  approved plan and reviewed here. `esr --json` now carries both ADR queues, and the review-queue
  section names the concrete mechanism that answers each flag.
- Investigation for R9 established a fact worth keeping: there is NO standalone command that
  records reviewed-no-change - the only writer is the session-append ADR review gate, which only
  the MCP surface drives. The preamble now says so; a standalone recorder is drafted as a separate
  proposal rather than built.

### Decisions

#### D1 - Both ADR queues reach esr --json, and a completeness guard makes the next omission loud

- D: `EsrReport.to_dict()` gains `adr_attachment_candidates` and `adr_head_reviews` as top-level
  keys named after their fields; every existing key (including the `last_sidecar_date` rename and
  the nested `diagrams` bucket) stays byte-stable. A new field-by-field completeness test walks an
  explicit path table so any future EsrReport field missing from the dict fails a test instead of
  silently vanishing from automation.
- R: Both queues rendered only in prose - the sections shipped without their JSON surface, twice,
  because nothing checked coverage. The guard converts the class of bug into a test failure; the
  shape-stability constraint exists because downstream consumers replay stored payloads.
- A: Restructuring the JSON (renaming the diagrams bucket, nesting an adrs block) - rejected:
  shape stability outweighs tidiness on a consumed payload.
- F: memory_seed/esr.py, tests/test_esr.py, CHANGELOG.md
- T: Completeness walk over every dataclass field; both keys list-typed for a populated queue;
  first CLI-level `esr --json` smoke test. Full suite 1168.

#### D2 - The review-queue preamble states both answer paths, because one of them has no command

- D: The `## ADR review queue` preamble names the revision pair (`adr revise` then
  `adr transition --status accepted`, after authoring the successor decision entry) and states
  that reviewed-no-change is recorded ONLY through the MCP `memory_session_append` review gate
  (`{"outcome": "no-change", "reason": ...}`) - the CLI has no equivalent. Syntax lives once at
  section level (the diagram-section precedent); per-ADR line formats are byte-identical.
- R: A queue that tells the reader what is stale but not how to answer it routes every flag
  through re-discovery. The honest version of "carry the command" is naming the mechanism,
  because the no-change path has no command to carry - which is itself the finding.
- F: memory_seed/esr.py, tests/test_esr.py
- T: Rendered-section assertions for the revision pair and the no-change mention.

## 2026-08-10 07:01 - links graph-diff: the edge-set gate becomes a command instead of a campaign script

```yaml
entry_id: mse_qsxwvg1g0n75r2c3
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/links-graph-diff
```

### Summary

- The graph assertion that has twice caught silent edge loss - `links check` read OK across both -
  is now a command instead of a throwaway campaign script: `links graph-diff` snapshots the
  effective evolves graph and gates a later comparison on the edge set being unchanged.
  Implemented by a sonnet subagent against the approved plan; reviewed and landed here.

### Decision

- D: `effective_graph_snapshot(cwd)` serialises the effective graph (schema_version 1,
  `corpus_revision` git-pin per the quality-report convention, aggregates, per-node sorted
  `evolves` targets, decision-keyed `refines_successors`); `diff_graph_snapshots` names every
  per-node delta. CLI `links graph-diff --snapshot <path>` / `--against <path> [--json]`. The GATE
  is the entry-level evolves edge set only - any per-node addition/removal or evolves-aggregate
  delta fails (exit 1); refines/typing deltas are reported but informational, so a pure retype
  campaign passes. Exit 2 for unusable baselines (missing, malformed, schema mismatch).
  `link_swarm.md` (+ byte-identical seed twin) now instructs every bulk sidecar write to bracket
  with snapshot/against, and states plainly that `links check` alone is not sufficient.
- R: The assertion earned tool status the hard way - 807 edges vanished under a green `links
  check`, then a +3 resurrection was caught only by the same hand-rolled check in a second
  throwaway script. A gate that must be copy-pasted per campaign is one omission from not
  existing. The typed-delta exemption matches the assertion's history exactly: both real uses
  gated retype campaigns whose refines counts changed BY DESIGN while the edge set had to hold.
- A: Auto-persisting baselines to a fixed location - rejected: explicit paths, nothing committed,
  no hidden state; the caller owns the snapshot's lifetime.
- F: memory_seed/retrieval.py, memory_seed/cli.py, tests/test_graph_diff.py,
  .memory-seed/skills/link_swarm.md, memory_seed/seed/.memory-seed/skills/link_swarm.md,
  CHANGELOG.md
- T: Eleven new tests - snapshot shape, empty corpus, unchanged/added/removed edges, the
  retype-is-informational case, three unusable-baseline exits, CLI usage errors. Seed twins
  byte-identical (filecmp). Full suite 1175 on the branch.

## 2026-08-10 07:12 - Fuse refusals abort their own merge, and refusal messages name the validated side

```yaml
entry_id: mse_j41ywke76agqw4yj
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/fuse-attribution-autoabort
```

### Summary

- Two merge-fuse fixes from the storylines review (R10, R11), implemented by an opus subagent
  against the approved plan and reviewed here. A fuse refusal now aborts its own half-started
  merge, and every apply-phase refusal message names WHICH side's copy failed validation.
- The change refines the merge-branch primitive's contract, so it is proposed as a revision on
  `adr_merge_branch_primitive` through the review gate in this same append - the head moves only
  if JNL accepts.

### Decisions

#### D1 - A fuse refusal aborts its own merge; genuine conflicts still wait for a human

- D: `session_merge_branch` and `session_prepare_pr_branch` auto-abort (`git merge --abort`) at
  every refusal exit that leaves no content conflicts - fuse validation refusals, unfusable
  paths, and git plumbing failures across six exits in each function, via one shared helper so
  CLI and MCP get identical behaviour. On success the result reports `merge_aborted=True` with
  "merge aborted automatically; nothing was committed" appended AFTER the refusal reason.
  Non-session content conflicts keep the leave-in-progress contract unchanged, and the
  `git commit failed` exit deliberately does not abort - the fuse succeeded there and the tree
  holds correctly fused work worth inspecting.
- R: A refusal's half-started merge has no value a human can extract - there are no conflict
  markers to resolve, only an outright rejection - and committing that state half-applies a
  changeset (nearly happened 2026-08-09). Conflicts are different in kind: they hold exactly the
  state a human needs. The prior "never abort" rule in `adr_merge_branch_primitive`'s orbit was
  written before refusals and conflicts were distinguished; this refines it rather than reversing
  it, which is why the ADR gets a revision proposal, not a silent drift.
- A: Blanket abort like the MCP handler - rejected by JNL: it would discard resolvable conflict
  state and flip the pinned conflicts-test contract.
- F: memory_seed/core.py, memory_seed/cli.py, memory_seed/mcp_server.py,
  tests/test_session_fuse_and_merge.py
- T: New chronology-refusal test asserts no MERGE_HEAD, clean tree, HEAD unmoved, and the abort
  line; the unrecognized-path refusal test flipped to expect auto-abort; the conflicts tests and
  MCP integrate tests unchanged and green. Full suite 1416 on the branch (this checkout collects
  the memory-trace suites; delta over baseline is exactly the three new tests).

#### D2 - Apply-phase refusals name the side whose copy was validated

- D: The twelve `_apply_session_fuse_plan` refusal messages carry side attribution in three
  families: "existing ... not chronological" names the BASE side (the committed content the merge
  reset the path to, with the base short-sha, and that the repair must land on base first);
  "already exists with different text" names both sides; "imported ... not chronological" names
  the source branch. The BASE claim is double-gated - only when the caller performed the
  base-reset AND the base commit actually holds the path - so a standalone `session fuse --apply`
  or a branch-new file is attributed to the working tree or branch instead, never falsely to base.
- R: The 2026-08-09 deadlock came from exactly this silence: "existing blocks are not
  chronological" reads as the branch's fault, the natural fix lands on the branch, and the fuse -
  which validates main's copy - refuses forever. The double gate exists because a blanket "it is
  base's fault" suffix would reproduce the same misdirection mirrored.
- F: memory_seed/core.py, tests/test_session_fuse_and_merge.py
- T: The chronology test asserts the BASE attribution text and sha;
  `test_standalone_fuse_apply_does_not_claim_the_base_side` pins the gate.

## 2026-08-10 07:13 - link retract lands on both surfaces, and the checker learns entry-YAML edges are declared

```yaml
entry_id: mse_fbvx1c0vx64hthm4
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/link-retract
```

### Summary

- The append-only correction mechanism finally has a tool: `link retract` (CLI) and
  `memory_link_retract` (the first MCP link-write tool) render a correctly-grammared retract or
  retract-and-retype block, pre-validated against the same rules `links check` enforces after the
  fact. Implemented by an opus subagent against the approved plan; reviewed and landed here.
- The build surfaced a latent checker defect: `links check` counted an edge as "declared" only
  from link sidecars, so a retract of an entry-YAML edge - which the reader has honoured since
  2026-08-09 - would have been reported as dangling. Fixed, verified byte-identical on the live
  corpus (179 issues before and after; the defect was latent here, not firing).

### Decisions

#### D1 - Retract-and-retype is a command on both surfaces, refused before it can go wrong

- D: `apply_link_retract` renders ONE sidecar block - wall-clock heading, `retracts:` with one
  line per ordinal (comma multi-ordinal input fans out), optional re-authored edge of the new
  kind/type, `note:` passthrough - through the shared chronological writer stack, filed under the
  source entry's own session date. Pre-validation mirrors the checker: unknown kinds, malformed
  refs, undeclared edges (dangling), a taken refines slot on retype, a date pin that is not the
  declaration date, and a colliding `(entry_id, heading-stamp)` block identity are all refused
  BEFORE anything is written. CLI `link retract <kind> <ref> --from <entry>` follows `link add`'s
  write-then-recheck pattern; MCP `memory_link_retract` follows the session-append write-tool
  shape with dry_run, no merge_trigger gate (an append-only correction, not an integration).
- R: Retract-and-retype is the mandated fix for three `links check` errors, yet every retract in
  the corpus was hand-formatted markdown - the system's only sanctioned correction path had the
  highest authoring friction and no guard between the author and a malformed or dangling block.
  Pre-validation at the moment of writing is the same argument as every write-time gate here: the
  refusal costs a keystroke now instead of a checker error after publication.
- A: A `reclassifies:` one-liner (retract-plus-readd sugar in the grammar) - stays deferred per
  the spec doc; the command gives the same ergonomics without a second grammar surface.
- F: memory_seed/core.py, memory_seed/cli.py, memory_seed/mcp_server.py,
  tests/test_link_retract.py, tests/test_links_check.py, tests/test_mcp_session_append.py,
  docs/3_Spec/draft/link-retraction.md, CHANGELOG.md
- T: Twenty new tests across core/CLI/MCP - including retype-to-refines refused when the slot is
  held by another entry but allowed when held by the retracting entry itself, dry-run writes
  nothing, and the previously uncovered `retract-before-declaration` checker rule. The MCP
  write-surface pin widened explicitly from two tools to three. Full suite 1433 on the branch.

#### D2 - The checker's declared-edge index includes entry-YAML edges

- D: `check_session_links` feeds entry-YAML lifecycle edges into `declared_entry_edges` /
  `declared_decision_edges` / `declared_edge_dates`, so a retract naming an entry-YAML edge
  validates instead of reporting `dangling-retract`.
- R: The reader has honoured retracts of entry-YAML edges since 2026-08-09; the checker still
  believed only sidecars declare edges, so the reader and its integrity gate disagreed about
  which retracts are legal - the command's own primary use case was unreachable. Verified
  read-only on the live corpus before and after: 179 issues, byte-identical - latent, not firing.
- A: none considered viable; the asymmetry was simply an omission.
- F: memory_seed/core.py, tests/test_links_check.py
- T: `test_retract_of_an_entry_yaml_edge_is_not_dangling`; the declared-edge collector agrees
  with the checker by test rather than shared code - noted in-code for whoever changes the
  identity tuple next.

## 2026-08-10 07:35 - The storylines review goes live: six R-items resolved, stale abort-rule prose swept, recorder proposal drafted

```yaml
entry_id: mse_spn9wdhjasq6r344
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/storylines-live
```

### Summary

- The storylines review becomes a living document: the six resolved items (R3, R4, R7, R9, R10,
  R11) are marked RESOLVED with what shipped, the affected storylines (S4, S5, S6, S7, S8) and
  the parity matrix now describe the as-is system, and the closing priorities were re-ranked -
  R8 (the MCP/CLI surface split) is now the sharpest open item, since `links graph-diff` shipped
  CLI-only and widened it. Every claim was verified against the code before writing.
- Three docs still stated the pre-R11 rule that the merge is never aborted; all three now carry
  the refined contract (refusals self-abort; genuine content conflicts wait for a human). The
  `adr reviewed` recorder question is captured as a design-only proposal rather than left in a
  chat log.

### Decision

- D: `docs/1_Inbox/agent-interaction-storylines-review.md` is committed for the first time as a
  living document (staying in 1_Inbox per JNL) and updated to the post-tranche state.
  `.memory-seed/index.md`, `README.md` and `docs/3_Spec/functionality-audit.md` state the refined
  abort rule. New `docs/2_Todo/adr-reviewed-recorder-proposal.md` lays out the standalone
  reviewed-no-change recorder with the `update_entry_id` provenance question (three options,
  recommendation: require an existing entry ref) and the write-surface-parity constraint; build
  explicitly out of scope. Lane indexes regenerated.
- R: A review snapshot that stays frozen after its findings are acted on becomes misinformation
  with a byline; JNL's direction was that the document goes live the moment the fixes do. The
  stale never-abort lines were flagged by the fuse implementer as newly false - the sweep also
  confirmed the ADR itself already carries the refined wording, so only prose lagged.
- F: docs/1_Inbox/agent-interaction-storylines-review.md, .memory-seed/index.md, README.md,
  docs/3_Spec/functionality-audit.md, docs/2_Todo/adr-reviewed-recorder-proposal.md, lane READMEs
- T: docs check OK (208 files, 16 pre-existing unrelated warnings); full suite 1210 unchanged.

## 2026-08-10 08:13 - Lean DRAFT compression fails the fidelity gate

```yaml
entry_id: mse_rdtwf7sc4pexq9km
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Tested whether a lean extractive DRAFT front door can reduce context while preserving retrieval and fidelity across 100 frozen decisions, with 30 independent-query targets and 120 audited cards.
- The result is negative for canonical shortening: the lean card used 44.1% of raw context but lost 0.118 semantic MRR and carried adjudicated critical omissions on 50% of cards versus 0% raw.

### Decision

- D: Do not shorten canonical DRAFT decisions with the measured decision+rationale+boundary+six-anchor selector. Keep full prose canonical. Progressive disclosure with a safer selector and immediate full-source access remains an unvalidated future hypothesis, not an implementation recommendation.
- R: Decision-only compression was worse, at 13.3% context, 0.110 semantic MRR, and 70% critical-error cards. Supplemental exact identifiers improved identifier MRR by 0.069, but the interval crossed zero, the strict no-semantic-regression gate missed, and identifier-boundary coverage was only 86.9%.
- A: Shipping either compact selector and changing DRAFT grammar were rejected. No diagram sidecar is needed because this is a scalar arm comparison without spatial, temporal, or topology structure.
- F: `experiments/semantic-compression/lean_draft_pilot.py`, `lean-draft-design.md`, query/reviewer/adjudication artifacts, `lean-draft-metrics.json`, `lean-draft-results.md`, README/design/recommendation updates, and `tests/test_lean_draft_pilot.py`.
- T: 21 combined semantic-compression tests pass; query validation passes; two full regeneration runs are byte-stable; three independent final reviews report no remaining material code, method, or documentation concern.

### Follow-up

- Before changing authoring guidance, preregister a paired natural-authoring study with human comprehension, delayed recall, critical-fidelity gates, and a safer non-lossy preview selector.

## 2026-08-10 10:09 - The review's own recommendations meet the record: three refuted, two reframed, nothing deleted

```yaml
entry_id: mse_6gs60fb9ns77fftd
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/storylines-corrections
```

### Summary

- A memory pass over the storylines review's own recommendations refuted three of them outright and
  reframed two more. The document now records each verdict with the decision that overturned it, and
  carries a method note saying plainly why: the original twelve were written from code reading alone.
- Separately, a redundancy audit across CLI, MCP and the memory-trace HTTP API found five candidates
  that look retirable. None were deleted; each turned out to be an authored retention, uniquely
  capable, ADR-governed, or a published surface whose removal is a governed act.

### Decisions

#### D1 - A recommendation is not trustworthy until it has been checked against the record

- D: R1, R2 and R12 are marked REFUTED/STALE in the storylines document with the deciding evidence
  named; R5 and R6 keep their premise but carry corrected constraints; R8 stands and gains the
  read-versus-write governance split. A method note at the top states that the original R1-R12 were
  written from code reading alone and that recommendations here are trustworthy only once checked
  against recorded decisions.
- R: The hit rate is the argument. R1 proposed deprecating `branch`/`worktree` into `situate`, which
  passes no `agent_type` to `worktree_guard` and therefore cannot perform the namespace-collision
  check those tools exist for. R2 proposed collapsing two rankers that were considered and rejected
  by name the day before the review was written. R12 proposed building worktree cleanup into
  merge-branch eleven days after it shipped there, with the fallback half already rejected on record.
  R5's blocking objection (sidecar freshness) had been retracted by its own successor decision
  fourteen minutes after it was raised. Code shows what is true; only memory shows what was decided,
  and a review that skips the second half produces confident recommendations to undo deliberate work.
- A: Quietly correcting the entries and leaving the method unexamined - rejected: the failure was
  systematic rather than five separate slips, and a living document that hides its own error rate
  teaches its next reader to trust it more than it has earned.
- F: docs/1_Inbox/agent-interaction-storylines-review.md
- T: `docs check` OK across 208 files; MCP tool count verified against the `TOOLS` registry (19).

#### D2 - Nothing was deleted, and the audit records why each candidate stays

- D: A new Redundancy audit section records five candidates and their verdicts: the six duplicated
  legacy `/api/*` endpoints are deliberate transitional duplication gated by the vanilla parity
  checklist; `/api/timeline` was retained twice on record to keep the Timeline-tab retirement
  revertable; `/api/cache/rebuild` has no caller but is the only RUNTIME rebuild lever, since
  `--rebuild-cache` is launch-time only; `session integrate` adds no behaviour over merge-branch and
  open-pr but is referenced by the MCP refusal payload and governed by `adr_integration_mode`; and
  `memory_adr_review` is largely subsumed by the append refusal payload yet remains a published MCP
  surface. Also removed a phantom `memory_dir` entry from the MCP inventory - it is a `Runtime`
  dataclass field that was never a tool.
- R: Three of the five look dead by caller count alone, which is exactly the measurement that would
  have deleted them. `/api/cache/rebuild` is the sharpest: it has zero callers and removing it would
  still remove the only way to force a repair without restarting the server. Caller count measures
  usage; it does not measure capability, and it cannot see an authored decision to keep something.
- F: docs/1_Inbox/agent-interaction-storylines-review.md
- T: Verdicts verified against memory (two authored retentions quoted), against the code (the
  launch-only `--rebuild-cache` flag, the MCP `cli_command` pointer) and against the ADR ledger.

## 2026-08-10 10:12 - Front-door carry-forward test is locked before scoring

```yaml
entry_id: mse_2kc2v2pgvzpk0fkj
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Locked a follow-on development ablation before answer-key authoring or scoring. It separates raw-body ranking, exact-identifier retrieval evidence, and post-rank presentation instead of compressing the ranking corpus again.

### Decision

- D: Test the carry-forward improvements with complete raw ranking, current-versus-authored identifier fields, and a lossless complete-D/R/A front door with one query-selected F/T block and structural fallback to raw. Freeze and independently review exact source-span answer keys before the first scoring run.
- R: The prior selector mixed retrieval compression with presentation and lost 0.118 semantic MRR plus critical boundaries. This protocol preserves raw ranking, measures identifier value separately, and scores displayed context by exact answer-evidence retention rather than treating shorter text as success.
- A: A natural-authoring or human-comprehension claim is explicitly out of scope; the reused 30-target set is a development replication set only. No diagram sidecar is needed because this is a scalar ablation without spatial, temporal, or topology structure.
- F: `experiments/semantic-compression/front-door-design.md`, `front_door_ablation.py`, and `tests/test_front_door_ablation.py`.
- T: 27 front-door and lean-pilot tests pass with three parameterized subtests; oracle packet generation passes; scoring fails closed while oracle and review hashes remain `PENDING`; `git diff --check` passes.

### Follow-up

- Author and source-only review all 60 exact-span answer keys, freeze both hashes, then execute the scored run once without changing the selector.

## 2026-08-10 10:45 - Front-door oracle is frozen before scoring

```yaml
entry_id: mse_f29hkasxxz61ghw6
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Froze a 60-query, 90-span exact-source oracle and a source-only acceptance receipt before the first front-door ablation score.
- Three bounded authors repaired one true omission from the first review and two further sufficiency gaps from the fresh review; no reviewer saw display candidates, rankings, or metrics.

### Decisions

#### D1 - The source oracle is frozen before scoring

- D: Pin the exact answer-key and review bytes in the harness and require both validators to pass before `run` can write any score artifact.
- R: The prior lean-selector result showed that shorter text can hide critical boundaries. Exact source spans plus an independent sufficiency review make evidence retention measurable without letting the projection or ranker see the answer key.
- A: The first review reported five encoding defects that exact UTF-8 source-slice validation disproved; it also found one real omission. A fresh review then found two additional insufficiencies. All true gaps were repaired at their source and the final review accepted 60/60; rejected rows were never waved through.
- F: `experiments/semantic-compression/front-door-answer-key-parts/part-0.json`, `experiments/semantic-compression/front-door-answer-key-parts/part-1.json`, `experiments/semantic-compression/front-door-answer-key-parts/part-2.json`, `experiments/semantic-compression/front-door-answer-key.json`, `experiments/semantic-compression/front-door-answer-key-review.json`, `experiments/semantic-compression/front_door_ablation.py`, `tests/test_front_door_ablation.py`.
- T: Harness validation confirms 60 answers, 90 exact spans, and 60 accepted reviews; `python -B -m pytest tests/test_front_door_ablation.py tests/test_lean_draft_pilot.py -q` passes 28 tests plus 3 subtests.

## 2026-08-10 10:55 - Front-door display misses the context-reduction gate

```yaml
entry_id: mse_27f36hwt896hncf8
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Executed the first frozen front-door development run at commit `0852637b` and preserved its raw metric/result artifacts unchanged.
- The display comparison is usable; the retrieval null requires a separate instrument correction recorded in the next milestone.

### Decisions

#### D1 - The measured front door does not advance to human Stage 2

- D: Reject `query_evidence` as the development candidate and keep full DRAFT decisions canonical and immediately available.
- R: It retained 88/90 exact answer spans (97.8%) and all required spans for 58/60 queries (96.7%), but median first-view context including the source affordance was 86.2% of raw: only a 13.8% reduction against the frozen 20% gate. `complete_dra` retained exactly the same evidence with less context, so the query-selected F/T block earned no measured benefit.
- A: Treating mean `complete_dra` context (78.8%) as a pass was rejected because the frozen gate is the per-card median including the affordance; that arm's median was 81.3%. The prior `current_lean` card was smaller but exposed only 16/90 answer spans.
- F: `experiments/semantic-compression/front-door-ablation-metrics.json`, `experiments/semantic-compression/front-door-results.md`.
- T: Frozen corpus/query/selector/oracle/review hashes passed; exact D/R/A projection integrity was 234/234; three independent audits agreed the overall development-success criteria did not pass.

## 2026-08-10 10:56 - Current lexical terms are inert under BM25F normalization

```yaml
entry_id: mse_t4sapqa9bwfk5bfb
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Audited the first run's ten exact-zero retrieval effects against production BM25F and a synthetic sensitivity probe before interpreting them.
- The current identifier field is inert under the shipped normalization path; a new isolated holdout will test the minimal normalization correction without rewriting the scored run or changing production yet.

### Decisions

#### D1 - The retrieval null is an instrument finding, not evidence that identifiers add no value

- D: Preserve the first run as an invalid retrieval comparison for `current_lexical_terms`, and test normalized exact-identifier evidence on a fresh held-out target/query set before considering a production change.
- R: Production extraction stores notable identifiers with punctuation, `_bm25f_score` normalizes each query term before lookup, but `_chunk_field_tokens` leaves lexical-term values raw. A synthetic `rare_symbol.py` probe scored 0.0 with the current representation and 0.545 when the field value was normalized, moving the intended chunk to rank 1. Across the 60 development queries, `current_lexical_terms` changed zero full orderings and produced zero lexical-field matches.
- A: Calling all four full rankings identical was rejected: the artifact records target rank only. `authored_exact_terms` changed ten full orderings because that selector also admits plain backtick words such as `update`; that is a differently defined, partly generic field, not proof the current identifier path works. Editing the frozen v1 selector or shipping a production fix from post-hoc data was also rejected.
- F: `memory_seed/semantic_cache.py`, `experiments/semantic-compression/front_door_ablation.py`, `experiments/semantic-compression/front-door-ablation-metrics.json`.
- T: Read-only call-path audit plus synthetic negative/sensitivity control; independent code and method reviewers agreed the v1 retrieval null is not usable as a performance conclusion.

## 2026-08-10 11:18 - Freeze the held-out identifier normalization protocol

```yaml
entry_id: mse_28eqx7r4zhfe6cah
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Froze a fresh, entry-disjoint holdout protocol for testing the lexical-term normalization defect without rewriting or reusing the failed front-door retrieval comparison.

### Decisions

#### D1 - Test identifier normalization on a sealed source-only holdout

- D: Freeze 30 source-entry-disjoint targets, the complete arm/selector contract, source-only query and review gates, a pre-score synthetic sensitivity control, a real-corpus exposure gate, and paired decision rules before any ranking result is produced.
- R: The first run established that raw current lexical terms were unreachable under the production BM25F normalization path, so its zero effects were evidence about the instrument rather than identifier value. A fresh holdout using the shipped ranker is the smallest test that can distinguish a useful normalization correction from a development-set artifact.
- A: Editing the already scored front-door harness was rejected because it would erase the failed instrument finding. Shipping the production normalization fix directly from the post-hoc development result was rejected because its confidence intervals crossed zero and its queries were already observed.
- F: `experiments/semantic-compression/identifier_lane_ablation.py`, `experiments/semantic-compression/identifier-lane-design.md`, `tests/test_identifier_lane_ablation.py`.
- T: Direct CLI `freeze-info` and source-only packet generation passed; 39 focused semantic-compression tests and 3 subtests passed. Independent code, method, and reproducibility audits were run before the freeze.

## 2026-08-10 11:24 - Gate the primary normalization arm on real identifier exposure

```yaml
entry_id: mse_k5cbbyvxjc2ffjds
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Tightened the frozen holdout after independent review found that the minimal normalized-current arm could still receive an identifier absent from the current field and produce another non-diagnostic null.

### Decisions

#### D1 - Every held-out identifier must expose the primary corrected lane

- D: Limit target/query eligibility to exact authored identifiers already represented in the current extracted lexical field, require target lexical-field matches in normalized-current as well as authored and union arms, distinguish confirmed point-estimate harm from failure to establish semantic non-inferiority, and require the scoring output directory not to exist before the run.
- R: A normalization test cannot evaluate the minimal correction when its chosen identifier was never extracted into the field being corrected. The extra eligibility and real-corpus gates make a zero effect interpretable as performance rather than absent treatment.
- A: Reporting exposure as telemetry without failing was rejected because it would repeat the first run's null-instrument failure. Calling every confidence-bound miss a regression was rejected because uncertainty is not evidence of harm.
- F: `experiments/semantic-compression/identifier_lane_ablation.py`, `experiments/semantic-compression/identifier-lane-design.md`, `tests/test_identifier_lane_ablation.py`.
- T: The revised selector and 30-target entry-disjoint selection reproduce their pins; 42 focused tests and 3 subtests pass, including drift, sensitivity, real-corpus exposure, stale-output, and decision-rule controls.

## 2026-08-10 11:46 - Freeze the held-out query and review keys

```yaml
entry_id: mse_z1ywrrjvva0jb3ms
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Froze the 30 semantic and 30 exact-identifier holdout queries plus their independent source-only review receipt before any ranking run.

### Decisions

#### D1 - Seal source-only queries and review as the final score unlock

- D: Pin the query artifact at `sha256:6bb711f0b3f87c675f624e0704d26c17a30eff4da160926b2aea825f5759ed22` and the 60-item review receipt at `sha256:940b5c05a233ce3bde0200cd3288740754ef117b80ce30b748daf1cf3244beea`; scoring now fails unless both bytes and all provenance/acceptance fields match.
- R: Three disjoint authors saw only canonical source plus permitted identifiers, and the independent reviewer saw only source and queries. Freezing their outputs before ranking preserves the experiment's separation between representation and result selection.
- A: Letting the orchestrator rewrite rejected queries was rejected because it had seen the arm design and prior development result. Mechanical delimiter and hyphen repairs went back to the original source-only authors before the final hash.
- F: `experiments/semantic-compression/identifier-lane-queries.json`, `experiments/semantic-compression/identifier-lane-query-review.json`, `experiments/semantic-compression/identifier_lane_ablation.py`, `tests/test_identifier_lane_ablation.py`.
- T: Full frozen-input validation reports 30 queries, 60 accepted reviews, and `ready_to_score: true`; 14 focused harness tests pass.

## 2026-08-10 11:56 - Identifier normalization changes scores but not retrieval

```yaml
entry_id: mse_sc1vzy95cabj74zz
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/semantic-compression-benchmark
```

### Summary

- Ran the sealed identifier-normalization holdout once. The corrected lexical fields were active and changed scores and full rankings, but the complete raw body already ranked every exact-identifier target first.

### Decisions

#### D1 - Do not ship lexical normalization or compact drafting from these results

- D: Keep the complete DRAFT decision canonical and rankable; do not ship the tested lexical normalization repair, automatic compact selector, or query-evidence front door as performance improvements. The next candidate is a separate natural-authoring study that removes duplicated prose without deleting decision, reason, boundary, rejected-alternative, or exact-identifier evidence.
- R: On the fresh 30-decision entry-disjoint holdout, normalized fields changed 12-18 full rankings and 31 target scores but changed zero target ranks; exact-identifier MRR was already 1.000 from the raw body and semantic MRR stayed 0.957. The earlier compact selector failed semantic non-inferiority and its same-model fidelity audit, while the safer D/R/A view missed the 20% median context-reduction gate.
- A: Shipping the normalization defect fix because the field is demonstrably inert was rejected: the defect is real, but the measured user-facing benefit on the tested current surface is zero. Further tuning on the observed holdout was rejected; a new question requires a new sealed evaluation.
- F: `experiments/semantic-compression/identifier-lane-metrics.json`, `experiments/semantic-compression/identifier-lane-results.md`, `experiments/semantic-compression/identifier-lane-run-manifest.json`, `experiments/semantic-compression/identifier-lane-audit.md`, `experiments/semantic-compression/README.md`, `experiments/semantic-compression/recommendation.md`.
- T: One official score run from commit `8e4adb94f7d37306ac4f771320150cce9b2431ba`; sensitivity and all real-corpus exposure gates passed; copied artifacts match the completion manifest byte-for-byte; 42 focused tests and 3 subtests pass; three independent post-score reviews found no remaining P1/P2 issue after wording corrections.

## 2026-08-10 13:00 - Memory-grounded conclusions close the retrieval-to-write loop

```yaml
entry_id: mse_axkrkd339br970kw
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/memory-grounded-conclusions
```

### Summary

- Consequential reviews and recommendations now retrieve recorded reasoning before concluding that
  behavior is redundant, obsolete, removable, replaceable, or ready to consolidate. The rule,
  trigger registry, retrieval runbook, and Decision Harvest agree in both live and seed copies.
- The write path now closes the loop: unlinked MCP appends return memory-aware candidates, CLI and
  MCP share one content-bound ADR review preflight, and reviewed-no-change has an evidence-backed
  recorder on both surfaces.
- A harness-neutral code-only redundancy trap records the expected search, full fetch, evidence,
  and constrained conclusion so the behavior can be regression-tested without changing the
  preregistered agent-capture experiment.

### Decisions

#### D1 - Consequential conclusions must consult and disposition memory

- D: Treat consequential reviews, audits, recommendations, and redundancy/removal conclusions as
  proactive history-retrieval triggers. Fetch the relevant full entries before concluding, carry
  every consequential consulted entry into Decision Harvest, and classify it as `replaces`, typed
  `evolves`, `related`, or authoring-only `no-edge`. When an MCP append decision has no edge, return
  the same draft-ranked `link_suggestions` on dry-run and real write; consulted ids sort first and
  no edge is auto-authored.
- R: The latest storylines audit demonstrated the failure directly: code-only review produced three
  false recommendations and two mechanism errors because current code can show duplication or no
  callers while memory records compatibility, rejected alternatives, and deliberate retention.
  Retrieval must therefore happen before the conclusion, while explicit write-time disposition
  prevents the evidence from evaporating before the next entry is authored.
- A: A hard tool gate or task-aware file-touch hook was deferred. The earlier decision rejected a
  hard memory gate, and the current hook sees files rather than task intent; the approved response
  nudge and behavioral fixture add evidence without pretending the hook can infer a conclusion.
- F: `.memory-seed/agent-rules.md`, `.memory-seed/skills/index.md`,
  `.memory-seed/skills/history_retrieval.md`, `.memory-seed/skills/session_logging.md`, their seed
  twins, `memory_seed/semantic_cache.py`, `memory_seed/mcp_server.py`,
  `experiments/memory-grounded-conclusions/`, `tests/test_memory_grounded_conclusion_fixture.py`.
- T: Link-suggestion preview/write parity, consulted-first ranking, linked-decision suppression,
  seed-twin equality, and the code-only trap all pass.

#### D2 - ADR review is one shared preflight, and no-change reviews need no fake entry

- D: Run lifecycle-linked session appends through one content-bound ADR review preflight from both
  CLI and MCP. The first call returns full contexts and a receipt while writing zero bytes; the
  retry supplies exact outcomes. Add `adr reviewed --adr-id --entry --reason` and MCP twin
  `memory_adr_reviewed`, both calling one writer that requires an existing evidence entry and
  appends `reviewed-no-change` without moving authority or manufacturing a session entry.
- R: MCP already proved the content-bound, parent-first transaction. Reusing that path closes the
  CLI bypass and satisfies write-surface parity. The approved existing-entry design preserves the
  provenance invariant while avoiding a corpus entry whose only purpose is to say nothing changed.
- A: Minting a minimal review entry was rejected as proportional corpus noise; relaxing provenance
  only for no-change was rejected as a weaker asymmetric contract. The task-aware hook and local
  `.codex/config.toml` change remain explicitly out of scope.
- F: `memory_seed/adr.py`, `memory_seed/core.py`, `memory_seed/cli.py`,
  `memory_seed/mcp_server.py`, `memory_seed/esr.py`, `tests/test_adr_sidecar.py`,
  `tests/test_mcp_session_append.py`, `docs/3_Spec/adr-lifecycle-sidecar-contract.md`,
  `docs/5_Completed/adr-reviewed-recorder-proposal.md`, `README.md`, `CHANGELOG.md`.
- T: 1,100-test discovery completed with no assertion failures; its one sandbox-blocked local clone
  test passed when rerun with permission. Focused 128-test parity/schema/docs suite, doctor, links
  integrity, ADR replay, docs lifecycle/index, and `git diff --check` pass. Four unrelated existing
  encoding findings remain outside this workstream.

## 2026-08-10 13:01 - Correction: the shared ADR preflight reused core.py without modifying it

```yaml
entry_id: mse_8c0x8hstg4xc88r8
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/memory-grounded-conclusions
```

### Summary

- Correct the changed-file list in the immediately preceding implementation entry.

### Correction

- In `mse_axkrkd339br970kw:d2`, the `F:` list names `memory_seed/core.py`, but that file was not
  modified. The shared preflight and recorder reuse its existing transaction primitives; the code
  changes are in the other files named there.

## 2026-08-10 16:00 - Refresh user storylines and expose the merge-commit timeout false negative

```yaml
entry_id: mse_87n8q05m0k01kjr4
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/docs/update-user-storylines
```

### Summary

- Reconciled the living agent-interaction storylines review with the memory-grounded conclusions implementation: S2/S3 now describe recall-before-conclusion and explicit disposition, S6 records the shared CLI/MCP ADR preflight and reviewed-no-change parity, and the inventory and surface matrix reflect 20 MCP tools.
- Corrected S8's cleanup flow and added R13 after an actual integration produced a valid merge commit while `session merge-branch` reported `git commit failed: (no output)`.

### Decision

#### D1 - Keep the living storylines grounded in shipped behavior and expose false merge failures

- D: Update the user-story review from current code and memory, close R6, narrow R8, and add R13: after a commit subprocess reports failure, integration must reconcile Git state before deciding whether the merge genuinely failed or already landed.
- R: `_git_text` converts its 30-second `TimeoutExpired` into `(1, "")`, while merge commit `c79b0627` exists with both expected `Memory-Entry` trailers. Trusting only the subprocess result misreports success and skips the exact safe-cleanup path.
- A: Marking R13 resolved was rejected because the state reconciliation is not implemented. Weakening the timeout or adding raw recursive cleanup was rejected because it would erode the recorded fail-closed and exact-target cleanup rules.
- F: `docs/1_Inbox/agent-interaction-storylines-review.md` and this session record.
- T: `memory-seed docs check` passed with 16 pre-existing TODO-metadata warnings; `memory-seed docs index --check` and `git diff --check` passed.

## 2026-08-10 18:14 - Draft the Memory Seed marketing constitution

```yaml
entry_id: mse_bacxhj7wad3k0cgr
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/docs/marketing-constitution
```

### Summary

- Created a draft commercial marketing constitution around the framework "entertain the mass,
  educate the interested, and sell to the committed."
- Defined the audience, job, acceptable tactics, boundaries, formats, success evidence, and handoff
  for each stage, plus shared voice, trust, decision-test, planning, and amendment rules.
- Added the new marketing dossier to the `business/README.md` front door.

### Validation

- `git diff --check` passed; the new Markdown target exists and the business index points to it.

### Follow-up

- The constitution remains draft v0.1 pending JNL's ratification or revision; audience, channels,
  cadence, and baseline measures remain explicit open decisions.

## 2026-08-10 21:00 - Close storyline gaps with cache, MCP reads, and merge reconciliation

```yaml
entry_id: mse_sr9jqepsh66eynha
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/storyline-gap-tranche
```

### Summary

Closed the final three open findings in the living agent-interaction storylines review. R13 now
reconciles a timed-out merge commit from repository evidence, R5 now has a reconstructable core
corpus projection with independent read-only ESR health, and R8 now exposes the three approved
read-only MCP twins. The review and tranche plan were reconciled from implementation evidence; the
original refuted and stale recommendations remain distinguished from resolved work.

### Decisions

#### D1 - A failed commit subprocess is not the merge verdict

- D: After `git commit --no-edit` reports nonzero or times out, accept the merge as committed only
  when HEAD changed to the exact ordered two-parent merge for the recorded base and source tip, the
  final Git trailer block exactly matches the planned `Memory-Entry` values, and `MERGE_HEAD` is
  provably absent. Otherwise preserve the failure and inspectable merge state.
- R: The motivating integration had already created a valid merge commit while the subprocess
  result reported failure. Exact repository evidence closes that false negative without weakening
  the timeout or conflict/refusal behavior.
- A: Increasing the timeout or accepting any changed two-parent HEAD was rejected because neither
  proves this operation committed safely.
- F: `memory_seed/core.py`; `tests/test_session_fuse_and_merge.py`; `CHANGELOG.md`.
- T: Full merge/fuse module: 65 passed; closure review passed.

#### D2 - The corpus cache is disposable projection, never memory authority

- D: Persist exact raw and sidecar-augmented entry, section, and decision views outside the
  repository under a stable runtime/worktree key. Bind them to the complete source manifest, Git
  HEAD, schema, serializer, and integrity envelope; reconstruct from Markdown and sidecars on every
  missing, stale, corrupt, ambiguous, no-Git, source-motion, lease, or publication uncertainty.
  ESR independently reconstructs live source once, reports cache health without repair, and shares
  that invocation snapshot through its audits. Append paths share snapshots when useful but source
  writes never depend on cache maintenance or suggestion enrichment.
- R: Repeated canonical corpus construction cost seconds in ESR and repeated work in append flows.
  A reconstructable projection removes that waste while retaining exact source authority and a
  deterministic conflict rule: rebuild.
- A: Reusing Memory Trace as a core dependency, allowing ESR to self-certify from cached values,
  incremental delta application before exact equivalence, and cache-coupled source writes were
  rejected.
- F: `memory_seed/corpus_cache.py`; `memory_seed/retrieval.py`; `memory_seed/core.py`;
  `memory_seed/esr.py`; `memory_seed/mcp_server.py`; `memory_seed/semantic_cache.py`; focused cache,
  ESR, append, retrieval, and MCP tests.
- T: Integrated tranche suite: 343 passed and 13 subtests; related R5/R8 suite: 342 passed and 15
  subtests. Observed cache measurement retained one source build and reduced warm latency.

#### D3 - MCP parity expands only through approved read-only twins

- D: Add `memory_links_chain`, `memory_link_audit`, and `memory_esr` with closed schemas and canonical
  core/CLI payloads. Keep graph-diff snapshots and every write surface out of scope. The registry is
  now 23 tools and the authoritative mutating set remains exactly four.
- R: MCP-confined agents can now complete lifecycle recall, gap sweep, and ESR close-out without
  switching to CLI, while the governance boundary around writes remains unchanged.
- A: Duplicating formatters, exposing audit apply/dry-run controls, and adding graph-diff or ADR
  write parity were rejected.
- F: `memory_seed/mcp_server.py`; `memory_seed/retrieval.py`; `memory_seed/cli.py`;
  `tests/test_mcp_read_parity.py`; `README.md`; `CHANGELOG.md`.
- T: Focused MCP/CLI/audit/ESR/cache suite: 190 passed; closure review passed.

### Validation

- Integrated R13/R5/R8 suite: 343 passed, 13 subtests.
- Documentation lifecycle: 209 files checked; indexes current; 16 pre-existing incomplete-TODO
  metadata warnings remain non-blocking.
- Repository-wide run: 1,476 passed; remaining failures/errors share a pre-existing frozen ADR
  fixture SHA drift. A separate Windows Git shallow-clone test still hits the known signal-pipe
  environment failure.

### Outcome

The storylines review now has no open R-items: R5, R8, and R13 are resolved; R1 and R2 remain
refuted, and R12 remains stale. The two unrelated validation debts are recorded in the completed
tranche plan rather than misclassified as regressions in this work.
