---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-08
---

## 2026-08-08 02:13 - Five ADR titles were invalid YAML: quote on write, and lint the frontmatter

```yaml
entry_id: mse_vsmzwb4xqq1cdv0g
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/adr-frontmatter-quoting
topics:
  area:
    - schema:d1
    - schema:d2
  activity:
    - bugfix:d1
    - feature-build:d2
evolves:
  - d1 -> mse_7gw3cmhcz2d3n6qp:d1
```

### Summary

- JNL hit "Failed to parse frontmatter" in the Trace UI. Five ADR titles contain `": "`, which YAML
  reads as a nested mapping; `render_adr` wrote titles unquoted. Four of the five predate today.
- Fixed at the writer (quote only when needed, so the corpus written before stays byte-identical),
  repaired the five, and added a frontmatter linter to `adr check` and the fuse.

### Decisions

#### D1 - Quote a frontmatter scalar when YAML needs it, and only then

- D: `render_adr` renders the title through `_yaml_scalar()`, which quotes a value that contains
  `": "` or `" #"`, ends in a colon, opens with a YAML indicator, or carries edge whitespace. The
  five affected files were repaired by re-rendering, with the repair refusing to write if the diff
  touched anything but the title line.
- R: Our own reader is a regex (`_scalar`) and accepts frontmatter no YAML parser would, so five
  broken titles sat in the corpus unnoticed - `adr check` was green throughout. The Trace UI parses
  properly and failed. Quoting conditionally rather than always is what keeps every record written
  before this byte-identical, which matters because three ADRs are sha256-pinned by the codex
  fixtures. Re-rendering IS the repair: the contract already calls metadata repair an explicit
  file-repair operation rather than a lifecycle event, so no ledger events were added.
- A: Quote every scalar unconditionally. Rejected - it rewrites 40 files including the three pinned
  ones, turning a five-file repair into frozen-source drift.
- A: Rename the titles to avoid colons. Rejected - the corpus should carry the title the concern
  deserves; the writer is what was wrong.
- F: `memory_seed/adr.py`, five files under `.memory-seed/decisions/`
- T: All 40 ADRs now parse under a real YAML parser (0 failures, was 5); pinned three
  byte-unchanged; titles read back with their colons intact.

#### D2 - Lint the raw frontmatter, because the canonical check names the wrong half

- D: `frontmatter_issues()` lints raw frontmatter for unquoted risky scalars, tabs, duplicate keys
  and missing fences, naming the cause ("must be quoted: it contains ': ', which YAML reads as a
  nested mapping"). Wired into `check_adrs` and into the fuse's ADR path, which refuses before
  parsing.
- R: Once the writer quoted correctly, the existing byte-canonical comparison did catch all five -
  but only as "derived Current view is stale", which points at the ledger when the fault is in the
  header. A check that detects without diagnosing costs the next reader the same hour it cost me.
  The linter is hand-rolled because `model2vec` is the package's only dependency, so no YAML
  library is available to validate with; hand-rolling the risk cases is the price of that
  constraint and is why the rule list is explicit rather than delegated.
- A: Add PyYAML as a dependency to validate properly. Rejected - one required dependency is a
  standing decision, and this check needs five string tests.
- F: `memory_seed/adr.py`, `memory_seed/core.py`, `tests/test_adr_contract_extension.py`
- T: Four new tests - a risky title quotes and round-trips, a safe title stays bare (the
  byte-stability guarantee), the linter names each cause, and every live ADR passes. Full suite
  1118.

### Facts

- Detection existed and diagnosis did not: `adr check` would have flagged these the moment the
  writer was fixed, but as a stale-view error. The linter exists for the message, not the catch.
- The same leniency may affect other sidecar families - link sidecars carry a free-text `note:`
  that could contain `": "`. Not investigated here; worth a sweep.

### Follow-ups

- Sweep link, topic and diagram sidecars for the same unquoted-scalar exposure, particularly
  `note:` values.

## 2026-08-08 02:29 - Twelve chains founded: every lineage chain now has an ADR

```yaml
entry_id: mse_g9xegd9ct9sns5s4
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/found-remaining-chains
topics:
  area:
    - decision-harvest:d1
    - decision-harvest:d2
  activity:
    - feature-build:d1
    - process-correction:d2
evolves:
  - d1 -> mse_7gw3cmhcz2d3n6qp:d1
```

### Summary

- The remaining 12 unclaimed chains are founded as `proposed` ADRs. Unclaimed chains fall 12 -> 0;
  the corpus is 52 ADRs and every lineage chain of 3+ decisions now has a concern record.
- Three haiku workers drafted; mechanical validation grounded 6 of 12 on the first pass, and the
  other 6 were recovered by supplying quotes myself.

### Decisions

#### D1 - Found all twelve, heads on the newest chain member

- D: Twelve ADRs written `proposed`, each headed on its chain's newest decision with the remaining
  members as `supporting_decisions`. The three `docs-lifecycle` chains were founded as three
  separate concerns - inbox promotion, documentation lanes, and completion moving lane - rather
  than merged on their shared area.
- R: A shared area is not a shared concern; the chains are separately connected in the lineage
  graph, which is the evidence that they are three threads rather than one. Supporting rather than
  predecessors again, because a predecessor needs a link assertion matching a real edge between
  that exact pair and these chains connect members to each other rather than all to the head.
- F: `.memory-seed/decisions/` (12 new), `experiments/adr-campaign/write_chain_adrs.py`,
  `found_chain_adrs.py`, `founding-brief.md`, `chain-draft-*.json`
- T: `adr check` green; 52/52 ADRs parse under a real YAML parser; unclaimed chains 12 -> 0; the
  three pinned ADRs byte-unchanged; full suite 1118.

#### D2 - The workers chose well and proved it badly, so the proof is mine and says so

- D: Six drafts failed grounding - two cited a `quote_ref` outside their chain's member list, four
  quoted text that is not a verbatim span. Rather than drop them, I read each head myself, supplied
  a verified quote, and recorded on each record that the quote is the orchestrator's while the
  selection is the worker's.
- R: This is the third run with the same signature: the SELECTIONS were sound and the EVIDENCE was
  not. Dropping six good concerns to punish six bad quotes would have cost the corpus more than it
  protected, but silently substituting my quote would make my judgement wear the swarm's
  provenance - the rule the 2026-07-27 campaign set. Saying whose quote it is on the record keeps
  both. The two length outliers (182 and 39 chars) were kept and reported, per the rule that
  grounding decides and length never does.
- A: Drop the six and re-run the swarm. Rejected - a fourth attempt at the same failure mode is not
  a measurement, and the reading was already done to check them.
- F: `experiments/adr-campaign/chain-draft-*.json`, `write_chain_adrs.py`
- T: 6/12 grounded before repair, 12/12 after; every `quote_ref` a verified chain member; every
  quote a verbatim span of the decision it cites.

### Facts

- Unclaimed chains are now 0, so the chain report's ADR-candidate section is empty until new
  lineage accumulates. That is the plateau the governance model predicts, reached for chains of 3+.
- All 52 are `proposed` except the pre-existing accepted ones; acceptance remains JNL's gate.

### Follow-ups

- 12 ADRs from this batch plus 2 from the previous one await acceptance.
- Chains of exactly 2 decisions were never in scope; worth deciding whether they are concerns.

## 2026-08-08 15:23 - Pairs are candidates, growth is a review gate, and the gate caught my own omissions

```yaml
entry_id: mse_sgj06gq2wga3900f
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/chain-growth-and-pairs
topics:
  area:
    - decision-harvest:d1
    - decision-harvest:d2
    - lifecycle-edges:d3
  activity:
    - feature-build:d1
    - process-correction:d2
    - feature-build:d3
evolves:
  - d1 -> mse_g9xegd9ct9sns5s4:d1
```

### Summary

- Two-decision chains are now reported as CANDIDATE PAIRS, held to a higher bar than chains: 26
  evaluated, 3 founded, 23 correctly declined.
- Chain growth is a standing re-review gate: a chain whose claiming ADR does not name every member
  has outgrown its review. It fired on 5 chains, two of which were my own omissions.

### Decisions

#### D1 - A pair is a candidate, not a chain, and most pairs are not concerns

- D: Pairs get their own report section and their own bar. The brief tells evaluators that
  `architectural: false` is a good answer, and asks them to found only what a reader would expect
  recorded as a standing concern. 26 unclaimed pairs evaluated: 3 founded, 23 declined.
- R: A pair is two decisions sharing one edge and one area, which is much weaker evidence than a
  chain - it may be a concern, or may just be two decisions in the same neighbourhood. Making
  refusal the easy answer is what stops the mechanism inflating the corpus, because a weak ADR is
  worse than a missing one: it claims a concern exists where none does, and every later decision
  has to route around it. The 88% decline rate is the mechanism working rather than failing.
- A: Found every pair that shares an area, as chains of 3+ were founded. Rejected on the evidence
  below - two of the four positive verdicts were wrong, and both would have damaged the corpus.
- F: `experiments/adr-campaign/lineage_chains.py`, `pair-brief.md`, `pair-verdict-*.json`,
  `chain-draft-pairs.json`
- T: 3 written `proposed` - `adr_append_rule_is_an_invariant`, `adr_diagram_sidecar_structure`,
  `adr_lifecycle_link_audit_workflow`. Corpus 52 -> 55; `adr check` green; 1118 tests.

#### D2 - Two worker verdicts were rejected on knowledge the workers could not have had

- D: `adr_append_only_link_authoring` and `adr_sidecar_positive_triggers` were declined despite
  passing the brief, and a third was re-framed rather than accepted as drafted.
- R: The first duplicates `adr_forward_only_edges`, which already records forward-only authoring -
  a duplicate concern is exactly the harm the pair bar exists to prevent. The second records the
  per-entry diagram trigger list as a positive requirement, and that rule was RETIRED on
  2026-08-07 in favour of ADR attachment earning a diagram; founding it would have written a
  superseded position into the corpus as current. Neither worker could have known: one would need
  the whole ADR corpus in context, the other a decision made hours earlier. That is the structural
  limit of a bounded worker, and the reason the orchestrator reads verdicts rather than counting
  them. The third had the right pick and the wrong frame - its title restated Constitution
  Invariant #2, when the concern is how that rule is EXPRESSED to agents: mandating shell append
  assumed a writing surface not every agent has.
- A: Accept all four positive verdicts. Rejected - two would have been wrong in ways the corpus
  would then carry indefinitely.
- F: `.memory-seed/decisions/adr_append_rule_is_an_invariant.md`,
  `experiments/adr-campaign/pair-verdict-1.json`
- T: Duplicate check run against the live ADR corpus by id and title before writing any pair ADR.

#### D3 - A chain that outgrows its ADR is a stale review, and the check found my own errors first

- D: `lineage_chains.py` reports GROWN chains - a chain some ADR claims that carries members the
  ADR does not name. Each is a stale review in one of two ways: the new decisions belong to the
  concern, or the chain has grown into a second concern that should split off.
- R: JNL asked for subsequent review gates as chains grow. Membership is already mechanically
  determinable, so the gate needs no new state - the diff between a chain and its claimant's named
  refs IS the signal. It fired immediately on 5 chains, and the two worst were mine:
  `adr_mcp_integration_surface` named 9 of its chain's 11 because I dropped two when writing the
  supporting list by hand, and `adr_merge_branch_primitive` named 1 of 8 because it was founded
  from a lineage pass carrying only one related decision. A check that catches the author's own
  omission on the day he makes it is worth more than one that catches a stranger's.
- A: Track a reviewed-size field on the ADR. Rejected - it would need maintaining, and the
  membership diff already answers the question from data that exists.
- F: `experiments/adr-campaign/lineage_chains.py`,
  `.memory-seed/decisions/adr_mcp_integration_surface.md`,
  `.memory-seed/decisions/adr_merge_branch_primitive.md`
- T: GROWN fell 5 -> 3 after attaching the 9 omitted members as soft `context-added` events, which
  add evidence without moving a head; the remaining 3 are genuine judgement calls.

### Facts

- Pair founding rate was 3 of 26 (12%), against 12 of 12 for chains of 3+. The size threshold
  carries real signal rather than being an arbitrary cutoff.
- The omitted-member fixes used `context-added`, not a head move: they attach evidence the ADR
  already covers in substance, and neither changes what the concern currently rests on.

### Follow-ups

- Three chains still show as grown - `adr_trace_boundary` (+2), `adr_control_file_authority` (+1),
  `adr_topic_backfill_rejected` (+1). These are judgement calls for JNL, not omissions.
- 17 ADRs now await acceptance.

## 2026-08-08 16:46 - The fuse handles all five sidecar families: ADRs were the missing one

```yaml
entry_id: mse_x1ag0r38wr2cfym3
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/fuse-recognises-adrs
topics:
  area:
    - session-fuse:d1
  activity:
    - bugfix:d1
evolves:
  - d1 -> mse_sgj06gq2wga3900f:d3
```

### Summary

- JNL asked for `session merge-branch` to handle ADRs, topics, links and diagrams mechanically.
  Three of the four already were; ADRs were the gap, and only for MODIFIED records.
- One predicate closes it. The recognizer's own test already said its set must equal the fuse's
  handled set "or fails to eat the next gap" - ADRs were that gap.

### Decision

#### D1 - The reset guard recognises ADR records, closing the added-versus-modified asymmetry

- D: `_is_recognized_session_tree_path` gains `_is_adr_relative_path`, matching `.md` files under
  `.memory-seed/decisions/`. The refusal message widens to name the ADR classifier.
- R: The fuse already handled ADRs everywhere except this guard: `_changed_session_paths` has
  scoped `decisions/` from the start, and the apply step writes reconciled records back through
  `reconcile_adr_records`. The recognizer alone did not know the tree, and it guards the loop that
  resets changed paths to base before the fuse re-applies them. That produced a silent asymmetry -
  an ADDED ADR is not in `base_paths`, so it never reaches the reset loop and merges fine, while a
  MODIFIED one hits the guard and is refused. Twelve new ADRs merged cleanly this session and two
  revised ones did not, which is the asymmetry rather than two different bugs. The refusal was
  correct behaviour throughout: resetting an unrecognised path to base would discard branch work,
  which is exactly what the guard exists to prevent. The fix is to recognise the family, not to
  weaken the guard.
- A: Special-case `decisions/` in the reset loop instead. Rejected - the recognizer is deliberately
  one function so the guard's set and the fuse's handled set cannot drift, and a second predicate
  elsewhere would restart that drift.
- F: `memory_seed/core.py` (`_is_recognized_session_tree_path`, `_is_adr_relative_path`,
  `_unfusable_session_path_reason`), `tests/test_session_fuse_and_merge.py`
- T: The families test now asserts ADR paths recognised and non-`.md` under `decisions/` refused,
  keeping `.memory-seed/sessions/decisions/` unrecognised so the defence-in-depth test still has a
  genuine unknown to refuse. A dry-run merge of a branch carrying a modified ADR reports the import
  rather than refusing. Fuse suite 54 passed; full suite 1118.

### Facts

- Families the fuse now handles mechanically: session entries, diagrams, links, topics, ADRs.
- The wording change broke one assertion that matched the old message verbatim; the guard's
  behaviour was unchanged, only its text.
- Caught late: a `pytest ... | tail -3` pipeline reports tail's exit code, so a chained `&&`
  commit ran over a failing test. Check the suite's own exit code when gating on it.
