---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-08
---

## 2026-08-08 02:13 - Five ADR titles were invalid YAML: quote on write, and lint the frontmatter

```yaml
entry_id: mse_vsmzwb4xqq1cdv0g
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/adr-frontmatter-quoting
topics:
  area:
    - schema:d1
    - schema:d2
  activity:
    - bugfix:d1
    - feature-build:d2
evolves:
  - d1 -> mse_7gw3cmhcz2d3n6qp:d1
```

### Summary

- JNL hit "Failed to parse frontmatter" in the Trace UI. Five ADR titles contain `": "`, which YAML
  reads as a nested mapping; `render_adr` wrote titles unquoted. Four of the five predate today.
- Fixed at the writer (quote only when needed, so the corpus written before stays byte-identical),
  repaired the five, and added a frontmatter linter to `adr check` and the fuse.

### Decisions

#### D1 - Quote a frontmatter scalar when YAML needs it, and only then

- D: `render_adr` renders the title through `_yaml_scalar()`, which quotes a value that contains
  `": "` or `" #"`, ends in a colon, opens with a YAML indicator, or carries edge whitespace. The
  five affected files were repaired by re-rendering, with the repair refusing to write if the diff
  touched anything but the title line.
- R: Our own reader is a regex (`_scalar`) and accepts frontmatter no YAML parser would, so five
  broken titles sat in the corpus unnoticed - `adr check` was green throughout. The Trace UI parses
  properly and failed. Quoting conditionally rather than always is what keeps every record written
  before this byte-identical, which matters because three ADRs are sha256-pinned by the codex
  fixtures. Re-rendering IS the repair: the contract already calls metadata repair an explicit
  file-repair operation rather than a lifecycle event, so no ledger events were added.
- A: Quote every scalar unconditionally. Rejected - it rewrites 40 files including the three pinned
  ones, turning a five-file repair into frozen-source drift.
- A: Rename the titles to avoid colons. Rejected - the corpus should carry the title the concern
  deserves; the writer is what was wrong.
- F: `memory_seed/adr.py`, five files under `.memory-seed/decisions/`
- T: All 40 ADRs now parse under a real YAML parser (0 failures, was 5); pinned three
  byte-unchanged; titles read back with their colons intact.

#### D2 - Lint the raw frontmatter, because the canonical check names the wrong half

- D: `frontmatter_issues()` lints raw frontmatter for unquoted risky scalars, tabs, duplicate keys
  and missing fences, naming the cause ("must be quoted: it contains ': ', which YAML reads as a
  nested mapping"). Wired into `check_adrs` and into the fuse's ADR path, which refuses before
  parsing.
- R: Once the writer quoted correctly, the existing byte-canonical comparison did catch all five -
  but only as "derived Current view is stale", which points at the ledger when the fault is in the
  header. A check that detects without diagnosing costs the next reader the same hour it cost me.
  The linter is hand-rolled because `model2vec` is the package's only dependency, so no YAML
  library is available to validate with; hand-rolling the risk cases is the price of that
  constraint and is why the rule list is explicit rather than delegated.
- A: Add PyYAML as a dependency to validate properly. Rejected - one required dependency is a
  standing decision, and this check needs five string tests.
- F: `memory_seed/adr.py`, `memory_seed/core.py`, `tests/test_adr_contract_extension.py`
- T: Four new tests - a risky title quotes and round-trips, a safe title stays bare (the
  byte-stability guarantee), the linter names each cause, and every live ADR passes. Full suite
  1118.

### Facts

- Detection existed and diagnosis did not: `adr check` would have flagged these the moment the
  writer was fixed, but as a stale-view error. The linter exists for the message, not the catch.
- The same leniency may affect other sidecar families - link sidecars carry a free-text `note:`
  that could contain `": "`. Not investigated here; worth a sweep.

### Follow-ups

- Sweep link, topic and diagram sidecars for the same unquoted-scalar exposure, particularly
  `note:` values.

## 2026-08-08 02:29 - Twelve chains founded: every lineage chain now has an ADR

```yaml
entry_id: mse_g9xegd9ct9sns5s4
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/found-remaining-chains
topics:
  area:
    - decision-harvest:d1
    - decision-harvest:d2
  activity:
    - feature-build:d1
    - process-correction:d2
evolves:
  - d1 -> mse_7gw3cmhcz2d3n6qp:d1
```

### Summary

- The remaining 12 unclaimed chains are founded as `proposed` ADRs. Unclaimed chains fall 12 -> 0;
  the corpus is 52 ADRs and every lineage chain of 3+ decisions now has a concern record.
- Three haiku workers drafted; mechanical validation grounded 6 of 12 on the first pass, and the
  other 6 were recovered by supplying quotes myself.

### Decisions

#### D1 - Found all twelve, heads on the newest chain member

- D: Twelve ADRs written `proposed`, each headed on its chain's newest decision with the remaining
  members as `supporting_decisions`. The three `docs-lifecycle` chains were founded as three
  separate concerns - inbox promotion, documentation lanes, and completion moving lane - rather
  than merged on their shared area.
- R: A shared area is not a shared concern; the chains are separately connected in the lineage
  graph, which is the evidence that they are three threads rather than one. Supporting rather than
  predecessors again, because a predecessor needs a link assertion matching a real edge between
  that exact pair and these chains connect members to each other rather than all to the head.
- F: `.memory-seed/decisions/` (12 new), `experiments/adr-campaign/write_chain_adrs.py`,
  `found_chain_adrs.py`, `founding-brief.md`, `chain-draft-*.json`
- T: `adr check` green; 52/52 ADRs parse under a real YAML parser; unclaimed chains 12 -> 0; the
  three pinned ADRs byte-unchanged; full suite 1118.

#### D2 - The workers chose well and proved it badly, so the proof is mine and says so

- D: Six drafts failed grounding - two cited a `quote_ref` outside their chain's member list, four
  quoted text that is not a verbatim span. Rather than drop them, I read each head myself, supplied
  a verified quote, and recorded on each record that the quote is the orchestrator's while the
  selection is the worker's.
- R: This is the third run with the same signature: the SELECTIONS were sound and the EVIDENCE was
  not. Dropping six good concerns to punish six bad quotes would have cost the corpus more than it
  protected, but silently substituting my quote would make my judgement wear the swarm's
  provenance - the rule the 2026-07-27 campaign set. Saying whose quote it is on the record keeps
  both. The two length outliers (182 and 39 chars) were kept and reported, per the rule that
  grounding decides and length never does.
- A: Drop the six and re-run the swarm. Rejected - a fourth attempt at the same failure mode is not
  a measurement, and the reading was already done to check them.
- F: `experiments/adr-campaign/chain-draft-*.json`, `write_chain_adrs.py`
- T: 6/12 grounded before repair, 12/12 after; every `quote_ref` a verified chain member; every
  quote a verbatim span of the decision it cites.

### Facts

- Unclaimed chains are now 0, so the chain report's ADR-candidate section is empty until new
  lineage accumulates. That is the plateau the governance model predicts, reached for chains of 3+.
- All 52 are `proposed` except the pre-existing accepted ones; acceptance remains JNL's gate.

### Follow-ups

- 12 ADRs from this batch plus 2 from the previous one await acceptance.
- Chains of exactly 2 decisions were never in scope; worth deciding whether they are concerns.

## 2026-08-08 15:23 - Pairs are candidates, growth is a review gate, and the gate caught my own omissions

```yaml
entry_id: mse_sgj06gq2wga3900f
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/chain-growth-and-pairs
topics:
  area:
    - decision-harvest:d1
    - decision-harvest:d2
    - lifecycle-edges:d3
  activity:
    - feature-build:d1
    - process-correction:d2
    - feature-build:d3
evolves:
  - d1 -> mse_g9xegd9ct9sns5s4:d1
```

### Summary

- Two-decision chains are now reported as CANDIDATE PAIRS, held to a higher bar than chains: 26
  evaluated, 3 founded, 23 correctly declined.
- Chain growth is a standing re-review gate: a chain whose claiming ADR does not name every member
  has outgrown its review. It fired on 5 chains, two of which were my own omissions.

### Decisions

#### D1 - A pair is a candidate, not a chain, and most pairs are not concerns

- D: Pairs get their own report section and their own bar. The brief tells evaluators that
  `architectural: false` is a good answer, and asks them to found only what a reader would expect
  recorded as a standing concern. 26 unclaimed pairs evaluated: 3 founded, 23 declined.
- R: A pair is two decisions sharing one edge and one area, which is much weaker evidence than a
  chain - it may be a concern, or may just be two decisions in the same neighbourhood. Making
  refusal the easy answer is what stops the mechanism inflating the corpus, because a weak ADR is
  worse than a missing one: it claims a concern exists where none does, and every later decision
  has to route around it. The 88% decline rate is the mechanism working rather than failing.
- A: Found every pair that shares an area, as chains of 3+ were founded. Rejected on the evidence
  below - two of the four positive verdicts were wrong, and both would have damaged the corpus.
- F: `experiments/adr-campaign/lineage_chains.py`, `pair-brief.md`, `pair-verdict-*.json`,
  `chain-draft-pairs.json`
- T: 3 written `proposed` - `adr_append_rule_is_an_invariant`, `adr_diagram_sidecar_structure`,
  `adr_lifecycle_link_audit_workflow`. Corpus 52 -> 55; `adr check` green; 1118 tests.

#### D2 - Two worker verdicts were rejected on knowledge the workers could not have had

- D: `adr_append_only_link_authoring` and `adr_sidecar_positive_triggers` were declined despite
  passing the brief, and a third was re-framed rather than accepted as drafted.
- R: The first duplicates `adr_forward_only_edges`, which already records forward-only authoring -
  a duplicate concern is exactly the harm the pair bar exists to prevent. The second records the
  per-entry diagram trigger list as a positive requirement, and that rule was RETIRED on
  2026-08-07 in favour of ADR attachment earning a diagram; founding it would have written a
  superseded position into the corpus as current. Neither worker could have known: one would need
  the whole ADR corpus in context, the other a decision made hours earlier. That is the structural
  limit of a bounded worker, and the reason the orchestrator reads verdicts rather than counting
  them. The third had the right pick and the wrong frame - its title restated Constitution
  Invariant #2, when the concern is how that rule is EXPRESSED to agents: mandating shell append
  assumed a writing surface not every agent has.
- A: Accept all four positive verdicts. Rejected - two would have been wrong in ways the corpus
  would then carry indefinitely.
- F: `.memory-seed/decisions/adr_append_rule_is_an_invariant.md`,
  `experiments/adr-campaign/pair-verdict-1.json`
- T: Duplicate check run against the live ADR corpus by id and title before writing any pair ADR.

#### D3 - A chain that outgrows its ADR is a stale review, and the check found my own errors first

- D: `lineage_chains.py` reports GROWN chains - a chain some ADR claims that carries members the
  ADR does not name. Each is a stale review in one of two ways: the new decisions belong to the
  concern, or the chain has grown into a second concern that should split off.
- R: JNL asked for subsequent review gates as chains grow. Membership is already mechanically
  determinable, so the gate needs no new state - the diff between a chain and its claimant's named
  refs IS the signal. It fired immediately on 5 chains, and the two worst were mine:
  `adr_mcp_integration_surface` named 9 of its chain's 11 because I dropped two when writing the
  supporting list by hand, and `adr_merge_branch_primitive` named 1 of 8 because it was founded
  from a lineage pass carrying only one related decision. A check that catches the author's own
  omission on the day he makes it is worth more than one that catches a stranger's.
- A: Track a reviewed-size field on the ADR. Rejected - it would need maintaining, and the
  membership diff already answers the question from data that exists.
- F: `experiments/adr-campaign/lineage_chains.py`,
  `.memory-seed/decisions/adr_mcp_integration_surface.md`,
  `.memory-seed/decisions/adr_merge_branch_primitive.md`
- T: GROWN fell 5 -> 3 after attaching the 9 omitted members as soft `context-added` events, which
  add evidence without moving a head; the remaining 3 are genuine judgement calls.

### Facts

- Pair founding rate was 3 of 26 (12%), against 12 of 12 for chains of 3+. The size threshold
  carries real signal rather than being an arbitrary cutoff.
- The omitted-member fixes used `context-added`, not a head move: they attach evidence the ADR
  already covers in substance, and neither changes what the concern currently rests on.

### Follow-ups

- Three chains still show as grown - `adr_trace_boundary` (+2), `adr_control_file_authority` (+1),
  `adr_topic_backfill_rejected` (+1). These are judgement calls for JNL, not omissions.
- 17 ADRs now await acceptance.

## 2026-08-08 16:46 - The fuse handles all five sidecar families: ADRs were the missing one

```yaml
entry_id: mse_x1ag0r38wr2cfym3
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/fuse-recognises-adrs
topics:
  area:
    - session-fuse:d1
  activity:
    - bugfix:d1
evolves:
  - d1 -> mse_sgj06gq2wga3900f:d3
```

### Summary

- JNL asked for `session merge-branch` to handle ADRs, topics, links and diagrams mechanically.
  Three of the four already were; ADRs were the gap, and only for MODIFIED records.
- One predicate closes it. The recognizer's own test already said its set must equal the fuse's
  handled set "or fails to eat the next gap" - ADRs were that gap.

### Decision

#### D1 - The reset guard recognises ADR records, closing the added-versus-modified asymmetry

- D: `_is_recognized_session_tree_path` gains `_is_adr_relative_path`, matching `.md` files under
  `.memory-seed/decisions/`. The refusal message widens to name the ADR classifier.
- R: The fuse already handled ADRs everywhere except this guard: `_changed_session_paths` has
  scoped `decisions/` from the start, and the apply step writes reconciled records back through
  `reconcile_adr_records`. The recognizer alone did not know the tree, and it guards the loop that
  resets changed paths to base before the fuse re-applies them. That produced a silent asymmetry -
  an ADDED ADR is not in `base_paths`, so it never reaches the reset loop and merges fine, while a
  MODIFIED one hits the guard and is refused. Twelve new ADRs merged cleanly this session and two
  revised ones did not, which is the asymmetry rather than two different bugs. The refusal was
  correct behaviour throughout: resetting an unrecognised path to base would discard branch work,
  which is exactly what the guard exists to prevent. The fix is to recognise the family, not to
  weaken the guard.
- A: Special-case `decisions/` in the reset loop instead. Rejected - the recognizer is deliberately
  one function so the guard's set and the fuse's handled set cannot drift, and a second predicate
  elsewhere would restart that drift.
- F: `memory_seed/core.py` (`_is_recognized_session_tree_path`, `_is_adr_relative_path`,
  `_unfusable_session_path_reason`), `tests/test_session_fuse_and_merge.py`
- T: The families test now asserts ADR paths recognised and non-`.md` under `decisions/` refused,
  keeping `.memory-seed/sessions/decisions/` unrecognised so the defence-in-depth test still has a
  genuine unknown to refuse. A dry-run merge of a branch carrying a modified ADR reports the import
  rather than refusing. Fuse suite 54 passed; full suite 1118.

### Facts

- Families the fuse now handles mechanically: session entries, diagrams, links, topics, ADRs.
- The wording change broke one assertion that matched the old message verbatim; the guard's
  behaviour was unchanged, only its text.
- Caught late: a `pytest ... | tail -3` pipeline reports tail's exit code, so a chained `&&`
  commit ran over a failing test. Check the suite's own exit code when gating on it.

## 2026-08-08 16:55 - Reverted: the fuse cannot rebuild a modified ADR, so refusing was right

```yaml
entry_id: mse_9szn9geevrgdntng
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/revert-adr-fuse-recognition
topics:
  area:
    - session-fuse:d1
  activity:
    - process-correction:d1
replaces:
  - d1 -> mse_x1ag0r38wr2cfym3:d1
```

### Summary

- The ADR recognition added an hour earlier is REVERTED. Teaching the reset guard to accept ADR
  paths caused a merge to silently lose a branch-modified ADR - the exact data loss the guard
  exists to prevent.
- The refusal is now pinned by a test that says WHY, so the next reader does not repeat the fix.

### Decision

#### D1 - Recognising a path asserts the fuse can rebuild it, and for a modified ADR that is false

- D: `_is_recognized_session_tree_path` no longer accepts `.memory-seed/decisions/*.md`;
  `_is_adr_relative_path` is removed and the refusal message returns to its original wording. The
  families test now asserts ADR paths are NOT recognised, with the measurement in a comment.
- R: Measured end to end rather than reasoned about. A probe branch added one `context-added` event
  to `adr_edge_kinds`, and the merge reported success while the merged file did not contain the
  event: probe branch 1 occurrence, merge result 0. The recognizer is not a naming convention - it
  is an assertion that the fuse can REBUILD the path from parsed records after the base reset, and
  for a modified ADR that assertion is false, so the reset discards branch work and nothing puts it
  back. The original refusal was therefore correct, and my reading of it as an "asymmetry between
  added and modified ADRs" mistook the symptom for the bug. The asymmetry is real but benign: an
  added ADR is not in `base_paths`, so it never reaches the reset loop, which is why 12 new ADRs
  merged cleanly.
- A: Keep the recognition and fix the apply path in the same change. Rejected for sequencing, not
  merit: the loss is live on `main` the moment the guard opens, so the guard closes first and the
  apply path is fixed on its own evidence. That is the real work JNL asked for and it remains open.
- F: `memory_seed/core.py`, `tests/test_session_fuse_and_merge.py`
- T: Probe reproduced the loss before the revert and the refusal after it; fuse suite 54 passed;
  full suite 1118 with the suite's own exit code checked, not a pipeline's.

### Facts

- Families the fuse genuinely rebuilds: session entries, diagrams, links, topics. ADRs are handled
  for ADDED files only; a modified ADR must still be merged by hand.
- The probe merge and its revert are in history; `adr_edge_kinds` is byte-identical to its
  pre-probe state.
- Two process errors on the way, both worth keeping: `pytest | tail -3` returns tail's exit code,
  so a chained `&&` commit ran over a failing test; and a dry-run merge executed from a tree
  WITHOUT the fix proved nothing, which is why the real proof needed the fix on main first.

### Follow-ups

- The actual request stands: make the ADR apply path restore a reconciled record after the base
  reset, then re-open the recognizer with this probe as its regression test.

## 2026-08-08 17:56 - ADRs merge mechanically: the fuse never staged its own ADR writes

```yaml
entry_id: mse_ath81hrh9t3wzw6y
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/fuse-stages-adr-writes
topics:
  area:
    - session-fuse:d1
  activity:
    - bugfix:d1
replaces:
  - d1 -> mse_9szn9geevrgdntng:d1
```

### Summary

- Root cause found: the fuse wrote reconciled ADR records into the working tree and then staged
  only `sessions/`, so `git commit` took the index - still base content from the reset - and the
  branch's ADR event vanished. One pathspec.
- With staging fixed, ADR recognition is back on and a branch-modified ADR now survives the merge,
  proven by the same probe that lost data before.

### Decision

#### D1 - Stage what the fuse writes, and only then let the recognizer claim the family

- D: `_fuse_stage_pathspecs()` returns `sessions/` plus `decisions/` when the project has any, used
  by both staging call sites; `_is_adr_relative_path` is restored to the recognizer. The pathspec is
  conditional because one matching nothing makes `git add` fail, which broke 15 fixture tests that
  have no ADRs.
- R: JNL proposed an ADR version number, auto-incremented on modification, with the higher version
  surviving and the older archived. Investigating before building found the defect was a staging
  omission rather than a merge-semantics gap, and that matters because versioning would have been
  the WRONG shape here: an ADR is an append-only event ledger, and `reconcile_adr_records` already
  unions events by id, so a branch appending X and another appending Y keeps BOTH. Higher-version-
  wins would discard one side's events wholesale - data loss where union is exactly correct. Where
  reconciliation genuinely cannot merge (competing acceptances, divergent identity fields) it
  already refuses and asks, which for an authority record beats resolving automatically by
  discarding. The half of JNL's idea that has real force - a branch must not change an ADR
  invisibly - is already delivered by content-hashed event ids: same id with different content is
  detected as divergence, not silently taken.
- A: Add a version field with higher-wins resolution. Rejected on the reasoning above; the append-
  only union is strictly stronger for this record type.
- A: Land recognition and staging separately. Rejected - recognition without staging is precisely
  the data loss reverted an hour earlier, so the two are one change.
- F: `memory_seed/core.py`, `tests/test_session_fuse_and_merge.py`
- T: The probe that lost its event before now keeps it - merge result contains the branch's event
  where the parent commit does not. Fuse suite 54; full suite 1118, reading the suite's own exit
  code. Probe reverted afterwards, `adr_edge_kinds` byte-identical to its pre-probe state.

### Facts

- All five families now merge mechanically: session entries, diagrams, links, topics, ADRs -
  added or modified.
- The reverted recognizer from 16:55 is reinstated here, with its docstring naming the staging
  precondition so the next reader does not open it again on a broken apply path.

### Follow-ups

- The 17 proposed ADRs still await JNL's acceptance; nothing in this fix touches their status.

## 2026-08-08 18:08 - The ADR summary now shows revisions awaiting review, so concurrent branches are visible

```yaml
entry_id: mse_6a4tcm6acefzwe4a
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/adr-awaiting-review-view
topics:
  area:
    - schema:d1
  activity:
    - feature-build:d1
evolves:
  - d1 -> mse_ath81hrh9t3wzw6y:d1
```

### Summary

- The Current view renders an `### Awaiting review` list of revisions proposed but not yet
  accepted, other than the one on display.
- Answers JNL's question directly: the summary was already never STALE, but it showed only the
  accepted head, so two branches could each propose a change and the summary would report the old
  position with no sign either existed.

### Decision

#### D1 - Surface pending revisions in the summary, omit-empty so nothing else moves

- D: `render_adr` lists each pending revision key not already displayed, with a one-line gist of
  its decision, inside the derived Current view block.
- R: Measured before building. Two synthetic branches each appended a `revision-proposed` to
  `adr_edge_kinds`; `reconcile_adr_records` merged them with zero issues and replay held BOTH in
  `pending_decisions` - so the merge semantics were already right. What was missing was visibility:
  `current_proposal()` returns the accepted proposal when one exists, so the view rendered the
  founding position and neither branch appeared anywhere a reader looks. The pending set already
  existed in replay; this only puts it where the summary is read. Excluding the displayed revision
  is what keeps the change inert for the existing corpus - measured at 0 of 55 ADRs affected, so
  every record including the three sha256-pinned ones renders byte-identically.
- A: Show every pending revision including the displayed one. Rejected - it would duplicate the
  Decision section on all 17 proposed ADRs and rewrite 55 files for no added information.
- A: A version number with higher-version-wins, as JNL proposed for the merge problem. Rejected
  there and not revived here: the summary is DERIVED from the ledger, so it cannot drift from the
  merged state, and a version field would add a second thing to keep true.
- F: `memory_seed/adr.py` (`render_adr`, `_first_sentence`),
  `tests/test_adr_contract_extension.py`
- T: Two branches proposing against one ADR reconcile cleanly and both appear under Awaiting
  review while the head stays put; an ADR whose only pending revision is the displayed one omits
  the section; every live ADR still renders to its own bytes. Full suite 1121; pinned three
  unchanged.

### Facts

- The Current view has always been regenerated from the ledger and byte-checked by `adr check`, so
  staleness was never the risk - incompleteness was.
- `pending_decisions` was already computed by `replay_adr`; no new state was introduced.

## 2026-08-08 18:52 - Founding-only ADRs get real decisions: semantic recall found what topic recall could not

```yaml
entry_id: mse_kqna9hegj35dwsqj
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/attach-founding-adrs
topics:
  area:
    - decision-harvest:d1
    - governance-profile:d2
  activity:
    - design-evaluation:d1
    - design-evaluation:d2
replaces:
  - d1 -> mse_gm39dmzzs4nbysm1:d2
evolves:
  - d2 -> mse_g9xegd9ct9sns5s4:d2
```

### Summary

- JNL asked whether every ADR now carries at least one decision. It does not: **29 of 55 rest on a
  `founding:` control-file line**, 15 with nothing attached at all and 14 carrying only soft
  context. The earlier answer of 15 counted only the stricter set.
- Twenty-two of those 29 now carry a real session decision as a proposed head, each one grounded in
  a verbatim quote and survived by an independent refutation pass. Seven keep their founding line,
  which for a concern older than the session log is the honest record.

### Decisions

#### D1 - Candidates for a founding-only ADR come from semantic recall over its own concern text

- D: `build_attach_payload.py` queries the corpus with a MECHANICAL query built from the ADR's
  title, topics and founding quote, takes the top 25 with `supersession_damping` OFF, and renders
  each candidate's decision BODY inline in the worker payload.
- R: The first attempt used topic-area recall and returned 13 `none` out of 15. Validation showed
  the workers were right and the LISTS were wrong: one worker named the true founding decision for
  `adr_branch_session_fuse` and observed it was not on offer. A semantic query ranks that decision
  first at 34.18 against 26.23 for the runner-up. Held out against the eight ADRs that already
  carry a real head, the query recovers 8/8 - but that arm is contaminated, because `revise_adr`
  copies an attached decision's own text into the record, so it searches with the answer. The
  leak-free arm (title and topics only, no decision prose) gives 5/8 @5, 7/8 @10 and 8/8 @25, and
  that is what sets K=25. It is a strict lower bound: the 29 targets carry long verbatim founding
  quotes where those eight carry none. Damping is off because it demotes anything with a
  `replaced_by`, and a founding decision is by construction the most-superseded node in its chain.
- A: Keep topic recall and widen the area filter. Rejected - the failure was not the filter's width
  but the signal: topics were measured months ago to work for recall and fail for ranking, and a
  founding decision often sits in a different area from the concern it founded.
- A: Read every decision in the corpus per ADR. Rejected on cost with no measured gain over K=25.
- F: `experiments/adr-campaign/recall_probe.py`, `build_attach_payload.py`,
  `validate_attach.py`
- T: Recall probe over the eight attached ADRs, three arms, reported per-K. Grounding validation
  over all 29 answers: 25 primary, 2 honest `none`, 2 dropped where the quote was not a verbatim
  span of the decision cited.

#### D2 - A machine-chosen ADR head must survive an adversarial refutation before it is written

- D: Every grounded pick goes to a second worker prompted to REFUTE it, holding the same candidate
  list so "a different candidate founds it" and "nothing here founds it" are both available.
  `adjudicate_attach.py` joins the two passes; only `stands` is written.
- R: 25 of 29 came back positive. That rate is the signal to distrust, not to celebrate: a worker
  handed 25 semantically ranked candidates will find something plausible in every list, and
  grounding proves only that the quote is real, never that the right decision was chosen. The
  refutation moved 8 of the 25. Four were replaced by a decision that INSTITUTES the rule where the
  claim only proposed or applied it - `adr_edge_kinds` moved off a decision that merely drafted a
  proposal for a fourth edge kind onto the one that set `DEFAULT_GRAPH_EDGE_TYPES`. Four more were
  refuted with no alternative, which is the correct answer for a concern that predates the session
  log. This also honours the standing rule that machine edges never move heads unattended: these
  are written as `revision-proposed`, so JNL's acceptance remains the gate.
- A: Write all 25 and let `adr check` catch problems. Rejected - `adr check` validates grammar and
  replay, not whether a head is the right decision, so nothing downstream would have caught it.
- A: Raise the bar inside the first pass instead of adding a second. Rejected - the first pass
  cannot see its own fitting, which is precisely what a second, adversarially framed reader can.
- F: `experiments/adr-campaign/build_refute_payload.py`, `refute-brief.md`,
  `adjudicate_attach.py`
- T: Five refutation workers over 25 claims; 17 stand, 4 name a better ref, 4 are refuted outright.
  The four named alternatives were REPORTED rather than swapped in, then re-grounded on their own
  quotes in a separate pass - 5 of 5 verbatim, including a re-quote of `adr_forward_only_edges`
  whose original pick was sound and whose only fault was a paraphrase. `adr check` green after
  both writes.

### Facts

- ADRs with no decision attached anywhere: **29 before this session, 7 after**. Twenty-two now
  carry a proposed head; none is accepted.
- The seven that keep only their founding line, and why each does - they are NOT one category:
  - Predates the session log, so no decision could found it: `adr_runtime_discovery`,
    `adr_control_file_authority` ("it preexists in control files").
  - Every candidate applies or revises the rule rather than instituting it: `adr_semantic_provider`
    ("keep model2vec required" maintains an existing requirement), `adr_worktree_convention`
    (the candidate moves worktree scope from task to session, which the refuter read as revising a
    prior rule). The second is a judgement call I would contest - moving the scope IS what founded
    the current convention - and it is left refused rather than overridden, so JNL decides.
  - Compound concern no single decision covers: `adr_agent_selective_install` (selective install
    via `project.yaml` is founded; "remove strips only our entries" is not).
  - First-pass `none` that never reached a refuter: `adr_release_topology`.
  - Unresolved grounding failure, not a reader's judgement: `adr_legacy_agents_compat`. Its quote
    was a paraphrase AND its ref was one the same worker had already given to
    `adr_foreign_file_routing`, so the pick was wrong on substance too. It got no second look.
- Five of the 22 came from the refutation pass rather than the first: four replacement heads it
  named, plus `adr_forward_only_edges`, whose original pick was right and whose only fault was a
  paraphrased quote. All five re-grounded verbatim, 5 of 5.
- Inlining decision bodies in the payload cut worker tool calls from 31-36 per batch to 2, and
  removed the title-quoting failure mode by putting the body in front of the worker.

- Because the revision carries the founding Decision text forward verbatim, each proposal's gist
  under `### Awaiting review` reads the same as the displayed Decision. That is correct - the
  concern did not change, only what it rests on - but it means the evidence for an attachment is in
  the revision's Why, not in the summary line.

### Follow-ups

- The 22 new proposals join the 17 already awaiting JNL's acceptance, for 39 pending in total.
- Accepting all 22 owes **22 diagram answers** - one per ADR, not one per attached decision.
- `adr_legacy_agents_compat` never got a second reader; it is the one holdout that is an open
  question rather than a settled answer.

## 2026-08-08 20:41 - Two ADRs held one concern: the windows-encoding duplicate folds into adr_encoding_policy

```yaml
entry_id: mse_cq88k8kb8k3wp1cb
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/fold-windows-encoding-adr
topics:
  area:
    - governance-profile:d1
  activity:
    - process-correction:d1
evolves:
  - d1 -> mse_kqna9hegj35dwsqj:d2
```

### Summary

- Listing the pending revisions exposed two ADRs proposing the SAME head, `mse_74ddxsena9nj2afk:d1`:
  `adr_encoding_policy` (S1, founded from `index.md#L183`, accepted) and
  `adr_windows_encoding_policy` (chain-founded 2026-08-08, never accepted). One concern, two records.
- JNL's call: fold the proposed one into the existing one. Done - evidence carried across as soft
  context, the duplicate rejected with a pointer to where the concern lives.

### Decision

#### D1 - The accepted record survives; the duplicate is rejected, not superseded

- D: `adr_encoding_policy` keeps the concern. Its two supporting decisions
  (`mse_76r59d5yxcqy0kb8:d2`, `mse_2rvggg1jy6y4m1g3:d2`) are carried across as a `context-added`
  event, and `adr_windows_encoding_policy` takes a `revision-rejected` whose Reason names the
  survivor.
- R: Direction was not a coin toss - `adr_encoding_policy` is already referenced by two skills'
  `governing_adr` (`docx_render_windows`, `memory_hygiene`), by `index.md#L183` and by a diagram
  sidecar, while nothing outside its own file references the duplicate. Its id is also the honest
  one: UTF-8/LF/NFC is not a Windows-scoped policy, Windows is only where the mojibake surfaces.
  Rejection rather than supersession because `adr-superseded` requires an accepted head to retire
  and the duplicate never had one - measured, not assumed: the supersession dry-run refuses with
  "invalid ADR supersession" while the rejection passes.
- A: Keep both and narrow the duplicate to a Windows-specific concern. Rejected - the two Decision
  texts state the same rule, and they cite the same deciding decision, which is the definition of
  one concern.
- A: Re-propose the duplicate's richer text on `adr_encoding_policy` so nothing is lost. Impossible,
  and measured before being ruled out: the same `decision_ref` cannot be proposed twice on one ADR,
  and the dry run refuses with "duplicates revision mse_74ddxsena9nj2afk:d1". That is the ledger
  working correctly, so the loss is recorded rather than worked around.
- F: `.memory-seed/decisions/adr_encoding_policy.md`,
  `.memory-seed/decisions/adr_windows_encoding_policy.md`
- T: `adr check` green; pending revisions 39 -> 38 across 38 ADRs; no two ADRs propose the same head;
  the two skills' `governing_adr` still resolve; full suite 1121.

### Facts

- **Two things could not be folded and are recorded here so they are recoverable.** The duplicate's
  Decision carried one clause the survivor's does not - invalid UTF-8 and likely mojibake stay
  blocked for manual review rather than being repaired automatically, because the intended
  characters cannot be inferred from corrupted bytes. It also carried
  `constitution:v1#prove-automation` as a supporting binding. Neither can be appended to the
  survivor's pending revision, which is already keyed on the same decision. The next revision of
  `adr_encoding_policy` should carry both; the rejected record keeps them readable in the meantime.
- The duplicate was born in the 12-chain founding run on 2026-08-08 02:29, which had no check for
  "does an existing ADR already claim this concern" - only "does an existing ADR claim any member
  of this chain". The two ADRs did not share a member until today's attachment gave them one.

### Follow-ups

- Give the chain-founding path a same-concern check, not only a same-member check; today it took a
  head collision showing up in a pending-revisions listing to catch this one.
- Fold the manual-review-not-auto-repair clause into `adr_encoding_policy` at its next revision.

## 2026-08-08 21:34 - A rejected revision can be proposed again, because a summary written from bad evidence must be correctable

```yaml
entry_id: mse_j4pfgyms1dyxe81g
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/repropose-after-reject
topics:
  area:
    - schema:d1
  activity:
    - feature-build:d1
evolves:
  - d1 -> mse_kqna9hegj35dwsqj:d1
```

### Summary

- JNL set the rule that an ADR summary is synthesised from every chain member not completely
  replaced, not dictated by whichever decision sits at the end of the chain. Applying that to the
  38 pending revisions means rewriting summaries that are already proposed.
- The ledger would not allow it. This is the one-line contract change that does, landed on its own
  before any data uses it.

### Decision

#### D1 - A decision_ref may be proposed again once its previous proposal was rejected

- D: `validate_adr` refuses a duplicate revision only when the ref is currently `proposed` or
  `accepted`; a `rejected` ref may be proposed again. `proposal_for` returns the LAST matching
  proposal rather than the first, so the corrected text is what the Current view renders.
- R: Measured before building, because the alternative would have been to invent a second head. A
  revision's Decision/Why text is fixed at proposal time and the ledger is append-only, so the only
  way to correct wording is a new proposal - and `revise_adr` keys on `decision_ref`, so the only
  honest key for a correction is the same decision. The old check keyed on `ref in statuses`, which
  is populated by any prior proposal regardless of its later state, so a rejection did not reopen
  the ref: a dry run returned `duplicates revision mse_74ddxsena9nj2afk:d1`. That is what forced the
  encoding-policy fold an hour earlier to record two things it could not carry across rather than
  fold them. Keeping the refusal for LIVE refs is the half that was always right - two competing
  texts on one decision leave no way to say which the ADR rests on. `replay_adr` needed no change
  at all; it already overwrites `statuses[key]`, so a re-proposal is pending again by construction.
- A: Let a correction key on a different decision. Rejected - that puts a decision at the head of a
  concern it did not decide, purely to carry text. It is the same false-provenance move that
  founding pseudo-refs exist to avoid.
- A: Derive the Current view from the live chain at render time instead, so no correction is ever
  needed. Rejected by JNL when asked: the synthesis is authored into the ledger event and ratified,
  because a machine-written summary at the head of an authority record has no approval gate.
- F: `memory_seed/adr.py` (`validate_adr`, `proposal_for`), `tests/test_adr_contract_extension.py`
- T: Three new tests - a rejected ref re-proposes and the CORRECTED text is what renders while the
  head stays put; a ref that is proposed, and again once accepted, still refuses with
  `duplicates revision`; and every live ADR validates and renders to its own bytes, which holds
  because no record has two proposals for one ref, so first-match and last-match agree. Suite 1124,
  reading its own exit code. No file under `.memory-seed/decisions/` changed, so the three
  sha256-pinned records are untouched.

### Facts

- The relaxation is deliberately narrow: rejection is the only thing that reopens a ref. There is
  no way to overwrite a live position, which is the property the original rule was protecting.
- Landed alone and merged first, per the standing rule that a format change ships before any data
  that uses it - `session merge-branch` validates with main's parser.

### Follow-ups

- The 38 pending summaries are re-synthesised from their live chains on top of this.
