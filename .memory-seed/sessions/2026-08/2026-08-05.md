## 2026-08-05 01:53 - Confirm scoped Codex MCP approval with one cheap smoke

```yaml
entry_id: mse_r7any82fmjbs2cz9
user_initials: JNL
agent_type: codex
agent_name: Codex
project_path: .
subproject_path: null
branch: codex/experiment/context-derivation-run
topics:
  - mcp-tools
  - testing
evolves:
  - mse_rvrnt2yb60c80wz2
related_entries:
  - mse_eg0fakq8jf5h7tfw
```

### Summary

- Proved the corrected Codex MCP approval path with one cheap, unscored Luna call: the broker received and successfully delivered exactly one `memory_adrs_list({})` request, and the model returned the exact expected JSON.

### Decision

- D: Accept the one-call approval smoke as confirmation that the scoped per-server approval override works, preserve it as unscored protocol evidence, and keep the 288 scored subjects plus 96 blind judges blocked behind a separate user decision.
- R: The prior smoke isolated automatic MCP cancellation as the blocker. This low-effort retry passed the exact read-only call, argument, delivery, final-answer, teardown, parent-store, and fixture gates without enabling any broader tool or scored-run path.
- A: Re-running the broader ADR workflow smoke was rejected because its one-shot claim is already consumed and it would spend more tokens without isolating the approval variable. Treating this protocol pass as an accuracy result was rejected because it asked no benchmark question.
- F: `experiments/context-derivation/broker.py`, `experiments/context-derivation/mcp_wrapper.py`, `experiments/context-derivation/run.py`, `experiments/context-derivation/README.md`, `experiments/context-derivation/APPROVAL_SMOKE_RESULT.md`, `tests/test_context_derivation_broker.py`, `tests/test_context_derivation_harness.py`.
- T: The sole live call used `gpt-5.6-luna`, Codex CLI `0.146.0`, and low effort; exit 0; 33,618 input tokens (23,808 cached), 427 output tokens; exact successful `memory_adrs_list({})`; no integrity failures, undeclared tools, direct filesystem retrieval, parent/fixture mutation, retained bearer token/raw MCP arguments, scored `runs/`, or broker teardown failure. Provider-free validation passed with 86 tests and 6 subtests, followed by clean independent protocol and security re-reviews.

## 2026-08-05 03:44 - Run the one-shot ADR context pilot and hold the full trial

```yaml
entry_id: mse_brc7d4n4mqw7ac5y
user_initials: JNL
agent_type: codex
agent_name: Codex
project_path: .
subproject_path: null
branch: codex/experiment/context-derivation-run
topics:
  - mcp-tools
  - testing
evolves:
  - mse_r7any82fmjbs2cz9
```

### Summary

- Ran the owner-approved one-shot ADR context pilot across eight unscored cells, preserved the failed result, and corrected only the harness defects it proved.

### Decisions

#### D1 - Keep the scored experiment blocked after the pilot failed

- D: Treat the pilot as a failed preflight, not as benchmark evidence: preserve its artifacts and result note, do not retry its consumed claim, and keep the 288 subject runs plus 96 judges blocked pending a separately approved live proof.
- R: Only 2 of 8 cells were completely correct. Claude's two fixed-packet arms passed, its MCP arms were invalidated by disabled tool exposure, and all four Codex arms failed without a proven root cause. A result with harness-invalid arms and unresolved subject behavior cannot support a production recommendation.
- A: Pooling the two correct packet answers into an accuracy claim was rejected because this was one synthetic task, one repetition, and an explicitly unscored pilot. Replaying the one-shot claim was rejected by the pre-registered no-retry contract.
- F: `experiments/context-derivation/UNSCORED_PILOT_RESULT.md`, `experiments/context-derivation/pilot.py`, `experiments/context-derivation/pilot_spec.py`.
- T: Eight of eight cells launched once with zero retries; all five protected snapshots remained unchanged; no timeout, containment breach, direct filesystem retrieval, undeclared tool, scored run, or judge call occurred. Usage was 60,970 input and 2,684 output tokens; provider cost was unavailable.

#### D2 - Correct only defects directly demonstrated by the pilot

- D: Make cell claims LF-stable with a binary descriptor, omit Claude's built-in `--tools` switch on MCP arms while retaining the built-in denylist and exact MCP allowlist, classify zero-call interactive cells as protocol failures, and preserve an all-unknown cost total as null.
- R: Each correction has direct artifact evidence: Windows wrote CRLF claims against an LF checker; both Claude MCP answers reported tools unavailable under `--tools ""`; every interactive zero-call answer was mislabeled protocol-clean; and the summary converted eight unknown costs to a misleading zero.
- A: Changing the frozen models, prompts, benchmark questions, production retrieval contracts, or ranking behavior was rejected because the pilot does not identify the cause of Codex's four failures. Those variables need a new bounded proof and user approval.
- F: `experiments/context-derivation/run.py`, `experiments/context-derivation/pilot.py`, `tests/test_context_derivation_harness.py`, `tests/test_context_derivation_pilot.py`, `experiments/context-derivation/README.md`.
- T: Provider-free validation passed 116 context-derivation tests plus 6 subtests; the focused harness/pilot set passed 55 tests; `git diff --check` was clean; independent protocol and security reviews were clean after adversarial fixes.

### Follow-up

- Ask the user before any further provider call. The next live proof should be newly authorized and narrower than the full matrix, specifically proving corrected Claude MCP exposure and investigating Codex prompt/subject compliance without changing the scored contract midstream.
- No Mermaid sidecar was added because the frozen experiment plan already defines the two-queue/four-arm topology and this turn changed only launch guards and reporting, not the retrieval or production architecture.
