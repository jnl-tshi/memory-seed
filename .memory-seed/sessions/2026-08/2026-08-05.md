---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-05
---

## 2026-08-05 01:14 - Record the Verging Labs memory index as E9

```yaml
entry_id: mse_asfgma3qw5v42jtp
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/verging-labs-signal
topics:
  - tooling-evaluation
related_entries:
  - mse_p1mvnzehwekymwx0
```

### Summary

- Recorded the Verging Labs Agentic Memory Index (v0.1, August 2026) as field evidence E9 and folded
  it into the competitor landscape: the agent-memory category is now independently benchmarked, four
  of the top five scorers were unknown to the dossier, and a submission path exists.

### Decision

- D: Treat the index as a market-formation signal and adopt a three-step sequence before any
  contact - profile the four unknown top scorers, dry-run the six probe categories privately
  against Memory Seed with the existing harness machinery, and only then email for v0.2
  inclusion. Recorded in E9 and the competitor landscape; none of the three steps started.
- R: An independent benchmark existing at all qualifies Report 3's "unoccupied" finding for the
  recall layer, and its probe taxonomy converging on the same instrument this programme built
  independently (updated facts ↔ supersession, false memory check ↔ faithfulness) is evidence for
  both instruments - with Memory Seed's validated strengths sitting exactly on the separating
  probes. But fit is unverified: the harness plausibly assumes an arbitrary-fact store/recall API,
  and a decision-shaped validated store may need an adapter or be structurally penalised on
  arbitrary-fact recall. Entering a published benchmark blind risks a public number that measures
  the adapter, not the product. The index also measures recall only - capture, the programme's core
  evidence, has no external benchmark - which is stated in E9 so the score is never read as a
  verdict on the whole product.
- A: Emailing immediately was declined (blind entry). Ignoring the index was declined - it is the
  concrete vehicle for the external-replication gap E5/E6 recorded as their standing limitation.
- F: `business/research/field-evidence-log.md` (E9), `business/market/competitor-landscape.md`.
- T: n/a - market observation; sequence is falsifiable at the dry-run step.

## 2026-08-05 01:21 - Profile the memory-index field and design the dry-run

```yaml
entry_id: mse_38hrm8xm2bx6y27t
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/memory-index-profiles
topics:
  - tooling-evaluation
evolves:
  - d2 -> mse_asfgma3qw5v42jtp
```

### Summary

- Profiled the four unknown Agentic Memory Index top scorers, folded the index author's launch
  thread into E9, and designed the private dry-run plan that gates any v0.2 submission.

### Decisions

#### D1 - Read the index as substrate validation with a governance-shaped gap, not as a threat ranking

- D: The competitor landscape now frames the benchmark around the April 2026 category event -
  Karpathy's llm-wiki pattern (98.5, a gist not a product) and Garry Tan's gbrain (92.9, Markdown +
  typed graph + MCP) - as market validation of Memory Seed's substrate philosophy, with the
  contested ground being what sits on top: the winner was the only system to hallucinate on a
  never-stored question, products failed hardest on updated facts (Zep 8/24), and a top comment
  states the differentiation unprompted ("wins because it's inspectable... the durable memory
  layer isn't recall, it's provenance, an append-only record").
- R: The raw table reads as "four unknowns beat the market" until profiled: the #1 is a pattern
  anyone can adopt, the #1 hosted is a just-launched company with no independent footprint (a
  commenter alleged it was fabricated; verified real), and the two Markdown-native entries carry
  the two most influential names in the space - meaning the bet Memory Seed made before this index
  existed just won it. The weaknesses the author's own data assigns to the winners (fabrication,
  updates, staleness, collaboration) map one-to-one onto Memory Seed's validated write path,
  supersession machinery, and provenance - E8's flawless stale-head navigation and the v2 matrix's
  zero fabricated rationale sit exactly on the losing probes.
- A: Treating gbrain as the primary threat was considered and narrowed: it is the closest shape
  and the biggest distribution, but it has no decision governance, no validated writes, and no
  lifecycle edges - recorded as the neighbour to watch, not the verdict.
- F: `business/market/competitor-landscape.md`, `business/research/field-evidence-log.md` (E9
  addendum).
- T: All four profiles verified by direct fetch/search 2026-08-05, sources linked in the dossier.

#### D2 - Gate any submission behind a pre-registered private dry-run with a kill condition

- D: `docs/2_Todo/memory-index-dry-run-plan.md` designs the six-category approximation on the
  agent-capture machinery (~10 seeding + ~25 quiz sessions, ~$25): benchmark-faithful mechanics
  (agent follows the shipped docs), blind two-stage judging, pre-registered expectations - updated
  facts and false-memory as strengths, arbitrary-fact recall as the expected weakness - and a kill
  condition: score below the published #8 (Zep 75.1) means fix first, do not submit v0.2.
- R: v0.2 lands early September, which converts "never enter a benchmark blind" from principle to
  deadline. Pre-registering the expected weakness matters because the store is decision-shaped by
  design: if direct recall drags the blend down, the honest finding is "adapter needed", and only a
  measured dry-run can distinguish that from "memory is bad" before a public number exists. The
  expectations are written now so the result cannot be read opportunistically - the same
  pattern-over-threshold discipline JNL set for E5/E6.
- A: Submitting immediately on the strength of E8/v2 results was declined - those measure
  decision-governance probes, not their taxonomy, and the benchmark's own mechanics (docs-driven
  end-to-end) mean the score also measures the instruction surface, which has never been quizzed
  this way. Replicating their full 272-probe set was declined on cost; directional with error bars
  suffices for a go/no-go.
- F: `docs/2_Todo/memory-index-dry-run-plan.md`.
- T: n/a - design; falsifiable at the dry-run.

## 2026-08-05 02:28 - Memory-index dry-run: kill condition triggered, fix located

```yaml
entry_id: mse_czs6tcqdnsbsfzfc
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/memory-index-dryrun
topics:
  - tooling-evaluation
  - testing
evolves:
  - mse_38hrm8xm2bx6y27t:d2
```

### Summary

- Ran the memory-index dry-run (10 seeding + 8 quiz sessions, 31 judged questions): blended 71.0,
  below the pre-registered kill line (Zep 75.1), so v0.2 submission is off until the fix lands.
  Zero fabrications and 8/8 on never-stored traps; every miss was an over-abstention traced to
  either capture loss on fact-dense sessions or retrieval under-confidence on stored facts.

### Decisions

#### D1 - Honour the kill condition and locate the fix in routing, not honesty

- D: Do not submit to Verging Labs v0.2. The recorded fix is (1) route durable non-decision facts
  to index.md at capture time - the home memory_consolidation.md already assigns them - and
  (2) let retrieval trust project files and superseding-entry context it already holds; re-run the
  dry-run and submit only if it clears 75.1.
- R: The kill condition existed precisely for this moment - a private 71.0 with a diagnosis is
  cheap; a published 71.0 is a permanent number. The diagnosis is unusually clean: all nine misses
  are "not recorded" answers, never wrong assertions, splitting into capture loss (the kickoff
  session recorded zero entries, so roster/cadence/budget/codename died at storage - E5's capture
  finding replayed on diary-style sessions with no code change) and under-confidence (Sofia,
  the original CI budget, and the Granite codename were all on disk and denied). Meanwhile both
  pre-registered strengths held: false-memory 8/8 with zero fabrications - the axis the published
  winner failed on - and updated-facts/thread-growth strong. Loosening the honesty posture to
  score higher would trade away the one property the field's own commentary says matters most for
  acting agents; routing and confidence are the real defects.
- A: Submitting anyway on the strength of the perfect false-memory column was declined - the
  blended number is what gets published. Widening the answer-key tolerance was declined as
  score-shopping. Re-running immediately tonight was declined: the routing fix is a control-plane
  change that deserves its own considered slice, not a 2am patch.
- F: `docs/2_Todo/memory-index-dry-run-plan.md` (results + revised next_action),
  `experiments/memory-index-dryrun/dryrun.py`, `experiments/memory-index-dryrun/facts.json`,
  `.gitignore`.
- T: 10/10 seeding sessions exit 0 (7 entries recorded - the capture gaps are themselves data);
  8/8 quiz batches on isolated workspace copies; 31/31 judged by Codex over stdin with a schema;
  miss provenance checked against the seeded store file by file.

## 2026-08-05 03:23 - Decision-level retrieval with whole DRAFT blocks and relevance bands

```yaml
entry_id: mse_qeht233q9ebvf2ms
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
topics:
  - retrieval
  - mcp-tools
evolves:
  - d1 -> ms-845042c7:d1
```

### Summary

- Made retrieval decision-level: `granularity="decision"` serves one result per `#### Dn` DRAFT
  block, keyed `mse_x:dN`, carrying the whole block plus that decision's own topics and lifecycle
  edges - and made it the memory_search default. Answer visibility at rank 1 went 2/8 to 5/8 on the
  dry-run store.

### Decisions

#### D1 - Serve retrieval at decision granularity with canonical mse_x:dN identity

- D: New `granularity="decision"` mode, default for `memory_search` and `rank_session_memory`:
  one chunk per `#### Dn - ...` heading with `chunk_id = mse_<entry>:dN`, decision-scoped topics
  (`operations:d1` resolves to that decision only; bare topics apply to all) and decision-scoped
  lifecycle edges. Entries without decision headings keep the whole-entry unit.
- R: Every other subsystem - topics, lifecycle edges, ADRs, Trace's D1/D2 selector - already speaks
  decision identity; retrieval was the only one still working in entries, which is why an entry
  holding three unrelated decisions was scored and served as one blob. Diagnosis from the E9
  dry-run: search recall was 100% (7 of 7 entries returned on a 7-entry store) and the agent still
  answered "not recorded", because 59% of each 280-char excerpt was identical YAML frontmatter and
  the facts sat in bodies the excerpt never reached. Decision chunks make the served unit the thing
  the agent must reason about, and they largely dissolve the length bias too - entry chunks span
  1,945-11,855 chars, decision chunks 1,125 mean.
- A: Character-budget excerpt tuning was JNL's first target and was rejected as treating the
  symptom. Serving whole entries was priced and rejected (18.6k tokens for 8 results against 2.3k
  for decisions). Keeping entry as the default and making decision opt-in was rejected: an opt-in
  default nobody sets is the state that produced the misses.
- F: `memory_seed/semantic_cache.py` (`_find_decision_ranges`, `_decision_topics`, decision branch),
  `memory_seed/retrieval.py`, `memory_seed/mcp_server.py`, `tests/test_decision_retrieval.py`.
- T: 7 new tests + full suite 929 passed. Answer-visible@1 on the dry-run store: entry 2/8,
  decision 5/8.

#### D2 - Serve the whole DRAFT block, not a preview

- D: A decision result's `excerpt` is the complete block up to `DECISION_TEXT_LIMIT = 2500` chars,
  newlines preserved, with an explicit truncation marker naming `memory_get_chunk` past the cap.
  Entry and section results keep the 280-char preview.
- R: JNL's instruction - give the agent the whole relevant DRAFT section, not the D and R lines.
  The block is the unit of reasoning, and a preview of a decision is exactly the failure the
  dry-run measured. Priced first: 2,250 tokens for a typical 8-result search and 3,616 at p90,
  against 560 today and 18,648 for whole entries; 2,500 chars covers 97.6% of recorded decisions in
  this repo, so truncation is the rare case and it announces itself rather than silently cutting a
  rationale in half.
- A: Extracting only D/R lines was my proposal and JNL corrected it - A/F/T carry the rejected
  alternatives and the validation, which is the part that stops a decision being re-litigated.
- F: `memory_seed/retrieval.py` (`_decision_body`, both result builders).
- T: `test_search_serves_whole_draft_block_not_a_preview` pins the full rationale present and no
  ellipsis; verified live that the Sofia decision now serves its complete D/R where the old excerpt
  showed only YAML.

#### D3 - Classify relevance in the tool so abstention is tool-reported

- D: `memory_search` attaches `relevance` (strong/weak/none) per result, `no_match_above_threshold`
  to the payload, and `relevance_rule` stating the rule in words. Strong = clears an absolute floor
  AND holds >=55% of the top score.
- R: The agent was being handed raw floats - 51.6 for an irrelevant hit, 36.5 for the one holding
  the answer - and asked to infer existence from them. That inference is what produced nine
  "not recorded" answers over facts that were on disk. Moving the classification into the tool
  makes "not recorded" a fact the tool reports rather than a judgement the model guesses, which is
  the precondition for ever promoting "the top result is authoritative" into the instructions.
  The rule ships as stated text, per the Constitution §3 expose-before-you-rank principle and the
  Trace M5 ruling that attention must be a stated rule and not a hidden score.
- A: Promoting top-1 authority now was declined - measured top-1 accuracy was 5/8 at entry level
  and the band needs calibrating on both corpora before any such claim earns its place.
- F: `memory_seed/retrieval.py` (`_classify_relevance`, `RELEVANCE_FLOOR`,
  `RELEVANCE_STRONG_RATIO`).
- T: `test_relevance_band_and_no_match_signal` covers both directions including a query with no
  real match.

## 2026-08-05 08:50 - Route durable facts at capture and trust the relevance verdict

```yaml
entry_id: mse_0k8mtvqzq8hbcgev
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/decision-level-retrieval
topics:
  - session-logging
  - retrieval
evolves:
  - d2 -> mse_qeht233q9ebvf2ms:d3
```

### Summary

- Closed the capture gap the dry-run exposed: durable non-decision facts now route to `index.md` at
  harvest time, and retrieval instructions now tell agents to trust the tool's relevance verdict.
  Dry-run blended score 74.2 -> 96.8 with zero fabrications; seeding sessions went 7/10 to 10/10
  recording.

### Decisions

#### D1 - Give durable non-decision facts a home at capture time

- D: Decision Harvest gains step 9 - roles, ownership, codenames, cadences, thresholds, budgets,
  environments and external locations are promoted to `.memory-seed/index.md` under `## Active
  State` in the turn they are established, named in the entry; and a turn that establishes only
  facts and changes no code still records. `end_of_turn.md` step 2 references it so the runbook and
  the schema agree.
- R: The harvest asked only "what did you decide?", and a fact has no `R:` to give, so facts fell
  through every capture path - measured, not theorised: a kickoff session that set the maintainer
  roster, release cadence, CI budget and 1.0 codename recorded nothing at all, and every one of
  those facts was unanswerable weeks later. `memory_consolidation.md` already assigned facts to
  index.md; nothing routed them there in-session, so the destination existed with no path to it.
  This is also the E1/E7 field convention - practitioners running index-plus-facts files by hand
  arrived at the same split.
- A: Storing facts as pseudo-decisions with invented rationale was rejected - it would pollute the
  decision graph and fabricate reasons, violating the policy against inferred rationale. Widening
  `D:` to mean "anything durable" was rejected for the same reason.
- F: `.memory-seed/skills/session_logging.md`, `.memory-seed/skills/end_of_turn.md`, and both seed
  twins.
- T: Re-seeded the dry-run corpus: 10/10 sessions recorded (was 7/10), `index.md` gained Dana and
  the 1.2 microsecond baseline it had dropped; long_term_retention scored 4/4 (was 1/4).

#### D2 - Tell agents to trust the tool's relevance verdict

- D: `history_retrieval.md` gains a "Trusting The Tool's Relevance Verdict" section: a `strong`
  result is answered from, `weak` is context, and only `no_match_above_threshold` licenses "not
  recorded" - plus the note that decision-granular results already carry the whole DRAFT block, so
  `memory_get_chunk` is for truncation overflow, not a routine second hop.
- R: The relevance band shipped with the retrieval change but nothing in the agent contract
  referenced it, so agents kept inferring existence from raw scores - and kept denying facts that
  sat in the payload they had been served. Naming which signal licenses abstention converts honest
  caution into grounded caution without loosening it: ungrounded denial is the error, abstention on
  a real no-match stays correct. That distinction is what preserved the zero-fabrication column
  (8/8 traps) while updated_facts and synthesis went to full marks.
- A: Weakening the abstention posture to raise the score was rejected outright - zero fabrications
  is the axis the published benchmark winner failed, and for an acting agent an invented fact is
  strictly worse than a missing one.
- F: `.memory-seed/skills/history_retrieval.md` and its seed twin.
- T: Dry-run run 3: updated_facts 6/6, synthesis 4/4, false_memory 8/8 with zero fabrications;
  blended 96.8 against a pre-registered kill line of 75.1. The single remaining miss is an
  answer-key ambiguity (Sofia counted into a maintainer roster she was not on), not a retrieval or
  capture failure.

## 2026-08-05 09:01 - Brief an independent agent to attack the programme's claims

```yaml
entry_id: mse_9zz397x0d2770z96
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/independent-validation-brief
topics:
  - testing
  - proposal
related_entries:
  - mse_0k8mtvqzq8hbcgev
```

### Summary

- Wrote `docs/2_Todo/independent-validation-brief.md`: five numbered claims from this programme
  stated as falsifiable hypotheses for an independent agent to attack, with the method deliberately
  left undesigned.

### Decision

- D: Specify aims and forbid method inheritance - the brief states what must be established (C1
  capture, C2 faithfulness, C3 lifecycle honesty, C4 retrieval QA, C5 fix causality) with the
  claimed numbers and their samples, instructs the agent to write its own design down *before*
  reading anything under `experiments/`, and names four equally-valid outcomes including "the
  measure is wrong" and "the claim is unfalsifiable as written".
- R: JNL's instruction, and it is the correct answer to this programme's standing limitation:
  every number behind these claims was produced by the same agent that wrote the code being
  measured, and two silent measurement bugs were already caught mid-run - both of which had
  produced clean, plausible, wrong results. A validator handed a procedure inherits its blind
  spots, so prescribing the method would reproduce the exact flaw the exercise exists to remove.
  Naming "the measure is wrong" as the most valuable available outcome is deliberate: the failure
  mode this programme is most exposed to is a true claim about an uninteresting quantity.
- A: Shipping the existing harness for reproduction was rejected outright - reproduction tests the
  code, not the design, and the design is what is suspect. Withholding the claimed numbers was
  considered and rejected: without them the validator cannot aim at anything, and falsifiability
  requires a stated target. Environment hazards (store-resolution walks upward without a boundary,
  Windows `.CMD` argv truncation, account-level tooling leaking into fixtures) are included because
  they are facts about the terrain that cost real time to rediscover, not method choices.
- F: `docs/2_Todo/independent-validation-brief.md`.
- T: n/a - the brief is the artifact; its test is whether an agent can act on it without asking
  clarifying questions.

## 2026-08-05 09:34 - Fix Active State fact accretion that misfiled a role

```yaml
entry_id: mse_y9sr61rxfw1rcz9k
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/active-state-one-claim
topics:
  - session-logging
evolves:
  - mse_0k8mtvqzq8hbcgev:d1
```

### Summary

- Traced the dry-run's last remaining miss to a real capture defect rather than the answer-key
  ambiguity I first called it, and fixed it: Active State facts are now one claim per bullet filed
  by their own predicate, with duplication across bullets named as the tell.

### Decision

- D: Extend Decision Harvest step 9 with a one-claim-per-bullet rule - when a fact's subject is
  already named in `## Active State`, it gets its own line under the predicate it asserts rather
  than being appended to the subject's existing bullet; appending to a membership bullet silently
  asserts membership; if one subject appears on two Active State bullets, one of them is wrong;
  correct an existing bullet only to supersede what it claims.
- R: JNL asked whether anything could be done about the maintainer misallocation, which forced the
  check I had skipped. The record read `Maintainers: Dana is lead maintainer; Priya and Marcus are
  the other two maintainers. Sofia joined 2026-08-05, specifically to own Windows CI.` - Sofia
  inside the Maintainers bullet. The quiz agent was not wrong; it answered correctly from a record
  that had misfiled her, and the same store simultaneously filed her correctly under `Ownership:`.
  So the defect is fact accretion without role discrimination: step 9 told agents to promote facts
  but said nothing about where a fact goes when its subject is already on the page, and the
  duplicate-subject signature is what makes it detectable by eye. This is the E7 staleness critique
  arriving from the write side - a wrong index line is retrieved confidently and acted on, which is
  worse than a miss.
- A: Leaving it as an answer-key ambiguity was my original call and was wrong - I dismissed it
  without reading the record, and the record showed a product defect. Adding a mechanical linter
  for membership bullets was considered and declined as over-clever for a rule a reader can apply;
  the duplicate-subject tell is the cheap check.
- F: `.memory-seed/skills/session_logging.md` and its seed twin.
- T: Replayed the two sessions that produced the collision against a regenerated fixture. Before:
  Sofia inside the Maintainers bullet. After: `Maintainers: Dana (lead maintainer), Priya, Marcus.`
  and `Ownership: Sofia owns Windows CI (joined 2026-08-05 for that scope; not a maintainer).` -
  the agent volunteered the explicit negative, which is stronger than the rule required.

## 2026-08-05 10:02 - Add ADR Constitution context experiment bridge

```yaml
entry_id: mse_a68ddwhcna0ef86m
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/strong-adr-constitution
topics:
  - retrieval
  - testing
related_entries:
  - mse_qeht233q9ebvf2ms
```

### Summary

- Added an experiment-only bridge that joins generated Constitution blocks, parsed living ADR ledgers, and the production decision-level ranking path; added a bounded parallel allocation sweep and offline-only top-K diagnostics.

### Decision

- D: Preserve the accepted head, revision status, and typed `evolves`/`replaces` evidence mechanically in every full ADR context slice; Constitutional text is expanded only for high-ranked strong signals, while lower-ranked hits retain compact ADR references.
- R: A generic ADR-wide binding and untyped/uncapped lineage can either hide a pending or rejected branch or spend the same ADR/Constitution context twice. The experiment must prove authority and links before it can claim a smaller packet is sufficient.
- A: Do not change production ADR Markdown, MCP responses, ranking defaults, or live-agent workflow yet. ADR-wide Constitution fixture bindings remain a control; the proposed revision-scoped representation is documented only as a draft.
- F: `experiments/context-derivation/strong_context_v2.py`, `experiments/context-derivation/strong_context_fixture_v2.py`, `experiments/context-derivation/strong_context_sweep_v2.py`, `experiments/context-derivation/STRONG_CONTEXT_V2_NOTES.md`, and focused tests.
- T: 91 context-derivation tests plus 6 subtests passed. Independent review found and verified fixes for authority/status scoring, cap safety, duplicate ADR blocks, and malformed binding rejection.

### Follow-up

- Amend and approve the Constitution-aware task and gold definitions before any scored offline selection or Codex live smoke test. No diagram sidecar: this checkpoint changes an isolated evaluator rather than the project's durable runtime topology.

## 2026-08-05 10:29 - Validate three instruments: one failed and was withdrawn

```yaml
entry_id: mse_ss97qw0m8makbng2
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/session-gap-instruments
topics:
  - retrieval
  - testing
evolves:
  - d1 -> mse_qeht233q9ebvf2ms:d3
```

### Summary

- Validated three instruments against reality rather than against the fixtures that produced them.
  Two held; one failed. The faithfulness judge provably catches fabricated rationale (3/3 tampered,
  3/3 controls clean), the ranking-ab gate now refuses to pass on an empty evidence base, and the
  relevance band was found not to discriminate at 836 entries - a defect shipped this morning and
  withdrawn the same day.

### Decisions

#### D1 - Withdraw the trust-the-band instruction; the band does not discriminate at scale

- D: Replace the `history_retrieval` "Trusting The Tool's Relevance Verdict" section with an
  explicit uncalibrated warning, and have `search_memory` return `relevance_calibrated: False` on
  every payload. Consumers judge relevance from the served DRAFT block; abstention is licensed by
  the content, not by a band.
- R: Measured on the live 836-entry corpus, 88% of results band `strong` and
  `no_match_above_threshold` fired for 0 of 12 probe queries - including "recipe for sourdough
  starter hydration", which returned eight `strong` results. Neither the absolute score (real
  17.9-53.5 vs nonsense 8.8-19.3) nor the top-over-median gap (1.11-2.12 vs 1.00-1.41) separates
  them, with semantic ranking on or off. That makes the instruction shipped this morning actively
  dangerous: "answer from a strong result" plus "only no_match licenses not-recorded" told agents
  to answer confidently from noise and never abstain. Withdrawing an instruction I wrote hours
  earlier is cheaper than leaving a fabrication path open in the control plane, and the honest
  posture predates the band anyway.
- A: Tuning new constants tonight was declined - twelve queries is not a calibration set, and
  fitting thresholds to the sample that exposed the problem is the same error that produced the
  original constants against a 7-entry store. Deleting the band outright was declined: the fields
  are harmless once labelled, and the recorded distribution is what a real recalibration starts
  from.
- F: `.memory-seed/skills/history_retrieval.md` and seed twin, `memory_seed/retrieval.py`,
  `docs/2_Todo/memory-index-dry-run-plan.md` (scale addendum).
- T: `experiments/decision-retrieval-scale/measure.py` over 836 entries / 1,241 decision chunks;
  band distribution, token cost and lifecycle stability recorded; 12-query real-vs-nonsense
  comparison at both granularities.

#### D2 - The faithfulness judge is a verified instrument

- D: Keep E6's "0 of 121 reasons judged unfaithful" as a real result, on the evidence of a negative
  control: `judge_calibration.py` tampers real packets by replacing recorded `R:` lines with a
  fluent rationale that contradicts the transcript, and runs them through the unmodified stage-1
  path.
- R: A judge that can only answer yes/unclear would produce exactly the same 0/121 on a corpus full
  of fabrications, so the claim rested on a verdict never once observed. It caught 3/3 tampered
  packets, returned `no` only on the tampered decision while leaving its untouched siblings `yes`,
  and returned no false positives on 3/3 controls. That is the discrimination the claim needs.
- A: Accepting 0/121 on the strength of the variance already present (104 yes / 17 unclear) was
  declined - `unclear` is not `no`, and only the negative control tests the verdict that matters.
- F: `experiments/agent-capture/judge_calibration.py`, `experiments/agent-capture/judge.py`.
- T: 3/3 tampered caught, 3/3 controls clean. The first run reported a confident FAIL - "null
  instrument" - which was my harness swallowing a `--skip-git-repo-check` error from a non-git
  scratch dir as an empty verdict; `judge.py` gained the flag and the calibration script now aborts
  loudly on a judge error rather than scoring it as silence.

#### D3 - The ranking-ab gate fails closed when its evidence base is empty

- D: Add `requires_affected_hits` to `Signal` and `ABResult`; the `attention` signal opts in, so an
  empty retrieval log now yields exit 1 and a REFUSED line rather than a pass.
- R: As shipped this morning the gate returned PASS whenever `--query` was supplied against an
  empty log: nothing was affected, every query fell into the control bucket, both arms were
  trivially identical, and the pass certified nothing - the exact vacuous approval that
  expose-before-you-rank exists to prevent, and the registry comment claimed the opposite. The
  distinction the flag encodes is real: a structural signal with nothing affected genuinely has
  nothing to change, while a usage signal with nothing affected has no evidence yet.
- A: Special-casing attention inside `passed` was declined in favour of a declared property, so any
  future usage-derived signal inherits the refusal instead of repeating the bug.
- F: `memory_seed/ranking_ab.py`, `tests/test_ranking_ab.py`,
  `docs/2_Todo/attention-retrieval-signal-proposal.md`.
- T: `attention` exits 1 on the empty log, `supersession_damping` still exits 0; three new tests
  covering refusal, populated-log pass, and non-regression of structural signals. Also recorded why
  the log is empty at all: this repo's `.mcp.json` runs the published package, not the local build,
  so today's instrumentation never executes here.

## 2026-08-05 10:45 - Preserve the experiment evidence instead of reclaiming the disk

```yaml
entry_id: mse_zz1v0a5we0gyqrp6
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/session-gap-closeout
topics:
  - testing
  - docs-lifecycle
evolves:
  - d2 -> mse_ptcadyekn3j2ga9r:d3
related_entries:
  - d1 -> mse_9zz397x0d2770z96
  - d3 -> mse_asfgma3qw5v42jtp
```

### Summary

- Closed the session's documentation debt and reversed one item of the approved plan on inspection.
- The guard columns are now a measurement rather than a gap: 60/60 v2 runs carry a reliable guard
  signal, `guard_called` 0/60 and `guard_blocked` 0/60, so the pre-registered confound is retired on
  evidence instead of on silence. E5's "unreliable" caveat and E6's retirement note both say so.
- The plan's disk-cleanup item would have deleted 99 agent-written session files, 78 transcripts and
  45 copies of the pre-fix skill text. It was not executed as written.

### Decisions

#### D1 - Prune the regenerable layer; do not delete the archived run directories

- D: Keep `runs-v1-json/` and `runs-veto/`. Delete only the `.agents/` copy inside each archived run:
  it is byte-identical across all 45 directories and tracked in `memory_seed/seed/.agents/`, so it
  is fully recoverable from git. Reclaims 3M of the 38M and destroys nothing.
- R: The approved plan said delete both directories outright, on a disk-pressure rationale written
  when I estimated 76M. Inspection found the estimate was the least important fact about them: they
  hold 99 session files the subject agents actually wrote, which is the primary evidence for the
  capture rates in E5 and E6, and 45 copies of the pre-fix `risk_signaling.md`, which is the
  independent variable of the E8 veto experiment - the thing a replicator needs in order to tell
  what the agents were actually instructed to do. Those claims left the experiment this session:
  E8 and E9 now sit in two strategy reports, and `independent-validation-brief.md` is about to hand
  C1-C5 to an agent whose job is to attack them. Deleting the evidence base the day before
  commissioning an adversarial review is the wrong trade at any disk saving, and 38M is not a
  constraint on this machine.
- A: Executing the plan as approved was declined - a recorded approval is not a reason to carry out
  an action whose premise inspection has falsified, and being more conservative than an approval
  needs no fresh authorization. Compressing the directories to archives was declined as well: it
  saves a similar amount and makes the evidence one decompression step harder to read, which is the
  wrong direction for material a third party is meant to audit.
- F: `experiments/agent-capture/runs-veto/`, `experiments/agent-capture/runs-v1-json/`.
- T: Post-prune inventory: 78 transcripts, 99 session files, 155 manifests, 45 skill texts, all
  present. 37312K -> 33892K.

#### D2 - A clean result is a claim about the instrument, and that is now a standing check

- D: Add "A Clean Result Is A Claim About The Instrument" to `risk_signaling.md` (live and seed,
  byte-identical): three named grounds to distrust a tidy measurement - zero variance in a column, a
  metric that improved with no mechanism, a complete-looking output built from an incomplete input -
  and the instruction to state what would make you distrust a result and confirm that thing is
  absent before reporting it. Four dated instances are cited by name.
- R: The session-local version of this lesson was recorded yesterday as "treat the harness as
  failing silently toward success", and the same failure shape then produced three more defects
  within a day: the DRAFT-heading counter that scored prose captures as silence, the truncated judge
  prompts that left 56 of 60 judgements answering a fragment while the keyed stage dutifully filled
  in every verdict, and the negative-control harness that read a CLI error as an empty verdict and
  reported a confident FAIL about a working judge. A treatment that has to be re-derived each time
  it is needed is not a control. Every one of the four was caught by a human noticing something
  looked too tidy, which is luck wearing the costume of a process.
- A: Writing it as a maxim without the instances was declined - the instances are what make it
  checkable, and a rule with no worked examples reads as a sentiment.
- F: `.memory-seed/skills/risk_signaling.md`, `memory_seed/seed/.memory-seed/skills/risk_signaling.md`.
- T: 939 tests pass, including the seed/live parity pin in `tests/test_session_schema.py`; diff
  confirms the two copies are identical. Repaired a splice while writing it: the morning's reversal
  section had been inserted mid-STOP-list, orphaning the External and Financial bullets.

#### D3 - Fold E8 and E9 into the strategy reports rather than leaving them as field evidence

- D: Amend the roadmap report with the veto finding and the benchmark, and the fit report with the
  competitive correction, using the established blockquote-amendment pattern.
- R: Both findings change strategy documents rather than only the evidence log. E8 showed the
  shipped control plane refusing user instructions - a product posture no roadmap ever chose - so
  the research gaps should include probing what the rules make agents refuse, not only what they
  make agents record. E9 removes the fit report's premise that the decision layer is unoccupied:
  two agent-curated Markdown systems now top an independent benchmark, which validates the substrate
  and compresses the differentiation onto validated writes, supersession and provenance - precisely
  where the benchmark's own winners fail, since the top-scoring wiki was the only entrant to
  fabricate on a never-stored question. Leaving that in the evidence log would let both reports keep
  asserting a market position that the August measurements contradict.
- A: Rewriting the affected sections was declined in favour of dated amendments, per the append-only
  invariant and the existing pattern - a reader needs to see that the claim changed and when.
- F: `business/research/memory-seed-strategic-roadmap-report.md`,
  `business/research/memory-seed-strategic-fit-report.md`,
  `business/research/field-evidence-log.md`, `CHANGELOG.md`.
- T: docs check green over 195 files; links check green over 157.

### Facts

- The guard confound from the capture experiment is retired on a measurement: 60/60 v2 runs report a
  reliable guard signal, `guard_called` 0/60.
- `experiments/agent-capture/runs-veto/` holds the pre-fix `risk_signaling.md` in 45 run directories;
  it is the independent variable of E8 and must survive any future cleanup.
- Archived run directories carry roughly 700K each, of which the transcript is 100K; the bulk is
  scaffolding, and `.agents/` within them is a byte-identical duplicate of the tracked seed.

## 2026-08-05 10:46 - Draft revision-scoped Constitution context benchmark

```yaml
entry_id: mse_kdd6fdwb18bmvtkp
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/experiment/strong-adr-constitution
topics:
  - retrieval
  - testing
evolves:
  - mse_a68ddwhcna0ef86m
```

### Summary

- Drafted the revision-scoped Constitution-aware ADR context benchmark after the owner approved definition work; no production retrieval contract, MCP surface, ADR ledger, scored sweep, or provider run changed.

### Decision

- D: Select experiment expansion roots by ranked canonical decision position, with the live relevance band retained only as an explicit diagnostic ablation; require v2 gold to bind Constitution evidence to the specific ADR revision rather than to an ADR concern as a whole.
- R: The live 836-entry validation withdrew `strong`/`weak`/`none` as a relevance authority (`relevance_calibrated: false`), so using it to decide context expansion would repeat a disproven assumption. Revision-scoped binding is needed to test the user's actual hypothesis: the top ranked decision may need its full decision block, relevant ADR context, and the governing Constitution text, while lower-ranked decisions remain compact references.
- A: Changing live `memory_search`, an ADR Markdown format, or a retrieval default was declined. This branch only supplies a reviewable experiment definition and deterministic materializer changes; CTX-12 deliberately remains a no-Constitution abstention control.
- F: `experiments/context-derivation/strong_context_v2.py`, `experiments/context-derivation/strong_context_fixture_v2.py`, `experiments/context-derivation/strong_context_sweep_v2.py`, `experiments/context-derivation/topk_metrics_v2.py`, `experiments/context-derivation/tasks/revision_constitution_manifest.v2.json`, `experiments/context-derivation/tasks/revision_constitution_gold.v2.json`, `experiments/context-derivation/revision_constitution_v2.py`.
- T: `python -m pytest tests\\test_context_derivation*.py -q` - 94 passed, 6 subtests passed. ESR integrity and topic checks passed; historical link-audit warnings and unrelated worktree residue remain unchanged. A Mermaid sidecar was not added because the new schema's three-level relationship is already explicit in the v2 binding pairs and test assertions.

### Follow-up

- Owner review is still needed for the rewritten twelve questions and revision-scoped gold before any scored sweep, candidate freeze, or Codex-only smoke run.

## 2026-08-05 10:53 - The ranking gate cannot A/B a unit change, and now says so

```yaml
entry_id: mse_fvqs26sdqbvcem9r
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/ranking-ab-scope
topics:
  - retrieval
  - testing
related_entries:
  - d1 -> mse_qeht233q9ebvf2ms
```

### Summary

- Filed the deferred gate proposal and recorded its conclusion at the source: `ranking-ab` covers
  signal flips over a fixed corpus and structurally cannot express a change to the retrieval unit.
- The proposal's recommendation is not to build it. What shipped is the scope note that stops the
  next agent trusting a run that would have reported numbers rather than refused.

### Decision

- D: File `docs/2_Todo/ranking-ab-unit-change-gate-proposal.md` as proposed-not-built, and add a
  SCOPE paragraph to the `ranking_ab` module docstring naming the four structural blocks and
  pointing at the measurement harness as the right instrument for unit changes.
- R: The entry-to-decision granularity flip did not pass through `ranking-ab`, and the first reading
  of that was that a gate had been skipped. It had not been: the corpus is extracted once before the
  arms split with the unit hardcoded (`ranking_ab.py:466`), `_rank` pins `chunks=` so the
  granularity kwarg is dead (`:310-321`, `semantic_cache.py:278`), the rank maps key on `entry_id`
  which decision chunks from one entry share (`:388-392`), and, in the block that actually decides
  it, a unit flip affects every entry, so the no-hit control that makes a pass mean anything has no
  control bucket to draw from. Building it anyway would require inventing a weaker control, which is
  how the attention signal came to return a vacuous PASS earlier today. The dangerous property is
  that a unit-flip run would not error: it would silently keep whichever colliding chunk sorted last
  and report a clean comparison, which is the failure shape this session just made a standing check.
- A: Building the per-arm corpus factories now was declined - the real work is the replacement
  control (lineage stability: a superseded decision's title should still surface its replacing head
  across the flip), and whether that is strong enough to make a unit-flip pass meaningful is itself
  an open question rather than an implementation detail. Leaving the limitation undocumented was
  declined for the same reason the proposal exists.
- F: `docs/2_Todo/ranking-ab-unit-change-gate-proposal.md`, `memory_seed/ranking_ab.py`.
- T: 13 ranking-ab tests pass unchanged (docstring-only code change); docs check green over 196
  files; links check green over 157.

### Facts

- The granularity flip was validated by the dry-run harness (71.0 to 96.8) and the real-corpus
  measurement in `experiments/decision-retrieval-scale/`, not by `ranking-ab`; that is the intended
  division, not a gap.
- `rank_session_memory` ignores its granularity argument whenever `chunks=` is supplied
  (`semantic_cache.py:278`) - a silent no-op, not an error.

## 2026-08-05 12:04 - Point the repo at its own build so the attention layer can accumulate

```yaml
entry_id: mse_57e1ek08qq9smwv3
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/mcp-local-build
topics:
  - retrieval
  - testing
evolves:
  - d1 -> mse_ss97qw0m8makbng2:d3
```

### Summary

- The attention layer shipped this morning had never executed: `.mcp.json` ran the published wheel,
  so the instrumentation was not the code any session here loads. This checkout now runs its own
  build, and a `memory_search` through it created the retrieval log that the published package had
  never produced.
- Wiring it up exposed a counting bug that would have biased the signal, and a preservation
  behaviour that was holding only by accident.

### Decisions

#### D1 - Dogfood the local build through `uv run`, and pin the config against a silent revert

- D: `.mcp.json` becomes `uv run --no-sync python -m memory_seed.mcp_server --stdio`. The published
  `uvx --from memory-seed` invocation stays as the constant `init` writes into user projects, so
  nothing changes for anyone downstream. `tests/test_mcp_local_build.py` pins that an update leaves
  a local-build entry alone.
- R: The attention layer, its MCP instrumentation and its A/B gate all shipped today against a
  server that never ran them, so the feature could not have accumulated a single event no matter how
  long it sat there - and the gate would have refused forever while looking like it was waiting for
  usage. Verified rather than assumed: before the change no `.retrieval-log.jsonl` existed, after it
  one search wrote six events. The invocation was chosen on measured behaviour. In the primary
  checkout it loads the working tree; in a real worktree it loads *that worktree's* source, which is
  what a dogfooding config should do; in a directory that is not a Python project it fails loudly
  with `ModuleNotFoundError` rather than silently falling through to a globally installed published
  copy, which is the stale-console-script trap this project has already been bitten by.
- A: `uvx --from .` was declined: it works in both places too, but its command is `uvx` and its args
  carry `memory-seed-mcp`, so `_merge_*_mcp` classifies it as ours and an update rewrites it back to
  the published package. An absolute path to `.venv/Scripts/python.exe` was declined as
  machine-specific in a tracked file. Adding an explicit local-build guard to all five upsert sites
  was declined as churn - the entry already survives because `"uv"` is not in `_OWN_MCP_COMMANDS`,
  and the real risk is not that the guard is missing but that the absence of that string looks like
  an oversight someone tidies up. So the fix is a named invariant plus a test, not more branching.
- F: `.mcp.json`, `memory_seed/core.py` (`_OWN_MCP_COMMANDS` comment),
  `tests/test_mcp_local_build.py`, `docs/2_Todo/attention-retrieval-signal-proposal.md`.
- T: 944 pass (5 new). The new tests were confirmed capable of failing: adding `"uv"` to
  `_OWN_MCP_COMMANDS` reverts the entry to `uvx --from memory-seed` and the assertions catch it.
  Local server verified by JSON-RPC probe in both the primary checkout and a real worktree.

#### D2 - Log one impression per entry per search, not one per result row

- D: `_record_retrieval_attention` dedupes `entry_id` within a single `memory_search` result before
  recording.
- R: Decision granularity became the default this morning, and one entry supplies several result
  rows under it, so the recorder was writing an event per row: the first real search logged 8 events
  across 6 entries. Scores are not affected today because impressions weigh zero
  (`attention.py:184`), which is exactly why this would have gone unnoticed - it inflates the log
  toward its compaction threshold now, and the moment anyone gives impressions a non-zero weight the
  signal would systematically favour entries holding the most decisions, which measures entry size
  rather than attention. Cheaper to fix while the log holds six events than after it holds five
  thousand.
- A: Weighting impressions by 1/N per entry was declined - it encodes a claim about how much a
  multi-decision hit is worth, and nothing measured supports one. Deduping records what happened:
  the entry was surfaced once, by one search.
- F: `memory_seed/mcp_server.py`.
- T: Same probe re-run after the fix: 6 events, 6 unique entries, no entry logged twice.

### Facts

- The repo's `.mcp.json` is this checkout's dev config only; what `init` writes into user projects
  comes from `_MCP_SERVER_COMMAND`/`_MCP_SERVER_ARGS` and is unchanged.
- A local-build `.mcp.json` entry survives `init`/`update` because `_merge_*_mcp` treats any command
  outside `_OWN_MCP_COMMANDS` as a third-party server and declines to overwrite it.
- The session's declared worktree path was an empty, unregistered directory - the phantom-worktree
  case. Two invocation probes run there failed for that reason and had to be redone against a real
  worktree.

## 2026-08-05 13:44 - Calibrate the band, and find that retrieval loses a third of its recall

```yaml
entry_id: mse_44fh1qcq29bwnbt6
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/experiment/band-calibration
topics:
  - retrieval
  - testing
evolves:
  - d1 -> mse_ss97qw0m8makbng2:d1
```

### Summary

- Calibrated the relevance band against 278 mechanically labelled queries on the 842-entry corpus,
  with a stratified fit/held-out split and a kill condition fixed before the run.
- The band is fixable - `top_over_median` reaches 0.85-0.87 held-out balanced accuracy against the
  shipped floor's 0.50 - but calibrating it turned out to be the least valuable thing the run found.
- Two unvalidated constants are costing roughly a third of achievable recall, and no abstention
  signal can compensate for an answer that never enters the result set.

### Decisions

#### D1 - Replace the absolute floor with a scale-free statistic; do not retune 6.0

- D: Recommend `top_over_median` (threshold ~1.22-1.32) in place of `RELEVANCE_FLOOR`, with
  `matched_frac` as the stronger alternative below ~100 entries, and keep
  `relevance_calibrated: False` until the two recall fixes land - both change the distribution any
  threshold would be fitted against, so fitting the band first means fitting it twice.
- R: The proposal's premise was that scores drift with corpus size. Reading `_lexical_score` said
  otherwise - pure per-chunk term overlap, no corpus statistics - so the N-curve was run as a test
  of the reading rather than as a fit, and confirmed it exactly: a positive scores 17.962 at every N
  from 25 to 800, invariant to the digit. The floor still fails at scale by a different mechanism.
  The best *spurious* match improves as the corpus grows, so a negative's top score climbs 19.1 to
  27.8 and by 800 entries the average negative outscores the average positive's own answer. Nothing
  can sit between them, which is why re-fitting `top_abs` optimally still only reaches 0.65
  balanced accuracy. The shipped rule meanwhile called 34 of 34 negatives answerable - specificity
  0.00, CI [0.00, 0.10], balanced accuracy exactly 0.50 - so it is not merely uncalibrated but
  carries no information at all. `top_over_median` measures the shape of the result set instead of
  its magnitude, which is why it is immune to the mechanism that kills the floor, and it held at
  0.899/0.900 AUC across both the lexical and semantic arms.
- A: Retuning 6.0 was declined on the N-curve evidence rather than on principle. Deleting the band
  outright was declined because a statistic that clears the pre-registered kill condition now
  exists, and the honest reading of the kill condition is that it was written to prevent shipping a
  prettier version of a broken rule, not to prevent shipping a working one.
- F: `experiments/band-calibration/` (labels.py, calibrate.py, FINDINGS.md, results-*.json).
- T: 278 labels, stratified fit/held-out (122 positives / 17 negatives held out), bootstrap AUC CIs,
  both arms. Two instrument bugs caught and fixed mid-run: the N-curve ranked to top-8 and scored a
  target that fell outside the window as 0.0, which reported "the answer's score dropped" when it
  measured "the answer left the window"; and the small-corpus stage took positives in build order,
  giving it zero title queries and silently changing the mix between stages.

#### D2 - The recall findings outrank the band, and both causes are single constants

- D: Record two candidate fixes ahead of the band in priority: raise the semantic blend weight
  (`semantic_component = cosine * 3.0`, `semantic_cache.py:927`), and re-examine the recency
  multiplier. Neither is applied in this branch - both need their own held-out fit.
- R: Semantic ranking is on by default and changes paraphrase recall by exactly zero - 63/120 in
  the top 8 with it on and off. The reason is arithmetic: cosine is bounded by 1, so a perfect
  semantic match contributes 3.0 against one tag match's 12.0, and the measured semantic share of
  `match_score` is 21.6%. It cannot reorder anything separated by more than three lexical points.
  That is not the embedding failing - sweeping the weight with everything else fixed lifts top-8
  paraphrase recall from 62% to 78% at weight 60, turning over by 120, so the optimum is a real
  interior point and the signal was there all along. Separately, chasing a 73-versus-63 discrepancy
  between two of my own harnesses isolated the recency multiplier: it removes 10 of 120 correct
  answers from the top 8. In a store whose purpose is retrieving old decisions, down-weighting age
  deserves an explicit argument, and `lambda_days=0.01` has never been validated against recall.
  Together these are worth more than any abstention label, because a perfect band on 62% recall
  correctly reports "nothing found" while the answer sits in the store.
- A: Shipping weight 60 now was declined - it is fitted on all 120 paraphrase queries with no
  held-out split, so it is an order of magnitude rather than a constant, and `ranking-ab` cannot
  gate a weight change any more than it can gate a granularity change.
- F: `experiments/band-calibration/semantic_weight.py`, `FINDINGS.md` sections 4b and 4c.
- T: Weight sweep over 0/3/10/30/60/120 with the corpus embedded once; recency isolated by
  re-running the same 120 queries with `recency_enabled` off; entry-coverage checked first and
  ruled out (a top-8 chunk window holds 7.47 distinct entries, so crowding is not the cause).

### Facts

- A chunk's lexical score is independent of corpus size; `_lexical_score` uses fixed field weights
  (tags 12, contexts 8, heading_path 6, lexical_terms 4, +1 per body term) and no corpus statistics.
- Served payload is flat in corpus size at ~3.6k tokens for top-8, so retrieval cost is set by
  `top_k` and `DECISION_TEXT_LIMIT`, not by how much is stored. The measured mean runs above the
  ~2.3k "typical" figure recorded when decision granularity shipped.
- The corpus holds 4 `replaced_by` edges across 808 nodes against 144 `evolves`, so every
  lifecycle-stability measurement in this project - including the one in
  `decision-retrieval-scale/measure.py`, which asked for 25 - rests on n=4.
- A local Model2Vec provider (`potion-base-8M`) supplies semantic ranking with no API cost, so the
  semantic arm is free to re-run.

## 2026-08-05 15:06 - Specify the recall fixes, and why the obvious one is wrong

```yaml
entry_id: mse_dka59wfne2be5fr0
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
topics:
  - retrieval
  - testing
evolves:
  - d1 -> mse_44fh1qcq29bwnbt6:d2
```

### Summary

- Specified the four retrieval fixes plus a prerequisite in
  `docs/2_Todo/retrieval-recall-fixes-proposal.md`. None applied.
- Committed straight to main rather than a `claude/docs/*` branch, against the branch convention.
  Recorded here rather than quietly, since the entry is the only place that slip is visible.

### Decisions

#### D1 - Order the fixes by dependency, and specify what each one must not break

- D: F0 (re-derive lifecycle labels from `evolves`) runs before F2 (recency); F3 (the band) is
  fitted last; each fix carries named non-regression guards that can fail, and `ranking-ab` gates
  none of them.
- R: Two orderings are load-bearing rather than tidy. Recency is partly doing supersession's job by
  accident, so weakening it may surface retired decisions above their replacements - and with 4
  `replaced_by` edges against 144 `evolves`, the current lifecycle label set cannot detect that.
  F2 would report a clean recall win while quietly regressing the thing the store exists for.
  Separately, F1 and F2 both move the score distribution that F3's threshold is fitted against, and
  the rank-fusion candidate in F1 would discard score magnitude entirely, so fitting the band first
  means fitting it twice or invalidating it outright. `ranking-ab` cannot gate any of this for the
  reason already recorded: it covers boolean flips over a fixed corpus, not weight changes.
- A: Applying the measured semantic weight now was declined - see D2. Fitting the band first was
  declined on the ordering argument above, despite it being the fix with the cleanest evidence.
- F: `docs/2_Todo/retrieval-recall-fixes-proposal.md`.
- T: n/a - specification. Each fix carries its own falsifiable guard; the proposal is falsifiable at
  F0, which either yields a powered lifecycle set or does not.

#### D2 - Do not ship the weight that the sweep found; the shape of the fix is wrong

- D: Specify three candidates for the lexical/semantic blend - raise the constant, normalise both
  components per query term, or reciprocal-rank fusion - and evaluate them on a held-out split
  stratified by query length rather than adopting weight 60.
- R: 60 lifted top-8 paraphrase recall from 62% to 78%, which is the largest measured win available
  and tempting to take directly. But lexical score accumulates over matched terms and fields while
  cosine is a single bounded number, so the two components scale differently with query length and
  no single additive constant can be right for a 3-term and a 20-term query. The sweep used 7-term
  queries throughout, so it measured the optimum at one point on the axis the fix is most likely to
  fail on. Stratifying by query length is the whole point of the evaluation. Rank fusion is
  specified because it sidesteps the class entirely, and because the band analysis independently
  found that the scale-free statistic beat every magnitude-based one - the same lesson arriving
  twice from different directions is worth taking seriously, though not worth assuming.
- A: Shipping 60 behind a feature flag was declined - a flag defers the measurement rather than
  making it, and the guards (P_title must not regress below ~95%, negatives must not become
  answerable) are cheap enough that there is no reason to skip them.
- F: `docs/2_Todo/retrieval-recall-fixes-proposal.md` (F1).
- T: Specified: `answer@1`/`answer@8` on paraphrase positives at 3/7/15 terms, held-out, with the
  two named guards as rejection criteria.

### Facts

- Corpus age is median 21 days, max 80, so recency's current spread is 1.00 to 0.45 (2.2x) and
  nothing reaches the 0.15 floor. It costs 8.3 points of top-8 recall at that mild setting; past
  ~190 days the penalty becomes 6.7x, so the damage grows as the archive ages.
- `_effective_lambda` already halves the decay for structural queries, which is a second recency
  mechanism overlapping the first - the state that produced the current confusion.
- This entry was appended on main because the proposal was committed there directly; the branch
  convention (`claude/<kind>/<topic>` then `session merge-branch`) was skipped.
