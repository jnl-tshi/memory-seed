## 2026-08-03 14:55 - Register the business knowledge area in the runtime index

```yaml
entry_id: mse_5y962348e85x7grr
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
topics:
  - control-plane
  - documentation
related_entries:
  - mse_q0csfqb2darg474m
```

### Summary

- Closed the session: ran the ESR report, registered the previously unrecorded `business/` knowledge area in the runtime index, and recorded the findings the closeout surfaced that need a human decision rather than an automated fix.

### Decision

- D: Add `business/` to the runtime index's Topology section, describing its dossier-versus-provenance convention, its three areas, its move-not-copy rule, and the fact that `docs check` does not validate it.
- R: The index contained no mention of `business/` at all, despite the area holding the market, wedge and pricing dossiers plus a seven-report research programme. An agent orienting from the index would not learn that a whole commercial-strategy tree exists, which is the restricted-update criterion of topology having changed and the immediate-update exception of stale content misleading active work. It is also the precise failure the research programme spent seven reports arguing about, so leaving it would have been an awkward thing to defend.
- A: Leaving the gap for the session that created `business/` was rejected because that session has ended and the omission is now load-bearing for anyone orienting. Updating `docs/2_Todo/0_NEXT_STEPS.md` with the programme's recommendations was rejected as out of scope: Report 7's actions are proposals awaiting JNL's approval, and promoting them unilaterally would contradict the plan's own rule that findings are not silently promoted.
- F: `.memory-seed/index.md`.
- T: `memory-seed esr --date 2026-08-02` reported integrity, topics, and docs lifecycle all OK; `links check` passed across 150 files.

### Follow-up

- No decision diagram was written for today's entries. The policy relocation is a placement change in the control plane with no spatial, temporal, migration, or concurrency structure, so prose carries it fully and a sidecar would add nothing.
- The lifecycle link sweep reports 55 open classification stubs corpus-wide, one of which belongs to this session: `mse_q0csfqb2darg474m` against `mse_re2dzyar7mcc32rp`, flagged as already related and a candidate for a lifecycle upgrade. The litmus points to `evolves` rather than `replaces` - moving two rules to `policy.md` refined where the earlier decision put them while leaving that decision standing, since Response Length stayed in `CLAUDE.md`. This is proposed, not written: converting a stub to a live edge requires explicit approval.
- Two seed twins have drifted from their live counterparts, `agent_collaboration.md` and `session_logging.md`. Neither was touched this session and skills are not edited without approval, so this is recorded rather than resolved.
- Two Codex worktrees remain: `adr-sidecar` carries six dirty files and `product-interest-landing-page` is clean. Both belong to another agent's work and were left alone. This session's own worktree is deregistered from git; its empty directory could not be deleted because the running shell holds it open.

## 2026-08-03 15:03 - Verify Codex ESR invocation and run closeout

```yaml
entry_id: mse_nv573gkpfd9m1fqf
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: main
topics:
  - control-plane
  - documentation
```

### Summary

- Verified why Memory Seed's `/esr` shortcut works in Claude but not Codex, then ran the full mechanical ESR preflight for 2026-08-03 from the canonical repository.

### Validation

- Current Codex documentation confirms repository-scoped reusable workflows now belong in `.agents/skills`; deprecated custom prompts are user-local and invoke as `/prompts:<name>`, while Claude loads the existing `.claude/commands/esr.md` directly.
- `memory-seed esr --date 2026-08-03` reported integrity and topics OK. It retained existing warnings for 55 lifecycle classification stubs, two docs-lifecycle paths, two drifted seed twins, and two pre-existing Codex worktrees.

### Follow-up

- Memory Seed can add a repository-scoped Codex ESR skill in a separate implementation workstream; no control-plane files were changed during this verification-only closeout.

## 2026-08-03 15:38 - Implement living ADR sidecar core and Trace workspace

```yaml
entry_id: mse_kbc2mq9972ppy1vw
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/adr-sidecar
```

### Summary

- Implemented the living ADR sidecar core, mandatory content-bound MCP review gate, CLI and read-only MCP surfaces, structural branch reconciliation, typed Trace endpoints, and the ADR workspace.

### Decisions

#### D1 - Derive ADR authority from an append-only ledger and gate linked writes before mutation

- D: Store one living ADR per architectural concern, derive its current authority by replaying append-only proposal and transition events, and fail closed before any session or sidecar write when a new decision evolves or replaces an ADR lineage member.
- R: This keeps the concise architectural view current without rewriting evidence history, while a content-bound receipt guarantees the LLM reviewed the exact affected ADR state and draft before the transactional writer resumes.
- A: A mutable current-status field and a post-write review hook were rejected because either can drift from history or permit an unreviewed mutation.

#### D2 - Keep ADR writers in the existing transaction and expose every other surface as read-only

- D: Publish ADR events through the parent-first recoverable session transaction, reconcile ledgers structurally during branch fuse, and expose ADR inspection through CLI, read-only MCP tools, typed Trace APIs, and a dedicated Trace workspace.
- R: Reusing the existing writer and recovery boundary avoids a second mutation path, while read-only projections give humans and agents the same authority, rationale, lineage, and source-decision navigation.
- A: A standalone MCP ADR writer and the unfinished pointer-only ADR prototype were rejected because they would create competing authority and transaction models.

### Implementation

- Added `.memory-seed/decisions/<adr_id>.md` parsing, validation, replay, rendering, reverse membership, review receipts, ADR outcome events, and structural reconciliation.
- Added `adr promote`, `adr revise`, `adr transition`, `adr list`, `adr show`, and `adr check`; added `memory_adrs_list`, `memory_adr_show`, `memory_adr_review`, and `memory_adrs_check`.
- Added typed `GET /api/v1/adrs` and `GET /api/v1/adrs/{adr_id}` endpoints plus the searchable ADR Trace workspace and regenerated OpenAPI, TypeScript, and production assets.

### Validation

- ADR-focused core/MCP tests: 8 passed.
- Trace compile and API/contract tests: 23 passed.
- Trace client typecheck passed; client suite: 232 passed; production build passed.
- `git diff --check` passed.

### Follow-up

- Reconcile the checkpoint with current `main`, dogfood three accepted architectural concerns through the mandatory review path, run the full regression/browser gates, and integrate locally without pushing.
