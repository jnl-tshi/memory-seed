---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-21
---

## 2026-08-21 20:55 - Review the 2026-08-13 inbox drop and re-baseline it against current HEAD

```yaml
entry_id: mse_fjsmxx9z9yhny7ga
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/inbox-proposals-review-fb731f
```

### Summary

- Reviewed the four documents dropped into `docs/1_Inbox/` on 2026-08-13 (harness gap-opportunity
  synthesis, Claude and Codex comparison lines, exempt storylines living doc), which `0_NEXT_STEPS.md`
  had recorded on 2026-08-14 as untriaged.
- Re-verified every load-bearing claim in the synthesis report against current HEAD (67 commits past
  its `9c4b8f8f` pin) and wrote up findings and recommended dispositions, without applying any lane move.

### Decision

- D: Wrote `docs/4_Reference/INBOX-ASSESSMENT-2026-08-13-DROP.md`, a findings-only review of the four
  2026-08-13 Inbox documents. It re-verifies every load-bearing claim in the synthesis report against
  the current tree, finds no material drift, surfaces that the report's own O1 question ("does
  preserved rationale improve a later decision") is now being directly tested by the separate
  `experiments/decision-replay/` v4/v5 pilots (2026-08-13 through 2026-08-18) that the report could not
  have known about, and recommends — without applying — the same retire-to-`7_Replaced/`-pointing-at-
  the-survivor precedent the 2026-07-20 triage used: keep the synthesis as the surviving record, retire
  both comparison lines once it is promoted. Also corrected `docs/1_Inbox/README.md`'s stale "3
  documents" status line to 4 and pointed it at the new assessment, and regenerated the stale
  `docs/4_Reference/README.md` / `docs/README.md` indexes.
- R: `0_NEXT_STEPS.md` recorded on 2026-08-14 that the drop had not yet been triaged; JNL asked for that
  review now. The ADR governing inbox promotion (`adr_inbox_promotion_workflow`, still Proposed) requires
  evidence-checked triage precisely because proposals "read as missing capability only because they
  predated the code that shipped them" — so the review had to re-verify the report's dated claims rather
  than restate them, and the decision-replay cross-reference is the concrete case that would have been
  missed otherwise. No lane move was made because that disposition is JNL's call, not this review's;
  the assessment states recommendations and stops there, following the `INBOX-ASSESSMENT.md` precedent
  format.
- A: Considered applying the retire-and-promote lane moves directly rather than recommending them —
  rejected because the promotion ADR is unratified and this is explicitly a review task, not a triage
  execution. Considered leaving the README count uncorrected as out of scope — kept the one-line fix
  because it is a plain factual miscount, not a disposition judgment.
- F: `docs/4_Reference/INBOX-ASSESSMENT-2026-08-13-DROP.md` (new), `docs/1_Inbox/README.md`,
  `docs/4_Reference/README.md`, `docs/README.md`.
- T: `docs check` clean before and after (16 pre-existing `missing-todo-yaml` warnings, no errors,
  214 files checked after the new doc). `docs index --check` flagged both generated indexes as stale
  after the new file landed; `docs index` regenerated them and a re-check confirmed current.

## 2026-08-21 21:14 - Retire both harness-engineering comparison lines to 7_Replaced

```yaml
entry_id: mse_arws1v667apebgbd
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/inbox-proposals-review-fb731f
```

### Summary

- JNL confirmed the retirement recommended in the 2026-08-13-drop review: the Claude and Codex
  harness-engineering comparison lines move to `7_Replaced/`, pointing back at the synthesis report.
- Updated every inbound reference (Inbox README, the synthesis report's own source links,
  `0_NEXT_STEPS.md`, `7_Replaced/README.md`) and regenerated the three stale generated indexes.

### Decision

- D: `git mv` both `harness-engineering-comparison-claude.md` and `-codex.md` from `1_Inbox/` to
  `7_Replaced/`, added `status: superseded` / `replaced_by` / `replaced_on` front matter and a
  superseded banner to each pointing at `memory-seed-harness-gap-opportunity-report.md` (which stays in
  `1_Inbox/` — its own O1-O10 register is still undispositioned). Updated the synthesis report's
  "Primary inputs" links, `docs/1_Inbox/README.md` (status line, contents list, new "Triaged out on
  2026-08-21" section), `docs/2_Todo/0_NEXT_STEPS.md`'s drop section, `docs/7_Replaced/README.md`'s
  prose contents list, and the disposition table in `docs/4_Reference/INBOX-ASSESSMENT-2026-08-13-DROP.md`
  (added an update note rather than rewriting its past-tense recommendation prose). Regenerated
  `docs/1_Inbox/README.md`, `docs/7_Replaced/README.md`, and `docs/README.md`'s generated tables.
- R: JNL asked directly to retire the comparisons, which is the confirmation the prior review
  (`mse_fjsmxx9z9yhny7ga`) said was needed before applying its own recommendation — that entry
  deliberately stopped short of moving files because the disposition was JNL's call, not something to
  infer from a review task. Both lines' unique contributions are already folded into the synthesis's
  §2, so nothing is lost by retiring rather than keeping both readable in full; the synthesis itself was
  not promoted to `4_Reference/` because that promotion was conditioned on JNL confirming its §7
  decision sequence too, which was not part of this ask.
- A: Considered promoting the synthesis report to `4_Reference/` in the same pass, matching the full
  crosswalk precedent where survivor and retirement happened together — rejected because JNL asked only
  to retire the comparisons, and the synthesis promotion has its own unmet condition (confirming the §7
  sequence). Considered leaving the assessment doc's disposition table as originally written (still
  "recommended") — rejected as misleading once applied; added an update note instead of rewriting it
  wholesale, since the original findings and re-verification are still accurate as of when they were
  written.
- F: `docs/7_Replaced/harness-engineering-comparison-claude.md` (moved from `1_Inbox/`),
  `docs/7_Replaced/harness-engineering-comparison-codex.md` (moved from `1_Inbox/`),
  `docs/1_Inbox/memory-seed-harness-gap-opportunity-report.md`, `docs/1_Inbox/README.md`,
  `docs/2_Todo/0_NEXT_STEPS.md`, `docs/7_Replaced/README.md`, `docs/4_Reference/INBOX-ASSESSMENT-2026-08-13-DROP.md`,
  `docs/README.md`.
- T: `docs check` clean throughout (16 pre-existing `missing-todo-yaml` warnings, no errors, 214 files).
  `docs index --check` caught 4 broken links and 3 stale generated indexes mid-edit;
  `docs index` regenerated them and a final `docs check` / `docs index --check` pass confirmed clean.

## 2026-08-21 21:34 - Merge inbox review to main; capture and assess the 2026-08-20 proposal drop

```yaml
entry_id: mse_egtmeyy2w5k24cac
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- Merged `claude/inbox-proposals-review-fb731f` into `main` via `memory_session_integrate`, landing the
  2026-08-13-drop review and comparison-line retirement.
- Found four new, untracked, unreviewed proposals (dated 2026-08-20) sitting in the shared root
  checkout; captured them verbatim in their own commit, then wrote a full crosswalk assessment against
  shipped capability and recommended dispositions per document, without applying any.

### Decision

- D: In the primary checkout (root, `main`): committed the four untracked 2026-08-20 proposal captures
  as-is (`f4e5bb06`), merged the review branch (`d5414b9e`), then wrote
  `docs/4_Reference/INBOX-ASSESSMENT-2026-08-20-DROP.md` — a crosswalk of all four proposals against
  already-shipped capability (lifecycle edges, DRAFT decision shape, evidence packets, write-time
  refusal guards, quality-v0, existing interactive-retrieval policy) plus a per-document recommendation.
  Updated `docs/1_Inbox/README.md`'s status/contents to name the drop and point at the assessment, and
  regenerated the three affected generated indexes. Two root-checkout writes required an explicit
  `memory_worktree_guard --allow-root-write` override (the capture commit and this session's document
  writes); recorded here per `.memory-seed/skills/agent_collaboration.md`'s handoff requirement.
- R: The primary checkout was dirty with substantial (2,829-line) untracked content that
  `memory_session_integrate` correctly refused to merge over. Per project convention (a shared root
  checkout in this state is handed to JNL rather than stashed), the files were inspected first: they
  were plainly new inbox captures matching the existing naming/lane convention, not incidental scratch
  output, so capturing them as their own commit (not stashing, not deleting) was the safe path, and it
  unblocked the requested merge. The crosswalk assessment follows the same discipline the 2026-08-13-drop
  review used and the ADR `adr_inbox_promotion_workflow` requires: none of the four proposals cited this
  repository's actual code, and two of them (Docs 1 and partially 2) substantially restate already-
  shipped mechanism under new vocabulary — exactly the failure mode conservative triage exists to catch.
- A: Considered leaving the untracked files alone and asking JNL before touching them — rejected because
  they were unambiguously inbox-shaped captures already sitting in the correct lane, not ambiguous scratch
  state, and the user's own request ("review the docs in the main worktree") pointed directly at them.
  Considered triaging (retiring/promoting) the four proposals outright — rejected because this turn was a
  review request, not an authorization to apply dispositions, consistent with how the 2026-08-13 review
  was handled before JNL separately confirmed the retirement.
- F: `docs/1_Inbox/active-truth-execution-control-proposal.md`,
  `docs/1_Inbox/memory-seed-first-principles-proposal.md`,
  `docs/1_Inbox/memory-seed-governed-interactive-retrieval-proposal.md`,
  `docs/1_Inbox/semantic-compression-benchmark-proposal.md` (all captured, not authored),
  `docs/4_Reference/INBOX-ASSESSMENT-2026-08-20-DROP.md` (new), `docs/1_Inbox/README.md`,
  `docs/4_Reference/README.md`, `docs/README.md`.
- T: `docs check` clean throughout (16 pre-existing `missing-todo-yaml` warnings, no errors; 219 files
  after the new assessment doc). `docs index --check` flagged stale generated tables after each commit;
  `docs index` regenerated them and a final check confirmed current. `memory_session_integrate` dry-run
  showed a clean two-entry fuse plan with no conflicts before the real merge.

## 2026-08-21 21:59 - Write the Claude-attributed review of the 2026-08-20 drop and correct the ICM error

```yaml
entry_id: mse_7f949b09wyhkhxh6
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: main
```

### Summary

- Wrote `docs/4_Reference/inbox-2026-08-20-drop-review-claude.md`, an opinion-and-verdict review of the
  four 2026-08-20 proposals, attributed to Claude in the title so a parallel Codex line can be written
  and compared rather than merged.
- Corrected a factual error in the same-day assessment: it claimed Document 4's "ICM" references were
  invented. They are not.

### Decisions

#### D1 - Correct the ICM absence claim in the 2026-08-20 assessment

- D: Rewrite the assessment's headline finding and its Document 4 section. The claim that "ICM ... is
  not [an existing component] — no such term appears anywhere else in this repository" is withdrawn.
  ICM is Interpretable Context Methodology (the methodology behind ICM Architect), and it is named in
  this repository's own `experiments/stack-benchmark/solar-filaments-001/PREREGISTRATION.md` as a
  treatment evaluated alongside Memory Seed, Graphify, and Semble. The headline finding is restated
  precisely: the four proposals ARE grounded in the project's component ecosystem (Memory Trace, Trail,
  sidecars, Constitution, ADRs, MCP/CLI/REST, Graphify, Semble, ICM, all placed correctly), and are NOT
  grounded in shipped repository state (no file path, entry id, ADR id, spec ref, or command output
  across 2,829 lines).
- R: The original claim was produced by a `grep -rl "ICM"` over `docs/` whose only non-binary hit was a
  substring match, from which absence was inferred; a `grep -rnw` across `docs/`, `memory_seed/`, and
  `experiments/` finds the real reference immediately, and JNL confirmed the expansion. The error was
  load-bearing: it was used as the headline example of "ungrounded authorship" against the one document
  that explicitly requires a repository audit before implementation, which was unfair to that proposal
  and would have biased its triage. Correcting the record matters more than the tidiness of the original
  framing, and the restated finding is both narrower and defensible.
- A: Considered leaving the assessment as written and noting the correction only in the new review —
  rejected because the assessment is the factual record other documents will cite, so the error had to
  be fixed at source rather than annotated elsewhere. Considered deleting the ecosystem-grounding
  criticism entirely — rejected because the underlying observation (no citation of shipped repository
  state) is true, useful, and survives once stated precisely.
- F: `docs/4_Reference/INBOX-ASSESSMENT-2026-08-20-DROP.md`.
- T: `grep -rnw "ICM"` across `docs/`, `memory_seed/`, `experiments/` returns the stack-benchmark
  preregistration; `docs check` clean (220 files, 16 pre-existing warnings, no errors).

#### D2 - Publish the review as a named Claude line, separate from the assessment

- D: Write `docs/4_Reference/inbox-2026-08-20-drop-review-claude.md` as a distinct document from the
  assessment: the assessment stays the factual crosswalk, the review carries judgment, per-document
  verdicts, and a recommendation table. The `-claude` suffix follows the harness-comparison line
  convention. The review records its own method limits (shipped-capability judgments came from specs,
  not from reading each implementing module), carries the ICM error forward as a stated caveat rather
  than burying it, and flags that the four proposals appear to share one author/context so the drop is
  one strong opinion rather than four independent signals.
- R: JNL asked for a review labelled as my work. Keeping it separate from the assessment preserves the
  distinction the 2026-07-18 `INBOX-ASSESSMENT.md` precedent established between an evidence record and
  a position, and the `-claude` naming makes a future Codex line comparable rather than a merge
  conflict. Recording the correlated-source observation matters for triage: the project's own harness
  report already established that non-independent reviewers are not multiplied evidence, and the same
  logic applies to four proposals that converge on identical themes and vocabulary.
- A: Considered folding the opinion into the existing assessment as a "my take" section — rejected
  because it would mix the evidence record with the argument and leave no clean surface for a parallel
  Codex evaluation. Considered placing the review in `1_Inbox/` alongside the proposals as the harness
  lines were — rejected because it is a completed evaluation with a clear status and author, which the
  lane rule assigns to `4_Reference/`, not to the folder for untriaged captures.
- F: `docs/4_Reference/inbox-2026-08-20-drop-review-claude.md` (new), `docs/4_Reference/README.md`,
  `docs/README.md`.
- T: `docs check` reports `Docs lifecycle OK (220 file(s) checked)` with no errors; `docs index`
  regenerated two stale tables and `docs index --check` confirms current.

## 2026-08-21 22:10 - Publish Codex assessment of the four 2026-08-20 inbox proposals

```yaml
entry_id: mse_1pnca07xsfs8tcay
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/docs/inbox-four-assessment
```

### Summary

- Reviewed the four 2026-08-20 inbox proposals and published a separate Codex comparative assessment in the reference lane; no inbox document changed lifecycle.

### Decisions

#### D1 - Publish assessment without changing proposal disposition

- D: Added `docs/4_Reference/inbox-2026-08-20-drop-review-codex.md` and regenerated the documentation indexes; the four source proposals remain assessed but untriaged in `docs/1_Inbox/`.
- R: The review finds one coherent thesis expressed through four documents, with useful strategic framing and two bounded experimental candidates, but no evidence for promoting a replacement control plane as submitted.
- A: Did not move or retire any proposal; a lifecycle disposition requires a user decision beyond the requested assessment.
- F: `docs/4_Reference/inbox-2026-08-20-drop-review-codex.md`, `docs/4_Reference/README.md`, `docs/README.md`.
- T: `python -m memory_seed.cli docs check` passed with the repository's 16 pre-existing missing-todo-yaml warnings; `git diff --check` passed.

### Follow-up

- The proposed next order is: use first-principles as strategy, instrument governed interactive retrieval minimally, run a thin semantic-compression pilot, then revisit active-truth controls from measured failures. No diagram sidecar: this review has no decision topology beyond that prose sequence.

## 2026-08-21 22:23 - Synthesize the evidence-first governed retrieval plan

```yaml
entry_id: mse_qg9p71qx57rtq99n
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/docs/governed-retrieval-evidence-plan
```

### Summary

- Synthesised the four 2026-08-20 Inbox proposals and the Claude/Codex assessments into one promotion-ready evidence programme, while preserving the user's instruction that it remain in the Inbox.

### Decisions

#### D1 - Create a bounded evidence programme but keep it in Inbox

- D: Add `docs/1_Inbox/memory-seed-evidence-first-governed-retrieval-plan.md` as the candidate Todo plan, with no lifecycle promotion or implementation authorization yet.
- R: Both reviews converge on one thesis, but current repository evidence shows that existing retrieval, quality, and authority owners must be measured before any new architecture is admitted.
- R: The plan therefore sequences owner/baseline mapping, governed task-time retrieval instrumentation, a zero-generation D/R compression pilot, and three independent execution-assurance negative controls.
- R: Only a reproduced gap may extend an existing owner or become a focused new proposal; a passing control closes the candidate as `DO NOT BUILD`.
- A: Rejected promoting the four source documents unchanged or treating Active Truth as a P0 control plane, because those routes duplicate current owners and assume unmeasured gaps.
- A: No diagram sidecar was added: this turn authored a planning document but did not implement or attach an ADR-governed retrieval/data pipeline.
- F: Added `docs/1_Inbox/memory-seed-evidence-first-governed-retrieval-plan.md`; updated `docs/1_Inbox/README.md` and the generated count in `docs/README.md`.
- T: `memory-seed docs index --check`, `memory-seed docs check`, and `git diff --check` passed; the docs check retained 16 pre-existing incomplete-metadata warnings in unrelated Todo files.

### Follow-up

- JNL reviews the synthesis. Promotion, if approved later, is a file move to `docs/2_Todo/`, a status change to `active`, and activation of Workstream 0; the source proposals remain independently dispositioned.
