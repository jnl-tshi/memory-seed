---
tags:
  - session-log
  - memory-seed
session_date: 2026-08-27
---

## 2026-08-27 00:00 - Reject the swarm topic-discovery model; name write-time proposal as the alternative (JNL)

```yaml
entry_id: mse_z1fck64f011sv41k
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/reject-topic-discovery-swarm-model
```

### Summary

- JNL ruled on the three genuinely-open items presented interactively this turn. Recorded the
  topic-discovery rejection and new direction now (docs-only, done); the other two
  (adjudication-queue re-review, Verging Labs replication) are recorded as open decisions #12/#13
  pending further work this session.

### Decisions

#### D1 - Reject the swarm batch-discovery model; name write-time proposal as the replacement direction

- D: Moved `docs/2_Todo/topic-discovery-from-evidence.md` to
  `docs/6_Rejected/topic-discovery-from-evidence.md` with a closing note, and added open decision
  #11 in `docs/2_Todo/0_NEXT_STEPS.md` naming JNL's alternative: topics proposable at write time by
  any writing agent, growing the tree incrementally, with review (perhaps by a swarm) deferred
  rather than discovery itself being a batch sweep. Not yet scoped as a proposal - explicitly
  flagged as needing its own scoping pass before anything is built.
- R: JNL: this shouldn't be done using a swarm; proposed topics should be available at write time
  so agents can propose better topics and grow the tree as needed, with proposals reviewed later.
  This rejects the doc's core mechanism (a retrospective batch sweep aggregating swarm-emitted
  candidates into statistics before any proposal exists) while leaving its measured findings intact
  (DAG claim unsupported, keyword-estimate unreliability, agreement-vs-validity gap) - those survive
  in the closing note as evidence that would carry over to whatever replaces this.
- A: Rejected treating this as a straightforward accept/reject of the existing model - JNL's answer
  named a materially different mechanism (per-entry/write-time vs batch/swarm), so recorded as a
  rejection-plus-new-direction rather than silently substituting a redesigned version of the same
  doc. Rejected folding the new direction into `vocabulary-proposal-mode-proposal.md` directly -
  that proposal is measurement-triggered and swarm-run, not write-time and per-agent, so conflating
  them would misrepresent both.
- F: `docs/2_Todo/topic-discovery-from-evidence.md` (moved),
  `docs/6_Rejected/topic-discovery-from-evidence.md` (new location + closing note),
  `docs/2_Todo/0_NEXT_STEPS.md` (open decisions #11, #12, #13 added; item 3 of the Evidence
  programme section still references the adjudication queue's original framing).
- T: `memory-seed docs index` regenerated (`2_Todo/README.md`, `6_Rejected/README.md`,
  `docs/README.md`); `memory-seed docs check` - 223 files, 16 pre-existing warnings unchanged;
  `memory-seed docs index --check` clean.

### Follow-up

- Open decision #11 (write-time topic proposal) needs its own scoping proposal before any build -
  not attempted here, deliberately, since JNL only named the direction, not a mechanism.
- Open decision #12 (adjudication queue): JNL asked to re-review the 22 rows rather than sign off
  as-is - the rows were presented this turn; row-by-row outcome pending a follow-up pass.
- Open decision #13 (Verging Labs replication): JNL asked to run the independent replication before
  deciding on submission - concrete plan (harness, cost ~$25, ~1 day wall-clock) identified this
  turn; not yet kicked off pending confirmation to spend that budget now, given the early-September
  deadline is close.

## 2026-08-27 00:12 - Document model-tier task-packet budgets and sweep routing rationale

```yaml
entry_id: mse_3r4rwsad2ssn5fg8
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/docs/model-tier-packet-budgets
```

### Summary

- Documented the accepted provisional model-tier task-packet budgets, the evidence boundary behind them, and their consequences for ESR/ADR orchestration.
- Kept the assessment in the reference lane because it records research and an operating baseline; implementation in the sweep skills remains a separate change.

### Decisions

#### D1 - Adopt capability-tier task-packet bands

- D: Use 16K/24K/32K target-soft-shard thresholds for economy workers, 32K/48K/64K for balanced workers, and 64K/96K/128K for frontier workers.
- R: Advertised context capacity is not a reliable inference limit. Larger models can accept more prepared evidence, but all tiers should still receive the smallest sufficient packet rather than a corpus dump.
- A: A universal 32K ceiling was rejected because it confuses the economy-tier safety boundary with larger-model capacity. Scaling linearly to each model's full context window was rejected because retrieval distraction, synthesis quality, latency, and cost do not scale with the advertised request limit.
- F: `docs/4_Reference/model-tier-task-packet-budget-assessment.md`, `docs/4_Reference/README.md`, and `docs/README.md`.
- T: `python -m memory_seed.cli docs index --check`, `python -m memory_seed.cli docs check`, and `git diff --check` passed; docs check retained 16 pre-existing incomplete-metadata warnings.

#### D2 - Route sweeps by packet tokens and worker tier

- D: Make estimated prepared-packet tokens and the selected leaf worker tier the primary ESR/ADR orchestration signal; retain the existing 20-candidate threshold only as a compatibility fallback when token estimation is unavailable. Use a lightweight offline estimate with safety margin, then provider-native counting at dispatch when available.
- R: Candidate count ignores variation in evidence size and decision-chain density. The worker receiving a leaf packet determines its safe band; a frontier orchestrator does not raise an economy worker's shard limit. A built-in approximation preserves the network-free core, while provider counts provide model-specific request-fit and benchmark evidence.
- A: Adding a mandatory tokenizer or network dependency to the core was rejected because tokenization differs across providers and the core must remain network-free and model-independent.
- F: The accepted design and benchmark requirements are recorded in `docs/4_Reference/model-tier-task-packet-budget-assessment.md`; `.memory-seed/skills/adr_sweep.md` and `.memory-seed/skills/end_of_turn.md` are deliberately unchanged pending implementation.
- T: The assessment identifies generic benchmark evidence separately from the provisional engineering defaults and specifies the Memory Seed benchmark required before durable locking.

### Follow-up

- Implement the estimator and replace the sweep's candidate-count-first routing in a separate, tested workstream.
- Run provider-counted packet benchmarks before promoting the provisional thresholds into durable skill behavior.

## 2026-08-27 01:06 - Run the independent memory-index dry-run replication on a second, unrelated corpus

```yaml
entry_id: mse_97jrpfbzq4pyr77h
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/memory-index-independent-replication
```

### Summary

- Ran the independent replication JNL called for on open decision #13: built a second,
  unrelated fixture and narrative (durstr, not strutil), ran the full seed/quiz/judge/score
  pipeline, and recorded the result as "Run 6" in the dry-run plan. Kill condition CLEAR (80.6 vs
  Zep's 75.1), but capture loss reproduced worse than the original corpus - a real, generalizable
  finding, not a fixture artifact.

### Decisions

#### D1 - Build a fully independent second fixture rather than reuse strutil with new facts

- D: Cloned `experiments/agent-capture/templates/claude-L3` to `claude-L3-durstr` (gitignored,
  like all fixture templates), replaced only the project-specific files (`strutil/` ->
  `durstr/duration.py`+`config.py`, `run_checks.py`, `README.md`, `settings.json`) leaving the
  control-plane files untouched, and wrote a new `facts.json` (10 seeding briefs, 31 questions,
  same 6/6/3/4/4/8 category shape) at `experiments/memory-index-dryrun-corpus2/`, retargeting a
  copied `dryrun.py`'s `TEMPLATE` constant at the new fixture.
- R: JNL chose the more rigorous of two options presented (full new fixture vs. same code + new
  facts only) specifically because a shared codebase between the two corpora could let a
  `strutil`-specific quirk explain agreement between them either way.
- A: Rejected parameterizing the original `experiments/memory-index-dryrun/dryrun.py` with a
  `--facts`/`--template` flag instead of copying it - the original is a frozen, already-cited
  instrument (run 3's 96.8 is quoted elsewhere); changing its interface risked altering behavior
  the existing citations depend on. A full copy keeps both instruments independently frozen.
- F: `experiments/memory-index-dryrun-corpus2/{dryrun.py,facts.json,answer_visible.py}`,
  `experiments/agent-capture/templates/claude-L3-durstr/` (gitignored fixture, not committed),
  `.gitignore` (new `experiments/memory-index-dryrun-corpus2/runs/` entry).
- T: Fixture sanity check - `PYTHONPATH=. python run_checks.py` passed on the new durstr fixture
  before any seeding session touched it.

#### D2 - Run the full pipeline and record the result as-measured, including the capture-loss finding

- D: Ran `seed` (10/10 sessions exit=0), `quiz` (all 31 questions answered across 8 batches, one
  Windows `rmtree_force` cleanup crash on a temp copy dir after all answers were already written -
  cleaned up manually with `rm -rf` per the project's known Windows/OneDrive file-lock pattern,
  no data lost), `judge` (codex, 31/31), and `score`. Recorded the full result as "Run 6" in
  `docs/2_Todo/memory-index-dry-run-plan.md`, diagnosed both failure shapes by reading the seeded
  workspace directly (not just the quiz transcript): total capture loss for 3 facts (present in
  `README.md`/`RELEASE.md`, absent from both session entries and `index.md`) and one stale
  `index.md` Active State fact (captured once early, never updated after a later revision) that
  produced a confidently wrong answer rather than an abstention.
- R: Run 3's own session record (`mse_rrv13mdsnrm8e2n0`) established the original corpus's score is
  carried by `index.md`, not retrieval - checking whether the same held here, and whether it held
  correctly, was necessary to diagnose this run's misses honestly rather than guessing "retrieval
  failed". Reading the workspace directly (RELEASE.md, README.md, index.md) rather than trusting
  only the quiz answers is what let the two failure shapes be told apart instead of lumped together
  as one vague "capture loss" claim.
- A: Rejected treating the lower 80.6 as a fixture-quality problem and re-running with a "cleaner"
  fixture - the seeded workspace evidence shows the seeding agent did the real work correctly every
  time (the files are right); the gap is entirely on the memory-capture side, which is the honest
  and more useful finding for JNL's submission decision. Rejected omitting the mid-run crash from
  the write-up - it is a real operational wrinkle (Windows file locks during Python `rmtree`) worth
  recording even though it did not cost any data.
- F: `docs/2_Todo/memory-index-dry-run-plan.md` ("Run 6" section, frontmatter `status`/
  `next_action`), `docs/2_Todo/0_NEXT_STEPS.md` (open decision #13 updated with the result).
- T: `memory-seed docs check` - 223 files, 16 pre-existing warnings unchanged; `memory-seed docs
  index` regenerated (`2_Todo/README.md`, `docs/README.md`); `memory-seed docs index --check`
  clean.

### Follow-up

- JNL still needs to make the actual submission call (open decision #13 remains open, now with the
  replication result in hand): the kill condition doesn't block submission, but the capture-loss
  finding is worth reading first.
- The capture-loss finding itself (real files updated, memory not) is a candidate for its own
  follow-up investigation independent of the Verging Labs question - not filed as a separate
  next-steps item here, since that scoping decision belongs to JNL, not to this entry.
