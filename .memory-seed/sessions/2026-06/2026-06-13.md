---
tags:
  - session-log
  - memory-seed
session_date: 2026-06-13
default_user_initials: JN
---

# Session Log - 2026-06-13

## 2026-06-13 14:25 - Release 2.6.0 prep: Gemini hook fix, Copilot VS Code, hygiene, version bump (uncommitted)

```yaml
entry_id: ms-757053d4
user_initials: JN
agent_type: claude-opus-4-8
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Executed Release 2.6.0 prep per the approved roadmap (NEXT_STEPS.md "Active Roadmap"). All content + version bump done and verified (116 tests OK, doctor healthy, version 2.6). **Stopped before any commit** per user instruction; commits to be split logically when authorized. Rides along with the uncommitted 2026-06-11 SessionStart + Copilot CLI work.

### Decisions

#### D1 - Fix dead Gemini hook wiring (verified events)

- D: Repointed Gemini session-log `Stop`→`AfterAgent` and retrieval `UserPromptSubmit`→`BeforeAgent`; added `_strip_gemini_dead_hooks` to remove the stale entries on `update`; aligned the `--gemini` hook output to `hookSpecificOutput.additionalContext`.
- R: Verified via geminicli.com/docs/hooks/reference that Gemini exposes neither `Stop` nor `UserPromptSubmit` (turn-end is `AfterAgent`, prompt-submit is `BeforeAgent`), so the prior wiring never fired. Fixing only the event without the output field or the strip would leave it half-broken.
- F: `memory_seed/core.py` (merges + strip + both orchestration lists), `session-log-check.py` / `memory-retrieval-check.py` `--gemini` branches (+seed twins), tests.
- T: New `test_gemini_session_log_hook_uses_afteragent_not_stop` + `test_strip_gemini_dead_hooks_*`; dogfood shows `.gemini/settings.json` now `AfterAgent`/`BeforeAgent`/`SessionStart` with the dead entries stripped.

#### D2 - GitHub Copilot VS Code surface

- D: Added `_merge_vscode_mcp` → `.vscode/mcp.json` (the **`servers`** key, not `mcpServers`) and a seeded thin router `.github/copilot-instructions.md` (new SeedFile + seed twin + live copy). Documented the coding-agent path (repo/org settings, manual). Reconciled docs/todo item 5 to reality.
- R: Completes the Copilot story started in 2.6.0 (CLI shipped 2026-06-11); VS Code is the surface most users hit and uses a distinct config key.
- F: `core.py`, `memory_seed/seed/.github/copilot-instructions.md`, `.github/copilot-instructions.md`, `docs/todo/baseline-seed-promotions.md`, tests.

#### D3 - Hygiene: per-prompt hook reconcile + test trims + index staleness

- D: Reconciled `memory-retrieval-check.py` wording (topical→`memory_search`, latest→newest-file/SessionStart). Trimmed the over-specified `test_agent_rules_document_mcp_history_conflict_resolution` to load-bearing tokens (+added a guard for the new "Recency vs. Topical Retrieval" section). Repointed `test_current_session_files_*` to validate all frontmatter-bearing session files instead of two frozen dates. Fixed `index.md` Active State (was 2.4.0 — the bump trap had hit 2.5.0 too) and added Topology entries for the SessionStart hook + Copilot/VS Code config targets.
- R: Closes the wording inconsistency the user flagged, reduces brittle doc-substring tests, and makes the session-file test catch real new-entry mistakes.

#### D4 - Version bump 2.5→2.6 (release content)

- D: Bumped `memory-system-version` across 30 control-plane `.md` files (live+seed, incl. repo-root routers + new copilot-instructions.md), `core.py` VERSION, `pyproject.toml` 2.5.0→2.6.0, index Active State; updated CHANGELOG + README (agent table, hook-events table gained a Session-start-orientation column + Copilot row); archived the true 2.5 reusable procedure files to `.memory-seed/archive/2.5/` (captured from git HEAD since the bump was in place).
- R: Incremental release cadence chosen by user (2.6.0 now). The version-bump trap (repo-root + index Active State) is covered, guarded by `test_repo_root_control_plane_files_match_version`.
- T: Full suite 116 OK (updated the one canonical `get_version()` pin to 2.6); `doctor` healthy; `version` reports 2.6.

### Follow-up

- **NOT committed / NOT published.** Remaining release steps need user go: split into logical commits (per decision), branch off `main`, tag `v2.6.0`, `gh release create` → `publish.yml` (pauses at the `pypi` manual-approval gate).
- **Demo refresh deferred:** `demo/.memory-seed` is still at 2.4 and untracked (not in the published package or the commit). Refreshing it to 2.6 is optional housekeeping; flagged rather than done to respect the prep-only stop.
- Release 2.7.0 (seed promotions: document_ingestion + office_document_editing skills + Working Principles; ESR generalization) remains specified but not started.


## 2026-06-13 15:08 - Evaluate + refine multi-user session proposal; shelve as deferred 3.0

```yaml
entry_id: ms-41f4f32a
user_initials: JN
agent_type: claude-opus-4-8
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Evaluated the multi-user per-day session proposal (`sessions/YYYY-MM-DD/<user>.memory.md`) against the codebase and its backing research report. Refined it into an execution-ready spec and shelved it as a deferred 3.0 initiative. Documentation-only turn; no code touched.

### Decision

- D: Authored `docs/todo/multi-user-session-memory-proposal.md` (refined, gap-fixed), cleaned the research report's citation artifacts, and recorded a Deferred-3.0 entry in `NEXT_STEPS.md`.
- R: Proposal is well-engineered but a core-data-model change + product pivot (solo-first -> team-capable). User confirmed team-capable is worth exploring but chose to refine-then-hold until 2.6.0 ships and 2.7.0 lands. Refining now (while the analysis is fresh) makes it execution-ready later without committing to the high-blast-radius build.
- A: Full implementation now (rejected: 2.6.0 uncommitted, high risk); decline outright (rejected: team-capable is a wanted direction).
- F: `docs/todo/multi-user-session-memory-proposal.md` (new), `docs/todo/multi-user-deep-research-report.md` (citation cleanup), `NEXT_STEPS.md` (deferral entry).

### Implementation

- Five gap-fixes folded into the spec: (1) complete reader inventory incl. the just-shipped `session-start-context.py` (its `sessions/*.md` glob silently misses per-user subdirs); (2) defined multi-user SessionStart orientation semantics (active user's latest entry in full + co-contributor files as headings-only); (3) standardized `schema_version: 2` + `session_date`; (4) named the deferred 32-bit `ms-` entry-ID collision concern; (5) cleaned `citeturn...` artifacts.
- Answered two user design questions in-spec: upgrade path (`update` is layout-agnostic and never migrates data; dual-read + opt-in writes + explicit `migrate sessions-layout`; never rewrite legacy files) and caching (out of scope initially; if added, local/gitignored/rebuildable per-file cache — explicitly NOT SQLite, because this repo is OneDrive-synced and the research's own Trilium citation warns Drive-synced SQLite corrupts).

### Follow-up

- Do not implement before 2.6.0 ships + 2.7.0 lands. The 2.6.0 release content remains uncommitted (prep-only stop from the prior turn).


## 2026-06-13 15:58 - Agent-selective init: per-agent install, project.yaml, agents add/remove

```yaml
entry_id: ms-1bcfcc91
user_initials: JN
agent_type: claude-opus-4-8
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Implemented agent-selective `memory-seed init` so a project installs only the agents it uses (e.g. Claude+Codex, no `GEMINI.md`). 126 tests pass; CLI verified end-to-end. Uncommitted (stacked on the uncommitted 2.6.0 tree).

### Decisions

#### D1 - Central AGENTS registry + per-agent tagging

- D: Added `KNOWN_AGENTS` + `_AGENT_MERGES`/`_AGENT_UNINSTALLS` registries in `core.py`; tagged routing `SeedFile`s with `agent=`; made both init/update orchestration lists registry-driven (`_agent_merges(selected)`).
- R: One source of truth for slug -> {routing file, merge ops, uninstall ops}; removes the duplicated 21-row merge tuples; makes filtering trivial.
- F: `memory_seed/core.py`.

#### D2 - Persist selection in project.yaml; absent = ALL (backward-compat)

- D: `read_project_agents`/`selected_agents`/`write_project_agents` with a dependency-free YAML parser. `init` writes `project.yaml` **only on a proper subset** (all-agents default writes nothing). Absent/malformed/no-`agents:` -> None -> ALL. Present-but-empty `agents:` -> empty set (a real zero-agents state, distinct from unconfigured).
- R: Keeps every existing project + test byte-identical (advisor's key point). The zero-vs-None distinction fixes an edge bug where removing the last agent re-flagged routing files as missing.
- A: agents.json (rejected: project.yaml aligns with the deferred multi-user proposal); infer-from-files (rejected: can't tell intentionally-absent from missing).
- F: `core.py`; `init_project`/`update_project`/`doctor` filter by `selected_agents`.

#### D3 - `agents add/remove` with strip-in-place uninstall

- D: Added `add_agent`/`remove_agent` + `memory-seed agents list|add|remove`. Removal uses generic strippers (`_strip_grouped_hooks`/`_strip_cursor_hooks`/`_strip_mcp_entry`/`_strip_copilot_startup`/`_strip_codex_mcp`) that remove only our entries, delete a config file only when nothing of value remains, never delete shared dirs (`.github`/`.vscode`), back up everything first, and delete routing files by name. Warns when the last agent is removed.
- R: User chose dedicated add/remove with cleanup. Strip-in-place is the safe default — verified that removing claude preserves a foreign `permissions` key in `.claude/settings.json` while deleting our ours-only `.mcp.json`.
- F: `core.py`, `memory_seed/cli.py`.

#### D4 - Cursor needs no routing file (researched)

- D: Cursor (like Codex) reads `AGENTS.md` natively (2026); routing files exist only for agents preferring their own filename (Claude/Gemini/Copilot). Cursor's per-agent artifacts are just `.cursor/hooks.json` + `.cursor/mcp.json`.
- R: Confirmed via Cursor 2026 docs; `.cursor/rules/*.mdc` is only for glob-scoped overrides, which memory-seed doesn't need; `.cursorrules` is deprecated.

### Validation

- `python -m unittest discover -s tests` -> 126 OK (+10 `AgentSelectionTests`). CLI: `init --agents claude,codex` (no gemini/cursor/copilot files), `agents list/add/remove`, invalid slug exits 1, backups created. Repo's own `doctor` healthy (no project.yaml -> all agents, unchanged).

### Follow-up

- Uncommitted. This is the **third** feature stacked on the uncommitted 2.6.0 tree (advisor flagged the commit-split is getting materially harder) — committing 2.6.0 soon is advisable. Suggested release slot: 2.7.0/2.8.0 (no control-plane version bump required; behavior is CLI/code only). README + CHANGELOG (Unreleased) updated.


## 2026-06-13 16:04 - ESR close: agent-selective install validated; persona-evolution proposals pending

```yaml
entry_id: ms-52b5690c
user_initials: JN
agent_type: claude-opus-4-8
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- End-of-session routine. Closes out the agent-selective `init` feature (entry ms-1bcfcc91): added the two advisor-requested tests (Codex-TOML foreign-preservation, remove-from-unconfigured); suite now **128 OK** (the 126 in ms-1bcfcc91 predated these). README + CHANGELOG (Unreleased) updated.

### Validation

- `python -m unittest discover -s tests` -> 128 OK. CLI round-trip verified. Repo `doctor` healthy.

### ESR review outcome

- index.md: added a Design Decision for agent-selective install + project.yaml to Topology.
- policy.md: no behavioral-constraint change — left unchanged.
- Persona evolution: 3 proposals drafted for the **developer (Ada)** persona (commit-discipline / don't stack on uncommitted trees; foreign-preservation tests for destructive strippers; verify agent/tool conventions via research before wiring) — **presented to user, awaiting approval; not yet applied.** Other personas had no activity this session.
- Skill evolution: no new skill gap (recurring items are discipline/test patterns better captured as persona adaptations than a new runbook).
- Unregistered personas: none (all 6 .agents personas registered).

### Follow-up

- Persona adaptations pending user approval (will append to developer.md `## Project Adaptations` on approval).
- Still uncommitted: 2.6.0 + multi-user proposal docs + this agent-selective feature. Recommend committing before further work.


## 2026-06-13 16:16 - Persona evolution applied: developer.md (3 adaptations)

```yaml
entry_id: ms-74475f71
user_initials: JN
agent_type: claude-opus-4-8
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Follow-up to the ESR review (ms-52b5690c). User approved all three proposed developer-persona adaptations; applied to `.agents/developer.md`.

### Decision

- D: Added three rules to `developer.md`: commit-discipline (III. Git Workflow), foreign-preservation test for destructive/strip code (IV. Review Checklist), and verify-external-agent-conventions-before-wiring (VI. Self-Correction). Logged as a `### 2026-06-13` entry in the persona's `## Project Adaptations`.
- R: Each is evidence-based from this session (stacked uncommitted features; the untested Codex line-based stripper; corrected Gemini/Cursor/Copilot/Codex assumptions).
- F: `.agents/developer.md`.

Signed: user approved 2026-06-13 16:16


## 2026-06-13 16:49 - CV-update document for Memory Seed repository evidence

```yaml
entry_id: ms-bde55cc2
user_initials: JN
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Evaluated the repository's package metadata, README/CHANGELOG, CLI/MCP/retrieval code, tests, roadmap docs, and recent session state to create a CV-update source document.

### Decision

- D: Added `docs/cv-memory-seed.md` as a reusable CV/portfolio note covering project facts, skills, capabilities, achievement bullets, interview talking points, evidence paths, and caveats about uncommitted release claims.
- R: The user asked for a document to update their CV; grounding it in repository evidence makes the claims easier to reuse without overstating unpublished or uncommitted work.
- F: `docs/cv-memory-seed.md`.
- T: Read-through completed; placeholder scan found no `TBD`, `TODO`, or bracket placeholders.
