---
tags:
  - session-log
  - memory-seed
session_date: 2026-06-11
default_user_initials: JN
---

# Session Log - 2026-06-11

## 2026-06-11 16:58 - Harden current-state retrieval: SessionStart orientation hook + multi-agent/Copilot wiring

```yaml
entry_id: ms-5e366ef4
user_initials: JN
agent_type: claude-opus-4-8
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Root cause confirmed empirically at session start: `memory_search` for "current state" is a recency question answered by a similarity tool. A live query ("current work in progress recent session state") returned 2 of 8 entries from the newest session file and **missed the most recent entry entirely** (`2026-06-04 12:55`, the .agents/2.5.0 work) — older topically-similar entries from prior weeks outranked it. Correct orientation came only from reading the newest dated file directly.
- Built a SessionStart hook that delivers current state as self-sufficient injected content, wired it across Claude/Codex/Gemini/Cursor, brought **GitHub Copilot** into the supported-agent set (MCP + sessionStart prompt hook), and hardened AGENTS.md/agent-rules.md. Plan approved in plan mode (`dreamy-yawning-quill.md`). Release deferred.

### Decisions

#### D1 - SessionStart hook injects content, not a "go read it" nudge

- D: New `.memory-seed/hooks/session-start-context.py` (+ byte-identical seed twin) finds the newest `YYYY-MM-DD.md` session file deterministically and injects its path, **all** entry headings, and the **full body of the most recent entry** (capped 1500 chars), plus the recency-vs-search rule. Per-agent output field: `hookSpecificOutput.additionalContext` (Claude/Codex/Gemini), `additional_context` (Cursor).
- R: A hook that merely says "read the newest file" is the same ignorable nudge that caused the bug. Injecting the actual latest entry makes the orientation self-sufficient — the agent knows current state with zero follow-up. (advisor's load-bearing point.)
- A: Reuse `memory-retrieval-check.py` at SessionStart — rejected; its "call memory_search first" text is exactly the bias being removed.
- F: `.memory-seed/hooks/session-start-context.py`, `memory_seed/seed/.memory-seed/hooks/session-start-context.py`.
- T: Regression check passes — run against this repo, the injected output contains the `2026-06-04 12:55` entry that semantic search lost, plus all 8 headings.

#### D2 - Multi-agent wiring reuses the existing merge framework; Copilot added as 5th agent

- D: Wired the hook through `core.py` via the existing `_merge_grouped_hook` (Claude/Codex/Gemini `SessionStart`) and `_merge_cursor_event_hook` (Cursor `sessionStart`), registered in both init/update orchestration lists. Added Copilot: `_merge_copilot_mcp` writes repo-local `.github/mcp.json` (`type:local`, `tools:["*"]`), `_merge_copilot_startup_hook` writes `.github/hooks/memory-seed.json` with a **static `prompt`-type** sessionStart hook.
- R: The merge framework already proved the per-agent pattern; new wiring is additive and idempotent. Copilot's MCP is repo-local (`.github/mcp.json`), fitting the project-scoped pattern.
- A: A `--copilot` branch on the python script — rejected: Copilot **command** hooks cannot inject context at sessionStart (stdout consumed, not processed); only a `prompt` hook injects. So Copilot gets a static directive (must glob the dir itself), strictly weaker than the other four.
- F: `memory_seed/core.py` (SeedFile entry, startup command constants, Copilot constants, 6 merge fns, both orchestration lists).
- T: Dogfooded `memory-seed update` on this repo → `.claude/settings.json` gains `SessionStart` with Stop/UserPromptSubmit unchanged; `.github/mcp.json` + `.github/hooks/memory-seed.json` created; `doctor` healthy.

#### D3 - Copilot session-log + retrieval reminders are NOT wired (platform limitation, documented)

- D: Did not wire Copilot's per-turn reminders. Copilot gets MCP + sessionStart orientation prompt only.
- R: Authoritative GitHub docs confirm only `sessionStart`, `notification`, `postToolUse`, `postToolUseFailure` support `additionalContext` output. `userPromptSubmitted`, `agentStop`, and `sessionEnd` do **not** ("Output processed: No"). So the retrieval reminder (UserPromptSubmit-equivalent) and session-log reminder (Stop-equivalent) cannot inject context in Copilot — a platform limitation, not a memory-seed gap. Copilot relies on AGENTS.md guidance + the sessionStart prompt hook for those.

#### D4 - Doc hardening + Recency-vs-Topical distinction reconciles the coexisting hooks

- D: Added a deterministic "read newest session file directly" step to AGENTS.md "Operating Mode" and agent-rules.md "Operating Mode Start", and a new "Recency vs. Topical Retrieval" subsection to agent-rules.md "History Retrieval". Per the user's boundary, the per-prompt `memory-retrieval-check.py` was left untouched.
- R: The new SessionStart hook ("read the file, not search") and the untouched per-prompt hook ("call memory_search before substantive work") look contradictory; the Recency-vs-Topical framing reconciles them (search = topical/why; direct read = recency/latest). Docs also cover mid-session "what's the latest" requests and hook-less agents.
- F: `AGENTS.md`, `.memory-seed/agent-rules.md` (+ seed twin, mirrored & equality-verified).
- T: `python -m unittest discover -s tests` → 112 OK (was 100; +12 covering SessionStart merges, Copilot mcp/hook, hook-script behavior, seed/live parity). Locked agent-rules substrings preserved.

### Follow-up

- **Release deferred** (per decision): control-plane 2.4→2.6, package 2.5.0→2.6.0, CHANGELOG (SessionStart hook + Copilot support), README supported-agent list, demo/.memory-seed refresh, archive snapshot, GitHub Release/PyPI publish. At release, also fix `index.md` Active State (still says 2.4.0/2.4 — pre-existing staleness found at session start) and add Topology entries for the new hook + Copilot config targets (`.github/mcp.json`, `.github/hooks/`).
- **Gemini event-name mismatch (out of scope, flagged for follow-up):** Gemini CLI's real hook events are `BeforeTool/AfterTool/BeforeAgent/AfterAgent/SessionStart/SessionEnd/...` — there is **no `UserPromptSubmit` and no `Stop`**. The repo's existing Gemini session-log/retrieval wiring targets `Stop`/`UserPromptSubmit`, which likely never fire. The new Gemini `SessionStart` wiring is correct regardless. Worth a separate fix.
- **Codex hooks enable state:** OpenAI's hooks doc says hooks are enabled by default now (`codex_hooks` is a deprecated alias for `[features] hooks`); older Codex may need the flag. Documented; no action needed for current Codex.
- **Copilot sessionStart command-vs-prompt ambiguity:** two reads of the same GitHub reference disagreed on whether a *command* hook can inject `additionalContext` at sessionStart. Chose the unambiguously-documented `prompt` hook. If command-hook injection is confirmed empirically against the real Copilot CLI, Copilot could run `session-start-context.py` and get the same dynamic content as the other four — a future enhancement.
- **Persona loop:** `agent_name: developer` reflects the engineering role that governed this task, but the persona file was not formally loaded and the End-Of-Turn persona/skill-evolution checks (agent-rules steps 7–8) were **deferred to `/esr`**, not run this turn.
- **Post-advisor fixes applied this turn:** tightened the hook's entry-heading regex to the strict timestamped pattern (a stray `## ` line in an entry body no longer mis-splits the latest-entry extraction; covered by a new test); switched Copilot MCP `type` `local`→`stdio` (GitHub-preferred for cross-client compat); verified the hook under the exact wired `python3` interpreter. Suite now 113 OK.
