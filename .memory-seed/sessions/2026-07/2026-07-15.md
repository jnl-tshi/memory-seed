---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-15
---

## 2026-07-15 00:25 - Fix Memory-Entry trailer-block parsing: body-scan all lines + contiguous stamping (daisy-chain rendered parallel)

```yaml
entry_id: mse_skpkqx0hhkw6ve4f
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/fix/trailer-block-parsing
topics:
  - memory-trace
  - git-workflow
evolves:
  - mse_p4tj8rw2mzvq6k1e
```

### Decision

- D: Fixed a Memory-Entry **trailer-block parsing** bug that mis-rendered daisy-chained branches as
  parallel in the Trail. Two prongs: (1) **backend** (`memory-trace` `_first_parent_trailer_commits`) now
  scans the full commit body (`git log -z --format=...%B`) for EVERY `Memory-Entry:` line, instead of
  git's `%(trailers:key=Memory-Entry,...)` which parses only the final contiguous trailer block; (2)
  **stamping** (the `prepare-commit-msg` hook live+seed, and core `_stamp_memory_entry_trailers`) now
  appends new trailers CONTIGUOUSLY to an existing trailer block (blank-line-separate only after prose) and
  dedups against trailers already present.
- R: User caught it on the Trail: `claude/chore/link-pass-2026-07-13` and
  `claude/chore/evolves-upgrade-2026-07-13` (a git daisy-chain — evolves-upgrade forks from exactly where
  link-pass merges, `8194a82`) rendered as PARALLEL lanes. Root cause: each merge commit DOES carry the
  branch's own entry trailer, but a blank line (and a `Co-Authored-By`) splits it from the fused-entries
  block, so `%(trailers:...)` silently dropped the branch entry → the branch had no merge event →
  `estimated`/positional fallback → parallel. The body-scan is layout-agnostic and REPAIRS already-committed
  merges with no history rewrite; the stamping fix prevents recurrence.
- A: The backend body-scan is the high-value half — it retroactively repaired every affected merge in the
  live history. Verified in-process (fresh cache rebuild; preview stopped first to release the Windows
  cache-file lock): both branches now `estimated:false` with real merge/fork, the daisy-chain junction is
  real (`evolves-upgrade.fork == link-pass.merge == 8194a82`), and the live DOM shows both entries' dots at
  the same lane column (`cx=55`) — daisy-chained, no longer parallel. Backend-only + stamping; no frontend
  change, so no `app.js` cache-bust. This entry `evolves` the merge-accuracy feature that introduced
  `_first_parent_trailer_commits`.
- F: memory-trace/memory_trace/service.py, memory_seed/core.py,
  .memory-seed/hooks/prepare-commit-msg.py (+ seed twin `memory_seed/seed/...`),
  memory-trace/tests/test_trail_merge_accuracy.py, tests/test_git_hooks.py.
- T: memory-trace merge-accuracy + tests/test_git_hooks **21 passed** (incl. new split-trailer extraction +
  hook contiguity, each with a git-strict-parser guardrail); test_memory_seed merge/fuse/hook **242 passed**;
  live DOM `cx=55==55` (shared lane).

## 2026-07-15 00:38 - Add manual Refresh button to Memory Trace topbar (re-read memory, keep filters, auto-extend date to latest)

```yaml
entry_id: mse_1gjtkr5nny843rx2
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/trace-refresh-button
topics:
  - memory-trace
  - ui-design
```

### Decision

- D: Added a manual **Refresh** button (⟳) to the Memory Trace topbar (between the Trail/Graph tabs and the
  theme toggle). `refreshData()` re-fetches `/api/runtime` + `/api/facets` and reloads the active view
  WITHOUT a full page reload, spinning the control (`.is-refreshing`) until done. The server rebuilds its
  projection on demand when the corpus changed, so this surfaces entries added since the page opened.
- R: User request after a post-midnight entry (2026-07-15) sat past a stale "To 14/07" date filter with no
  way to pull fresh data short of restarting the server. Filters PERSIST across a refresh (view, query,
  selection, a manually-narrowed date window) — with one deliberate nudge: if the user was viewing up to the
  previous latest (dateTo at/beyond the old max bound), the window auto-extends to the new latest, so an
  after-midnight entry appears instead of hiding past the filter; a manually-narrowed window is kept as-is.
- A: Reused the existing load path (`api()` + `loadView()` + `render()`) rather than a bespoke fetch;
  `loadSeq` guards overlapping loads and a failed refresh clears the spinner in `finally` (never wedges).
  No new server surface — the server already rebuilds on request. Reduced-motion disables the spin (the
  existing global animation rule).
- F: memory-trace/memory_trace/static/app.js (refreshData + topbar button + click dispatch + `refreshing`
  state flag), memory-trace/memory_trace/static/styles.css (spin keyframes),
  memory-trace/memory_trace/static/index.html (cache-bust -> trace-refresh-button-20260715).
- T: Live DOM (primary preview): the button renders (⟳, correct title); a click fires runtime+facets+graph
  re-fetches with no page reload and clears the spinner; no console errors. memory-trace test_service +
  test_trail_golden 44 passed (the change is client-side JS, untested by Python; the data layer is intact).

## 2026-07-15 00:44 - Trail To-date defaults to today (not newest entry): shared defaultDateTo helper across seed/chip/clear/refresh

```yaml
entry_id: mse_860h4ksbvn69nb1y
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/trace-to-date-tracks-today
topics:
  - memory-trace
  - ui-design
evolves:
  - mse_1gjtkr5nny843rx2
```

### Decision

- D: The Trail's "To" date filter now defaults to **today** (local) at load, not just the newest entry's
  date. Factored a `defaultDateTo(bounds)` helper (`= max(today, newest-entry-date)`) and routed
  `seedDates`, the filter-chip test, and the clear-reset through it so "today = default" is coherent (no
  phantom "to …" chip; clearing the date filter resets to today). `refreshData`'s auto-extend now targets
  the same helper.
- R: An entry written today — especially after midnight, before any refresh — fell outside a window whose
  "To" tracked the newest ENTRY date at load. Defaulting to today keeps new work in view from the first
  paint; `max(today, newestEntry)` still guards a future-dated entry. This closes the loop on the reported
  missing `evolves` edge (`mse_skpkqx0hhkw6ve4f -> mse_p4tj8rw2mzvq6k1e`): never a data bug — the 07-15
  source was just sitting past a "To 14/07" default.
- A: Chose `max(today, bounds[1])` over plain `bounds[1]` (misses today's new entries) and over plain today
  (would hide a future-dated entry). One helper used at all four sites so chip / clear / seed / refresh
  never disagree on "default". Evolves the refresh-button entry — generalizes its inline auto-extend into
  the shared helper and applies it at load, not only on a manual refresh.
- F: memory-trace/memory_trace/static/app.js (`defaultDateTo` + `todayLocalISO`; seedDates, chip, clear,
  refreshData), memory-trace/memory_trace/static/index.html (cache-bust -> trace-to-date-today-20260715).
- T: Live DOM (primary preview): `#date-to` = 2026-07-15 (== browser local today), `#date-from` unchanged
  (min bound), zero filter chips (no phantom); the `evolves` edge is now in the DEFAULT graph
  (`evolves_edge_visible_by_default: true`); no console errors. test_service + test_trail_golden 44 passed.

## 2026-07-15 00:55 - Trail: selecting an entry reveals its links (grow window to linked neighbours), fixing edges to off-window targets

```yaml
entry_id: mse_bq1x6drw6j7wy39n
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/fix/trail-reveal-linked-neighbors
topics:
  - memory-trace
  - ui-design
```

### Decision

- D: Selecting a Trail entry now **reveals its links**. Added `ensureLinkedNeighborsVisible(chunkId)`: on
  selection it grows the client-side `trailWindow` (page-rounded to `TRAIL_WINDOW_STEP`, reusing the
  `ensureTrailVisible` pattern) to include the selected entry's lifecycle/related neighbours, so an edge to
  an older, not-yet-shown entry actually draws. Branch-lane edges are excluded (continuity, not a "link").
- R: The Trail fetches the full corpus but windows to 60 rows client-side; an edge only draws between two
  SHOWN rows. So the user's `evolves` edge (`mse_skpkqx0hhkw6ve4f -> mse_p4tj8rw2mzvq6k1e`, 3 days older)
  rendered as NO line - the entry looked unlinked despite the edge being present in the data and the reader.
  The corpus is already loaded client-side, so revealing the neighbour is a cheap window bump, not a fetch.
- A: Chose "reveal on select" over an off-window stub/indicator - showing the actual edge to the actual row
  matches "I selected it to see its links" better than a placeholder. Excluded branch edges so selecting an
  entry doesn't drag a whole long-lived branch into view. Trail-view only. (An off-window indicator remains
  a possible follow-up for links beyond the fetched 1000-entry cap - not hit at this corpus size.)
- F: memory-trace/memory_trace/static/app.js (`ensureLinkedNeighborsVisible` + `selectChunk` wiring),
  memory-trace/memory_trace/static/index.html (cache-bust -> trail-reveal-links-20260715).
- T: Live DOM (primary preview): selecting `mse_skpkqx0hhkw6ve4f` grew the window 60 -> 240 (= ceil to cover
  the target row), the 07-12 target now renders (`y=2190`), and exactly one long dashed evolves-colored
  (`#4f98d9`) path spans source (`y=90`) -> target; no console errors. test_service + test_trail_golden 44
  passed (client-side JS, untested by Python; data layer intact).

## 2026-07-15 01:14 - Ship derived-projection Phase 1 warm start: git-watermark + dirty-signature freshness (no whole-corpus scan), schema-versioned atomic rebuild

```yaml
entry_id: mse_c70ze3saqsw6mcme
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/derived-projection-phase1
topics:
  - memory-trace
  - git-workflow
evolves:
  - mse_99219wsng7xj4h03
```

### Decision

- D: Shipped **derived-projection Phase 1 (warm start)** — the foundation item. Extended the Memory Trace
  `TraceCache` into a formalized read-model: a `PROJECTION_SCHEMA_VERSION` (bump -> full rebuild), a stored
  **build watermark** (last-built HEAD) + a **signature of the git-dirty session files**, and a reworked
  `ensure_current` that proves freshness in **O(changes)** — HEAD unmoved AND the dirty signature unchanged
  -> no rebuild, **no whole-corpus scan** (contract G5). Every git ambiguity (moved/gone watermark, `git
  status` error, schema mismatch) **fails toward a full rebuild**; no git -> the existing mtime scan (G7).
  Atomic build/swap (G4) was already present, now schema-gated. New git-repo suite
  `test_projection_warm_start.py` (7): fast-path no-rebuild + no-corpus-scan, dirty/commit/schema-bump
  rebuilds, no-git degradation, byte-identical rebuild (G2).
- R: Killed the load-time trap: `ensure_current` (called on every API request) previously did an O(corpus)
  mtime scan, and any change triggered a full rebuild. Measured on the real repo: full rebuild **~6.2 s ->
  warm `ensure_current` ~78 ms** with no rebuild when sessions are clean (~80x). Using a dirty-file
  *signature* (not a clean-only fast path) makes the common actively-editing case O(dirty), not a rebuild
  per call. Invariant #6-clean: the projection stays derived / disposable / rebuildable, never authoritative.
- A: Tiered per advisor. Shipped the warm-start + atomic-swap + schema-version tier (satisfies the goal:
  explicit ingest, byte-identical rebuild, git-watermark warm start, atomic swap). **Deferred incremental
  ingest** (re-project only the delta files' chunks — O(1) ingest on change; whole-history git meta
  recomputed only when HEAD moved) as a documented fast-follow, gated on an incremental==full-rebuild
  equivalence test — a full rebuild on change is correct now, whereas an unverified incremental path would
  be a latent G2 violation. Detection is already O(changes); the deferred work makes the *ingest* O(changes).
- F: memory-trace/memory_trace/service.py (`PROJECTION_SCHEMA_VERSION`, `_git_head`, `_porcelain_paths`,
  `_working_tree_signature`; `TraceCache._sessions_pathspec`/`_is_current`, reworked `ensure_current` +
  rebuild provenance), memory-trace/tests/test_projection_warm_start.py (new),
  docs/2_Todo/derived-projection-implementation-plan.md (Phase 1 status), CHANGELOG.md.
- T: memory-trace **106 passed** (99 + 7 new warm-start). Real-repo timing: 6224 ms full rebuild vs 78/70 ms
  warm `ensure_current`, `rebuilt=False` on clean sessions.

## 2026-07-15 06:53 - Fix Trail switching lag: memoize the projection freshness check (git per-read cost) over a short window

```yaml
entry_id: mse_znsxhpkrw39j0mwn
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/fix/projection-freshness-memoize
topics:
  - memory-trace
  - bugfix
evolves:
  - mse_c70ze3saqsw6mcme
```

### Decision

- D: Fixed a Trail switching-lag **regression** from the Phase 1 warm start. The git freshness check
  (~78 ms on Windows/OneDrive — `git rev-parse` alone 87 ms) ran on EVERY read, and one entry-switch fires
  several reads, so switching went from instant to a few hundred ms. **Memoize the freshness verdict** for a
  short window (`_FRESHNESS_TTL_SECONDS = 2.0`): within the window `ensure_current` trusts the last verdict
  without re-spawning git, so a burst of reads does ONE check, not one per read. `rebuild()` opens the
  window too, and a positive `_is_current` refreshes it.
- R: Measured: the git check is ~78 ms vs the old mtime scan's ~9 ms — Phase 1 optimized for large-corpus
  scale (O(changes) via git) at the cost of the small-corpus interactive path (git's fixed subprocess cost
  dominates at ~380 entries). Memoization keeps the git approach (scale + G5) while amortizing its fixed
  cost: a burst of 6 reads dropped **~468 ms -> ~84 ms**; sequential switching within the window is
  effectively free (~0.7 ms/read). Correctness intact — a real change is still seen within the TTL, and the
  refresh button re-fetches once the window turns over.
- A: TTL memoization over the alternatives: reading `.git/HEAD` directly to skip the rev-parse subprocess is
  fiddly (packed-refs, worktree `.git` files) and risky; reverting to the mtime scan abandons the
  large-corpus benefit. 2 s balances snappiness against staleness for a read-only browsing UI. Tests that
  pin change-detection set the instance TTL to 0 so the check runs immediately; a new test pins that a burst
  inside the window makes zero git calls. Original mis-step: I benchmarked warm-start against a full REBUILD
  (6.2 s) and missed that the relevant baseline was the old ~9 ms mtime scan.
- F: memory-trace/memory_trace/service.py (`_FRESHNESS_TTL_SECONDS`, `_fresh_until`, `_mark_fresh`;
  `ensure_current` fast path; `rebuild` marks fresh), memory-trace/tests/test_projection_warm_start.py
  (memoization test + TTL=0 in the check tests), memory-trace/tests/test_service.py (TTL=0 in the
  metadata-change test), CHANGELOG.md.
- T: memory-trace **107 passed**. In-process burst: 6 `ensure_current` **468 ms -> 84 ms**; 1 cold check
  76 ms + 5 warm 0.7 ms.

## 2026-07-15 07:26 - Memoize deserialized chunks in TraceCache: chunk() 132ms -> 12.5ms (dominant read cost was JSON deserialization)

```yaml
entry_id: mse_vqrefk31fpj02jwb
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/perf/memoize-chunk-deserialization
topics:
  - memory-trace
  - bugfix
evolves:
  - mse_znsxhpkrw39j0mwn
```

### Decision

- D: Cut the Trail read-path latency further by **memoizing the deserialized chunk lists** in `TraceCache`.
  Profiling showed `/api/chunks` (~132 ms) was dominated by SQLite chunk *deserialization* —
  `cache.chunks(all)` ~61 ms, `cache.chunks(entry)` ~22 ms called **3x** per request — while the graph build
  (1.3 ms) and sidecar disk reads (6 ms) were negligible. `chunks(granularity)` now caches the parsed
  `list[MemoryChunk]` per granularity; `_rebuild_locked` clears the memo on a successful swap. Result:
  **chunk() 132 -> 12.5 ms, graph() 46 -> 17 ms, facets() -> 0.8 ms.**
- R: The chunk rows change only when the projection rebuilds, and every read endpoint deserializes them
  1-3x per request. Memoizing keyed to the rebuild (cleared in `_rebuild_locked`) is **safe**: chunks derive
  ONLY from session documents, which participate in the rebuild trigger on BOTH the git and no-git paths
  (unlike sidecars). So sidecars stay read-live per request (freshness preserved), the graph is still built
  live from the memoized chunks + live sidecars, and `MemoryChunk` is frozen with callers filtering/copying,
  so sharing the memoized list is safe.
- A: Two Explore agents mapped the cost + the invalidation input-set. Verdict (confirmed by reading
  `iter_session_documents`, core.py:362): the **no-git** freshness scan skips `links/` and `diagrams/`
  (their dir names match neither the day nor month regex), so keying a *sidecar-derived* memo on
  `rebuilt_at` would regress no-git sidecar freshness — which is why I memoized ONLY the sidecar-independent
  chunk deserialization (the dominant cost anyway) and left sidecars live. Did NOT memoize the
  augmented-entries/graph bundle (that would need a sidecar-signature memo key for ~12 ms of gain — not
  worth the complexity/risk). Also noted but skipped: `graph()` builds the related graph twice per request
  (748 + inside `_graph_edges`), only ~1.3 ms each, so not worth touching now.
- F: memory-trace/memory_trace/service.py (`_chunks_memo` field, `chunks()` memo read/store, cleared in
  `_rebuild_locked` after the atomic swap), memory-trace/tests/test_projection_warm_start.py (memo + invalidation test).
- T: memory-trace **108 passed**; real-repo: chunk() **132 -> 12.5 ms**, graph() 46 -> 17 ms, facets() ~0.8 ms.

## 2026-07-15 07:40 - Sidecars first-class in freshness signal + memoize derived bundle: chunk() 12.5ms -> 3.9ms (git-captured sidecar changes)

```yaml
entry_id: mse_4av5twf24m6xy7br
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/perf/sidecar-freshness-derived-memo
topics:
  - memory-trace
  - git-workflow
evolves:
  - mse_vqrefk31fpj02jwb
```

### Decision

- D: Made link/diagram sidecars **first-class inputs to the projection freshness signal**, then memoized the
  sidecar-derived read structures. The git freshness check already caught sidecar changes (they live under
  the `.memory-seed/sessions` pathspec), but the no-git mtime scan skipped them (`iter_session_documents`
  only recurses into `YYYY-MM`/`YYYY-MM-DD` dirs). Added `_tracked_document_paths` (session docs + link +
  diagram sidecars) used by both `_rebuild_locked` (files table) and `_metadata_matches`, so a new cache
  `generation` counter is a COMPLETE token over all read inputs on both paths. Then added a per-generation
  `TraceService._derived()` memo (augmented all-entry chunks + related graph + diagram map); `chunk()` and
  `graph()` pull from it instead of re-reading sidecars + re-augmenting per request.
- R: User's insight: if a sidecar change is deterministically flagged (git already does it; extend to
  no-git), the sidecar-derived structures can be cached like the chunks — "use the pre-existing cache or
  rebuild what changed". Result: **chunk() 12.5 -> 3.9 ms, graph() 17 -> 9.6 ms** (chunk now ~34x faster
  than the original 132 ms). Correctness preserved: a sidecar edit bumps the generation on BOTH paths, so
  the bundle refreshes exactly when a sidecar changes — the "sidecars always fresh" contract now holds via
  the generation, not per-request disk reads. `graph()` filters the pre-augmented all-entry set (link-sidecar
  augmentation is per-entry, so filter-then-augment == augment-then-filter for the edge fields, and the
  agent/user/date/topic filters never touch those fields).
- A: Fold sidecars into the freshness signal (making the generation complete) over keeping them read-live —
  the live reads were a workaround for the old mtime-only invalidation, not a fundamental need. This also
  lays groundwork for the deferred incremental ingest (`git diff --name-only` already lists the exact
  changed files, sidecars included). Left `graph()`'s redundant second graph-build inside `_graph_edges`
  (~1.3 ms) alone - separate, marginal.
- F: memory-trace/memory_trace/service.py (`_tracked_document_paths` + sidecar imports; TraceCache
  `generation()`/`_generation`, freshness + rebuild use the tracked set; TraceService `_derived()` memo;
  `chunk()` + `graph()` rewired), memory-trace/tests/test_projection_warm_start.py (no-git sidecar-change
  rebuild + derived-bundle memo tests).
- T: memory-trace **110 passed** (+2 new). Real-repo: chunk() **12.5 -> 3.9 ms**, graph() 17 -> 9.6 ms,
  facets() ~1 ms.

## 2026-07-15 08:04 - Branching policy: branch = workstream (batch follow-on evolutions), self-gate merge at stable completion

```yaml
entry_id: mse_xza4tj40p52bsbhn
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/workstream-branch-policy
topics:
  - control-plane
  - git-workflow
evolves:
  - mse_bzk370rfxdtta2bx
```

### Decision

- D: Refined the agent branching policy from "one branch per task/commit" to **"one branch per WORKSTREAM"**
  (user-chosen). Batch follow-on fixes, evolutions, and adjacent tweaks of the same goal onto the SAME
  branch — the tell is an `evolves`/`related` lifecycle edge to the entry just written, or the same
  files/subsystem — and merge the batch at a **stable, tested stopping point** (Claude self-gates; no
  per-merge approval pause). Open a new branch only for a genuinely new, independent goal. Seeded in
  `agent-rules.md` (the always-loaded rule) and `agent_collaboration.md` (Branch/Worktree Defaults +
  Branch History Preservation), both live + seed twins, and in the `feedback_use_agent_branches` memory.
- R: User saw it in the Trail: this session's read-path perf chain (Phase 1 warm start -> freshness memoize
  -> chunk memoize -> sidecar memoize, each an `evolves` of the last) landed as FOUR separate one-commit
  branches when it was really one workstream — the "too many branches, each evolving the previous"
  anti-pattern. My prior rule ("distinct fix/refactor/test tasks -> own branch") plus local-merge
  auto-merge drove branch-per-commit; the earlier good pattern batched related entries on one branch
  behind one merge gate.
- A: User chose, via AskUserQuestion: (1) workstream branch (batch related iterations) over
  branch-per-change; (2) self-gate merge at a stable completion over an explicit per-merge approval gate or
  immediate-per-change merge. So the policy keeps the autonomous flow, just coarser-grained. This change
  dogfoods the rule — the whole policy update is one workstream branch merged once at completion.
- F: .memory-seed/agent-rules.md, .memory-seed/skills/agent_collaboration.md (+ both seed twins under
  memory_seed/seed/); the ~/.claude memory feedback_use_agent_branches.md.
- T: esr Seed twins OK; `diff` confirms live == seed for both edited control-plane files.

## 2026-07-15 08:34 - Re-baseline roadmap docs after Foundation ship: memory-quality trio leads, 2.19 after trio, React deferred (graph-view attention first)

```yaml
entry_id: mse_304sneevyersqvh6
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/roadmap-reconcile
topics:
  - control-plane
  - proposal-lifecycle
```

### Decision

- D: Re-baselined the roadmap docs after the derived-projection Foundation shipped 2026-07-15 and per JNL's
  direction (memory-quality trio first → cut 2.19 after it → React deferred). A read-only roadmap-audit
  subagent cross-referenced every `2_Todo/*` plan, the CHANGELOG, and the newest sessions against what
  actually shipped; this pass applied its findings. `0_NEXT_STEPS.md`: the Foundation section is now marked
  SHIPPED (warm start + atomic swap + three perf refinements, `chunk()` 132 ms → 3.9 ms) with incremental
  ingest called out as a deferred low-urgency fast-follow; the **Ranking & graph quality trio is now the
  CURRENT LEAD**; release cadence = cut 2.19 after the trio; the Discipline branch rule was updated to
  **branch = workstream**; a new **B0 — Graph-view attention** workstream is recorded by name (pre-React,
  the reason React is deferred — it opens with a taste-elicitation from JNL, not a guess). CHANGELOG gained
  the six 2026-07-15 items the audit found missing (read-path perf fast-follows + four Trail UI/correctness
  fixes) so the 2.19 notes won't undercount. Section-E status-line drift fixed: derived-projection
  frontmatter (`next_action`/`priority` no longer contradict the SHIPPED body), evolution-edges "(unreleased)"
  → shipped in 2.18.0, related-entries metadata `2.15`→`2.18` + priority reframed, integration-mode
  P0.1/P0.2 harmonized to Phase 1/Phases 2–4, and Constitution §10 governance-first question marked RESOLVED
  (development resumed) with the stale "while development is paused" tail removed.
- R: `0_NEXT_STEPS.md` (dated 2026-07-14) predated the entire 07-15 session, so it still led with
  "Foundation (do first)" for work that had shipped — every subagent spawned for the trio would read that
  stale lead and misorient. Reconciling the plan + CHANGELOG is cheap and everything downstream reads those
  docs, so the advisor and the audit both put it first in the sequence. No seed twins are affected (all
  edits are under `docs/` + `CHANGELOG.md`). This is the first workstream of the memory-quality push; the
  branches that follow (ranking-ab → superseding_head → link-audit-apply) build the trio itself.

## 2026-07-15 17:24 - Match inbox to main state for ranking branch handoff

```yaml
entry_id: mse_907b547jerwssy7e
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feat/ranking-ab
topics:
  - proposal-lifecycle
```

### Summary

- Removed four untracked Memory Trace proposal Markdown files from `docs/1_Inbox` so the inbox matches `main` while taking over `claude/feat/ranking-ab`.
- Left Claude's `ranking-ab` implementation state untouched.

### Validation

- `git status --short --branch -- docs/1_Inbox` reports no inbox delta.
- `Get-ChildItem docs\\1_Inbox` shows only the two files tracked on `main`: `agent-namespaced-branch-worktree-lifecycle-proposal.md` and `worktree-gc-proposal.md`.

## 2026-07-15 17:33 - Promote Memory Trace graph/workspace proposal set into B0 next steps

```yaml
entry_id: mse_rsjbgk4qnyrtvv09
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feat/ranking-ab
topics:
  - proposal-lifecycle
  - memory-trace
  - ui-design
evolves:
  - mse_304sneevyersqvh6
```

### Summary

- Evaluated the four Memory Trace graph/workspace proposal docs supplied from Downloads and promoted the full set into `docs/2_Todo/`.
- Folded the set into `docs/2_Todo/0_NEXT_STEPS.md` as the concrete B0 Graph/workspace attention programme, replacing the prior placeholder.

### Decision

- D: Promoted the four-document Memory Trace graph/workspace proposal set to active `2_Todo` work and made it the detailed shape of B0: shell clarification/shared selection, renderer-neutral graph contract, renderer benchmark, topology-first graph, dockable inspector, and a later optional structural-provider tail.
- R: This is the work JNL explicitly asked to evaluate and fold into the plan, and it directly refines the 2026-07-15 roadmap decision that React stays deferred until the graph view gets proper attention. The documents are actionable proposals rather than source-only reference reports: they define scope, order, acceptance criteria, and open benchmark decisions.
- A: Did not put the provider proposal first. It depends on the native graph/workspace contract, so it is sequenced as the tail after graph/workspace foundations; `code-review-graph` remains an optional pilot, Graphify a benchmark, and SCIP later.
- A: No diagram sidecar; the workstream sequence is already captured in the promoted index and B0 plan text, and a sidecar would duplicate the roadmap structure rather than clarify it.
- F: `docs/2_Todo/memory-trace-graph-and-workspace-proposal-set-index.md`, `docs/2_Todo/memory-trace-graph-visualisation-and-temporal-topology-proposal.md`, `docs/2_Todo/memory-trace-structural-graph-enrichment-provider-proposal.md`, `docs/2_Todo/memory-trace-three-region-workspace-and-dockable-inspector-proposal.md`, `docs/2_Todo/0_NEXT_STEPS.md`, `docs/README.md`.
- T: `python -m memory_seed.cli links check` OK (45 files); `python -m memory_seed.cli doctor` healthy except pre-existing encoding warnings; `python -m memory_seed.cli esr` OK for integrity/topics/seed twins; lane count verified as Inbox 2 / Todo 25.

## 2026-07-15 17:46 - Sense-check long-horizon roadmap against Constitution v1.0

```yaml
entry_id: mse_txvnzgxx1gkw0br1
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feat/ranking-ab
topics:
  - proposal-lifecycle
  - memory-trace
related_entries:
  - mse_rsjbgk4qnyrtvv09
```

### Summary

- Reviewed the active long-horizon roadmap, Memory Trace architecture, B0 graph/workspace proposals, Constitution v1.0, and the roadmap decisions from 2026-07-14/15.

### Decision

- D: Treat the long-horizon direction as Constitution-aligned in substance, but not yet fully certified at the document level.
- R: The plan preserves local/offline core operation, append-only history, attribution, canonical Memory Seed semantics, model/provider independence, rebuildable projections, and down-rank-never-hide retrieval. However, the four new B0 proposals do not explicitly declare their five-question contribution, the older Phase 0-10 roadmap still sequences graph after React while current B0 puts graph/workspace attention before React, and the hosted-sync phase needs an explicit Markdown-reconciliation/rebuildability exit gate.
- A: Did not accept the `0_NEXT_STEPS.md` Constitution-aligned label as sufficient evidence; checked the underlying active proposals and architecture boundaries directly. No diagram sidecar because this was a compliance audit, not a new architecture or lifecycle flow.
- F: No roadmap or source documents changed; assessment recorded for follow-up.
- T: Checked `docs/CONSTITUTION.md`, `docs/2_Todo/0_NEXT_STEPS.md`, `docs/2_Todo/memory-trace-next-generation-implementation-roadmap.md`, `docs/2_Todo/memory-trace-product-and-system-architecture-blueprint.md`, and the four B0 graph/workspace proposal documents. ESR integrity, topics, and seed twins were OK; pre-existing lifecycle-link suggestions remain approval-gated.

## 2026-07-15 17:49 - Propose constitutional hardening actions for long-horizon roadmap

```yaml
entry_id: mse_bftnd96rvxb6vbh3
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feat/ranking-ab
topics:
  - proposal-lifecycle
  - memory-trace
related_entries:
  - mse_txvnzgxx1gkw0br1
```

### Summary

- Proposed four bounded constitutional-hardening actions, pending JNL approval: add explicit five-question/invariant sections to B0; split B0 into pre-React contract/benchmark work and later React implementation; add a hosted Markdown-settlement/rebuildability gate; and define provenance/authority taxonomy plus read-only quality metrics before agent automation.
- Recommended avoiding a single opaque trust score and preventing derived/provider confidence or quality metrics from affecting ranking until exposed and real-corpus validated.

### Validation

- Recommendations derive from the gaps recorded in `mse_txvnzgxx1gkw0br1` and preserve the existing strategic sequence rather than reopening it.

### Follow-up

- No roadmap or proposal edits made. Apply only after JNL chooses to adopt the recommendations.
- No diagram sidecar because this records advisory options, not an adopted architecture or implementation flow.

## 2026-07-15 18:05 - Constitution-harden long-horizon roadmap: B0a/B0b, hosted settlement, trust and quality gates

```yaml
entry_id: mse_a0bxp5n1wcnsjxvw
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feat/ranking-ab
topics:
  - proposal-lifecycle
  - memory-trace
supersedes:
  - mse_txvnzgxx1gkw0br1
evolves:
  - mse_bftnd96rvxb6vbh3
  - mse_rsjbgk4qnyrtvv09
```

### Summary

- Applied the approved constitutional-hardening pass to the Memory Trace long-horizon plan.
- The roadmap is now Constitution-aligned at the underlying proposal and phase-gate level, not only by a label in `0_NEXT_STEPS.md`.

### Decisions

#### D1 - Split graph/workspace attention into B0a and B0b

- D: Make B0a the pre-React shell-behaviour, graph-contract, fixture, topology-model, and renderer-benchmark gate; deliver B0b's selected renderer and dockable Inspector after the React shell through roadmap Phases 3 and 5. Keep structural providers as an optional tail after native B0b acceptance.
- R: This preserves JNL's graph-before-React direction while avoiding duplicate vanilla and React implementations. Each of the four graph/workspace documents now states its five-question contribution and invariant guards.
- A: Rejected full renderer migration and persisted dock implementation before React because that would build the same experience twice.

#### D2 - Gate hosted writes on Markdown settlement

- D: Add a non-binding candidate hosted settlement contract: project-owned hosted writes become durable only in append-only Markdown/YAML; concurrent conflicts are explicit; queues are idempotent; hosted project-memory projections must pass wipe/rebuild equivalence; complete export and local continuation survive expiry.
- R: Constitution Invariant #6 applies to hosted collaboration as well as local caches. Provider-owned evidence and server-owned authentication/billing remain separate authority domains rather than project-memory truth.
- A: Hosted implementation remains deferred. The candidate contract requires an explicit tier decision, security review, fixture proof, maintainer approval, and promotion into `3_Spec/`.

#### D3 - Add narrow provenance/authority and quality gates

- D: Promote two P1 proposals: one extends the shipped seven-value `ProvenanceClass` while separating authority, lifecycle, confidence, and fail-closed actionability; the other defines five read-only quality metrics with explicit populations, denominators, exclusions, and `not_applicable` behaviour.
- R: This operationalises Constitution sections 7 and 8 without an opaque trust score, composite quality score, telemetry, or premature behavioural effects. No taxonomy or quality field may alter ranking or agent behaviour without exposure, fixtures, and real-corpus validation.
- A: Broader entry-type taxonomy, maturity ladder, outcome benchmark, and undefined stale-rate remain reference inputs rather than active work.

#### D4 - Reconcile the canonical planning surfaces

- D: Update the next-steps brief, Phase 0-10 roadmap, coverage matrix, architecture blueprint, annotation/provider plans, deferred hosted plan, draft-spec index, and docs front door to one sequence and lifecycle state.
- R: Agents read these surfaces as planning authority; leaving graph-after-React, hosted-as-active, or no-proposals-promoted statements would reintroduce contradictory instructions.
- F: `docs/2_Todo/0_NEXT_STEPS.md`, `docs/2_Todo/memory-provenance-and-authority-taxonomy-proposal.md`, `docs/2_Todo/memory-quality-metrics-v0-proposal.md`, `docs/2_Todo/memory-trace-evidence-annotations-and-projection-architecture.md`, `docs/2_Todo/memory-trace-graph-and-workspace-proposal-set-index.md`, `docs/2_Todo/memory-trace-graph-visualisation-and-temporal-topology-proposal.md`, `docs/2_Todo/memory-trace-next-generation-coverage-matrix.md`, `docs/2_Todo/memory-trace-next-generation-implementation-roadmap.md`, `docs/2_Todo/memory-trace-product-and-system-architecture-blueprint.md`, `docs/2_Todo/memory-trace-structural-graph-enrichment-provider-proposal.md`, `docs/2_Todo/memory-trace-three-region-workspace-and-dockable-inspector-proposal.md`, `docs/3_Spec/draft/README.md`, `docs/3_Spec/draft/memory-trace-hosted-markdown-settlement-contract.md`, `docs/8_Deferred/memory-trace-hosted-product-and-security-architecture.md`, `docs/README.md`.

### Validation

- `git diff --check` passed.
- Local Markdown link check passed for 67 links across the changed planning documents.
- `python -m memory_seed.cli links check` passed (45 session-memory files).
- `python -m memory_seed.cli doctor` reports a healthy bootstrapped control plane; its only warning is the pre-existing 19-file encoding/CRLF set.
- Lane counts verified: Inbox 2, Todo 27, four candidate specs total (two physical in `3_Spec/draft/` plus two candidate specs retained in `3_Spec/`).
- `.memory-seed/index.md` was reviewed and left unchanged: it already points to the canonical next-generation roadmap/coverage documents, and no runtime topology, policy, or skill pointer changed.
- No diagram sidecar: the exact B0a -> B2 -> B0b and hosted-gate topology is already represented in the roadmap dependency block and proposal-set index; duplicating it in session memory would add no structure.

## 2026-07-15 18:17 - Assess shipping docs lifecycle surfaces in the planning profile

```yaml
entry_id: mse_wew6nkxc92b2en4k
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feat/ranking-ab
topics:
  - proposal-lifecycle
  - control-plane
related_entries:
  - mse_yfsrahvq87hxkcv9
  - mse_s3t523yvtr39978k
```

### Summary

- Assessed whether Memory Seed should ship project documentation and document-management control-plane surfaces.
- Recommended an opt-in, project-neutral expansion of the existing `planning` profile rather than adding docs lifecycle scaffolding to the default core.

### Validation

- Confirmed the current planning profile already installs `proposal_lifecycle.md` and four empty generic lane anchors.
- Checked the folder-primary lifecycle plan, the planning-profile implementation and tests, the reusable-skill boundary, and Constitution invariants 1, 2, 4, 5, and 6.

### Follow-up

- Recommendation only, pending JNL approval. If adopted, first complete and prove `docs index` / `docs check` locally, then extract a generic deploy-once scaffold with non-destructive adoption for repositories that already have docs.
- Do not seed this repository's Constitution, numbered paths, roadmap, product plans, or project-specific documentation corpus.

## 2026-07-15 18:31 - Capture seeded docs proposal and make SessionStart AGENTS-first with five entries

```yaml
entry_id: mse_m0xs623m4cs0kjag
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feat/ranking-ab
topics:
  - control-plane
  - proposal-lifecycle
evolves:
  - mse_wew6nkxc92b2en4k
  - ms-5e366ef4
```

### Summary

- Captured the proposed planning-profile document lifecycle as an unapproved inbox proposal.
- Changed the cross-agent SessionStart orientation contract to begin at `AGENTS.md` and inject the five newest applicable session entries across recent files.

### Decisions

#### D1 - Keep seeded docs lifecycle optional and proposal-gated

- D: Added `seeded-document-lifecycle-control-plane-proposal.md` to `docs/1_Inbox/` and updated the docs front-door count; implementation remains unapproved.
- R: The existing planning profile has only four empty anchors and a skill, while the richer lifecycle needs local `docs index` / `docs check` proof and non-destructive adoption semantics before it can become reusable seed behavior.
- A: Rejected copying this repository's Constitution, numbered paths, roadmap, or product corpus into new projects; the proposal keeps only generic opt-in machinery.

#### D2 - Make SessionStart AGENTS-first with five-entry recency context

- D: Claude, Codex, Gemini, and Cursor now receive an `AGENTS.md`-first startup directive plus the five newest applicable session entries in chronological order, spanning recent files when needed; Copilot's static prompt carries the same instructions. Empty/new projects still receive the directive.
- R: The old payload listed every heading but injected only one entry body. Five recent entries give bounded continuity while preserving direct-file recency, per-user layout selection, source attribution, and the no-semantic-search-for-latest rule.
- A: Kept per-entry 1,500-character caps and source paths instead of injecting unbounded history. Did not add a diagram sidecar because agent wiring and session topology are unchanged; this is a payload-window change covered directly by fixtures.
- F: `docs/1_Inbox/seeded-document-lifecycle-control-plane-proposal.md`, `docs/README.md`, `.memory-seed/hooks/session-start-context.py`, `memory_seed/seed/.memory-seed/hooks/session-start-context.py`, `memory_seed/core.py`, `.github/hooks/memory-seed.json`, `tests/test_memory_seed.py`, `README.md`, `docs/3_Spec/functionality-audit.md`, `CHANGELOG.md`.
- T: Live hook smoke check reports `AGENTS.md=true`, five injected entries, and requested window 5. Hook class 16/16 passed; hook-merge class 12/12 passed; core module 243/243 passed; full suite 457 passed with one expected skip; live/seed hook SHA-256 hashes match; `git diff --check` passed (with the existing CRLF normalization warning for `.github/hooks/memory-seed.json`).

## 2026-07-15 18:53 - Complete ranking-ab gate and bind real-corpus validation

```yaml
entry_id: mse_hexq9x671dww29te
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feat/ranking-ab
topics:
  - retrieval
  - mcp-tools
  - proposal-lifecycle
evolves:
  - mse_304sneevyersqvh6
```

### Summary

- Completed the accepted real-corpus ranking validation gate and prepared the ranking workstream for integration.

### Decisions

#### D1 - Ship a fail-closed full-corpus ranking gate

- D: Ship `memory-seed ranking-ab --signal <name> [--query <q> ...] [--json]` as the reusable branch-time gate for default ranking changes. The supersession signal derives every live lineage query, compares off/on at full corpus size, requires each replacement to out-rank the decision it retires, and requires a real no-affected-hit control to remain identical.
- R: Fixtures prove that a mechanism fires, but only the live corpus demonstrates that the intended decision wins without changing unrelated retrieval. Empty query sets, missing directional entries, and missing required controls must fail closed so incomplete evidence cannot produce a green gate.
- A: The inherited branch had registered the parser but not the command dispatch, so the advertised command returned `Unknown command: ranking-ab`; dispatch and exit semantics were completed before integration. An initial fixture expected damping to move a retired entry's rank, but equal-score ordering can leave the rank unchanged even when the score is correctly reduced; the test now asserts score reduction plus the actual contract outcome, replacement above predecessor. Brute-force control-query search was avoided in favour of title-token prefiltering followed by one real ranking verification per candidate.
- F: `memory_seed/ranking_ab.py`, `memory_seed/cli.py`, `tests/test_ranking_ab.py`, `README.md`, `docs/3_Spec/functionality-audit.md`, `CHANGELOG.md`.
- T: `python -m unittest tests.test_ranking_ab` passed 6 tests; `python -m memory_seed.cli ranking-ab --signal supersession_damping --json` passed three live lineages plus the `shipping surfaces profile` no-hit control across 428 entries; `python -m unittest discover -s tests` passed 463 tests with 1 expected skip.

#### D2 - Make the real-corpus gate binding and close the proposal

- D: Amend the normative "Expose before you rank" rule to require fixtures and the repeatable full-corpus A/B before a default flip, move the accepted ranking-gate proposal to `docs/5_Completed/`, and advance `superseding_head` to the next ranking-and-graph-quality item.
- R: The implementation now satisfies both acceptance criteria, so leaving the proposal in Todo or retaining conditional roadmap wording would make the document lifecycle contradict the shipped control surface.
- F: `docs/3_Spec/graph-edge-contract.md`, `docs/2_Todo/0_NEXT_STEPS.md`, `docs/2_Todo/memory-quality-metrics-v0-proposal.md`, `docs/2_Todo/supersession-successor-surfacing-proposal.md`, `docs/5_Completed/real-corpus-ranking-validation-gate-proposal.md`, `docs/5_Completed/README.md`, `docs/README.md`, `docs/5_Completed/grounding-provenance-write-time-links-proposal.md`.

### Implementation

- Added a named signal registry and reusable result model, deterministic lexical full-corpus comparisons, computed JSON verdicts, human output, and CLI exit codes `0` pass, `1` failed or incomplete gate, and `2` unknown signal.
- Custom queries extend rather than replace signal-derived directional cases, preserving the mandatory evidence while allowing extra probes.

### Validation

- Focused tests, live-corpus A/B, full unit suite, Python compilation, and encoding checks passed before proposal completion.

### Follow-up

- Implement additive/read-only `superseding_head` next, then use this gate before considering its bounded replacement boost.
- No decision-diagram sidecar was added because the command's linear off/on validation flow adds no spatial, temporal, or concurrent structure beyond the tests and contract prose.

## 2026-07-15 19:01 - Integrate ranking and long-horizon workstream into main

```yaml
entry_id: mse_4z9wcanedg82wp16
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - git-workflow
  - session-fuse
  - proposal-lifecycle
related_entries:
  - mse_hexq9x671dww29te
  - mse_a0bxp5n1wcnsjxvw
```

### Summary

- Committed the prepared ranking and long-horizon control-plane workstream as `9208a35a2faab0ecc0f22130c39d46b18e080dbb` and integrated `claude/feat/ranking-ab` into local `main` with session-aware merge commit `8a5a4501b67ce95764a7e7bb3fe34bf0e5793c67`.
- The merge fused all eight branch-local session entries and stamped their `Memory-Entry` trailers on the merge commit.

### Validation

- The merge dry run reported eight imports and no conflicts before the actual merge.
- On merged `main`, `python -m unittest discover -s tests` passed 463 tests with 1 expected skip; the six focused ranking tests passed; the 429-entry live supersession A/B passed all three lineages and its unchanged no-hit control; `links check` and `topics check` passed with only the 13 pre-existing four-topic warnings.

### Follow-up

- `main` is the clean long-horizon baseline. The next ranking-and-graph-quality item is additive/read-only `superseding_head`; the broader Memory Trace graph/workspace sequence remains governed by `docs/2_Todo/0_NEXT_STEPS.md` and Constitution v1.0.
- No diagram sidecar was added because this was a single branch-to-main merge with no concurrent worktree topology; the commit graph and session-fuse record already represent it exactly.

## 2026-07-15 19:08 - Launch long-horizon Wave 1 worker branches

```yaml
entry_id: mse_675taf80bxkmjy7j
user_initials: JNL
agent_type: codex
agent_name: orchestrator
project_path: .
subproject_path: null
branch: codex/integration/long-horizon-wave-1
topics:
  - agent-collaboration
  - git-workflow
  - proposal-lifecycle
related_entries:
  - mse_a0bxp5n1wcnsjxvw
evolves:
  - mse_4z9wcanedg82wp16
```

### Summary

- Started Wave 1 of the long-horizon goal as four isolated worker-owned branches from `e54b74b6c88141499e4e9b9a70041569739d52b0`, with this branch reserved for sequential review and integration.

### Decisions

#### D1 - Parallelize four disjoint workstreams

- D: Delegate successor surfacing/ranking, `topics suggest --from`, deterministic Evidence Packs, and Trail continuity display to separate branch owners with non-overlapping file allowlists.
- R: These are independently useful roadmap items whose primary write sets separate cleanly across core retrieval, topic CLI, a new Memory Trace evidence module, and vanilla Trail assets. The ranking trio's `link audit --apply` remains in the next wave because it overlaps the successor branch's retrieval files and the topics branch's CLI file.
- F: `codex/feature/superseding-head` in `.codex/worktrees/superseding-head`; `codex/feature/topics-suggest` in `.codex/worktrees/topics-suggest`; `codex/feature/evidence-pack-builder` in `.codex/worktrees/evidence-pack-builder`; `codex/feature/trail-continuity-axis` in `.codex/worktrees/trail-continuity-axis`.

#### D2 - Map the requested worker hierarchy to available agents

- D: Use one strong `gpt-5.4` worker as the accountable owner of each branch, with permission to delegate a bounded subtask to a smaller nested worker, while the current frontier agent remains orchestrator and reviewer.
- R: This runtime exposes GPT worker agents but no Claude Opus/Sonnet models. The branch-owner and nested-worker responsibilities can be preserved honestly without claiming unavailable model identities.
- A: The first spawn placed workers in the root checkout; all four correctly stopped at the guard without edits. The orchestrator then provisioned owned `.codex/worktrees/` paths and repeated the identity gate before authorizing implementation.

#### D3 - Integrate through a reviewed wave branch

- D: Merge worker branches one at a time into `codex/integration/long-horizon-wave-1`, inspect ownership and diffs after each merge, run targeted validation, obtain an independent validator pass, and only then promote the integration branch to `main` with session-aware merge.
- R: This preserves visible workstream topology, isolates review corrections from `main`, and gives cross-branch behavior one final validation surface.

### Validation

- Every worker reported the exact pinned base, clean branch, owned-worktree classification, and `Safe to write: yes` before implementation approval.

### Follow-up

- Review-to-rework is capped at two iterations per branch. Shared docs, proposals, changelog, and session memory remain orchestrator-owned.
- A diagram sidecar records the parallel branch-to-integration topology because concurrency is materially clearer spatially than in prose.
