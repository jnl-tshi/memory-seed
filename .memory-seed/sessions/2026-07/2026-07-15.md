## 2026-07-15 00:25 - Fix Memory-Entry trailer-block parsing: body-scan all lines + contiguous stamping (daisy-chain rendered parallel)

```yaml
entry_id: mse_skpkqx0hhkw6ve4f
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/fix/trailer-block-parsing
topics:
  - memory-trace
  - git-workflow
evolves:
  - mse_p4tj8rw2mzvq6k1e
```

### Decision

- D: Fixed a Memory-Entry **trailer-block parsing** bug that mis-rendered daisy-chained branches as
  parallel in the Trail. Two prongs: (1) **backend** (`memory-trace` `_first_parent_trailer_commits`) now
  scans the full commit body (`git log -z --format=...%B`) for EVERY `Memory-Entry:` line, instead of
  git's `%(trailers:key=Memory-Entry,...)` which parses only the final contiguous trailer block; (2)
  **stamping** (the `prepare-commit-msg` hook live+seed, and core `_stamp_memory_entry_trailers`) now
  appends new trailers CONTIGUOUSLY to an existing trailer block (blank-line-separate only after prose) and
  dedups against trailers already present.
- R: User caught it on the Trail: `claude/chore/link-pass-2026-07-13` and
  `claude/chore/evolves-upgrade-2026-07-13` (a git daisy-chain — evolves-upgrade forks from exactly where
  link-pass merges, `8194a82`) rendered as PARALLEL lanes. Root cause: each merge commit DOES carry the
  branch's own entry trailer, but a blank line (and a `Co-Authored-By`) splits it from the fused-entries
  block, so `%(trailers:...)` silently dropped the branch entry → the branch had no merge event →
  `estimated`/positional fallback → parallel. The body-scan is layout-agnostic and REPAIRS already-committed
  merges with no history rewrite; the stamping fix prevents recurrence.
- A: The backend body-scan is the high-value half — it retroactively repaired every affected merge in the
  live history. Verified in-process (fresh cache rebuild; preview stopped first to release the Windows
  cache-file lock): both branches now `estimated:false` with real merge/fork, the daisy-chain junction is
  real (`evolves-upgrade.fork == link-pass.merge == 8194a82`), and the live DOM shows both entries' dots at
  the same lane column (`cx=55`) — daisy-chained, no longer parallel. Backend-only + stamping; no frontend
  change, so no `app.js` cache-bust. This entry `evolves` the merge-accuracy feature that introduced
  `_first_parent_trailer_commits`.
- F: memory-trace/memory_trace/service.py, memory_seed/core.py,
  .memory-seed/hooks/prepare-commit-msg.py (+ seed twin `memory_seed/seed/...`),
  memory-trace/tests/test_trail_merge_accuracy.py, tests/test_git_hooks.py.
- T: memory-trace merge-accuracy + tests/test_git_hooks **21 passed** (incl. new split-trailer extraction +
  hook contiguity, each with a git-strict-parser guardrail); test_memory_seed merge/fuse/hook **242 passed**;
  live DOM `cx=55==55` (shared lane).

## 2026-07-15 00:38 - Add manual Refresh button to Memory Trace topbar (re-read memory, keep filters, auto-extend date to latest)

```yaml
entry_id: mse_1gjtkr5nny843rx2
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/trace-refresh-button
topics:
  - memory-trace
  - ui-design
```

### Decision

- D: Added a manual **Refresh** button (⟳) to the Memory Trace topbar (between the Trail/Graph tabs and the
  theme toggle). `refreshData()` re-fetches `/api/runtime` + `/api/facets` and reloads the active view
  WITHOUT a full page reload, spinning the control (`.is-refreshing`) until done. The server rebuilds its
  projection on demand when the corpus changed, so this surfaces entries added since the page opened.
- R: User request after a post-midnight entry (2026-07-15) sat past a stale "To 14/07" date filter with no
  way to pull fresh data short of restarting the server. Filters PERSIST across a refresh (view, query,
  selection, a manually-narrowed date window) — with one deliberate nudge: if the user was viewing up to the
  previous latest (dateTo at/beyond the old max bound), the window auto-extends to the new latest, so an
  after-midnight entry appears instead of hiding past the filter; a manually-narrowed window is kept as-is.
- A: Reused the existing load path (`api()` + `loadView()` + `render()`) rather than a bespoke fetch;
  `loadSeq` guards overlapping loads and a failed refresh clears the spinner in `finally` (never wedges).
  No new server surface — the server already rebuilds on request. Reduced-motion disables the spin (the
  existing global animation rule).
- F: memory-trace/memory_trace/static/app.js (refreshData + topbar button + click dispatch + `refreshing`
  state flag), memory-trace/memory_trace/static/styles.css (spin keyframes),
  memory-trace/memory_trace/static/index.html (cache-bust -> trace-refresh-button-20260715).
- T: Live DOM (primary preview): the button renders (⟳, correct title); a click fires runtime+facets+graph
  re-fetches with no page reload and clears the spinner; no console errors. memory-trace test_service +
  test_trail_golden 44 passed (the change is client-side JS, untested by Python; the data layer is intact).

## 2026-07-15 00:44 - Trail To-date defaults to today (not newest entry): shared defaultDateTo helper across seed/chip/clear/refresh

```yaml
entry_id: mse_860h4ksbvn69nb1y
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/trace-to-date-tracks-today
topics:
  - memory-trace
  - ui-design
evolves:
  - mse_1gjtkr5nny843rx2
```

### Decision

- D: The Trail's "To" date filter now defaults to **today** (local) at load, not just the newest entry's
  date. Factored a `defaultDateTo(bounds)` helper (`= max(today, newest-entry-date)`) and routed
  `seedDates`, the filter-chip test, and the clear-reset through it so "today = default" is coherent (no
  phantom "to …" chip; clearing the date filter resets to today). `refreshData`'s auto-extend now targets
  the same helper.
- R: An entry written today — especially after midnight, before any refresh — fell outside a window whose
  "To" tracked the newest ENTRY date at load. Defaulting to today keeps new work in view from the first
  paint; `max(today, newestEntry)` still guards a future-dated entry. This closes the loop on the reported
  missing `evolves` edge (`mse_skpkqx0hhkw6ve4f -> mse_p4tj8rw2mzvq6k1e`): never a data bug — the 07-15
  source was just sitting past a "To 14/07" default.
- A: Chose `max(today, bounds[1])` over plain `bounds[1]` (misses today's new entries) and over plain today
  (would hide a future-dated entry). One helper used at all four sites so chip / clear / seed / refresh
  never disagree on "default". Evolves the refresh-button entry — generalizes its inline auto-extend into
  the shared helper and applies it at load, not only on a manual refresh.
- F: memory-trace/memory_trace/static/app.js (`defaultDateTo` + `todayLocalISO`; seedDates, chip, clear,
  refreshData), memory-trace/memory_trace/static/index.html (cache-bust -> trace-to-date-today-20260715).
- T: Live DOM (primary preview): `#date-to` = 2026-07-15 (== browser local today), `#date-from` unchanged
  (min bound), zero filter chips (no phantom); the `evolves` edge is now in the DEFAULT graph
  (`evolves_edge_visible_by_default: true`); no console errors. test_service + test_trail_golden 44 passed.

## 2026-07-15 00:55 - Trail: selecting an entry reveals its links (grow window to linked neighbours), fixing edges to off-window targets

```yaml
entry_id: mse_bq1x6drw6j7wy39n
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/fix/trail-reveal-linked-neighbors
topics:
  - memory-trace
  - ui-design
```

### Decision

- D: Selecting a Trail entry now **reveals its links**. Added `ensureLinkedNeighborsVisible(chunkId)`: on
  selection it grows the client-side `trailWindow` (page-rounded to `TRAIL_WINDOW_STEP`, reusing the
  `ensureTrailVisible` pattern) to include the selected entry's lifecycle/related neighbours, so an edge to
  an older, not-yet-shown entry actually draws. Branch-lane edges are excluded (continuity, not a "link").
- R: The Trail fetches the full corpus but windows to 60 rows client-side; an edge only draws between two
  SHOWN rows. So the user's `evolves` edge (`mse_skpkqx0hhkw6ve4f -> mse_p4tj8rw2mzvq6k1e`, 3 days older)
  rendered as NO line - the entry looked unlinked despite the edge being present in the data and the reader.
  The corpus is already loaded client-side, so revealing the neighbour is a cheap window bump, not a fetch.
- A: Chose "reveal on select" over an off-window stub/indicator - showing the actual edge to the actual row
  matches "I selected it to see its links" better than a placeholder. Excluded branch edges so selecting an
  entry doesn't drag a whole long-lived branch into view. Trail-view only. (An off-window indicator remains
  a possible follow-up for links beyond the fetched 1000-entry cap - not hit at this corpus size.)
- F: memory-trace/memory_trace/static/app.js (`ensureLinkedNeighborsVisible` + `selectChunk` wiring),
  memory-trace/memory_trace/static/index.html (cache-bust -> trail-reveal-links-20260715).
- T: Live DOM (primary preview): selecting `mse_skpkqx0hhkw6ve4f` grew the window 60 -> 240 (= ceil to cover
  the target row), the 07-12 target now renders (`y=2190`), and exactly one long dashed evolves-colored
  (`#4f98d9`) path spans source (`y=90`) -> target; no console errors. test_service + test_trail_golden 44
  passed (client-side JS, untested by Python; data layer intact).

## 2026-07-15 01:14 - Ship derived-projection Phase 1 warm start: git-watermark + dirty-signature freshness (no whole-corpus scan), schema-versioned atomic rebuild

```yaml
entry_id: mse_c70ze3saqsw6mcme
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/derived-projection-phase1
topics:
  - memory-trace
  - git-workflow
evolves:
  - mse_99219wsng7xj4h03
```

### Decision

- D: Shipped **derived-projection Phase 1 (warm start)** — the foundation item. Extended the Memory Trace
  `TraceCache` into a formalized read-model: a `PROJECTION_SCHEMA_VERSION` (bump -> full rebuild), a stored
  **build watermark** (last-built HEAD) + a **signature of the git-dirty session files**, and a reworked
  `ensure_current` that proves freshness in **O(changes)** — HEAD unmoved AND the dirty signature unchanged
  -> no rebuild, **no whole-corpus scan** (contract G5). Every git ambiguity (moved/gone watermark, `git
  status` error, schema mismatch) **fails toward a full rebuild**; no git -> the existing mtime scan (G7).
  Atomic build/swap (G4) was already present, now schema-gated. New git-repo suite
  `test_projection_warm_start.py` (7): fast-path no-rebuild + no-corpus-scan, dirty/commit/schema-bump
  rebuilds, no-git degradation, byte-identical rebuild (G2).
- R: Killed the load-time trap: `ensure_current` (called on every API request) previously did an O(corpus)
  mtime scan, and any change triggered a full rebuild. Measured on the real repo: full rebuild **~6.2 s ->
  warm `ensure_current` ~78 ms** with no rebuild when sessions are clean (~80x). Using a dirty-file
  *signature* (not a clean-only fast path) makes the common actively-editing case O(dirty), not a rebuild
  per call. Invariant #6-clean: the projection stays derived / disposable / rebuildable, never authoritative.
- A: Tiered per advisor. Shipped the warm-start + atomic-swap + schema-version tier (satisfies the goal:
  explicit ingest, byte-identical rebuild, git-watermark warm start, atomic swap). **Deferred incremental
  ingest** (re-project only the delta files' chunks — O(1) ingest on change; whole-history git meta
  recomputed only when HEAD moved) as a documented fast-follow, gated on an incremental==full-rebuild
  equivalence test — a full rebuild on change is correct now, whereas an unverified incremental path would
  be a latent G2 violation. Detection is already O(changes); the deferred work makes the *ingest* O(changes).
- F: memory-trace/memory_trace/service.py (`PROJECTION_SCHEMA_VERSION`, `_git_head`, `_porcelain_paths`,
  `_working_tree_signature`; `TraceCache._sessions_pathspec`/`_is_current`, reworked `ensure_current` +
  rebuild provenance), memory-trace/tests/test_projection_warm_start.py (new),
  docs/2_Todo/derived-projection-implementation-plan.md (Phase 1 status), CHANGELOG.md.
- T: memory-trace **106 passed** (99 + 7 new warm-start). Real-repo timing: 6224 ms full rebuild vs 78/70 ms
  warm `ensure_current`, `rebuilt=False` on clean sessions.
