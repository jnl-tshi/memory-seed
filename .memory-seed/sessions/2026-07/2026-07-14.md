## 2026-07-14 00:15 - link show augments link sidecars (reflects the effective graph, not raw entry YAML)

```yaml
entry_id: mse_8cz9th5s9z1wv9gp
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/fix/link-show-augment-sidecars
topics:
  - graph
related_entries:
  - mse_cbexvsnxenvy08fh
```

### Decision

- D: `link show` now unions late-authored link-sidecar edges into the graph it prints (calls
  `augment_chunks_with_link_sidecars` on the extracted chunks before `build_related_entry_graph`),
  so its supersedes / superseded_by / evolves / evolved_by / inbound / importance_score reflect the
  same EFFECTIVE graph that retrieval, MCP, and the Trace UI read - not just the raw entry YAML.
- R: The CLI `show` handler built the graph straight from `extract_memory_chunks` with no sidecar
  augmentation, so any late-authored (sidecar) edge - the 6 curated evolves edges and the earlier
  `mse_c1ak23v` one - was invisible in `show` (`evolved_by (0)`) even though the effective graph
  carried it. The gap surfaced while verifying the curated evolves-upgrade the previous turn.
- A: Augment inside `build_related_entry_graph` itself (rejected: would double-augment the callers
  that already augment and would change a core function's contract; the established pattern is
  caller-augments - retrieval/MCP/Trace each call the augmenter - so bringing `show` in line with the
  other callers is the minimal, consistent fix).
- F: memory_seed/cli.py (link `show` handler), tests/test_memory_seed.py (regression test).
- T: new `test_link_show_reflects_sidecar_edges` (a sidecar-only evolves edge is now visible from both
  ends: `evolves (1)` on the source, `evolved_by (1)` on the target) - fails pre-fix, passes post;
  real corpus `link show mse_wh33mc61swqx0vkv` now prints `evolved_by (1): mse_07vwfbgsk31ec7d6`,
  importance unchanged at 2.00; full CLI test file green.

## 2026-07-14 00:41 - Doc-lifecycle evaluation: refreshed 0_NEXT_STEPS + 10 lane moves (evidence-backed)

```yaml
entry_id: mse_msgs6qzn3mcnmkn5
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/next-steps-refresh
topics:
  - documentation
  - session-logging
```

### Decisions

Goal: evaluate the inbox + todo plan docs, update the docs, and spec an acceptable next-steps plan.
Method: a 3-subagent evidence pass (each doc verified against `CHANGELOG.md` + code, NOT `0_NEXT_STEPS`'s
own claims — that index was the drifted artifact being fixed), then lead synthesis + advisor-guided scope.

#### D1 - Status verdicts from evidence, not self-report; move discriminator = "any live tail?"

- D: Classified every top-level inbox (6) + todo (~20) doc by REAL status (CHANGELOG line / code symbol),
  not stated status. A doc moves to a terminal lane only with ZERO remaining work; anything with a tail
  stays in `2_Todo` with shipped parts marked. Key findings: the 5 sibling inbox proposals are each a
  genuine unbuilt gap (none redundant with shipped work — e.g. `superseding_head` is the unbuilt
  symmetric mirror of the shipped `evolved_head`, not a duplicate); freshness-ranking + proactive-retrieval
  + fontjoy are fully shipped; the two goal-run briefs are spent one-shot artifacts; the two
  proposal-synergy reviews are 2.13-era and self-closed.
- R: `0_NEXT_STEPS` asserted statuses that lagged reality (evolution-edges still said "unreleased" for work
  shipped in 2.18.0), so laundering its claims into lane decisions would propagate drift. Verifying against
  CHANGELOG+code is the only trustworthy signal. The live-tail discriminator prevents the two failure modes
  the folder-primary model exists to kill: burying live work in Completed, or falsely closing it.
- A: Mass-recording every doc as "done because the headline shipped" (rejected — buries the real tails on
  integration-mode, evolution-edges continuity display, topics-suggest, decision-diagrams Phase 3).
- F: docs moves below; `.claude` scratchpad synthesis.
- T: `links check` OK (43 files) after all moves.

#### D2 - 10 low-risk lane moves; the 43-doc `completed/` bulk migration stays P2

- D: `git mv` (history-preserving): 5 fully-shipped/spent docs -> `5_Completed/` (freshness-ranking,
  proactive-retrieval, fontjoy, both goal-run briefs); the 2 spent synergy reviews -> `7_Superseded/`
  (renamed `claude-`/`codex-` to deconflict), emptying + removing `2_Todo/{Claude,codex}/`; the 2 parked
  strategy docs (commercialisation, hosted-security) -> `8_Deferred/`; `document-lifecycle-system-plan.md`
  `1_Inbox` -> `2_Todo` (it is mid-execution: Phase 1 shipped). Fixed 3 broken clickable links + ~8 stale
  `docs/2_Todo/<moved>` path mentions in active docs; refreshed the front door (path, side-folder allowlist,
  state snapshot) and the 5/7/8 lane README contents; corrected the moved plan's stale frontmatter.
- R: The 5 inbox proposals stay in `1_Inbox` (raw, unbuilt, awaiting user approval — correct lane). The
  43-doc `2_Todo/completed/` archive migration is deliberately NOT done here: it's link-churn across ~13
  files that changes nothing about "what's next" (advisor call) - it stays the documented Phase 2.
- A: Move commercialisation/hosted only in the plan text but leave in `2_Todo` (rejected - folder-primary
  means the parked state must be visible in the tree, not just prose).
- F: docs/1_Inbox, docs/2_Todo, docs/5_Completed, docs/7_Superseded, docs/8_Deferred, docs/README.md,
  docs/3_Spec/graph-edge-contract.md, docs/4_Reference/*, CHANGELOG.md.
- T: `links check` OK; grep confirms no remaining broken clickable link to a moved path; code provenance
  mentions are bare filenames (folder-agnostic, unaffected).

#### D3 - Rewrote 0_NEXT_STEPS as a forward-looking plan (477 -> ~120 lines)

- D: Replaced the history-laden index with {current state + CHANGELOG pointer; live work sequenced as
  Track A (close open tails) + Track B (Memory Trace next-gen: B1 Evidence Pack Builder now, B2 React/Vite
  shell as the greenlight-gated strategic bet, B3 annotations long-horizon); captured inbox proposals
  awaiting approval; doc-lifecycle Phase 2; a distinct PARKED section for user/market/account items;
  discipline + naming}. Also added a CHANGELOG Unreleased line for the `link show` sidecar-augment fix.
- R: Advisor: the plan IS the deliverable; cut the ~200 lines of release history (CHANGELOG is the
  authority) and keep it forward-looking; keep monetisation/hosted/launch as PARKED, never sequenced as
  engineering next-steps; the real judgment is sequencing the ~10 memory-trace docs (governance trio =
  read-to-sequence; two parallel bucket-A items; annotations gated on the shell) - done by the lead, not
  the subagents. "Acceptable" implies user sign-off, so the plan is surfaced for review.
- F: docs/2_Todo/0_NEXT_STEPS.md.
- T: internal links verified; front door + plan cross-consistent.

## 2026-07-14 08:46 - Add /situate slash command (verify published version from PyPI, orient before work)

```yaml
entry_id: mse_w0qw3asfdnetfkqa
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/chore/situate-command
topics:
  - documentation
```

### Decision

- D: Added a `/situate` slash command (`.claude/commands/situate.md`) that re-orients in the repo before
  work: git state (dirty / ahead-of-origin, noting `local-merge` = no push), **published-version
  verification against PyPI's source of truth**, newest-session-log-by-date read (not `memory_search`),
  worktree posture (stale vs another agent's dirty), and a ≤6-line briefing including the top next actions
  from `0_NEXT_STEPS.md`. Read-only — reconcile and brief, never edit/commit/start work.
- R: The `/insights` usage report named "stale environment and version assumptions" a top friction class —
  the agent twice oriented from a stale snapshot (claiming 2.13 / 2.16 unreleased when they were live on
  PyPI). Codifying the ritual — above all the "verify the published version, never assume" step — makes
  orientation deterministic and kills that whole correction class.
- A: `.claude/skills/situate/SKILL.md` as the insights literally suggested (rejected — this repo's
  slash-command convention is `.claude/commands/*.md`, per the existing `esr.md`, not `skills/`); a portable
  `.memory-seed/skills/` orientation skill (rejected — avoids control-plane churn + seed-twin sync for what
  is a Claude-Code-only convenience command).
- F: .claude/commands/situate.md
- T: Verified both version-check commands work live — PyPI JSON and `pip index versions` both return 2.18.0,
  matching local `pyproject.toml`, with the unreleased tranche in CHANGELOG (exactly the local==published +
  unreleased-on-main nuance the check must surface).

## 2026-07-14 11:53 - memory-seed situate: cross-agent orientation CLI + shims + routine; developer persona ground-truth-state check

```yaml
entry_id: mse_5p94m2c3rwy7kbtj
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/situate-cli
topics:
  - documentation
```

### Decisions

Full cross-agent orientation: a `memory-seed situate` CLI + per-agent command shims + a portable routine,
plus the developer-persona disposition that consumes it. Motivated by the `/insights` report naming
stale-version/state assumptions a top cross-session friction class.

#### D1 - `memory-seed situate`: network-free orientation preflight with full cross-agent parity

- D: New `memory-seed situate` CLI (`situate.py`, mirroring `esr.py`) — a read-only report of LOCAL
  authoritative facts: git posture (branch / dirty / ahead-of-origin), `integration_mode`, the newest
  session entry (via the layout-aware `iter_session_documents` reader, not a re-derivation), worktree
  posture (reusing esr's helpers), and the local `pyproject` version + whether the CHANGELOG has a
  non-empty `## Unreleased`. Portable routine in `.memory-seed/skills/orientation.md` (registered core
  skill + seed twin); thin shims `.claude/commands/situate.md` + `.gemini/commands/situate.toml` (seed
  twins); `agent-rules.md` Operating-Mode-Start step 7 gains a one-line pointer so Codex/Cursor follow it.
  Registered in SEED_FILES + CORE_SKILL_NAMES + package-data + the seed-manifest test.
- R: The `/insights` report named stale environment/version assumptions a top friction across multiple
  sessions (asserting a version was unreleased when it was live on PyPI). `.claude/commands/situate.md`
  alone was Claude-only; the `esr` model — CLI mechanical report + portable routine + per-agent shims,
  Codex/Cursor via agent-rules — is how this repo makes a command all-agent, and the user chose full
  parity. The CLI stays NETWORK-FREE (advisor): the authoritative PyPI check lives in the routine/shims
  prose where it degrades offline, so the CLI is fast, offline-safe, and trivially testable like esr;
  "verify, don't assume" is met by the routine reading ground truth, not by the CLI fetching it. The
  dev-repo-only "unreleased tranche" note is gated on `pyproject name == memory-seed`, mirroring how
  `seed_twin_drift` self-limits to the control-plane repo.
- A: Fetch PyPI inside `situate_report()` (rejected, advisor — breaks the zero-network invariant:
  flaky/hanging/generalization); `.claude/skills/situate/SKILL.md` per the insights wording (rejected —
  repo convention is `.claude/commands/*.md`); leave situate Claude-only (rejected — user wants
  Codex/Gemini parity).
- F: memory_seed/situate.py, memory_seed/cli.py, memory_seed/core.py (SEED_FILES + CORE_SKILL_NAMES),
  pyproject.toml, .memory-seed/skills/{orientation.md,index.md}, .memory-seed/agent-rules.md,
  .claude/commands/situate.md, .gemini/commands/situate.toml, memory_seed/seed/** twins,
  tests/test_situate.py, tests/test_memory_seed.py (seed-manifest pin).
- T: tests/test_situate.py (7: fail-open non-git; cross-layout newest-session; version + dev-repo
  detection; CHANGELOG-unreleased; real-git branch/dirty/ahead; static network-free guard) + test_esr.py
  green; 242-test seed/init/update/doctor/skill/registry slice green; `esr` seed-twins OK; live `situate`
  + `--json` verified.

#### D2 - Developer persona evolution: verify ground-truth STATE from source of truth, not a snapshot

- D: Added to `.agents/developer.md` Section VI Self-Correction a mandatory check — before asserting
  project state (published/released version, whether something shipped, "the latest" entry/branch), verify
  from the source of truth (run `memory-seed situate` + check PyPI), never from an in-context snapshot.
  Logged in the persona's Project Adaptations (2026-07-14). Live-only adaptation; the seed persona
  template stays generic (this project's convention — persona adaptations are live-only, confirmed by the
  seed template carrying no dated adaptations and `.agents/` being outside the seed-twin drift check).
- R: User-approved (Proposal 1 of the persona-evolution review). Section VI's "execute fresh, not from
  memory" covered TEST commands but not STATE/version assertions — exactly the drift the `/insights`
  report flagged across sessions. The disposition sharpens the base orientation rule (agent-rules +
  orientation.md) and is backed by the situate tool from D1.
- A: A second "surface the deliverable before housekeeping" disposition (Proposal 2) was offered; the user
  applied only Proposal 1.
- F: .agents/developer.md.
- T: n/a (persona doc; not seed-twin-checked — the drift check is skills-only).

## 2026-07-14 12:12 - Triage 4 dropped strategy docs: 1 proposal stays inbox, 3 source reports to reference; plan updated

```yaml
entry_id: mse_brt83hq9cmtmbqxq
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/inbox-triage
topics:
  - documentation
related_entries:
  - mse_msgs6qzn3mcnmkn5
```

### Decision

- D: Triaged 4 strategy/research docs the user dropped into `docs/1_Inbox`. Routed by NATURE per the
  `proposal_lifecycle` protocol: the one actionable proposal (`memory-seed-architectural-discovery-proposal.md`)
  stayed in `1_Inbox` pending approval; the three source docs moved to `4_Reference` with `-report` kind
  names (`memory-seed-gitlens-competitor-report.md`, `memory-seed-strategic-synthesis-report.md`,
  `memory-seed-rectification-priorities-report.md`). Added conformant frontmatter + a per-doc triage note
  (net-new vs overlap). Surfaced the genuinely-net-new ideas + the strategic TENSION in
  `0_NEXT_STEPS.md` "Captured strategic input"; bumped the front-door snapshot (Inbox 6, Reference 13).
  Repaired CRLF→LF on all 4 (the user's Windows drop).
- R: A subagent dedup pass showed the set overlaps the existing corpus (`memory-seed-market-fit-report.md`,
  the next-gen blueprint, `agent-rules.md` Working Principles) more than it adds. Genuinely net-new worth
  mining: the Constitution + amendment process, an entry-type taxonomy, a content-trust-level taxonomy, an
  outcome-comparison benchmark, Memory-Quality-as-KPI, and a GitLens competitor/integration profile. The
  architectural-discovery proposal's ask — *pause major architectural expansion to derive a governance
  Constitution first* — DIRECTLY conflicts with `0_NEXT_STEPS` Tracks A/B, so it is flagged as a user
  sequencing decision, not resolved. Confirmed NOT a duplicate of `memory-trail-competitor-analysis.md`
  (a different, unrelated tool). Nothing promoted to active work without user approval.
- A: Keep all 4 in `1_Inbox` as "proposals" per the user's framing (rejected — 3 are source
  research/synthesis, not buildable plans; the protocol routes source → reference, with the actionable
  items surfaced in the plan so nothing is lost). `git rm` the heavily-overlapping material (rejected —
  never delete; reference keeps the why, mirroring supersede-don't-delete).
- F: docs/1_Inbox/memory-seed-architectural-discovery-proposal.md,
  docs/4_Reference/memory-seed-{gitlens-competitor,strategic-synthesis,rectification-priorities}-report.md,
  docs/2_Todo/0_NEXT_STEPS.md, docs/README.md.
- T: `links check` OK (43 files); `doctor` healthy; `encoding check` OK on all 4 (CRLF repaired to LF);
  grep confirms no stale refs to the old filenames.

## 2026-07-14 13:46 - Constitution-first pause: authored docs/CONSTITUTION.md v1 (ratification draft); Tracks A/B paused

```yaml
entry_id: mse_mhfreh4t521ryc2v
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/constitution-v1
topics:
  - documentation
related_entries:
  - mse_brt83hq9cmtmbqxq
```

### Decision

- D: On the maintainer's decision, PAUSED feature development (`0_NEXT_STEPS` Tracks A/B) and authored the
  **Memory Seed Constitution v1** as a living, top-level governing document (`docs/CONSTITUTION.md`, ~1300
  words). It distils DEMONSTRATED behaviour into a tiered hierarchy — Vision → Invariants (7: users-own-
  memory, append-only-never-rewrite, explainable/attributable, files-authority-now/memory-authority-why,
  model-independent, human-readable-durable, retrieval-never-hides-history) → Principles → Policies →
  Implementations — each clause CITED to the spec/skill/code that proves it, or tagged `[candidate]` where
  aspirational (trust model, memory-quality metrics). Adds the four-layer model (Constitution above
  Platform above Experience), a five-question proposal test (Capture/Validation/Retrieval/Trust/
  Application), an Open Questions section (incl. the resume-A/B-vs-governance tension itself), and a
  lightweight governance rule (evolution vs constitutional amendment; versioned; amendment log). Shipped as
  a RATIFICATION DRAFT (v1.0-draft) — NOT yet ratified. Promoted `architectural-discovery-proposal`
  1_Inbox→2_Todo (P0, active); banner-marked the pause in `0_NEXT_STEPS` + the `docs/README.md` front door.
- R: The maintainer chose Constitution-first governance over continuing Tracks A/B. Per the advisor: write
  the Constitution DIRECTLY (not a separate discovery report — fold evidence in as inline citations); keep
  it SHORT and readable in one sitting ("grows" = accretes by amendment, not comprehensive at v1); tier
  RUTHLESSLY (Markdown / folder=state / forward-only are policies/implementations, NOT invariants); never
  launder aspiration as evidence; place it top-level ABOVE `3_Spec` (specs conform to it). Derived — not
  invented — from `agent-rules.md` Working Principles + the `3_Spec` contracts + the demonstrated append-
  only / forward-only / network-free / vendor-neutral / supersede-don't-delete patterns. Vision anchored on
  the discovery proposal's strategic-position line + the market-fit thesis, not a new mission.
- A: The proposal's two-step (big Discovery Report THEN Constitution) — rejected (process-for-its-own-sake;
  folded evidence inline + kept its valuable "open questions" as a live section). A subagent fan-out to
  derive invariants — rejected (advisor: open-ended discovery is a sprawl engine that returns 40 candidates
  you mostly demote; synthesised directly, then attached evidence pointers). Seed twin / `SEED_FILES` /
  `CORE_SKILL_NAMES` registration — rejected (this is Memory Seed's OWN doc, not a shipped template).
  Subordinating the locked `agent-rules.md` to the Constitution now — deferred (a separate user-approved
  amendment; v1 only references it).
- F: docs/CONSTITUTION.md (new); docs/2_Todo/0_NEXT_STEPS.md (pause banner + Tracks A/B marked paused);
  docs/2_Todo/memory-seed-architectural-discovery-proposal.md (promoted from 1_Inbox, P0 + next_action);
  docs/README.md (front-door Constitution pointer + snapshot Inbox 5 / Todo 16).
- T: `links check` OK (43 files); the Constitution's internal link resolves; `encoding check` OK; ~1298
  words (within the one-sitting target). NOT ratified — surfaced to the maintainer for ratify/amend.

## 2026-07-14 15:10 - Constitution: derived-layer / optional-tier model — Markdown authoritative everywhere (open-core); resolves the collaboration fork

```yaml
entry_id: mse_gn2kmdenk0p4cn7z
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/constitution-derived-layers
topics:
  - documentation
related_entries:
  - mse_mhfreh4t521ryc2v
```

### Decision

- D: Refined the Constitution draft (`docs/CONSTITUTION.md`) after a design-conversation exploration of
  optional implementation layers. Widened the pending "database" edit into a general **derived-layer /
  optional-tier model** and RESOLVED the source-of-truth-under-collaboration fork: **Markdown is
  authoritative *everywhere*** (Invariant #6 reframed), including a future hosted/collaborative tier — every
  other store (cache, index, DB, embedding, hosted backend) is a derived, rebuildable projection, never a
  second source of truth; concurrent writes resolve *into* Markdown. Invariant #1 gained a core carve-out
  (optional layers may add cache/index/DB/hosted service; the core never depends on them). Added an
  **"Open-core, one source of truth"** principle. Rewrote §5 Implementations as bands by distance from the
  core: core / derived accelerators (DuckDB, SQLite FTS5, vector index) / optional-local (React, **VS Code
  extension**, desktop, local AI) / hosted-collaborative. §10: marked the collaboration fork RESOLVED; the
  remaining open item is *which* commercial tier to reach (local-pro / team-hosted / enterprise) and when.
- R: The maintainer wanted the human-readability invariant reviewed against a potential optional Postgres
  accelerator while keeping Markdown the foundation. Per the advisor: this is a design conversation, not a
  research deliverable — do NOT fan out (open-ended architecture ideation is a sprawl engine); sketch the
  option space by *distance from the invariant line*. The real fork a flat sweep buries: does a hosted,
  multi-user tier keep Markdown authoritative, or make a server DB authoritative with Markdown as an export?
  The maintainer chose Markdown-authoritative everywhere (open-core). That makes the wording clean — the
  source-of-truth invariant extends to all tiers, so a read-only *or* collaborative DB is always a derived
  projection — and it unblocks the performance/commercial direction (DuckDB/vector accelerators, a VS Code
  extension, a hosted tier) without weakening any invariant. This is exactly the case the Constitution's
  invariant/implementation tiering exists to resolve.
- A: Fan out ~5 agents to survey architecture options (rejected, advisor — open-ended ideation sprawls;
  sketched directly). DB-authoritative on the hosted tier with Markdown as export (rejected by the
  maintainer — splits the truth model and undercuts the core differentiation). A separate new
  "source-of-truth" invariant (rejected — folded into #6 to keep invariants at 7, no sprawl).
- F: docs/CONSTITUTION.md (Invariants #1 & #6; new open-core principle; §5 Implementations bands; §10 open
  questions; header note).
- T: `links check` OK (43 files); the internal link resolves; `encoding check` OK; 1608 words (within the
  one-sitting target). Still a RATIFICATION DRAFT — not ratified.

## 2026-07-14 15:57 - Derived read-model / projection contract (draft spec) + implementation plan — Platform layer under Invariant #6

```yaml
entry_id: mse_99219wsng7xj4h03
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/projection-contract-spec
topics:
  - documentation
related_entries:
  - mse_gn2kmdenk0p4cn7z
```

### Decision

- D: Authored the Platform-layer design that makes Constitution Invariant #6 concrete — a candidate spec
  (`3_Spec/draft/derived-read-model-projection-contract.md`) + a blocked implementation plan
  (`2_Todo/derived-projection-implementation-plan.md`). Contract: Markdown = append-only log / source of
  truth, the DB = a materialized **read-model projection**; authority one-directional (MD→projection);
  Markdown-first **write-through**; **rebuildable + disposable**; **atomic build/swap** with a
  build-in-progress self-heal; **cheap warm start** via `git diff <watermark>..HEAD` + dirty tree (NO
  whole-corpus scan); **git-rooted historical-integrity** (a historical file changed after its introducing
  `Memory-Entry` commit = append-only violation flagged by `links check`/`esr`; git is recovery), with
  **honest no-git degradation**; **cold-start** on large histories via background/progressive build.
  Hosted/collaborative multi-writer is explicitly OUT OF SCOPE (Constitution §10 tier decision). Plan
  Phase 1 formalizes + extends the EXISTING SQLite cache, not greenfield.
- R: The maintainer asked for a well-designed bridge between performance and the Markdown source of truth
  (Trace compute + large-history startup + out-of-band edits to historical MD). Advisor-reviewed twice; the
  two correctness fixes baked in from the first draft: (a) integrity must be **git-rooted, not
  projection-rooted** — a disposable store can't be its own tamper oracle (rebuild would launder the
  evidence; git + `Memory-Entry` trailers are the durable ledger); (b) warm start is a **git-diff, not a
  corpus hash-scan** (the load-time trap; mtime unreliable on OneDrive). Plus: don't spec the hosted path
  (undecided product); atomic build/swap; freshness-delta and integrity-check are two distinct git reads;
  deletions leave no projection tombstone by design (flag + git are the record).
- A: Bidirectional MD↔DB sync / DB-as-write-buffer (rejected — makes the DB a shadow source of truth,
  violates Invariant #6). Content-hash-per-entry manifest as the tamper baseline (rejected — circular; a
  rebuild launders it; git is the baseline, the manifest only a warm-process / no-git optimization). Spec
  the hosted multi-writer path now (rejected — that is the open commercial-tier question). Greenfield
  projection (rejected — extend the existing rebuildable SQLite cache).
- F: docs/3_Spec/draft/derived-read-model-projection-contract.md (new);
  docs/2_Todo/derived-projection-implementation-plan.md (new, blocked); docs/2_Todo/0_NEXT_STEPS.md (pause
  banner pointer); docs/README.md (snapshot).
- T: `links check` OK (43 files); both docs' internal links resolve; `encoding check` OK; spec 870w / plan
  634w (tight). Design only — no code (development paused); the plan is `blocked_by` the Constitution-first
  pause.
