---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-26
---

## 2026-07-26 01:05 - Graph orphans: edge ceiling, chronological spine, and edge direction in the inspector

```yaml
entry_id: mse_ws2prm8b5ec525ye
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/fix/graph-orphans-and-edge-direction
topics:
  - memory-trace
  - graph
```

### Summary

- Root-caused entries rendering as orphans despite having authored relationships, fixed the dominant
  cause, and reworked how the Graph's Show more selects entries.

### Decisions

#### D1 - Give graph edges their own payload ceiling, separate from nodes

- D: `EDGE_PAYLOAD_CEILING = 6000` governs edges; nodes keep the 1000 ceiling.
- R: The cap was `max(limit, len(nodes))`, so edges were bounded by the node count. Measured on the
  real corpus: 626 entries carry **1231** entry-level lifecycle edges, so 231 were silently cut, and
  **133 of the 215 apparent orphans were pure truncation** — nodes drawn with the relationships that
  justify their presence removed. Node count is what `limit` governs; once a node is in, the edges
  among the selected set are the whole point of drawing it. The intra-slice set is self-bounding by
  node count, so a higher ceiling is not open-ended. Orphans fell 215 → 82.
- A: Raise the shared ceiling — rejected: node and edge counts scale differently, and a slice of N
  nodes holds many times N edges. Leave it and accept sparse maps — rejected: the map was wrong.
- F: `memory-trace/memory_trace/service.py`.

#### D2 - Overview selection is a chronological spine plus one hop, not a connectivity ranking

- D: `_overview_slice` takes the newest `limit` entries as a spine, then expands one hop over the
  lifecycle edges currently rendered, pulling in referenced entries regardless of date. `limit` sizes
  the spine; expansion is additive.
- R: JNL: the Trail must stay purely chronological, and the Graph should lead with the same
  chronological core so the spine is shared, with the Graph adding relationship context for
  visualisation. The old greedy connectivity ranking (highest-degree seeds, frontier by degree) built
  a well-connected map whose membership had no relation to what the user was reading. That ranking
  existed to avoid an edgeless slice, since plain corpus order starts at the OLDEST entries;
  newest-first avoids that by construction, because recent entries are the linked ones. Measured: the
  window recedes monotonically (06-15 → 06-12 → 06-03 → 05-26) as Show more is pressed.
- A: Expand iteratively rather than one hop — rejected: one old entry would drag its whole
  neighbourhood in. Change the Trail too — rejected by JNL; the Trail stays chronological.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/tests/test_service.py`.

#### D3 - Decision-level edges stay out of the entry-level surface; the fix is decision rows

- D: Reverted an attempt to emit entry-level lines for decision-level edges. Recorded the remaining
  9 orphans and the sanctioned fix as a NEXT_STEPS item instead.
- R: `test_decision_edges_never_reach_entry_level_consumers` asserts by set-equality against a
  sidecar-deleted control that the entry-level surface is indistinguishable from a world where
  decision edges were never built. That guard is deliberate and reasoned, so lifting it is a contract
  decision rather than an implementation one — "don't remove guards you don't understand". The real
  fix costs nothing structurally: `_expand_decision_rows` exists and `include_decisions` is Trail-only,
  so having the Graph render decision rows gives these edges a genuine endpoint. It is also step 1 of
  the decision-node-graph sequencing already recorded.
- A: Mark the emitted edges `decision_derived` and keep them — rejected: the guard tests the edge SET,
  not its labelling, and a marker does not make the surface indistinguishable.
- F: `memory-trace/memory_trace/service.py` (comment recording the attempt), `docs/2_Todo/0_NEXT_STEPS.md`.

#### D4 - The inspector separates edge direction, and `Links` counts rendered lines

- D: Replaces / Evolves / Related each get their own row with per-item direction (outgoing marked with
  an arrow and accent border, incoming dashed). `Links` now counts edges rendered for that node under
  the current edge-type filter.
- R: Three independent never-merged edge kinds deserve three rows — "retires", "refines" and "also
  see" are different claims. Direction rides on the item rather than splitting into six rows, since
  incoming and outgoing are one relationship seen from two ends. Surfacing inbound edges at all is
  new: the previous row was outbound-only, so one sample entry's five inbound `evolves` were
  invisible. `Links` previously showed `connectivity`, a **related-only** display weight that drives
  node radius — left untouched server-side so the graph's visual encoding does not silently change.
- A: Redefine `connectivity` server-side — rejected: it would change node sizing, which nobody asked
  for. Six rows split by direction — rejected: buries that they are one tie.
- F: `memory-trace/client/src/App.tsx`, `memory-trace/client/src/styles.css`.
- T: 212 memory-trace tests pass. Verified live: `Links: 5` equals the node's rendered cytoscape
  degree, direction marks render correctly, and the previously-orphaned screenshot entry now has
  degree 6.

## 2026-07-26 01:14 - Reconcile the ADR corpus table from one classifier replayed over historical trees

```yaml
entry_id: mse_qj69fzzvf6y6de93
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/adr-corpus-recount
topics:
  - documentation
  - session-logging
  - graph
```

### Summary

Closed open item 6 in `0_NEXT_STEPS.md` — the ADR corpus table that "does not reconcile" (612 entries on
2026-07-20 against a recount of 580). Pure measurement, nothing built. The committed classifier
`scripts/count_decision_shapes.py` was run against today's tree and, decisively, against the git trees of
the two days that produced the rival figures, which turns "unreproducible" into a named mechanism for each
number. Both rival figures were counting different populations and neither said which; one of them turns
out to be perfectly real. Three documents refreshed so no two of them state a different present-tense
count: the ADR contract, the resolved open question in `decision-level-link-sidecar-refs.md`, and the
NEXT_STEPS item itself.

### Decisions

#### D1 - State the population, and refresh the ADR corpus table from one classifier at a named commit

- D: The corpus section of `adr-lifecycle-sidecar-contract.md` now opens with an explicit **Population**
  paragraph — the stamped-heading splitter `_ENTRY_HEADING_RE` (what `links check` validates against), with
  the 25 date-only May-2026 legacy headings **excluded** — and every figure is tagged with the commit it
  was measured at (`0dc423b`). The table goes 138/383/1/42 -> **186 numbered / 407 singular / 1 inline / 42
  no-decision = 636 entries**, 593 addressable, 163 multi-decision, and the decisions total in the sidecar
  section goes 710 -> **876**.
- R: The item asked for one classifier and a stated population, and the population statement is the part
  that actually prevents recurrence — the whole dispute existed because two correct counts over
  byte-identical files never said which boundary they used. Measured today: 636 stamped entries vs **661**
  under the date-only-tolerant chunk-extractor boundary, a 25-entry gap that will keep generating fake
  disagreements until it is written down. Naming the commit matters for the same reason a version does:
  the corpus grew 537 -> 562 -> 636 over six days, so an undated count is a moving target. Consistency
  check now stated in the doc rather than left implicit: 186 + 407 = 593, exactly what
  `_entry_decision_ordinals` returns, so the table and the shipped addressability cannot drift.
- F: `docs/3_Spec/draft/adr-lifecycle-sidecar-contract.md`.

#### D2 - Reconcile by replaying the classifier over historical git trees, which rehabilitates 580 and condemns 612

- D: Item 6 is marked **RESOLVED 2026-07-26** with a per-figure verdict rather than a single new number:
  **580 is real** — a mid-day 2026-07-21 count under the date-only-tolerant splitter — while **612 is
  inflated** and reproduces under neither splitter. This corrects the 2026-07-22 amendment's claim that 580
  "reproduces under neither splitter", which is why that note is superseded in
  `decision-level-link-sidecar-refs.md` rather than left standing.
- R: Extracting `.memory-seed` at `7d39214` (end of 07-20) and `0f69e48` (end of 07-21) and running today's
  classifier over them is what converts assertion into evidence, and it was cheap. At the 07-20 tree the
  classifier finds **537 stamped / 562 tolerant**, not 612; the hand count's numbered (125) and inline (1)
  are exactly right and the entire error sits in its no-decision bucket, **140 claimed against 42 stamped /
  67 tolerant** — consistent with a `sessions/**/*.md` sweep absorbing the **99** decision-less date
  headings the `links/` (72) and `diagrams/` (27) sidecars held that day. The 580 recount identifies its
  own splitter unambiguously: its no-decision bucket of **66** matches the tolerant splitter's **67**, not
  the stamped **42**, and its numbered (131) and singular (382) sit between the end-of-20th and end-of-21st
  measurements (125 -> 136, 369 -> 383), placing it mid-day on the 21st at a tolerant total of 581. So the
  premise that made this look impossible — a fall under an append-only corpus — dissolves: there was no
  fall, only an inflated figure. Two frozen buckets corroborate the classifier rather than the corpus
  moving under it: **no-decision is 42 at all three snapshots and the legacy date-only heading count is 25
  at all three**, which are precisely the buckets append-only growth cannot move, so the whole +99 is
  genuine growth. Safe now to size decision-coverage work from these.
- A: Reporting a fresh number alone and declaring both prior figures superseded — rejected: it is what the
  2026-07-22 pass did, and it left a wrong claim about 580 in two documents plus an item that stayed open
  for four days. Rebuilding a bespoke classifier — rejected: the committed script is the agreed one, and
  re-deriving by hand is the original failure mode.
- F: `docs/2_Todo/0_NEXT_STEPS.md` (item 6 resolved),
  `docs/3_Spec/draft/decision-level-link-sidecar-refs.md` (open question 4 corrected and dated),
  `docs/README.md` (regenerated).
- T: `docs check` clean apart from two pre-existing unrelated lane errors (verified identical on a stashed
  tree); `docs index` regenerated.

## 2026-07-26 01:15 - Close the compact-Mermaid vs arc2d item as stale

```yaml
entry_id: mse_de179b4x508f5pws
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/mermaid-subgraph-item
topics:
  - documentation
  - mermaid
  - memory-trace
  - tooling-evaluation
```

### Summary

Resolved open item 4 in `docs/2_Todo/0_NEXT_STEPS.md` — the claimed `compact_mermaid_diagrams`
vs `arc2d` mismatch. The item was stale as written: it asserts the renderer parses no `subgraph`
"in both clients", but `71cea36` (2026-07-22) deleted `client/src/arc2d.ts` and `DiagramView.tsx`
now renders every sidecar block through real Mermaid. Both React surfaces route through that one
component (`EntryReader.tsx:352` inline, `DiagramViewer.tsx:219` zoom modal), so there is no
second path with a hole. The skill had already been reversed in the same tranche and already
carries the transitional caveat naming the legacy `/` subset parser, and its seed twin is
byte-identical — so the "narrow the skill" remedy was already done and no skill edit was needed.

The one true residue is the vanilla `/` UI, which still ships the hand-written subset renderer
(`static/app.js`, `renderDiagramBlock` at 2588). That is documented rather than silent, and it
retires with the `/` UI under the B2 parity-sign-off gate recorded in Track B of the same file.

Verification also turned up a live trap: `uv run --no-sync memory-seed ...` does not run this
repo's code. See D2.

### Decisions

#### D1 - Close item 4 as stale rather than build a fix

- D: Marked item 4 RESOLVED in `docs/2_Todo/0_NEXT_STEPS.md` with an evidence-backed note, and
  changed no code and no skill. The note states precisely what is resolved (the skill/renderer
  mismatch) versus what remains (the vanilla `/` subset parser), and points the residue at the
  existing B2 parity-sign-off gate with an explicit instruction to delete the skill's caveat
  paragraph when that sign-off lands.
- R: The item's premise had already been overtaken by shipped work, so "extend the renderer" would
  have been a fix for a non-problem and "narrow the skill" was already done. Recording the residue
  honestly matters more than declaring a clean close: a future reader who hits stray boxes in `/`
  must find the note truthful rather than contradicted by the UI. Hanging the residue on an
  already-recorded gate keeps the only pointer to the caveat's eventual deletion from being
  dropped along with the item. The underlying trade is Invariant #6 — the sidecar Markdown is the
  source of truth and VS Code, GitHub and `/next` all run real Mermaid, so three correct renderers
  beat accommodating a retiring fourth.
- F: `docs/2_Todo/0_NEXT_STEPS.md`

#### D2 - Verify through the worktree module, never the `memory-seed` console script

- D: Ran `uv run --no-sync python -X utf8 -m memory_seed.cli docs check|index` instead of
  `uv run --no-sync memory-seed docs ...`, and reverted a `docs/README.md` regeneration produced
  by the latter.
- R: `--no-sync` leaves the worktree `.venv` empty, so the console script falls through PATH to a
  stale globally installed `C:\Users\johnn\.local\bin\memory-seed.EXE`. That binary reported two
  phantom `docs check` errors (`7_Replaced is neither a lane nor allowlisted`,
  `4_Reference/trace-humanised-dashboard-references` off-allowlist) and regenerated `docs/README.md`
  with the `7_Replaced 29` lane count silently dropped — even though both names are present in
  `docs_check.LANES`, `SIDE_FOLDER_ALLOWLIST` and `docs_index.LANES` in this tree. `-m memory_seed.cli`
  resolves the package from cwd, so it runs the code under test; against it `docs check` is clean
  (0 errors, 14 pre-existing warnings) and `docs index` reports already-current. Had the stale
  output been committed it would have landed a real docs-index regression attributable to nothing
  in the diff.
- F: `docs/README.md`, `memory_seed/docs_check.py`, `memory_seed/docs_index.py`

## 2026-07-26 01:22 - Graph decision rows: opt-in granularity, expanded only where an edge lands

```yaml
entry_id: mse_wb6d4gmjpmrj0k67
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/graph-decision-rows
topics:
  - memory-trace
  - graph
```

### Summary

- Gave the Graph a sanctioned route to decision granularity, and measured why the obvious version of
  it is wrong.

### Decisions

#### D1 - Decision rows in the Graph are an opt-in change of granularity, not an entry-level edge

- D: `/api/v1/graph/projection` accepts `include_decisions` (default **false**), and `graph()` gains
  `decision_row_scope`. The entry-level surface is byte-identical when the flag is off.
- R: This is NEXT_STEPS item 8 — the fix for entries whose only relationships are decision-level and
  therefore render as orphans, because an entry-per-node view has no row for `B:d2 evolves A:d1` to
  land on. Emitting an entry-level line instead was tried on 2026-07-26 and reverted against
  `test_decision_edges_never_reach_entry_level_consumers`. Asking for rows is legitimate where
  forging an edge is not: the edge terminates on the decision it actually names, and the claim
  grammar v2 forbids ("B evolves A" as an authored entry-level edge) is still never made.
- A: Make it the default — rejected: it changes every existing consumer's granularity for a 9-entry
  tail. Keep forging entry-level edges behind a marker — rejected: the guard tests the edge SET, so a
  label does not make the surface indistinguishable.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/tests/contract/openapi.v1.json`,
  `memory-trace/tests/contract/types.ts`.

#### D2 - Expansion is filtered by ORDINAL, and the Trail and Graph want different scopes

- D: `decision_row_scope="all"` (Trail) expands every decision of every multi-decision entry;
  `"linked"` (Graph) expands only the ordinals a decision edge terminates on. `_expand_decision_rows`
  takes `only_ordinals` rather than a set of entry ids.
- R: Measured, not stylistic. A timeline row earns its place by existing, so the Trail's rule (and its
  "a document does not skip heading 1" reading) is right there. A force layout is the opposite: a row
  with no edge is simply a new isolate. Expanding the whole corpus produced **446 rows of which 276
  were isolated** — a cure worse than the 9 orphans it set out to fix. Filtering by entry got that to
  110; filtering by **ordinal** gets it to **0**, because a row then exists exactly where a
  relationship lands. A graph is not a document, so the heading rule does not carry over. The group
  anchor keeps the entry id, so entry-level edges still terminate correctly.
- A: Filter by entry — rejected, measured at 110 isolates. Emit rows for all ordinals and hide
  edgeless ones client-side — rejected: the server should not ship nodes it expects the client to
  discard.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/tests/test_trail_decision_edges.py`.
- T: 213 memory-trace tests pass. New test asserts the default stays entry-per-node, that opting in
  produces rows, that **no row is edgeless**, and that opting in never removes an entry node.

### Follow-up

- **Anchors can now float.** Full-corpus orphans go 83 → 94 with rows on: ~11 anchors whose only
  edges were decision-level are now edgeless while their rows carry the relationships. A timeline
  gets parent/child from adjacency; a force graph has no tether between an anchor and its rows.
  Adding one means a **fifth edge kind** (containment), which the four-independent-kinds contract
  makes a deliberate decision rather than an implementation detail. Decide before wiring a client.
- No client wiring shipped: there is no consumer yet, and shipping UI for an undecided anchor
  treatment would bake in the wrong answer.

## 2026-07-26 01:39 - Confidence threshold control for machine-suggested graph edges

```yaml
entry_id: mse_qyp5p2w8z1gtmb37
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/graph-confidence-filter
topics:
  - memory-trace
  - ui-design
```

### Summary

- Shipped the user-facing confidence threshold deferred when `edge_confidence` landed: the graph
  faded low-confidence edges but offered no way to hide them.

### Decision

- D: A three-step control in the graph filter bar — **All / ≥0.7 / ≥0.9** — hiding machine-suggested
  edges below the chosen score. Defaults to All and persists with the other graph settings.
  Human-authored edges are never hidden by it. A filtered edge is also removed from the per-pair
  outranking input, not merely hidden.
- R: The thresholds reuse the tiers the sidecar already stores (`low` <0.7, `review` ≥0.7, `high`
  ≥0.9), so the control filters on exactly the boundaries the graph already fades on rather than
  inventing a second, disagreeing notion of "confident". Defaulting to All follows the same reasoning
  as the Orphans toggle: hiding evidence by default lets a viewing preference quietly decide what the
  corpus appears to contain. Authored edges carry no confidence at all, and the field's contract is
  that absence means "authored, not scored" — never "confidence zero" — so the threshold must not
  touch them. The outranking detail is the subtle one: one line is drawn per PAIR and the most
  consequential relationship wins, so a hidden low-confidence edge that still competed could win its
  pair and blank the visible relationship underneath it.
- A: A continuous slider — rejected: invites thresholds that correspond to nothing the data records.
  Hide low-confidence edges by default — rejected, per the evidence-hiding argument above.
- F: `memory-trace/client/src/GraphWorkspace.tsx`, `memory-trace/client/src/App.tsx`,
  `memory-trace/client/src/SettingsMenu.tsx`, `memory-trace/client/src/styles.css`.
- T: Typecheck clean; verified live on the real corpus — 98 edges (51 scored, 47 authored); ≥0.7
  leaves 81 (hiding exactly the 17 below it), ≥0.9 leaves 60 (hiding the 38 below it), All restores
  98. The 47 authored edges survive every threshold.

### Follow-up

- First attempt filtered nothing: `applyPresentation` read the threshold from a ref, but the
  presentation effect's dependency list did not include it, so nothing re-ran. Caught by the live
  check, not by typecheck — a ref-plus-stale-deps combination is invisible to the compiler.
- **Pre-existing test failure, not caused by this work:** `SettingsMenu.stories.tsx > Keyboard Tab
  Navigation` fails on the client vitest run. Confirmed by stashing all client changes and re-running:
  it fails identically on a clean tree. Needs its own fix.

## 2026-07-26 01:44 - Repair the SettingsMenu keyboard story; CI does not run client vitest

```yaml
entry_id: mse_e7awdxceskrmajdd
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/fix/settings-keyboard-test
topics:
  - memory-trace
  - bugfix
```

### Summary

- Repaired a client test that had been failing on main unnoticed, and recorded why CI never caught it.

### Decision

- D: `SettingsMenu.stories.tsx` meta now supplies a complete `graphSettings` (plus `onGraphSettings`),
  and the keyboard-navigation story traverses Trail → **Graph** → Inspector rather than asserting
  ArrowRight reaches Inspector directly.
- R: Two separate rots, both from the same cause. The tablist gained a **Graph** tab between Trail and
  Inspector, so the story's single ArrowRight no longer landed where it claimed; and landing on Graph
  rendered a panel reading `graphSettings.forces`, which the meta had never supplied. The missing prop
  survived because only a story that actually NAVIGATES to that tab renders the panel that reads it —
  every other story passed while the prop was absent. Keeping the Inspector assertion behind a second
  ArrowRight preserves what the test originally proved (the tablist traverses the whole way) instead
  of quietly narrowing it to the new first tab.
- A: Delete the Inspector assertion and assert only Graph — rejected: it would shrink coverage to
  match the bug. Make `SettingsMenu` defensive about a missing `graphSettings` — rejected: the prop is
  required by its type, so tolerating absence would hide the next fixture that forgets it.
- F: `memory-trace/client/src/SettingsMenu.stories.tsx`.
- T: Client vitest was **1 failed | 6 passed** on a clean tree (verified by stashing all local client
  changes and re-running, so the failure was demonstrably pre-existing rather than mine). Now
  **7 passed**. Typecheck clean.

### Follow-up

- **CI does not run the client vitest suite.** `Verify` has been green throughout while this test was
  red, which is how a broken guard survived. Worth deciding whether the storybook interaction tests
  should join CI — a test nobody runs is a test that silently stops guarding.

## 2026-07-26 01:47 - Split branch: contamination into its decidable and undecidable halves

```yaml
entry_id: mse_eaep9x4t50yesqb4
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/fix/branch-field-contamination
topics:
  - session-logging
  - git-workflow
  - agent-collaboration
  - bugfix
```

### Summary

Investigated NEXT_STEPS item 5 ("cross-session `branch:` contamination") against a synthetic-repository
matrix instead of by reasoning, and it split cleanly in two. The escalation premise turned out not to
hold for this repo, and the item's scope claim was overstated; one genuinely broken layout surfaced that
nobody had named, and that half is fixed.

Two corrections. `branch:` is the **only** git-derived field on an entry — `session_append_entry` makes
exactly one git call; every other key is caller-supplied, `read_local_user` is a file read, the entry id
hashes caller metadata, and diagram/topic/link sidecars carry no branch — so "affects every git-derived
field" shrinks to one field. And worktree isolation is a consequence of **`.memory-seed` being
committed**, not of worktrees: because it is tracked here, each worktree checks out its own copy, the
`resolve_runtime` walk-up stops there, and git reports that worktree's own HEAD. Today's parallel agents
record their branches correctly.

The matrix did expose a case with no concurrency in it at all: the same worktree layout with
`.memory-seed` gitignored. The walk-up escapes into the primary, so the entry is written into the
primary's memory dir *and* stamped with the primary's HEAD. Fixed by omission. What remains — two agents
in one checkout — is undecidable from git and is written up with options rather than guessed at.

### Decisions

#### D1 - Omit `branch:` when the memory dir belongs to another working tree

- D: When `rev-parse --show-toplevel` for the caller's directory differs from the same for
  `runtime.workspace_root`, omit `branch:` rather than stamping the enclosing checkout's HEAD. Extends the
  existing detached-HEAD / not-a-repository omission rule by one clause via a new `_auto_captured_branch`
  helper; no new flag, no advisory channel, no new machinery.
- R: This layout produces a wrong durable value with **no concurrency at all**, so the task's own
  principle decides it without a judgement call — a wrong `branch:` is append-only history, and refusing
  beats recording. Omission specifically, because *neither* HEAD is right there: the session is on the
  worktree's branch while the entry file lands in the primary's `.memory-seed` and will be committed on
  the primary's branch. The detector is exact rather than heuristic — the two toplevels agree in every
  layout that behaves correctly and disagree precisely when the write crosses trees — so it carries no
  false positives. Shipped rather than proposed because `memory-seed` is on PyPI: this repo tracks
  `.memory-seed` so the rule never fires here, but any downstream user who gitignores it gets silently
  wrong branches in every worktree.
- A: Also omits for a caller inside a submodule whose superproject owns the memory dir. Judged correct —
  the submodule's branch is not the superproject's, and the entry commits on the superproject's. Costs one
  extra `rev-parse` per append, which is a once-per-entry path, not a hot loop.
- F: `memory_seed/core.py` `_auto_captured_branch` plus the `session_append_entry` docstring;
  `tests/test_session_append.py::BranchProvenanceTests` pins the whole matrix. Full suite 743 passed;
  `links check` and `docs check` both OK.
- T: `.memory-seed` being tracked is load-bearing for worktree isolation and nothing states that anywhere
  — worth asserting in a test over this repo's own layout if it is ever to be relied on.

#### D2 - Leave the shared-checkout half to a maintainer decision rather than a heuristic

- D: Do not implement a warning for two agents sharing one checkout. Record the finding and four options
  in `docs/2_Todo/branch-field-provenance.md`, mark item 5 as needing JNL's decision, and recommend (not
  decide) option A plus D: document the existing `--branch`/`--no-branch` workaround, and have the agent
  harness pass `--branch` unconditionally.
- R: Two agents in one working tree have a genuinely identical HEAD, and an agent's session branch is
  never passed to the CLI — there is no state from which any code could recover it. The only implementable
  signal, "this repo has more than one worktree", would fire on every legitimate primary-checkout append
  here, which is now routine; that trains the warning to be ignored within a week. Inventing a heuristic
  the maintainer did not ask for would be guessing at a policy question, and the task was explicit that a
  judgement call belongs in a proposal.
- A: Assumes agents keep honouring `--branch` by convention rather than enforcement; option C (session
  locks) stays available if that proves too weak.
- F: `docs/2_Todo/branch-field-provenance.md` (matrix, options A-D, blast radius); item 5 of
  `docs/2_Todo/0_NEXT_STEPS.md` rewritten with the empirical finding;
  `test_shared_checkout_concurrency_is_still_invisible` pins the gap so D1's rule is not mistaken for a
  complete answer.
- T: The item was escalated as urgent on the belief that parallel worktree agents were actively recording
  wrong branches. They are not. Reasoning about git behaviour would have confirmed the escalation; the
  synthetic matrix contradicted it and found a different bug instead.

## 2026-07-26 01:49 - Run the Storybook interaction tests in CI

```yaml
entry_id: mse_kwm61z11k5336fq4
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/ci/client-story-tests
topics:
  - memory-trace
  - tooling-evaluation
```

### Summary

- Put the Storybook interaction tests into CI, closing the gap that let a red test survive on main.

### Decision

- D: Added a `test:stories` npm script (`vitest run`) and two steps to the Verify workflow's React job
  — install headless chromium via Playwright, then run the suite.
- R: The suite had **no npm script and no CI step**, so it ran only when someone typed `npx vitest run`
  by hand. That is precisely how the `SettingsMenu` keyboard story sat failing on main while Verify
  reported green: a Graph tab was inserted into the tablist, the story's fixture never gained the prop
  that tab's panel reads, and nothing in the pipeline noticed. A test nobody runs has stopped being a
  guard. The browser binary must be fetched explicitly because `npm ci` installs
  `@vitest/browser-playwright` but not chromium, and `vite.config.ts` pins the suite to a headless
  chromium browser project.
- A: Add the script but leave CI alone and record the decision for the maintainer — rejected: it would
  have left the guard exactly as unenforced as it was, which is the actual defect. Convert the
  storybook tests to jsdom to avoid the browser download — rejected: it would change what the tests
  exercise (real layout and focus behaviour is the point of an interaction test) to save under a
  minute of CI.
- F: `memory-trace/client/package.json`, `.github/workflows/verify.yml`.
- T: `npm run test:stories` passes locally (7 passed). Workflow YAML parses. CI itself is the real
  proof — this is the first run that exercises the new steps, so the push is being watched.

### Follow-up

- Cost is one chromium download per Verify run. If that proves slow enough to matter, the standard
  remedy is caching `~/.cache/ms-playwright` keyed on the lockfile rather than dropping the step.

## 2026-07-26 01:55 - Size graph nodes by degree centrality

```yaml
entry_id: mse_r0r8kpbcj5meb2rm
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/graph-degree-prominence
topics:
  - memory-trace
  - graph
```

### Summary

- Node prominence now follows degree centrality over the whole payload rather than a related-only
  weight — the first increment of NEXT_STEPS item 7.

### Decision

- D: Node size is `22 + min(20, sqrt(degree) * 5)`, where degree counts every edge kind present in the
  graph payload. Position is untouched. `connectivity` stays on the node as the server computes it.
- R: Item 7 called for degree to drive visual weight, and the note added to it on 2026-07-26 warned
  that the input must be chosen deliberately, because `connectivity` and the inspector's `Links` had
  become different numbers. `connectivity` is a **related-only** display weight, so an entry whose
  ties are mostly `evolves` drew small while a chattier but less consequential one drew large — the
  encoding disagreed with the map. Reading the PAYLOAD's edges rather than the visible set keeps
  sizing stable when an edge filter is toggled: filtering should restyle lines, not resize every node
  beneath them. Scaling is sqrt because degree is heavy-tailed — a linear ramp spends its range on a
  few hubs and flattens everything else — and the cap stops one outlier dominating the canvas.
- A: Compute it server-side and add a node field — rejected for now: it is a pure function of edges
  the payload already carries, so a contract addition would buy nothing until betweenness or PageRank
  (which are not local) arrive. Size by the VISIBLE edge set — rejected: node sizes would jump on
  every filter toggle.
- F: `memory-trace/client/src/GraphWorkspace.tsx`.
- T: Typecheck clean; story tests 7 passed. Verified live on the real corpus — 98 nodes resolve to 9
  distinct sizes across 22–36, with `deg=8 → 36.1` and `deg=0 → 22.0`, confirming the ramp is legible
  rather than collapsing to two buckets.

### Follow-up

- Betweenness and PageRank remain unbuilt. Both are global rather than local computations, so unlike
  degree they are a genuine argument for computing server-side and adding a node field.
- The cap (20) is not reached on the default slice, whose maximum degree is 8. It will bind on the
  full corpus, which is the intent, but the exact cap is untested at that scale.

## 2026-07-26 02:25 - Author the pilot-gated topic_swarm backfill skill

```yaml
entry_id: mse_31m31wsrwe77jh78
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/topic-swarm-skill
topics:
  - control-plane
  - session-logging
  - graph
```

### Summary

Authored `topic_swarm.md`, the skill that will run the decision-level topic backfill, and registered it
across the whole skill surface (live + byte-identical seed twin, both trigger registries, the `curation`
profile, `SEED_FILES`, `SKILL_DESCRIPTIONS`, `pyproject` package-data, the install-list test, the runtime
`index.md`, and CHANGELOG). No swarm was run and zero topic sidecars exist — the backfill stays gated.
Re-measuring the scope against the live corpus while writing the skill turned up a unit error in the
draft spec, which changed what the campaign is actually counting.

### Decisions

#### D1 - The judgment unit is the addressable ordinal, not the decision count

- D: The topic-backfill scope is **922 judgment units** — 888 addressable decision ordinals plus 34
  zero-decision entries — across 635 entries, and the swarm enumerates with `entry_body_decisions()`,
  never by counting `- D:` bullets. The draft spec's 881/621 is superseded for two independent reasons,
  both stated in the skill so a reader does not re-derive the wrong figure: corpus growth (621 to 635
  entries) and this unit correction.
- R: `dangling-topic-decision` validates `dN` against `_entry_decision_ordinals` (`#### Dn - name`
  headings, else a singular `### Decision` reading as `d1`), while `entry_body_decision_count` falls back
  to `- D:` / `- Dn:` bullets. Measured on the live corpus the two disagree on 10 entries — 906 bullets
  vs 888 addressable. An old-style entry with one `### Decision` heading and inline `- D1:`/`- D2:`
  bullets has exactly ONE addressable ordinal, so a swarm that enumerated by bullet would emit `slug:d2`
  on it and hard-error at write time. Scope also stays the FULL corpus rather than the 206-entry
  topicless tail: 648 of the 888 ordinals sit inside already-topiced entries, which carry entry-level
  topics but no per-decision attribution — the attribution is the product.
- A: Quote the spec's 881 unchanged — rejected: it is now wrong on both axes and the failure mode is
  silent hard errors mid-campaign. Cite only the growth delta (881 to 888) — rejected: it reads as drift
  and invites a reader to re-derive from bullet counts.
- F: `.memory-seed/skills/topic_swarm.md` + seed twin, `.memory-seed/index.md`, `CHANGELOG.md`.
- T: Counts re-measured with `extract_memory_chunks` + `entry_body_decisions`; `links check` reports
  `Session memory integrity OK (91 files)`.

#### D2 - The pilot gate is two-legged, and Leg B benchmarks against free inheritance

- D: The pilot is 20 blind-judged entries that already carry good authored topics, stratified 14
  multi-decision + 6 single-decision with a recorded fixed seed. **Leg A** (machine-scored) is macro-
  averaged recall of the authored set after roll-up, canonicalized on both sides: PROCEED at >= 0.70,
  ABORT below 0.55, one re-prompt on a fresh disjoint sample in between. **Leg B** (human-adjudicated) is
  per-decision attribution precision, which must beat inheritance precision by >= 15 points AND clear
  0.75 absolute; not beating inheritance aborts.
- R: The human never recorded per-decision attribution, so agreement with what they wrote can only score
  the rolled-up union — Leg A alone would pass a swarm that gets every slug right and every attribution
  wrong, which is precisely what decision-level topics exist to provide. Leg B fixes that, and
  inheritance (every decision gets its entry's whole topic set) is the right benchmark because it is
  free, is already the proposal's fallback, and has 100% recall by construction, leaving precision as the
  only axis where 922 judgments can earn their cost. The Leg A numbers are anchored on baselines measured
  on this corpus rather than plucked: random-2 scores 0.10, always-top-2 scores 0.32, always-top-3 scores
  0.43, always-top-4 scores 0.52 — so 0.55 is barely above a predictor that reads nothing, and 0.70 beats
  the strongest constant guess by a wide margin. The skill states the gate costs ~150-240 pair judgments
  (~an hour), because a gate nobody sits through is not a gate.
- A: Reuse the prior 28%-exact-match bar — rejected: exact set match punishes legitimate additions (an
  authored list is a floor, not a ceiling) and is not comparable to recall; the skill says so explicitly
  so nobody reads 0.70 as "improved from 28%". Score precision against the authored list as a gate —
  rejected for the same reason, kept as a diagnostic only.
- F: `.memory-seed/skills/topic_swarm.md` + seed twin.
- T: Baselines computed against the live corpus; full backend suite 743 passed, 17 subtests.

#### D3 - Topic sidecars file under the ENTRY date, which reshapes the write and the correction path

- D: The skill states as its sharpest divergence from `link_swarm` that a batch writes one file per
  distinct ENTRY date under `sessions/topics/YYYY-MM/YYYY-MM-DD.md` — a 40-entry batch touches ~40 files
  spread across every month the corpus covers — and that a wrong batch is corrected by appending a block
  with a strictly later heading time to that SAME file, never by editing the published block.
- R: `topic-sidecar-date-mismatch` is error severity (constructed with the default `severity="error"`),
  so the filename is pinned to the entry's own logged date, unlike link sidecars which file under the day
  the edge was authored — the highest-probability copy-paste error when mirroring the sibling. That
  severity also settles the correction path: since every block for an entry is forced into one file, the
  `(heading timestamp, block index)` precedence in `entry_topic_sidecars` means a later FILE can never
  supersede an earlier one, so re-attribution must be a later-timestamped block in place.
- A: File the batch under today's date like the link family — rejected: hard-errors on every entry not
  logged today. Add a `retracts:`-style mechanism for topics — rejected: a topic list is a state replaced
  wholesale, so most-recent-wins already expresses the correction with no new syntax.
- F: `.memory-seed/skills/topic_swarm.md` + seed twin.
- T: Severity and precedence verified by reading `core.py` topic validation and `retrieval.py`
  `entry_topic_sidecars`; `links check` and `docs check` both OK.

#### D4 - Byte parity for the new twin is enforced by a test, because it was not enforced for link_swarm

- D: `topic_swarm.md` was added to the `extracted` dict in `tests/test_session_schema.py` with anchors
  pinned to the skill's load-bearing claims (addressable-ordinal unit, `entry_body_decisions`, the pilot
  gate, `MAX_TOPICS_PER_DECISION = 3`, `topic-sidecar-date-mismatch`) rather than to its measured counts.
- R: That dict IS the existing byte-equality mechanism, and the gap it left was real: `link_swarm.md`'s
  twins are byte-identical today but nothing asserts it — only presence in both trees is enforced by
  `test_universal_registry_entries_have_live_and_seed_skill_files` and the SEED_FILES tests. Anchoring on
  counts instead of claims would have made the test fail every time the skill is honestly re-measured,
  which the skill itself instructs the next agent to do before every campaign.
- A: Add a generic all-skills byte-parity loop — deferred: it would newly constrain files outside this
  change's scope and belongs to a `skill_architecture` pass, flagged rather than smuggled in here.
- F: `tests/test_session_schema.py`, `tests/test_project_lifecycle.py`.
- T: 743 backend tests pass; `cmp` confirms both `topic_swarm.md` and `skills/index.md` twins are
  byte-identical; `skills list` shows `curation: link_swarm.md, topic_swarm.md`.

### Follow-up

- `link_swarm.md` has no byte-parity assertion; its twins happen to match. Worth a generic parity loop
  over `.memory-seed/skills/*.md` vs the seed tree.
- Pre-existing and untouched: `encoding check` flags 3 issues in vendored `memory-trace` JS assets
  (katex/cynefin), and the prepare-commit-msg git hook reports stale.

## 2026-07-26 02:33 - Verify the topic sidecar block shape empirically

```yaml
entry_id: mse_09p094mr0qwm3qjw
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/topic-swarm-skill
topics:
  - control-plane
  - graph
```

### Summary

Follow-up to the `topic_swarm` skill: the sidecar block shape it teaches had been derived by reading the
parser, not by running it, and zero topic sidecars exist in the corpus to copy from. Exercised the block
end to end against a real entry, confirmed the date-mismatch claim the skill leads with, and confirmed
the skill's enumeration recipe agrees with the validator's own ordinal path on every entry. Both probe
files were removed; no topic sidecar is committed.

### Decision

- D: The exact block in `topic_swarm.md` §6 is now empirically verified rather than inferred, and the
  skill says so with the observed output. Also corrected the closing check line: `links check` is the
  only command that validates a topic sidecar; `topics check` validates the vocabulary file and
  entry-**authored** topics and never reads sidecars, so pairing the two read as if both gated the write.
- R: The previous entry's D3 recorded the date-mismatch severity and the precedence rule as "verified by
  reading `core.py` and `retrieval.py`" — but that block is the artifact a future campaign copies 600+
  times, and its frontmatter (`tags: session-log-topics`, `topic_date:`) was pure analogy to the diagram
  and link families, since the only existing example is a unit-test helper that writes no frontmatter at
  all. Running it settled four things at once: `links check` passes on the documented shape;
  `entry_topic_sidecars` returns `(('d1','memory-trace'), ('d1','ui-design'), ('d2','graph'),
  ('d3','bugfix'))` with the roll-up union on `inferred_topics`; `memory-trace:d1` against an authored
  entry-level `memory-trace` correctly does NOT warn, confirming the enrichment inversion; and refiling
  the same block under today's date fails with `topic-sidecar-date-mismatch` at error severity, which is
  the sharpest divergence from `link_swarm` and the highest-probability copy-paste error.
- A: Ship the inferred format and let the first campaign discover any defect — rejected: a wrong block
  would propagate across ~600 writes before anything failed loudly. Add a permanent fixture sidecar to
  the corpus as the worked example — rejected: it would be a real attribution nobody judged, and the
  backfill is deliberately ungated only after the pilot.
- F: `.memory-seed/skills/topic_swarm.md` + byte-identical seed twin.
- T: `links check` clean with the probe present (92 files) and after removal (91 files); the wrong-date
  probe produced exactly `1 error(s)` of kind `topic-sidecar-date-mismatch`; separately, the skill's
  enumeration recipe (`entry_body_decisions` on chunk text) and the validator's path
  (`_walk_entry_bodies` + `_entry_decision_ordinals` over raw file text) agree on every entry — 892
  ordinals each, zero disagreements — so the recipe cannot emit a task the validator rejects as
  `dangling-topic-decision`. Full backend suite 743 passed, 17 subtests; `docs check` OK.

## 2026-07-26 08:55 - Link audit semantic scoring was already shipped; expose the term and stop the silent fallback

```yaml
entry_id: mse_trt2tdzxw9jx6h2a
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/link-audit-semantic-scoring
topics:
  - retrieval
  - documentation
  - performance
```

### Summary

Opened `0_NEXT_STEPS.md` item 3 ("semantic scoring for `link audit` — measured, unbuilt") to build it, and
found it was **already built**: semantic ranking shipped 2026-07-22 in `3deb9c2`, and the spec's
"no all-pairs semantic scan" rationale — the thing the item said "should be retracted when it lands" — was
retracted in that same tranche. The item's own figures (recall@5 61% -> 75% at weight ~120) came from a
pre-ship local scorer; the shipped sweep superseded them at weight 160. So the task became correcting the
record and then fixing what nobody had noticed *was* broken.

Re-measured everything end-to-end through the shipped `audit_link_gaps` (the spec warns a local copy of the
scorer once silently diverged, so nothing here reimplements scoring). On the now-**637**-entry corpus, against
the **133** resolvable author-declared lifecycle pairs, at `top_k=200` so reachability and rank stay separable:
lexical-only recall@5 **56%**, +semantic **74%**; recall@10 **63% -> 82%**; median rank **3 -> 2**. Embedding
637 entries takes **0.17 s**; the cold model load is **~6 s** here, not the 2.9 s on record. recall@10
reproduces the shipped 82% exactly; recall@5 lands 3 points under the shipped 77% on a corpus grown from 544.

That measurement exposed two real defects, both violations of *expose before you rank*. The cosine was folded
invisibly into a field named `file_overlap_score`, and a missing provider degraded to lexical **silently** —
which is not cosmetic, because the semantic term changes the top-5 for **610 of 629** sources (1,501 candidates
hold a top-5 slot only because of it). Demonstrated live: this worktree's venv lacked `model2vec`, and
`link audit` returned byte-identical output to a lexical run with nothing indicating the ranking had changed.

### Decisions

#### D1 - Correct the record instead of rebuilding, and fix the spec claim that was backwards

- D: Marked item 3 **RESOLVED — stale as written**, citing `3deb9c2` and the existing dated retraction, and
  recorded that its figures were *superseded* rather than merely reproduced. Separately corrected the spec's
  "the gain is in *reachability*, not reordering: median rank is 3 either way" — it is the reverse. Added the
  re-measured table and the structural reason: cosine only ever adds to the score of a pair the lexical gate
  already admitted, so it **cannot** make an unreachable target reachable. Reachability is 132/133 either way.
- R: Rebuilding a shipped feature would have been the expensive kind of wrong — and the second-order damage of
  a stale todo is that it invites exactly that. The reachability claim mattered more than a typo because it
  contradicted its own table (a recall@5 moving 59% -> 77% *is* reordering, by definition) and it misdescribes
  where the signal's value lives, which is the sentence a future weight-tuning pass would read first. The
  earlier "true edges surfaced" column was measured at a small `top_k`, which is why it read as reach.
- A: Delete the retraction wholesale — rejected: it was accurate. The precise problem was that "no all-pairs
  semantic scan" is *half* true, so the retraction now states which half survives: an all-pairs cosine is
  computed, and what stays forbidden is cosine deciding candidate *membership*.
- F: `docs/2_Todo/0_NEXT_STEPS.md`, `docs/3_Spec/lifecycle-edge-linking-sidecars.md`.
- T: `links check` and `docs check` both OK (91 and 179 files); only pre-existing warnings.

#### D2 - Keep semantic ranking default-on and add `--no-semantic`, rather than flipping it to opt-in

- D: `link audit` keeps ranking semantically by default; `--no-semantic` (and `semantic_enabled=False`) ranks
  lexically and resolves no provider at all, so no model is loaded. Confirmed the laziness the task required
  is already structural: `Model2VecEmbeddingProvider.__init__` loads nothing and `_load_model2vec_model`
  imports `model2vec` inside the function body, so importing `memory_seed.retrieval` never touches the model.
- R: **This deviates from my instruction to make the signal opt-in, and deliberately.** Opt-in was specified on
  the premise that the feature was unbuilt; it is not, and it is a swept, spec-documented default. Reverting it
  would have discarded ~18 points of recall@5 on no evidence, which is the opposite of what
  *expose before you rank* is for — that principle gates a signal on being proven on real data, and this one
  has been, twice now. What the principle actually demanded here was inspectability (D3), not demotion. The
  genuine cost of the default is the ~6 s cold model load, and that is what `--no-semantic` answers.
- A: Flip the default to lexical as instructed — rejected for the reason above. Also rejected: leaving no flag
  at all, since without it the 6 s load is unavoidable and no operator can A/B the two rankings.
- F: `memory_seed/retrieval.py`, `memory_seed/cli.py`.
- T: A tripwire test asserts `semantic_enabled=False` never calls `resolve_semantic_provider`.

#### D3 - Expose the semantic term and report its absence, instead of folding it into the total

- D: `LinkGapCandidate` gains `lexical_score` and `semantic_score` (raw cosine in `[0,1]` *before* the ×160
  boost, `None` when semantic was off or unavailable); `file_overlap_score` keeps its meaning as the total and
  its misleading name, now documented. `_embed_entries_for_link_audit` returns the provider name and fallback
  reason instead of swallowing them, `audit_link_gaps` fills a `semantic_status` out-dict, and `link audit`
  prints a ranking-provenance line plus the per-candidate cosine, with a `semantic` block in `--json`.
- R: At weight 160 against small idf sums, the semantic term dominates ordering — so hiding it inside a field
  named after file overlap left an operator unable to tell whether a candidate arrived on shared-file evidence
  they could verify or on an opaque embedding. Worse, the silent fallback meant the ranking could change
  wholesale (610/629 sources) with nothing on screen to say so; `search_memory` already surfaces
  `semantic_fallback_reason`, so link audit was the outlier. This is the same contract, applied consistently.
- A: Rename `file_overlap_score` to `total_score` — rejected: the name is wrong but renaming widens the blast
  radius past what this task warrants (a comment plus the two new fields carries the meaning). Additive fields
  keep every existing consumer working.
- F: `memory_seed/retrieval.py`, `memory_seed/cli.py`, `tests/test_link_audit.py`.
- T: Eight new tests in `LinkAuditSemanticExposureTests` (score decomposes into lexical + 160×cosine; reorders
  without changing membership; fallback reason surfaced; CLI/JSON shape). Stub embeddings key on `entry_id`
  rather than a marker word, so the marker cannot leak into title-term scoring. Full backend suite: **751
  passed, 17 subtests**.

## 2026-07-26 09:02 - Seed semantic status before the empty-corpus return; expose the cosine in ESR

```yaml
entry_id: mse_dve8x9msjgpbp1t1
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/link-audit-semantic-scoring
topics:
  - retrieval
  - documentation
```

### Summary

Review pass over the preceding entry's change, which found one real bug of my own making. Also corrects two
counts in that entry: the suite is **752 passed, 17 subtests** (not 751) and the new test class holds **nine**
tests (not eight).

Verified the one genuinely breaking edit in the diff: `_embed_entries_for_link_audit` changed return type from
`dict | None` to a 3-tuple, and it has exactly **one** caller, the one updated alongside it. `audit_link_gaps`
has a caller I had not checked — `esr.py:288`, the end-of-session sweep — which the additive keyword arguments
leave working, and which is now wired for exposure too.

### Decision

- D: Seeded `semantic_status` **before** `audit_link_gaps`' empty-corpus early return; added
  `semantic_score` to `esr_report`'s link-gap candidate dicts; corrected `test_link_audit.py`'s module
  docstring, which still opened with the very claim the previous entry retracted ("without an all-pairs
  semantic scan"); and softened a spec sentence that asserted *why* the older table read as a reachability
  gain into an explicitly-labelled inference.
- R: The status bug is the one that mattered: `if not chunks: return []` fired before the block that fills the
  dict, so on an empty corpus a **default** run reported `requested: False` and the CLI cheerfully printed
  "lexical only (--no-semantic)" for a flag nobody passed — the same class of misreporting this whole change
  set out to remove, reintroduced two commits later. The test docstring mattered for a different reason: it is
  the first thing a reader of that file sees, so leaving the retracted claim there would have re-taught the
  wrong model of the system regardless of what the spec says. On the softened sentence: I never re-ran the
  2026-07-22 method, so attributing its shape to a small `top_k` was an inference presented as a finding, and
  the distinction is the whole point of this file.
- A: Add report-level semantic provenance to `EsrReport` as well — deferred, not rejected: it needs a new
  dataclass field plus renderer changes, and the per-candidate `semantic_score` already lets an ESR reader see
  whether the term was applied at all. Recorded here so it is a known gap rather than an oversight.
- F: `memory_seed/retrieval.py`, `memory_seed/cli.py`, `memory_seed/esr.py`, `tests/test_link_audit.py`,
  `docs/3_Spec/lifecycle-edge-linking-sidecars.md`.
- T: Full backend suite **752 passed, 17 subtests**; `links check` OK (91 files) and `docs check` OK (179
  files), warnings pre-existing. New test asserts the empty-corpus status reports `requested: True` and that
  the CLI does not claim `--no-semantic`.

## 2026-07-26 09:11 - Reconcile the open-threads list with eleven landed workstreams

```yaml
entry_id: mse_1ghkqp2qa8e926nx
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/next-steps-reconcile
topics:
  - memory-seed
  - documentation
```

### Summary

- Reconciled the open-threads list against what eleven workstreams actually landed overnight, so the
  roadmap stops describing a corpus and codebase that no longer exist.

### Decision

- D: Items 7 and 8 rewritten from "unstarted" to what shipped plus what remains; the `edge_confidence`
  follow-ups closed (confidence control shipped) and superseded (the topic backfill is ~922 units
  corpus-wide, not the ~200-entry topicless tail, and the `topic_swarm` skill now owns it).
- R: Two items were mine and no agent could have updated them, so the list still read as though the
  work had not begun. Leaving that is worse than a stale figure: the next reader would either redo
  shipped work or, in item 8's case, wire a client against a contract question nobody has answered.
  Both now state the measurement that drove the design — 276 isolated rows under blanket expansion, 0
  under ordinal filtering — and item 8 names the one decision that blocks a client rather than
  implying it is merely unbuilt.
- A: Mark 7 and 8 simply RESOLVED — rejected: both have real remainders (betweenness/PageRank; the
  anchor-tether contract question), and a false RESOLVED is how the stale items corrected today got
  that way. Rewrite the whole file — rejected: the sections describing earlier tranches are accurate
  and were not touched.
- F: `docs/2_Todo/0_NEXT_STEPS.md`.
- T: `docs check` OK across 179 files; `docs index` already current.

### Follow-up

- Three things are explicitly the maintainer's call and are recorded as such rather than guessed:
  the fifth-edge-kind question that gates a decision-row client; shared-checkout `branch:` provenance
  (`branch-field-provenance.md`); and whether to run the topic swarm at all.
- Two items in this tranche turned out **stale rather than unbuilt** (semantic scoring, the mermaid
  subgraph mismatch) — in both cases the work had shipped days earlier and the item's own figures were
  superseded. Worth a habit: verify an item's premise against the code before implementing it, since
  the roadmap decays faster than the code does.

## 2026-07-26 09:26 - Topology communities: measured over the renderer payload and closed

```yaml
entry_id: mse_rfy7tmwx5198v8pp
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/graph-topology-communities
topics:
  - graph
  - memory-trace
  - ui-design
  - proposal-lifecycle
```

### Summary

Picked up the last open B0b implementation item - "Topology communities (algorithm choice - Louvain
recommended, client-side over the existing bounded projection; designed, not yet built)" at
`0_NEXT_STEPS.md:566` - and found it was not open. `docs/3_Spec/draft/adr-graph-community-detection.md`
had measured community detection against the live corpus on 2026-07-22 and recommends **not
implementing**; the NEXT_STEPS line predates it by a day and was never reconciled. So the deliverable
became evidence plus reconciliation, not code.

The ADR did leave one gap worth measuring. It clustered the **full corpus with derived file and
branch cliques**, and files were the single lever it found that rescued density (degree 2.24 -> 21.3).
The NEXT_STEPS line proposed something different: Louvain **client-side over the renderer payload**,
whose edges are authored-only (`related`/`replaces`/`evolves`; `topic` is off by default per
`api.ts`). Files are never in the payload. Nobody had measured that variant.

Measured it in-process with the route's exact kwargs (`TraceService.graph` -> `project_trace_graph`,
`include_decisions=False` / `decision_row_scope="linked"` included), then Louvain via `networkx` under
`uv run --no-project` - throwaway tooling matching the ADR's, no manifest touched. One snapshot,
corpus at 641 entries, which includes this entry.

| Slice | n | unique pairs | deg/node | isolated | components | giant |
|---|---:|---:|---:|---:|---:|---:|
| Overview default (`limit=60`) | 71 | 31 | 0.87 | 35 | 46 | 12.7% |
| Full corpus | 641 | 1,123 | 3.50 | 95 | 110 | 77.2% |

Louvain, seed pinned, resolution swept 0.02-2.0: k = 46-47 at the default slice at *every* resolution;
k = 110-133 at full corpus. Modularity 0.68-0.997, runtime 2-35ms.

Four findings. First, and this sharpens the ADR rather than repeating it: Louvain never merges across
connected components, so the **floor on k is the component count** - 46 at the default slice, 110 at
full corpus. A 16-slot legend is therefore arithmetically unreachable at any resolution, not merely a
bad partition. The ADR reached k=15-18 only because file cliques collapsed the graph to 5 components
with a 93% giant; strip the file edges, as a client-side implementation must, and that granularity
does not exist in the search space. There is no matched-k comparison left to run.

Second, at the default slice there is nothing to detect at all: 71 nodes carry 31 authored pairs, 35
of them isolated, largest component 12.7%. Louvain returns the component structure. Verified not an
artefact - zero dangling endpoints, zero self-loops, 31 raw edges = 31 unique pairs.

Third - and this is the finding that rules out the client-side variant independently of density -
that sparsity is structural. `_overview_slice` is a **chronological spine**: newest 60 by date plus
depth-1 expansion over the rendered lifecycle types, which is exactly the observed 60 + 11 = 71. It
is deliberately NOT a connectivity ranking; that was removed because its membership bore no relation
to what the user reads in the Trail. So the default payload is a time window, a recent entry's ties
mostly point outside it, and clustering it partitions a week rather than a neighbourhood structure.
Worse for a colour encoding: "Show more" walks the window backwards, so every node's colour would be
recomputed over a different graph on each page. Topic slugs are immune by construction. I had this
backwards in a first draft of the ADR addendum - wrote that the slice was connectivity-ranked and
therefore the densest window available - and corrected it after reading `_overview_slice`.

Fourth, the most plausible way to reopen the ADR has now been tested and failed: since its measurement
the corpus went 565 -> 641 entries and 633 -> 1,123 unique authored pairs, degree 2.24 -> 3.50, a 56%
density increase largely from the 2026-07-25 decision-level edge campaign. Component count still 110;
seed stability at the resolutions where the largest community is not a blob (0.25-2.0) still
0.593-0.737 mean pairwise ARI, i.e. three seeds disagree about a quarter to a half of assignments, so
a pinned seed buys stability against reload but not against growth. "Wait for denser linking" is now
on record as insufficient once.

Wrote the numbers as a dated addendum inside the existing ADR, corrected the NEXT_STEPS line, marked
proposal 4.3's stable-community apparatus not required, and updated the risk-table row that still
promised overlap-based colour reassignment. No client source touched, so no `npm run build` and no
packaged-asset churn. `docs check` 179 files OK, `links check` 92 files OK, backend 752 passed, client
139 passed (warnings in both checks are pre-existing and unrelated; `npx tsc --noEmit` reports 5
pre-existing `vite.config.ts` errors from absent `node_modules/@types` in this worktree, and
`test:stories` was skipped deliberately - docs-only diff, needs a browser).

### Decision

- D: Do not implement topology communities, in any mode - not as the default colour, and not as an
  opt-in "Colour by: Topology" second mode either. Record the client-side-over-the-payload measurement
  as an addendum to the existing ADR and reconcile the three stale doc claims, rather than writing a
  new proposal.
- R: The guard on this work was that node colour currently means "authored topic community", so
  Louvain would change what a colour MEANS and would need to be a distinct explicitly-chosen mode with
  a legend that follows the active mode. Measurement removes that design question rather than
  answering it: on the payload's authored-only edges the component count floors k at 46/110, so the
  mode would show either 46+ legend rows or, at resolutions that shrink the largest community, 110+
  communities. It is not a lens that works badly; it is not a colour encoding. Honest-but-useless is
  still useless. A second proposal doc re-arguing a settled question is sprawl - the ADR is more
  thorough than one session would produce, and the repo has just been through a next-steps
  reconciliation pass. The one route that could overturn this stays named and unowned: Leiden needs
  `leidenalg`/`igraph`, a new runtime dependency, which is a maintainer call and not something to slip
  in under a UI slice.
- A: Left the ADR at `status: draft`. Promoting draft -> accepted is the maintainer's decision, and it
  is now the only thing between this thread and closed. Also deviated from the literal instruction to
  write a proposal in `docs/2_Todo/`, on the grounds above - surfaced rather than silent, so it is
  cheap to disagree with.
- F: docs/3_Spec/draft/adr-graph-community-detection.md, docs/2_Todo/0_NEXT_STEPS.md,
  docs/2_Todo/memory-trace-graph-visualisation-and-temporal-topology-proposal.md
- T: graph, memory-trace, ui-design, proposal-lifecycle

## 2026-07-26 09:51 - Constitution v1.5: retire the diagram-repair carve-out, ratify minimal-sufficient-context

```yaml
entry_id: mse_zq69mbr57qnppm36
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/constitution-1-5
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Ratified Constitution v1.5: the v1.4 diagram-repair carve-out is withdrawn, and *minimal but
  sufficient context* is ratified as a §3 `[candidate]` principle.

### Decisions

#### D1 - Retire the v1.4 in-place diagram-repair exception

- D: Invariant #2's v1.4 carve-out permitting in-place repair of an unrenderable diagram sidecar is
  **withdrawn**. Invariant #2 now applies to diagram sidecars without exception. The 1.4 amendment-log
  row stays.
- R: The exception existed solely because there was no append path — diagram blocks were keyed by
  `entry_id` alone, so a second block blocked the fuse and editing the published one was the only
  route to a diagram that renders. Blocks now key on `(entry_id, heading timestamp)`, so a repair is an
  appended block that supersedes while the original stays readable as authored, which is what the
  invariant asks for in the first place. So the carve-out is **unnecessary rather than dormant**, and a
  carve-out that buys no capability is a standing invitation to edit history. Hardening, so 1.x per the
  v1.1–v1.4 precedent for narrowing under a minor bump.
- A: Leave it dormant and unused — rejected: an unused exception is still a precedent a future
  proposal can lean on, and one already did (the editable-lens proposal cited v1.4 as its narrow half).
  Repeal the log row too — rejected: the exception was real while it existed and one repair landed
  under it; Invariant #2 applies to the Constitution's own history.
- F: `docs/CONSTITUTION.md`, `docs/3_Spec/draft/sidecar-supersession-model.md`,
  `docs/2_Todo/sidecar-editable-lens-refinement-proposal.md`.

#### D2 - Ratify *minimal but sufficient context* retroactively

- D: §3 gains **minimal but sufficient context** as a `[candidate]` principle: retrieval provides the
  smallest context that preserves the ability to decide.
- R: It was written into §3 on 2026-07-25 **without** an amendment, which §11 requires for a core
  principle. Ratifying it corrects that rather than leaving an unratified clause standing — an
  unapproved principle in the Constitution is precisely the silent override §11 forbids, and it would
  have been cited as law by the next reader. Substantively it earns its place: the inbox crosswalk
  found it a genuine gap (A6-5, "not present at any layer") and the information-theoretic disposition
  re-derived it independently. Stays `[candidate]` because no retrieval surface enforces or measures it
  yet; it graduates when the constrained-context gold set exists.
- A: Silently leave it in §3 — rejected, per the above. Remove it and re-propose later — rejected: the
  claim is sound and twice-evidenced; the defect was procedural, not substantive.
- F: `docs/CONSTITUTION.md`.
- T: 752 backend tests pass; `docs check` OK across 179 files. No test pins the Constitution version, so
  the bump is prose-only — verified rather than assumed.

### Follow-up

- Two previously-blocked decisions arrived from JNL alongside this: anchor↔decision-row tethering takes
  the **non-edge structural channel** (option b — a `parent` field rendered as a compound node, leaving
  the four-kind edge contract untouched), and shared-checkout `branch:` takes **A now, D as standing
  convention**. Both unblock work recorded as blocked in `0_NEXT_STEPS.md`.

## 2026-07-26 10:03 - Adopt the shared-checkout branch: policy -- workaround in session_logging, convention in agent_collaboration

```yaml
entry_id: mse_j23dvr3zehz8g7xh
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/branch-provenance-policy
topics:
  - session-logging
  - agent-collaboration
  - control-plane
evolves:
  - mse_eaep9x4t50yesqb4:d2
```

### Summary

- JNL decided the open half of the `branch:` provenance item: **A now, D as the standing convention**.
  This turn made the decision real — wrote both halves into the control plane, synced the seed twins,
  and flipped `branch-field-provenance.md` and `0_NEXT_STEPS.md` item 5 from proposed to decided.

### Decision

- D: Document the `--branch`/`--no-branch` workaround (A) in `.memory-seed/skills/session_logging.md`,
  appended to the prose that already defines the `branch` field, and cross-reference it from
  `README.md`'s `session append` command entry; record the standing convention (D) — a harness passes
  `--branch` unconditionally, sourced from the Task Packet's `working_branch` — as a bullet under
  "Branch And Worktree Defaults" in `.memory-seed/skills/agent_collaboration.md`. Leave
  `.memory-seed/agent-rules.md` untouched.
- R: Two files, chosen so an agent and a human each hit the guidance on the path they already walk,
  cross-referencing rather than duplicating prose. `session_logging.md` is where `agent-rules.md`
  routes for the End Of Turn append, so every agent writing an entry loads it, and it is the canonical
  definition of the very field the caveat qualifies; `agent_collaboration.md`'s branch/worktree section
  is loaded for exactly the work that creates the hazard and sits beside the worktree-identity rules
  that share the failure mode. The README clause fixes a real gap — `--branch` and `--no-branch` were
  undocumented there entirely — without restating the reasoning.
- A: `.memory-seed/agent-rules.md` was rejected for D: its ~260-line startup budget is documented as
  exactly full, and the repo's own skill-architecture rule puts procedural detail in a lazily loaded
  skill rather than the non-deferrable startup contract. `.memory-seed/policy.md` was rejected for A:
  it is scoped to behavioral constraints only and has **no seed twin**, so a downstream `init`/`update`
  user would never receive the guidance. Options B (warn on multi-worktree repos) and C (lock/marker
  enforcement) were closed rather than left dangling — B fires on every legitimate primary-checkout
  append, C needs session-identity state that does not exist. No code change was attempted: two
  sessions in one working tree have a genuinely identical HEAD and a session's branch is never passed
  to the CLI, so this is policy about an unknowable value, and
  `test_shared_checkout_concurrency_is_still_invisible` was deliberately left intact as the pin that
  the shipped omission rule is not a complete answer.
- F: `.memory-seed/skills/session_logging.md`, `.memory-seed/skills/agent_collaboration.md`,
  `memory_seed/seed/.memory-seed/skills/session_logging.md`,
  `memory_seed/seed/.memory-seed/skills/agent_collaboration.md`, `README.md`,
  `docs/2_Todo/branch-field-provenance.md` (status `active` -> `resolved`, recommendation replaced by
  the decision plus a "where the decision is written down" section),
  `docs/2_Todo/0_NEXT_STEPS.md` (item 5 only). Both skill edits were written generically — no
  dependence on this repo's tracked-`.memory-seed` layout — because they ship downstream.
- T: `cmp` confirms both seed twins byte-identical to their live counterparts. Full backend suite
  752 passed, 17 subtests passed. `links check` exit 0 (Session memory integrity OK, 92 files;
  warnings pre-existing). `docs check` exit 0 (Docs lifecycle OK, 179 files; 14 pre-existing
  warnings, and `branch-field-provenance.md` dropped out of the `missing-todo-yaml` set now that it
  is resolved). Targeted seed-parity / skill-architecture / `BranchProvenanceTests` selection:
  53 passed.

## 2026-07-26 10:12 - Topic-swarm pilot run 1: Leg A 0.583, re-prompt band, gate still open

```yaml
entry_id: mse_s2v8qjc51hx0zp65
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/pilot-run1-record
topics:
  - memory-seed
  - tooling-evaluation
```

### Summary

- Topic-swarm pilot run 1 scored **0.583** on Leg A — inside the re-prompt band, so the gate is neither
  passed nor failed. Logged here because the measuring agent died to an API 529 before it could record
  the result itself.

### Decision

- D: Record run 1 as measured and leave the gate **open**. No topic sidecars written; no threshold
  moved. Run 2 (fresh disjoint sample) is owed before the gate resolves either way.
- R: The number is the deliverable, and it must survive the agent that produced it — the agent
  recognised this and made an *empty commit* carrying the measurement before attempting the re-prompt,
  which is why nothing was lost when it died mid-run-2. The merge carried no session entry, so without
  this the Trail would show the work on the trunk lane with the result visible only in a commit
  message.
  **Leg A macro-recall 0.583** (diagnostic precision 0.521) against a pass line of PROCEED ≥ 0.70 /
  ABORT < 0.55 — the 0.55–0.70 re-prompt band. Worth reading against the baselines computed on the
  same 20: random-2 0.087, top-2 0.217, top-3 0.362, top-4 0.404. So the swarm clearly beats the
  strongest free constant guess, but does not clear the bar set for spending 933 judgments.
  The **validator behaved exactly as designed**, which is the other useful result: 88 slugs proposed,
  83 survived, and the only drop reason was quote-not-grounded (5). Zero non-canonical or alias slugs,
  zero dangling ordinals, zero over-cap blocks — the mechanical gate caught nothing it should not have,
  and the failures were all groundedness rather than grammar.
- A: Nudge the threshold to make 0.583 pass — rejected outright; the skill forbids it and a gate that
  moves is not a gate. Proceed to the full corpus on a band result — rejected for the same reason.
  Abort now — rejected: 0.583 is above the ABORT line, and the skill grants exactly one re-prompt.
- F: pilot harness and measurement commits on `claude/feature/topic-swarm-pilot` (merged); no corpus
  files touched.
- T: 752 backend tests pass. `.memory-seed/sessions/topics/` still does not exist — the "write zero
  sidecars" boundary held under a mid-run crash, which is the stronger form of that guarantee.

### Follow-up

- **Run 2 is owed**: fresh disjoint sample of 20, same seed discipline, threshold untouched. A second
  band-or-below result aborts the backfill per the skill.
- Corpus re-measured during the pilot and has moved again since the skill's snapshot: 641 entries, 899
  addressable ordinals, 933 judgment units, 659 of those inside already-topiced entries. The skill
  already instructs re-measurement, so no figure needs chasing.

## 2026-07-26 13:30 - Topic-swarm pilot run 2 aborts the decision-level backfill at Leg A 0.613

```yaml
entry_id: mse_gymjttp497nvahnw
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/topic-swarm-pilot-run2
topics:
  - memory-seed
  - tooling-evaluation
  - proposal-lifecycle
```

### Summary

- Topic-swarm pilot **run 2** — the one re-prompt the skill grants — scored Leg A macro-recall
  **0.613** against `PROCEED ≥ 0.70`. Both runs are now spent, so the decision-level topic
  **backfill is ABORTED**. Zero topic sidecars written; `.memory-seed/sessions/topics/` still
  does not exist.

### Decision

- D: **Abort the ~933-unit decision-level topic backfill** on a measured Leg A of **0.613**
  (diagnostic precision 0.587), and record the abort in `topic_swarm.md` + its byte-identical
  seed twin and in `0_NEXT_STEPS.md` so nobody re-runs the pilot blind. The threshold was not
  moved, softened, or re-read.
- R: Run 1 scored 0.583 and landed in the 0.55–0.70 band, which buys exactly one re-prompt; a
  second band-or-below result aborts. Run 2 was a genuine re-prompt on genuinely fresh data:
  **disjointness was structural, not seed luck** — the harness filters `--exclude` out of the
  pool *before* `rng.sample`, the exclusion list was rebuilt from run 1's own `draw.json`
  (asserted 14 multi + 6 single = 20) plus `mse_s2v8qjc51hx0zp65`, which *is* run 1's session
  record and would have been reflexive to judge, and `|run1 ∩ run2| = 0` was asserted after the
  draw (seed 20260726002, pools 114/239, 45 units — same count as run 1).
  The brief was rewritten from a **measured** diagnosis, not a hunch: run 1's recall loss was
  63% *area*-slug misses (`memory-seed` 5, `graph` 4 = 47% of all loss) and only 24 of 45 units
  emitted the intended one-area + one-activity shape, **10 emitting no area at all**; budget was
  not the problem (1.96 slugs/unit, union 3.40 vs authored 2.45, precision ≈ recall = substitution
  not under-generation). So v2 **re-allocated the same budget** — the two axes became an output
  contract with both failure shapes named, an area table with per-slug applicability replaced
  "the area or subsystem", `memory-seed` was defined as the residual core area *in the
  vocabulary's own words*, brakes went on the two over-called activities (`bugfix` 6× spurious,
  `documentation` 4×), the quote rule became a recipe, and two worked examples were added using
  low-frequency slug pairs so they teach shape rather than the popular slugs. The diagnosis
  section was **fenced out of the worker copy** — telling a worker which slug run 1 missed is
  frequency information dressed as guidance, i.e. fitting to the gate.
  **The revision worked on everything except the number.** 45 of 45 units held the axis shape
  (run 1: 24), spurious slugs fell 38 → 27, quote drops 5 → 2, precision rose 0.521 → 0.587 —
  and recall rose only 0.030, which at n=20 (SE ≈ ±0.065) is noise. The residual misses are not
  prompt-fixable: `graph` is both a subsystem and a subject, so the three entries authored
  `graph`+`memory-trace`+`ui-design` lose `graph` to a defensible `memory-trace` read; and one
  area per decision structurally caps the rolled-up union at one area slug while authors write
  two or three, so the rule that maximises per-decision precision fights the metric Leg A scores.
  That last point is a real limitation of Leg A as a gate and is recorded as such — **but it is
  not used to reinterpret the result.** The clinching number is the baseline: always-top-4 scores
  **0.500** on run 2's own sample, so the swarm bought **+0.113** for 45 model calls against 933
  for the campaign, and the 0.70 line exists precisely to demand a wide margin over a predictor
  that reads nothing.
- A: Move or soften the threshold so 0.613 passes — rejected; the skill forbids it and a gate that
  moves is not a gate. Argue the ±0.065 standard error makes 0.613 "really" 0.70 — rejected; an
  error bar is a reason not to treat a near-miss as a pass, not a licence to. Spend a second
  re-prompt — rejected; the skill grants one and it is spent, and a third attempt on a fourth
  sample would be fitting to the gate by search. Run the fan-out on a stronger model to lift the
  number — rejected; it would measure the model tier, not the prompt, and would silently re-cost
  933 judgments at a tier nobody budgeted. Adjudicate Leg B anyway — rejected as moot: Leg A is
  the first gate, so the ~150–240-pair hour of human attention was not spent. Delete the skill —
  rejected; the pipeline, validator, block grammar, harness and brief are sound and reusable, and
  only the verdict on spending 933 judgments is settled.
- F: Branch `claude/feature/topic-swarm-pilot-run2`. `scripts/topic_swarm_worker_brief.md` (the
  revised brief, committed **before** the fan-out so the prompt under test could not be edited
  after seeing the number); an empty commit carrying the measurement; STOP banner + "Recorded
  outcome" table in `.memory-seed/skills/topic_swarm.md` and its seed twin (`cmp`-verified
  byte-identical); `docs/2_Todo/0_NEXT_STEPS.md` item 2 flipped to ABORTED with its corpus
  figures corrected (922 → 933 units, 888 → 899 ordinals, 648 → 659 inside topiced entries).
- T: 752 backend tests pass; `links check` and `docs check` both OK (pre-existing warnings only).
  The fan-out was asserted **45/45 complete** before scoring — one worker printed its JSON in
  chat instead of writing the file and was re-dispatched; unasserted, that unit would have left
  the numerator while its entry stayed in the denominator and quietly depressed the result.
  `.memory-seed/sessions/topics/` confirmed absent.

### Follow-up

- **Do not re-run this pilot as-is.** Reviving the campaign needs a changed *premise*: sharpen the
  `area` axis in `topics.yaml` so `graph`-the-subsystem and `graph`-the-subject are separable, or
  gate on per-decision attribution directly (Leg B style) instead of roll-up recall — then re-run
  the pilot from scratch with a fresh pass line.
- Entry-level authored `topics:` plus inheritance remain the fallback, as the proposal's sequencing
  already anticipated.
- The decision-level *consumer* gate in `decision-level-topics-proposal.md` is now untested by any
  campaign; it stands unblocked but also unexercised.

## 2026-07-26 19:03 - Write-time consolidation proposal: same surface, sidecar storage, swarm as sweep

```yaml
entry_id: mse_z7rfq8x5qjfbyzdc
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/write-time-consolidation
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Wrote up JNL's write-time consolidation design: topics and links keep the same authoring surface but
  land in the sidecar, with the swarm demoted to a lagging-field sweep.

### Decision

- D: Proposal recorded at `docs/2_Todo/write-time-sidecar-consolidation-proposal.md`. The gated write
  tool keeps taking `--topics`/`--related`/`--replaces`/`--evolves` and folds them into the entry's
  **sidecar block** instead of its YAML; provenance moves from positional (which file) to an explicit
  `source: write-time|derived` field; the swarm becomes a sweep for omissions rather than a source.
  Recommended write order is **sidecar first**, and the build order is format → ordering → flip the
  write path → sweep.
- R: The design separates *where the author writes* from *where the data lives*, which is what every
  earlier objection was actually about — a second file to remember, a step to forget. Keeping the
  surface identical removes all of that while giving Invariant #6 exactly one owner per field, which
  the status quo does not: measured today, **294 of 645 entries populate both link homes** with no rule
  saying which to use. Topics are the mirror image — 1,023 slugs all in YAML, sidecar empty — so
  consolidating there prevents a split rather than fixing one.
  The atomicity risk has a cheap answer found by reading the write path: `write_text_file` is a plain
  `path.write_text`, so two files means a crash window. The failure modes are **asymmetric** — entry
  first leaves an entry silently missing its topics, indistinguishable from one legitimately
  unlabelled; sidecar first leaves a block naming a non-existent entry, which is *already* an error
  class (`orphan-topic-sidecar`, dangling refs). Ordering alone converts silent loss into a loud,
  already-implemented validation failure, so no new machinery is needed.
- A: Real two-file atomicity via staging-dir rename — rejected: unreliable on Windows, the primary
  platform. Temp-write both then `os.replace` both — rejected: still not atomic across two files, and
  buys little over ordering. Leave the two homes and just write a rule — weaker: it fixes the ambiguity
  but keeps two owners for one field, which is the #6 problem underneath.
- F: `docs/2_Todo/write-time-sidecar-consolidation-proposal.md`, `docs/2_Todo/README.md`,
  `docs/README.md`.
- T: `docs check` OK across 180 files; index regenerated. No code changed — this is a design record.

### Follow-up

- **A correction the proposal forces into the open:** the contract calls entry YAML "what the author
  knew at write time", but in this repo the author is an LLM — every one of those 1,023 slugs and 671
  edges was chosen by an agent, with `user_initials` recording who the session was *for*. The honest
  distinction is **first-hand vs reconstructed**, not human vs machine, and the contract should stop
  implying otherwise.
- That reframing casts doubt on the pilot's abort: Leg A scored the swarm against authored topics as
  if they were ground truth, so 0.613 may be inter-annotator agreement between two LLMs rather than
  accuracy. Nobody has adjudicated who is right when they disagree, and Leg B — which measured the
  campaign's actual value — never ran. Resolving this changes how much weight the sweep deserves; it
  does not block steps 1–3.
- Needs a §4 Policy change plus contract updates. Whether the mutability change (write-time
  declarations become correctable via `retracts:`) touches Invariant #2's *meaning* is a §11 question
  to settle before building, not during.

## 2026-07-26 19:23 - Constitution v1.6: first-hand vs reconstructed provenance; consolidation accepted

```yaml
entry_id: mse_tyj5wy264y84rhnf
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/constitution-1-6
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Accepted the write-time consolidation proposal and ratified Constitution v1.6, which supplies the
  clause the design needs: provenance is first-hand vs reconstructed, declared rather than positional.

### Decisions

#### D1 - Provenance is first-hand vs reconstructed, and must be declared not inferred

- D: Invariant #4 gains a clarifying clause (v1.6). A value recorded when the work was done is
  **first-hand**; one derived afterwards by reading the finished record is **reconstructed**. Both are
  legitimate, they are **not equal evidence**, and which one a value is must be **declared on the
  record, never inferred from where it is stored**.
- R: JNL's observation forced it: this repository's links and topics are written by *agents* at write
  time as well as derived by sweeps afterwards, so the contract's "what the author knew" language had
  been describing **write-time authorship** while reading as **human authorship** — and `user_initials`
  records who a session was *for*, not who chose the values. Every one of the 1,023 YAML topic slugs
  and 671 YAML edges in this corpus was chosen by an agent. Storing provenance *positionally* (entry
  YAML = authored, sidecar = inferred) is the fragile second derivation this clarifies away — the same
  failure `graphCommunities.ts` warns about, where two derivations let a legend quietly lie. The
  inequality is measured, not asserted: a cold sweep scored **0.583 / 0.613** macro-recall against
  first-hand values, which makes reconstruction sound as a **gap-filler where nothing was recorded**
  and weak as a replacement.
- A: Leave the wording alone and let "author" keep meaning both — rejected: it is already misleading
  and would silently justify treating agent-written metadata as human-vouched. Restrict reconstructed
  values instead of ranking them — rejected: the pilot shows they are useful precisely where nothing
  exists, so a ban would forfeit the case they serve. Bump to 2.0 — rejected: clarifying, adds and
  removes no capability, so 1.x per the v1.1–v1.5 precedent.
- F: `docs/CONSTITUTION.md`.

#### D2 - Accept the write-time consolidation proposal

- D: `write-time-sidecar-consolidation-proposal.md` marked **ACCEPTED**. Build order fixed: block
  format + `source` field → sidecar-first write ordering → flip `session append` → branch-scoped sweep.
- R: v1.6 changes the design's standing from *permitted* to *required*: Invariant #6 already allowed a
  sidecar to own a declared field, but #4 now demands the provenance be declared on the record, which
  **is** the `source` field in step 1. It also settles the question the proposal deliberately left
  open — whether making write-time declarations correctable touches Invariant #2's meaning. It does
  not: v1.6 adds and removes no capability, so this is a §4 Policy change, and the ratification is the
  answer rather than a separate ruling.
- A: Accept without the amendment — rejected: the design's core move (declared provenance) would have
  rested on convention, and §11 forbids a proposal silently overriding contract language it
  contradicts.
- F: `docs/2_Todo/write-time-sidecar-consolidation-proposal.md`, `docs/2_Todo/README.md`.
- T: 752 backend tests pass; `docs check` OK across 180 files; index regenerated and current. No test
  pins the Constitution version — verified, not assumed.

### Follow-up

- Step 4's weighting depends on the pilot adjudication now running (does the swarm lose to first-hand
  because it is worse, or because Leg A scored it against another LLM's guess?). Steps 1–3 do not
  depend on it and can proceed.
- The ADR sidecar contract is being brought onto the same pattern in parallel.

## 2026-07-26 19:27 - ADR sidecar contract: first-hand vs reconstructed, with declared provenance

```yaml
entry_id: mse_hj0hh657j693rqzj
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/adr-sidecar-provenance
topics:
  - graph
  - proposal-lifecycle
```

### Summary

Brought the draft ADR lifecycle sidecar contract onto the first-hand / reconstructed pattern JNL
accepted in `write-time-sidecar-consolidation-proposal.md`, corrected one genuinely misleading
write-time-means-human instance in `decision-level-topic-sidecars.md`, and added a scoped pointer to
`sidecar-supersession-model.md`. Alignment pass on a draft only — status stays `draft`, no
`.memory-seed/decisions/` directory, no ADR layer implemented, and no judgment made about whether the
ADR layer gets built at all.

### Decisions

#### D1 - First-hand vs reconstructed, with provenance as a declared field

- D: `adr-lifecycle-sidecar-contract.md` now (1) states the **first-hand vs reconstructed** split
  explicitly — an agent that just did the work versus a sweep reading finished prose — rather than
  implying a human wrote the entry YAML; (2) carries `source: write-time | derived` as a **declared
  field** on the frontmatter and on every transition block, using the consolidation proposal's exact
  vocabulary, with missing or out-of-set values as validation errors and no default; and (3) replaces
  `created_by: jean` with the session entry's own `user_initials` / `agent_type` / `agent_name` trio,
  stating that those record who the work was *for* and are **not** a provenance signal.
- R: Provenance had to become explicit rather than positional because the ADR contract inherits the
  same defect the consolidation proposal names: if "authored" is readable only from which file a value
  sits in, folding write-time values into the sidecar destroys the signal. The human-vs-machine framing
  was factually wrong here — every decision an ADR would promote was written by an agent, with
  `user_initials` recording who the session was *for* — so the contract was asserting a distinction the
  corpus does not contain. The measured asymmetry that does hold is the pilot's 0.583/0.613 macro-recall
  for a cold sweep against write-time output: weak as a source, reasonable as an omission check.

#### D2 - The sweep proposes an ADR; it never writes one

- D: A swarm may **never** create an ADR sidecar or append a transition block. It may propose any
  movement — creation, accept, reject, supersede — as a candidate in the ordinary suggestion channel;
  human approval always commits, and the resulting file records `source: derived` so the reconstructed
  origin survives the approval. A four-row table enumerates every movement so no reader has to infer.
- R: This is deliberately stricter than the topic and link families, and the contract now says why:
  there, a sweep appends `source: derived` blocks directly because a wrong slug is cheap to retract;
  here the existence of the sidecar *is* the promotion, so a written block is already an authority
  claim. Provenance records where a judgment came from and approval records who let it in — two
  questions, and the file answers both rather than collapsing them. The posture is what the owning plan
  already requires ("no automatic ADR promotion", "leave historical records legacy/unclassified unless
  a human explicitly promotes a decision"); the change carries it into the file format instead of
  leaving it as a policy sentence in a different document.
- A: Considered letting a sweep create an ADR file whose only block is `Proposed` with
  `source: derived`, unable to advance without a human transition. Rejected: it keeps the "sweep appends
  to the same place" symmetry with topics and links, but the replaced proposal is explicit that the
  presence of a sidecar is what establishes the ADR, so that path is automatic promotion wearing a
  different label. Also considered leaving `source:` off the ADR family entirely on the grounds that
  every transition already references a first-hand `decision-update` entry — rejected because it makes
  the reconstructed-origin case unrecordable and reintroduces inference. Considered mass-renaming
  "author" to "agent" across the sibling drafts — rejected as churn; most uses correctly contrast
  write-time against sidecar-inferred.
- F: `docs/3_Spec/draft/adr-lifecycle-sidecar-contract.md`,
  `docs/3_Spec/draft/decision-level-topic-sidecars.md`,
  `docs/3_Spec/draft/sidecar-supersession-model.md`.
- T: Backend suite 752 passed / 17 subtests in 181s. `links check` reports
  `Session memory integrity OK` (92 files); `docs check` OK across 180 files. Both leave only
  pre-existing warnings untouched by this change. Docs-only edit, so no runtime behaviour changed.

### Follow-up

- Two documents outside this pass carry the same write-time-means-human wording and were deliberately
  not edited: `docs/4_Reference/INBOX-CAPABILITY-CROSSWALK.md` line 191 ("write-time YAML is canonical
  for what the author knew"), and `docs/3_Spec/lifecycle-edge-linking-sidecars.md`, which the
  consolidation proposal already lists for its own update. `docs/3_Spec/draft/edge-confidence-metadata.md`
  uses "Human authorship" as a confidence factor — arguably correct in substance (it distinguishes a
  directly authored edge from a swarm-suggested one) but mislabelled; it is the first-hand/reconstructed
  distinction under the wrong name.
- **Beyond the wording brief, flagged for review.** `sidecar-supersession-model.md` had **no**
  misleading human-authorship instance — line 71's "human-gated exception" is genuine Constitution v1.4
  governance and was left alone. The paragraph added there instead flags, without settling, that once
  write-time values live in the sidecars the newest-wins sort will order first-hand and reconstructed
  blocks against each other, so a `derived` block could outrank a `write-time` one on recency alone.
  That is the mutability change the consolidation proposal marks for ratification. It is a pointer, not
  a rule, and it is a content addition rather than a wording correction — revert that one paragraph if
  it pre-frames a question that should stay open.

## 2026-07-26 19:33 - Source outranks recency: close the precedence hole in the consolidation design

```yaml
entry_id: mse_x1ha2e26md3q83zv
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/source-precedence
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Closed a precedence hole in the accepted consolidation proposal: `source` outranks recency, so a
  reconstructed block can fill a gap but never supersede a first-hand one.

### Decision

- D: Sidecar precedence becomes `(source rank, then recency)` rather than recency alone. A `derived`
  block may **never** supersede a `write-time` block for the same subject; it may only fill a gap where
  no write-time value exists. Within a source class, most-recent-wins is unchanged.
- R: Found while aligning the ADR contract — an agent flagged that once write-time values live in the
  sidecar, the existing most-recent-wins sort orders first-hand and reconstructed blocks *against each
  other*, so a sweep appending later would supersede the write-time value on recency alone. That
  directly contradicts Constitution v1.6, ratified hours earlier, which holds the two are **not equal
  evidence**. It is also already answered by JNL's stated intent — the sweep exists "for items which
  did not receive them at write time" — so a sweep able to overwrite a first-hand value would be a
  *source*, which the measured 0.583/0.613 reconstruction quality says it should not be. Leaving the
  hole open in an accepted proposal was the worse option: the read path is written against this rule.
  Useful side effect: it bounds the sweep's candidate set to subjects with no write-time value, which
  is what makes it cheap — it never re-judges what the write-time agent already answered.
- A: Leave it as an open question for JNL — rejected: the answer follows from a ratified invariant plus
  an explicit instruction, and an accepted proposal with a hole in its precedence rule invites whoever
  builds step 1 to guess. Allow `derived` to win after human approval — not adopted, but flagged in the
  proposal as the one rule worth correcting before step 1, since a human-approved correction is the
  plausible case where recency *should* win.
- F: `docs/2_Todo/write-time-sidecar-consolidation-proposal.md`, plus the ADR-alignment work merged
  from `claude/docs/adr-sidecar-provenance`.
- T: `docs check` OK (180 files); `links check` OK (92 files). Docs-only.

### Follow-up

- The ADR contract now carries the same `source` field and resolves promotion authority: a swarm may
  propose any ADR movement as a candidate but may never create a sidecar or append a transition block
  — human approval always commits. That asymmetry with the topic and link families is deliberate and
  stated: there a wrong slug is cheap to retract, whereas for an ADR the sidecar's existence *is* the
  promotion.
- Three documents outside the brief still carry "what the author knew" framing and were deliberately
  left: `INBOX-CAPABILITY-CROSSWALK.md`, `lifecycle-edge-linking-sidecars.md` principle 3 (a live spec
  the consolidation proposal already lists for update), and `edge-confidence-metadata.md`, whose
  "Human authorship" confidence factor is substantively right but named for the wrong distinction.

## 2026-07-26 19:36 - Adjudicate the topic-swarm gate: authored topics are not ground truth

```yaml
entry_id: mse_xhkmzgttgtgpe241
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/feature/pilot-adjudication
topics:
  - control-plane
  - tooling-evaluation
  - agent-collaboration
```

### Summary

The topic-swarm pilot aborted on Leg A macro-recall 0.613 against a 0.70 pass line, scoring the
swarm's topics against the **authored** topics as if authored were ground truth. But in this repo
the author of a topic slug is itself an agent at write time, so nobody had ever adjudicated who was
right when the two disagreed. This session tested that premise rather than assuming it, and
separately measured the population Leg A structurally cannot score: the entries that received no
topics at all.

Run 2's verdict files survived and reproduce bit-identically (0.613, 94 raw / 92 surviving slugs,
sole drop reason `quote-not-grounded` x2). The 27 swarm-only slugs match the skill's own recorded
"spurious fell 38 -> 27", which independently confirms provenance. All 80 pooled `(entry, slug)`
candidates across the 20 pilot entries were then marked blind by a third annotator that saw the
decision bodies and a shuffled, unlabelled candidate list, and never saw which side proposed which.

The abort was not moved and the STOP banner in `topic_swarm.md` was not touched. Zero topic
sidecars were written; `.memory-seed/sessions/topics/` does not exist.

### Decision

- D: Record that Leg A's denominator was never adjudicated, so 0.613 was not a measurement of
  accuracy - and that at this margin the gate's verdict is decided by how ONE vocabulary slug,
  `memory-seed`, is read, not by the swarm's competence. Record separately that the swarm's
  demonstrated value sits on the topicless tail, not in competing with first-hand attribution. The
  campaign stays aborted; what changes is the stated reason and what a revival would have to fix
  first.
- R: Blind adjudication of all 80 pooled candidates: AGREED (both proposed) n=33, 29 upheld / 4 not
  (88%); AUTHOR-ONLY - the 20 recall misses Leg A punished - n=20, **10 upheld / 10 not (50%)**;
  SWARM-ONLY - the 27 slugs Leg A charged as precision errors - n=27, **15 upheld / 12 not (56%)**.
  The author's misses were upheld at a coin flip, and the swarm's "spurious" slugs were upheld more
  often than the author's misses. Recomputing Leg A with the swarm verdicts, harness and pass line
  all untouched and only the denominator adjudicated, under **two readings of `memory-seed`**, which
  is 14 of the 80 pooled rows and 7 of the 14 rejected authored slugs:

  | reading of `memory-seed` | (a) upper bound | (b) adjudicated | verdict |
  |---|---|---|---|
  | original, unadjudicated | 0.613 | - | RE-PROMPT BAND |
  | strict package-surface | **0.798** | 0.763 | PROCEED |
  | residual-area (topics.yaml's own wording) | **0.683** | 0.675 | RE-PROMPT BAND |

  `.memory-seed/topics.yaml` defines it as "product behavior **not captured by narrower topics**",
  and the run-2 brief taught workers exactly that; the blind marker was given a stricter
  package-surface rule and rejected it wherever a narrower area covered the work. Under the
  vocabulary's own residual reading the swarm still does not clear 0.70, so **the abort stands on
  its own terms**. Under either reading it clears the 0.55 ABORT floor comfortably and beats the
  unadjudicated 0.613. Separately, on the topicless population - re-derived as **206 of 646
  entries, 240 ordinals, 261 judgment units, 21 bare / 146 single / 39 multi** - a seeded sample
  (20260726004) of 12 entries and 16 units, run through the run-2 brief verbatim with the same
  blind decision-body-only payload, produced **32 raw slugs, 32 surviving, ZERO validator drops**,
  15 of 16 units holding the one-area + one-activity contract, and blind acceptance of **23 of 32
  labels (72%)** with **6 entries accepted as-is, 6 accepted with one edit, 0 rejected**.
- A: The 0.70 line was calibrated to demand a wide margin over a predictor that reads nothing, and
  that reasoning is still sound. What this shows is narrower and more useful than "the gate was
  wrong": at the margin the swarm actually scored, Leg A was measuring an unresolved vocabulary
  question rather than the swarm's judgment. That is the quantified form of a claim the skill
  already makes in prose - "the `area` axis is genuinely ambiguous... no prompt resolves it. The
  vocabulary would have to." Three limits are stated rather than buried. The adjudicator is itself
  an LLM - a third annotator with the entry text in front of it, which breaks a two-way tie but does
  not escape the critique that started this. Variant (c) is a ceiling, not an estimate, because the
  candidate pool contains only slugs one of the two predictors already proposed. And the strict
  reading's 0.798 rests on 19 scorable entries, because one entry had its whole authored set
  rejected. The topicless result is the more decision-relevant half and is not exposed to the
  `memory-seed` question in the same way: there is no authored set to disagree with, and every one
  of 12 entries came out better than unlabelled.
- F: JNL decides whether to rebuild the gate or leave the campaign closed. The premise change a
  revival needs is now specific and is not another prompt rewrite: **disambiguate `memory-seed` in
  `topics.yaml` before re-running anything**, since the gate's verdict currently turns on it. The
  alternative with the strongest evidence behind it is to scope the campaign to the 206 topicless
  entries, where Leg A is undefined, acceptance is the honest metric, and the 261-unit cost is 28%
  of the 933-unit campaign the pilot was gating.
- T: `scripts/topic_swarm_adjudication.py` (new) versions the mechanical half - `sheet`, `join`,
  `rescore`, `topicless`, `validate` - alongside `scripts/topic_swarm_pilot.py`, on the same
  argument that produced that file: a gate that cannot be re-run is not reproducible evidence, and
  neither is an adjudication of one. It regenerates run 2's key byte-identically and reproduces
  0.613 / 0.798 / 0.763 / 0.842. It makes no model calls and writes no sidecars. Backend suite 752
  passed, `links check` and `docs check` both OK with pre-existing warnings only.

## 2026-07-26 19:37 - Derived overrides write-time only via a reviewed retracts

```yaml
entry_id: mse_dqwvb8gxnn88ch3b
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/derived-retracts-rule
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Corrected the precedence rule set hours earlier: a derived value *may* override a write-time one,
  but only through an explicit `retracts:` that a human reviews before it is written.

### Decision

- D: The rule now has two halves. (1) A `derived` block never **implicitly** supersedes a `write-time`
  block — precedence is `(source rank, then recency)`, so a sweep cannot win by being newer. (2) It may
  override one through an explicit `retracts:` naming it, **reviewed by a human before writing**.
- R: My first draft stopped at half 1, which made write-time values permanently uncorrectable. JNL
  corrected it, and the correction is right: a write-time agent *can* be wrong, and a sweep reading the
  finished record sometimes has the better view, so freezing first-hand values would forfeit exactly
  the corrections worth having. What both halves together preserve is that a reconstructed value never
  displaces a first-hand one **silently** — it must state what it retracts, and a person must agree.
  This also reads Constitution v1.6 correctly: "not equal evidence" does not mean "reconstructed is
  never right", it means the burden sits on the reconstructed side — which is exactly what an explicit,
  reviewed retraction expresses. The human gate is not new machinery either; swarm output is already
  suggest-only and human-approved.
- A: Keep half 1 alone (derived can only fill gaps) — rejected by JNL, and rightly: it optimises
  against one failure (silent override) by guaranteeing another (uncorrectable first-hand error).
  Allow implicit override once a human approves the sweep batch — rejected: approval of a batch is not
  approval of a specific displacement, and the read path could not tell the two apart.
- F: `docs/2_Todo/write-time-sidecar-consolidation-proposal.md`,
  `docs/3_Spec/draft/sidecar-supersession-model.md`.
- T: `docs check` OK (180 files); `links check` OK (92 files). Docs-only.

### Follow-up

- **This is not free for topics, and step 1 of the build order now says so.** `retracts:` exists only
  for *links* (built 2026-07-25, with `malformed-retract` / `dangling-retract` /
  `retract-before-declaration`), because a link edge is an independent assertion. Topics deliberately
  have **no** retract construct — they supersede per entry, wholesale, on recency. That rule holds
  *within* a source class but not across them: a `derived` topic block wanting to correct a
  `write-time` one currently has no legal way to say so, and would fall back to the implicit ordering
  half 1 forbids. A retract-shaped mechanism for topics is therefore part of step 1, not a refinement.
- `sidecar-supersession-model.md` previously recorded topics as retract-free as a settled property;
  it now carries the cross-class exception so the two documents cannot drift.

## 2026-07-26 19:54 - Resolve the memory-seed slug ambiguity that was deciding the swarm gate

```yaml
entry_id: mse_xc3f1we7p4808mra
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/topic-vocabulary-residual
topics:
  - memory-seed
  - tooling-evaluation
```

### Summary

- Resolved the `memory-seed` slug ambiguity the pilot adjudication proved was deciding the gate:
  it is now unambiguously the **residual** area.

### Decision

- D: `memory-seed` is relabelled "Memory Seed (residual area)" and redescribed as a RESIDUAL — use only
  when no narrower area applies, **never alongside another area slug** — with the narrower owners named
  explicitly (mcp-tools owns CLI/MCP, retrieval owns search, graph owns edges, session-fuse owns merge
  machinery, session-logging owns authoring, control-plane owns agent runtime files, release owns
  packaging). The slug string is unchanged, so no entry is invalidated.
- R: The old description named a concrete surface *and* a residual in one sentence — "Core package,
  CLI, MCP, seed runtime, and Memory Seed product behavior not captured by narrower topics" — which no
  annotator can apply consistently. That is not a theoretical complaint: of **107 entries using it, 53
  used it as the only area and 54 alongside a narrower one**, and a true residual can never co-occur
  with a narrower area. The corpus split almost exactly in half. A second defect compounded it: the
  concrete half double-named `mcp-tools`' territory, since CLI and MCP appear in both descriptions.
  This mattered beyond tidiness. The pilot adjudication showed the swarm's rescore swings **0.798 →
  0.683 purely on which reading is applied** — one vocabulary slug, 14 of 80 pooled rows, was deciding
  a gate that was supposed to be measuring swarm competence.
  Residual is the right resolution because every concrete surface the old description claimed is
  already owned by a narrower slug, so the "specific" half was nearly empty by subtraction — and a
  vocabulary needs exactly one residual. It also *sharpens* topics.yaml's own existing wording rather
  than reversing it, and it agrees with what the run-2 worker brief already taught.
- A: Make it a specific area and drop the residual clause — rejected: after subtracting mcp-tools,
  retrieval, graph, session-fuse, session-logging, control-plane and release there is little left to
  name, and entries with no narrow home would have nowhere to go. Split into two slugs — rejected:
  the 107 existing uses would divide arbitrarily between them with no way to tell which was meant.
  Leave it and let the swarm brief carry the reading — rejected: a prompt cannot fix a vocabulary that
  contradicts itself, and every other consumer (search filter, facets, community colour) reads the
  same slug.
- F: `.memory-seed/topics.yaml`, `docs/2_Todo/decision-level-topics-proposal.md` (the live record of
  what the swarm prompt teaches).
- T: `topics check` OK — 23 topics, 445 entries, vocabulary parses with the longer description intact
  (verified the 483-char string round-trips through `load_topic_index`). 87 topic/links tests pass;
  `docs check` OK across 180 files. No seed-twin constraint: `memory_seed/seed/.memory-seed/topics.yaml`
  is a generic 5-slug starter, deliberately different from this project's local vocabulary.

### Follow-up

- **This confirms the abort rather than rescuing it.** Under the residual reading the swarm scores
  0.683, still short of the 0.70 gate. Sharpening the vocabulary was worth doing because it stops a
  *future* re-run from measuring an ambiguity instead of the swarm — it does not reopen the campaign.
- The "never alongside another area slug" rule is currently prose, not enforcement. If it becomes a
  check, apply it **at write time only** — 54 historical entries would trip it, and a warning that
  fires on a fifth of the corpus is the trained-to-ignore failure already rejected once in the
  branch-provenance analysis.
- Worth a sweep of the other 22 slugs for the same defect: any description containing "not captured
  by" or naming a surface another slug already owns is the same bug.

## 2026-07-26 20:06 - Topic concentration review: memory-trace and graph are too broad

```yaml
entry_id: mse_r1kqge2jnv7w9mwp
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/topic-concentration-review
topics:
  - memory-seed
  - tooling-evaluation
```

### Summary

- Measured topic concentration across 446 topiced entries and found two over-broad area slugs, with a
  measurably healthy activity axis.

### Decision

- D: Recorded the review at `docs/2_Todo/topic-vocabulary-concentration-review.md`. Recommends carving
  `trail` out of `memory-trace`, and scoping `graph` to the edge data model with a separate slug for
  the graph view. No slug removed, renamed, or retroactively re-tagged. Splits should land **before**
  any topic sweep.
- R: Two defects, both measured rather than asserted. **`memory-trace` sits on 44.2% of entries** and
  **116 carry it as their only area** — a quarter of the corpus described by one undifferentiated
  label, which is the "a label applied to everything distinguishes nothing" principle already written
  into the inferred-topic cap. Sampling those 116 shows at least four subsystems inside the bucket:
  Trail, shell/chrome/layout, search UI, and general app work. That `graph` already exists as a peer
  is the proof the split works — it was carved out of `memory-trace` successfully.
  **`graph` (23.8%) conflates a data model with a view**: 46 uses without `memory-trace` are edge
  semantics in the core package (MCP link tools, the evolves proposal, `evolved_by`, F-file overlap),
  60 uses with it are rendering (git-graph timeline, force layout, relationship lanes). Two subsystems
  in different packages under one slug, and the split is near-even, so neither reading dominates.
  The **activity axis needs no change** — a graded distribution topping out at 21.3%, no structural
  defect. Worth stating explicitly so the review is not read as "split everything".
  Ordering matters and is the operative point: a sweep can **add** a narrower slug to an entry that
  already carries a broad one (enrichment against a gap, permitted under `(source rank, then
  recency)`), but nothing can remove the broad one. Sweeping first would mint ~933 judgments against a
  vocabulary that cannot express the distinction being asked for — exactly how the aborted pilot ended
  up measuring the `memory-seed` ambiguity instead of the swarm.
- A: Remove the thin-tail slugs (`security` 1 use, `performance` 4) — rejected: they were added
  deliberately as rare cross-cutting add-ons and the decision-level-topics proposal already records
  that low counts are correct there. A slug used rarely but precisely is not the defect a slug used
  constantly but vaguely is. Rename `memory-trace` — rejected: renaming orphans existing entries; the
  alias mechanism handles spelling, not meaning.
- F: `docs/2_Todo/topic-vocabulary-concentration-review.md`, `docs/2_Todo/README.md`, `docs/README.md`.
- T: `docs check` OK across 181 files; index regenerated. Measurement is alias-resolved through
  `load_topic_index().resolution()` — a first pass counting raw stored slugs was dominated by aliases
  (`memory-trace-ui`, `supersession`, `related-entries`, …) and would have understated every canonical
  count.

### Follow-up

- The splits are recommended, not made — 23 slugs affecting 446 entries is JNL's call, and the review
  names which two are worth changing rather than proposing a rewrite.
- Worth re-measuring concentration after any split lands, since carving `trail` out will redistribute
  the 116 solo-`memory-trace` entries only for *future* work, not historically.

## 2026-07-26 20:22 - Hierarchical topic vocabulary: parents colour, children group

```yaml
entry_id: mse_ptqkjznweyy1qefc
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/hierarchical-topics
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Proposed a hierarchical topic vocabulary (parent/child subsystems), which supersedes the flat splits
  recommended an hour earlier and supplies the community structure a decision-level graph needs.

### Decisions

#### D1 - The hierarchy already exists, mis-filed in the alias field

- D: Add a `parent:` field (`schema_version: 1 → 2`), store the most specific slug and derive ancestors
  at read time, and reclassify the aliases that are really children. Aliases keep exactly one job:
  spelling variants of the same concept.
- R: `aliases:` has been carrying two different relations at once — genuine spellings
  (`memory-trace-ui` → `memory-trace`) and *narrower concepts* (`continuity`, `supersession`, `schema`,
  `related-entries` → `graph`; `agent-rules`, `skill-architecture`, `lazy-loading` → `control-plane`).
  Most of the 43 aliases in use are the second kind. That is a flat schema forcing a hierarchical
  vocabulary through the only field available. It has a measurable cost: alias resolution maps child →
  parent and **discards the child**, so an entry authored `continuity` is read as `graph` forever with
  no way to recover the specificity — **43 distinct aliases across 45 entries** are losing it at read
  time. It also inflates the parents, which is part of why `graph` measures 23.8%.
  Store-child-derive-parent is what makes this cheap: the cap counts stored slugs so a parent never
  consumes budget, filters expand downward exactly as alias expansion already does in
  `expand_topic_filter`, and **no entry is rewritten** — the 197 `memory-trace` entries are tagged at
  parent granularity, coarse rather than wrong, so Invariant #2 is untouched.
- A: The flat splits proposed an hour earlier (carve `trail` out, split `graph`) — **superseded by my
  own recommendation**: a flat split leaves the new slug an unrelated peer, so the 197 historical
  entries become permanently unclassifiable at the finer grain with nothing connecting old label to
  new. It also forces one field to choose between breadth and specificity. Arbitrary hierarchy depth —
  rejected: two levels, and a third should face the five-question test before it exists.
- F: `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md` (new),
  `docs/2_Todo/topic-vocabulary-concentration-review.md` (remedy superseded, measurements stand).

#### D2 - Parent colours the community, child groups it — which is what unlocks decision granularity

- D: Community colour reads the **parent** level; grouping and filtering read the **child** level.
- R: JNL's point, and it resolves something the graph could not otherwise get. Node colour already
  comes from topics rather than topology, and the topology route is closed by measurement — Louvain
  never merges across connected components, so `k` is floored at 110 on this corpus and a ~16-slot
  legend is arithmetically unreachable. Topic-derived communities have no such floor because they are
  *designed*, not discovered — but today they inherit the concentration, so three slugs
  (44.2% / 24.2% / 23.8%) dominate the palette and most of the map is three colours.
  Splitting the two jobs fixes it: parents are few, stable and legible, and change rarely, so the
  palette does not reshuffle as the corpus grows; children carry specificity without spending colours.
  That is precisely what makes it work at **decision** granularity — a decision is too small to
  justify its own colour but exactly the right size to sit inside a parent's, with its child slug doing
  the clustering.
- A: Keep pursuing topology-derived communities — rejected on measurement, already closed by ADR.
  Colour by child — rejected: that is the current situation one level down, and it would multiply the
  palette rather than bound it.
- F: `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md`.
- T: `docs check` OK across 182 files; index regenerated. Design record only — no code, no schema
  change yet.

### Follow-up

- Activities get the hierarchy too, but for **specificity recovery rather than concentration relief** —
  the activity axis measures healthy (tops at 21.3%). Areas first; activities reuse the mechanism.
- Step 1 must be behaviour-neutral so the blast radius across `topics check`, facets, the search
  filter, community colour and the swarm brief is visible before step 2 moves any resolution.
- Watch the two-derivations trap: deriving the parent at read time is a second derivation of a
  displayed fact, so legend and node colour must both read it from one function.

## 2026-07-26 20:37 - Declare the topic axis, de-software the vocabulary language, split the surface

```yaml
entry_id: mse_kdtas1ae28tk3x3j
user_initials: JNL
agent_type: claude
agent_name: Claude Fable 5
project_path: .
subproject_path: null
branch: claude/docs/topic-axis-explicit
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Made the area/activity split an explicit schema field, required domain-neutral language for it, and
  carried the split through to the inspector at decision granularity.

### Decisions

#### D1 - `axis: area | activity` becomes a declared field, not a convention

- D: A slug declares its `axis`. `schema_version: 1 -> 2` carries `axis:` and `parent:` together — one
  bump. A child inherits its parent's axis; mixing axes across a parent/child edge is a validation
  error. Shape validation ("at most one area per decision, at least one axis represented") replaces the
  blunt `MAX_TOPICS_PER_DECISION` count.
- R: The two-axis model exists only as prose today — a project-local proposal and a paragraph in the
  swarm brief — so nothing can check it. The count guard cannot tell three activities and no area from
  a well-formed one-of-each, which is exactly the failure that occurred: in pilot run 1, **10 of 45
  judgment units emitted no area at all** and only 24/45 held the intended shape. Run 2 got it to 45/45
  **by prompting harder**, and a prompt-enforced invariant regresses the moment someone edits the
  prompt. Declaring the axis moves it from advice to structure, where the validator holds it.
- A: Keep it as convention and rely on the brief — rejected on the measurement above. Two separate
  schema bumps for `axis:` and `parent:` — rejected: they land in the same file for the same reason.
- F: `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md`.

#### D2 - The language must travel outside software; the shipped starter vocabulary currently does not

- D: `topics:` stays the field and the category name. The word **"subsystem"** is retired as the label
  for the first axis, which becomes **area** ("what are you working on") against **activity** ("what
  kind of work"). Starter vocabularies vary by project type, keyed off the existing `SKILL_PROFILES`
  mechanism; the two-axis *structure* is universal, the slugs filling it are per-profile. This
  project's own software slugs stay exactly as they are.
- R: Memory Seed is a general memory substrate — a Substack folder, a research project, a legal matter.
  This is a **shipped defect**, not a naming preference: the starter vocabulary in
  `memory_seed/seed/.memory-seed/topics.yaml` is `architecture, bugfix, documentation, release,
  workflow` — every slug assumes software, and four of five are activities, so a new project begins
  with **no area axis at all**. "subsystem" also appears across the shipped control plane
  (`agent-rules.md`, `agent_collaboration.md`, `session_logging.md`, `topic_swarm.md`). A writer's
  first encounter with the vocabulary is being offered "bugfix" and "release".
  JNL's precision matters and is recorded: the fix is **not** renaming `topics:` to `subsystems` — that
  would make the container itself untranslatable. The container is already neutral; only the axis label
  fails to travel.
- A: Rename the `topics:` field — rejected, per the above. Keep one universal starter vocabulary —
  rejected: no single slug set serves both a compiler and a newsletter, and profiles already exist to
  key content by project type.
- F: `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md`.

#### D3 - The inspector splits into Area and Activity, per decision

- D: The single **Topics** row becomes two sections, and the split is shown **per decision** rather
  than per entry. Sequenced after the graph work.
- R: If the vocabulary distinguishes two axes, one undifferentiated row hides exactly the distinction
  the schema just made explicit. Per decision is where it earns most: an entry can legitimately span
  several areas, but a decision is a single act of work with one area and usually one activity, so the
  pair is meaningful rather than a union of everything the entry touched. Cheap to build — the read
  side already exposes `inferred_decision_topics` as `(ordinal, slug)` pairs alongside the rolled-up
  view (shipped 2026-07-26), so this is grouping presentation, not a new backend field. After the graph
  because the same `axis:` field feeds both and the parent-colour/child-group split is the harder
  consumer.
- A: Split per entry — rejected: reproduces the union problem one level up. Build it before the graph —
  rejected: the graph is the harder consumer of the same field and should shake it out first.
- F: `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md`.
- T: `docs check` OK across 182 files; index current. Design record only.

### Follow-up

- Build order is now seven steps, still gated on JNL accepting the proposal. Step 1 remains
  deliberately behaviour-neutral.
- The starter-vocabulary defect is the most user-facing item here and is independent of the rest — it
  could ship on its own if the hierarchy work stalls.
