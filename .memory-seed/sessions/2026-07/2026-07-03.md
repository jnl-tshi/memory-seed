---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-03
---

## 2026-07-03 07:39 - Read AGENTS.md and memory startup context

```yaml
entry_id: mse_ys4f5wsqdw5qc37w
user_initials: JNL
agent_type: codex
agent_name: solo-founder
project_path: .
subproject_path: null
related_entries:
  - mse_m6dgqw1dbdmq1hpk
```

### Summary

- Read `AGENTS.md`, discovered the repository-local `.memory-seed/` runtime, loaded `agent-rules.md`, `index.md`, `policy.md`, the newest session log in full, the prior session log for context, and the skill trigger registry.
- Loaded `.agents/_registry.yaml` and active persona files; for this orientation-only turn, the registry fallback made `solo-founder` the governing persona.

### Decision

- D: Treated the repository as initialized operating mode, not bootstrap or repair mode.
- R: The required control-plane files and directories are present under `.memory-seed/`, and `index.md` records no parent runtime.
- F: Created today's session log with this startup entry only.
- T: Verified latest session state by direct dated-file reads, not semantic search.

## 2026-07-03 12:34 - Evaluate multi-agent fanout review workflow image

```yaml
entry_id: mse_44r69a5psy0n6fb3
user_initials: JNL
agent_type: codex
agent_name: solo-founder
project_path: .
subproject_path: null
related_entries:
  - mse_ys4f5wsqdw5qc37w
```

### Summary

- User asked to inspect the `docs/inbox/` image and use subagents to research and evaluate the depicted multi-agent fanout workflow with a vendor-agnostic lens.
- Located `docs/inbox/multi-agent-fanout-review-workflow.jpeg`, visually inspected it, and dispatched three read-only subagents covering orchestration architecture, git/worktree merge mechanics, and Memory Seed product fit.

### Decision

- D: Evaluated the image as a good Level 2/3 collaboration pattern when translated into vendor-neutral roles and guarded by explicit integration contracts.
- R: The diagram's useful core is not the named model stack; it is the role separation: explore, plan, bounded implementation in isolated worktrees, sequential integration, final review, and PR/local handoff. Existing `.memory-seed/skills/agent_collaboration.md` already contains most of this contract.
- A: Rejected copying the diagram literally into always-read rules because it bakes in provider/model names and would bloat startup context. Best fit is a named recipe inside `agent_collaboration.md`; a `docs/todo/` plan only makes sense if productizing workflow scaffolding becomes a goal.
- F: Session log only.
- T: Subagent reviews completed; cross-checked Git worktree and protected-branch/status-check concepts against official Git and GitHub documentation.

## 2026-07-03 15:41 - Add fanout workflow report to inbox

```yaml
entry_id: mse_ytzf3n0g6gtze690
user_initials: JNL
agent_type: codex
agent_name: solo-founder
project_path: .
subproject_path: null
related_entries:
  - mse_44r69a5psy0n6fb3
```

### Summary

- User asked to add the multi-agent fanout workflow recommendations as a summarized Markdown report in the inbox folder.

### Decision

- D: Created `docs/inbox/multi-agent-fanout-review-workflow-report.md` beside the source image.
- R: The report preserves the useful recommendation in a vendor-neutral, local-first form: roles and capability tiers instead of model/provider names, Git worktrees and task packets instead of a hosted orchestration dependency.
- F: Added the report file only.
- T: Read the generated Markdown back and ran session-link validation.

## 2026-07-03 15:50 - Synthesize inbox multi-agent workflow proposal

```yaml
entry_id: mse_afja0r8m34916964
user_initials: JNL
agent_type: codex
agent_name: solo-founder
project_path: .
subproject_path: null
related_entries:
  - mse_ytzf3n0g6gtze690
```

### Summary

- User asked to check the two Markdown files in `docs/inbox/` and synthesize them to improve the proposal for Memory Seed.

### Decision

- D: Added `docs/inbox/multi-agent-workflow-memory-seed-proposal.md` as the synthesized proposal, preserving the two source notes unchanged.
- R: The combined proposal is more actionable than either source: it keeps the vendor-neutral fanout recipe from the first report while incorporating the second file's corrections around bounded rework loops, identity/base preflight, validation before review, explicit conflict ownership, strong planning capability, and avoiding parallel implementers by default.
- A: Did not edit `.memory-seed/skills/agent_collaboration.md` yet because this turn asked for synthesis of inbox files, not implementation of the runbook change.
- F: Added the synthesized proposal file.
- T: Read the generated proposal back and ran session-link validation.

## 2026-07-03 11:01 - Guard-regression tests and Working Principles convention

```yaml
entry_id: mse_hejpp9whj0nn8jyy
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_ys4f5wsqdw5qc37w
```

### Summary

- Followed up on this session's ponytail-plugin fan-out evaluation (rejected the plugin; agreed to absorb two ideas as documentation instead): added regression tests for the two highest-value untested guard spots a follow-up audit found, and folded the two agreed conventions into Working Principles instead of a new lazy skill. Used Plan Mode for this: an Explore agent audited the codebase for risky guards, a Plan agent then verified that audit against the real files (finding it had overstated the MCP-guard gap) and designed the scoped plan below, approved before implementation.

### Decisions

#### D1 - Working Principles over a new lazy skill

- D: Added two bullets to the existing Working Principles section of `agent-rules.md` (decision ladder + note-why-deferred habit; do not strip terse guards without understanding why) instead of creating `.memory-seed/skills/code_simplification_review.md`.
- R: Working Principles is read unconditionally at every session start and applies to any task; a lazy-loaded skill only loads when a task is recognized as "code simplification," which would miss the actual risk — a guard stripped during an incidental cleanup inside an unrelated edit.
- F: `.memory-seed/agent-rules.md` and `memory_seed/seed/.memory-seed/agent-rules.md` (byte-identical edit, both copies).

#### D2 - Scoped regression tests to 2 real gaps, not all 13 originally-flagged spots

- D: Added `test_session_target_rejects_invalid_session_date` and `test_mcp_merge_preserves_foreign_server_on_our_key` (parameterized over claude/cursor/gemini via `subTest`). Added `test_seed_and_live_agent_rules_match` to cover the `agent-rules.md` twin this change itself hand-edits. Skipped tests for the 9 isinstance/malformed-config guards and for `_merge_vscode_mcp`'s foreign-key case.
- R: A design-phase re-verification found the initial audit overstated the MCP gap — `_merge_copilot_mcp` and `_merge_codex_mcp` already had foreign-key tests; the real untested gap was only claude/cursor/gemini (identical logic) plus vscode (different container key, deferred). The isinstance guards degrade gracefully on bad input, a materially lower-severity failure mode than silently clobbering a user's MCP config or accepting a malformed date; adding tests there would be the validation churn this project's philosophy already rejects.
- A: Considered one test per all 7 originally-flagged MCP occurrences and one per isinstance guard; rejected both as disproportionate to actual risk.
- F: `tests/test_memory_seed.py` (3 new test functions, no production code changes — the guards already worked correctly).
- T: Full suite green. Both new discrimination-relevant tests confirmed to fail when their guard is temporarily removed (date guard, and the `is_ours` check across all three merge functions), then pass again once restored. `memory-seed doctor` and `links check` clean. Manual diff of both `agent-rules.md` copies confirmed identical, now also enforced by `test_seed_and_live_agent_rules_match` going forward.

### Follow-up

- Changes remain uncommitted.

## 2026-07-03 11:18 - Reorganize docs folder and refresh functionality audit

```yaml
entry_id: mse_djvwtfx02kjr5j1n
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_hejpp9whj0nn8jyy
```

### Summary

- User asked to put items in `docs/` in the right folder and refresh `docs/functionality-audit.md` to current state. Also checked for git merge conflicts on request mid-task (none found — local `main` was simply ahead of `origin/main` by 4 commits, no divergence).

### Decisions

#### D1 - Reorganized docs/ placement

- D: Moved `docs/inbox/ponytail implementation.md` → `docs/todo/completed/ponytail-implementation.md` (its evaluation is fully resolved and shipped); removed the now-empty `docs/inbox/`. Moved `docs/todo/agent-templates/` back to `docs/agent-templates/` (a standalone CLAUDE.md persona-template asset, not an open memory-seed roadmap proposal). Deleted `docs/todo/completed/memory_lense_claude_design_output/` after explicit user confirmation — byte-identical to the `docs/todo/memory lense/` prototype folder already deleted earlier this session, and not a plan doc so it didn't fit `completed/`.
- R: `docs/todo/` and `docs/todo/completed/` are specifically for open/evaluated memory-seed roadmap proposals; neither the ponytail evaluation's source doc nor a standalone persona-template package nor a raw design-tool export fit that semantics once their status was known.
- A: A `Bash` call bundling all three actions (two `mv` + one `rm -rf`) was blocked in full by the auto-mode safety classifier because of the un-vetted `rm -rf` target — the whole multi-line script was blocked atomically, so nothing executed, not even the safe moves. Split the work: ran the two moves as separate, non-destructive commands, then asked the user directly via `AskUserQuestion` before deleting the one genuinely destructive, previously-unexplained item rather than retrying around the block.
- F: `docs/inbox/` (removed), `docs/todo/completed/ponytail-implementation.md` (moved in), `docs/agent-templates/` (restored), `docs/todo/completed/memory_lense_claude_design_output/` (deleted, user-confirmed).
- T: Confirmed no existing references anywhere in tracked docs pointed at the old paths before moving; `git status` after each step showed only the expected renames/deletions, no surprises.

#### D2 - Refreshed functionality-audit.md

- D: Updated the "As of" date to 2026-07-03; documented the participant-count session-layout gate, the SessionStart identity-offer hook, the `doctor` local-user/participant-mismatch warning, and the two new Working Principles bullets (§3D, §3H, §3J, §3L); updated the roadmap section to mark the ponytail evaluation completed and note the 4 remaining open logic-capture proposals; updated the test count from 205 to the verified 219.
- R: The doc's own contract is "status reflects the current tree" — a full session's worth of shipped-but-unreleased work (participant gating, identity hook, guard tests, Working Principles) wasn't reflected anywhere, which would mislead anyone using it as an architecture reference.
- F: `docs/functionality-audit.md` (header, §3D, §3H, §3J, §3L, §14, §15).
- T: Full suite (219/219), `memory-seed doctor` (healthy), `memory-seed links check` (clean) all re-run after the docs edits.

### Follow-up

- Changes remain uncommitted.

## 2026-07-03 15:35 - Fan-out evaluation of multi-agent workflow diagram

```yaml
entry_id: mse_7v4p692txy0a8nkr
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_djvwtfx02kjr5j1n
```

### Summary

- User asked to run the same fan-out-and-merge evaluation loop used for the ponytail-plugin proposal against `docs/inbox/multi-agent-fanout-review-workflow.jpeg` (an Explore→Plan→Implement(worktrees)→Merge→Review→PR diagram), with an explicit constraint: no fable-tier subagents, cap at opus.

### Decision

- D: Verdict is adopt-with-refinements, parallel implementers deferred. Adopt: (1) an identity-verification step at worker spawn (confirm `pwd` + `git rev-parse HEAD` against the intended base) before a worker acts; (2) a bounded review→rework loop (cap N iterations, then escalate to a human checkpoint); (3) a validation/CI gate before the review stage, plus a named merge-conflict owner. Reject: rigid per-stage model-name assignment (keep the existing vendor-neutral capability tiers) and the loop as drawn (no visible bound). Defer: parallel implementers on non-overlapping subtasks, until a change actually warrants the overhead. Open question raised by review, not yet resolved: if any stage deserves the top (opus) tier, it may be planning, not review — a weak plan poisons every downstream worker, while review only catches what's already built. This inverts the diagram's own allocation and needs a decision before any tier guidance is written.
- R: Three sonnet-tier research agents (fit vs. existing docs, worktree/merge feasibility, model-tier + loop design) plus a model=opus critique pass found: the diagram's core skeleton (worktree-per-worker, task packets, integration checklist) is already codified in `.memory-seed/skills/agent_collaboration.md` — not a gap. The one confirmed, genuine gap is the unbounded review→rework loop, which conflicts with this repo's own consistent precedent (the evolution-nudge hook was deliberately never shipped; the identity-offer hook is deliberately one-shot via a stamp file). The cwd/stale-worktree bug this session hit repeatedly was reproduced live again during this very evaluation (a research agent's default cwd was 3 commits behind real `main`), directly motivating the identity-verification refinement. The opus pass also caught that one research agent's "select-best vs. merge" distinction was a self-introduced interpretation, not something the diagram actually shows, and demoted it from a headline finding to a minor clarifying note.
- A: One research agent caught that my own orchestrating prompt overstated a claim — that a "sonnet fan-out + opus review" combination was a deliberately verified pattern used earlier today — as fact not actually recorded anywhere in this repo's session logs (`agent_type` records vendor names only, never tiers). Corrected rather than left standing.
- F: No files changed — evaluation only, no implementation yet (per instruction to run the evaluation loop, not build anything).
- T: All three research agents' and the opus agent's citations spot-checked against real repo content (`agent_collaboration.md`, `agent-rules.md`, session logs) rather than accepted at face value, following the same discipline established during the ponytail evaluation.

### Follow-up

- Not yet resolved: the planning-vs-review tier-allocation question the opus pass raised.
- No implementation started — awaiting user direction on whether/how to act on the adopt-with-refinements verdict.

## 2026-07-03 15:43 - Resolve planning-tier question in workflow recommendations

```yaml
entry_id: mse_fe6gd01df9vf9t0d
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_7v4p692txy0a8nkr
```

### Summary

- User resolved the one open question from the prior evaluation: planning should get the strong model, not just review.

### Decision

- D: Updated `docs/inbox/multi-agent-workflow-evaluation-recommendations.md` — the "Open question" section is now a resolved decision: planning warrants a strong/top-tier model on the same footing as review (or above), since a weak plan poisons every downstream worker while review only catches what's already built. Explore and implement stay lighter/mid tier.
- R: User's explicit call, matching the opus pass's reasoning from the prior entry.
- F: `docs/inbox/multi-agent-workflow-evaluation-recommendations.md` only.
- T: `memory-seed links check` clean.

### Follow-up

- Recommendations file now has no open questions; still sitting in the inbox, not yet implemented.

## 2026-07-03 15:50 - Synthesize the two inbox workflow evaluations

```yaml
entry_id: mse_j1wvtzfkqrv87eah
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_fe6gd01df9vf9t0d
```

### Summary

- User asked to check both markdown files in `docs/inbox/` (mine, and one independently written by a parallel Codex/solo-founder session on the same source image) and synthesize them into an improved proposal.

### Decision

- D: Wrote `docs/inbox/agent-fanout-workflow-proposal-synthesis.md`, merging both. Kept Codex's report's stronger structure (vendor-neutral translation table, 8-gate pipeline, branch/worktree protocol) and its genuinely new risk neither of my own passes raised (shared files — session logs, lockfiles, seed templates, control-plane files — as high-risk under concurrent writers). Added back what that report's linear pipeline had dropped from the original diagram: the bounded review→rework loop. Folded in my own findings: the identity-verification pre-condition on the Worktree Gate (motivated by a bug reproduced live during the earlier evaluation) and the resolved planning-tier decision. Added one connection neither source made: the shared-session-log risk Codex flagged is exactly what this project's own participant-count `session_target()` gate (built earlier this session) partially mitigates already — generalized here to "route all shared/control-plane writes through the orchestrator only."
- R: The two reports were genuinely complementary, not redundant — each had a real gap the other filled. Codex's pipeline is more complete and concrete but silently lost the original diagram's most distinctive feature (the loop-back arrow) by translating it into a strictly linear 8-gate sequence; my own pass had the loop and the bug evidence but was thin on mechanics.
- F: `docs/inbox/agent-fanout-workflow-proposal-synthesis.md` (new). Left both source files untouched.
- T: `memory-seed links check` clean.

### Follow-up

- Still a proposal only — no implementation into `agent_collaboration.md` yet.
- Note: a parallel Codex session independently produced its own synthesis at the same time (`docs/inbox/multi-agent-workflow-memory-seed-proposal.md`) — left untouched, not reconciled with this one.

## 2026-07-03 15:57 - Consolidate all four inbox workflow docs into one file

```yaml
entry_id: mse_jrakvf3j55prrshd
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_j1wvtzfkqrv87eah
```

### Summary

- User asked to read all four markdown files in `docs/inbox/` (the two original evaluations plus both parallel syntheses of them — mine and a concurrent Codex session's) and merge them into one file covering all findings.

### Decision

- D: Wrote `docs/inbox/agent-fanout-workflow-consolidated.md`. Took the 9-gate structure from the Codex parallel synthesis (it added a dedicated Worker Identity Gate with concrete preflight commands, a full per-stage capability-tier table, and a task-packet YAML schema diff — all more concrete than my own synthesis had), and folded in what only my synthesis had: explicit per-source attribution, and the connection between the shared-file/session-log risk and this project's own existing participant-count `session_target()` mitigation. Also kept Codex's "what not to build yet" list and its later-productization CLI sketch, both absent from mine.
- R: The two parallel syntheses agreed on the verdict but differed in depth in different places — neither fully superseded the other, so a straight "pick one" would have dropped real content either way.
- F: `docs/inbox/agent-fanout-workflow-consolidated.md` (new). All four source files left untouched for provenance.
- T: `memory-seed links check` clean.

### Follow-up

- Five files now sit in the inbox (image + 4 md); none deleted or archived — only added the consolidated one, since deletion wasn't asked for.
- Still a proposal only — no implementation into `agent_collaboration.md`.

## 2026-07-03 16:03 - Delete superseded inbox workflow docs

```yaml
entry_id: mse_b38zj3ktrwf1c7ye
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_jrakvf3j55prrshd
```

### Summary

- User asked to delete the four now-superseded workflow docs, keeping only `docs/inbox/agent-fanout-workflow-consolidated.md` and the source jpeg.

### Decision

- D: Deleted `multi-agent-fanout-review-workflow-report.md`, `multi-agent-workflow-evaluation-recommendations.md`, `agent-fanout-workflow-proposal-synthesis.md`, and `multi-agent-workflow-memory-seed-proposal.md`. `docs/inbox/` now holds exactly two files: the consolidated proposal and the source image.
- R: Explicit, unambiguous user instruction naming exactly what to keep — no confirmation step needed this time, unlike the earlier `memory_lense_claude_design_output/` deletion which was ambiguous and unnamed.
- F: Four files removed from `docs/inbox/`.
- T: `memory-seed links check` clean; confirmed final inbox listing shows only the two intended files.

### Follow-up

- `docs/inbox/` is now clean: consolidated proposal + source image only.

## 2026-07-03 16:25 - Promote today's durable facts to index.md and policy.md

```yaml
entry_id: mse_52cw7g10wmaha5h4
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_b38zj3ktrwf1c7ye
```

### Summary

- User asked to consolidate today's durable facts into `.memory-seed/index.md` and `.memory-seed/policy.md`, following this repo's own `memory_consolidation` skill and End Of Turn scope rules (index.md = topology/active-state/design-decisions/skill-pointers; policy.md = behavioral constraints only; reasoning/alternatives stay in session history).

### Decision

- D: Updated `index.md` — folded the participant-count session-layout gate and the doctor local-user/participant-mismatch warning into the existing "Participant registry parsing (current unreleased worktree)" Design Decision bullet; added a new adjacent bullet for the one-time identity-offer hook (`.memory-seed/.identity-offer-stamp`); added a Topology bullet documenting the `docs/todo/` / `docs/todo/completed/` / `docs/agent-templates/` taxonomy finalized by today's docs reorg; added a second Active State "Current risk" bullet for the stale/frozen-worktree-under-subagents risk reproduced twice this session. Updated `policy.md` — added one Safety bullet requiring `pwd`/`git rev-parse HEAD` verification against the intended base commit before trusting a subagent's reads or citations for this repo.
- R: The 2.10.0 "User-aware session targets" bullet documents what that release actually shipped and was left untouched (per advisor guidance, editing a released-version bullet to describe unreleased behavior would corrupt the version history); the gate and doctor warning are unreleased refinements layered on top, matching how this section already handles incremental unreleased changes (entry-ID widening, participant parsing, etc.). Skipped promoting: the fan-out-workflow evaluation methodology and the docs/inbox/ proposal-only convention (advisor flagged `docs/inbox/` as an ad-hoc holding area recreated today, not durable topology, and noted policy already covers "ask before broad rewrites"); the two new Working Principles bullets (already correctly live in `agent-rules.md`, which owns operating-mode rules, not `index.md`/`policy.md`); the guard-regression tests and functionality-audit refresh (code/doc-derivable, not active-state facts).
- A: Also wrote several personal cross-session memory files (not part of this repo) before the user clarified they meant this repo's own index.md/policy.md — those personal-memory entries (user identity, terse-report feedback, subagent-tier feedback) are being kept since they're genuinely correct for that separate system; only the two project-specific ones (stale-worktree risk, fanout-proposal-pending status) are now superseded by their equivalents here.
- F: `.memory-seed/index.md`, `.memory-seed/policy.md`. No seed-twin mirroring needed — both files are bootstrap-generated, not seeded, per Topology.
- T: `memory-seed doctor` (healthy), `memory-seed links check` (clean, 23 files) both re-run after the edits. Verified in the live code (not the stale worktree) that `session_target()`'s participant-count gate, the `doctor` warning, and `offer_identity_setup()`/`.identity-offer-stamp` all exist as described before writing the bullets.

### Follow-up

- Changes remain uncommitted.

## 2026-07-03 18:35 - Promote fanout workflow proposal to docs/todo and NEXT_STEPS

```yaml
entry_id: mse_q3vc3nntkc8g8k86
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_52cw7g10wmaha5h4
```

### Summary

- User asked whether the consolidated fan-out workflow proposal sitting in `docs/inbox/` was ready to move to `docs/todo/`; recommended yes (fully evaluated, open question resolved, matches the existing "proposed, not yet decided or built" plan-doc pattern) and offered to make the move. User confirmed: add it to the todo folder and NEXT_STEPS.md only (a broader follow-on request to also evaluate cross-proposal synergies via subagent orchestration was started, then interrupted and retracted — not acted on).

### Decision

- D: Moved `docs/inbox/agent-fanout-workflow-consolidated.md` → `docs/todo/agent-fanout-workflow-plan.md`. Updated its frontmatter to match the other `docs/todo/*-plan.md` docs (`memory-system-version`, `tags: [memory-seed, plan, agent-collaboration, workflow]`, dropped the ad hoc `status`/`sources` fields) and added the same `> **Status: PROPOSED...**` blockquote convention, noting its source is an internal fan-out evaluation rather than the external `Memory-Seed Logic Capture Improvement.md` doc the other 5 plans derive from. Fixed the now-stale "four source files left untouched for provenance" closing line (those four were deleted in an earlier turn) to correctly say only the source jpeg remains in `docs/inbox/`. Added a new "Agent Collaboration Workflow - Proposed" subsection to `NEXT_STEPS.md` (after "Logic Capture Improvements") linking the moved plan doc, mirroring the existing subsection style.
- R: The consolidated proposal is fully specced (9-gate pipeline, task-packet schema additions, capability-tier table) and evaluated to the same or greater rigor as the other 5 `docs/todo/` plan docs, so it belongs alongside them as a tracked, not-yet-built roadmap item rather than staying in the inbox holding area.
- A: Considered also moving the source jpeg out of `docs/inbox/` alongside the plan doc; left it in `docs/inbox/` since it's source material, not a proposal, and the user didn't specify. Considered also refreshing `docs/functionality-audit.md`'s roadmap section (§14) to mention this new plan doc (it currently only covers the Logic Capture items and the completed ponytail evaluation); deferred since the user's confirmed instruction was scoped to the todo folder and NEXT_STEPS.md only, not the audit doc.
- F: `docs/todo/agent-fanout-workflow-plan.md` (moved + edited from `docs/inbox/agent-fanout-workflow-consolidated.md`), `docs/todo/NEXT_STEPS.md` (new subsection).
- T: `memory-seed doctor` (healthy) and `memory-seed links check` (clean, 23 files) both re-run after the move.

### Follow-up

- `docs/functionality-audit.md` §14 roadmap does not yet mention this plan doc — a candidate for a future audit refresh, not done now per scope above.
- The broader synergy-evaluation request (cross-checking all `docs/todo/` proposals against `functionality-audit.md` for hidden synergies/clashes, via subagent orchestration) was raised then retracted this turn — not started. Revisit only if the user re-asks.
- Changes remain uncommitted.

## 2026-07-03 19:26 - Align todo proposals across roadmap

```yaml
entry_id: mse_tznx70j8tk3meazk
user_initials: JNL
agent_type: codex
agent_name: solo-founder
project_path: .
subproject_path: null
related_entries:
  - mse_q3vc3nntkc8g8k86
  - mse_afja0r8m34916964
```

### Summary

- User asked to evaluate all current `docs/todo/` proposal lists for synergy and logical gaps against `docs/functionality-audit.md`, use subagent-style decomposition/research, document new ideas, write the output under `docs/todo/codex/`, and include proposal order/priority in both the synthesis and `NEXT_STEPS.md`.

### Decision

- D: Added `docs/todo/codex/proposal-synergy-evaluation.md` as the coordinating synthesis. It records cross-proposal dependencies, clashes, hidden synergies, P0-P3 priority order, and three new evaluation loops: Pillar B distribution, fanout CLI scaffolding, and render-verification workflow.
- D: Updated the active proposal docs so they work together: related-entries P1 is clearly read-only and `link add` is deferred; `supersedes` gates supersession-aware scoring; ranking is split into raw `related_degree`, later `importance_score`, then telemetry; commit linking now has a same-turn/newest-entry edit rule and full-SHA validation; failed approaches stay `A` evidence; Mermaid guidance includes semantic freshness; fanout workflow now uses handoff-only workers, orchestrator-only shared-file writes, bounded review loops, and preview-first CLI constraints.
- D: Marked stale or source-material docs explicitly: `3.0-plan.md` is partly historical after 2.12/2.13, the UI research report is historical input partially superseded by Memory Lense V1, the logic-capture report is source material for five scoped plans, and the DOCX render lessons are an active seed-skill proposal.
- D: Updated `docs/todo/NEXT_STEPS.md` with the Codex synthesis link, P0-P3 proposal priority order, explicit sequencing constraints, agent-fanout guardrails, and the DOCX render seed-skill proposal. Updated `docs/functionality-audit.md` to fix the stale `4 of 5` count, migration-label wording, and roadmap coverage for fanout/render proposals.
- R: The proposals had become individually useful but locally inconsistent because several were drafted before Memory Lense V1, `build_related_entry_graph()`, `link show`, and the legacy-flat `links check` fix. The alignment keeps behavior changes behind read-only inspection surfaces and shared validation contracts.
- A: Deferred actual implementation of the proposals; this turn only reconciled proposal docs and produced the requested synthesis/priority report. Also left remaining citation-artifact cleanup in the UI research report as a documented follow-up rather than broad-editing a historical research artifact.
- F: `docs/todo/codex/proposal-synergy-evaluation.md`, `docs/todo/NEXT_STEPS.md`, `docs/functionality-audit.md`, `docs/todo/3.0-plan.md`, `docs/todo/user-interface-deep-research-report.md`, `docs/todo/related-entries-generation-plan.md`, `docs/todo/supersession-edges-plan.md`, `docs/todo/interaction-frequency-ranking-plan.md`, `docs/todo/git-commit-entry-linking-plan.md`, `docs/todo/failed-approaches-logging-plan.md`, `docs/todo/mermaid-usage-guidance-plan.md`, `docs/todo/agent-fanout-workflow-plan.md`, `docs/todo/docx-render-windows-seed-lessons.md`, `docs/todo/Memory-Seed Logic Capture Improvement.md`.
- T: `python -m memory_seed.cli links check` clean before appending this entry and clean again after appending it; `python -m memory_seed.cli doctor` healthy; `python -m unittest tests.test_session_schema` passed 18 tests.

### Follow-up

- Decide P0 shared graph/validation contract before implementing `supersedes`, `commits`, or ranking metadata.
- The UI research report still contains historical citation artifacts and should be scrubbed before any public-facing reuse.
- Changes remain uncommitted.

## 2026-07-03 20:47 - Cross-proposal synergy evaluation, reconciled with concurrent Codex pass

```yaml
entry_id: mse_das03yy1zbyp991m
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_tznx70j8tk3meazk
```

### Summary

- User asked to evaluate the `docs/todo/` proposals for synergy/gaps against each other and `docs/functionality-audit.md`, using subagent orchestration, and to start a fresh research loop for any genuinely new idea that surfaced. Fanned out 3 read-only research subagents (ranking/graph cluster, guidance/convention cluster, audit cross-check + ship order), each briefed with specific hypotheses. Mid-evaluation, discovered a concurrent Codex session had independently run a near-identical synergy pass and already applied edits to almost every targeted file, plus written its own report to `docs/todo/codex/proposal-synergy-evaluation.md`.

### Decision

- D: Reconciled rather than duplicated: read every diff Codex had applied, confirmed each of the 3 subagents' findings against Codex's actual changes, and applied only the remainder — items neither Codex's report nor its edits covered. Applied: (1) `agent-fanout-workflow-plan.md`'s Exploration Gate now states read-only Explorers must still satisfy `.memory-seed/policy.md`'s pwd/`git rev-parse HEAD` check (the actual mechanism that would have caught the bug that motivated the whole plan, since that bug hit a read-only research role, not a write-side worker); (2) its Final Handoff Gate now notes the orchestrator backfills `commits:` from workers' reported hashes once `git-commit-entry-linking-plan.md` ships; (3) `supersession-edges-plan.md` gained a Validation-section note that its "provably acyclic" claim rests on an unenforced forward-only convention, with a `links check` cycle-guard added to the DoD as a P1 candidate (not committed); also fixed a stray mojibake character Codex's edit had introduced. (4) Wrote a new proposal, `exclude-superseded-filter-plan.md` — an opt-in `memory_search` filter to narrow to non-superseded entries, respecting the existing "never hide, only deprioritize" contract, blocked on `supersession-edges-plan.md` P1 — added as item 6 in `NEXT_STEPS.md`. Wrote `docs/todo/Claude/proposal-synergy-evaluation.md` documenting the full reconciliation and the unified ship order, linked from `NEXT_STEPS.md` alongside the Codex report.
- R: Both independent evaluations converged on the same core finding (interaction-frequency-ranking's P1 falsely claimed independence from supersession-edges) — strong signal it was real, not framing-dependent. Codex's applied fix (splitting P1a/P1b) was cleaner than this evaluation's own draft, so it was kept as-is rather than replaced. Everything else Codex fixed (stale 3.0-plan/UI-report status, audit §14 gap, "4 of 5" count, participants: concern) needed no further action.
- A: Considered a full opus-tier critique pass on the synthesis before editing, per this session's established pattern; skipped it once the remaining scope collapsed to 4 small, independently-justified items after reconciling with Codex's pass — used a direct advisor sanity-check instead, consistent with not manufacturing process for a small, well-evidenced delta.
- F: `docs/todo/agent-fanout-workflow-plan.md`, `docs/todo/supersession-edges-plan.md`, `docs/todo/exclude-superseded-filter-plan.md` (new), `docs/todo/Claude/proposal-synergy-evaluation.md` (new), `docs/todo/NEXT_STEPS.md`.
- T: `memory-seed doctor` and `memory-seed links check` run clean both before touching any Codex-modified file (baseline) and after this pass's edits; grepped for mojibake artifacts across `docs/todo/` to confirm the fix and that no other instances existed.

### Follow-up

- No open research loop from this pass — the one genuinely new idea (`exclude-superseded-filter-plan.md`) is fully specced and blocked cleanly on `supersession-edges-plan.md` P1, not left as a dangling question.
- The forward-only/cycle-detection guard added to `supersession-edges-plan.md`'s DoD is a candidate, not a committed requirement — worth revisiting when that plan actually gets implemented.
- Changes remain uncommitted.

## 2026-07-03 21:46 - Implement docs-only proposal batch and bump to 2.14.0

```yaml
entry_id: mse_dwstzzfnz5w2626v
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_das03yy1zbyp991m
```

### Summary

- After committing the day's doc work as checkpoint `2d7e00f`, the user green-lit the docs-only implementation batch from the synergy evaluation (failed-approaches logging, Mermaid guidance, fanout recipe) and made four sign-off decisions via direct questions: docs-only scope first, `Memory-Entry:` as the commit-trailer key, the forward-only/cycle guard into `supersedes` P1 scope, and one version bump at the end of the batch. Also resolved two pre-commit inconsistencies: agent-templates stay in `docs/todo/completed/` (index.md taxonomy updated to match), and `.vscode/` is now gitignored (its pytest config conflicts with the repo's unittest usage).

### Decisions

#### D1 - Implemented the three docs-only increments

- D: Added the failed-approaches rule to `session_logging.md`'s Reason Rules; added the Mermaid usage bullet (with semantic freshness) to `agent-rules.md`'s Working Principles; added the named "Fan-Out Recipe: Explore / Plan / Implement / Validate" (9 gates, condensed) plus the new task-packet fields (`base_sha`, `expected_pwd`, `integration_artifact`, `capability_tier`, `shared_file_policy`, `conflict_owner`, `preflight`, `review_loop`) and capability-tier guidance to `agent_collaboration.md`. All three changes applied byte-identically to live and seed twins.
- R: Exactly as specced in the three plan docs — the specs were implementation-ready after the synergy passes, needing no further design.
- A: Considered copying the full plan-doc prose into the skill; condensed the 9 gates to one line each instead, since skills are lazy-loaded but still shouldn't bloat, and the plan doc remains the detailed reference.
- F: `.memory-seed/skills/session_logging.md`, `.memory-seed/agent-rules.md`, `.memory-seed/skills/agent_collaboration.md` + their three seed twins; three plan docs moved to `docs/todo/completed/` with IMPLEMENTED status blocks; `NEXT_STEPS.md`, `docs/functionality-audit.md` (§3D, §3F, §14), `CHANGELOG.md` (three Unreleased bullets, then folded); sign-offs recorded in `git-commit-entry-linking-plan.md` (trailer key) and `supersession-edges-plan.md` (cycle guard into P1).
- T: Twins verified byte-identical via `filecmp` (a git-bash `diff` false alarm on `agent_collaboration.md` was diagnosed as a process-substitution artifact, not a real difference — confirmed by Python byte comparison).

#### D2 - Version bump 2.13 -> 2.14.0

- D: Bumped `VERSION` in `core.py`, `version` in `pyproject.toml`, the literal test pin, `memory-system-version` frontmatter across 49 control-plane files (byte-level replace, skipping `archive/`, `sessions/`, `docs/`, `demo/`), `index.md` Active State, and folded CHANGELOG's Unreleased into a new `## 2.14.0 - 2026-07-03` section with an empty `## Unreleased` placeholder retained.
- R: User chose "bump once at the end of the batch, ready for release" over queue-only or per-increment bumps. Skipped an `.memory-seed/archive/2.13/` snapshot: the archive convention demonstrably stopped at 2.5 (no 2.6-2.13 folders exist); archiving is handled by `memory-seed update` in downstream projects.
- F: `memory_seed/core.py`, `pyproject.toml`, `tests/test_memory_seed.py` (version pin), 49 control-plane `.md` files, `.memory-seed/index.md`, `CHANGELOG.md`.
- T: Full suite via `python -m unittest discover tests`: 219/219 green. `memory-seed doctor` healthy (no version-mismatch warnings after the bump), `memory-seed links check` clean.

### Follow-up

- 2.14.0 is release-ready but NOT yet tagged/published — the GitHub Release + PyPI approval gate remains a separate, user-driven step.
- Next implementation increments per the signed-off ship order: `supersedes` P1 (with the cycle guard), then commit-linking P1, then ranking P1a — awaiting go-ahead on the code batch.

## 2026-07-03 22:00 - Push main and create v2.14.0 release

```yaml
entry_id: mse_205vnae3pk91r1xn
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_dwstzzfnz5w2626v
```

### Summary

- User chose "publish 2.14.0 first" over starting the code batch. Pushed the 6 local commits (`d923c63..9c07fae`) to `origin/main` after explicit user confirmation (the auto-mode classifier blocked the initial direct-to-main push; confirmed via a direct question rather than working around it), then created GitHub Release `v2.14.0` targeting `main` with notes summarized from the CHANGELOG 2.14.0 section.

### Decision

- D: Released via the established flow: `git push origin main` → `gh release create v2.14.0 --target main` → publish workflow `28683120449`. Build job (tests + `uv build`) passed in 10s; the run is now paused at the `pypi` environment's manual-approval gate, awaiting the maintainer's click — deliberately left to the user since the PyPI push is irreversible.
- R: Repository publish process is release-driven through GitHub Actions with a trusted-publisher environment gate; the approval is the final human checkpoint and stays user-owned by standing convention.
- F: Remote `main` updated (fast-forward, no divergence); GitHub Release `https://github.com/jnl-tshi/memory-seed/releases/tag/v2.14.0`; workflow run `28683120449`.
- T: `git fetch` confirmed clean fast-forward before pushing (6 ahead, 0 behind). `gh run view` confirmed the Build job green and the Publish job waiting; `pending_deployments` API confirmed the `pypi` gate with `current_user_can_approve: true`.

### Follow-up

- Awaiting maintainer approval of the `pypi` deployment on run `28683120449`; after approval, verify PyPI shows `memory-seed==2.14.0` wheel + sdist.
- Then: the signed-off code batch (`supersedes` P1 → commit-linking P1 → ranking P1a).

## 2026-07-03 22:22 - Implement typed supersession edges P1

```yaml
entry_id: mse_mk82r25x7mvb1aqa
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_205vnae3pk91r1xn
  - mse_dwstzzfnz5w2626v
```

### Summary

- User green-lit the code batch; started with `supersedes` P1 per the signed-off ship order. Built the typed supersession edge end to end: schema parsing, read-time inverse, validation with the signed-off forward-only/cycle guard, CLI + MCP exposure, and the schema doc.

### Decision

- D: `MemoryChunk` gained a `supersedes` tuple parsed from entry YAML by the existing generic metadata scan (`_metadata_list`); `RelatedEntryNode`/`build_related_entry_graph()` gained `supersedes` (stored) and `superseded_by` (computed inverse, built exactly like `inbound`, resolvable refs only, never merged with relatedness edges). `check_session_links()` validates four new issue kinds: `dangling-supersedes` (any fenced-yaml block, both layouts), `self-supersedes`, `supersedes-postdates` (a heading-anchored second scan attributes each ref to its source entry's heading timestamp; a target strictly later than its source is an error), and `supersedes-cycle` (iterative DFS over resolvable edges — same-minute entries are unordered by the timestamp check, so mutual supersession there is only caught by the DFS). Exposure: `link show` prints `supersedes`/`superseded_by` rows; `memory_get_chunk` returns both (inverse computed by reusing the already-extracted entry corpus via `build_related_entry_graph(chunks=...)`); `memory_search` results carry stored `supersedes`. Schema documented in `session_logging.md` + seed twin (forward-only rule, never-hide-only-deprioritize, don't hand-write `superseded_by`).
- R: Exactly as specced in `supersession-edges-plan.md` with the 2026-07-03 sign-offs (field name `supersedes`, cycle guard in P1 scope). The separate-field design meant zero changes to existing `related_entries` behavior — everything shipped is additive.
- A: Considered a position-in-file ordering for the forward-only guard; rejected it because interleaved concurrent sessions (as happened live in this very file today — entries appear positionally out of timestamp order) can legitimately append an earlier-timestamped entry after a later one, which would false-positive. Heading-timestamp comparison is immune to that: an honestly-authored ref always targets an entry whose timestamp is ≤ the author's clock. Same-minute pairs remain unordered, which is why the DFS cycle check exists as the backstop.
- F: `memory_seed/semantic_cache.py`, `memory_seed/core.py`, `memory_seed/cli.py`, `memory_seed/mcp_server.py`, `.memory-seed/skills/session_logging.md` + seed twin, `tests/test_memory_seed.py` (5 new links-check tests + `_flat_session` helper), `tests/test_semantic_cache.py` (2 new graph tests, `_entry` helper extended), `tests/test_mcp_server.py` (1 new exposure test), `CHANGELOG.md`, `docs/todo/supersession-edges-plan.md` (status → P1 core implemented), `docs/todo/NEXT_STEPS.md`.
- T: Full suite 227/227 (up from 219). Discrimination check: cycle guard temporarily disabled → cycle test fails; restored → passes. `memory-seed doctor` healthy, `links check` clean (23 files). Live smoke test of `link show` on a real entry shows the two new rows. `session_logging.md` twins byte-identical via `filecmp`.

### Follow-up

- Harmony-contract dampening intentionally NOT built here — it lands with `interaction-frequency-ranking-plan.md` P1b, which is now unblocked.
- Next per ship order: commit-linking P1 (`Memory-Entry:` trailer, `commits:` field, git-gated validation, `link commits`), then ranking P1a.

## 2026-07-03 22:30 - Implement git commit entry linking P1

```yaml
entry_id: mse_9fxr60m6kbg98fdk
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_mk82r25x7mvb1aqa
```

### Summary

- Second increment of the code batch: git commit <-> decision entry linking P1 per `git-commit-entry-linking-plan.md` with the 2026-07-03 sign-offs (`Memory-Entry:` trailer key; full 40-character SHAs required in the stored field).

### Decision

- D: `MemoryChunk` gained a `commits` tuple (same generic YAML parse as `supersedes`). `check_session_links()` validates `commits:` entries: `malformed-commit-hash` for anything not a full 40-char lowercase SHA (format-checked even outside git), and `unknown-commit` via `git cat-file -e <sha>^{commit}` only when a `.git` directory is present AND git responds — `_commit_exists()` returns None on missing/unresponsive git and the check skips, never fails. New read-only `memory-seed link commits <entry_id>` prints the stored field plus `find_trailer_commits()` results (`git log --all --grep="Memory-Entry: <id>" --format="%H %s"`), degrading to a "skipped" note outside git. Convention documented twice: a new Working Principles bullet in `agent-rules.md` (trailer + same-turn-only field backfill, convention not hook) and a `commits` schema paragraph in `session_logging.md`, both with byte-identical seed twins.
- R: As specced; the dual-source read path means trailer-only coverage works when the backfill window has closed — proven live before tests were even written: the two trailers dogfooded onto earlier commits today (`9c07fae`, `b67be26`) resolve through `link commits` against this repo.
- A: Considered backfilling this very entry's `commits:` field as a full-loop dogfood; skipped because the SHA doesn't exist until after the commit is made, and appending it post-commit would need a second noise commit — exactly the chicken-and-egg the trailer (and the deferred P2 reminder hook) exists to solve. The trailer on this increment's commit carries the link instead.
- F: `memory_seed/core.py` (`_commit_exists`, `find_trailer_commits`, `commits:` scan + validation in `check_session_links`), `memory_seed/semantic_cache.py` (`MemoryChunk.commits`), `memory_seed/cli.py` (`link commits` subcommand), `.memory-seed/agent-rules.md` + seed twin, `.memory-seed/skills/session_logging.md` + seed twin, `tests/test_memory_seed.py` (5 new tests + git-fixture helper), `tests/test_semantic_cache.py` (1 parse test), `CHANGELOG.md`, `docs/todo/git-commit-entry-linking-plan.md` (status → P1 implemented), `docs/todo/NEXT_STEPS.md`.
- T: Full suite 233/233 (up from 227; the git-fixture tests run `git init`/`commit` in temp dirs with inline `-c user.*` config). Twins byte-identical via `filecmp`. `doctor` healthy, `links check` clean. Live `link commits` smoke test resolves both existing dogfooded trailers.

### Follow-up

- Remaining in the batch: ranking P1a (raw `related_degree` exposed via `link show` and `memory_get_chunk` metadata) — after which P1b (supersession-aware `importance_score`) is fully unblocked since `supersedes` edges now exist.
