---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-25
---

## 2026-07-25 04:40 - Cache loader tolerates schema drift instead of 500-ing the Trail

```yaml
entry_id: mse_xpqy4kxjevdd8ehj
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/cache-loader-schema-drift
topics:
  - memory-trace
  - bugfix
evolves:
  - mse_h9tq5r49000nc7eq:d1
```

### Summary

- Made `_chunk_from_storage` tolerant of a cache written by an older binary, so a stale projection degrades to a rebuild instead of 500-ing the whole Trail.

### Decision

- D: The cached-chunk loader no longer trusts the stored shape blindly. A renamed field is canonicalised (`supersedes` → `replaces`, merged without duplicates) so its edges survive; any other unknown key is dropped rather than passed to `MemoryChunk(**data)`. A stale cache therefore degrades to a rebuild, never a hard crash.
- R: A pre-rename cache carrying `supersedes` raised `unexpected keyword argument` and took the entire Trail down (500 on every request) until the cache file was found and cleared by hand — observed live 2026-07-24 when an old orphaned server's cache was read by the new binary. The cache is a rebuildable projection (Constitution: every DB/cache is derived), so a crash on READ is strictly worse than a stale read: the stale read loses at most one field until the next rebuild, the crash loses the UI. Canonicalising the renamed field also honours the vocabulary rename's standing promise to read the old spelling forever, which the cache reader was silently violating.
- A: Bump a cache schema version so a shape change invalidates the cache proactively — the more thorough fix, but bigger, and it does not help a cache already on disk from a mismatched binary; deferred. Just deleting the cache on mismatch — rejected as more destructive than needed when the data is readable after canonicalisation.
- F: `memory-trace/memory_trace/service.py` (`_chunk_from_storage`, `fields` import).
- T: `memory-trace/tests/test_incremental_projection.py::ChunkStorageSchemaDriftTests` — reverting the fix reproduces the exact live `TypeError: unexpected keyword argument 'supersedes'`; with it, the renamed field canonicalises, merges without duplicates, and an unknown key is dropped. 207 memory-trace tests pass.

## 2026-07-25 11:13 - Topic vocabulary: raise the cap to 4, defer decision-level keying

```yaml
entry_id: mse_jz0pwv0ngzzxr484
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/topic-cap-and-decision-proposal
topics:
  - memory-seed
  - proposal-lifecycle
evolves:
  - d1 -> mse_zm2h343r4shfre3j:d1
```

### Summary

- Assessed JNL's three topic-vocabulary ideas against corpus data: raised the inferred-topic cap to 4, wrote a proposal deferring decision-level topic keying, and rejected a third axis.

### Decisions

#### D1 - Raise the inferred-topic cap to 4 to match the corpus, not hold the machine stricter than the author

- D: `MAX_INFERRED_TOPICS` goes from 3 to 4 — the corpus authored maximum. The topic-sidecar overreach check fires at 5+.
- R: JNL's intuition that multi-decision entries touch more topics is confirmed by measurement: 2- and 3+-decision entries average 2.76 / 2.60 topics vs 2.10 for single-decision, and authored entries reach 4. The sidecar cap was 3, so an inferred fourth topic on a genuinely four-facet entry would be rejected — the inference path held to a stricter standard than the author holds themselves, which is backwards. The cap is kept (not removed): a label applied to everything distinguishes nothing, and the effect is bounded — nothing in the corpus exceeds 4.
- A: A decision-aware cap (`min(4, 2 + decisions)`) — rejected as more machinery for a ~0.6-topic difference; a flat 4 matching the corpus max is simpler and correct. Removing the cap entirely — rejected: the restraint guard is the reason it exists.
- F: `memory_seed/core.py` (`MAX_INFERRED_TOPICS`), `tests/test_links_check.py` (cap test now asserts 4 passes, 5 is overreach; test vocab widened), `docs/3_Spec/graph-edge-contract.md`.
- T: 922 backend tests pass; corpus links check clean.

#### D2 - Defer decision-level topic keying to a proposal, and reject a third axis

- D: Keying each topic to its decision (`topic:dN`) is written up as a proposal and deferred, not built. A third mandatory topic axis is rejected. The proposal doc also records the live topic-inference conventions (two-axis model, cross-cutting concerns as rare add-ons, the cap) — the durable home for what the backfill swarm prompt teaches.
- R: `topic:dN` would mirror the `:dN` lifecycle grammar and unify the multi-decision question elegantly, but topics are a fuzzy associative signal where per-decision precision fights their purpose (a lifecycle edge is a precise claim; a topic is soft aboutness). The swarm already struggles with the coarse version (28% exact on the blind pilot), so per-decision attribution would make the 206-entry backfill less reliable, at full-stack cost (grammar, parser, chunk shape, every consumer) for a thin payoff. A third axis has no home in the vocabulary beyond three low-use cross-cutting slugs (encoding/perf/security, ~11 uses total) and would inflate topics/entry past the measured 2.26 norm. The `:dN` precedent makes decision-level topics cheap to add later if a concrete consumer needs it (e.g. tinting Trail decision rows by topic).
- F: `docs/2_Todo/decision-level-topics-proposal.md` (new).
- T: `docs check` clean on the new doc; `docs index` regenerated.

### Validation

- 922 backend tests, corpus `links check`, and `docs check` all green.

## 2026-07-25 11:28 - Reframe: decision-level topics as the enabler of a decision-level graph

```yaml
entry_id: mse_pww2f69g5ket96h6
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/docs/decision-level-graph-vision
topics:
  - graph
  - proposal-lifecycle
evolves:
  - mse_jz0pwv0ngzzxr484:d2
```

### Summary

- Reframed the decision-level-topics proposal around JNL's vision of a decision-level graph, and recorded the de-risked two-step sequencing.

### Decision

- D: Decision-level topics are not built now, but the proposal is reframed: they are the colouring/clustering layer that a DECISION-LEVEL GRAPH (each node a decision, not an entry) requires — not standalone precision. The plan is sequenced: (1) render the decision-node graph using the substrate that already exists (`_expand_decision_rows` and `:dN` decision-edges already power the Trail's decision rows; the graph endpoint just does not call them), with each decision inheriting its entry's topics for colour; (2) build per-decision topics only if inherited colouring proves too coarse.
- R: JNL's framing supplies the concrete consumer the earlier deferral was waiting for, and it aligns with the project thesis that decisions are the unit of memory. But per-decision topic INFERENCE is the risky part — the swarm manages only 28% exact on the coarse entry-level version — so building it first would be building the hardest, least reliable piece against an unproven view. Rendering the decision-graph with inherited topics proves the value cheaply and may suffice on its own; if it does not, the muddy colouring is the demonstrated need to build per-decision topics against a graph already shown to want them. This also dissolves the entry-level topic-cap debate — the natural unit becomes ~2 topics per decision.
- A: Build per-decision topics first (the literal reading of the idea) — rejected: it front-loads the unreliable swarm work against an unvalidated consumer. Couple the two into one big build — rejected: decoupling lets step 1 stand alone and de-risks step 2.
- F: `docs/2_Todo/decision-level-topics-proposal.md`.
- T: docs check clean; docs index regenerated. No code change — this is a planning decision.

## 2026-07-25 12:17 - Lifecycle-link judgment swarm becomes a durable repo skill

```yaml
entry_id: mse_pndcc168895x59ex
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/link-swarm-skill
topics:
  - memory-seed
  - control-plane
related_entries:
  - mse_6s46whjzghew9w08
```

### Summary

- Turned the proven lifecycle-link judgment swarm into a durable, invocable repo skill, resolving the swarm proposal's open orchestration question.

### Decision

- D: The link-swarm pipeline (mechanical recall via `link audit --json` → haiku fan-out judgment → orchestrator validation → batch human approval → `:dN` edge writes → `links check`) is now a repo skill, `link_swarm.md`, registered as an OPTIONAL skill under a new "curation" profile. The runbook captures the six judging rules measured against the 68-edge campaign, the mechanical-first validation (verbatim quote grounding, ordinal/id existence, forward-only, consistency), and the Constitution alignment (network only in this optional layer; every stored edge human-gated and model-independent).
- R: JNL chose durable orchestration, as a skill, over all audited pairs. A skill is the durable, discoverable home — the agent loads it when running a link-enrichment campaign — and it makes the one-off 68-edge run repeatable. It is optional, not core, because the swarm operates on a mature corpus with lifecycle-edge gaps: a fresh `memory-seed init` project has nothing to sweep, so seeding it universally would be noise. The "curation" profile is the natural future home for the topic-curation swarm as well. The batch-approval gate and the mechanical-first validation are what keep the network-using swarm inside the Constitution's model-independence and network-free-core invariants.
- A: A saved Workflow script with no skill wrapper — rejected: less discoverable; the user asked for a repo skill. A core skill — rejected: not universal. Full literal seed integration was done (SEED_FILES, profile, registries, pyproject, the hardcoded seed-file-list test) despite the swarm being this-repo-focused, to keep the seed mirror exact and the manifest tests green.
- F: `memory_seed/seed/.memory-seed/skills/link_swarm.md` + live copy, `memory_seed/core.py` (SEED_FILES, SKILL_PROFILES "curation", SKILL_DESCRIPTIONS), both `index.md` registries, `pyproject.toml` package-data, `tests/test_project_lifecycle.py`.
- T: 922 backend tests pass; `skills list` shows link_swarm under the curation profile; doctor healthy; the new file is encoding-clean.

## 2026-07-25 13:41 - Decision-level related_entries: generalize the decision-ref grammar

```yaml
entry_id: mse_jkfnrc6r9jwr353k
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/decision-level-related
topics:
  - graph
  - memory-seed
evolves:
  - mse_h297nf3qghp7ysyk
```

### Summary

- Generalized the decision-ref grammar from {replaces, evolves} to include related, so `related_entries` may carry `:dN` — laying the groundwork for the link-swarm to emit decision-level related in one corpus pass.

### Decision

- D: `related_entries` now accepts decision-level refs (`mse_x:d2`, `d1 -> mse_x:d2`), validated exactly like a lifecycle decision ref and rendered on the target's decision row — but ALLOWED, not MANDATED. A bare related ref to a multi-decision target stays valid; the granularity mandate (name the decision when 2+) remains lifecycle-only. The canonical decision-edge kind for related is `related`; the entry-level list key stays `related_entries`.
- R: JNL's economic call: the expensive step is the multi-million-token swarm run over ~1,106 corpus pairs. If we run it once, it should emit the finest granularity the grammar allows, so the corpus never needs a second pass to add decision-level related. The related-track pilot confirmed the swarm reasons at decision level already (its `why` names "D5", "Slice 4 deferred") — it just had nowhere to put it, because grammar v2 had kept related entry-level. Related is allowed-not-mandated because related is a casual associative signal; forcing `:dN` on hand-authored related edges would be friction for no benefit, while the swarm can opt into precision.
- A: Keep related entry-level and run a second decision-level swarm later — rejected: doubles the largest cost for work we can front-load once. Mandate decision-level related like lifecycle — rejected: over-formalizes a casual signal and breaks hand-authoring ergonomics.
- F: `memory_seed/core.py` (append validation unified; links-check entry-yaml + sidecar passes collect/validate related decision refs; mandate advisory gated lifecycle-only), `memory_seed/semantic_cache.py` (extractor peels related :dN into decision_edges), `memory_seed/retrieval.py` (sidecar reader decision_kind; related_of unions related decision edges), `memory-trace/memory_trace/service.py` (kind map: related), skill + seed, decision-refs spec, CHANGELOG.
- T: 924 backend tests pass; two old tests asserting rejection flipped to the new contract; new coverage for append accept/dangling/no-mandate, links-check valid/dangling, and a Trail related edge landing on the decision row. Corpus `links check` clean; end-to-end append of a `related_entries: mse_x:d2` verified.
