---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-22
---

## 2026-07-22 00:21 - Clickable decision diagrams, ESR coverage, and the sidecars owed for 2026-07-21

```yaml
entry_id: mse_3gg9grc7ecrj7a6j
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/diagram-viewer-and-esr-coverage
topics:
  - memory-trace
  - ui-design
```

### Summary

- Decision diagrams are clickable in the React client (badge or reader figure -> zoom/pan modal),
  `esr` reports diagram coverage, and the three sidecars owed for 2026-07-21 are written.

### Decisions

#### D1 - Report diagram coverage as a count, never as a verdict

- D: `esr` gains a "Decision diagrams" section: how many of the session's entries carry a sidecar,
  and the date of the last sidecar anywhere. No opinion on whether any given entry needed one.
- R: JNL noticed a full day with no diagrams. Measuring showed it was not one bad day: 12 diagrams on
  2026-07-16 (the day the feature was built), then **zero across five days and 131 entries**, with the
  explain-the-skip half peaking at 5-of-20 on 07-15 and fading too. So the convention never became
  load-bearing, and nothing anywhere reported that.
  I first built the obvious thing - a keyword heuristic over the skill's own positive-trigger list -
  and measured it before shipping: it flagged **35% of all entries**, matching "worktree" in a passing
  validation line and "topology" in a feature name. A check that fires on one entry in three teaches
  its reader to skip it. It was dropped.
- A: The deciding argument was not the measurement but this module's own docstring - "Judgment stays
  with the agent - this reports, it never fixes." A trigger heuristic would have `esr` making the
  authoring judgement, which is the one thing it promises not to do. The bare fact is enough: the
  lapse is only visible as a RUN of empty days, which no single day's view shows, and that is exactly
  what "last sidecar anywhere" surfaces.

#### D2 - Clickable diagrams in React, reusing the vanilla class names

- D: A diamond badge in the Trail, or a diagram figure in the reader, opens a modal viewer with
  zoom/pan, Fit, Escape-to-close and backdrop-to-close. Zoom maths lives in `diagramZoom.ts` as pure
  functions; the class names and geometry are lifted verbatim from the vanilla stylesheet.
- R: JNL asked for parity with the vanilla JS. The React badge was an inert `<span>` whose tooltip
  said "open the entry to view" - and opening the entry only rendered the diagram inline in the
  inspector column, which is narrower than most authored diagrams. So a wide diagram was effectively
  unreadable in the React client with no way to enlarge it. Reusing vanilla's class names rather than
  inventing new ones means one stylesheet change tracks both UIs instead of them drifting.
- A: The badge fetches the entry's own chunk rather than requiring selection first, because the point
  of a preview badge is to look WITHOUT navigating away from what you are reading; it reuses the
  loaded chunk when it is already the selected entry. Zoom is extracted to a pure module for the same
  reason as graphLayout/graphEdges: the arithmetic (clamping, fit-scale, zoom-about-cursor) is where
  the bugs are and none of it needs a DOM.

#### D3 - Diagnosed the wrong cause, and was corrected by the user mid-turn

- D: The viewer's bad first render was a MISSING CSS RULE - `.diagram-stage .diagram-svg { width:
  100%; height: auto; }`, which I dropped when porting the vanilla styles. Restored.
- R: Seeing the modal render nodes strung out in a wide unreadable row, I concluded the diagrams
  themselves were at fault: `arc2d` has genuinely zero `subgraph` support (verified), and my sidecars
  used the tier-subgraph layout the `compact_mermaid_diagrams` skill prescribes, so "the house style
  is unsupported by the renderer" was a tidy story. JNL said plainly that the inspector renders the
  diagrams correctly - the same diagrams, the same renderer - which falsified it in one line. The
  difference was never the source; it was that the inspector's inline `.diagram-svg` sizes with
  `max-width` plus `overflow-x: auto`, while the viewer's stage needs `width: 100%` because the stage
  is what scales.
- A: I had already rewritten the three diagrams before the correction. They are kept, because the
  subgraph finding IS true and independently worth acting on - `arc2d` cannot draw the construct the
  skill tells authors to use, so a subgraph diagram silently loses its grouping in both UIs. That is a
  live contradiction between `compact_mermaid_diagrams.md` and the renderer, recorded here and NOT
  fixed in this entry.
  The process lesson is the recurring one for the day: a plausible story built from one observation,
  corrected by someone who checked a second surface. The cheap test I skipped was comparing the
  inspector and the viewer on the same diagram before theorising.

### Validation

- 644 backend tests, 87 client unit (5 new in `diagramZoom.test.ts`), 7 axe a11y, typecheck, build.
- `links check` 58 files OK - the new sidecars validate against `malformed-diagram` /
  `orphan-diagram` / `diagram-date-mismatch`, filed under the ENTRIES' date (2026-07-21), not the
  authoring date.
- Live: 3 badges render as real `<button>`s; the modal opens with `role="dialog"`, `aria-modal`,
  rendered SVG and the three zoom controls; zoom changes the transform, Fit restores it, Escape and
  backdrop-click both close; each badge opens its own entry's diagram. After the CSS fix the viewer
  SVG measures 778px in an 827px viewport instead of its intrinsic width.

### Follow-up

- `compact_mermaid_diagrams.md` prescribes tier subgraphs; `arc2d` does not parse `subgraph` at all,
  so those diagrams lose their grouping silently in both clients. Either the skill narrows to the
  supported subset or the renderer grows subgraph support - unresolved, and it affects every future
  sidecar author plus both seed twins.

## 2026-07-22 00:36 - Reconcile roadmap, durable facts and audit with the 2026-07-21/22 tranche

```yaml
entry_id: mse_61bnt9ty6rfgpw1e
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/docs/roadmap-refresh-2026-07-22
topics:
  - documentation
  - memory-seed
```

### Summary

- Reconciled the roadmap, the runtime index's durable facts, and the functionality audit against what
  the 2026-07-21/22 tranche actually shipped, and sequenced the open threads it generated.

### Decisions

#### D1 - The refresh records open threads, not just completed boxes

- D: `0_NEXT_STEPS.md` gains a themed "Shipped 2026-07-21/22" block AND a numbered "Open threads from
  this tranche" list: the two silent-corruption paths in ref extraction, decision-level link refs,
  semantic scoring for `link audit`, the skill-vs-renderer subgraph contradiction, cross-session
  `branch:` contamination, and the unreconciled ADR corpus table.
- R: Ticking done-boxes is the easy half and the less valuable one. Everything in that list exists
  today ONLY inside session-entry `Follow-up` sections - which are append-only, chronological, and
  effectively invisible once the day scrolls past. The roadmap is the document anyone reads to decide
  what to do next, so a thread that never reaches it is a thread that rots. Two of the six are live
  data-integrity defects, not ideas.
- A: 26 entries were theme-grouped rather than transcribed as 26 changelog lines; the changelog
  already exists for that. Each "shipped" claim was verified against the session log and code rather
  than against the file's own prior text, per the file's own stated methodology - which is how the
  now-false "incremental-ingest remains deferred" claim was caught in BOTH the header blockquote and
  the Current-state bullet.

#### D2 - Left every version stamp alone

- D: Did not reconcile the version spread - `index.md` says 2.0, `VERSION` says 2.19, the audit's
  frontmatter says 2.20 - despite editing all three files.
- R: Tempting and wrong. The full 644-test suite passes with exactly that spread, so nothing pins doc
  frontmatter to `VERSION` and leaving it is provably safe. More to the point, 2.20 is plausibly
  intentional-in-transition rather than drift: the lense-shim removal was explicitly "targeted at the
  2.20 release", so the audit may be correctly describing the release being assembled while `VERSION`
  lags. From inside these three files I cannot tell whether the fix is audit->2.19 or VERSION->2.20,
  and guessing wrong walks into the control-plane version-bump trap already recorded in memory.
  Content is mine to correct; stamps need the release intent, so they go to JNL instead.

#### D3 - Wrote around a concurrent session rather than through it

- D: Edited `0_NEXT_STEPS.md` in regions disjoint from the concurrent session's in-flight change
  (their bounded-force-motion addition to the B0b topology bullet), stashed the one file I was NOT
  editing before branching, and disclosed that their line rides along in my commit.
- R: Third collision with the same session today, and the first anticipated rather than discovered
  afterwards. Their change was already STAGED in my index from an earlier stash restore, so a plain
  `git add` would have swept it silently - the exact failure that put another session's work on my
  branch this morning. Stashing the roadmap itself was rejected: I have to edit that file, so
  stash-then-pop would manufacture a merge conflict rather than avoid one. Incorporate-and-disclose
  is the same call made for the 22:41 session entry and for the same reason - the content belongs on
  main either way, and the only question is whether its arrival is announced.
- A: Applied the guard shipped 40 minutes earlier: stash unrelated files BEFORE branching, so a dirty
  tree cannot reorder the sequence and displace the append-on-branch step.

### Validation

- `docs check` 167 files, lifecycle OK (14 pre-existing `missing-todo-yaml` warnings); `docs index
  --check` current; 14 control-plane/version/seed-twin tests pass.
- Verified the concurrent session's force-motion lines survive intact alongside both new sections.

### Follow-up

- The version spread (index 2.0 / VERSION 2.19 / audit 2.20) needs JNL's release intent to resolve;
  deliberately untouched here.
- Two of JNL's sessions are co-editing the roadmap. Worth coordinating rather than both writing it.

## 2026-07-22 01:08 - Narrow the compact-Mermaid skill to the subset Arc 2d actually parses

```yaml
entry_id: mse_bz03mjc3mvxk8t68
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/docs/diagram-subset
topics:
  - documentation
  - memory-seed
related_entries:
  - mse_542z3qn0azma9mmx
  - mse_3gg9grc7ecrj7a6j
evolves:
  - mse_4f8g7h2j9k3m5n6p
```

### Summary

- `compact_mermaid_diagrams.md` is rewritten to document only the Mermaid subset Arc 2d actually
  parses, resolving the skill-vs-renderer contradiction filed as a follow-up earlier today. The
  measured subset is narrower, and the failure mode uglier, than the follow-up assumed.

### Decisions

#### D1 - Narrow the skill to the measured subset instead of teaching the renderer `subgraph`

- D: The skill now documents the verified Arc 2d subset — `flowchart`/`graph`/`sequenceDiagram`
  headers, `[]`/`()`/`{}` nodes, `-->` and `-->|label|` edges, `<br/>` label breaks — and forbids
  `subgraph` in sidecars. The renderer is unchanged.
- R: JNL's call, and the measurement supports it. I ran every candidate form through `parseDiagram`
  rather than reading the Mermaid docs, and the subset is small enough that documenting it is a page,
  while widening the renderer to match the docs would mean containers, nested layout and a second
  layout pass in two hand-written twins that currently agree line for line.
- A: The follow-up said grouping is "silently dropped". It is not silent. `subgraph`, `direction` and
  `end` each satisfy the node regex, so they render as **stray rectangles labelled with those
  keywords**: the skill's own six-line tier example produces five boxes, three of them syntax noise.
  I would have written the weaker claim if I had trusted the handoff instead of running it. That
  changed the framing of the whole document — this is a corruption warning, not a degradation note.
- A: Two other findings reshaped the guidance. Every non-`-->` arrow (`~~~`, `---`, `-.->`, `==>`)
  fails to parse as an edge AND **drops its right-hand node from the diagram**, so the skill's old
  rules 2 and 5 — "use invisible `~~~` links to force a grid" — were actively deleting nodes, not
  merely failing to align them. And `flowchart BT` is not honoured: only `LR`/`RL` change the axis,
  so `BT` silently renders top-down. Rule 5 was outside the brief's "replace rules 1-3" but had to go
  for the same reason.
- F: `.memory-seed/skills/compact_mermaid_diagrams.md` and its seed twin (byte-identical, md5
  `912111e8`). The four new Mermaid examples were themselves run through `parseDiagram`: each yields
  exactly its intended nodes and edges with no stray boxes.

#### D2 - Scope the prohibition to sidecars, not to Mermaid everywhere

- D: The ban on `subgraph` applies to `.memory-seed/sessions/diagrams/**` only. Authored Markdown
  under `docs/**` keeps full Mermaid, grouping included.
- R: Checked before asserting: there is no `mermaid` dependency anywhere in `memory-trace`, and the
  only `renderDiagramBlock`/`parseDiagram` call sites render diagram sidecars. So Arc 2d's reach ends
  at the sidecars; `docs/**` diagrams are rendered by whatever full Mermaid engine the reader has,
  where `subgraph` is ordinary valid syntax. A blanket ban would have degraded the docs for nothing.
- A: The skill frames the safe sidecar syntax as the *intersection* of Arc 2d and full Mermaid, so a
  diagram authored for a sidecar also renders correctly on GitHub — which is the rule to follow when
  it is unclear where a diagram will be read.

#### D3 - Move the test's content anchors off the guidance being removed

- D: `test_extracted_lazy_skills_are_registered_seeded_and_standalone` required the literal phrase
  "invisible `~~~` links"; it now requires "`subgraph` Is Not Parsed In Sidecars" and "is the only
  arrow that works".
- R: The test pins representative phrases so a skill cannot be gutted silently. It was pinning the
  exact sentence this change deletes, so it failed by design and the fix is to re-anchor it, not to
  weaken it. The new anchors were chosen to pin the two claims that carry the change — the
  prohibition and the single-arrow rule — so a future edit that quietly reinstates subgraphs trips it.
- A: Left `memory-system-version: 2.19` alone; nothing required a bump and it would be a delta the
  brief did not ask for.

### Validation

- `tests/test_session_schema.py` 21 passed (seed-twin parity); full `tests/` 644 passed.
- `docs check` OK, 167 files, 14 pre-existing `missing-todo-yaml` warnings unrelated to this change.
- Twins verified byte-identical by md5 and `git diff --no-index`, not by inspection.
- `memory-trace/tests` fail to collect with `No module named 'memory_trace'` — pre-existing
  environment gap in this worktree, untouched by a Markdown change.

### Follow-up

- Five already-published sidecars use `subgraph` and are therefore rendering stray `subgraph` /
  `direction` / `end` boxes today: `.memory-seed/sessions/diagrams/2026-07/` for 07-10, 07-12, 07-13,
  07-15 and 07-16. Out of scope here; they need rewriting into the documented subset.
- `.memory-seed/agent-rules.md` also mentions `subgraph`; worth checking it does not re-teach the
  pattern the skill now forbids.

## 2026-07-22 01:17 - Graph scale acceptance measured; communities blocked on undefined detection

```yaml
entry_id: mse_xmtax6v2sn6hybzk
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/b0b-topology-scale
topics:
  - memory-trace
  - graph
```

### Summary

B0b workstream D: close the graph's topology-communities and scale-acceptance slices. The scale half
landed with measurements and a fix that was only findable by measuring. The topology half hit its
declared fallback and is recorded as blocked, not attempted.

### Decisions

#### D1 - Took the fallback on communities rather than inventing a topology design overnight

- D: Did not implement community detection or community colouring. Recorded the blocker with evidence
  instead.
- R: The premise that communities merely needed "making visible" is false. `project_trace_graph`
  (memory-trace/memory_trace/graph_projection.py) assigns EVERY live node
  `community:unassigned` / `derived:unassigned`, with a documented rationale that B0a deliberately
  omits detection so a renderer cannot promote its own clustering to canonical graph data. The
  client's `colourForCommunity` therefore hashes one constant and paints the whole graph one colour -
  measured, `distinctColours` is exactly `["#8f76d4"]` at every size from 52 to 467 nodes. Making
  membership visible needs real detection, and the cheap non-arbitrary candidate does not work:
  connected components over the default edge set (related/supersedes/evolves/topic) at full size give
  5 components of sizes 454, 4, 4, 3, 2 - a hairball, not a partition. Lifecycle-only edges give 27
  components but still a 422-node giant. So detection means choosing and tuning Louvain/Leiden/label
  propagation, and because those are order- and seed-sensitive it also means building §4.3's
  fingerprint + member-overlap retention + `community_previous_id` persistence apparatus, which the
  proposal specifies only as a recommended process. That is the overnight-invention line the
  workstream explicitly told me not to cross.
- A: The exit criterion's stability half passes but VACUOUSLY: 467 shared nodes, 0 changed colour
  across two full-size reloads, because there is one community and one colour. It holds by
  construction from the absence of detection, not from any stability mechanism. Cross-rebuild colour
  retention, algorithm choice, and projection persistence all remain unimplemented and unproven.

#### D2 - Fixed Overview's exhaustion test on the edge axis, not just the node axis

- D: Replaced `graph.nodes.length < overviewLimit` with a growth-based rule in a new pure module
  `graphOverview.ts`, and kept the under-limit test only as the first-fetch case, applied to both
  nodes and edges.
- R: Measuring scale is what exposed it: "Show more" retired itself at 397 of 589 entries, and I
  could not reach full corpus size at all. The server applies the SAME limit to nodes and to edges
  (`_limit(limit, maximum=1000)`), and this corpus saturates its node count long before its edge
  list - limit=540 gives 540 nodes/540 edges/360 rendered, limit=600 gives 555/600/397, limit=660
  gives 555/660/449, limit=1000 gives 555/1000/467. At limit=600 the node count is already saturated
  so the old test fired, while 70 more entries were still reachable purely because more EDGES would
  be served. A pure limit-comparison cannot fix this either: at the server's hard 1000 cap a larger
  ask returns the identical payload forever, so the button would never retire. "Did asking for more
  bring back more, on either axis?" is the rule that both reaches the ceiling and terminates at it.
- A: Full corpus size is 467 of 589 entries, and that ceiling is a server contract, not a shortfall
  of this fix: edges are hard-capped at 1000, and nodes without an `entry_id` or without any edge
  never render. Overview now reaches exactly that ceiling and stops.

### Validation

- Verified against MY build, not the shared server: rebuilt the packaged assets in this worktree
  (`npm run build` -> memory-trace/memory_trace/static/react/) and ran `python -m memory_trace.cli`
  from the worktree on port 8791. Port 8756 was never touched, and the harness config carries no
  `webServer` block, so it cannot silently fall back to the main checkout.
- `npx tsc --noEmit` clean; `npm test` 95/95 (8 new `graphOverview` cases); `npm run build` clean.
- Playwright harness `memory-trace/client/e2e-scale/` drives the real UI through every "Show more"
  step to exhaustion; raw numbers in `e2e-scale/measurements.json`.

- A: `layoutIterations` needs no adjustment - the measurements say the scaling function is doing its
  job. Left it alone rather than tuning on a hunch.
- F: memory-trace/client/src/graphOverview.ts (new), memory-trace/client/src/graphOverview.test.ts
  (new), memory-trace/client/src/App.tsx, memory-trace/client/e2e-scale/ (new harness + measurements),
  memory-trace/memory_trace/static/react/ (rebuilt assets).
- T: Full corpus size measured at 467 nodes / 1000 edges (589 entries; 555 returned, 467 connected),
  first paint into Graph view 1105ms. Layout at that size: 1177ms at the shipped WARM budget (145
  iterations), 2130ms at the shipped COLD budget (289), 4962ms unscaled at 900 - so `layoutIterations`
  cuts the full-size block by 2.3x and holds. Warm is the number production actually pays, because
  `settledPositions` is module-level and every step after the first small layout is warm-seeded; cold
  only validates the scaling curve. Growth curve at the shipped cold budget: 52 nodes 41ms, 134 nodes
  222ms, 276 nodes 1245ms, 397 nodes 1597ms, 467 nodes 2130ms. Interaction at full size: zoom 33/34ms,
  pan 37ms, fit 33ms - flat from 52 to 467 nodes and sitting on the ~33ms double-rAF measurement
  floor, so no degradation is detectable. The one metric that DOES grow is selection restyle, 5-13ms
  below ~400 nodes but spiking to 47-102ms at 1000 edges, where `applyPresentation` plus
  `outrankedEdgeIds` sweep the whole edge set on every selection. Community colour stability: 467
  shared nodes, 0 colour changes across two reloads, 1 distinct colour - vacuous, see D1.

### Follow-up

- Communities remain unimplemented. The blocking design questions are: which detection algorithm and
  at what resolution (connected components are measurably useless here); and whether the
  fingerprint/overlap/`community_previous_id` retention of proposal §4.3 lives in the backend
  projection or a renderer-side colour-slot cache. `community_colour_slot` is already in
  `RENDERER_OWNED_FIELDS`, which suggests identity backend-side and slot client-side, but that split
  needs ratifying before code.
- Selection restyle at 1000 edges is the next real scale cost. `outrankedEdgeIds` recomputes over
  every edge on every selection though its result only depends on the edge set and the visible-type
  filter; memoising it on those two inputs would remove it from the selection path.
- The 1000-edge server cap is now the binding limit on how much of the corpus Overview can show.
  Whether that cap should rise is a product question, not a client one.

## 2026-07-22 01:21 - Clear the control-plane references to the banned subgraph technique

```yaml
entry_id: mse_y8eds7vvbcshxvcp
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/docs/diagram-subset
topics:
  - documentation
  - memory-seed
evolves:
  - mse_bz03mjc3mvxk8t68
```

### Summary

- Cleared the follow-up filed 13 minutes earlier: the control plane described the compact-Mermaid
  skill in terms of the technique that entry had just banned. Two live files plus both seed twins.

### Decision

- D: `agent-rules.md`'s skill description no longer reads "using tiers, grids, subgraphs, and
  invisible links" — it now names the sidecar subset and states that `subgraph` and non-`-->` arrows
  do not render. The `skills/index.md` `load_when` trigger "needing compact rectangular Mermaid layout
  with tiers, grids, subgraphs, or invisible links" becomes "authoring a diagram sidecar under
  `.memory-seed/sessions/diagrams/`". Seed twins mirrored.
- R: Rewriting the skill was not sufficient on its own. An agent reads `agent-rules.md` and the
  registry to decide *whether* to load a skill, so both were still advertising the forbidden pattern
  to every author who never opened the file. The `index.md` trigger was the worse of the two: it fired
  on the intent to use subgraphs, which is now precisely the intent that must not be acted on. The
  replacement trigger fires on authoring a sidecar, which is the population that actually needs the
  subset rules.
- A: I had written "worth checking `agent-rules.md`" into the previous entry's Follow-up rather than
  spending the 30 seconds to grep it. The check was cheap and the answer was yes — deferring it was
  the measure-before-asserting rule failing on my own follow-up list. Worse, my first grep for the
  registry used `-A6` and cut off one line above the `index.md` hit, so I recorded "index.md is clean"
  from a truncated window. Re-running it unbounded found both. A negative result from a windowed grep
  is not a negative result.
- A: Copied live over seed with `cp`, which would silently clobber any legitimate live/seed
  divergence. Checked the resulting `git diff` per file before trusting it: exactly one line changed
  in each, so the twins had already been identical. Verifying after the blunt operation is what makes
  it safe to use.
- A: Left `do_not_load_when: editing sequence diagrams without graph or flowchart layout pressure`
  alone. The skill now documents sequence-diagram limits (`: text` required, `Note over` silently
  dropped), so that trigger is arguably too broad, but it is a pre-existing judgement about load cost
  rather than stale guidance, and widening it was outside this change.
- F: `.memory-seed/agent-rules.md`, `.memory-seed/skills/index.md`, and both
  `memory_seed/seed/.memory-seed/` twins.
- T: `tests/` 644 passed; `encoding check` OK (the new description carries an em-dash, and this repo
  has a mojibake history); `docs check` OK, 167 files, same 14 pre-existing warnings.

## 2026-07-22 01:24 - Rank link-audit candidates by semantic similarity

```yaml
entry_id: mse_xfbcr3n4vet62hmh
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/link-audit-semantic
topics:
  - memory-seed
  - performance
```

### Decisions

#### D1 - Semantic similarity ranks link-audit candidates; it never decides membership

- D: `audit_link_gaps` adds `SEMANTIC_OVERLAP_BOOST * cosine(source, candidate)` to the existing
  lexical score, using model2vec embeddings of each entry's full text. The lexical gate (shared
  files, shared title terms, or shared topics when unlinked) still decides which pairs are candidates
  at all; semantic similarity only reorders what got through.
- R: Measured against the 104 supersedes/evolves edges authors declared in entry YAML, recall@5 goes
  60% -> 77% and recall@10 64% -> 82%, with 85/104 -> 97/104 true targets surfaced at all. That is a
  large gain for a term that costs 0.15s to compute across 544 entries.
  The gate/rank split is not a stylistic choice: cosine is DENSE. Every pair of entries has non-zero
  similarity, so using it to decide candidacy would make every earlier entry a candidate for every
  later one - the all-pairs explosion the live spec feared, arrived at from the opposite direction.
  Ranking has no such problem because the candidate set is already bounded by the lexical gate.
- A: Fails open. `_embed_entries_for_link_audit` returns None on ANY failure - no provider, no cached
  model, an embed exception - and the score degrades to lexical rather than raising. That path is not
  hypothetical: `pip install --no-deps memory-seed` is the documented lightweight install, and it is
  now tested explicitly by blocking the model2vec import and asserting link audit still returns
  results with finite scores.

#### D2 - The weight is a measured peak, and measured through the shipped code

- D: `SEMANTIC_OVERLAP_BOOST = 160.0`.
- R: Swept 0/5/10/20/40/80/160, then 160/320/640/1280 because recall was still climbing at the top of
  the first range. recall@5 peaks at 160 (77%) and falls away to 72% by 320 and 62% by 1280, so 160
  is a real optimum rather than the largest value tried. An earlier standalone estimate of ~120 came
  from a reimplementation of the scorer and is superseded by this end-to-end number.
  The weight is large only because the terms are on different scales: cosine is bounded to [0,1]
  while an idf sum is unbounded. It is not a claim that semantics matter 160x more than files.
- A: The harness imports the real `audit_link_gaps` rather than reimplementing its scoring. That is
  the direct lesson from 2026-07-21, when a local copy of the scorer silently diverged from the
  shipped one (it stripped the title timestamp the shipped code kept), so the prototype numbers
  measured something that was never going to ship.
  Method detail that matters: the auditor SUPPRESSES pairs that already carry an edge, so measuring
  against declared edges requires a corpus copy with those declarations stripped - otherwise ground
  truth is invisible to the thing being measured and every weight scores zero.

#### D3 - Retract the spec's "no all-pairs semantic scan" rationale

- D: The live spec's as-built paragraph claimed avoiding a semantic scan was a deliberate design
  choice. Replaced with a dated retraction plus the measurement, and the scoring table gained the
  semantic row.
- R: The implied cost was never measured and does not hold at this corpus size: 2.9s model load, then
  0.15s to embed all 544 entries. Leaving the claim in place would have left a false justification
  standing next to code that now contradicts it - the same stale-rationale problem this document has
  been repeatedly corrected for.
- A: Repaired a self-inflicted formatting fault while editing: the retraction blockquote initially
  swallowed the unrelated `--apply` stub sentence into itself, which would have silently changed
  what that sentence appeared to qualify.

### Validation

- Sweep, end-to-end through `audit_link_gaps` on the real corpus: weight 0 -> 60%/64% recall@5/@10,
  85/104 surfaced; weight 160 -> 77%/82%, 97/104. Declines from 320 upward.
- 644 backend tests pass WITH model2vec installed; link audit separately verified with the model2vec
  import blocked, returning 25 gaps / 125 candidates with finite scores.
- `docs check` 167 files OK.

### Follow-up

- The sweep re-embeds the whole corpus once per `audit_link_gaps` call, so a 7-weight sweep embedded
  580 entries 728 times. Fine for a one-off measurement, wasteful if the sweep is ever rerun in CI -
  a cached vector store keyed on corpus signature would fix it.
- `SEMANTIC_OVERLAP_BOOST` is tuned on one corpus of 580 entries by one author. It should be
  re-measured before anyone treats it as a universal default for other projects.

## 2026-07-22 01:43 - Decision-level sidecar refs reach the Trail decision row; ADR corpus recount

```yaml
entry_id: mse_3e63pfckz4rzkw25
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/ref-grammar-integrity
topics:
  - memory-seed
  - session-logging
  - memory-trace
```

### Summary

- Closed the second half of the ref-grammar workstream: a `<entry_id>:dN` ref authored in a link sidecar now draws to that entry's dN row in the Trail, verified live; and the ADR's corpus table was re-derived from a committed classifier after neither prior figure could be reproduced.

### Decisions

#### D1 - Decision edges are a separate stream, computed after row expansion and outside the visibility filter

- D: Build decision edges in `_decision_edges_for_rows`, called only from the `include_decisions` branch after `_expand_decision_rows`, and append them to `visible_edges` after the `limited_ids` filter rather than through it.
- R: Two independent reasons converged on the same placement. The ratified draft forbids projecting a decision edge into entry-level `supersedes`/`evolves` - "D2 of B supersedes D1 of A" does not license "B supersedes A" - so it cannot go through `build_related_entry_graph` or the sidecar union. And `limited_ids` is keyed on entry ids, so a decision row id (`{entry_id}#decisions/dN-<slug>`) is never a member: routing the edge through that filter would have dropped every one of them silently, which is the same failure mode as the parser bug this workstream exists to close, one layer up.
- A: Considered tagging each edge with a `decision: true` flag so the payload distinguishes it. Rejected: `GraphEdge` is a pydantic response model, so the field would have dragged in the v1 contract, `openapi.v1.json` and `types.ts`, and the endpoint ids already carry the distinction. No frontend change was needed at all - `trailModel.ts` keys rows by `node.id`, so a decision-row endpoint resolved on its own.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/tests/test_trail_decision_edges.py`.
- T: 6 new tests. 3 fail against the pre-change `service.py` (verified by swapping in the HEAD copy and re-running); the other 3 are no-regression guards that should pass either way, the strongest being a set equality of the non-decision edge payload against a control corpus with the sidecar deleted.

#### D2 - The focus view expands over a decision edge's two entries, as membership only

- D: When an entry is focused, feed `_neighborhood` an extra entry-to-entry pair per decision edge so the far entry is displayed. The drawn edge still terminates on the decision row and nothing is added to entry-level edge lists.
- R: Found by measuring rather than reasoning: after upgrading the real ref from `mse_2m56qnk1473vmjz1` to `:d2`, focusing the source entry made the target vanish entirely, because the neighbourhood is computed from the entry-level edge set that no longer contained it. Upgrading a ref to a more precise statement must not cost visibility, or authors will decline to upgrade. Membership answers what to render; it is not an assertion that B evolves A.
- F: `memory-trace/memory_trace/service.py`.
- T: `test_focused_view_still_reaches_the_far_end_of_a_decision_edge`, which fails against the old code.

#### D3 - A dangling ordinal on an expanded entry draws nothing, rather than falling back to the entry row

- D: Fall back to the target's entry row only when that entry genuinely has no decision rows (the singular-`### Decision` shape, where the draft says the entry-level and `d1` forms denote the same edge). If the entry is expanded and the ordinal is not among its rows, drop the edge.
- R: The first cut used `decision_row.get(...) or entry_row.get(...)`, which quietly widened a bad `:d7` into an entry-level edge - reintroducing the exact overstatement the feature removes, in the one case where the author had already made a mistake. `links check` errors on `dangling-decision-ref` so this cannot reach a clean corpus, but the renderer should not depend on the validator having run.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/tests/test_trail_decision_edges.py`.
- T: `test_dangling_ordinal_on_an_expanded_entry_draws_nothing`.

#### D4 - The ADR corpus table is re-derived from a committed script, and disagrees with both prior counts

- D: Ship `scripts/count_decision_shapes.py` and amend the ADR table in place to 138 numbered / 383 singular / 1 inline / 42 no-decision = 564 entries, 521 with an addressable decision, 115 multi-decision, 710 addressable decisions in total.
- R: The ADR's 2026-07-20 hand count (612 entries) and the 2026-07-21 recount (580) were both unreproducible, and the honest finding is that the new numbers match neither. The mechanism turned out to be two entry splitters over byte-identical files: `_ENTRY_HEADING_RE` requires a `HH:MM` stamp and yields 564, while the semantic-cache entry extractor accepts the date-only `## YYYY-MM-DD - title` headings the May-2026 entries use and yields 589 - which is exactly the `entry_count` Memory Trace displays, so that figure has been on screen the whole time. 580 reproduces under neither splitter, on this branch or on the primary checkout. The script prints both totals for that reason: a single number without its splitter is what made the original table unciteable.
- A: The 521/115 figures do reproduce exactly, which is the corroboration that matters - they come from the shipped `_entry_decision_ordinals`, so coverage claims and validation cannot drift apart.
- F: `scripts/count_decision_shapes.py`, `docs/3_Spec/draft/adr-lifecycle-sidecar-contract.md`, `docs/3_Spec/draft/decision-level-link-sidecar-refs.md`.
- T: `python scripts/count_decision_shapes.py` re-run against this worktree and against the primary checkout (565 there, the one extra being its own close-out entry) - the difference is corpus drift, not classifier disagreement.

#### D5 - Reverted the demonstration edge rather than bypassing the append-only guard

- D: Restore `.memory-seed/sessions/links/2026-07/2026-07-21.md` to its published state and land the rest, instead of forcing the merge or authoring a substitute pair.
- R: Two rejected alternatives make the reasoning: a plain `git merge` would bypass a guard that was correctly identifying a real rule violation, and inventing a fresh 2026-07-22 pair (this entry "evolving" some decision) to satisfy the exit criterion would put a fabricated lifecycle edge into the corpus - a false record, which is precisely what this workstream exists to prevent. Reverting is the least opinionated move available: it restores the status quo and changes no design.
- A: The real finding is the gap itself. An entry-level edge that overstates can be *narrowed* only by editing a published block, which append-only forbids, so today the overstatement is permanent once written. A retraction or supersession mechanism for sidecar edges is the follow-up this exposes; it is a corpus-rule decision, not something to settle inside a rendering change.
- F: `.memory-seed/sessions/links/2026-07/2026-07-21.md` (restored, net zero).
- T: `session merge-branch --dry-run` clean after the revert; `links check` clean.

### Validation

- 650 root tests and 184 memory-trace tests pass; `links check` and `docs check` clean (warnings only, all pre-existing).
- Live verification, the exit criterion: ran a Memory Trace server from this worktree on port 8791 rather than trusting the one on 8756, which serves the primary checkout and would not show this branch. `/api/v1/runtime` confirmed `workspace_root` = `wt-fix-ref-grammar-integrity`, and the worktree selector showed `ref-grammar-integrity` selected. In the Trail, selecting the 2026-07-21 13:22 entry and loading back to 2026-07-16 produced an SVG `path` with `stroke=var(--edge-evolves)` and `marker-end=url(#trail-arrow-evolves)`, tooltip "Overview ranks by connectivity across all dates, with a coverage indicator evolves D2 - Graph overview defaults to current connected work", running from the source row to the D2 row. The arrow lands on the decision, not on the entry heading.
- The demonstration sidecar edit was then REVERTED, and the corpus is unchanged by this branch. `session merge-branch` refused it: link sidecars are append-only across a merge, and narrowing a published entry-level edge to `:d2` is an in-place modification of a published block, not an append. The corpus has no sanctioned operation for that, and the guard is right to refuse - a bypass would be the same category of silent rewrite this workstream exists to stop. The prose comment on the block already records the D2 relationship in words, exactly as before. The verification it served is discharged; the code, tests, script and doc amendments are independent of it and land.

### Follow-up

- Source-side decisions (`source_decision:` on the sidecar block) remain unbuilt - step 3 of the draft's implementation order. All four granularity combinations are specified; only the target side ships here.
- The sister draft's chunk-addressability table (381/131/309, dated 2026-07-21) measures chunks rather than entries and was left alone; stale-but-not-wrong beside the new entry counts.

## 2026-07-22 07:47 - Render diagram sidecars with Mermaid itself

```yaml
entry_id: mse_zwzdjn0m9e34gdth
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/mermaid-renderer
topics:
  - memory-trace
  - ui-design
  - documentation
evolves:
  - mse_bz03mjc3mvxk8t68
```

### Summary

- Diagram sidecars are rendered by Mermaid 11 itself in the React client, replacing `arc2d.ts`, the
  hand-written flowchart/sequence subset renderer. Trace's Drawn/Slick toggle and light/dark theme
  now drive Mermaid's `look` and palette. The authoring skill is rewritten from a subset restriction
  into a diagram-type catalogue plus an ADR diagram expectation.

### Decisions

#### D1 - Adopt Mermaid rather than reproduce it

- D: `mermaid` becomes a `memory-trace` client dependency and `DiagramView` renders through it.
  `arc2d.ts` and its 16 tests are deleted. JNL's framing was "faithfully reproduce what Mermaid does
  using the official documentation, covering all types of available graphs"; the counter-proposal
  taken instead was to use Mermaid.
- R: Mermaid ships ~30 diagram types over dagre/ELK layout engines. Reproducing that from the docs
  is not writing a renderer, it is rebuilding Mermaid, and it would never converge. The constraint
  that had forced hand-writing was the vanilla client's lack of a build step, and JNL removed it in
  the same message by saying the vanilla twin can go.
- R: The deciding argument is JNL's and it is a Constitution one: a sidecar is Markdown, read in VS
  Code and on GitHub as well as in Trace. Both of those run real Mermaid. A diagram that is correct
  in the standard renderers failing in ours is OUR defect - the Markdown is the source of truth and
  every renderer is a projection over it. That reframes the subset parser as the bug rather than the
  spec.
- A: I had argued the opposite the day before, that adopting Mermaid would mean fighting it to keep
  the Trace design language. That was wrong on the facts: Mermaid 11 has `look: handDrawn`
  (rough.js) and `theme: base` + `themeVariables`. Checking it took one search, and it was the
  strongest objection I raised.

#### D2 - Trace's own settings drive Mermaid, and the palette is read, not duplicated

- D: `mermaidConfig.ts` maps `trailStyle.style` (hand|slick) to `look` (handDrawn|classic) and
  resolves `themeVariables` from CSS custom properties via `getComputedStyle` on `:root`.
- R: JNL asked for the Drawn setting to auto-switch the diagram style; the theme binding comes free
  from the same call. Reading tokens rather than duplicating hex keeps `styles.css` the single
  source of truth, so retuning a token moves the diagrams with it and the two cannot drift. Mermaid
  only accepts hex, not `var(...)`, which is exactly why it has to be resolved at call time.
- A: Measured, not asserted. The same diagram renders 16 paths in Slick against 38 in Drawn -
  rough.js lays multiple strokes per shape. Edge stroke resolves to `#70624d` in light and
  `#8b9a93` in dark, both `--muted` in their theme.

#### D3 - The htmlLabels claim was false, and the test that guarded it could not have caught that

- D: `flowchart.htmlLabels: false` is removed rather than left set, and the test that pinned it now
  asserts only `securityLevel: "strict"`.
- R: Mermaid 11 ignores the flag here. Rendering a real sidecar produced one `<foreignObject>` per
  node - 8 nodes, 8 foreignObjects - while `look` and the palette from the same config object both
  took effect, so the config reached Mermaid and this option specifically was not honoured. Leaving
  the flag set would read as a guarantee the code does not have.
- A: The failure mode matters more than the flag. My test asserted `config.flowchart.htmlLabels ===
  false` - it checked this module's own output, not Mermaid's behaviour, so it passed while the
  claim it appeared to guard was false. Same shape as the 2026-07-21 scorer bug: a check that can
  only confirm what the code already says is not a check. The security boundary is DOMPurify via
  strict mode (a direct Mermaid dependency, verified present), since `DiagramView` sets innerHTML.

#### D4 - The skill becomes a type catalogue with an ADR diagram expectation

- D: `compact_mermaid_diagrams.md` drops the subgraph prohibition and the `~~~` warning, gains a
  table of ~14 diagram types with when-to-reach-for-which, and states that an ADR sidecar should
  normally carry a diagram - "no diagram" being the exception needing a reason.
- R: JNL's reasoning: a decision worth recording as an ADR almost always has a shape, and that shape
  is the justification. The wider type vocabulary is what makes this practical - a lifecycle change
  wants `stateDiagram-v2`, a branch change wants `gitGraph`, and drawing either as a flowchart loses
  the thing that made it worth drawing.
- A: Scope is guidance only. `memory-seed` gains no dependency, so `pip install --no-deps
  memory-seed` is exactly as light as before; Mermaid lives entirely in the Trace client.
- A: The pinning test was re-anchored for the second time in two days - yesterday onto "`subgraph`
  Is Not Parsed In Sidecars", today off it. Yesterday's anchors were chosen to pin the subset rule,
  which this entry reverses.

### Validation

- Live, against the real corpus on the packaged app: the 2026-07-12 sidecar with three `subgraph`
  blocks, `direction LR` and two `~~~` links renders three labelled containers ("Install choices",
  "Published package", "Runtime behavior") with ZERO stray keyword boxes. Under arc2d the same
  source produced boxes labelled `subgraph`, `direction` and `end`.
- Server identity checked before trusting the verification: the running server served
  `index-Dh_1rLdy.js`, the exact hash the build had just emitted.
- Modal viewer fit resolves to `scale(0.98)` with a real offset, so it measured rendered content
  rather than an empty stage - the async-render fix.
- 650 root tests, 184 memory-trace tests, 87 client tests, typecheck and build all pass.
  `docs check` 167 files OK; `links check` 59 files OK. Warnings pre-existing.
- Bundle: entry chunk unchanged at 248kB (78.6kB gzip). Vite splits Mermaid core and every diagram
  type into separate lazy chunks, so a flowchart entry fetches `flowDiagram` and leaves gantt, C4
  and the rest unfetched.

### Follow-up

- The vanilla `/` client still renders sidecars through its own arc2d twin, so a `subgraph` sidecar
  is correct in `/next`, VS Code and GitHub but shows stray boxes in `/`. The skill documents this
  as transitional. It resolves when the vanilla client retires - which is JNL's call and still open,
  and gated on non-diagram parity items (accessibility focus restoration, screen-reader labels, no
  non-visual equivalent for the Cytoscape canvas).
- The five sidecars authored under the subset rule (07-10, 07-12, 07-13, 07-15, 07-16) now render
  correctly without being touched. Nothing to repair; recorded so the earlier follow-up is closed.
- I altered persisted UI settings while verifying (inspector dock, theme, trail style) and restored
  them to dark + Drawn + inspector-right. The dock had been left `hidden` by my own toggling, not by
  JNL.

## 2026-07-22 19:17 - Name graph communities after authored topics

```yaml
entry_id: mse_3yvakpxdshc95e68
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/topic-communities
topics:
  - memory-trace
  - graph
  - ui-design
evolves:
  - mse_xmtax6v2sn6hybzk
```

### Summary

- Graph communities are now named after authored topics, unblocking the item `mse_xmtax6v2sn6hybzk`
  recorded as blocked. The choice removes proposal §4.3's retention apparatus rather than
  implementing it. Selection restyle at full size is memoised off the selection path in the same
  pass.

### Decisions

#### D1 - Communities come from authored topics, not from structural detection

- D: A node's community is its most DISTINCTIVE authored topic - the one with the lowest corpus-wide
  frequency among those clearing a floor of 10 - falling back to its next qualifying topic, and
  `unassigned` when it has none. Louvain/Leiden are not implemented.
- R: This is what makes proposal §4.3 unnecessary rather than pending. Its fingerprint,
  member-overlap comparison and `community_previous_id` retention exist for exactly one reason:
  detected community ids are unstable across rebuilds. A topic slug is already a stable identity, so
  the fingerprint IS `topic:<slug>` and colour retention is a property of the identity rather than an
  algorithm layered over it. It is also authored data rather than renderer-inferred, which is the
  side of the Constitution line this project keeps choosing.
- A: Coverage is honestly partial. 351 of 561 rendered nodes (63%) carry a qualifying topic; the
  other 210 are genuinely unassigned and take a neutral tone rather than a palette colour, so
  "no community" reads as absence rather than as a sixteenth category. That ceiling is a property of
  the corpus, not of the rule: 200 of 555 entries carry no topics at all.
- A: This answers the second blocked question too. Identity is backend-side and the colour slot is
  renderer-side - the split `RENDERER_OWNED_FIELDS` already implied by listing
  `community_colour_slot`. Nothing about community colour is persisted in the projection.

#### D2 - Most distinctive, not most common, and never first-listed

- D: `min` by (corpus frequency, name). Ties break alphabetically so the result is deterministic.
- R: Both alternatives were measured on the live graph and rejected on the numbers. Most-common puts
  46% of the graph in a single community - the same giant-component hairball that made connected
  components useless when this was first attempted. Rarest without a floor fragments into 55
  communities, 42 of them below 8 members, which is dust rather than structure. Rarest with the floor
  gives 15 communities sized 42/42/37/34/27/27/23/18/17/17/15/15/14/12/11 - largest 12%, no giant, no
  dust.
- A: First-listed looked like the best option on balance alone (32 communities, largest 23%) and I
  nearly took it, on the theory that authors put the primary topic first. Checking killed it: topic
  lists are stored alphabetically sorted, 304 of 304 multi-topic nodes. Its balance was an accident
  of the alphabet and it carries no authored meaning whatever. The measurement that mattered took one
  command and reversed the choice.
- A: The floor is not tuned. 10 and 12 produce an identical partition, so the rule sits on a plateau
  rather than a knife edge; a test pins both.

#### D3 - Frequencies must be corpus-wide, which is why they live on the service

- D: New `TraceService.topic_frequencies()`, memoised per cache generation, passed into
  `project_trace_graph` as an explicit argument. Omitting it preserves the previous single
  `unassigned` community.
- R: `project_trace_graph` only ever receives the LIMITED subset a request returned. Counting
  frequencies there would let a node change community as more of the graph loaded - re-creating, from
  a new direction, the precise instability that authored-topic identity was chosen to avoid. Making
  the argument additive also keeps the bounded benchmark fixtures valid unchanged.
- A: Verified rather than argued: 0 of 100 shared nodes change community between limit=100 and
  limit=561, and 0 change across a full service rebuild. Both non-vacuous this time - 15 distinct
  communities, where the earlier stability pass held only because there was one community and one
  colour.

#### D4 - Selection restyle memoised on its two real inputs

- D: `outrankedEdgeIds` is cached on the graph's edge-array identity plus the visible-type filter key.
- R: The prior entry measured it as the one metric that grew with corpus size - 5-13ms below ~400
  nodes, 47-102ms at 1000 edges - because every selection swept the whole edge set. The result never
  depended on the selection, only on those two inputs, so the sweep leaves the selection path
  entirely. Keying on array identity rather than a content hash means a reload cannot serve a stale
  set.

### Validation

- In-process against the real corpus: 15 communities, 351/561 coloured (63%), largest 42.
- Stability: 0 community changes from limit=100 to limit=561; 0 across a full rebuild.
- Live through the restarted packaged server on the real endpoint: same 15 communities, and the graph
  renders 10 distinct colours on the default 52-node view against a previously measured
  `["#8f76d4"]` - one colour for the entire graph.
- 195 memory-trace tests pass (11 new, pinning the rule and both rejected alternatives); client
  typecheck, 87 tests and build clean.

### Follow-up

- The 16-slot palette is greedy: three of its entries are near-identical blues (`#6688e8`,
  `#7b93e0`, `#5f8fd6`), so blue dominates a view that should read as several distinct communities.
  It needs retuning against the warm palette rather than extending by variants, which is what I did.
- Community LABELS are not surfaced anywhere yet - no legend, no filter, no hover. The colours are
  currently uninterpretable without reading the API. A legend keyed to topic names is the obvious
  next slice and is what turns this from decoration into navigation.
- Six of §4.2's eight named clusters appear (graph, ui-design, documentation, release, retrieval,
  mcp-tools). "Tests" and "provider integrations" do not, because no entry carries a topic for them.
  Whether that is a gap in the topic vocabulary or in the work is a corpus question, not a code one.
- 37% of rendered nodes stay neutral. Raising that means authoring topics on the 200 topic-less
  entries, not changing the rule.
