---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-04
---

## 2026-07-04 01:43 - Implement ranking P1a raw related_degree exposure

```yaml
entry_id: mse_wh33mc61swqx0vkv
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_9fxr60m6kbg98fdk
```

### Summary

- Final increment of the green-lit code batch: ranking P1a per `interaction-frequency-ranking-plan.md` — the raw `related_degree` signal exposed read-only, with default `memory_search` ranking untouched.

### Decision

- D: `memory-seed link show` now prints a `related_degree` line and `memory_get_chunk` returns a `related_degree` integer — both defined as `len(node.inbound)` from `build_related_entry_graph()` (inbound backlinks only), reusing the graph node already fetched for `superseded_by` so no extra graph build. Not blended into any ranking math.
- R: As specced by P1a; the graph machinery from supersedes P1 made this a near-trivial addition.
- A: The plan also lists "Memory Lense graph node metadata" as a P1a exposure surface — deliberately NOT done, because implementation revealed a naming collision: Lense graph nodes already ship a `related_degree` field (`_related_degrees()` in `lense.py`, Lense V1) counting **combined inbound+outbound** explicit edges as a display-connectivity metric, a different number than this plan's inbound-only importance precursor. Silently redefining a shipped UI field under the same name (or shipping two same-named different numbers side by side) would be worse than deferring; the reconciliation is now flagged in the plan doc as a prerequisite for P1b's Lense exposure, under the P0 shared-graph-contract work.
- F: `memory_seed/cli.py` (one line in `link show`), `memory_seed/mcp_server.py` (`related_degree` in the get_chunk payload), `tests/test_mcp_server.py` (1 new test: cited entry scores 2, citing entry scores 0), `CHANGELOG.md`, `docs/todo/interaction-frequency-ranking-plan.md` (status → P1a implemented + collision note), `docs/todo/NEXT_STEPS.md`.
- T: Full suite 234/234. `doctor` healthy, `links check` clean. Live smoke test: `link show` on a real entry reports `related_degree: 2` matching its two inbound backlinks.

### Follow-up

- The green-lit code batch is complete: supersedes P1 (`b67be26`), commit-linking P1 (`4f7d9c2`), ranking P1a (this commit). All three queued under `## Unreleased`.
- Next candidates, in order: settle the Lense `related_degree` naming collision (small, part of the P0 graph contract), then ranking P1b (supersession-aware `importance_score` + the shared harmony-contract fixture), then the exclude-superseded filter.

## 2026-07-04 02:28 - Push code batch and verify 2.14.0 on PyPI

```yaml
entry_id: mse_7etgfz5qay3zs8fj
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_wh33mc61swqx0vkv
  - mse_205vnae3pk91r1xn
```

### Summary

- Pushed the three code-batch commits (`b67be26`, `4f7d9c2`, `954c744`) to `origin/main` on the user's instruction (clean fast-forward, 3 ahead / 0 behind). User confirmed they had approved the `pypi` deployment; verified the publish run `28683120449` completed (both jobs green) and PyPI serves `memory_seed-2.14.0-py3-none-any.whl` and `memory_seed-2.14.0.tar.gz`. Release 2.14.0 is fully out.

### Validation

- `git fetch` before push showed no divergence; `git status -sb` clean after. `gh run view` both jobs ✓; PyPI JSON for `memory-seed/2.14.0` lists wheel + sdist.

### Follow-up

- Next working batch when green-lit: Lense `related_degree` naming reconciliation → ranking P1b → exclude-superseded filter → 2.15.0.

## 2026-07-04 10:18 - Rename related_degree collision: connectivity + inbound_relation_count

```yaml
entry_id: mse_07vwfbgsk31ec7d6
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_wh33mc61swqx0vkv
```

### Summary

- Resolved the `related_degree` naming collision flagged during ranking P1a. User's call: rename the Lense field to `connectivity`, and rename the backend field to something descriptive of what it measures. Established (before editing) that the two are genuinely different metrics — Lense's counts combined inbound+outbound `related_entries` edges (a node-sizing connectivity weight), the backend's counts inbound only (the directional importance/attention precursor) — so this is a two-way rename to distinct names, not "keep the newer one."

### Decision

- D: Lense's graph-node `related_degree` → `connectivity` (computation unchanged: `_related_degrees()` → `_connectivity_degrees()`, the `_graph_node` param/dict key, and the `app.js` node-radius consumer). Backend's inbound-only signal `related_degree` → `inbound_relation_count` on `link show` and `memory_get_chunk`. Both renames complete across code, frontend JS, and tests.
- R: The names now describe what each computes. `connectivity` reads as a graph-topology display weight; `inbound_relation_count` reads as "how many entries reference this one," which is exactly `len(node.inbound)`. Renaming was free of external-contract risk: the backend field was unreleased (ranking P1a, unshipped), and the Lense field, though shipped in 2.13.0, is a local read-only UI whose frontend ships in lockstep with the backend — no third-party consumer.
- A: Considered aligning Lense to the inbound-only definition instead (one metric everywhere). Rejected: Lense genuinely wants the combined-degree number for node sizing (a node connected in either direction should draw larger), so collapsing to inbound-only would degrade the visualization to serve a naming convenience. Keeping two well-named metrics is correct.
- F: `memory_seed/cli.py`, `memory_seed/mcp_server.py`, `memory_seed/lense.py`, `memory_seed/lense_static/app.js`, `tests/test_mcp_server.py`, `tests/test_lense.py`, `CHANGELOG.md` (P1a bullet reworded + new Lense-rename bullet), `docs/todo/interaction-frequency-ranking-plan.md` (P1a heading/body + collision note now marked resolved + DoD checkboxes), `docs/todo/NEXT_STEPS.md`, `docs/functionality-audit.md` (§14 refreshed: 5 of 6 logic-capture items now implemented, not 2).
- T: `node --check app.js` clean; full suite 234/234; `doctor` healthy; `links check` clean; live `link show` reports `inbound_relation_count: 2`. Grep confirmed no `related_degree` source references remain (only stale `.pyc` caches).

### Follow-up

- P1b is now fully unblocked with no naming prerequisite left. Needs a concrete dampening multiplier chosen (plan Open Decision 3; my standing suggestion: start at 0.25).
- Then the exclude-superseded filter, then a 2.15.0 cut folding all of Unreleased.

## 2026-07-04 10:39 - Implement ranking P1b supersession-aware importance_score

```yaml
entry_id: mse_yjpvx6yp6ek50whk
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_07vwfbgsk31ec7d6
```

### Summary

- Implemented ranking P1b: the supersession-aware `importance_score`, completing both the ranking plan's P1 and the supersession-edges harmony contract. This is the final graph increment before the exclude-superseded filter and a 2.15.0 cut.

### Decision

- D: Added `SUPERSEDED_IMPORTANCE_DAMPING = 0.25` and an `importance_score` field on `RelatedEntryNode`, computed in `build_related_entry_graph()` as `len(inbound)` dampened by the multiplier iff `superseded_by` is non-empty. Exposed read-only on `link show` (with a "(superseded: dampened)" flag) and in `memory_get_chunk` metadata. Not blended into default `memory_search` ranking.
- R: Directly implements the harmony contract from `supersession-edges-plan.md`: importance = inbound `related_entries` count only; supersession never folds into the count; the dampener is a post-hoc override; superseded entries deprioritize but stay fully retrievable. Chose 0.25 for the open multiplier (plan Open Decision 3) — a superseded entry drops to a quarter of its raw weight, strong enough that a well-cited retired decision (4 inbound → 1.0) sits below a live moderately-cited one (2 inbound → 2.0) without erasure. Tunable; read-only-only, so no ranking-stability risk.
- A: Considered exposing `importance_score` on Lense graph nodes too (the collision-resolution note cleared that prerequisite). Deferred: Lense doesn't surface importance at all yet, node sizing already uses `connectivity`, and the P1b DoD only requires the CLI/MCP read surfaces (matching where `inbound_relation_count` landed). Adding a Lense importance overlay is its own UI increment, not P1b plumbing. Also considered adding the optional commit-reference term to the score now that commit-linking P1 shipped; left out of scope until there's a demonstrated need, per the plan.
- F: `memory_seed/semantic_cache.py` (constant + `RelatedEntryNode.importance_score` + builder computation), `memory_seed/cli.py` (`link show`), `memory_seed/mcp_server.py` (`memory_get_chunk` payload), `tests/test_semantic_cache.py` (2 tests: undampened + the DoD dampened-below-live fixture), `tests/test_mcp_server.py` (importance assertions on the existing test + a new dampened-when-superseded test), `CHANGELOG.md`, `docs/todo/interaction-frequency-ranking-plan.md` (status → P1a+P1b implemented, Open Decision 3 resolved, DoD checkboxes), `docs/todo/supersession-edges-plan.md` (status + DoD checkboxes + Open Decision 4 resolved), `docs/todo/NEXT_STEPS.md`, `docs/functionality-audit.md`.
- T: Full suite 237/237. Discrimination check: disabling the dampener makes the DoD fixture fail; restored, it passes. Live `link show` reports `importance_score: 2.00`. `doctor` healthy, `links check` clean.

### Follow-up

- `supersession-edges-plan.md` P1 is now fully done (its last open DoD item, the harmony dampening, landed here).
- Next: the exclude-superseded `memory_search` filter (`exclude-superseded-filter-plan.md`), then a 2.15.0 cut folding the whole Unreleased section (supersedes P1, commit-linking P1, ranking P1a+P1b, the connectivity rename, and the earlier guidance/fanout batch).
- Deferred/optional: commit-reference term on `importance_score`; Lense `importance_score` overlay; the P0 shared graph-edge contract doc.

## 2026-07-04 11:06 - Organize completed proposal docs and refresh audit

```yaml
entry_id: mse_r1ey59bbv1jsseth
user_initials: JNL
agent_type: codex
agent_name: solo-founder
project_path: .
subproject_path: null
related_entries:
  - mse_yjpvx6yp6ek50whk
```

### Summary

- User asked to review the git tree, organize `docs/` based on completed work, move done proposals into `docs/todo/completed/` where appropriate, and update `docs/functionality-audit.md`.

### Decision

- D: Moved the implemented logic-capture proposal docs from active `docs/todo/` into `docs/todo/completed/`: `git-commit-entry-linking-plan.md`, `supersession-edges-plan.md`, and `interaction-frequency-ranking-plan.md`. Left genuinely active/deferred docs in active `docs/todo/`: related-entries P2, exclude-superseded filter, DOCX render seed lessons, UI/Pillar B distribution, 3.0 plan, and source/synthesis reports.
- D: Updated roadmap links and status language in `docs/todo/NEXT_STEPS.md`, `docs/todo/Memory-Seed Logic Capture Improvement.md`, `docs/todo/exclude-superseded-filter-plan.md`, `docs/todo/3.0-plan.md`, `docs/todo/codex/proposal-synergy-evaluation.md`, and the completed fanout plan. `exclude-superseded-filter-plan.md` is now explicitly unblocked because `superseded_by` exists.
- D: Refreshed `docs/functionality-audit.md` to current state: 2.14.0 is the current published release, the graph/commit/ranking work is unreleased through `a72ffc3`, completed docs point at `docs/todo/completed/`, `link show`/`link commits` surfaces are described, and the test surface is updated to 237 tests.
- R: The git tree showed `main` clean except for being two commits ahead of `origin/main` before this docs cleanup; recent session entries confirmed `supersedes` P1, commit-linking P1, ranking P1a, ranking P1b, Mermaid guidance, failed-approach logging, and fanout guidance had all been implemented or shipped. Keeping their proposal docs in active `docs/todo/` made the roadmap look less done than the code and session log say it is.
- A: Did not move `exclude-superseded-filter-plan.md`; it is now startable but not built. Did not move `docx-render-windows-seed-lessons.md`; it remains a proposed seed-skill promotion. Did not move `user-interface-deep-research-report.md`; the Pillar B distribution decision remains open.
- F: `docs/todo/completed/git-commit-entry-linking-plan.md`, `docs/todo/completed/supersession-edges-plan.md`, `docs/todo/completed/interaction-frequency-ranking-plan.md`, `docs/todo/NEXT_STEPS.md`, `docs/functionality-audit.md`, `CHANGELOG.md`, `docs/todo/Memory-Seed Logic Capture Improvement.md`, `docs/todo/exclude-superseded-filter-plan.md`, `docs/todo/completed/agent-fanout-workflow-plan.md`, `docs/todo/3.0-plan.md`, `docs/todo/codex/proposal-synergy-evaluation.md`.
- T: `python -m memory_seed.cli links check` clean before appending this entry and clean again after appending it; `python -m memory_seed.cli doctor` healthy; `python -m unittest tests.test_session_schema` passed 18 tests.

### Follow-up

- Changes remain uncommitted; `main` was already ahead of `origin/main` by two commits before this docs-only cleanup.

## 2026-07-04 15:08 - Implement exclude_superseded memory_search filter

```yaml
entry_id: mse_dbt6c32kk5dk55xs
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_yjpvx6yp6ek50whk
  - mse_r1ey59bbv1jsseth
```

### Summary

- Implemented the last graph increment, the opt-in `exclude_superseded` filter on `memory_search`, completing all 6 logic-capture proposals. On resuming, found a concurrent Codex session had done a large roadmap-hygiene reorg (moved the three implemented plan docs to `docs/todo/completed/`, refreshed the audit/CHANGELOG/NEXT_STEPS, dropped a new inbox worktree/tmux/dependency proposal). Followed this repo's own concurrent-writer discipline before touching anything.

### Decision

- D: Added `exclude_superseded: bool = False` to `rank_session_memory`. When set, it builds the related-entry graph, collects entry_ids with a non-empty `superseded_by`, and drops those chunks from the candidate set for that query only (before ranking, so top_k still fills from survivors). Wired it into `memory_search`'s MCP input schema (default `false`) and dispatch. Chose parameter name `exclude_superseded` (plan Open Decision 1) — descriptive and matches the plan/CHANGELOG references. Backend-only; no CLI/UI default, per the plan's "never a default, never a hard exclusion unless requested" constraint.
- R: The plan was fully specced and unblocked once `superseded_by` shipped (supersedes P1). Filtering pre-ranking (not post-top_k) keeps the surviving result set full and doesn't touch default ranking math — consistent with "opt-in narrowing like date_from/date_to."
- A: Before implementing, committed the concurrent Codex doc-reorg pile as its own checkpoint (`2f6d0d6`) rather than sweeping it into this feature commit or reverting it — got a clean baseline and preserved Codex's work (it had its own session entry, mse_r1ey59bbv1jsseth). Verified the reorg was healthy (links/doctor clean) and that my P1b status edits survived the plan-doc moves before committing it. Considered building the superseded set from the already-extracted corpus to avoid a second graph build; kept the simpler `build_related_entry_graph(cwd)` call since it's read-only and correctness-obvious.
- F: `memory_seed/semantic_cache.py` (`exclude_superseded` in `rank_session_memory`), `memory_seed/mcp_server.py` (schema + dispatch), `tests/test_mcp_server.py` (2 paired tests: default returns the superseded entry, flag drops it — mutually discriminating), `docs/todo/completed/exclude-superseded-filter-plan.md` (moved from `docs/todo/`, status → implemented, sibling links fixed), `CHANGELOG.md`, `docs/todo/NEXT_STEPS.md`, `docs/functionality-audit.md` (§3I filter list, roadmap "all 6 implemented", test count 239).
- T: Full suite 239/239. `doctor` healthy, `links check` clean (24 files). The two search tests discriminate: with the flag off the superseded entry is present, with it on the entry is absent while the current one stays.

### Follow-up

- All six logic-capture proposals are now implemented. The whole graph/ranking arc (supersedes P1, commit-linking P1, ranking P1a+P1b, connectivity rename, exclude_superseded) plus the earlier guidance/fanout batch sits under `## Unreleased`, ready for a 2.15.0 cut.
- Unpushed: 5df5d92, a72ffc3, 2f6d0d6 (Codex reorg), and this filter commit — origin/main is at 954c744.
- Deferred/optional: commit-reference term on `importance_score`; Lense `importance_score` overlay; the P0 shared graph-edge contract doc; the new inbox worktree/tmux/dependency proposal (Codex, proposal-only).

## 2026-07-04 15:12 - Cut 2.15.0 release

```yaml
entry_id: mse_4b5hn5e92ag56q15
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_dbt6c32kk5dk55xs
```

### Summary

- Pushed the four unpushed commits to `origin/main`, then cut the 2.15.0 release folding the whole graph/ranking + guidance/fanout Unreleased batch.

### Decision

- D: Bumped control-plane `2.14` → `2.15` and package `2.14.0` → `2.15.0` via the established byte-level frontmatter script (49 control-plane files, skipping `archive/`/`sessions/`/`docs/`/`demo/`), plus `core.py VERSION`, `pyproject.toml`, the version test pin, `index.md` Active State, and the `functionality-audit.md` header/frontmatter (handled manually since the script skips `docs/`). Folded the CHANGELOG `## Unreleased` into `## 2.15.0 - 2026-07-04` with a fresh empty `## Unreleased` placeholder (keeps the standing-header doc test green).
- R: The user approved "push and cut." 2.15.0 bundles: supersedes P1, commit-linking P1, ranking P1a+P1b, the `connectivity`/`inbound_relation_count` rename, and the `exclude_superseded` filter — all six logic-capture proposals — plus the earlier guidance (Mermaid, failed-approaches) and fanout collaboration recipe that were queued but unreleased.
- F: 49 control-plane `.md` (version frontmatter), `memory_seed/core.py`, `pyproject.toml`, `tests/test_memory_seed.py` (version pin), `.memory-seed/index.md`, `docs/functionality-audit.md`, `CHANGELOG.md`.
- T: Full suite 239/239 (version-lockstep test passes at 2.15). `doctor` healthy (no version-mismatch warnings), `links check` clean. Release created via `gh release create v2.15.0`; publish workflow + PyPI verification recorded in the follow-up.

### Follow-up

- Watch the publish run to the `pypi` approval gate; the user approves the irreversible PyPI push.
- Next roadmap is all deferred/optional (Option B telemetry, commit-reference term, Lense importance overlay, P0 graph-edge contract) plus Codex's proposal-only inbox docs.

## 2026-07-04 15:38 - Add proposal lifecycle skill and refine worktree dependency plan

```yaml
entry_id: mse_7n25phrhv9kdnnc0
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_r1ey59bbv1jsseth
  - mse_4b5hn5e92ag56q15
```

### Summary

- Restored the docs lifecycle after `docs/todo/` had been nested under `docs/inbox/todo/`, created a
  refined active proposal for the inbox worktree/tmux/dependency strategy, moved completed/resolved
  proposal material into `docs/todo/completed/`, and added a seeded proposal lifecycle skill.

### Decision

- D: Added `proposal_lifecycle.md` to the live runtime and seed templates, registered it in both
  skill registries, added it to `SEED_FILES`, package data, runtime index, seed inventory tests, and
  `CHANGELOG.md`. The skill codifies the `docs/inbox/` -> `docs/todo/` ->
  `docs/todo/completed/` flow, required status block, completion criteria, and roadmap/audit update
  surfaces.
- D: Promoted the inbox worktree/tmux/dependency source into
  `docs/todo/worktree-dependency-strategy-plan.md` with priority P3 in the roadmap ordering: after
  shipped collaboration and graph metadata work, before any deferred scaffold. The raw source was
  retained in `docs/todo/completed/worktree-dependency-control-plane-source.md` with a resolved-source
  status.
- D: Moved `related-entries-generation-plan.md` into `docs/todo/completed/` because P1 shipped in
  2.13.0 and the remaining P2 is explicitly deferred. Updated current roadmap/audit links plus
  sibling completed-plan links.
- D: Kept `MEMORY_SEED_LENSE_CACHE_ROOT` as a compatibility alias for Lense cache placement after
  the full suite exposed that existing env-var mismatch; `MEMORY_SEED_CACHE_DIR` remains the
  preferred generic variable.
- R: The user asked for a refined proposal moved to todo, all complete proposals moved to completed,
  and a skill that enforces proposal movement through inbox -> todo -> completed. Treating the raw
  inbox document as a completed source artifact preserves provenance while keeping inbox free of
  accepted actionable work.
- A: Did not move historical/source reports (`3.0-plan.md`, UI research, logic-capture source) just
  because they contain shipped material; they still carry open context or historical decisions. Did
  not implement the worktree dependency strategy itself; the request was to create the refined active
  proposal and lifecycle skill.
- F: `.memory-seed/skills/proposal_lifecycle.md`,
  `memory_seed/seed/.memory-seed/skills/proposal_lifecycle.md`, both `skills/index.md` files,
  `.memory-seed/index.md`, `memory_seed/core.py`, `pyproject.toml`, `tests/test_memory_seed.py`,
  `tests/test_session_schema.py`, `docs/todo/worktree-dependency-strategy-plan.md`,
  `docs/todo/completed/worktree-dependency-control-plane-source.md`,
  `docs/todo/completed/related-entries-generation-plan.md`, `docs/todo/NEXT_STEPS.md`,
  `docs/functionality-audit.md`, `CHANGELOG.md`, `memory_seed/lense.py`, and completed-plan sibling
  links.
- T: `python -m unittest tests.test_memory_seed tests.test_session_schema` passed 171 tests;
  `python -m unittest tests.test_lense.LenseServiceTests.test_chunk_api_accepts_encoded_path_chunk_ids`
  passed after the env alias fix; full `python -m unittest discover tests` passed 239 tests;
  `python -m memory_seed.cli links check` clean (24 files); `python -m memory_seed.cli doctor`
  healthy.

### Follow-up

- Worktree dependency strategy remains proposed documentation work in `docs/todo/`; the first
  implementation should update `agent_collaboration.md` and the seed twin, not add automation.

## 2026-07-04 17:50 - Extract UI design source learnings into inbox

```yaml
entry_id: mse_z6349bztr0v5vrtr
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_7n25phrhv9kdnnc0
```

### Summary

- Read the local PDF `_OceanofPDF.com_Designing_user_interfaces_-_Michal_Malewicz.pdf` with
  `pdfplumber` because MarkItDown was not installed in the bundled runtime, then captured useful
  UI-design learnings as a source note in `docs/inbox/`.

### Decision

- D: Added `docs/inbox/designing-user-interfaces-source-learnings.md` as `status:
  source-learnings`, not an active proposal. It summarizes practical principles for Memory Lense and
  UI work: system-level UI coherence, tested patterns before novelty, perception/hierarchy, grid and
  spacing rules, purposeful color, reduced typography sets, labeled icons, consistent interactive
  states, form reliability, navigation clarity, purposeful animation, microcopy, design systems,
  periodic UI audits, and design/developer QA handoff.
- D: Included candidate Memory Seed applications for later triage: Memory Lense UI audit checklist,
  design-token baseline, microcopy pass, generated-UI handoff evidence, and component consistency
  rule.
- R: The PDF includes purchaser/license-identifying text and copyrighted book content, so the inbox
  note intentionally omits account/license details, avoids long excerpts, and preserves only
  summarized source learnings.
- A: Did not create a persistent full-text Markdown conversion or cache; sampled chapter text
  directly with `pdfplumber` and retained no extraction artifact. Did not promote the learnings into
  `docs/todo/`; that requires later proposal triage.
- F: `docs/inbox/designing-user-interfaces-source-learnings.md`.
- T: `python -m memory_seed.cli links check` clean before appending this entry; no `tmp/` or
  `.cache/markdown/` extraction directory existed after the work.

### Follow-up

- If promoted, keep the scope narrow: likely either a Memory Lense UI audit checklist or a Memory
  Lense design-token baseline.

## 2026-07-04 20:01 - Graph-edge contract, commit_reference_count, Lense importance toggle

```yaml
entry_id: mse_ay5tctkhx0218sn9
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
related_entries:
  - mse_dbt6c32kk5dk55xs
  - mse_r1ey59bbv1jsseth
```

### Summary

- Attacked the three deferred items the user picked as "fully scoped and light" (with the Lense overlay chosen as a size-by-importance toggle): the P0 shared graph-edge contract doc, the commit-reference signal, and the Lense importance sizing. On resume, a concurrent Codex session had again added a pile (a new seeded `proposal_lifecycle.md` skill wired into SEED_FILES/pyproject/skills-index + twins, the worktree proposal processed into a plan, related-entries plan moved to completed); verified it healthy and committed it as a checkpoint (`7f22bad`) before building, per the now-standard concurrent-writer discipline.

### Decisions

#### D1 - Graph-edge contract as a standalone reference doc

- D: Wrote `docs/graph-edge-contract.md` — one reference for the edge kinds (`related_entries`/`supersedes`/`commits`), derived metrics (`inbound_relation_count`, `importance_score`, `connectivity`, `commit_reference_count`), the canonical reader (`build_related_entry_graph`), the validation authority (`links check`), the read surfaces, and four standing rules (read the canonical graph; keep git out of the hot path; expose before you rank; one name one meaning).
- R: Codex flagged this as the P0 precondition; codifying it prevents the four surfaces from drifting and gave me the constraint that shaped D2.

#### D2 - commit_reference_count as a separate field, NOT folded into importance_score

- D: Added `commit_reference_ids(root, entry_id, commits_field)` in `core.py` (union of the entry's `commits:` field and `Memory-Entry:` trailer hits, deduped by SHA, git-gated). Exposed `commit_reference_count` on `link show` and `memory_get_chunk`. `importance_score` left inbound-only and unchanged.
- R: Called the advisor before writing (design-heaviest task). Decisive point: folding a git-derived term into `importance_score` would make the score git-context-dependent — inbound-only (2.0) in search/Lense, inbound+commit (2.5) in link show/get_chunk — the exact "same name, two values" defect the connectivity rename fixed, and a direct violation of the "one name, one meaning" rule I'd just written into the contract. Exposing it as its own read-only count is also strictly lighter (no graph change, no weight constant, reuse `find_trailer_commits`), and matches "expose before you rank": composing it into a score is a later evidence-gated experiment.
- A: The plan's original wording ("extend importance_score with a second free term") — rejected as pre-dating both the hot-path realization and the contract. Recorded the reversal in the ranking plan and the contract doc.
- F: `memory_seed/core.py` (`commit_reference_ids` + `Sequence` import), `memory_seed/cli.py` (`link show`), `memory_seed/mcp_server.py` (`get_chunk`), `docs/graph-edge-contract.md`, `docs/todo/completed/interaction-frequency-ranking-plan.md`.

#### D3 - Lense size-by-importance toggle

- D: Added `importance_score` to Lense graph nodes (`_importance_scores()` mirroring `_connectivity_degrees`, built from one shared graph pass in `graph()`), plus a client-side "Size:" chip toggle in `app.js` that sizes nodes by `connectivity` (default) or `importance_score`, persisted in localStorage.
- R: User's explicit UI choice (size toggle over badge). Kept it additive: connectivity stays the default so existing behavior is unchanged until toggled; the toggle is pure client-side re-render (importance_score already in node data), no reload.
- F: `memory_seed/lense.py` (`_importance_scores`, `_connectivity_degrees` gains optional `graph=`, `_graph_node` gains `importance_score`, `graph()` builds the graph once), `memory_seed/lense_static/app.js` (state + control + radius calc + legend + handler), `tests/test_lense.py`.

### Validation

- Full suite 243/243 (+4: two `commit_reference_ids`, one get_chunk `commit_reference_count`, one Lense `importance_score`). `node --check app.js` clean. `doctor` healthy, `links check` clean. Live `link show` on this batch's prior commit reported `commit_reference_count: 1` (its trailer).
- **Verification split:** the Lense toggle is verified at the data layer (backend `importance_score` per node), the content layer (app.js contains the toggle control, handler, and importance-based radius calc — asserted in tests), and JS syntax. The live *visual* rendering (node sizes actually changing on click) was not exercised in a browser this session — offered as a follow-up if wanted.

### Follow-up

- The only remaining deferred item is Option B (real access-frequency telemetry) — the genuinely heavy one, to be scheduled deliberately.
- Optional live browser check of the Lense size toggle.
- All this batch is unreleased (2.16-bound); nothing pushed yet.
