---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-23
---

## 2026-07-23 07:32 - The diagram repair became a constitutional amendment

```yaml
entry_id: mse_x4hxjf88v3vns5sp
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/sidecar-mermaid-syntax
topics:
  - memory-seed
  - documentation
  - proposal-lifecycle
  - session-logging
evolves:
  - mse_dc48xabd9c2gg6pa
```

### Summary

- The reported broken diagram (23:04 sidecar) turned out to need a constitutional amendment, not a
  code fix. Editing a published sidecar hits append-only (Invariant #2), and `session merge-branch`
  refused it — correctly. Ratified Constitution v1.4 (a narrow, human-gated diagram-syntax-repair
  exception), fixed both broken diagrams, and deferred the broader "editable lens" idea to its own
  proposal.
- This continues [[mse_dc48xabd9c2gg6pa]], whose D1 reasoned the freeze rule did not reach a syntax
  repair. That was right about `session_logging.md`'s freeze but missed that `session merge-branch`'s
  own append-only guard does reach it — which is what forced the amendment.

### Decisions

#### D1 - Land the repair as a ratified exception, not a standing capability

- D: Amended Invariant #2 with a narrow v1.4 exception and landed the fix under it, rather than
  building an `--allow-sidecar-repair` path into the fuse machinery.
- R: §11 is explicit — a change that conflicts with a live invariant "must first amend the invariant,
  it cannot silently override it." Editing a published sidecar is exactly that conflict. The project
  has amended #2 this way twice (1.2, 1.3), both shaped "one-off, never core functionality, no
  standing command." A standing flag is the shape those amendments avoided.
- A: The deciding fact is that the control plane cannot parse Mermaid, so no guard can verify "was
  unrenderable, now renders." Any buildable operation is necessarily broader than "repair a broken
  diagram" — it is "edit diagram content" — which is why it cannot be an unattended standing feature
  and must be human-gated per repair.
- A: Landed via a documented fast-forward (main was already an ancestor), not `merge-branch`. The
  exception sanctions this one in-place edit; deliberately NOT teaching the fuse machinery about it
  keeps "no standing command" true.

#### D2 - Scope the exception to diagram sidecars; defer the broad idea

- D: v1.4 covers *diagram* sidecars only. JNL's broader framing — any sidecar is a refinable lens
  under a conservative editing posture — is filed as its own proposal, not folded in.
- R: "Any sidecar" is not one category. A diagram sidecar owns no authoritative field; a link sidecar
  authoritatively owns typed lifecycle edges, which amendment 1.2 deliberately walls off from
  after-the-fact editing. Generalising in one step would have partially reversed a ratified amendment
  on a bug-fix branch.
- A: Verified rather than assumed, on the advisor's prompt: entry `mse_sewpap8b4q6sb0zj` declares no
  lifecycle edge in its own YAML, yet its link sidecar carries `evolves: mse_emv963patckeftx8`, and
  the Trail sources that edge from the sidecar. So the link sidecar is the sole, authoritative home of
  that edge — editing it edits the decision graph. The distinction is real, and load-bearing.
- A: The deferral is not a rejection. The 2026-07-22 link revert already recorded demand ("no
  operation exists to narrow an over-broad edge"). It earns deliberate design, in
  `sidecar-editable-lens-refinement-proposal.md`.

#### D3 - Fix both broken diagrams, reversing the earlier "leave 07-10 alone"

- D: Repaired the 07-10 flowchart too, converting `DryRun -- no, --dry-run flag --> Plan` to the
  piped form `-->|"no, --dry-run flag"|`. [[mse_dc48xabd9c2gg6pa]] had flagged it out of scope.
- R: Once the exception existed, both published breakages were in scope, and JNL approved fixing both.
  The piped form keeps every label byte, so the diagram depicts exactly what it did — only the edge
  syntax changes, which is why it needs no separate re-authoring judgment.
- A: This is the honest correction of the earlier entry's Follow-up, which said 07-10 was "left alone,
  flagged for JNL." It was, until JNL chose to land both.

### Validation

- Live corpus sweep through the real Mermaid parser (Trace's own config, `securityLevel: strict`):
  56 blocks across 43 diagram-bearing entries, **2 failures before, 0 after**.
- Both diagrams render live: 0 `.diagram-source` fallbacks on the 23:04 entry after a clean reload.
- 369 doc/seed/skill/link tests pass; `session_logging.md` seed twin back in sync; `links check`
  reports integrity OK (60 files), no new warnings.
- Amendment log kept ascending (1.4 after 1.3); no test pins the Constitution version.

### Follow-up

- Prevention is still open and separate: a test that parses every sidecar block would have caught
  both. It is Evolution (no invariant conflict), but `mermaid.parse` under vitest/jsdom is unverified
  (my earlier note), so its feasibility is not assumed. Not built here.
- The broad "editable lens" proposal is filed in `docs/2_Todo/` for a deliberate, own-standing
  decision — shape (diagram refinement / link structure-repair / editable link edges), gate, and
  provenance all open.

## 2026-07-23 08:25 - Diagram viewer: click to zoom, scroll to move, fit stays centred

```yaml
entry_id: mse_g3c3tamy9d449w79
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/diagram-viewer-scroll-zoom
topics:
  - memory-trace
  - ui-design
  - bugfix
```

### Summary

- Two fixes to the multi-diagram modal viewer, both from the wheel doing one job too many. Scrolling
  a multi-diagram view now moves between diagrams; a diagram zooms only after you click it; and the
  fit stays centred across resizes. All from JNL's report.

### Decisions

#### D1 - The wheel scrolls until a diagram is clicked, then zooms

- D: A viewport claims the wheel only once ACTIVE (a click). Inactive, the wheel scrolls the modal
  body between diagrams. An accent ring plus a "Click to zoom" hint signal the state; Escape releases
  an active diagram before it closes the modal.
- R: Every viewport ate the wheel and zoomed, so you could not scroll from one diagram to the next -
  you were stuck zooming whichever one the pointer was over. Gating on an explicit active state gives
  the wheel back to the list until you ask for zoom.
- A: One active diagram at a time, held in the parent, so activating one releases the rest. A press
  activates and arms a drag in the same gesture; panning waits until active.

#### D2 - A native, non-passive wheel listener, because React's onWheel cannot preventDefault

- D: The wheel is attached with `addEventListener("wheel", ..., { passive: false })` on the viewport
  ref, not through React's `onWheel`.
- R: React registers its delegated root wheel listener as PASSIVE, so `event.preventDefault()` inside
  `onWheel` is silently ignored. Measured live: an active diagram zoomed but `defaultPrevented` stayed
  false - it would have zoomed AND let the body scroll underneath at the same time. The original code
  carried the same latent no-op; gating the zoom is what made it bite. A native non-passive listener
  is the only place preventDefault actually holds.
- A: The listener reads `active` through a ref, so it attaches once and never has to re-bind when the
  active diagram changes.

#### D3 - Fit stays centred through a resize, then yields to the reader

- D: A ResizeObserver re-fits the diagram while the view is still "just fitted", and stops the moment
  the reader zooms or pans (an `adjusted` ref). "Fit" re-centres and clears the flag.
- R: The fit maths already centred on both axes; what was missing is that a viewport resize (window,
  the inspector dock moving) never re-ran fit, so the diagram could sit off to one side. Re-fitting
  only while untouched keeps it centred without ever yanking a view the reader has framed themselves.

### Validation

- Live, on the 2-diagram 23:04 entry: inactive wheel does not zoom and does not preventDefault (body
  free to scroll); a click adds `.active` and hides the hint; active wheel zooms (scale 0.589 ->
  0.660) and preventDefault holds (body locked).
- Centred on fresh open (both diagrams, x and y), after "Fit", and across a 1149 -> 675 -> 1149
  viewport resize.
- 134 client unit tests pass, including new assertions that `fitTransform` centres on the downscale
  path; typecheck and vite build clean; no console errors.

### Follow-up

- The wheel gating, activation and resize-refit are DOM behaviours verified live; there is no React
  component-test harness here (only `diagramZoom` pure-maths tests), so those three are not unit
  covered. A harness would be its own piece of work, not a rider on this fix.

## 2026-07-23 10:53 - Pre-commit hook parses sidecar Mermaid before it can publish

```yaml
entry_id: mse_9yqgtyn0hvdrjf0e
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/mermaid-precommit-hook
topics:
  - memory-trace
  - control-plane
  - bugfix
```

### Summary

- A pre-commit hook now parses every staged diagram sidecar through real Mermaid and blocks a commit
  that would ship an unparseable diagram - the backstop `links check` cannot be, because it validates
  a sidecar's structure and never parses the Mermaid. JNL asked for it after the two published breaks.

### Decisions

#### D1 - The check needs a DOM, because parse runs HTML labels through DOMPurify

- D: The checker sets up jsdom (a dev-only dependency) before importing mermaid, then calls
  `mermaid.parse` on each ```mermaid block.
- R: A first spike parsed a trivial `flowchart TD; A-->B` in bare node and I nearly concluded parse
  needs no DOM. The full corpus then failed 54/56 with "DOMPurify.addHook is not a function": every
  real sidecar uses `<br/>`, and mermaid runs HTML labels through DOMPurify, which needs a `window` to
  initialise. jsdom set before the import fixes it; the broken cases still fail correctly.
- A: `render()` is never called, only `parse()`, so this stays a lint, not a headless renderer -
  jsdom is enough, no puppeteer/Chromium. Measured: 56/56 parse, and a `;`-in-message block still
  fails on line 2.
- A: The trivial spike was a false green - it did not exercise the `<br/>` path the whole corpus uses.
  The lesson is the same one as the flowchart.htmlLabels false-positive earlier: a probe that does not
  exercise the real input can pass while the thing it claims to prove is false.

#### D2 - Best-effort hook, reusable checker, not a seeded requirement

- D: The hook skips cleanly when node or the client's mermaid/jsdom are absent (exit 0, never blocks);
  `check-sidecar-mermaid.mjs` exports `checkSidecars()` and also runs as a CLI; `install-git-hooks.mjs`
  writes the pre-commit shim idempotently and refuses to clobber a foreign hook.
- R: Mermaid is a Trace dependency, not a seed one, so a mermaid-parsing hook cannot be a mandatory
  part of `memory-seed hooks install` (which owns only prepare-commit-msg) without breaking the seed's
  lightweight, dependency-free posture. A repo-local, self-skipping hook is the right shape.
- A: The exported `checkSidecars` is deliberately the seam the planned CI parse-test reuses, so the
  parser is wired once, not twice. `npm run lint:sidecars` is the manual/CI entry.

### Validation

- End to end: a staged broken sidecar aborted the commit with file + block + parse error, HEAD
  unchanged; a commit with no staged sidecars passed (this feature's own commit).
- `npm run lint:sidecars` -> 56 blocks parse across 8 files; typecheck clean; 134 client unit tests
  pass; `npm run hooks:install` wrote `.git/hooks/pre-commit`.

### Follow-up

- The CI parse-test task can now shrink to "call `checkSidecars` / `npm run lint:sidecars` in CI" -
  parse-headless-with-jsdom is verified and the checker exists. The chip is updated.
- jsdom is a new client devDependency; it ships in no runtime bundle (`render()` is never invoked).

## 2026-07-23 11:20 - Wire the sidecar Mermaid lint into CI

```yaml
entry_id: mse_5q0z6rgfwwgzj0zs
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/ci-lint-sidecars
topics:
  - memory-trace
  - control-plane
  - git-workflow
```

### Summary

- CI now parses every diagram-sidecar Mermaid block, so an unrenderable diagram fails the build even
  when the local pre-commit hook is bypassed. Closes the loop the hook opened: local gate plus CI
  backstop.

### Decision

- D: Added a `Lint diagram-sidecar Mermaid` step (`npm run lint:sidecars`) to the `react` job in
  `.github/workflows/verify.yml`, between the unit tests and the build - not folded into `npm test`.
- R: There IS CI (`verify.yml`, four workflows), so the task shrank to wiring. The react job already
  runs `npm ci` (installs jsdom + mermaid), typecheck, `npm test`, and build, so the sidecar lint
  belongs there, beside the other node checks - not in the python job, whose `links check` cannot
  parse Mermaid.
- A: A dedicated step, not a node --test folded into `npm test`. Local commits are already gated by
  the pre-commit hook, so CI only needs the explicit-bypass backstop; a standalone step keeps the
  failure obvious in the logs and avoids a unit test that has to reach out of the client dir to the
  repo-root sidecars and depend on git to find them.

### Validation

- The exact CI command verified locally: `npm run lint:sidecars` from `memory-trace/client` -> 56
  blocks parse across 8 files, exit 0; a staged `;`-in-message block exits 1.
- `verify.yml` parses (PyYAML) with the step present and correctly ordered.
- jsdom is in the committed `package-lock.json`, so the job's `npm ci` installs it. The react job's
  full-working-tree checkout includes `.memory-seed/`, and the script finds the root via
  `git rev-parse` - both hold on the ubuntu-latest runner.

### Follow-up

- The check runs on every push to main and every PR, matching the workflow's existing triggers. If a
  future non-diagram sidecar type gains Mermaid, widen `check-sidecar-mermaid.mjs`'s discovery glob.

## 2026-07-23 11:25 - Regenerate the stale docs index and watch CI, not just local checks

```yaml
entry_id: mse_xf7wr3nxdrdd84f8
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/docs-index-stale
topics:
  - control-plane
  - git-workflow
  - documentation
```

### Summary

- main's CI had been red for three pushes. Wiring the sidecar lint into CI and then watching the run
  (`gh run list`) is what surfaced it - the failing step was not mine. Root cause: a stale docs index
  from adding the v1.4 proposal doc without regenerating it. Fixed.

### Decision

- D: Ran `memory-seed docs index` to regenerate `docs/2_Todo/README.md` and `docs/README.md`, and
  gave `sidecar-editable-lens-refinement-proposal.md` its `priority: P3` / `next_action` frontmatter.
- R: The `Validate docs index is current` CI step (`docs index --check`) failed because the proposal
  doc was added to 2_Todo without regenerating the derived lane table. It stayed red across the
  diagram-viewer and pre-commit-hook merges because those did not touch docs and I never watched CI.
- A: The lesson is the process one, not the fix: I verified each change locally (tests, links check,
  live) but never checked the CI run after pushing, so three merges landed on a red main unnoticed.
  Local green is not CI green. Verified now: `docs index --check` current, `docs check` OK (my
  `missing-todo-yaml` warning cleared, 15 -> 14), and I am watching this push's run to confirm.

### Validation

- `docs index --check` exits 0; `docs check` reports lifecycle OK (169 files); the only remaining
  warning is a pre-existing one on another doc.
- The CI failure was isolated with `gh run view`: Python job, `Validate docs index is current`; the
  React job (now including the new sidecar-lint step) was already green.

### Follow-up

- Adding or renaming a doc under `docs/` requires `memory-seed docs index` in the same change - it is
  a CI gate, not just a nicety. After any push to main, check `gh run list` rather than trusting local
  checks alone.

## 2026-07-23 12:45 - Close the P1 ref-extraction corruption in links check

```yaml
entry_id: mse_6cec6ad63n5wtpr1
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/trailer-ref-extraction
topics:
  - memory-seed
  - control-plane
  - bugfix
```

### Summary

- The P1 ref-extraction corruption is closed. It was not a fresh design: the safe per-item parser
  shipped in 21749f4 and was already trusted in two paths; seven `links check` call sites still
  scraped the region text. Migrated all seven. No user decision gated it - the decision-edge
  semantics were ratified 2026-07-22 and decision refs are spec-scoped to link sidecars.

### Decisions

#### D1 - Migrate the seven stragglers to a per-item parser, not the region scrape

- D: New `_entry_level_ref_ids` replaces `_TRAILER_ENTRY_ID_RE.findall(_frontmatter_list_region(...))`
  at all seven sites (session-entry ```yaml refs and edges, per-user file frontmatter, and the sidecar
  `related_entries` left behind when supersedes/evolves migrated in 21749f4).
- R: The scrape committed the two named corruptions: an id in an INDENTED comment became a real edge,
  and a `<entry_id>:dN` ref was truncated to its prefix and recorded as an entry-level edge. Parsing
  per authored item - comments stripped by YAML's rule, decision refs recognised - stops both.
- A: Validator-only. The output lists live and die inside `check_session_links`; the live graph
  already uses the migrated retrieval path (grep-confirmed). So the impact was "the integrity checker
  itself is unreliable," not "the graph is corrupt."
- A: The gate was the before/after `links check` diff on the real corpus, not a sign-off: identical
  before and after (every minted id matches the strict grammar, so nothing new fires). The gain is
  future data plus the two corruptions closed.

#### D2 - Behaviour-preserving except the corruptions: surface misplaced decision refs, keep the old silent-drop for other non-id tokens

- D: The migration surfaces a `:dN` ref in entry frontmatter as `misplaced-decision-ref`, but skips
  any other non-id token silently, exactly as the scrape skipped a substring it could not match.
- R: The full suite caught it: two retrieval parity tests failed because the fixture references a
  registered entry whose id is `ms-bootstrap`. `_ENTRY_ID_RE` accepts any `\S+` as an entry_id, so an
  entry can carry an id the stricter REF grammar rejects. Surfacing every non-id token as malformed
  newly flagged a ref to a real, registered entry - over-strictness, not a caught defect.
- A: The advisor's rule applied cleanly - "real defect the wide regex masked = keep; over-strictness =
  adjust." A registered id that resolves is not malformed. Real minted ids all match the strict
  grammar, so this only ever bit the test fixture; surfacing arbitrary malformed refs is a separate
  linting concern, out of scope for closing the corruption.
- A: Surfacing the misplaced `:dN` was kept because it is the P1 corruption's own domain (a decision
  ref in the wrong place) and it makes the fix observable - it fires in the regression test and never
  on the clean corpus.

### Validation

- `links check` diff on the 61-file corpus: identical before and after.
- 653 backend tests pass (three new: an indented comment in entry frontmatter creates no phantom edge;
  a `:dN` there surfaces as misplaced, reported once not once-per-pass; a ref to a registered
  non-standard-id entry is not newly flagged). The two retrieval parity tests pass again.
- Only `_TRAILER_ENTRY_ID_RE` use left is the trailer-stamping `fullmatch`, which is correct.

### Follow-up

- This is P1 only - "stop the corruption," not thread #2 ("decision-level link refs" / does `link
  audit` gain decision awareness). Those open questions in `decision-level-link-sidecar-refs.md` §258-263
  stay open.
- Surfacing arbitrary malformed refs (a stricter lint than the old scrape) was deliberately not added;
  if wanted, it needs a resolve-first check so a registered non-standard id is never flagged.

## 2026-07-23 13:26 - link audit gains decision awareness in its output; swarm scoped as an optional layer

```yaml
entry_id: mse_qr4m514ad0ajrt1n
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/link-audit-decision-aware
topics:
  - memory-seed
  - proposal-lifecycle
  - agent-collaboration
```

### Summary

- link audit is now decision-aware in its output: every gap carries both ends' decisions (ordinal +
  name + body), the CLI surfaces them, and `--json` emits judgment-ready candidates. The scorer is
  unchanged. JNL resolved the spec's open question and proposed an agent swarm for the narrowing; the
  swarm is filed as an optional-layer proposal, the in-core foundation is built.

### Decisions

#### D1 - Decision awareness is surfaced, not scored

- D: `audit_link_gaps` attaches `entry_body_decisions` (ordinal, name, body) to each gap and candidate;
  the CLI shows them for multi-decision entries and `link audit --json` emits both ends' decision
  bodies plus link/no-link criteria. Scoring is byte-for-byte unchanged.
- R: The spec's finding is real and I confirmed it in the code: every ranking signal - files, topics,
  title terms, entry embeddings - is keyed on `entry_id`, so the scorer cannot discriminate two
  decisions of one entry. Teaching it to would need a new decision-body signal that fights link
  audit's own discipline ("lexical gate decides membership, semantic only reorders") for a signal the
  spec doubts is meaningful.
- A: The advisor's reframe was the unlock: link audit already defers the edge TYPE
  (supersedes/evolves/related) to a human at the sweep. By the same logic it defers the decision
  NARROWING to a human. The person writing today's entry already knows which of their decisions
  authors the edge; the machine need not guess. I had over-reached to "it needs decision-body
  evidence"; it does not, for a human-narrows design.

#### D2 - The judgment swarm is an optional layer outside the network-free core

- D: JNL's "swarm of small (haiku) agents that judge what is a link" is filed as its own proposal, not
  built into `link audit`. The core emits the judgment-ready task (`--json`); the model calls happen
  in an external orchestration; the suggested edge is human/guard-approved and stored as an ordinary
  `:dN` edge.
- R: An LLM sidesteps the evidence limit - it READS the two decision bodies and judges the link, which
  embeddings cannot. But LLM calls are network, and Invariant #1 forbids network in the core; calling
  Haiku from inside `link audit` would violate it. The split along the constitution's core/optional
  line (like the `[trace]` extra) satisfies both #1 (calls external) and #5 (the stored edge carries
  no model dependency, because a human approves it).
- A: This is not overriding JNL's "yes" - it is the evidence- and constitution-respecting shape of it.
  The swarm is a stronger `link suggest`, never a new authority. Recommended in the proposal: build the
  orchestration as a skill first, batch human approval, a confidence floor that only hides never writes.

### Validation

- Extractor verified on the real corpus: ms-f83a27d3 -> d1/d2 with names + bodies; a singular entry ->
  d1, no name; 26 entries carry >=4 decisions. Trailing `### Validation` is not folded into the last
  decision.
- Live: `link audit --date 2026-07-22` surfaces decisions for multi-decision targets and candidates
  with a "target one as <id>:dN" hint; `--json` emits `{criteria, gaps:[...decisions...]}`.
- 659 backend tests (six new: extractor cases, both-ends population, --json shape, --json/--apply
  exclusion). Docs index regenerated; the proposal carries priority/next_action so `docs check` is
  clean.

### Follow-up

- The swarm proposal's open questions are JNL's: where the orchestration lives (skill vs Workflow), the
  approval gate, the fan-out unit, and whether to gate on ADR-promotion. ADR-promotion is entry-level
  today (`has_diagram`); a per-decision ADR marker is a separable small addition noted in the proposal.
- Spec open questions #2 (ESR decision-edge coverage) and #3 (related at decision granularity) stay
  open.

## 2026-07-23 14:47 - Swarm run 0: eight validated decision edges land, four hit the append-only wall

```yaml
entry_id: mse_67y44fsj2srz0eyz
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/swarm-validated-edges
topics:
  - memory-seed
  - agent-collaboration
  - graph
```

### Summary

- The swarm pipeline ran end to end for the first time: mechanical recall (2,797 pairs) -> 24
  high-signal pairs judged by a haiku swarm -> validator kept 12 -> 8 written as sidecar edges. The
  graph understated its own lifecycle: 7 of the 8 were recorded as weak `related` but are `evolves` by
  their own decision texts. JNL mandated a 10-cycle experiment programme over the 214 already-related
  candidates to tune batching, agent context and validator heuristics.

### Decisions

#### D1 - Write the 8 collision-free validated edges; surface the 4 that hit published blocks

- D: Eight evolves edges landed as NEW link-sidecar blocks under each source entry's own date file.
  Four validated edges were withheld: their source entries already have published sidecar blocks, and
  adding an edge is an in-place modification of a published block.
- R: Append-only. The four split three ways, and the split matters: two would CONTRADICT a published
  `not_applicable` classification that explicitly listed this exact candidate before ruling it
  workstream-files-only (mse_myybjvx6pnscfdme and mse_pnkvm5vqzrcfk8vf, both vs
  mse_v890pqa43v7nwwrb); one would add to a block already carrying a live edge
  (mse_a7zs346tqznzpexn); one would complete a 2026-07-19 sweep that ran before title-term scoring
  existed and so never saw the candidate (mse_cz735tsd1z96yc46). Forcing any of them repeats the
  2026-07-22 revert. They are now concrete evidence for the editable-lens proposal's shape choice.
- A: Six of the eight carry decision-level `:dN` targets; one target is single-decision so the
  entry-level form denotes the same edge (the spec's identity rule). Source-side narrowing went into
  `note:` because the grammar has no `source_decision` yet - the spec's step 3, still unbuilt.
- A: The validator tier earned its keep against the swarm's 19/24 over-call: the rejected verdict was
  a merge-to-main entry judged `evolves` at 0.92 while the swarm judged a parallel merge entry `none`
  - executing a decision is not refining it. Kept 12 strong, 5 weaker unwritten (cycle-1 regression
  material).

#### D2 - A decision-level edge suppresses its pair in link audit

- D: `lifecycle_of()` now unions the sidecar `decision_edges` targets into the audit's suppression
  set.
- R: Found live minutes after writing the first `:dN` edge: the pair immediately re-surfaced as a
  "gap". Decision refs deliberately never project into entry-level edge sets, and the audit only
  consulted those - so every decision-narrowed edge would nag forever, punishing exactly the
  precision upgrade the workflow exists to produce.
- A: Verified twice: a pinned test, and the live pair no longer surfaces. This is the first
  "improve how the workflow handles mechanically suggested links" item of the experiment programme,
  found by the programme's own first write.

### Validation

- All 8 edges pass `links check` (63 files, integrity OK): forward-only, ordinals resolve, dates
  match. 660 backend tests pass (one new suppression test).
- Run-0 cost measured: 733k tokens for 24 pairs (~30k/pair), inflated by file-read agents; inline
  context is the first cost lever.

### Follow-up

- The 10-cycle programme JNL mandated, over the ~200 undeduped already-related candidates (~20 per
  cycle, one variable at a time, fixed metrics: link-rate, validator keep-rate, quote-verification
  pass rate, malformed rate, tokens/pair; a regression subset re-judged each cycle). Axes: batch size
  (single vs by-target vs mixed), context form (inline vs file-read, truncation length), prompt rules
  (workflow-milestone, same-session, corpus litmus verbatim, quote-grounding, confidence rubric),
  validator mechanisation (quote string-match, id/ordinal existence, milestone flags, calibration).
- Mechanical pre-filters to build from run-0 evidence: an end with no decisions cannot take a
  decision-lifecycle edge; a merge/land/integrate milestone entry does not evolve what it merges.
- The 4 collision edges wait on the editable-lens proposal's shape decision.

## 2026-07-23 22:09 - Cycle 1: ten validated upgrade edges land; overhead, not context, is the cost

```yaml
entry_id: mse_4f16xe7ztx7dwvgx
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/swarm-cycle1-edges
topics:
  - memory-seed
  - agent-collaboration
  - graph
```

### Summary

- Cycle 1 of the ten-cycle judgment programme JNL mandated: 20 already-related pairs judged by the
  haiku swarm with inline context, validator-ruled, ten upgrade edges written (two supersedes, eight
  evolves). The cycle's headline experimental finding is that inline context saved only ~10% - the
  cost lives in per-agent overhead, so batching is the real lever. Cycle 2 (rules-hardened prompt +
  quote-grounding) is running.

### Decisions

#### D1 - Validator rulings: 10 approved, 1 downgraded, 1 rejected, 1 overturned keep, 1 withheld

- D: Of 11 swarm-suggested upgrades: nine approved as judged; pair 14 downgraded supersedes->evolves;
  pair 19 rejected; one swarm keep (pair 4) overturned to evolves by the validator; pair 12 approved
  but withheld (its source has a published sidecar block - append-only).
- R: Each ruling grounded in the decision bodies, not the swarm's why. The downgrade: a completed
  evaluation whose judgment was followed is not retired - the litmus's "still valid" branch. The
  rejection: pairs 9 and 19 both claimed to supersede the same entry within 26 minutes; the bodies
  show rchj2 builds on the SPLIT architecture (`.timeline-stream`) that 9tbvf introduced, so its
  true target is 9tbvf, not the horizontal-scroll entry it allegedly superseded - a wrong-target
  edge, the failure decision granularity exists to prevent.
- A: The overturn is deliberate validator behaviour, not deference: the swarm split on
  proposal->implementation (pair 1 called it evolves, pair 4 called it "planning->execution, not
  lifecycle") and written precedent (run 0's h5trf->t22wn edge) settles it as evolves. Cycle 2's
  prompt now carries that rule explicitly.
- A: Consistency-grouping caught both the double-supersede and the proposal->implementation split -
  it is the validator heuristic that earns its place first.

#### D2 - Cycle-1 measurement: inline context is not the cost lever; batching is

- D: Recorded cycle metrics: 20/20 judged, 11 suggested (55% vs run 0's 79%), 549k tokens (~27.5k
  per pair vs ~30.5k), 73s wall. Next cycles test batch-by-target rather than chasing prompt-size
  savings.
- R: Inline pair data replaced file-read agents and saved only ~10%, so the dominant cost is
  per-agent fixed overhead (system prompt, tool schemas, reasoning floor), which batching amortises
  and prompt slimming cannot touch.
- A: Honest caveat logged: cycle 1 changed the output semantics too (related now reads link=false
  "keep existing" for these already-related pairs), so the lower suggestion rate is not attributable
  to a single variable. The cost comparison stands; the rate comparison is confounded.
- A: Schema hygiene finding: one agent wrote entry ids into source/target_decision. Cycle 2 pins
  those fields with a ^(d[0-9]+)?$ pattern and adds mandatory verbatim evidence quotes, which also
  gives the validator a mechanical string-match check.

### Validation

- All ten edges pass links check (67 files, integrity OK): forward-only, chronological insertion,
  ordinals n/a (every end single-decision, entry-level form per the identity rule).
- Cycle-2 pool regenerated after the writes: 171 pairs remain; 20 selected; workflow launched.

### Follow-up

- Withheld pair 12 (mse_9ca5mpekppn3b1y1 -> mse_av81ew76vbtyezwc) joins the four run-0 collision
  edges waiting on the editable-lens shape decision.
- Cycles 3+: batch-by-target (the cost lever), truncation length, validator mechanisation (quote
  string-match now possible), calibration curve per cycle.

## 2026-07-23 22:15 - Cycle 2: rules halve over-calling; quote check catches its first bad edge

```yaml
entry_id: mse_aaf70nt2cme37439
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/swarm-cycle2-edges
topics:
  - memory-seed
  - agent-collaboration
  - graph
```

### Summary

- Cycle 2 (rules-hardened prompt: canonical litmus, five lifecycle rules, mandatory verbatim evidence
  quotes, schema-pinned decision fields): suggestion rate halved (55% -> 25%) on a like population,
  reasoning now cites rules, and four validator-approved evolves edges landed. The mechanical quote
  check claimed its first rejection, and the pipeline self-corrected a cycle-1 wrong-target edge.

### Decisions

#### D1 - Keep the rules prompt: it halves over-calling and fails closed

- D: Cycle 2's prompt changes are KEPT for all later cycles: litmus verbatim, executing-is-not-
  refining, implementation-evolves-proposal, explicit-rewrite-supersedes, same-session caution,
  campaign-verdicts-are-parallel, plus mandatory verbatim quotes and `^(d[0-9]+)?$`-pinned decision
  fields.
- R: Suggestion rate fell 55% -> 25% with visibly rule-grounded reasoning, zero malformed decision
  fields (cycle 1 had entry ids in them), and one agent correctly refused to link where quote-
  grounding was impossible (empty decision body -> link=false at 0.95) - the fail-closed behaviour
  the rules were designed to produce. Cost was flat (~28.2k vs ~27.5k tokens/pair), confirming the
  overhead finding: rules are free, batching is the cost lever (cycle 3's axis).
- A: The star outcome: cycle 1 rejected rchj2 -> n772f as a wrong-target edge; after cycle-1's
  writes reshaped the pool, the audit resurfaced rchj2 against 9tbvf - the target the validator had
  named as the true predecessor - and the swarm judged it evolves with both quotes verifying. The
  recall/precision split self-corrects across cycles when the writes feed back into the pool.

#### D2 - Validator rulings: quote verification catches its first fabrication-shaped miss

- D: 4 of 5 suggestions approved; the fifth rejected because its claiming-side quote appears nowhere
  in the claimed entry's decision body and the reasoning reads with the ends swapped. One keep
  (Cytoscape-selection <- renderer-evidence deferral) overturned to evolves by the cycle-1
  deferral-completion precedent, but withheld - its source entry already carries a published sidecar
  block.
- R: The mechanical check (string-match both quotes against the actual decision texts) is exactly the
  cheap validator layer planned: two of five verified fully, two failed one side as paraphrase
  (flag-level; approved after reading the bodies), one failed on the claiming side (rejected). The
  check converts "trust the why" into "verify the evidence", and its first catch was real.
- A: Withheld ledger now: run-0's four collisions, cycle-1's #12, cycle-2's #10 - six validated
  edges waiting on the editable-lens shape decision.

### Validation

- All four edges pass links check (67 files, integrity OK); chronology holds after insertion.
- Cycle metrics recorded: 20/20 judged, 5 suggested, 4 approved, 1 rejected, 1 overturn-withheld;
  563,861 tokens (~28.2k/pair), 137s; quote pass rate 2 full / 2 partial / 1 fail.

### Follow-up

- Cycle 3: batch-by-agent (five pairs per haiku worker), rules prompt unchanged - the overhead-
  amortisation experiment the cost data demands.
- The deferral-completion pattern (executing a decision another entry deferred = evolves) is now
  applied twice by the validator but absent from the swarm rules; add it as rule 6 in cycle 4 and
  measure whether the swarm stops keep-ing those pairs.

## 2026-07-23 22:26 - Cycle 3: batching cuts cost 4.6x at equal quality; five more edges land

```yaml
entry_id: mse_x4ek3tdcpkcpkkra
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/swarm-cycle3-edges
topics:
  - memory-seed
  - agent-collaboration
  - graph
```

### Summary

- Cycle 3 (batch axis: five pairs per haiku agent, cycle-2 rules unchanged) delivered the programme's
  cost result: ~6.1k tokens per pair versus ~28k in cycles 1-2 - a 4.6x reduction - with the
  suggestion rate steady at 25% and rule-cited reasoning intact. Five validator-approved evolves
  edges landed. Batching is KEPT.

### Decisions

#### D1 - Batch-of-five is the configuration going forward

- D: Five pairs per agent (4 agents for 20 pairs) becomes the standing shape; the rules prompt rides
  along unchanged from cycle 2.
- R: 122,739 tokens for 20 pairs (~6.1k/pair) against ~27.5k (cycle 1 inline) and ~28.2k (cycle 2
  rules) - the overhead-amortisation prediction from cycle 1's null result, confirmed. Quality did
  not move: 5/20 suggested (cycle 2: 5/20), rules cited per verdict, quotes present, zero malformed
  fields, zero cross-pair bleed visible in the whys (each cites its own pair's texts).
- A: The batch prompt explicitly instructs judging pairs independently; the verdicts bear it out -
  adjacent same-batch pairs got opposite verdicts where their texts differed. Anchoring is not
  detectable at n=20; the regression subset in cycle 4 is the stronger check.

#### D2 - Validator: five approvals; the quote check needs whitespace-normalisation

- D: All five suggestions approved (four rule-2 proposal-to-implementation shapes, one same-session
  refinement where the newer genuinely changes the older decision's design). The mechanical quote
  check gets a normalisation fix before cycle 4: two textbook-correct verdicts failed string-match on
  both sides purely because quotes cross newlines in the stored texts.
- R: Reading the bodies confirmed the two both-sides failures (#9 scope->Phase-1, #12
  constraint-refines-draft) are exact-content quotes broken only by line wrapping - treating those as
  rejects would have discarded the cycle's two strongest edges. Flag-then-read remains the policy;
  normalised matching (collapse whitespace before comparing) makes the flag meaningful again.
- A: One keep sharpened cycle 4's planned rule 6: an OPERATIONAL deferral-completion (stop the
  preview server, then delete the directory it blocked) was correctly kept as related. Rule 6 will
  scope deferral-completion-evolves to DESIGN decisions (evaluations, selections, scopings), not
  operational sequencing.

### Validation

- All five edges pass links check (67 files, integrity OK); one carries a `:d1` target (the only
  multi-decision end), the rest entry-level per the identity rule.
- Cycle metrics: 20 verdicts from 4 agents, 5 suggested, 5 approved, 0 rejected; 122,739 tokens,
  160s; quote verification 3 full / 2 whitespace-artefact.

### Follow-up

- Cycle 4: rule 6 (design-deferral completion evolves) + a regression subset - re-judge cycle-1's
  five weak/plausible pairs under the current configuration to measure stability, alongside 15 fresh
  pairs. Quote check gains whitespace normalisation.
- Pool after cycle 3: 133 already-related pairs. Withheld ledger unchanged at six (editable-lens
  decision still the bottleneck).

## 2026-07-23 22:36 - Cycle 4: the swarm converges with the validator on the held-out set

```yaml
entry_id: mse_nj92736b1vfzmhez
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/swarm-cycle4-edges
topics:
  - memory-seed
  - agent-collaboration
  - graph
```

### Summary

- Cycle 4 (rule 6 + a five-pair regression subset) is the programme's convergence point: the mature
  configuration now agrees with the validator 5/5 on the held-out run-0 "plausible" pairs it used to
  over-call, rule 6 fired correctly on two genuine design-deferral completions, and four more edges
  landed - including the Constitution-ratification -> resumption edge.

### Decisions

#### D1 - The regression subset proves the rules encoded the validator

- D: Recorded the regression result as the cycle's primary finding: all five run-0 PLAUSIBLE pairs
  (weakly link-called by the naive swarm, refused by the validator) now come back keep-related with
  rule citations. Swarm-validator agreement on the held-out set: 3/5 -> 5/5.
- R: This is the measurement the ten-cycle mandate asked for - proof the configuration changes
  improved judgment rather than just moving it. The keeps cite the exact rules the validator had
  applied ad hoc in run 0 (rule 1 for pure execution, rule 5 for campaign steps), so the swarm now
  makes the distinctions the large model was making, at haiku prices.
- A: One regression why (#19) shows residual nuance the swarm handles correctly: it NOTICED the
  parity-stance shift but kept related because the decisions govern different domains - the kind of
  reasoning that justified the validator tier in run 0, now happening inside the swarm.

#### D2 - Rule 6 works as scoped; four approvals, chain-deepening edges

- D: All four suggested upgrades approved; every quote verified under the new whitespace-normalised
  matching (no flag-then-read needed this cycle).
- R: Two are rule-6 completions of deferred design calls: resuming development Constitution-aligned
  completes the ratification entry's explicit resume-vs-gate deferral, and the fuse hardening
  completes the "harden or not, pending dry-run evidence" call - extending the fuse-encoding chain to
  dgrgw <- azn6b <- 4yrmd, three decisions deep at decision granularity.
- A: The operational-vs-design scoping in rule 6 held: cycle-3's operational deferral (stop server ->
  delete directory) would still be kept, and nothing this cycle mislabelled execution as completion.
  Rule 1 keeps (#5, #8) show the boundary being applied.

### Validation

- Four edges pass links check (68 files, integrity OK); all entry-level (every end single-decision).
- Cycle metrics: 20 verdicts from 4 agents, 4 suggested, 4 approved, 0 rejected; 125,046 tokens
  (~6.3k/pair - batching steady), 217s; quote verification 4/4 full under normalisation.

### Follow-up

- Pool: 103 already-related pairs remain (~5 cycles at 20). Cycle 5 tests the last unexamined context
  choice: full decision bodies versus the 500-char truncation.
- Withheld ledger still six validated edges; the editable-lens shape decision remains the only
  bottleneck to landing them.

## 2026-07-23 22:47 - Cycle 5: full bodies win; the repeat-source wall becomes the bottleneck

```yaml
entry_id: mse_h95qjk483b0ga8r8
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/swarm-cycle5-edges
topics:
  - memory-seed
  - agent-collaboration
  - graph
```

### Summary

- Cycle 5 settled the last context axis: full decision bodies cost ~10% more than the 500-char
  truncation and buy visibly more exact evidence quotes - kept. Three edges landed (the strongest at
  0.98 with both quotes verified); two approved edges were withheld because their SOURCE entries
  already carry sidecar blocks - the repeat-source pattern that now structurally grows the withheld
  ledger.

### Decisions

#### D1 - Full bodies are the standing context; the configuration is now final

- D: Untruncated decision bodies replace the 500-char cut for all remaining cycles. With this, every
  experimental axis JNL mandated is settled: batch-of-five agents, the six-rule prompt, verbatim
  quote-grounding with schema-pinned fields, mechanical pre-filters, and full bodies.
- R: 138,705 tokens for 20 pairs (~6.9k/pair) versus ~6.1-6.3k truncated - a ~10% premium - while the
  quotes became long exact spans instead of fragments, which makes the validator's mechanical check
  decisive more often (4/6 full passes this cycle, and the one both-sides pass at 0.98 needed no
  body-read at all). Suggestion rate stayed 25%: the truncation was not changing verdicts, only
  evidence quality.
- A: The empty-decision guard fired again at 0.99 (a pair whose newer end has no decision text
  refused with grounding-impossible) - the fail-closed behaviour is stable across cycles.

#### D2 - Five approvals; the repeat-source wall is now the programme's bottleneck

- D: All five suggestions approved on the texts. Three written: merge-branch implements the deferred
  fuse design evaluation (a second child of that evaluation, joining cycle-2's fuse-implementation
  edge), the retrieval rollup contract implements the Explorer entry-level proposal, and the
  memory-trace rename executes the release entry's name-gate resolution as a :d1-targeted edge. Two
  withheld: h5trf gained its block in cycle 1 and vzsef has a pre-existing one, so their new edges
  are in-place modifications append-only forbids.
- R: The withheld ledger is now eight validated edges, and the growth is structural, not incidental:
  every productive source entry that keeps evolving the corpus will eventually need a SECOND edge
  from an entry that already has a block. One block per entry_id plus append-only means the pipeline
  cannot record it. The editable-lens shape decision (or an explicit multi-block/append-to-block
  design) is no longer hygiene - it is the cap on how much of the validated decision graph can land.
- A: Multi-child and multi-parent shapes are now normal: 9c151 has two children (81v7v, dr5ep),
  h5trf has two parents approved (t22wn written, yjpvx withheld). The decision graph is a lattice,
  not a chain - which is what makes the one-block-per-entry constraint bind so quickly.

### Validation

- Three edges pass links check (69 files, integrity OK); one :d1 target on a genuinely
  multi-decision end, rest entry-level.
- Cycle metrics: 20 verdicts from 4 agents, 5 suggested, 5 approved (3 written, 2 withheld), 0
  rejected; 138,705 tokens, 281s.

### Follow-up

- Pool: 98 already-related pairs remain; cycles 6-9 run the final configuration as production sweeps.
- Withheld ledger (8): run-0's four collisions, cycle-1 #12, cycle-2 #10, cycle-5 h5trf->yjpvx and
  vzsef->2b1bt. All blocked on the editable-lens / append-to-block decision.

## 2026-07-23 23:07 - Link-sidecar block identity fix: later edges append, the withheld ledger empties

```yaml
entry_id: mse_58rgfg4n8vfqbvfw
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/link-sidecar-block-identity
topics:
  - memory-seed
  - control-plane
  - graph
```

### Summary

- The withheld-ledger cause is fixed at the root, at JNL's direction: link-sidecar block identity is
  now `(entry_id, heading timestamp)` in the fuse, so a later audit appends a NEW dated block instead
  of being refused as an in-place modification. All eight withheld edges landed as second blocks. No
  amendment was needed - nothing is ever rewritten, so append-only is intact.

### Decisions

#### D1 - Fix the identity, not the immutability

- D: The fuse's plan and apply phases key link-sidecar blocks by `(entry_id, timestamp)`. The same
  dated block remains immutable ("existing link sidecar modified" fires exactly as before); a new
  timestamp for a known entry imports as a later declaration; duplicate same-identity blocks are
  refused as authoring damage.
- R: The collision cause was never append-only - it was the one-block-per-entry assumption baked into
  the fuse's keying. The reader (`entry_link_sidecars`) already unioned multiple blocks per entry,
  with a comment anticipating "two sidecar blocks may key to one entry (different days, or a
  correction)" - the read path was built for this and the write path never caught up. Eight validated
  edges were withheld on the gap by cycle 5, and the growth was structural: every productive entry
  eventually needs a second declaration.
- A: This is Evolution, not amendment. Invariant #2 forbids rewriting history; appending a dated
  block that records a later declaration IS the sessions philosophy (corrections are new records
  pointing back) applied to sidecars. The editable-lens proposal narrows to true in-place editing
  only - repair and rewording - which stays forbidden pending its own decision.
- A: A block whose heading timestamp is edited now reads as delete+add: the base block survives the
  fuse (removals are never honoured) and the re-stamped one imports beside it - noisy but lossless,
  recorded in the code comment and the spec.

#### D2 - The eight withheld edges land, including two that revisit a published classification

- D: All eight ledger edges written as second blocks stamped with the wall clock, appended to each
  source's existing date file. Two of them (myybj and pnkvm, both -> v890) contradict the file's own
  earlier `edge_status: not_applicable` classification; their notes say so explicitly.
- R: The earlier classification weighed file overlap only; the swarm judged the decision bodies and
  the validator confirmed. Under block identity both statements stay in history - the old
  classification as the record of what the 2026-07-16 sweep concluded, the new block as the later
  declaration with better evidence - and the union carries the edge. That is supersede-by-append, not
  silent correction.
- A: Verified end to end: links check integrity OK over the second blocks, and the audit no longer
  surfaces the landed pairs (the reader's union feeds suppression). The decision graph gains, among
  others, the P1-generalises-P1b edge and the Cytoscape-selection deferral completion.

### Validation

- 663 backend tests (three new: a second dated block imports through session merge-branch and both
  blocks survive in the committed file; a same-identity text change is still refused; duplicate
  same-identity blocks are refused).
- links check: integrity OK, 69 files. Audit suppression confirmed live for the landed pairs.
- Docs: the lifecycle-edge spec records the identity rule and its rationale; the editable-lens
  proposal's next_action narrows to in-place editing; docs index regenerated (its staleness was
  caught by `docs index --check` before commit this time).

### Follow-up

- The withheld ledger is empty. Cycle 6's verdicts (7 suggested upgrades) are queued for validation
  and can now land without collision regardless of which sources already carry blocks.
- The venv lost its uv-managed metadata to an OneDrive lock mid-sync (annotated_types dist-info);
  tests run via `uv run --no-sync --with pytest`. Repair with a clean `uv sync` once OneDrive
  releases the handle.
