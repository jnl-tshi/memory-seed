---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-27
---

## 2026-07-27 00:31 - The Overview connectivity claim was already corrected; the drift that remained was in code comments

```yaml
entry_id: mse_ppxxgd34jkb5e81v
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/fix/overview-slice-stale-comments
topics:
  - memory-trace
  - graph
  - documentation
```

### Summary

- Sent to correct the stale "Overview ranked by connectivity" line in `0_NEXT_STEPS.md`. It was
  **already corrected** — `375a8f7` made exactly that edit and is an ancestor of `main`, as is the
  ADR fix `5311bd0`. Nothing in `docs/` still asserts the connectivity ranking.
- The brief's premise traced to a **stale worktree**: `git show 5311bd0:docs/2_Todo/0_NEXT_STEPS.md`
  reproduces the reported line 59 verbatim, and `375a8f7` landed 3 minutes *after* `5311bd0`. The
  parked checkout `.claude/worktrees/agent-aff1b3b0387c7d4d1` sits at `5311bd0`, which is also why
  the branch looked unmerged from there — its tip *is* `5311bd0`, fully in `main`.
- Real residual drift found one layer out: **two comments in `App.tsx`** still taught the removed
  connectivity ranking, one of them asserting the literal opposite of the code. Both corrected.
- Verified the untouched half of the claim: the honest coverage indicator still ships.

### Decisions

#### D1 - Correct the two `App.tsx` comments, one step past the `docs/` scope I was given

- D: Rewrote `App.tsx:273` (the `overviewLimit` declaration) and `App.tsx:780` (`showMoreOverview`)
  to describe the chronological spine plus additive depth-1 expansion. Left the `docs/` tree alone —
  a repo-wide grep for `connectivity-rank|connectivity ranking|next-most-connected|highest-degree`
  across `.ts/.tsx/.js/.py/.html` returned only these two; every other hit was an unrelated algorithm
  (trail lane packing, the TSP 2-opt) or `service.py:3314`, which correctly says the ranking *was
  replaced*.
- R: `App.tsx:780` did not merely lag the change, it inverted it: "Pages deeper into the server's
  connectivity ranking **rather than widening a window** — the next click reaches the
  next-most-connected entries". The code widens a window. That comment is also load-bearing for the
  community-detection ADR, whose rejection of Louvain colouring rests on each "Show more" page being
  a *different time window*, so a reader reconciling the ADR against this comment would find the
  client contradicting the ADR's premise. Same defect and same blast radius as the one I was sent to
  fix, two files from the docs that were already corrected; fixing the docs and leaving the code
  saying the opposite is how the misconception gets re-derived from source next time.
- A: Report and leave — rejected: the next reader has no reason to distrust a comment adjacent to the
  code it describes, and this is the second analysis this claim has misled.
- F: `memory-trace/client/src/App.tsx` (comments only, 11 insertions / 7 deletions; no behaviour).

#### D2 - Do not re-edit the `0_NEXT_STEPS.md` bullet, despite a real prose defect in it

- D: Left lines 58-64 exactly as `375a8f7` wrote them, and reported two observations to JNL instead:
  (a) the inserted clause leaves "Obsidian-style edge filters" dangling after an em-dash, breaking
  the comma-list the sentence was; (b) "across all dates" now does the opposite work — the candidate
  *pool* is all dates, but selection is the newest `limit` plus what they reference.
- R: The line is **factually correct**. Both defects are prose, and its self-referential clause
  ("the stale wording here is what led one later analysis...") is the prior session deliberately
  owning its error — silently smoothing that away would delete a deliberate record. A third rewrite
  of a line the requester believes is still on version one is how this becomes a fourth correction
  session; the useful output is telling JNL the line already moved twice, and letting them take the
  prose call.

### Verification

- `docs check`: **Docs lifecycle OK (183 files)**, 14 pre-existing `missing-todo-yaml` warnings,
  none touched by this change (no doc was edited).
- Coverage indicator **still accurate and now more honest than when written**: live at
  `App.tsx:1236` as `· {overviewShownCount} of {graphEntryTotal} entries shown`, and `api.ts:100-103`
  records that `connectedNodeIds` was *removed* because hiding orphans made the readout look like a
  hard cap. The 2026-07-21 design (`connectedNodeIds`-based counting) was superseded by a stricter
  one, so that half of the B0b claim stands unqualified.
- Mechanism reconfirmed against source, not inherited: `service.py:3290` `_overview_slice` docstring
  and `service.py:1665` call site (`expand_types={"replaces","evolves","related"} & edge_type_set`).

### Follow-ups

- `0_NEXT_STEPS.md:59` prose is JNL's call — see D2. Nothing blocks on it.
- Noted but out of scope: `_overview_slice` is not the only thing sizing the Overview payload — the
  pinned-entry block at `service.py:1679` is additive and **exempt from `limit`**, so a payload can
  exceed spine + expansion whenever the Trail has a loaded window. The 60 + 11 = 71 measurement was
  route-exact and unpinned; anything re-deriving node counts from `limit` alone should account for
  pins.

### Related

- Corrects comment drift from `[[mse_ws2prm8b5ec525ye]]` (2026-07-26 01:05, D2 — the decision that
  made Overview a chronological spine).
- Same misconception previously corrected in docs by `5311bd0` and `375a8f7`.

## 2026-07-27 00:39 - Generic seed/live byte parity for every shipped skill runbook

```yaml
entry_id: mse_vy5yzac5jqw1vd0e
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/test/skill-seed-parity
topics:
  - skill-architecture
  - bugfix
evolves:
  - d1 -> mse_31m31wsrwe77jh78:d4
  - d1 -> mse_1gcbzv5w9cgw7bga
```

### Summary

- Closed the seed/live parity gap flagged as deferred on 2026-07-26: every skill runbook shipped by
  `SEED_FILES` is now asserted byte-identical to its live twin, instead of only the nine skills
  hand-listed in the `extracted` dict plus three explicit pairs.
- All 26 twins were already byte-identical at the time of writing, `link_swarm.md` included — the
  defect was the absence of enforcement, not drift. 768 tests pass (was 767).

### Decisions

#### D1 - One generic loop over SEED_FILES, comparing `seed_file.source` bytes

- D: `test_every_seeded_skill_runbook_matches_its_live_twin` iterates `SEED_FILES`, keeps the entries
  whose destination starts with `.memory-seed/skills/`, and asserts
  `Path(destination).read_bytes() == seed_file.source.read_bytes()` — 25 runbooks today. The
  hand-listed per-skill `assertEqual`s are deliberately left in place.
- R: The three existing SEED_FILES tests enforce **presence** and **registration** — a skill exists in
  both trees and something triggers it — and none of them reads content, so a skill outside the
  hand-lists could drift silently. Coverage that depends on an author remembering to extend a dict is
  the failure mode itself: `link_swarm.md` shipped through that gap. The redundancy with the
  hand-listed skills is intentional and worth its cost, because the two tests assert different things
  — those pin per-skill CONTENT anchors, this pins COVERAGE — and a future reader who mistakes them
  for duplicates would delete the wrong one.
- A: Re-deriving the seed path as `SEED_SKILLS_DIR / name` (the shape of the existing
  `_seed_files_skill_names()` helper) was rejected: it discards the source and the two paths are
  identical today, so the test would pass either way and never catch a `SeedFile` wired to the wrong
  source. `read_text()` was rejected for a measured reason, not a stylistic one — rewriting
  `link_swarm.md`'s seed twin to CRLF leaves `read_text()` comparing **equal** while `read_bytes()`
  fails, and a Windows/OneDrive checkout is exactly where that divergence appears.
- F: `tests/test_session_schema.py`.
- T: Full suite 768 passed, 17 subtests, 337s. Two negative controls, both restored via
  `git checkout --`: appending bytes to the `link_swarm.md` seed twin fails the test naming
  `link_swarm.md`; the CRLF rewrite fails it while `read_text()` compares equal. A floor assertion
  (`len(checked) >= 25`, and `skipped == [index.md]`) guards the vacuous pass, since a filtered-loop
  test that matches nothing is green and silent.

#### D2 - `skills/index.md` is the sole exemption; `.agents/` is excluded structurally, not by rule

- D: `.memory-seed/skills/index.md` is skipped and the skip is asserted to be the only one. `.agents/`
  persona templates are not mentioned in the filter at all — they simply never match the
  `.memory-seed/skills/` prefix.
- R: `index.md` is runtime-local: projects extend the trigger registry with persona entries, so exact
  equality is the wrong assertion, and `test_skill_trigger_registry_is_deterministic_and_seeded`
  already checks seed content for per-line containment. Asserting the skip-list rather than just
  skipping means a second exemption cannot be added quietly. For `.agents/`, the measurement settles
  it: 4 of the 7 live personas (`content-creator`, `copywriter`, `developer`, `solo-founder`) already
  differ from their seed sources, so a parity assertion over them would fail on contact. That
  divergence is the design working — personas are project-local and evolve with user approval, which
  is why `test_control_plane_files_report_current_version` already skips them — not a defect to chase.
  Excluding them structurally rather than by an explicit carve-out keeps the test's scope legible:
  it is about shipped skill runbooks, and nothing else was ever in range.
- F: `tests/test_session_schema.py`.
- T: `cmp` across all 7 `.agents/` twins and all 26 skill twins recorded the counts above.

## 2026-07-27 00:44 - The verification command ran a stale global binary: guard provenance, not wording

```yaml
entry_id: mse_wh13f4kfxwr083jc
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/fix/stale-cli-binary-guard
topics:
  - agent-collaboration
  - bugfix
evolves:
  - d1 -> mse_2tfqhnx83p4qf453:d4
```

### Summary

The verification command handed to worktree agents — `uv run --no-sync memory-seed docs check` — was
not running this repository's code at all. `--no-sync` leaves a worktree `.venv` empty (this worktree
had no `.venv` at all), so no console script exists there and `memory-seed` resolves through PATH to a
**globally installed, older** build at `C:\Users\johnn\.local\bin\memory-seed.EXE`. Its `docs check`
reported two phantom lane errors for `7_Replaced` and `4_Reference/trace-humanised-dashboard-references`
— both present in this tree's `docs_check.py` — and its `docs index` regenerated `docs/README.md`
dropping the `7_Replaced 29` lane count. Committing that would have landed a docs-index regression
attributable to nothing in the diff, which is the most plausible mechanism behind the recorded pattern
of merges landing on a red main.

`package_provenance()` now makes that invocation refuse instead of lying. The honest limit is stated
below and in `CONTRIBUTING.md`: it cannot catch the build that caused the incident.

### Decisions

#### D1 - Guard the mechanism, not the wording; the documented command was never the defect

- D: Implemented option (c) — `package_provenance()` in `core.py`, with the CLI exiting 2 and the MCP
  server warning on stderr — and deliberately did **not** do the broad option (b) rewrite of
  `memory-seed <cmd>` to `python -m memory_seed.cli <cmd>` across the control plane.
- R: The premise did not survive measurement. `uv run --no-sync memory-seed` appears **nowhere** in the
  repository: `grep` over `*.md`/`*.yml`/`*.toml`/`*.py` outside `sessions/` returns two hits for
  `uv run`, both `--no-project` measurement scripts in an ADR draft. The command came from harness and
  orchestration prompts, not from a file that could be fixed. What the repo does document is the bare
  `memory-seed <cmd>`, and that form is **correct** in the primary checkout (editable install) and for
  anyone who installed the published package — rewriting it to `python -m memory_seed.cli` would be
  actively wrong in the seed twins, which ship to consumer projects where `memory_seed` is not
  importable from cwd. A guard also defends against a command nobody wrote down, which is the class
  the incident actually belongs to.
  Option (a), dropping `--no-sync`, is the correct human remedy for the invocation and is documented as
  such, but it is not durable: there is no `uv.lock`, sixteen worktrees exist on OneDrive, and the repo
  cannot control how the next agent types its command.
- A: Broad (b) — rejected as a phantom target and harmful in the seed twins. (a) alone — rejected as
  not durable.
- F: `memory_seed/core.py`, `memory_seed/cli.py`, `memory_seed/mcp_server.py`
- T: `tests/test_package_provenance.py`

#### D2 - The predicate fires only inside a source checkout, so no consumer and no CI run is impugned

- D: The condition is: cwd or an ancestor holds `memory_seed/__init__.py`, **and** the imported
  package resolves outside that tree. Path comparison reuses the existing `_is_relative_to_casefold`.
  `version` and `help` stay runnable; `MEMORY_SEED_ALLOW_FOREIGN_PACKAGE=1` opens the gate for a
  deliberate cross-version run; falsey values (`0`/`false`/`no`/empty) do not.
- R: A seeded consumer project has no `memory_seed/` source tree, so the answer there is always
  "not foreign" and an ordinary `pipx`/`uvx`/`pip` install is never second-guessed. `verify.yml` does
  `pip install -e .`, which resolves the package **into** the checkout, so CI never trips it — a future
  switch to a non-editable install would, and that failure would look mysterious, so the docstring says
  so explicitly. `version`/`help` are exempt because they are how the mismatch gets diagnosed and
  neither reads nor writes the tree.
  The MCP server warns rather than refuses: a hard block at launch would take the harness's server down,
  and the server's own tools are how an agent would read the diagnosis.
- F: `memory_seed/core.py`, `CONTRIBUTING.md`
- T: `tests/test_package_provenance.py` (consumer-project case, casefold case, editable-install case)

#### D3 - Record what the guard cannot do rather than write it up as closed

- D: `CONTRIBUTING.md` and the CHANGELOG both state that a **pre-guard** global build has no such check
  compiled in, so the specific binary that caused the incident stays silent until the global install is
  refreshed. The remedy named there is `uv tool upgrade memory-seed` or `pip install -e .` into the
  worktree venv.
- R: A guard written in `memory_seed/` only executes when `memory_seed/` is what got loaded — which is
  exactly the case where nothing is wrong. Claiming the hazard is closed would be the same class of
  false confidence as the clean-looking local run this fixes. What the guard does close is the class
  going forward, and sibling variants (an installed 2.19 run against a 2.18 checkout).
  The demonstration follows from that: the guard cannot be shown firing through the stale binary, so
  `tests/test_package_provenance.py` simulates provenance by repointing `core.__file__`, and the
  evidence that the hazard is real is the separate observation that the global `memory-seed` on PATH is
  not in any `.venv` this worktree has.
- F: `CONTRIBUTING.md`, `CHANGELOG.md`

#### D4 - The propagation went to a twin-free file, plus one consumer-true line in the twinned skill

- D: The repo-specific rule (`python -X utf8 -m memory_seed.cli`, the guard, the env var, the CI note)
  went into `CONTRIBUTING.md`, which has no seed twin. `agent_collaboration.md` and its seed twin gained
  one bullet — "The tool you run is measured too, not just the tree" — worded generically about console
  scripts and empty worktree venvs, sitting directly under the existing "Worktree identity is measured,
  never declared".
- R: `test_integration_mode_contract_has_live_seed_parity` compares four live/seed pairs
  **byte-for-byte**, so a twinned file cannot hold repo-true text and consumer-true text at once;
  anything repo-specific has to live somewhere untwinned. The generic bullet earns its place in the
  shipped skill on its own merits — an empty worktree venv silently resolving a console script through
  PATH is a hazard for any seeded Python project, and it is the same epistemic rule one level down:
  the previous bullet says measure the tree, this one says measure the tool.
  `.github/workflows/verify.yml` needed **no** change: it has always invoked `python -m memory_seed.cli`.
  CI was never the gap — CI is what caught the red main.
- F: `CONTRIBUTING.md`, `.memory-seed/skills/agent_collaboration.md`,
  `memory_seed/seed/.memory-seed/skills/agent_collaboration.md`

### Validation

- Full backend suite: **777 passed, 1 failed** (`test_session_prepare_pr_branch_commits_chronological_merge_on_task_branch`,
  `git commit failed: (no output)`), 21 subtests. That test **passes in isolation** (45/45 in its file)
  and exercises `session_prepare_pr_branch` directly, which the change does not touch — recorded as a
  load-related flake, not a green claim.
- `tests/test_package_provenance.py`: 11 passed, 4 subtests.
- `docs check` OK (183 files, 14 pre-existing warnings), `docs index --check` current, `links check` OK
  (92 files) — all run as `python -X utf8 -m memory_seed.cli`, which is the point.
- Twin parity confirmed with `cmp`; `git diff --check` clean.

### Follow-up

- The global `memory-seed` install is still pre-guard. Until it is refreshed, the exact invocation that
  caused the incident remains silent.
- Not built, flagged for JNL's call: the seeded `prepare-commit-msg` hook infrastructure could run
  `docs index --check` at commit time using an explicit `python -m memory_seed.cli` from the repo root.
  That would catch the *damage* (committing regenerated stale output) regardless of which binary the
  agent ran, which is strictly stronger than the guard — but it is outside the three options given.

## 2026-07-27 01:09 - Decision groups made complete, circular and free to rotate; the settle made cheaper

```yaml
entry_id: mse_74f1jkr8rv4b7hrp
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/fix/graph-settle-cost
topics:
  - memory-trace
  - graph
  - ui-design
```

### Summary

- Five defects JNL found by looking at the shipped decision-row Graph, plus the measured cause of
  the "Show more locks scrolling for a minute" report.

### Decisions

#### D1 - `linked` scope selects entries, not ordinals

- D: `decision_row_scope: "linked"` now selects ENTRIES and expands each selected one WHOLE.
  `_expand_decision_rows` takes `only_entries` in place of `only_ordinals`.
- R: JNL reported groups showing "1 less decision than is present". It was not an off-by-one — it was
  per-ordinal selection: `mse_a0bxp5n1wcnsjxvw` has 4 decisions and drew d1/d3/d4,
  `mse_m0xs623m4cs0kjag` has 2 and drew only d2. The gaps land wherever no edge names an ordinal.
  A reader cannot tell a decision with **no relationships** from a decision that **does not exist**, so
  a container that claims to hold an entry has to hold all of it — otherwise the group misrepresents
  the memory it frames. Per-ordinal filtering was only ever defensible because an edgeless row arrived
  as a fresh isolate in a force layout (measured: 276 of 446); compound containment retired that
  reasoning on 2026-07-26, so the constraint could go. Selection is still by evidence — an entry that
  no decision edge names is not expanded at all — so `linked` remains a narrowing, not `all`.
- A: Prune the edgeless rows instead (a task chip was already running for exactly this) — rejected and
  the session stopped: it optimises the wrong direction, since the rows are the thing missing.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/tests/test_trail_decision_edges.py`.

#### D2 - A decision row's visibility and credit both follow its ANCHOR

- D: One rule, `visibilityIdFor`, applied in two places: the Orphans filter keeps or drops a row with
  its anchor, and the Inspector's **Links** count reads endpoints through it.
- R: D1 alone made things briefly worse: the completed rows carried no edge, failed the connectedness
  test on their own degree, and were dropped — so with Orphans off a 4-decision entry drew a ring of
  **one**. And separately JNL found `mse_5zg50mzrmtx80c80` reporting "Links 0" while its d2 row carried
  two lines, because the count compared raw endpoint ids. Both are the same mistake: reading a decision
  row as if it were an entry. A decision inside a shown entry is not an orphan, it is a decision; and a
  line terminating on a row is a line that entry has. The Links case is the worse misreading of the
  two — those entries are precisely the ones decision rows exist to serve, and the panel was telling
  the reader their work was unconnected.
- A: Credit rows as edge endpoints in `connectedIdsWithDecisionAnchors` — rejected: that set answers
  "what did an author link", and inventing membership in it would leak a derived fact into an authored
  channel. Mapping at the point of USE keeps the derivation where it belongs.
- F: `memory-trace/client/src/graphDecisionRows.ts`, `GraphWorkspace.tsx`, `App.tsx`.

#### D3 - The circle is a halo child, because a compound parent cannot be one

- D: The group's compound parent is now invisible and carries only containment; an ordinary child node
  (`dhalo:{entry_id}`), sized by `haloDiameter` and centred on the anchor, draws the visible circle.
- R: JNL asked for circles rather than boxes, which is right — the rows sit on a ring, so a rectangle
  drew corners the content never reaches and read as a container of records rather than one memory with
  its decisions. Setting `shape: ellipse` on the parent did nothing: `style()` reported `ellipse` and
  the screen showed a square, because Cytoscape renders a compound parent as a rectangle regardless.
  Splitting the two jobs keeps the structural channel intact (the `parent` field is still what
  expresses containment) and puts the drawing on an element that can actually be a circle.
- A: Drop the compound parent and keep only the halo — rejected: containment would stop being
  structural, which is the whole basis of the 2026-07-26 non-edge decision.
- F: `memory-trace/client/src/graphDecisionRows.ts`, `GraphWorkspace.tsx`.

#### D4 - The ring is rigid but free to rotate

- D: Rows sit at equal angles (`360/N`, hard constraint) on a ring whose PHASE is fitted in closed form
  to the group's own decision edges — `ringPhase`, the circular mean of each row's target direction less
  the slot it occupies. Undetermined fits rest at 12 o'clock.
- R: Two of JNL's instructions in sequence: equal angles first, then rotational freedom "while
  preserving the equidistant angle between each decision too". They are only in tension if each row is
  aimed independently — which is what the original code did, and it bought the direction hint by
  spending the spacing. A rigid ring with one degree of freedom keeps both: spacing cannot vary, and the
  single free parameter is exactly enough to put a row on the side its relative is on. The closed form
  matters for determinism: no search, one pass, same answer every load. The fallback matters for
  stability: two rows pulling opposite ways leave the rotation genuinely undetermined, and picking one
  of two equally good answers would make the ring flip between them every frame.
- A: Rotate to avoid the anchor's own entry-level edges instead of toward counterparts — rejected: it
  optimises against lines that are not the group's, and the group's own edges are the information a
  reader is trying to follow.
- F: `memory-trace/client/src/graphDecisionRows.ts`, `GraphWorkspace.tsx`.

#### D5 - The settle re-measures the camera 4x a second, and a warm mount stops early

- D: `scaleToFitGrowth` is throttled to 250ms instead of every tick, and a warm-seeded mount runs from
  alpha 0.6 to an alpha floor of 0.03 instead of 1.0 to 0.001.
- R: JNL reported scrolling locked for about a minute after "Show more", cause unclear. Measured: idle
  is clean (**0** long tasks in 10.8s, worst frame 18ms), and the whole cost is the settle — ~365 long
  tasks averaging **74ms**, **27 of 37 seconds** with the main thread blocked. So nothing can scroll,
  and the fetches are not to blame (176–532ms each).
  Two cheap and invisible wins. `cy.elements().boundingBox()` ran **every tick** and costs 8.65ms at 294
  nodes / 13.48ms at 1463 elements — more than the physics and the paint together — to answer a question
  whose answer changes far slower than 60Hz. And the number of TICKS is what sets the duration: ~300 from
  alpha 1 to the d3 default floor. A warm mount does not need that budget, since every incumbent came
  back from a settled layout and only the arriving page has to travel; stopping at 0.03 gives ~130.
  Lowering the START alpha alone would have bought 14%, because ticks scale with `log(alpha0/alphaMin)`
  — worth recording, because it is the intuitive lever and it is the wrong one.
- A: Converge off-screen above a node threshold (instant, no motion) — offered and declined by JNL, who
  chose to keep the animation.
- F: `memory-trace/client/src/GraphWorkspace.tsx`.
- T: 980 backend tests, 167 client unit tests, 7 story tests, typecheck clean, build OK. Verified live
  against the real corpus: group ordinals match the authored `#### Dn` count exactly on every sampled
  entry (4/4, 3/3, 3/3, 2/2, 2/2); 12 groups drawn with **12 distinct rotations** and spacing exactly
  `360/N` in every one; halo `shape` is `ellipse` and encloses the ring at every row count;
  `mse_5zg50mzrmtx80c80` now reads Links **2**.

### Follow-up

- **The freeze is not fixed, and D5 alone cannot fix it.** With the app's own per-frame work down to
  ~4.5ms, the measured frame is still 150–320ms at 1463 elements, so ~97% of it is Cytoscape's canvas
  redraw — and D1 legitimately grew the element count (169 rows where there were 92, plus 67 halos),
  which ate the saving at large slices. One Show more at 689 nodes still blocks ~13–19s. The remaining
  lever is the one JNL declined while it was only a nicety: paint every 2nd or 3rd tick, which cuts
  redraws proportionally and makes the settle SHORTER rather than choppier (alpha decays per tick, so
  the same ~130 ticks land in ~45 frames). Worth re-offering now that it is the deciding factor rather
  than an optimisation.
- An earlier measurement in this session was inflated 3x: arming a `PerformanceObserver` repeatedly
  without disconnecting the previous ones delivers every entry once per observer. The "80.8 seconds of
  blocking in 37 seconds" that first looked alarming was three copies of ~27s. Deduplicating by
  `(startTime, duration)` is what caught it.

## 2026-07-27 01:27 - Zonal settling measured and declined; the settle repaints less often instead

```yaml
entry_id: mse_r5m6rdypd34dz9j0
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/fix/graph-paint-rate
topics:
  - memory-trace
  - graph
  - ui-design
```

### Summary

- Closed the Show-more freeze, after measuring JNL's zonal-rendering hypothesis and finding it does
  not hold on this renderer.

### Decisions

#### D1 - Zonal settling is declined on measurement, not on taste

- D: No centre-first, ripple-outward settle. The physics keeps running over the whole graph at once.
- R: JNL asked whether focusing compute on the centre and rippling out would help. Measured at ~1180
  elements: nothing moving is a 16.6ms frame, **one** node moving is **81.9ms**, and all 303 moving is
  93.6ms. So a single moved node already costs 87% of moving everything — zoning the physics saves ~12%,
  and staging the settle means MORE frames in which something moved, so the total gets worse rather
  than better. Two follow-ups closed the neighbouring ideas: drawing fewer elements is only weakly
  cheaper (hiding 47% of them moved the frame 94.7ms → 76.4ms), and distance moved does not matter at
  all (0.5px 147.8ms vs 1.5px 146.1ms in the same machine state). The cost is the act of repainting,
  not the movement in it — which is a property of the renderer, and the reason the intuition fails.
- A: Progressive DISCLOSURE (hold the periphery back, reveal outward) — declined for now on the same
  evidence: it is the strongest form of "draw less", and draw-less measured at 19% for halving the
  canvas. Recorded rather than dropped, because it is the right lever if element counts grow much
  further.
- F: none — this decision is what was NOT built.

#### D2 - Several physics steps per painted frame, scaled by element count

- D: `ticksPerPaint(elements)` runs 1 step per painted frame below ~400 elements and up to 3 above, and
  the settle loop ticks that many times before each paint.
- R: The only quantity that scales with the cost is the number of PAINTS, so paint less often. Alpha
  decays per tick, so N ticks per frame reaches rest after the same number of ticks in 1/N the frames:
  the settle comes out cheaper **and shorter**, which is the opposite of the usual perf trade. Scaled
  rather than flat because a small graph already paints inside its frame budget and has nothing to buy;
  capped at 3 so the coarsest case still reads as movement rather than teleporting.
- A: Lower the starting alpha further — already falsified earlier tonight: ticks scale with
  `log(alpha0/alphaMin)`, so alpha 0.4 buys 14%. Converge off-screen in one block above a size
  threshold — declined by JNL, who chose to keep the animation.
- F: `memory-trace/client/src/graphForces.ts`, `graphForces.test.ts`,
  `memory-trace/client/src/GraphWorkspace.tsx`.
- T: 168 client unit tests (the `ticksPerPaint` boundaries and its degenerate inputs included), 7 story
  tests, typecheck clean, build OK. Verified live by counting Cytoscape `render` events for one settle
  at 1463 elements: **47 renders, 48 long tasks** — one long task per painted frame, and ~141 ticks
  delivered in 47 frames at `ticksPerPaint` 3, where before it was one render per tick.

### Follow-up

- The wall-clock number is currently **loaded and should not be quoted as a baseline**: 9.5s blocked of
  14.2s, at 198ms per painted frame, while this machine ran four concurrent agent sessions. The same
  probe read 147ms where it read 93ms an hour earlier, so the machine is ~1.6-2x slower than when the
  original 74ms/frame figure was taken. Unloaded, 47 frames x ~85ms is ~4s against ~11s before. Re-measure
  on an idle machine before treating any absolute figure here as the new baseline.
- Absolute timings across a long session are unreliable for exactly that reason. What survived the noise
  was every RATIO measured inside a single run — 1 node vs 303, hidden vs drawn, 0.5px vs 1.5px, and the
  render count per settle. Prefer within-run ratios when measuring this on a shared machine.

## 2026-07-27 02:24 - Colour by child inside a focused topic; topic_canonical tells an alias from a child

```yaml
entry_id: mse_bfaa3ef75wgccys2
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/focused-child-colour
topics:
  - memory-trace
  - graph
  - ui-design
```

### Summary

- The graph now colours by topic ROOT with nothing filtered and by CHILD inside a focused family,
  which needed a new facet to keep an alias from posing as a child.

### Decisions

#### D1 - The colour key is the root, except inside the focus where it is the child

- D: `colourKeyOf(slug, roots, focus, canonical)`. With no filter the key is the root, as before.
  Filtered to a family, slugs inside that family key on their own canonical slug; everything outside
  keeps its root.
- R: Root colour is what makes the whole map splittable at a glance — and applied to a filtered view it
  paints everything one colour, because every node now shares the family and the family has stopped
  being information. JNL asked for the reversal, and the corpus can already show it: 43 child slugs are
  authored today (`goal` 6, `merge` 5, `branch-history` 3), so focusing `git-workflow` separates real
  entries rather than waiting on the sweep. Outside the focus the root still reads, because a node can
  carry a second topic from another family and that family is still what identifies it.
- A: Colour by child always — rejected, and it is the decision the root-colouring work already made:
  the palette would grow and reshuffle as a sweep moves entries onto children. The point is that root
  colour is right for the corpus view and wrong for the family view, not that either is right always.
- F: `memory-trace/client/src/graphCommunities.ts`, `GraphWorkspace.tsx`, `App.tsx`.

#### D2 - A colour is only spent when it separates something

- D: A focused family whose members reduce to ONE canonical slug mints no key at all; the member falls
  back to its parent's colour. Two or more and each earns its own.
- R: JNL: "if only a child is present derive its parent colour". A lone member has nothing to
  distinguish, so a new hue would be a new category the reader has to learn for no gain — and it would
  cost the recognisable family colour they already know. Implemented as a MISSED LOOKUP rather than a
  branch: `topicColourScale` tries the focused key and falls back to the root, so "derive the parent"
  is the absence of a slot rather than a rule that could disagree with the slot table.
- A: Decide it from what is on screen rather than from the corpus — rejected: colour would then shift
  as the view pages, which is the property the corpus-wide counts exist to protect.
- F: `memory-trace/client/src/graphCommunities.ts`.

#### D3 - Focused keys are APPENDED past the root table, never interleaved

- D: The root slot table is built exactly as before; the focused family's members are appended after it.
- R: The first version expanded into the ordered table, and its own test caught the consequence
  immediately: `memory-trace` moved from `#0a9a94` to `#43bdd4` merely because `git-workflow` was
  focused. That is precisely the instability that put colour on the root in the first place — an
  ordered table means an inserted key shifts every slot after it and repaints communities that did not
  change. Appending makes focusing a strictly additive operation.
- A: Sort the whole table with children included — rejected on the same measurement; it is the
  interleaving that does the damage, not the ordering rule.
- F: `memory-trace/client/src/graphCommunities.ts`, `graphCommunities.test.ts`.

#### D4 - A new facet, because roots cannot tell an alias from a child

- D: `Facets.topic_canonical` (every authorable name -> the canonical slug it denotes) joins
  `topic_roots`. Read only where colour is keyed below the root.
- R: Both an alias and a child map to a root, so through `topic_roots` alone `perf`/`performance` is
  indistinguishable from `merge`/`git-workflow`. That did not matter while colour was keyed at the root
  — both collapse there — and it matters the moment it is not: focusing `memory-trace` would have drawn
  `memory-trace-ui` as a second swatch for one concept, in the one view whose job is to make the
  distinctions honest. The map already existed server-side as `index.resolution()`; only the exposure
  was missing. 12 aliases are authored on this corpus, so this is a live case, not a hypothetical.
- A: Infer alias-ness client-side (e.g. "not in the wheel") — rejected: the wheel holds only qualifying
  topics, so the test would be wrong for every rare canonical slug.
- F: `memory-trace/memory_trace/service.py`, `models.py`, `tests/contract/openapi.v1.json`,
  `tests/contract/types.ts`.
- T: 175 client unit tests (8 new on the focus rules — distinct children, unchanged outside, floor
  waived, lone child derives its parent, alias collapses, child-focus expands its family, legend agrees),
  7 story tests, 213 memory-trace backend tests, typecheck clean, contract regenerated, build OK. Live:
  focusing `git-workflow` took the palette from 20 to 28 distinct node colours.

### Follow-up

- The left-hand topic chips are NOT filtered to roots — they are `Object.entries(facets.topics)
  .slice(0, 10)`, i.e. the ten most-authored slugs. They are all roots today only because every child
  carries 1-6 entries; a child crossing into the top ten after the sweep would displace a root and the
  pane would mix levels without saying so. If roots-only is the intent, it needs to be stated in code.
- The community FLOOR is waived inside a focus but the community assignment is not: the legend still
  lists server-named communities, which are root-level, so a focused view shows child colours against
  parent-named legend rows. Not wrong, but the legend is now less informative than the map it explains.

## 2026-07-27 02:52 - Proposal mode built; its first run rejects the memory-trace split and names why

```yaml
entry_id: mse_c34tddektr8x3t27
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/topic-proposal-mode
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Built proposal mode and ran it on `memory-trace`. It rejected the split, and the rejection is the
  finding: half the concentration is not missing children at all.

### Decisions

#### D1 - Proposal mode is two read-only commands that do not talk to each other

- D: `scripts/propose_topic_children.py` with `gather` (dump the entries under a slug, with the
  co-occurring topics that mark the likely seams) and `score` (take a candidate split, compute the
  projected share, the floor test, the leftover coverage). Neither can write `topics.yaml`.
- R: The design's whole safety argument is that assignment and proposal never merge: assignment writes
  and is bounded by a closed vocabulary, so proposal can afford to be speculative precisely because it
  cannot write. Splitting the tool in two carries that into the implementation — `gather` produces the
  material a judgement is made FROM and makes none itself; `score` can say what a slug would do but
  cannot create one. The floor (8 entries) and the target parent share (20%) live in code rather than in
  a reviewer's head, so a proposal can FAIL cleanly instead of being argued about.
- A: One command that clusters and proposes — rejected: it would put the judgement inside the tool,
  where the reviewer cannot see it, and make a speculative step look like a computed one.
- F: `scripts/propose_topic_children.py`.

#### D2 - The first proposal is REJECTED, and the reason is a redundant tag rather than a missing child

- D: The four candidates that clear the floor (`trail` 40, `trace-shell` 14, `trace-cache` 12,
  `trace-harness` 10) are recorded as a proposal for JNL, and the split is scored REJECT.
- R: The scorer refused it on its own stated rules — three candidates below the floor, and the parent
  landing at 24.3% against a 20% target. Chasing that gap turned up the real shape: **86 of the 202
  `memory-trace` entries (43%) already carry a finer AREA slug**, 63 of them `graph`. For those the
  parent is not under-specified, it is redundant, and no child can fix a tag that should not be there.
  Children alone take it 43.9% → 24.3%; children plus retracting those 86 take it to **8.7%**. So the
  concentration is roughly half a missing-children problem and half a redundant-tag problem, and only
  the first half is a vocabulary question.
  A `graph-view` child was deliberately not proposed for the same reason: 63 of these entries already
  carry the `graph` root, so a Trace-flavoured duplicate would be exactly the unearned depth the
  promotion rule refuses.
- A: Lower the floor so `inspector` (7), `diagram-view` (4) and `trace-api` (3) qualify — rejected: the
  floor is the only thing standing between "earned depth" and a vocabulary that grows whenever someone
  wants a finer label. `inspector` is one entry short and will qualify on its own soon enough.
- F: `docs/2_Todo/memory-trace-children-proposal.md`.
- T: `docs check` OK across 184 files. Every number in the proposal is reproducible from the two
  commands; nothing was written to `topics.yaml`.

### Follow-up

- **Two decisions are now with JNL**, and neither is an agent's to make: approve the four children
  (a governance change to deploy-once state), and rule on whether `memory-trace` should come off entries
  that already carry a finer area slug.
- The second of those needs a **topics `retracts:`** construct that does not exist. Links have one;
  topics do not, so there is currently no legal way to remove a topic an author wrote. That is now on
  the critical path to the sweep rather than a tidy-up item — it is the larger half of the largest
  concentration in the vocabulary.
- Clustering was keyword-led over titles and then reviewed, not a swarm pass over the bodies. Weaker
  evidence than the design asks for, and stated as such in the proposal: 26 of the 116 matched no
  pattern at all, and those boundaries are the ones a swarm would firm up.

## 2026-07-27 07:44 - Correction: depth deflates a parent, retraction is for corrections only

```yaml
entry_id: mse_kys1ygxq2sxcnfbs
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/docs/topic-hierarchy-reframe
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- JNL corrected the `memory-trace` proposal: co-tagging is a hierarchy that is not built yet, not
  redundancy, and the parent stays.

### Decisions

#### D1 - Concentration is measured on canonical, not on reach

- D: The `memory-trace` proposal is rewritten. `graph-view` (63) joins `trail`, `trace-shell`,
  `trace-cache` and `trace-harness` as a candidate child; nothing is retracted; the previous conclusion
  is left visible in the document with the correction beside it.
- R: I read the wrong column. 43.8% is `memory-trace`'s **reach**, and a subsystem root is supposed to
  reach most of the work — `measure_topic_concentration.py` prints canonical and rollup separately and
  says so in its own docstring. What a hierarchy deflates is the **canonical** count: entries that land
  on the root because nothing more specific exists. Today the two are equal (202/202) for one reason
  only — the root has no children, so nothing has anywhere else to land.
  JNL's framing is the operative one: `memory-trace` names the SUBSYSTEM, the finer slug names the
  COMPONENT, and both are true. Store the most specific and derive the ancestors — machinery that
  already exists — and reach is preserved while canonical falls: 202 → **~49 (10.6%)**, filtering still
  finding all 202. So nothing needs removing for the number to come down.
- A: Retract the parent from the 86 co-tagged entries (the previous conclusion) — rejected: it deletes
  true statements to make a number smaller, and it would have cost the subsystem level entirely.
- F: `docs/2_Todo/memory-trace-children-proposal.md`.

#### D2 - `graph` is two areas sharing a word, so `graph-view` is earned after all

- D: `graph-view` is proposed as a `memory-trace` child, reversing the previous document's refusal, and
  `graph`'s own split is queued as the next item rather than folded into this proposal.
- R: I declined it on the reasoning that the `graph` root already covered the seam. Measured, `graph`
  spans two subsystems: 63 entries carry it beside `memory-trace` and are Trace VIEW work (orphans,
  node sizing, force motion); 45 carry it alone and are Seed EDGE-MODEL work (evolution edges, the
  continuity field, MCP sidecar-edge parity). Its existing children — `continuity`, `related-entries`,
  `schema`, `supersession` — are all data-model concepts, which places that slug's centre of gravity on
  the Seed side and leaves the Trace view genuinely unnamed.
- A: Reparent `graph` wholesale under `memory-trace` — rejected: it would drag 45 entries of Seed work
  under the Trace root, which is the same error as the one being corrected, pointed the other way.
- F: `docs/2_Todo/memory-trace-children-proposal.md`.

#### D3 - Topic retraction exists, scoped to corrections

- D: A topic `retracts:` is worth building and is scoped narrowly to **corrections** — a wrong topic, or
  a `derived` block correcting a `write-time` one. It is explicitly not the instrument for reducing a
  parent's share. Recorded in the supersession spec, which already owns the links equivalent.
- R: JNL: "retraction might be useful for corrections, keep it as a narrow use case functionality". The
  guard is a question a reviewer can actually answer — *was this claim wrong?* If the answer is "no, it
  was true, I would just prefer something more specific", the answer is a child slug. Worth writing
  down because the temptation was not hypothetical: this session acted on it and produced a proposal
  arguing to retract 86 true tags.
- A: Leave topic retraction unbuilt — rejected: the cross-source-class gap is real and already recorded
  as step 1 of the consolidation build order. Narrowing its scope is not the same as declining it.
- F: `docs/3_Spec/draft/sidecar-supersession-model.md`.
- T: `docs check` OK across 184 files. Measurements reproducible from
  `measure_topic_concentration.py` and `propose_topic_children.py`.

### Follow-up

- The sweep is **not** blocked on topic retraction. That claim was made twice in this session, on the
  strength of the wrong conclusion, and it is withdrawn: depth is the critical path.
- One decision is with JNL: approve the five children. No entry is rewritten and no topic is removed if
  it lands.
- `graph`'s own two-subsystem split is now the next proposal-mode item.

## 2026-07-27 08:40 - Three swarm cycles: split the axes, and the constrained question scores 0.89

```yaml
entry_id: mse_hxz9sjqsbj0wx2nc
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/topic-swarm-cycles
topics:
  - memory-seed
  - agent-collaboration
```

### Summary

- Ran three swarm cycles over the 116 `memory-trace` entries, improving the worker context each time,
  and ended with the axes judged by separate swarms at 0.89 against authored labels.

### Decisions

#### D1 - Each cycle's context change is derived from the previous cycle's failure

- D: Cycle 1 gave a thin brief and eight candidate children. Cycle 2 added the subsystem definition,
  three boundary rules, `NONE` as a first-class answer, and entry summaries. Cycle 3 split the axes.
- R: JNL asked for iterative cycles improving both the context passed to the workers and the suggestions
  they return, so each change had to answer something measured rather than something imagined. Cycle 1's
  defect was concrete: five entries assigned `trace-api` were all Memory SEED work (link sidecars,
  entry-ids, trailer parsing). The brief had defined eight children and never defined their PARENT, so a
  worker reasoning "validation sounds like a data contract" had no way to know Trace is the viewer and
  Seed is the authoring tool. Adding that one paragraph took `trace-api` to **zero**.
  Cycle 1 → cycle 2 agreement was **81%**, and all 22 moves trace to a specific rule that was added:
  5 × `trace-api`→`NONE` (subsystem), 4 × `trail`→`trace-ui` (frame-beats-content), 4 × →`trace-cache`
  (api/cache boundary), 2 × `UNSURE` resolved (summaries). Disagreement attributable to a stated change
  is evidence the change worked; unattributable churn would have been noise.
- A: Re-run identical prompts for a majority vote — rejected: it measures variance, not whether the
  brief is right, and cycle 1's error was systematic rather than random. Every worker would have made it.
- F: `scripts/propose_topic_children.py`, `docs/4_Reference/topic-swarm-cycles/`.

#### D2 - Area and activity are judged by SEPARATE swarms

- D: Cycle 3 ran two independent swarms over the same 116 entries: one asked only "which part of the
  system", the other only "what kind of work", each told to ignore the other axis.
- R: JNL's design: "splitting the task of area swarms and activity swarms so that they can recommend
  independently based on the information". It pays three ways. The questions get smaller, which is the
  whole constrained-choice argument. The results become independent evidence that can be cross-checked
  rather than one worker's compound judgement. And **a mismatch between the axes becomes visible**: the
  area swarm has a home for harness work (`trace-harness`, 8) while the activity swarm does not, so
  Storybook and Playwright were filed as `ui-design` — which is how the missing `testing` activity was
  found. A single worker answering both axes would have papered over it.
- A: One worker per entry answering both axes — rejected on the above; it also re-decides activity labels
  that first-hand authors already wrote.
- F: `docs/2_Todo/memory-trace-children-proposal.md`.
- T: Activity swarm vs authored activity tags: **85 of 95 = 0.89**. The aborted pilot scored 0.583/0.613
  asking for ~2 slugs from 23 across both axes at once. 20 further entries carry no authored activity at
  all. Area: `trail` 32, `TRACE-WIDE` 26, `NONE` 17, `trace-cache` 12, `trace-harness` 8, `graph` 7
  (+63 dual-tagged = 70), `diagram-view` 4, `inspector` 1. Neither area worker proposed a new child.

#### D3 - `trace-ui` is withdrawn; the frame has no area child

- D: `trace-ui` is dropped from the candidate set. The 26 entries the area swarm called `TRACE-WIDE`
  stay on `memory-trace` itself, with `ui-design` as their activity.
- R: JNL corrected the axis: UI is an ACTIVITY, not an area. The measurement agreed before the swarm did
  — **77% of the entries I had called `trace-ui` already carried `ui-design`**, so the slug was an area
  name for something the other axis already said. The six that lacked it are all from 10–12 July, before
  `ui-design` was applied consistently, which makes the gap historical rather than conceptual.
  The consequence is worth stating plainly: `memory-trace`'s canonical count will not fall as far as the
  ~49 first projected, because frame work legitimately belongs to the app as a whole. The app frame is
  the app, not a part of it.
- A: Keep it as an area child anyway — rejected: it would duplicate an activity slug in the area axis and
  make every framework entry carry two names for one fact.
- F: `docs/2_Todo/memory-trace-children-proposal.md`.

### Follow-up

- **Proposal detection must run over the POOLED result, not per worker.** Each activity worker saw 58
  entries and neither proposed a `testing` slug, because it clears the floor of 8 only when the halves are
  combined. A category spread thinly across batches is invisible to every worker individually. This is a
  protocol fix the sweep needs, not a one-off.
- The two activity workers were markedly less consistent with each other than the two area workers: one
  reached for child slugs on 28% of rows, the other on 3%, and one used `git-workflow` for lifecycle-edge
  and worktree-switching work that is not git workflow. The activity axis needs either a sharper brief or
  two workers per batch with adjudication.
- Still with JNL: approve the children (`graph` 70, `trail` 32, `trace-cache` 12, `trace-harness` 8 at
  the floor), plus `lifecycle-edges` and a new `testing` activity. Nothing is written to `topics.yaml`.

## 2026-07-27 09:19 - Controlled swarm experiment: separate both axes, and one sentence about depth is worth 28 points

```yaml
entry_id: mse_fdvkdmq9mvfg9bv0
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/swarm-reliability-experiment
topics:
  - memory-seed
  - agent-collaboration
```

### Summary

- Ran the controlled comparison JNL asked for. Separating the axes is right, and the reason I doubted it
  turned out to be a defect in my own brief.

### Decisions

#### D1 - Separate both axes, including activity

- D: Area and activity are each judged by their own worker. The earlier doubt about activity is
  withdrawn.
- R: JNL asked whether separating helps, and I had been asserting it rather than measuring it — I had
  never run a combined-axis worker, so there was no control. Run properly over the same 39 rows: area
  reliability **97% separated vs 87% combined**; activity looked *worse* separated (54% vs 82%) until
  the number was decomposed. Collapsed to root both arms sit at 85%, and 12 of the 18 disagreements were
  parent-vs-child of one family. The workers agreed on the concept and differed on depth.
  Validity was flat across all four workers (84-90%), so separation does not buy correctness. What it
  buys is **auditability**: the combined workers produced FEWER coherence violations (0-1 vs 2-2),
  because one worker holding both answers rationalises them into a coherent-sounding pair. That is the
  one job independence is structurally required for, and it is how the missing `testing` activity
  surfaced.
- A: Hybrid — combined labelling plus an independent audit sample — was my stated prior going in. It is
  no longer justified: once the depth rule landed, separated activity matched combined at 82%, so the
  cheaper-looking option costs the same and audits worse.
- F: `docs/4_Reference/topic-swarm-reliability-experiment.md`,
  `docs/4_Reference/topic-swarm-cycles/`.

#### D2 - Depth is decided by a test, not by judgement

- D: The activity brief replaces "prefer a CHILD slug where the evidence supports it" with: *would filing
  this entry under a SIBLING of that child be plainly wrong?* Two plausible siblings means the parent
  wins. Four worked examples, drawn from the observed disagreements.
- R: The 54% was not a broken axis, it was an under-specified instruction: "where the evidence supports
  it" delegates depth to taste, and two workers took it differently (28% child usage against 3%).
  Changing that one sentence, with everything else byte-identical and the previous run as control, took
  exact-slug reliability **54% -> 82%** while validity stayed flat at 87-90%. Depth-only disagreements
  fell 12 -> 3.
  Worth recording as a general lesson: **one vague sentence was worth more than any architectural change
  measured here**. Look for the under-specified instruction before redesigning the pipeline.
- A: Forbid child slugs entirely — rejected: it would guarantee reliability by making the hierarchy
  useless, and populating the thin children is the whole point of the sweep.
- F: `docs/4_Reference/topic-swarm-reliability-experiment.md`.
- T: Reliability (2 workers, same brief, same 39 rows): area 97%/87%, activity 82% after the fix from
  54%. Validity vs authored tags 84-90% throughout. Coherence violations 0-1 combined vs 2-2 separated.

### Follow-up

- **82% is an in-sample number.** The depth rule was written after seeing the disagreements it fixes, so
  it will be optimistic. It has to be scored once on the untouched 77 entries before it is trusted, and
  that held-out run has not been done.
- Two workers per arm gives a noisy estimate and no majority label; three would give both.
- Validity still rests on authored tags, which the pilot adjudication already found are upheld only ~50%
  of the time when contested. The adjudicated gold set remains the expensive missing piece, and every
  validity figure in this session carries that caveat.
- Only one coherence rule exists (`trace-harness` implies a test-shaped activity). `lifecycle-edges`
  implying a model activity, and area `NONE` implying a process activity, are unwritten.

## 2026-07-27 09:53 - Held-out run: reliability generalises, validity is unestablished, and the behavioural gold set failed

```yaml
entry_id: mse_b95ahk5wz1pzeedn
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/area-gold-and-adjudication
topics:
  - memory-seed
  - agent-collaboration
```

### Summary

- Scored the frozen briefs once on 77 unseen entries. Reliability transferred, my earlier 0.89 validity
  claim did not, and the file-derived gold set turned out unsound.

### Decisions

#### D1 - The 0.89 validity claim is withdrawn; 74% is the estimate

- D: Held-out validity is **74% (n=128)** against authored activity tags, not the 87-90% (n=62) reported
  from the dev set. The claim that the constrained question comfortably clears the 0.70 pilot gate is
  withdrawn — it sits just above it.
- R: I reported 0.89 without a confidence interval, on 31 tagged entries. With one it reads 87%
  [75-99%], which is not a number anyone should have acted on. The held-out figure has twice the sample
  and lands at 74% [66-81%]; the intervals overlap, so the two runs are not cleanly distinguishable, but
  the larger sample is the better estimate and the smaller one was a high roll presented as a result.
  The lesson is procedural, not statistical: **a validity figure quoted against a gate must carry its
  interval**, or a lucky 31-entry sample reads as decisive evidence.
- A: Average the two runs — rejected: it launders an in-sample number into the estimate. The held-out
  run is the one designed to be believed.
- F: `docs/4_Reference/topic-swarm-reliability-experiment.md`.

#### D2 - The depth rule generalises; area's weak boundary is `NONE`

- D: Keep both briefs as frozen. Held-out reliability: area **90%** (dev 97%), activity **82% exact /
  90% root** — identical to dev.
- R: The activity rule was written *after* seeing the disagreements it fixes, so the honest expectation
  was decay. It transferred exactly, which is the strongest evidence in the session that the fix was a
  real clarification rather than a fit to 39 rows. Area's 7-point drop is the ordinary cost of tuning
  definitions against rows already inspected, and its shape is diagnostic: **three of the eight
  disagreements involve `NONE`**, the least-defined term in the brief. That names the next cycle's one
  variable without any further measurement.
- A: Re-tune now on the held-out failures — rejected: that would consume the only unseen data the
  project has and leave nothing to check the next brief against.
- F: `docs/4_Reference/topic-swarm-cycles/`.

#### D3 - The file-derived gold set is recorded as a FAILURE, not shipped

- D: `scripts/derive_area_gold.py` is committed with its result stated as negative. It is not used to
  score anything.
- R: 87% of entries record their touched files, so the area axis looked derivable with no human
  judgement. It is not. v1 labelled by "exactly one mappable area" and called *"Trail complete: brackets
  and two-stage selection"* `trace-api`, because the only mappable file it listed was `models.py`. v2
  added rarity weighting (the idf principle `link audit` already uses, dropping paths in >3% of
  entries); better built, but 76% → 78% and the same misses survived.
  Reading them settles it: **the workers are right and the derived labels are wrong.** Files record what
  an entry TOUCHED; topics record what it was ABOUT, and a Trail feature that also adjusts a payload
  model touches the model. No path map closes that gap. Publishing 78% as "validity" would have dressed
  a wrong label as objective purely because a script produced it — the exact failure mode this project
  keeps guarding against.
- A: Ship it as a weak signal anyway — rejected: a bad gold set is worse than none, because every later
  measurement inherits its errors silently.
- F: `scripts/derive_area_gold.py`, `docs/4_Reference/topic-swarm-reliability-experiment.md`.
- T: Held-out reliability area 90%, activity 82%/90%. Validity 74% (n=128) vs authored tags, 78% vs the
  failed gold set. All six workers' raw answers committed.

### Follow-up

- **22 of 77 held-out rows are contested** and are queued in `docs/2_Todo/adjudication-queue.md` with the
  decision text and touched files inline. Ruling those is the only remaining route to ground truth, and
  the 55 uncontested rows deliberately need none of JNL's time.
- The vocabulary decision does not depend on the gap — `graph` (70), `trail` (32), `trace-cache` (12)
  rest on counts three cycles agreed on, with 90% held-out reliability. **The sweep does.**
- A behavioural gold set could still work weighted (which area holds MOST of an entry's rare files) or
  restricted to entries whose files sit wholly in one area. Both give a much smaller set, which is
  correct: a gold set earns authority by being unarguable, not by being large.

## 2026-07-27 12:53 - Adjudicating 22 rows found the vocabulary's largest gap: a quarter of the corpus has no activity

```yaml
entry_id: mse_8392b2vme5pgvr55
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/docs/adjudicated-vocabulary
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- Ruled the 22 contested rows, JNL corrected them, and the corrections turned up four vocabulary gaps
  bigger than anything the swarm had found.

### Decisions

#### D1 - `feature-build` is the largest gap in the vocabulary

- D: A new ACTIVITY slug `feature-build`, distinguished from `ui-design` by: *a capability that did not
  exist before* versus *changing how an existing thing looks or behaves*.
- R: JNL's correction to row 5. I had ruled `activity-none` because nothing fitted "built the link-audit
  capability", and treated that as a one-off. Measured, it is **116 of 468 entries — 25% of the corpus
  carry no activity slug at all**. The axis has slugs for fixing, designing, documenting, proposing,
  merging and shipping, and none for BUILDING, which is most of what the project does. `ui-design`
  had been quietly absorbing it for UI work, which is exactly why the gap stayed invisible until a
  backend row forced the question.
  Applying the rule across the 22 moved six rows and took `ui-design` from 9 to 4 — so the line between
  the two is one decision, not six, and worth stating explicitly rather than left to taste. That is the
  same lesson as the depth rule, which was worth 28 points of reliability.
- A: Leave those entries with no activity — rejected: it makes the axis silent on the project's main
  activity, and the silence had already been misread (by me) as an authoring lapse rather than a gap.
- F: `docs/2_Todo/memory-trace-children-proposal.md`, `docs/2_Todo/adjudication-queue.md`.

#### D2 - The pane question and the aboutness question are different, and only one supports a child

- D: `inspector` (~33) and `settings` (~14) join the proposed children of `memory-trace`; `context` (~7)
  is declined for now.
- R: JNL, adjudicating row 21: *"a trace might also benefit from children for each of the panes"*. That
  reframes the question. The swarm gave `inspector` **1**, because it was answering *what is this entry
  ABOUT*, and inspector work is usually a slice of a larger change rather than its headline. Asking
  *which pane did this TOUCH* gives ~33. Both readings are defensible; only the second earns a child.
  Recorded because the discrepancy is 33x and neither number is wrong — they answer different questions,
  and which one the sweep asks decides what the hierarchy looks like.
- A: Take the swarm's 1 and decline `inspector` — rejected: it would leave the reader pane, a third of
  Trace work by the pane reading, with no slug at all.
- F: `docs/2_Todo/memory-trace-children-proposal.md`.

#### D3 - Merges belong to the session machinery, not to nothing

- D: Rows 4 and 7 move from `NONE` to `session-logging`, matching JNL's own ruling on row 1. `package`
  (~66) becomes a proposed area root.
- R: I had defaulted integration merges to `NONE` — "not about the system". JNL ruled row 1 as
  `session-logging` and the reason is better than mine: a merge that fuses branch entries into the
  chronology IS work on the session machinery, and the corpus records it as such. Three of 22 rows moved
  on one principle. Likewise `package`: I forced row 3 to `TRACE-WIDE` for want of anywhere better, and
  packaging measures ~66 entries scattered across `memory-seed`, `release` and `TRACE-WIDE`.
- A: Keep `NONE` for merges — rejected: it is the answer that stops a real category from ever earning a
  slug, because everything it would claim is filed as absence.
- F: `docs/2_Todo/adjudication-queue.md`.
- T: 22 of 22 rows ruled with reasoning inline. Six vocabulary items now evidenced: `feature-build`
  (~116), `package` (~66), `inspector` (~33), `settings` (~14), `lifecycle-edges` (~45), `testing` (~8).

### Follow-up

- **These rulings are mine, not independent ground truth** — I ran the swarm, so scoring it against them
  is circular. JNL's corrections are the independent part, and they changed 3 of 22 outright plus four
  vocabulary additions, which is a useful measure in itself: my unaided adjudication agreed with JNL on
  roughly 86% of rows but missed every one of the four gaps.
- The counts for `package`, `settings`, `inspector` and `feature-build` are keyword estimates over titles
  and bodies — the same method that put `trail` at 40 when the swarm found 31. Order-of-magnitude only.
- The vocabulary decision is now **one consolidated ask** in the children proposal rather than several.
  Nothing else is blocked on it.

## 2026-07-27 13:12 - Name every pane: TRACE-WIDE stops being where homeless work goes

```yaml
entry_id: mse_jyy10crh6wyzrr22
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/docs/pane-area-children
topics:
  - memory-trace
  - proposal-lifecycle
```

### Summary

- JNL: every pane should be named so it can act as an area child. Named them from the app's real
  regions, and three of six `TRACE-WIDE` rulings immediately found a home.

### Decisions

#### D1 - The pane slugs come from the DOM regions, not from invented groupings

- D: `graph`, `trail`, `inspector`, `topbar`, `navigation`, `settings`, `workspace-bar` — each named
  after a region that actually exists in `App.tsx`. `diagram-view` (3) stays below the floor.
- R: I had been proposing pane children from memory and getting the grain wrong. Reading the component
  tree fixed two errors at once. **`navigation` replaces `context`**: what I had proposed as a child was
  a SECTION inside `<aside class="navigation-pane">`, alongside the topics filter and project stats — the
  pane is the right unit, and at 14 it clears the floor where the section's 7 did not. And **`topbar` and
  `workspace-bar` are panes I never proposed at all**, both clearing the floor, covering the find bar,
  worktree picker, view switcher, graph scope and edge filters — a large share of what I had spent the
  night filing as `TRACE-WIDE` for want of a name.
- A: Keep inventing groupings from the entries — rejected: the code is the authority on what surfaces
  exist, and it is cheaper to read than to infer.
- F: `docs/2_Todo/memory-trace-children-proposal.md`.

#### D2 - `TRACE-WIDE` means cross-cutting, not unclassified

- D: Re-ruled the 22 adjudicated rows a third time with the full pane set. Three of six `TRACE-WIDE`
  rulings moved to `topbar` (search routing, full-text navigation, the unified find bar). Three remain.
- R: The value of naming every surface is not the new slugs, it is what it does to the residual bucket.
  Before, `TRACE-WIDE` collected two different things: work that genuinely spans the app, and work whose
  home had no name. Only the first is a real category, and while the second hid inside it the count was
  uninterpretable — I read 8 of 22 as "the frame is a big deal" when much of it was "I had nowhere to put
  this". What is left is a package-wide module rename, a multi-pane design pass, and a loading mechanism
  JNL ruled wide-impact — all genuinely cross-cutting.
- A: Leave `TRACE-WIDE` as-is and accept it as a large bucket — rejected: a residual category that
  absorbs the unnamed will always look load-bearing, and will never shrink on its own.
- F: `docs/2_Todo/adjudication-queue.md`.
- T: Pane counts over the 203 memory-trace entries: graph 146, trail 133, inspector 40, topbar 17,
  navigation 14, settings 14, workspace-bar 11, diagram-view 3.

### Follow-up

- **The pane counts are upper bounds and should not be quoted as ownership.** They are keyword presence
  over titles and bodies: `trail` 133 and `graph` 146 against 203 entries means most entries MENTION
  both. The swarm's stricter aboutness reading gave trail 32 and graph 70. What the numbers establish is
  that each pane is nameable and non-trivial, nothing more.
- `diagram-view` is a real surface (`DiagramViewer.tsx`) that has not earned a slug at 3 entries. Worth
  revisiting after any diagram work, not now.
- The `inspector` discrepancy stands unresolved and is the clearest case of the two readings diverging:
  1 by aboutness, 40 by pane. Whichever the sweep asks decides what the hierarchy looks like.

## 2026-07-27 14:03 - Two hierarchy rules: levels must be bounded, and depth below generation 2 is free

```yaml
entry_id: mse_ttym9avz2hhf7s6s
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/depth-free-hierarchy
topics:
  - memory-seed
  - proposal-lifecycle
```

### Summary

- JNL settled two rules for the topic hierarchy, and both are now enforced in the proposal scorer rather
  than left as things to remember.

### Decisions

#### D1 - Levels must be well bounded, and physical boundaries are the strongest kind

- D: A child earns its place by being distinguishable **without judgement**, not merely by being
  narrower. Where no boundary exists, the level is not created however large the parent.
- R: JNL's rule, and the `panes` split is why it is stated this way. Those six children are bounded by
  the DOM — a change is in one `<aside>` or another — so two siblings *cannot* overlap. That is a
  stronger guarantee than any definition I would have written, and it is why reading the component tree
  beat inferring groupings from entry text: I had proposed `context` as a child when it is a SECTION
  inside the navigation pane, and had missed `topbar` and `workspace-bar` entirely.
  `ui-design` (100) is the open counter-case: its plausible children — layout, theme, typography, motion,
  accessibility — are judgement categories with no physical edge, so the rule says leave it flat despite
  it being the largest activity in the vocabulary. A rule that only ever licenses more depth would not be
  a rule.
- A: Bound children by definition alone — rejected: that is what produced `context`, and what let two
  independent workers each invent their own `trail`/`trace-ui` boundary in cycle 1.
- F: `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md`.

#### D2 - The floor applies to generation 2 only

- D: `MIN_CHILD_ENTRIES` (8) governs whether a new BRANCH is justified. From generation 3 down there is
  **no floor**: a one-off grandchild is acceptable. Implemented as `FLOOR_APPLIES_UP_TO_GENERATION` in
  the scorer, which now reports the generation and says which regime applied.
- R: JNL: depth is cheap once a parent exists to aggregate it, because every consumer rolls up. Verified
  rather than assumed — `expand_topic_filter` matches a parent against every descendant **transitively**
  to three levels, `ancestors()` returns the full chain, and `axis_of()` inherits through two hops, so a
  grandchild needs no new machinery. Community colour keys on the ROOT, so the palette cannot grow
  either. A grandchild is therefore strictly more information than its parent carried alone, at no cost
  to a reader who wants the coarser view.
  The rule also creates a deliberate incentive to GROUP rather than flatten: sub-floor candidates sharing
  a natural parent should be proposed underneath it. `panes` is the worked example — six children of 2-6
  entries, every one failing the floor alone, all floor-free once `panes` (23) carried them. The scorer
  now says so in its rejection message rather than leaving the reader to work it out.
- A: Drop the floor entirely — rejected: at generation 2 it is the only check on whether a new branch is
  justified at all, which is the expensive decision. Refining an existing branch is not.
- F: `scripts/propose_topic_children.py`, `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md`.
- T: Verified both regimes: a generation-2 split reports "floor of 8 applies" and rejects `skills` (6);
  a generation-3 split under `merge` accepts children of 1 and 2 entries with "no floor at this depth".

### Follow-up

- **Do not conflate this with `COMMUNITY_TOPIC_FLOOR` (10)** in `graphCommunities.ts`. That decides which
  topics may NAME a graph community — how many colours the palette hands out — and it stays. The two
  floors govern different things and now differ in value, so the distinction is written into both.
- `memory-seed` (122, a declared RESIDUAL) is the next candidate for the treatment. Proposal mode found
  `entry-schema` (16) and `topic-vocabulary` (15) clearing the floor and the parent falling to 17%, under
  target — but my hand-written patterns reached only 42 of 122 entries. The 80 unmatched hold at least
  quality measurement, docs-lifecycle tooling and provenance/authority. That is the case the swarm exists
  for; hand-authored regexes are the thing it replaces.
- The topic vocabulary itself has **no area slug** — 29 entries by keyword, including all of tonight's
  work. The area doing the classifying cannot be classified.

## 2026-07-27 14:14 - The third hierarchy rule: a slug must fit every ancestor, or roll-up is a lie

```yaml
entry_id: mse_wj0rjzw49kyj4kxs
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/depth-free-hierarchy
topics:
  - memory-seed
  - proposal-lifecycle
related_entries:
  - mse_ttym9avz2hhf7s6s
```

### Summary

- JNL supplied the constraint that pays for the previous entry's rule 2: workers must check a candidate
  slug against its whole ancestor chain, not just its immediate parent.

### Decision

- D: A grandchild slug is only correct if **every** level above it is also true. Emitting `inspector`
  asserts inspector-pane work AND `panes` work AND `memory-trace` work; if any link is false the slug is
  false, and the worker emits the deepest ancestor that is true, or nothing. Taught inline in
  `topic_swarm.md` §2 rule 4 — where the pressure toward specificity is applied — rather than as a
  separate rule read after the choice is made. `propose_topic_children.py` now prints the chain a
  candidate must satisfy (`git-workflow > merge > <child>`) in both `gather` and `score`.
- R: JNL's rule, and it is the counterweight rule 2 was missing. Depth is free *because* consumers roll
  up, and roll-up is a promise: a filter on `memory-trace` returns everything filed beneath it. So a leaf
  that breaks its chain does not mislabel one entry — it pollutes every ancestor's filter above it, and
  **invisibly**, because nobody reading `memory-trace` can see which leaf put a stray entry there. The
  deeper the tree, the further one bad fit propagates; without this rule, "no floor below generation 2"
  compounds its own errors. The two pressures are now explicitly bounded against each other: rule 4 says
  take the narrowest slug that fits, this says narrower is better only while every level above stays
  true, and the tie-break is stated — a correct parent beats a plausible child.
- A: Enforce it mechanically in the scorer — rejected as impossible: the tree structure already
  guarantees the *structural* half (entries carrying a child roll up by construction), and the half that
  actually fails is semantic fit, which no script can judge. Printing the chain at the point of decision
  is the honest substitute; claiming a check that only tests the trivially-true half would be worse than
  none.
- F: `.memory-seed/skills/topic_swarm.md` and its seed template copy (kept byte-identical),
  `scripts/propose_topic_children.py`, `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md`.
- T: 779 tests pass; `docs check`, `docs index --check` and `links check` clean. Chain line verified at
  both depths: `memory-seed > <child>` for a root, `git-workflow > merge > <child>` at generation 3.

### Follow-up

- The rule raises the cost of a **wrong parent** far above the cost of a wrong leaf, which argues for
  auditing the vocabulary top-down rather than bottom-up when the swarm eventually runs.

## 2026-07-27 14:48 - One approvable vocabulary list, and the counts it quotes do not reproduce

```yaml
entry_id: mse_z110ha6vj0zx354s
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/docs/consolidated-vocabulary-ask
topics:
  - memory-seed
  - proposal-lifecycle
related_entries:
  - mse_wj0rjzw49kyj4kxs
```

### Summary

- Consolidating three generations of vocabulary decisions into one approvable table required
  re-deriving the counts, and the counts the document was asking JNL to sign did not reproduce.

### Decisions

#### D1 - The ask is one table with an evidence column, tiered by measurement strength

- D: `memory-trace-children-proposal.md` now opens with a single table — slug, parent, axis, count,
  **evidence**, which rule admits it, and whether it is ready. Fifteen rows, of which **three are
  measurement-backed and ready** (`graph` 70, `trail` 30, `trace-cache` 12), one needs a call, and the
  rest are argued but unmeasured. Everything below the table was demoted to "the reasoning and the
  record" and the competing asks removed.
- R: The document had layered three generations — the first run (`graph-view`, `trace-ui`), the split-axis
  cycles, and the pane ontology — and still ended with a "What is being asked" section requesting *five
  `parent: memory-trace` slugs*, listing `inspector` and `diagram-view` as REJECTED for want of 8
  entries. Both had been overturned hours earlier: `panes` is their parent and the floor does not apply
  at generation 3. Nothing was wrong with any individual section; the document as a whole asked for
  something nobody had decided.
  The evidence column is the substantive change. The rows are not equally supported — a swarm-agreed 30
  and a keyword-estimated 116 were being presented in the same weight of type — and separating them
  turned "approve nine slugs" into "approve three now, measure the rest", which is a decision JNL can
  actually make tonight. The three ready rows carry the whole concentration problem: 43.2% -> 16.7%,
  already under the 20% target, so the unmeasured rows block nothing.
- A: Rename the file to match its outgrown scope - rejected: it now covers a `memory-seed` child, a new
  root and two activity slugs, but renaming churns the docs index and every inbound link for a title
  the H1 can carry instead.
- F: `docs/2_Todo/memory-trace-children-proposal.md` (restructured), `docs/2_Todo/README.md`.

#### D2 - Quote the agreed column, which corrected six counts and flipped `trace-harness`

- D: `scripts/tally_swarm_area.py` re-derives the area counts from the committed worker TSVs and prints
  all three columns (worker 1, worker 2, **agreed**), and the split it feeds the scorer uses the agreed
  label only. Rule stated in the module docstring: *a count both workers reached independently is
  evidence; a count one worker reached is a hypothesis.*
- R: The document's cycle-3 table (`TRACE-WIDE` 26, `NONE` 17, `trace-harness` 8, `trace-api` 2) could
  not be reproduced from either worker file or from any stated rule for combining them — it had been
  read off a live result that was never saved. Since the whole point of committing the TSVs was
  auditability, a table that does not reproduce from them is worse than no table.
  The correction is not cosmetic. **`trace-harness` was presented as "8, exactly at the floor" and is 7
  agreed — it fails.** Worker 1 said 9, worker 2 said 7, so it clears the floor on one reading and fails
  on the other, and it has no grouping parent to fall under because a test harness is not a pane. That
  is now the single open decision in the ask, surfaced rather than resolved: it is a judgement about
  JNL's own vocabulary, and quoting the favourable number would have been arguing rather than measuring.
  Agreement is 107/116 = **92%**, which is the reliability figure the held-out run predicted (90%).
- A: Quote worker 1, or the union - rejected: both are the favourable reading, and the union is how a
  swarm's disagreements get laundered into consensus.
- F: `scripts/tally_swarm_area.py` (new), `docs/2_Todo/memory-trace-children-proposal.md`.
- T: Live scorer run committed verbatim into the document:
  `graph` 70 ok, `trail` 30 ok, `trace-cache` 12 ok, `trace-harness` 7 UNDER FLOOR, residual 79 = 16.7%.

### Follow-up

- **Do not `cat` the swarm TSVs.** `B_area1.tsv` has no trailing newline, so concatenation glues its last
  record to the first record of the next file and silently corrupts both — it produced a phantom label
  `trace-harnessmse_xcsvhpw3bncdxq20` that took a round to diagnose. The tally script reads line-by-line
  and carries the warning in a comment.
- **The pane split has never been measured over the population.** The swarm judged 116 entries against a
  vocabulary with no pane labels, so four of the six pane children rest on keyword estimates plus the DOM
  boundary argument. One area pass over the 79-entry residual with the pane vocabulary in hand closes
  that, `panes`' own floor test, and the `inspector` 1-vs-40 question together.
- `graph` and `lifecycle-edges` must land together — reparenting `graph` under `memory-trace` while 45 of
  its entries are Seed-side edge work is exactly the ancestor-fit violation the third rule forbids.

## 2026-07-27 14:58 - Floor down to 5: the constant stopped carrying the depth question

```yaml
entry_id: mse_zcmnjhax0k4w6nk5
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/floor-of-five
topics:
  - memory-seed
  - proposal-lifecycle
related_entries:
  - mse_z110ha6vj0zx354s
```

### Summary

- JNL lowered `MIN_CHILD_ENTRIES` from 8 to 5, which turned the open `trace-harness` question into a
  fourth ready row and made the `memory-trace` split ACCEPTABLE.

### Decision

- D: `MIN_CHILD_ENTRIES = 5`. The scorer now accepts the `memory-trace` split outright: `graph` 70,
  `trail` 30, `trace-cache` 12, `trace-harness` 7, residual **85 = 18.0%**, under the 20% target.
- R: JNL's call. The 8 was set before `FLOOR_APPLIES_UP_TO_GENERATION` existed, when a child was the
  only way to add specificity and this one constant was carrying the entire weight of the depth
  question. It is not any more — the generation rule decides *where* the floor bites, well-boundedness
  decides whether a level is coherent, and what is left for the number is the narrow job of asking
  whether a new BRANCH has evidence behind it. 8 was too blunt for that: `trace-harness` has a
  `.storybook/` directory, a Playwright config and a CI job behind it, and a rule that calls that
  unearned is measuring the wrong thing rather than measuring strictly.
  The better property is that it removes a dependency on which worker you believe. `trace-harness` was
  9 by worker 1 and 7 by worker 2, so under a floor of 8 the answer changed with the reading; under 5 it
  does not. A threshold that lands inside the spread of the measurement is a threshold in the wrong
  place.
- A: Approve `trace-harness` as a one-off exception at 8 - rejected: the same borderline recurs on the
  next category, and an exception is a floor nobody can quote.
- F: `scripts/propose_topic_children.py` (the constant + why it moved),
  `docs/2_Todo/hierarchical-topic-vocabulary-proposal.md` rule 2,
  `docs/2_Todo/memory-trace-children-proposal.md` (ask table, live run, projection, front matter),
  `scripts/tally_swarm_area.py` (docstring), `docs/2_Todo/README.md`.
- T: Re-scored live. Generation 2 now reports "floor of 5 applies" and returns
  `ACCEPTABLE: every child clears 5, parent falls to 18.0%`. Generation 3 still reports NO FLOOR, so
  the two rules remain independent. `trace-api` (1 agreed) is still rejected, correctly - a single
  entry is not a category at any floor.

### Follow-up

- **The 18.0% figure supersedes the 16.7% quoted an hour earlier.** That run had also claimed
  `inspector` (1), `diagram-view` (4) and `trace-api` (1) as generation-2 children, which is not the
  proposal - the first two belong under `panes` at generation 3. Scoring the actual shape gives 85/18.0%.
- **This retroactively clears candidates rejected earlier today.** The `memory-seed` proposal run
  declined `skills` at 6 against the old floor; it clears 5. Anything previously rejected in the 5-7
  band should be re-scored before being treated as settled.
- `COMMUNITY_TOPIC_FLOOR` (10) in `graphCommunities.ts` is untouched and now differs from this one by
  twice as much. It governs how many colours the palette hands out, not what the vocabulary may say.

## 2026-07-27 15:07 - memory-seed splits four ways, and lifecycle-edges is 63 not 45

```yaml
entry_id: mse_s71wpg0bwqck6cy0
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/docs/memory-seed-children
topics:
  - memory-seed
  - proposal-lifecycle
related_entries:
  - mse_zcmnjhax0k4w6nk5
```

### Summary

- Re-scoring `memory-seed` at the new floor gives four children and one consolidated ruling covering
  both roots, and exposed that `lifecycle-edges` had been counted at 45 when it is 63.

### Decisions

#### D1 - Four children for memory-seed, each bounded by a path

- D: `lifecycle-edges` (35 here, **63** overall), `topic-vocabulary` (25), `test-suite` (13),
  `docs-lifecycle` (8). Scorer: **ACCEPTABLE**, `memory-seed` falls 26.6% -> **9.5%**. Both roots now
  clear the 20% target on their ready rows alone, so the ask is one ruling over eight slugs rather than
  two rulings over two roots.
- R: `memory-seed` was 126 entries with no children at all - after the Trace split, the largest
  concentration left. Each child is bounded by a path rather than a definition, which is rule 1:
  `tests/`, `docs/`, `.memory-seed/sessions/links/`, `memory_seed/topics.py` + `topics.yaml`. None of
  them requires a judgement about where one ends and the next begins.
  `topic-vocabulary` closes a gap this work had been living inside - the area doing the classifying had
  no slug, so every entry about the vocabulary, including all of today's, landed on the root.
  Classification was by READING all 126 titles, not by regex. The earlier attempt used hand-written
  patterns and reached 42 of 126, and its unmatched 84 were where the real boundaries were. Reading
  reached 81, and the 45 left over turned out to be the answer rather than a failure - see the
  follow-up.
- A: Propose `worktree` (4) and `constitution` (4) as well - declined by the floor, correctly. Both are
  real distinctions with clean boundaries and neither has 5 entries yet. At a floor of 5 that is a near
  miss, not a dismissal.
- F: `docs/2_Todo/memory-trace-children-proposal.md` (the ask table + a memory-seed run section),
  `docs/4_Reference/memory-seed-area-split.tsv` (new: all 126 rows with their assignment).
- T: `ACCEPTABLE: every child clears 5, parent falls to 9.5%`.

#### D2 - lifecycle-edges is 63, because two sets were being counted separately

- D: Corrected from ~45 to **63** - 13.3% of the corpus, second-largest slug in the whole ask after
  `graph` (70).
- R: Two measurements existed and neither was the total. 45 entries carry `graph` without
  `memory-trace` (the Seed-side edge model, the count the Trace proposal quoted); 35 carry `memory-seed`
  and are edge work (the count this pass produced). They overlap on **17**, so the union is 63. Each
  number was correct about its own question, which is why neither looked wrong.
  It matters because `lifecycle-edges` is the slug that makes `graph` safe to reparent, and at 13.3% it
  is itself near the band that triggers a split - it will earn grandchildren immediately. The ~14
  `Cycle N` link-campaign entries are an obvious `link-swarm`, and at generation 3 no floor applies.
- F: `docs/2_Todo/memory-trace-children-proposal.md`.

### Follow-up

- **The 45-entry residual is healthy, not homeless** - and that is the finding, not the leftovers.
  Reading it back, most already has a better home on an EXISTING root: ~10 session-grammar entries
  carrying `session-logging`, 4 `release`, 4 `mermaid`, 4 worktree-hygiene carrying `git-workflow`. The
  opposite of the `TRACE-WIDE` problem, where the catch-all was absorbing work with nowhere to go. A
  residual made of entries that are already filed correctly is what "done" looks like.
- **The two halves of the ask are not equally evidenced, and the document says so.** The Trace children
  have 92% blind agreement between two workers; the Seed children have one reader who also wrote the
  boundaries - exactly the arrangement the split-swarm experiment was designed to avoid. If one half is
  measured before signing, it should be this one.
- One area swarm pass closes every open gap at once: the 85-entry Trace residual with the pane
  vocabulary, and the 126 Seed entries against these four children. Needs deliberate opt-in - it is a
  model fan-out.

## 2026-07-27 16:38 - Cycle 4: the pane children collapse by 40x, the Seed split validates at 87%

```yaml
entry_id: mse_fdf7war89az90kwy
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/docs/area-swarm-results
topics:
  - memory-seed
  - proposal-lifecycle
related_entries:
  - mse_s71wpg0bwqck6cy0
```

### Summary

- JNL authorised the swarm. Ten haiku workers judged both open populations; every area count in the ask
  is now measured, both splits score ACCEPTABLE, and the pane children turned out to be a mirage.

### Decisions

#### D1 - The pane children collapse; `panes` survives on different grounds

- D: Swarm-agreed counts against the keyword estimates the proposal carried: `inspector` **1** (est ~40),
  `topbar` **4** (~17), `navigation` **1** (~14), `diagram-view` **2** (~4), and `settings` and
  `workspace-bar` **0** each - neither worker placed a single entry on either. `panes` itself clears the
  floor at **13**. Recommended: create `panes`, `topbar` and `diagram-view`; `inspector` and `navigation`
  are JNL's call at 1 each; **do not create `settings` or `workspace-bar`**.
- R: The keyword column was counting which pane a change MENTIONED. A display pane is mentioned by nearly
  everything, so the estimate measured exposure, not subject. The document had flagged all six as
  order-of-magnitude - the order of magnitude was wrong, by up to 40x. This is the `inspector` 1-vs-40
  question resolved in favour of the smaller number, which is the one that was always answering the
  right question.
  `panes` surviving is the interesting part, and not for the reason it was proposed. **5 of its 13 are
  entries where a worker chose `panes` itself** - the work genuinely spanned several panes and no child
  fitted. So it clears the floor even if every child is dropped, which makes it a grouping parent doing
  its job rather than a bag holding six thin slugs.
  Rules 1 and 2 both admit `settings` and `workspace-bar` - perfectly bounded, no floor at generation 3 -
  and neither rule says to create a slug for which no evidence exists at all. A slug with no precedent is
  one that gets applied wrongly the first time it is used.
- A: Create all six on the DOM-boundary argument alone - rejected on the above. Drop `panes` too and keep
  the Trace tree flat - rejected: its 13 are real, and the 5 multi-pane entries have nowhere else to go
  except back into `TRACE-WIDE`, which is the problem naming the panes was meant to fix.
- F: `docs/2_Todo/memory-trace-children-proposal.md`,
  `docs/4_Reference/topic-swarm-cycles/cycle4-trace-residual-w{1,2}.tsv`.
- T: `ACCEPTABLE: every child clears 5, parent falls to 12.8%` - `graph` 70, `trail` 34, `trace-cache` 14,
  `trace-harness` 12, `panes` 13. Agreement 65/79 = 82%.

#### D2 - The memory-seed split validated; the residual claim was put at risk and held

- D: Counts stand with small corrections - `lifecycle-edges` 32 (I said 35), `topic-vocabulary` 25
  (exact), `test-suite` 12 (13), `docs-lifecycle` **11** (8, the swarm found more). `memory-seed` falls
  26.7% -> **9.9%**. My hand classification agreed with the two blind workers on **103 of 118 = 87%**.
- R: This half was the weaker one - one reader who also wrote the boundaries, which is the arrangement
  the split-swarm experiment exists to avoid. It is now the better-attested half: agreement **119/126 =
  94%**, the highest of any run.
  The residual claim was designed to be falsifiable rather than asserted. `OTHER-ROOT` was put on the
  ballot so a worker could say *the real subject is a different existing area and `memory-seed` is
  merely the package this happened in*. **34 of the 39 non-child entries went there**, 5 were genuinely
  `SEED-WIDE`. Had most come back `SEED-WIDE`, the residual would have been hiding homeless work and the
  four children would have been too few.
- F: `docs/2_Todo/memory-trace-children-proposal.md`,
  `docs/4_Reference/topic-swarm-cycles/cycle4-seed-population-w{1,2}.tsv`,
  `scripts/tally_area_swarm_json.py` (new).
- T: `ACCEPTABLE: every child clears 5, parent falls to 9.9%`.

### Follow-up

- **The projection row has now been wrong twice in opposite directions** - ~49/10.6% (a projection
  assuming everything landed), then 85/18.0% (a real run, of a shape that excluded `panes`). The measured
  answer is 61/12.8%. Quote the run, not the estimate; a scorer figure of the wrong SHAPE is still an
  estimate.
- **Reliability is not validity, and only reliability moved.** 94% agreement means the question is
  well-posed, not that the answers are right. The held-out run put validity at **74%** against authored
  tags and nothing since has touched that. Every count in the ask is a reliable measurement of a
  judgement.
- **The activity axis has had no equivalent pass.** `feature-build` at ~116 would be the largest activity
  slug in the vocabulary if the estimate is close - and `inspector` shows what these estimates are worth.
  It is the one remaining row big enough that being wrong about it would matter.
- Agreement ordering is worth keeping as a control: Seed population 94%, cycle-3 single-area 92%,
  Trace residual 82%. The residual is by construction what the earlier passes found hardest, so the low
  number there is the pool being difficult rather than the method degrading.

## 2026-07-27 17:15 - Vocabulary hierarchy applied: 17 slugs land, and rule 3 blocks its own author

```yaml
entry_id: mse_xz2pr6w8g5xkrecx
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/vocabulary-hierarchy
topics:
  - topic-vocabulary
  - proposal-lifecycle
related_entries:
  - mse_fdf7war89az90kwy
```

### Summary

- JNL approved the ask. Seventeen slugs are live in `topics.yaml`; the one piece that could not ship is
  the piece today's third rule was written to catch.

### Decisions

#### D1 - The four Seed areas are ROOTS, and the residual is renamed `seed-core`

- D: `lifecycle-edges`, `topic-vocabulary`, `test-suite` and `docs-lifecycle` are area ROOTS, not
  children of the old `memory-seed` slug, which is renamed **`seed-core`** with `memory-seed` kept as an
  alias. Also landed: `trail`, `trace-cache`, `trace-harness`, `panes` under `memory-trace`; `topbar`,
  `navigation`, `inspector`, `diagram-view` under `panes`; `package`, `feature-build`, `testing` as
  roots; `continuity`/`related-entries`/`schema`/`supersession` reparented from `graph` to
  `lifecycle-edges`. `settings` and `workspace-bar` DECLINED - zero agreed entries each.
- R: JNL's ruling, on a conflict I surfaced rather than resolved. `topics.yaml` declares the old slug a
  RESIDUAL area - *"a residual area takes no children by definition: anything specific enough to name is
  by construction not residual"* - and its peers `graph`, `retrieval`, `session-logging`, `mcp-tools`
  are already roots. The measured proposal (four children, 26.7% -> 9.9%) would have contradicted the
  file's own stated design, and the swarm had just measured that design HOLDING: 34 of the 39 entries it
  declined to file under a new area belonged to a different existing root.
  The rename follows from the same argument. `memory-seed` read like the whole package, which is exactly
  what a residual must not read like - it invited every entry with no obvious home and collected 127.
  Known consequence, accepted: filtering `seed-core` no longer reaches the four areas. Under the
  residual reading that is correct - those entries were never ABOUT the residual, they were parked there.
- A: Four children of `memory-seed` as measured - rejected by JNL. Roots without the rename - JNL took
  the third option, the rename being what stops the residual re-collecting.
- F: `.memory-seed/topics.yaml`, `docs/2_Todo/memory-trace-children-proposal.md`.
- T: `Topic vocabulary OK` (69 topics, 476 entries). 779 Seed tests + 213 Trace tests pass. Axis
  inherits to generation 3 (`inspector` -> area via `panes` -> `memory-trace`); `memory-seed` resolves
  to `seed-core`; reach unchanged at 204 / 109.

#### D2 - Reparenting `graph` is DEFERRED, blocked by the rule written this morning

- D: `graph` stays a ROOT. The move under `memory-trace` was approved and is still right in principle;
  it does not ship until the 45 Seed-side entries are reassigned to `lifecycle-edges`.
- R: I applied the move, then measured it. A filter on `memory-trace` returned **249 entries instead of
  204** - an **18% false-positive rate** - because the 45 entries carrying `graph` WITHOUT
  `memory-trace` resolve upward through it. The pollution is real and legible: "Draft evolution-edges
  proposal", "Replace core topics with structured continuity field", "Core continuity topics as D6".
  `lifecycle-edges` exists precisely to take those 45, and **nothing carries it yet** - it was created
  in the same change. So the safe ordering is reassign-then-reparent, and the proposal had the order
  backwards without noticing.
  This is rule 3 - a slug must fit EVERY ancestor - catching the change its own author was making, on
  the day it was written, in the exact failure mode it describes: invisible pollution of an ancestor's
  filter, where nobody inspecting `memory-trace` can see which descendant put a stray entry there. The
  rule was worth more than the argument for it.
- A: Ship it and reassign later - rejected: the pollution is live the moment it lands, and an 18% wrong
  answer on the largest area filter is not a transitional state worth accepting. Retag the 45 now -
  rejected: entries are append-only, so this is a reviewed `retracts:` campaign needing its own
  approval, not something to fold into a vocabulary change.
- F: `.memory-seed/topics.yaml` (the deferral and its measurement recorded in place).
- T: After reverting, `memory-trace` reach is 204 and `graph` is 109 - both unchanged from before the
  change, confirming no filter moved.

### Follow-up

- **The unblocking campaign is the next vocabulary task**: reassign the 45 `graph`-without-`memory-trace`
  entries to `lifecycle-edges` via reviewed derived overrides, then reparent `graph`. The four data-model
  children moving is the first step - it seeds `lifecycle-edges` with real entries.
- **13 of the 17 new slugs are `unused-topic` today**, which `topics check` reports as info and is
  correct: the vocabulary now has names the corpus has not used yet. The swarm's per-entry assignments
  exist but writing them is a separate, unapproved step.
- `graph`'s `topics` alias is now doubly wrong - `topic-vocabulary` is a canonical root, so the alias
  reads as a spelling of the graph while naming the thing next door. Flagged in place, not fixed.

## 2026-07-27 17:32 - 166 entries attributed by sidecar; the graph reparent is withdrawn, not deferred

```yaml
entry_id: mse_4pz7vzgh7jrwedq2
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/topic-sidecar-campaign
topics:
  - topic-vocabulary
  - lifecycle-edges
related_entries:
  - mse_xz2pr6w8g5xkrecx
```

### Summary

- JNL approved both the reassignment campaign and writing the swarm's attributions. One of the two
  turned out to be a mistake I had proposed, and the other is done.

### Decisions

#### D1 - The graph reparent is WITHDRAWN, and the reason is arithmetic

- D: `graph` stays a root permanently. No reassignment campaign. I reported this an hour ago as
  "deferred pending a `retracts:` campaign" and that framing was wrong.
- R: Three reasons, ordered by how long each stays true.
  **(1) It has no upside.** 64 entries carry `graph` WITH `memory-trace` and 45 carry it WITHOUT. The 64
  already reach `memory-trace` directly, so reparenting adds **0 true positives and 45 false ones**. The
  move was never buying reach - it was only ever buying pollution. That is arithmetic and no future
  mechanism changes it, which is why the campaign was not blocked, it was pointless.
  **(2) The mechanism does not exist.** `augment_chunks_with_topic_sidecars` deliberately does not union
  into `chunk.topics` - "authored and inferred stay separable all the way to the consumer" - so a
  sidecar cannot un-say an authored topic. `sidecar-supersession-model.md` specs a cross-class retract
  as "step 1 of the consolidation build order"; nothing implements it.
  **(3) It fails the retraction rule anyway.** JNL, this morning: *"a retraction should be judgeable as
  'was this claim wrong?'. If the answer is 'no, it was true but I would rather it were more specific',
  the answer is a child slug, not a retract."* `graph` on "Draft evolution-edges proposal" was TRUE -
  the Seed edge model IS the graph's data model. This is the 86-entry parent-retraction trap in a new
  costume, and I walked most of the way back into it before measuring.
- A: Resurrect `graph-view` as a Trace child - rejected: zero authored entries and zero swarm evidence,
  which is precisely the case `settings` and `workspace-bar` were declined for two hours earlier, and
  reason (1) makes it pointless as well as unearned.
- F: `.memory-seed/topics.yaml` (the withdrawal and its measurement recorded in place),
  `docs/2_Todo/memory-trace-children-proposal.md`.
- T: `memory-trace` reach 204, `graph` 109 - both unchanged from before any of today's changes.

#### D2 - 166 entries attributed via the first topic sidecars in this repo

- D: The swarm's agreed labels written to `.memory-seed/sessions/topics/YYYY-MM/YYYY-MM-DD.md` - 18
  files, 167 attributions across 166 entries, each block filed under its entry's own date.
  `trail` 34, `lifecycle-edges` 34, `topic-vocabulary` 25, `trace-cache` 14, `trace-harness` 12,
  `test-suite` 12, `docs-lifecycle` 11, `graph` 7, `diagram-view` 6, `panes` 5, `topbar` 4,
  `inspector` 2, `navigation` 1.
- R: Enrichment, not correction - the whole reason it needs no retraction machinery. Nothing authored is
  touched; the sidecar is a separate `inferred_topics` channel, so every entry keeps its coarse slug and
  gains a specific one. This also seeds `lifecycle-edges` with 34 real entries, which was the only part
  of the abandoned campaign that had value.
  Filtering happened BEFORE writing rather than letting the validator catch it: `NONE` (46),
  `TRACE-WIDE` (35), `OTHER-ROOT` (34) and `SEED-WIDE` (5) are ballot answers, not slugs; `seed-other`
  (3) and `trace-api` (1) are non-canonical and `trace-api` was explicitly declined. The 21 rows where
  the workers disagreed get nothing.
  One block was written and validated ALONE before generating the other 165. This family had never been
  exercised against real data - `sessions/topics/` did not exist - so `malformed-topic-sidecar` had
  never fired, and discovering a grammar error across 18 files at validation time was the one avoidable
  disaster here.
- A: Write all 166 then validate - rejected on the above.
- F: `.memory-seed/sessions/topics/2026-0{5,6,7}/*.md` (18 new files),
  `docs/2_Todo/memory-trace-children-proposal.md`.
- T: `links check` 111 files OK (was 93). `entry_topic_sidecars` reads back all 166 with 13 distinct
  slugs; `augment_chunks_with_topic_sidecars` attaches `inferred_topics` to exactly 166 chunks. 779 Seed
  + 213 Trace tests pass. `Topic vocabulary OK`.

### Follow-up

- **Footgun for the next campaign.** `entry_topic_sidecars` is most-recent-wins per entry **wholesale** -
  a topic list is a state replaced by a better one, not a set that unions. These blocks carry one area
  slug and no activity, so an activity-axis campaign writing sidecars for the same entries will REPLACE
  them, not merge. It must carry the area slug forward in the same block.
- **`topics check` reports all 13 attributed slugs as `unused-topic`**, because that rule counts
  AUTHORED topics only and cannot see the sidecar channel. 166 entries carry them. Misleading rather
  than wrong, and a candidate fix: teach `unused-topic` the inferred channel, or say "no entry authors
  it" instead.
- **The `topic_swarm.md` STOP notice does not cover this.** It aborts the DECISION-level `<slug>:dN`
  backfill - 933 units against a 0.70 roll-up-recall gate. This was entry-level area attribution from an
  already-run swarm at 92-94% agreement, under the separated-axis premise the STOP explicitly asks for.
  Different unit, different gate, different premise - recorded so it is not later read as a bypass.

## 2026-07-27 18:15 - Trace reads the topic sidecar channel; two wrong layers found by measuring

```yaml
entry_id: mse_hhzeaa0j63tfpbhx
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/trace-inferred-topics
topics:
  - memory-trace
  - topic-vocabulary
related_entries:
  - mse_4pz7vzgh7jrwedq2
```

### Summary

- The 166 attributions were invisible in the product; wiring them in took three attempts, and the two
  wrong ones were each caught by measuring rather than by reading the code.

### Decision

- D: `_topics()` unions `inferred_topics` with authored `topics`, and the merge happens at READ time in
  `_entry_chunks()`. Topic sidecars are now tracked projection inputs alongside links and diagrams.
- R: `service.py` never called `augment_chunks_with_topic_sidecars` - zero references in the whole Trace
  package - so the vocabulary work changed nothing a user could see.
  `_topics()` is the right place for the union because it is the single chokepoint feeding facets,
  nodes, payloads, topic chains and the filter. Unioning at eight call sites instead is how a legend
  coloured from one channel and a chip list rendered from the other end up disagreeing.
  **Wrong layer 1: cache-build time.** The obvious first move was to augment where chunks are built and
  store `inferred_topics`. `_session_documents_for` deliberately excludes sidecars from the incremental
  reparse - "they produce no chunk rows" - which was true until this change made them produce chunk
  CONTENT. A new attribution would change no session document, reparse nothing, and leave the stored
  chunk carrying old topics until a full rebuild. Found by looking at the live API: 68 slugs with 166
  attributions already on disk.
  **Wrong layer 2: `_derived()` only.** Moving the merge to the read-time bundle fixed the graph and not
  the facet, because `facets()` reads `_entry_chunks()` and the graph reads `_derived()`. A slug the
  graph can colour but the facet cannot list is a filter offering no route to what it displays. Found
  the same way. Merging in `_entry_chunks()` serves all six call sites from one place, and `_derived()`
  now builds on it.
- A: Union inside each consumer - rejected: eight places to keep in step. Store on the chunk - rejected
  by wrong layer 1.
- F: `memory-trace/memory_trace/service.py` (`_topics`, `_entry_chunks`, `_derived`,
  `_tracked_document_paths`, imports), `memory-trace/tests/test_topic_sidecars.py` (new, 10 tests).
- T: Live `/api/facets` 68 -> 78 slugs with counts matching the sidecars exactly - trail 34,
  lifecycle-edges 35, trace-cache 14, trace-harness 12, test-suite 12, docs-lifecycle 11, diagram-view 6,
  panes 5, topbar 4, inspector 2, navigation 1. 223 Trace + 779 Seed tests pass.

### Follow-up

- **Tracked-input gap was latent, not harmless.** Topic sidecars were missing from
  `_tracked_document_paths` because no corpus had any, so nothing could go stale. The moment 166 landed,
  an untracked family meant editing an attribution left the no-git projection serving old topics with
  nothing to signal it. Worth checking the fourth family the same way if one is ever added.
- One regression test pins each wrong layer: an attribution written AFTER the cache was built must
  surface without a rebuild, and an attributed slug must reach the FACET, not just the graph node.
- I restarted JNL's running preview three times chasing a DOM check. The API and the tests were already
  decisive; the browser added nothing and disrupted their session. Verify backend changes in-process.

## 2026-07-27 18:25 - Decision-level edges pull their entry: 22% of drawn edges exerted no force

```yaml
entry_id: mse_94mv1bj5fec5hkkc
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/fix/decision-edge-forces
topics:
  - graph
  - bugfix
```

### Summary

- JNL's screenshot circled long unconstrained lines crossing the graph. They were edges the simulation
  had never been given: every edge terminating on a decision row was dropped from the link force while
  still being drawn.

### Decision

- D: New pure rule `simulationLinks(edges, known)` in `graphDecisionRows.ts`. An edge landing on a
  decision row is TRANSFERRED to that row's entry rather than dropped; an edge between two rows of the
  same entry is not a link at all; and a pair joined more than once contributes one link.
- R: Decision rows are not simulation bodies - they are satellites placed on a ring around their anchor
  every paint - so `links` was built with a `byId.has(source) && byId.has(target)` filter that silently
  removed every decision-level edge. **Measured on the live corpus with rows shown: 1227 nodes, 1463
  edges drawn, 316 of them (22%) touching a decision row and contributing NOTHING.** The simulation ran
  1147 links where it should have run 1326. That is exactly the symptom JNL circled - those lines had no
  spring in them, so they stayed at whatever length the rest of the layout left behind.
  Transfer rather than drop is JNL's rule: *"couple the decisions to the entry so that they remain
  rotationally free but the forces applied to them pass through to the central entry."* A decision is
  part of its entry, so a pull on the part is a pull on the whole. The row keeps the rotational freedom
  `ringPhase` gives it and the fixed offset `satellitePositions` gives it; only the force moves.
  The de-duplication is the half nobody asked for and the corpus demanded: 696 decision-level edges
  exist, so a pair joined by five of them would be pulled five times as hard as a pair joined by one.
  The map already collapses a pair to its strongest relationship for DRAWING; the forces now match what
  is drawn instead of secretly weighting by decision count.
- A: Add decision rows to the simulation as real bodies - rejected: they are placed, not simulated, and
  giving them to d3 would fight `positionDecisionRows` every tick. Sum the per-decision forces onto the
  entry instead of de-duplicating - rejected as an unrequested change to what an edge MEANS; the drawn
  map is one line per pair and the physics should agree with it.
- F: `memory-trace/client/src/graphDecisionRows.ts` (`simulationLinks`),
  `memory-trace/client/src/GraphWorkspace.tsx` (collapsed onto it),
  `memory-trace/client/src/graphDecisionRows.test.ts` (+7 tests), rebuilt `static/react`.
- T: 182 client tests pass (was 175), `tsc --noEmit` clean. Impact measured before and after: 316 edges
  recovered into 179 additional links after de-duplication.

### Follow-up

- **The bug was invisible precisely because the edge was still drawn.** A dropped force leaves no error
  and no missing element - only a line that does not behave like the others. Worth remembering the next
  time a layout looks subtly wrong: check what the simulation was HANDED, not what it did.
- De-duplication also affects the entry-only graph, which had 1241 drawn edges over 1132 distinct pairs -
  109 pairs were being double-pulled before this, independently of decision rows.
- JNL's message was cut off mid-sentence ("as long as the"), so the constraint after it is unrecorded.
  What shipped keeps rows rotationally free AND equidistant AND at a fixed radius, which covers every
  reading offered so far; if the missing clause said something else it is a small change on top.

## 2026-07-27 19:10 - Every decision in the corpus now carries an area and an activity

```yaml
entry_id: mse_8ckmgajc16fxywt2
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/decision-axis-campaign
topics:
  - topic-vocabulary
  - feature-build
related_entries:
  - mse_4pz7vzgh7jrwedq2
```

### Summary

- 1030 decision units judged, 2013 attributions written to sidecars: 95% carry both axes, and the
  vocabulary is now exercised at 68 of its 69 slugs.

### Decisions

#### D1 - Drop the comparison to the old topics; it was measuring a different question

- D: No agreement gate, no scoring against authored tags, and one worker per batch rather than two.
- R: JNL's diagnosis and it is correct. Every score this campaign chased - 0.583, 0.613, 0.89, 92%, 64% -
  was some form of "does this match what was there before". The old tags were written before the
  vocabulary had axes at all, so they encode a *different question*; agreeing with them was never
  evidence of a right answer and disagreeing was never evidence of a wrong one. The full-corpus run at
  64% was the clearest case: it looked like failure and was mostly the corpus being asked something the
  old labels had no opinion about.
  With the confound gone the design simplifies to what JNL specified: one worker, a handful of entries,
  sharply scoped rules. 58 batches of 12 entries, both axes at once, no second opinion to reconcile.
- A: Two blind workers for an agreement number - dropped on the same reasoning. Inter-rater agreement is
  a real measure but it was being used as a proxy for correctness, and the campaign's job is to produce
  a reading, not to keep measuring one.
- F: 43 files under `.memory-seed/sessions/topics/`.
- T: 58/58 batches returned, 0 errors, 0 empty. 1032 rows over 1030 units.

#### D2 - The ancestor rule is enforced by rendering, not by prose

- D: Every slug in the worker-facing vocabulary prints with its full chain -
  `inspector [memory-trace > panes > inspector]` - and the rule reads "check each link in the bracket".
  The tie-break is stated: a correct parent always beats a plausible child.
- R: JNL asked that a child respect its parents AND grandparents, especially where the tree is deep.
  Prose had already been tried; the previous run produced 80 answers in invented path syntax
  (`memory-trace/trail`, `lifecycle-edges > schema`, `memory-trace__trail`), which is a worker seeing
  depth it has no sanctioned way to express. Printing the chain gives it the words and shows exactly
  what a leaf claims. Rules 2 and 3 genuinely conflict - narrowest-that-fits versus true-at-every-level -
  so the tie-break had to be stated rather than left to be inferred.
- F: the generated `vocab_area.txt` / `vocab_activity.txt`, rendered from `topics.yaml` so the worker
  brief cannot drift from the vocabulary.
- T: path-form answers fell from 80 to 19 of 2064.

#### D3 - Normalise a path, drop a wrong axis

- D: `memory-trace > trail` is read as `trail`. An activity slug in the `area` field is DROPPED.
- R: These look like the same class of defect and are not. A path names exactly one slug and the worker
  plainly chose it - taking the leaf is transcription. A wrong-axis answer means the worker never picked
  an area at all, and supplying one would be my judgement wearing the swarm's provenance. So the
  drop-never-repair rule holds where it means something and is not applied where it would only discard
  a correct answer over its spelling.
  49 wrong-axis drops (2.4%), and the pattern is itself a finding: `agent-collaboration` (12), `release`
  (11), `proposal-lifecycle` (10) were offered as AREAS. Those are activities with no area counterpart -
  the worker had nothing to say for "what is this about" when the work is a release or a collaboration
  process. Recorded, not acted on.
- F: `scripts/`-adjacent writer (scratchpad), 43 sidecar files.
- T: 2013 of a possible 2064 attributions = 97.5%. `links check` OK across 136 files; `topics check` OK.

### Follow-up

- **The heading regex requires `HH:MM` and the legacy `ms-*` entries head themselves with a date only.**
  Copying their heading verbatim produced a block `_ENTRY_TS_YAML_RE` could not match, and `links check`
  rejected the whole file as `malformed-topic-sidecar`. A sidecar heading is block IDENTITY, not a claim
  about when the entry was written, so a synthesised `00:00` invents nothing.
- **The map: area 978 attributions over 40 slugs, activity 1023 over 28.** Largest areas `memory-trace`
  116, `docs-lifecycle` 79, `topic-vocabulary` 78; largest activities `feature-build` 273, `bugfix` 119,
  `ui-design` 108. `feature-build` was estimated at ~116 by keyword and measures 273 - the estimate was
  wrong in the opposite direction from `inspector`, which is the second time a keyword estimate has
  missed by more than 2x.
- Next: the sidecar becomes the topic AUTHORITY (JNL) - `_topics()` stops unioning and lets the sidecar
  replace the authored list - with `retracts:` for the topic family as the way back. Deliberately a
  separate commit so the data and the semantics can be reverted independently.

## 2026-07-27 19:10 - The sidecar becomes the topic authority

```yaml
entry_id: mse_8qmv0m07sy95vjm9
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/feature/decision-axis-campaign
topics:
  - memory-trace
  - topic-vocabulary
related_entries:
  - mse_8ckmgajc16fxywt2
```

### Summary

- Where an entry has topic-sidecar attributions, those ARE its topics. The authored field no longer
  unions into the answer, and the rollback path turned out to already exist.

### Decision

- D: `_topics()` now falls back in order: sidecar `inferred_topics`, then authored `topics:`, then the
  hashtag/heading axes. It unioned the first two until today.
- R: JNL's call, and the union stopped being right the moment the two-axis campaign finished. A union is
  correct while sidecars are sparse ENRICHMENT - an entry tagged `memory-trace` gaining `trail` really is
  both. It is wrong once the sidecar carries a COMPLETE reading of every decision on two declared axes,
  because unioning then re-admits the coarse pre-axis label the sweep exists to supersede: an entry
  reads as `memory-seed` AND `topic-vocabulary` with nothing to say which the corpus currently believes.
  Measured after the switch: the facet drops `memory-trace` from 204 entries to 76, because entries now
  answer with the specific slug rather than the root they were parked on. The root is still reachable -
  `expand_topic_filter` derives it from the child - so nothing lost reach, only false breadth.
  **Nothing authored is edited, deleted or contradicted on disk.** The entry's own YAML is untouched and
  still readable as what its author wrote. Only which of the two a READER is shown changed, and that is
  exactly what makes this reversible.
- A: Union and mark provenance in the UI - rejected: it keeps the ambiguity and moves the cost to every
  consumer. Union only where the sidecar has no area/activity pair - rejected as a rule nobody could
  predict from the outside.
- F: `memory-trace/memory_trace/service.py` (`_topics`),
  `memory-trace/tests/test_topic_sidecars.py` (assertions inverted, 2 tests added).
- T: 225 Trace tests pass. Live facets rebuild to 68 slugs led by `feature-build` 222, `bugfix` 99,
  `ui-design` 84 - the two-axis reading, not the old one.

### Follow-up

- **The granular rollback already exists and needs no new code.** JNL asked for retract as the way back
  to the old version. Because the sidecar family is most-recent-wins WHOLESALE per entry, a later
  sidecar block re-stating an entry's authored slugs restores them exactly - that IS retract semantics
  using machinery that shipped months ago. What does not exist is the explicit `retracts:` SPELLING for
  topics, which `sidecar-supersession-model.md` specs as "step 1 of the consolidation build order". The
  difference is ergonomics and intent-signalling, not capability.
- Coarse rollback is reverting one of two commits, kept separate for exactly this reason: `1272f95` is
  the data, this one is the semantics. Reverting the semantics restores every prior answer while keeping
  the 2013 attributions on disk.
- Two end-to-end tests failed on the switch and were RIGHT to - they asserted the union. Inverting them
  was the change, not a fix to them.

## 2026-07-27 20:12 - Two topic-family consumers taught: 520 trailers to 0, and a refusal that says what to do

```yaml
entry_id: mse_p0k0st2zmvmk70vn
user_initials: JNL
agent_type: claude
agent_name: Claude Opus 5
project_path: .
subproject_path: null
branch: claude/fix/topic-family-consumers
topics:
  - seed-core
  - bugfix
related_entries:
  - mse_8ckmgajc16fxywt2
```

### Summary

- Both defects the decision-axis campaign exposed are the same shape: the topic sidecar family shipped
  and its consumers were never told. One is fixed outright; the other is honestly bounded.

### Decisions

#### D1 - The trailer hook stamps entries AUTHORED, not entries MENTIONED

- D: `prepare-commit-msg.py` excludes `sessions/topics/`, `sessions/links/` and `sessions/diagrams/`
  from its staged-diff scan. Seed template kept byte-identical.
- R: A `Memory-Entry:` trailer claims *this commit carries this entry*. A session document's `entry_id:`
  is the entry being authored; a sidecar's is a REFERENCE to one authored somewhere else, often months
  earlier. The hook could not tell them apart because it scanned for `+entry_id:` anywhere under
  `sessions/`.
  Latent for as long as sidecars named two or three entries at a time. The topic sweep wrote one block
  per entry across the whole corpus, and two commits stamped **520 trailers each** - against 1 before
  sidecars existed. Links and diagrams have always had the same defect at a smaller scale, so all three
  are excluded rather than only the family that made it visible.
- A: Exclude only `topics/` - rejected: the other two are the same bug, just quieter, and fixing one
  would leave the next person to rediscover it.
- F: `.memory-seed/hooks/prepare-commit-msg.py` + seed copy, `tests/test_git_hooks.py` (+2).
- T: Both new tests FAIL against the old hook and pass against the new one - verified by stashing the
  fix and re-running. A commit carrying an entry *and* its sidecar still stamps the entry and not the
  sidecar's reference.

#### D2 - The merge refusal now names the topic family and gives the command; the fuse itself is NOT built

- D: `session merge-branch` still refuses on topic sidecars, but says which family it hit, why the fuse
  cannot rebuild it, and exactly what to run. `_topic_sidecar_tree_path` is deliberately NOT added to
  `_is_recognized_session_tree_path`.
- R: The refusal was correct - resetting a branch-touched topic sidecar to base would silently discard
  its attributions, which is the loss the guard exists to prevent. What was wrong was the message: *"not
  recognized by any session/diagram/link classifier"* reads like an unknown-file bug and sent me looking
  for a classifier defect, when the truth is that the family shipped after the fuse and was never taught
  to it.
  **Recognising the family without fusing it would be worse than the status quo**, and the function's own
  docstring says so: recogniser and fuse must not drift, because the recogniser gates the base-reset
  loop. Adding topics to it would let the loop reset them to base and drop branch work silently.
  I did not build the fuse. Sized honestly: the link family threads through **76 references** in
  `core.py` - a record type, a splitter, two ref-readers, a working-tree reader, a chronological writer,
  planner and applier integration, and fields on five dataclasses. Topic blocks are semantically
  identical to link blocks (keyed by `(entry_id, heading timestamp)`, most-recent-wins), so the mirror is
  mechanical - but a several-hundred-line change to the most safety-critical function in a memory
  system, at the end of a long session, is how silent data loss gets shipped. Starting it and abandoning
  it would be worse than not starting.
- A: Add topics to the recogniser now - rejected on the docstring's own argument. Build the fuse -
  deferred, not declined; it is the remaining work and it is bounded.
- F: `memory_seed/core.py` (`_topic_sidecar_tree_path`, `_unfusable_session_path_reason`, both refusal
  sites), `tests/test_session_fuse_and_merge.py` (+1, and one message assertion updated).
- T: 782 tests pass (was 779). The new test asserts the branch's content SURVIVES the refusal - had the
  guard let the reset run, it would read "base content" and the attributions would be gone.

### Follow-up

- **The remaining work is the topic fuse**, mirroring the link family. Its semantics are already settled
  (block identity `(entry_id, timestamp)`, most-recent-wins wholesale per entry) so the design question
  is answered; only the mechanical mirror is outstanding.
- Three consumers of the topic family have now been found and fixed one at a time -
  `_tracked_document_paths`, the incremental reparse, and the trailer hook - plus one bounded. That is a
  pattern worth acting on rather than continuing to discover: when a fourth sidecar family is added,
  grep for the third one's name and check every hit.
