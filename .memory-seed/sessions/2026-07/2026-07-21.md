---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-21
---

## 2026-07-21 07:05 - Phase 0 B0b: add evolution graph scope

```yaml
entry_id: mse_xee3vdwckhh0smx8
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/evolution-graph-scope
topics:
  - memory-trace
  - ui-design
related_entries:
  - mse_86mvcfdmcfjqmrvv
```

### Decision

- D: Shipped Evolution graph scope, the first of three designs JNL asked me to draft and review
  ("you design it, I'll review") for Phase 0's remaining undesigned B0b slices (file/evolution modes,
  topology communities). A third `GraphScope` value alongside the existing `overview`/`local`, showing
  only a selected entry's `evolves`/`supersedes` lifecycle chain rather than its full local neighborhood.
- R: Started with Evolution because investigating the actual codebase (not just the proposal doc) showed
  it needed zero new backend work: `/api/v1/graph/projection` already accepts arbitrary `edge_types` and
  `depth` params (`service.py:1447`), and the Trail already calls it with a fixed edge set
  (`branch, supersedes, evolves, related`) the same way. Evolution mode is the same pattern - a fixed
  edge-type preset plus a deeper depth, reusing 100% of existing machinery. This mirrors how the Arc-2d
  port was sequenced first among the B0b slices: pick the well-specified, zero-ambiguity piece first.
- A: Extended `GraphScope` to `"overview" | "local" | "evolution"`, added `EVOLUTION_EDGE_TYPES` (evolves
  + supersedes) and `EVOLUTION_DEPTH` (8, deep enough for a realistic chain without pulling in the whole
  graph) constants, threaded a `depth` parameter through `loadGraph`/`requestGraph` (previously absent
  entirely - every prior call relied on the API's depth=1 default), and updated `changeScope` to require
  a selected entry for any non-overview scope (previously local-only) and swap in the fixed edge set for
  evolution. Added a third segment-control button and replaced the edge-filter bar with a static label
  ("Evolves + Replaces only · lifecycle chain") while in evolution scope, since its edges aren't
  user-toggleable.
- F: `memory-trace/client/src/App.tsx`, rebuilt `memory-trace/memory_trace/static/react/`,
  `docs/2_Todo/0_NEXT_STEPS.md`.
- T: `npm run typecheck` - clean. `npm test` - 69/69 unchanged. `npx vitest --project storybook run` -
  7/7 unchanged. `npm run build` - succeeds. Live-verified against the real packaged app on two real
  entries: one with an actual `evolves` edge (`mse_x6qgkg61dnq26bk4` → `mse_wjntbg88n3m0qjss`) rendered
  exactly its 2-node chain in Evolution scope versus a much larger neighborhood in Local scope for the
  same entry; one with no evolution edges rendered an empty graph with no crash or console error.
  Switching back to Local from Evolution correctly restored the full edge-type toggle bar and
  neighborhood view. `memory-seed docs check` - "Docs lifecycle OK (166 file(s) checked)", 14
  pre-existing warnings, unchanged. Committed on `claude/feature/evolution-graph-scope`, merged `--no-ff`
  to main, branch deleted.

  Remaining from the reviewed design set: file mode (needs a new backend file→entries reverse index,
  extending `_entry_commit_map`'s existing git-log pass) and topology-community detection (Louvain,
  client-side, colour-stabilised by lowest-sorted-entry-id per community) - both designed and awaiting
  JNL's go-ahead to build, per the original review request.

## 2026-07-21 07:55 - Phase 0 B0b: add file graph mode

```yaml
entry_id: mse_sqzm8bg83adw2nc7
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/graph-file-mode
topics:
  - memory-trace
  - ui-design
  - bugfix
related_entries:
  - mse_xee3vdwckhh0smx8
```

### Decision

- D: Shipped file graph mode, the second of the two remaining reviewed designs JNL approved ("Yes build
  the two"): a new backend `file_entry_index` (path -> entry_ids) exposed as a `path` param on
  `/api/v1/graph/projection`, a fourth `GraphScope` seeded by an exact entry-membership set rather than a
  neighborhood expansion, and clickable file pills in the reader's `F:` blocks that open it directly.
- R: The first implementation attempt (trailer-based, mirroring the existing `trailer_merges` mechanism)
  shipped, verified via 6 passing unit tests against a synthetic fixture - then returned zero nodes
  against the real corpus. Root-caused live rather than assuming the fixture was representative: this
  session's own merges never carry a `Memory-Entry` trailer (only one exists in the whole repo, from
  2026-07-12), because that trailer is stamped only by `session merge-branch` fusing branch-local
  session entries, and this project's actual convention logs the session entry on `main` *after*
  merging - so there's never a branch-local entry to fuse. Pivoted to a branch-name-vs-merge-subject
  match ("Merge branch '<name>'" against entries' `branch:` field) - ALSO verified live, ALSO empty:
  entries logged after merging record `branch: main` (where they were logged), not the feature branch
  they document. Traced the real signal by hand (`git log --format="%H %P %s"` on the actual arc2d
  entry): its authoring commit's PARENT is the merge commit that carried the real files. Rebuilt the
  index on that signal - group entries by their authoring commit's parent ("work commit"), diff that
  parent against its own first parent (merge) or sole parent (plain commit) - and it resolved correctly
  on the first real-data check.
- A: `_file_entry_index(root, entry_commit_map)` - one `git log --format="%H%x1f%P" main` pass building
  a full sha->parents map (avoiding a subprocess per entry), grouping entries by shared work-commit
  before diffing so a batch of entries sharing one merge only pays for one diff. Extended `Service.graph()`
  with an `entry_ids: Sequence[str] | None` param (`if entry_ids is not None` - an explicitly empty list
  from an unrecognized path must return an empty graph, not fall through to the overview branch).
  `EntryReader.tsx`'s file-pill `<span>`s became `<button>`s calling a new `onOpenFile` prop; `App.tsx`
  added `filePath` state, threaded `path` through `loadGraph`/`requestGraph`, and added `openFileMode()`.
  Discovered mid-debug that the live preview server's SQLite cache doesn't invalidate on my own code
  changes (only on repo/git state) - the first two failed attempts were silently serving a stale index
  computed by the earlier buggy code, not a fresh failure each time. Fixed by adding `--rebuild-cache`
  temporarily to force a real rebuild, confirmed the fix, then reverted the flag (repo-state-based
  invalidation is correct for normal use; forcing it every launch would just add latency).
- F: `memory-trace/memory_trace/service.py` (`_file_entry_index`, `file_entry_index()` accessor,
  `_metadata_matches()` staleness check, `Service.graph()`, `/api/v1/graph/projection`),
  `memory-trace/client/src/App.tsx`, `memory-trace/client/src/EntryReader.tsx`,
  `memory-trace/client/src/api.ts`, `memory-trace/client/src/styles.css`,
  `memory-trace/tests/test_file_graph_mode.py` (new, 8 tests), rebuilt
  `memory-trace/memory_trace/static/react/`, `docs/2_Todo/0_NEXT_STEPS.md`.
- T: `pytest memory-trace/tests` - 158/158 passed (8 new: merge-based attribution, plain-commit-to-main
  attribution, unknown-path-empty, cross-file non-leakage, and an explicit "branch: main is not the
  signal used" regression test). `npm run typecheck` - clean. `npm test` - 69/69 unchanged.
  `npx vitest --project storybook run` - 7/7 unchanged. `npm run build` - succeeds. Live-verified against
  the real packaged app after forcing a cache rebuild: a single-entry file (`arc2d.ts`) resolved to
  exactly its one entry via direct API call and through the UI (file pill click -> correct scope,
  correct auto-selected entry, no console errors); a real 23-entry file (`App.tsx`) rendered a populated
  graph with genuine relationship edges between the entries, confirmed both via direct API and by
  clicking through the UI. `memory-seed docs check` - "Docs lifecycle OK (166 file(s) checked)", 14
  pre-existing warnings, unchanged. Committed on `claude/feature/graph-file-mode`, merged `--no-ff` to
  main, branch deleted.

  Both reviewed B0b designs (evolution scope, file mode) are now shipped. Remaining Phase-0 B0b work:
  topology-community detection (designed - client-side Louvain, not yet approved/built) and formal
  accessibility/scale acceptance. 24 commits now sit on local main, unpushed per JNL's earlier request
  to review locally first.

## 2026-07-21 09:54 - Find bar all-text toggle and app-wide button hover lift

```yaml
entry_id: mse_sewpap8b4q6sb0zj
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/find-bar-toggle-and-button-hover
topics:
  - memory-trace
  - ui-design
```

### Decision

- D: Made the find bar's "all text" control a real toggle (both directions), and gave every button in
  the client a mild hover lift, at JNL's direct request.
- R: The full-text control was a one-way door — clicking it in title mode turned full-text search on
  (a real button), but once active it rendered as a plain non-interactive `<span>` (`.find-mode`), so
  turning it back off required reaching for the separate dismiss button on the far right of the bar
  instead of clicking the same control again. Collapsed both states onto one `<button>` with
  `aria-pressed` reflecting the mode, calling `runSearch(query)` to turn full-text on and
  `dismissFullTextSearch()` to turn it back off — verified live in the packaged `/next` React shell
  (not the legacy vanilla `/` UI, which is a separate static bundle the preview server defaults to).
  Separately, JNL asked for buttons to read as clickable next to inert decorative icons (e.g. the search
  glyph inside the input, a plain `svg`, not a button) — added a shared `transition` + `translateY(-1px)`
  hover rule scoped to the `button` element itself, so it applies everywhere without touching each
  component's own hover-colour rules, with a `prefers-reduced-motion` opt-out matching the existing
  pattern used for the worktree-switch loader and collapsible metadata.
- A: `App.tsx`'s find-bar JSX: one `<button className="find-all">` for both modes instead of a
  mode-branched `span`/`button`. `styles.css`: dropped the now-dead `.find-mode` rule, added
  `.find-all[aria-pressed="true"]` for the pressed look, and a base `button:not(:disabled)` hover/active
  transform block (with the reduced-motion override) ahead of every existing button-specific style.
- F: `memory-trace/client/src/App.tsx`, `memory-trace/client/src/styles.css`, rebuilt
  `memory-trace/memory_trace/static/react/`.
- T: `npm run typecheck` - clean. `npm test` - 69/69 unchanged. `npm run build` - succeeds. Live-verified
  against the real packaged app's `/next` React shell: clicked "all text" to enable full-text (26 → 100+
  results, `aria-pressed` true), clicked it again to return to title matches (back to 9/26,
  `aria-pressed` false) without touching the dismiss button; confirmed the hover lift via computed style
  on both the find-bar button and an unrelated `.icon-button`, no console errors. Committed on
  `claude/feature/find-bar-toggle-and-button-hover`.

## 2026-07-21 10:58 - Graph edge filters are visual-only, no refetch or layout jump

```yaml
entry_id: mse_7qfd6tx63txn9v3v
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/graph-edge-filter-visual-only
topics:
  - memory-trace
  - ui-design
  - graph
```

### Decision

- D: Made the Graph Workspace's "Edges" filter row (Related/Replaces/Evolves/Topic) a pure
  client-side visibility toggle, at JNL's direct request to take inspiration from Obsidian's graph
  view: lines appear and disappear, but the node set and layout never move unless a genuine
  node-specific filter (scope, topic, date range) is applied.
- R: Explained to JNL first why toggling a chip previously made the whole map jump — each click added
  or removed that edge type from the `edge_types` query param and re-fetched
  `/api/v1/graph/projection`, which could change the returned node set (a node connected only via the
  toggled-off type simply vanished) and always caused `GraphWorkspace`'s mount effect to re-run:
  a fresh Cytoscape instance, new deterministic starting positions for whatever node set came back,
  and a from-scratch 900-iteration `cose` layout. Selection was already handled in place with no
  remount (`GraphWorkspace.tsx`'s "mount ONCE per graph payload" comment); the fix was to give the
  edge-type filter the same treatment instead of coupling it to the fetch. One wrinkle found while
  implementing: the filter row is hidden in Evolution/File scope, but `edgeTypes` state persists
  across scope changes — without a guard, turning "Evolves" off in Overview and then switching to
  Evolution scope would silently hide every edge in a scope whose entire purpose is showing the
  evolves/supersedes chain, with no visible chip to explain why. Fixed by having Evolution/File scope
  always pass their own fixed edge-type set as `visibleEdgeTypes`, ignoring whatever the (hidden) row
  is currently set to.
- A: Added `edgeTypesForScope(targetScope)` (`App.tsx`) so every fetch site (`load`, `requestGraph`'s
  default, `changeScope`/`openFileMode`/`chooseTopic`/`changeRange`) derives its edge-type query param
  purely from scope, never from the filter-row state; `toggleEdge` is now synchronous and only calls
  `setEdgeTypes`, no `requestGraph`. `GraphWorkspace` takes a new `visibleEdgeTypes` prop, applied in
  the existing `applyPresentation` in-place pass alongside selection/labels: an `edge-filtered` class
  (mirroring the existing `hidden-until-selected` mechanism for evolves) toggles `display: none` per
  edge type without touching nodes, positions, or the layout. The presentation effect's dependency
  array picked up `visibleEdgeTypes`; the mount effect's deps (`[graph, renderedNodes, theme]`) are
  unchanged, so it genuinely never re-runs on a chip click. `App.tsx` passes
  `scope === "evolution" || scope === "file" ? edgeTypesForScope(scope) : edgeTypes` as
  `visibleEdgeTypes` for the scope guard above.
- F: `memory-trace/client/src/App.tsx`, `memory-trace/client/src/GraphWorkspace.tsx`, rebuilt
  `memory-trace/memory_trace/static/react/`.
- T: `npm run typecheck` - clean. `npm test` - 69/69 unchanged. `npm run build` - succeeds.
  Live-verified against the real packaged app's `/next` React shell via the Cytoscape debug harness
  (`window.memoryTraceNextDebug.graphCy`, not screenshots — the Browser pane's screenshot tool hung on
  this canvas-heavy view, but the app itself was responsive throughout): toggling "Related" off in
  Overview scope (51 nodes, 60 edges) left node count and a sampled node's `position()` exactly
  unchanged while all 32 `related` edges gained the `edge-filtered` class (evolves/topic stayed
  visible); toggling back on restored all 32 instantly, `aria-pressed` correct both ways. Selected an
  entry, turned "Evolves" off in Overview, switched to Evolution scope: all 12 fetched edges (11
  evolves + 1 supersedes) rendered visible despite the stale toggle, confirming the scope guard;
  switching back to Overview correctly re-applied the "Evolves off" filter (14 hidden, node count still
  51). No console errors throughout. Committed on
  `claude/feature/graph-edge-filter-visual-only`.

## 2026-07-21 12:46 - Context-panel clicks no longer force Local scope

```yaml
entry_id: mse_8n3cbega82khef9j
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/context-panel-preserve-scope
topics:
  - memory-trace
  - ui-design
  - graph
```

### Decision

- D: Clicking a related/similar/evolves/commit entry in the left "Context" panel no longer forces the
  Graph Workspace out of whatever scope (Overview or Evolution) it was in, at JNL's direct request.
- R: `openContextEntry` always called `focusEntry`, which unconditionally fetches a fresh Local-scope
  projection centered on the clicked entry and sets `scope` to `"local"` — so browsing a related entry
  from the sidebar silently dropped the user out of Overview even when that entry was already a node
  on screen. `selectFromTrail` (the equivalent handler for clicking a Trail row) already had the right
  pattern: check whether the entry is already present in the currently loaded `graph`, and if so just
  call `select(node)` in place, only falling back to `focusEntry`'s Local-scope fetch when the entry
  genuinely isn't part of the current graph and has to be pulled in. Applied the same pattern to
  `openContextEntry`.
- A: `openContextEntry` in `App.tsx` now does `graph?.nodes.find((item) => item.source.entry_id ===
  entryId)` first; a hit calls `select(node)` and returns, a miss falls through to the existing
  `focusEntry(entryId)` call unchanged.
- F: `memory-trace/client/src/App.tsx`, rebuilt `memory-trace/memory_trace/static/react/`.
- T: `npm run typecheck` - clean. `npm test` - 69/69 unchanged. `npm run build` - succeeds.
  Live-verified against the packaged app's `/next` React shell: selected a node via the Cytoscape
  debug harness (`cy.$id(sourceId).emit('tap')`) that had an `evolves` edge in the loaded 52-node
  Overview (all-dates) graph, then clicked that edge's target in the Context panel's "evolves" group —
  scope stayed `Overview`, node count stayed 52 (same Cytoscape instance, no remount), inspector
  updated to the clicked entry. Separately confirmed the necessary fallback still works: clicking a
  context entry from outside the loaded graph's node set correctly switched to Local scope (4 nodes) to
  bring it into view. No console errors. Committed on `claude/fix/context-panel-preserve-scope`.

## 2026-07-21 12:51 - Context-panel fallback also stops touching graph/scope

```yaml
entry_id: mse_mb2dwk1b2wy6hbd0
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/context-panel-no-scope-fetch
topics:
  - memory-trace
  - ui-design
  - graph
evolves:
  - mse_8n3cbega82khef9j
```

### Decision

- D: Tightened the context-panel click contract, at JNL's direct clarification: an entry clicked in the
  left "Context" panel should simply be highlighted in the graph (if it's already there) and viewed in
  the Inspector — nothing else.
- R: The previous fix (entry `mse_8n3cbega82khef9j`) reused an
  already-loaded graph node when present, which covered the common case, but still fell back to
  `focusEntry` for an entry outside the current graph — and `focusEntry` unconditionally calls
  `setScope("local")` and replaces `graph` with a fresh Local-scope fetch. That still let a context click
  silently swap out the visible Overview/Evolution graph for an unrelated Local neighborhood, just for
  entries the fast path didn't cover. JNL's framing made the actual contract explicit: highlight-if-
  present + inspector-always, with no third behavior (scope/graph replacement) permitted at all.
- A: `openContextEntry` (`App.tsx`) no longer calls `focusEntry` in any branch. The fast path (entry
  already a node in the loaded `graph`) is unchanged — `select(node)`. The new fallback issues a
  throwaway `graphQuery({ entryId })` (the same entry-centered endpoint `focusEntry` used) purely to
  read that entry's `RendererGraphNode` shape for the Inspector's metadata grid; the response is never
  written to `graph` or `scope` state, so it cannot appear as a highlighted node (nothing on screen to
  ring) but `select()` still populates the Inspector via the existing `selected`→`chunk` fetch effect.
- F: `memory-trace/client/src/App.tsx`, rebuilt `memory-trace/memory_trace/static/react/`.
- T: `npm run typecheck` - clean. `npm test` - 69/69 unchanged. `npm run build` - succeeds.
  Live-verified against the packaged app's `/next` React shell: clicked a context entry confirmed (via
  the Cytoscape debug harness) to NOT be one of the 51 loaded Overview nodes — scope stayed `Overview`,
  node count and a sampled node's position were pixel-identical before/after, and the Inspector updated
  to the clicked entry's content. No console errors. Committed on
  `claude/fix/context-panel-no-scope-fetch`.

## 2026-07-21 13:22 - Overview ranks by connectivity across all dates, with a coverage indicator

```yaml
entry_id: mse_9ca5mpekppn3b1y1
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/overview-connectivity-and-coverage-indicator
topics:
  - memory-trace
  - ui-design
  - graph
```

### Decision

- D: Overview now ranks by connectivity across the whole corpus by default, with a "N of Y entries
  shown" indicator and a "Show more" control — JNL agreed to this pairing (approach #2 + #4) after a
  design discussion about why Overview looked sparse relative to the 567-entry corpus.
- R: Investigated the discussion's approach #2 ("rank by connectivity/importance instead of recency,
  across all dates") before building anything, and found the server already implements exactly that:
  `_overview_slice` (`service.py:2209`) does a greedy connectivity-ranked expansion from high-degree
  seeds with newest-first tie-breaks, over whatever `_filter_chunks` returns. The only reason Overview
  read as "recent activity" rather than a true overview was that `range` defaulted to `"recent"`,
  shrinking the candidate pool with a 7-day `date_from` filter *before* that ranking ever ran. So #2
  reduced to a one-line default flip, not new ranking logic — confirmed this reading with `advisor()`
  before implementing rather than building a redundant client-side or backend ranking pass.
  advisor() also caught two correctness issues before they shipped: (1) the coverage indicator must
  count rendered/connected nodes (`connectedNodeIds`), not the raw fetch size, since isolated entries
  never render regardless of limit; (2) "Show more"'s stop condition must be "last fetch returned fewer
  nodes than requested" (the connected corpus is exhausted), not "limit >= total entry count" — the
  latter would never disable, since `_overview_slice`'s connectivity expansion plus the client's
  edge-based noise filter mean the rendered count can never reach the full entry count.
- A: `App.tsx`: `range` state now defaults to `"all"`. Added `overviewLimit` state (default
  `OVERVIEW_LIMIT_STEP = 60`, matching the server's own default) threaded as a new trailing optional
  param on `requestGraph`/`loadGraph`, defaulting to `overviewLimit` only when `nextScope === "overview"`
  so Local/Evolution/File fetches are untouched. `showMoreOverview()` bumps `overviewLimit` by the step
  and re-fetches — paging deeper into the server's ranking, not widening a window, so each click surfaces
  the next-most-connected entries rather than a random slice. `overviewShownCount` (rendered/connected
  node count) and `overviewExhausted` (`graph.nodes.length < overviewLimit`) are both derived, not
  stored. `api.ts` gained `connectedNodeIds(graph)`, extracted as the single place "does this node
  render" is defined; `GraphWorkspace.tsx`'s `renderedNodes` memo now calls it instead of duplicating the
  predicate, so the mounted-node set and the coverage count can't drift apart. New UI: a
  `.graph-filter-bar` row (Overview scope only, reusing the existing `.count`/`.active-topic` classes —
  no new CSS) showing "N of Y entries shown" plus a "Show more" button that hides once exhausted.
- F: `memory-trace/client/src/App.tsx`, `memory-trace/client/src/GraphWorkspace.tsx`,
  `memory-trace/client/src/api.ts`, rebuilt `memory-trace/memory_trace/static/react/`.
- T: `npm run typecheck` - clean. `npm test` - 69/69 unchanged. `npm run build` - succeeds.
  Live-verified against the packaged app's `/next` React shell: fresh load selected a June entry (the
  corpus's highest-connectivity hub, a "Memory Lense" thread) as the default selection rather than
  whatever was chronologically newest, confirming the whole-corpus ranking. Coverage indicator read
  "52 of 568 entries shown"; three successive "Show more" clicks grew the rendered graph 52 → 96 → 129 →
  161 (each fetch pulling in the next tier of connected entries, not a flat jump), with the indicator
  text tracking exactly. Switched to Local scope (9-node neighborhood, unaffected by `overviewLimit`)
  and back to Overview — the 161-node expanded state was preserved rather than reset. No console errors.
  Committed on `claude/feature/overview-connectivity-and-coverage-indicator`.

## 2026-07-21 13:40 - Fix Overview coverage row collapsing the graph canvas

```yaml
entry_id: mse_5czah3ey0q9vv8y2
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/fix/graph-coverage-row-collapses-canvas
topics:
  - memory-trace
  - ui-design
  - graph
  - bugfix
evolves:
  - mse_9ca5mpekppn3b1y1
```

### Decision

- D: Fixed the Overview coverage row (entry `mse_9ca5mpekppn3b1y1`) collapsing the graph canvas to zero
  height, reported by JNL as "i can't see the graph anymore" right after that feature shipped.
- R: The coverage readout was rendered as a second sibling `<div className="graph-filter-bar">` alongside
  the existing Edges bar, both present at once in Overview scope. `.workspace`'s CSS grid only declares
  three explicit rows (`grid-template-rows: auto auto minmax(0, 1fr)`: title bar / filter bar / content);
  a second flow child there lands in an implicit fourth row (default `grid-auto-rows: auto`), pushing the
  actual Trail/Graph content out of the row sized to hold it. Diagnosed via the Cytoscape debug harness
  rather than guessing: `graphCy.nodes().length` was 52 (data fetched and mounted fine), but
  `.graph-canvas.getBoundingClientRect()` reported `height: 0` — confirming this was a pure layout bug,
  not a data or mount regression. Also traced an unrelated crash JNL hit first ("the app is crashing," a
  blank page): repeated `npm run build`s this session each wipe and regenerate every content-hashed
  chunk, so a browser tab holding an old `index-*.js` in memory can request a `GraphWorkspace-*.js` chunk
  hash that a later build already deleted — a stale dynamic-import 404 with no error boundary to catch
  it. That resolved with a hard reload and needed no code fix; noted here since it surfaced in the same
  exchange and could recur every time this session rebuilds the client again.
- A: Folded the coverage `<span>`/`<button>` into the existing Edges `.graph-filter-bar` div as trailing
  content (gated on `scope === "overview"` inline) instead of a second sibling div, so Overview scope
  renders exactly one flow child in that grid row again, matching Local/Evolution/File.
- F: `memory-trace/client/src/App.tsx`, rebuilt `memory-trace/memory_trace/static/react/`.
- T: `npm run typecheck` - clean. `npm test` - 69/69 unchanged. `npm run build` - succeeds.
  Live-verified against the packaged app's `/next` React shell after a hard reload: `.graph-canvas`
  went from `height: 0` to `726px`, graph visibly rendered with the merged "52 of 569 entries shown ·
  Show more" text inline in the Edges row (screenshot confirmed). Checked Evolution and Local scope too —
  both still get a real canvas height, and Local correctly shows no coverage readout at all (Overview-only
  by design). No console errors. Committed on `claude/fix/graph-coverage-row-collapses-canvas`.

## 2026-07-21 14:58 - Memory Trace incremental startup: bulk git reads, persisted derivations, lazy bootstrap

```yaml
entry_id: mse_gtn504wfjt3c34p6
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/perf/trace-incremental-startup
topics:
  - memory-trace
  - performance
  - memory-seed
```

### Decisions

#### D1 - Persist immutable git derivations per sha; bulk-read history in single passes

- D: Rebuilt the TraceCache enrichment pipeline around two principles: (1) immutable git facts (a
  merge's fork point, a commit's parents, its first-parent changed paths) are computed once per
  commit EVER and persisted in the disposable SQLite projection (schema v2: `fork_points`,
  `commit_parents`, `changed_paths`, `file_entries` tables), carried across full rebuilds; (2) all
  history harvesting happens in single delimited `git log` passes - fork points resolve in-process
  via maximal-common-ancestor over one bulk commit-graph read (with a `git merge-base` subprocess
  fallback only for criss-cross ambiguity), and per-work-commit changed paths come from one
  `git log --diff-merges=first-parent --name-only` pass instead of one `git diff` per commit.
- R: Direct profiling on this corpus (new `python -m memory_trace.bench` harness, built FIRST per the
  task brief): full rebuild 44.3s spawning 990 git subprocesses, of which trailer/fork
  reconstruction was 17.4s/297 procs and the file-entry index 15.2s/342 procs - per-historical-item
  subprocess spawns at ~50-90ms each on Windows were the entire problem; parsing was 0.5s. The
  2026-07-18 fork-point memo (entry `mse_81s84hmztx3ayqww`) fixed this within one process but was
  in-memory only, so every fresh server launch repaid the full walk. Retrieved the prior reasoning
  first: the projection contract (G1-G7) requires everything persisted to stay derived/rebuildable;
  unresolvable (None) fork points are deliberately NOT persisted so a deepened shallow clone can
  recover them. Body-scan trailer semantics (entry `mse_skpkqx0hhkw6ve4f`) and oldest-commit-wins
  attribution (entry `mse_e2vh8bqt4x0w6ljd`) preserved verbatim.
- A: `_trunk_ref_and_sha`, `_commit_graph`, `_bulk_fork_points` (+ downward-closed-set maximality
  argument in its docstring), `_trailer_walk`/`_resolve_event_forks` (walk and fork resolution
  decoupled), `_changed_paths_bulk`, `_file_entry_index_from_parts` (pure derivation). Also fixed the
  pre-existing hardcoded-`main` asymmetry in `_file_entry_index` (now uses the main/master/HEAD
  fallback chain like its siblings).
- T: Same corpus, same harness: full rebuild 44.3s/990 procs -> 1.46s/7 procs; trailer walk 17.4s ->
  0.23s; file index 15.2s -> 0.78s. New regression test pins the count: rebuild subprocess total is
  IDENTICAL at 3 merges and 9 merges (per-item git work cannot silently return).

#### D2 - Incremental reconciliation with fail-toward-full-rebuild, gated on the G2 equivalence test

- D: `ensure_current` now tries `_reconcile_locked()` before a full rebuild: forward HEAD/trunk moves
  reparse only changed session documents (committed delta + dirty-now + dirty-at-last-build), advance
  entry attribution over the new range only, re-emit trailer merges from the persisted fork table,
  and advance the file index only if it was already built. Every ambiguity - rewritten history
  (`merge-base --is-ancestor` fails), trunk non-fast-forward, re-added entry id (falls back to one
  bounded full `git log -p` pass), any failed git read, any sqlite/OS error mid-reconcile - falls
  back to the full rebuild. Updates are applied to a COPY of the projection and published with the
  same atomic-swap (G4) as a full rebuild; `rebuild()` and `--rebuild-cache` remain the explicit
  full-repair path. `PROJECTION_SCHEMA_VERSION` bumped 1 -> 2 (the standing schema-gate mechanism).
- R: This is the derived-projection plan's named, deferred "incremental ingest" fast-follow, gated in
  that plan on an incremental == full-rebuild equivalence test ("that equivalence IS G2"). The 07-15
  deferral reasoned from parse cost (~3% then) - but file graph mode (07-21) and fork reconstruction
  changed the profile: the dominant cost was git enrichment, not parsing, which the deferral never
  weighed. The user's brief supplied fresh profiling to that effect. `extract_memory_chunks` gained
  an additive public `paths=` filter (core package, no twin needed) rather than duplicating parse
  logic in Trace ("one canonical reader per concern"). One real bug caught by existing tests during
  implementation: my first changed-document classifier pattern-matched the month-dir layout only;
  the canonical `iter_session_documents` supports flat and per-user-day layouts too, so
  classification now delegates to it, and every affected date reparses ALL its current documents
  (chunks delete per date; a per-user date spans several files - deleting by date and reparsing only
  the changed one would silently drop the others' chunks).
- T: 15 new tests in `test_incremental_projection.py`: no-full-rebuild on commit/dirty/delete/rename,
  only-changed-doc reparse, new-merge-only fork resolution, fork persistence across a fresh process
  with bulk+per-merge recompute both forbidden via monkeypatch, re-added-id oldest attribution,
  amend -> full rebuild, and the G2 gate (chunks/entry_commits/main_commit_entries/trailer_merges/
  file_entry_index all identical to a clean full rebuild after a mixed commit+merge+dirty sequence).
  All 173 memory-trace tests and all 640 root memory-seed tests pass.

#### D3 - Lazy file-entry index; Trail-first client bootstrap; measured layout scaling

- D: (a) Ordinary startup no longer builds the file index at all - the first File-mode request
  builds it under the rebuild lock (concurrent callers share one build), persists it keyed by the
  trunk watermark, and reconciles advance it only if present. (b) The React client fetches Graph
  data only on first entry to Graph view; the topic-null Trail renders directly from the entry
  index, collapsing the duplicate full-corpus fetch; Trail clicks before any graph exists resolve
  the Inspector via the throwaway entry-scoped fetch (no scope jump); topic/worktree changes null
  the graph so a later switch fetches fresh. (c) loadGraph now KEEPS a prior selection absent from
  the fetched slice instead of replacing it with the first node - the new e2e spec caught a real
  bug where a Trail selection was stomped by the connectivity hub when entering Graph view.
  (d) Measured cose at 60/500/1000 nodes (86ms/4.6s/17.8s): settled positions now persist
  module-level so same-node-set remounts (theme toggle, view round trip) run zero simulation, grown
  sets warm-seed existing positions, and the iteration budget scales down above 150 nodes (900
  kept verbatim below, so default layouts are pixel-identical).
- R: The 500/1000-node measurements justified position caching + iteration scaling but not a worker
  or renderer swap (the brief's own bar); 60-node default cost was already fine. The lazy index
  respects "startup pays only for what the active view renders" - the same principle as the client
  changes.
- T: New `e2e/bootstrap.spec.ts` against the packaged build + real corpus: initial Trail load fires
  ZERO graph-projection requests and exactly ONE full trail/index fetch; entering Graph fetches the
  projection once, preserves the selection, and a Trail->Graph round trip refetches nothing. All 5
  e2e, 69 unit tests, typecheck, production build pass. Real-browser warm start: Trail data 308ms
  from navigation (target <500ms); reconcile-after-two-commits observed live at ~1.5s (target <3s).
- F: `memory-trace/memory_trace/service.py`, `memory-trace/memory_trace/bench.py` (new),
  `memory-trace/tests/test_incremental_projection.py` (new), `memory_seed/semantic_cache.py`,
  `memory-trace/client/src/App.tsx`, `memory-trace/client/src/GraphWorkspace.tsx`,
  `memory-trace/client/e2e/bootstrap.spec.ts` (new), `CHANGELOG.md`, rebuilt
  `memory-trace/memory_trace/static/react/`. Residual risks recorded: reconcile trusts
  `git diff --name-only` rename semantics (guarded by `--no-renames`); persisted stale sha rows are
  bounded garbage cleared only by cache deletion; the pre-existing off-allowlist docs-check error
  (`4_Reference/trace-humanised-dashboard-references`) predates this branch and is untouched.

## 2026-07-21 16:50 - Trail decision rows: one node per decision, pastel D2..DN, grouped by bracket

```yaml
entry_id: mse_9wsn23n3k8txnm2m
user_initials: JNL
agent_type: claude
agent_name: null
project_path: .
subproject_path: null
branch: claude/feature/trail-decision-rows
topics:
  - memory-trace
  - ui-design
  - session-logging
related_entries:
  - mse_v9stt3nxkg7s4m84
```

### Decisions

#### D1 - Surface decisions as Trail rows via the section chunks that already carry their identity

- D: Multi-decision entries (>=2 `#### Dn -` decisions) now render one Trail row per decision, at
  JNL's request: D1 is the entry's existing row (entry time, full branch colour); D2..DN are
  additional rows on the same branch lane with a pastel of the branch colour and a bracket right of
  the time column grouping the set. Implemented as a Trail-only backend opt-in
  (`TraceService.graph(include_decisions=True)`, passed solely by `/api/v1/trail`): the expansion
  reads the section-granularity chunks already materialised per `#### Dn` heading - whose chunk_id
  `{entry_id}#decisions/dN-<slug>` IS the ratified decision identity `(entry_id, dN)` from the ADR
  contract amendment (`mse_v9stt3nxkg7s4m84`) - so no new identity scheme, no new parsing, one
  memoized SQL read, zero git work (the incremental-startup guarantees stay intact).
- R: Retrieved the prior reasoning first: the ADR contract had already settled decision identity as
  the `(entry_id, ordinal)` pair with the section slug as the retrieval address, and
  `_find_section_ranges` already splits entries at `###`+ headings - the Trail was the only surface
  not exposing it. Detection reuses core's write-time-validated grammar via a newly exported
  `DECISION_TITLE_ORDINAL_RE` (bare-title form of `_NUMBERED_DECISION_HEADING_RE`) so Trail
  detection can never drift from what `session append` accepts. Singular-`### Decision` entries (the
  346-entry majority) deliberately keep `decision_ordinal: None` rather than the ADR "d1 by
  convention" reading - that convention is identity semantics, not rendering; None doubles as the
  client's "not part of a rendered group" flag and guarantees zero change for existing entries.
  Child rows strip continuity and diagram badges (entry-scoped affordances would misread as N
  events); lifecycle edges/merges/branches compute before expansion, staying entry-scoped by
  construction (decision-specific EDGES await the sidecar contract's implementation).
- A: Considered full section granularity through `graph()` instead - rejected: the trail needs only
  decision sections, and granularity="all" has been deliberately dead since the projection shipped.
- F: `memory_seed/core.py` (regex export), `memory-trace/memory_trace/models.py` (GraphNode
  `decision_ordinal`/`decision_count`), `memory-trace/memory_trace/service.py`
  (`_expand_decision_rows`, `include_decisions`), regenerated
  `memory-trace/tests/contract/{openapi.v1.json,types.ts}` per the documented workflow,
  `memory-trace/tests/test_trail_decision_rows.py` (new, 5 tests),
  `memory-trace/tests/test_v1_api_contract.py` (hardened: first-row-per-entry wins, the last-wins
  dict would silently compare a child's empty continuity).

#### D2 - Decision rows navigate within the entry: exact-row highlight, reader scroll, group-aware search

- D: Per JNL's two answered UX calls: clicking a D2..DN row selects the PARENT entry (full Inspector
  metadata) and scrolls/highlights that decision's heading in the reader, reusing the `matchHint`
  mechanism (extended with `decisionChunkId`); title-mode search highlights ALL of a matching
  entry's rows and counts the entry once; a full-text hit whose best matched section is a decision
  lands on that decision's row, while hits in other sections (Follow-up, Validation) keep the D1 row
  and only scroll the reader.
- R: The alternative (fetching the decision's section chunk into the Inspector) was put to JNL and
  declined - it loses the entry-level metadata grid and context. Implementation subtleties that made
  this non-trivial: (1) same-entry decision switches must NOT call `select()` - its same-node guard
  would toggle the mute state instead of moving decision focus, so `selectFromTrail` grew a
  three-way branch (same entry + same decision = mute toggle; same entry + new decision = hint move
  only; new entry = normal select-then-hint); (2) rows in a rendered group demand an EXACT chunk
  match for selection highlight - the old permissive entryId-OR-chunkId rule would light the whole
  group; ordinary rows keep the permissive rule untouched; (3) `matchEntries` had to map to
  `entry_id` and dedupe - decision rows carry section chunk_ids as `id`, which `focusEntry` cannot
  resolve, and a query matching two decisions must not visit the entry twice; (4) the shared
  `compareTrailNodes` comparator (extracted from the two hand-duplicated sorts in trailModel/App)
  adds a numeric ordinal tiebreak - identical group timestamps otherwise fell to string compare,
  which would put d10 before d2; (5) the window slice extends to a group's end so a bracket never
  dangles; (6) the viewbar and Recent panel filter child rows so both keep counting ENTRIES.
  Rendering: `pastelOf(hex)` is pure HSL lighten/desaturate in the framework-free trailModel;
  brackets draw in a full-width overlay SVG because the rail's viewBox stops at the lane zone and
  rowIndent can reach beyond it.
- F: `memory-trace/client/src/trailModel.ts` (`pastelOf`, `compareTrailNodes`, entry-honest total,
  group-safe window), `TrailWorkspace.tsx` (pastel dots, decision-row class, bracket overlay,
  exact-match selection, group search highlight, widened `onSelectEntry`), `App.tsx` (MatchHint
  `decisionChunkId`, three-way `selectFromTrail`, hintFor decision landing, matchEntries dedupe,
  Recent filter), `styles.css`, `trailModel.test.ts` (new, 3 tests), rebuilt
  `memory-trace/memory_trace/static/react/`.
- T: 178 backend + 72 client unit + 5 Playwright e2e pass; typecheck + production build clean; docs
  check unchanged (one pre-existing off-allowlist error), links check OK. Live-verified against the
  real corpus on `mse_gtn504wfjt3c34p6` (this session's own 3-decision entry): 3 rows with D1 at
  14:58 full colour and pastel D2/D3 beneath on the same lane, bracket visible; D2 click selected
  the parent entry, highlighted "D2 - Incremental reconciliation..." in the reader (scrollTop
  ~1900) and exactly the D2 row in the Trail; D2->D3 switch moved highlight without a mute flip;
  title search "incremental startup" highlighted all 3 rows counting 1/1; full-text "warm-seed
  existing positions" landed on the D3 row specifically; mobile + desktop smoke clean. Committed on
  `claude/feature/trail-decision-rows`.
