---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-17
---

## 2026-07-17 00:04 - Coordinate concurrent Memory Trace cache rebuilds

```yaml
entry_id: mse_d24ne1dvfa3j7sy2
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: codex/fix/trace-cache-concurrency
topics:
  - memory-trace
  - bugfix
evolves:
  - mse_8q7xccvk52812kve
  - mse_xcsvhpw3bncdxq20
```

### Summary

- Hardened Memory Trace's derived SQLite cache so two local server processes can rebuild the same workspace projection without producing a Windows file-lock 500.

### Decision

- D: Use a short-lived OS-level writer lease per primary cache path. A waiting process rechecks the winner's snapshot after it obtains the lease; when the lease remains unavailable after a bounded wait, it builds an isolated temporary projection instead of contending for the shared cache.
- R:
  - The observed failure was an `os.replace` denial during concurrent rebuild, not a Markdown or UI data error.
  - Markdown remains authoritative; both the shared SQLite cache and emergency temporary projections are disposable derived state.
- A: Rejected making unique temporary caches the normal default because it hides contention, duplicates warm caches, and leaves normal multi-process coordination unresolved. The session scaffolder could not create this new dated file in the OneDrive worktree, so this append uses the canonical generated entry ID.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/tests/test_service.py`, and `docs/3_Spec/functionality-audit.md`.
- T: `python -m unittest discover -s memory-trace/tests -p 'test_*.py'` passed 143 tests; a two-server shared-cache smoke test returned `200` from both `/api/runtime` endpoints with one shared SQLite file.

## 2026-07-17 00:25 - Add one-command dual Memory Trace launcher

```yaml
entry_id: mse_x3sp2m5r0qatmtcn
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: codex/feature/trace-dual-launch
topics:
  - memory-trace
  - process-management
related_entries:
  - mse_d24ne1dvfa3j7sy2
```

### Summary

- Added a single-command path to open both Memory Trace renderers without starting duplicate servers or manually constructing local import paths.

### Decision

- D: Add `memory-trace --open-both` for installed users and `scripts/launch-memory-trace.ps1` for this Windows source checkout. Both open vanilla `/` and React `/next` from one local Trace server.
- R:
  - The two renderers share the same API and server process, so separate launches add cache contention and port-management cost without adding capability.
  - The source wrapper makes the current repository runnable without a separately installed console script and reuses a healthy server already on the selected port.
- A: Rejected launching two Trace server processes, because it duplicates the derived cache and recreates the concurrency failure mode that the prior cache hardening just removed. The session scaffolder could not touch this existing worktree log under OneDrive, so this append uses the canonical generated entry ID.
- F: `memory-trace/memory_trace/cli.py`, `memory-trace/memory_trace/service.py`, `memory_seed/cli.py`, `scripts/launch-memory-trace.ps1`, `memory-trace/README.md`, `README.md`, `docs/3_Spec/functionality-audit.md`, `memory-trace/tests/test_service.py`, and `tests/test_memory_seed.py`.
- T: `python -m unittest memory-trace/tests/test_service.py` passed 55 tests; the focused legacy-shim test passed; `scripts/launch-memory-trace.ps1 -Port 8770 -NoOpen` reused the running Trace server and reported both URLs.

## 2026-07-17 01:05 - Audit test coverage, deprecation status, and release gates

```yaml
entry_id: mse_qgc90wxjngj6ss11
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - memory-trace
  - memory-seed
  - release
```

### Summary

- Audited the root, Memory Trace, release-workflow, and React typecheck gates to distinguish necessary regression coverage from deprecated compatibility coverage.

### Decision

- D: Retain the current suites; do not reduce coverage by raw test count. Retire the two `memory-seed lense` shim tests only in the same change that removes the still-supported deprecated alias.
- R:
  - The root suite is 518 collected cases because it exercises integration-heavy core behaviours; its 517 passing cases complete in about 86 seconds.
  - The documented one-release deprecation window for the `lense` extra and command remains open pending the explicit 2.19 decision.
- A: Rejected deleting tests merely because their class names or fixtures retain historical terminology. The `LenseCliTests` class contains current Memory Trace and vanilla UI regressions; only its name is stale. The initial session append attempt used standard input, which the current CLI does not accept for `--body-file`; it made no write.
- F: No production or test-selection files changed. The audit identified release-gate gaps: the release workflow omits 231 root pytest cases, does not run on ordinary pushes or pull requests, and does not install/type-check the React source.
- T: `python -m pytest tests` passed 517 with 1 skipped in 86.39s; `python -m unittest discover -s memory-trace/tests -p 'test_*.py'` passed 146 in 41.73s; the release workflow's core command passed 287 with 1 skipped in 74.78s; `npm run typecheck` passed after `npm ci`.

## 2026-07-17 01:05 - Reconcile NEXT_STEPS drift after plan soundness review

```yaml
entry_id: mse_qkvkd8qkza7kk4y8
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/next-steps-defect-fixes
topics:
  - documentation
  - proposal-lifecycle
related_entries:
  - mse_ddba1ztxqhasfbwf
evolves:
  - mse_mazbt6cek8m8a6st
```

### Summary

- Reviewed `docs/2_Todo/0_NEXT_STEPS.md` for soundness against `CHANGELOG.md` and the tree. The factual
  spine reconciles - no overclaim - so the plan is sound; three housekeeping defects were fixed.

### Decisions

#### D1 - Surface the Trace distribution plan in NEXT_STEPS rather than relane it

- D: Keep `memory-trace-distribution-plan.md` in `2_Todo/` and add it as Track A tail 4, naming its live
  obligation: the `memory-seed[lense]` deprecated alias kept "for one release window" is still open, and
  whether 2.19 drops it is an open user decision.
- R: The doc looked terminal (both phases implemented and released) and unreferenced by NEXT_STEPS, which
  claims to enumerate live work. But `memory-trace-next-generation-coverage-matrix.md` records a
  deliberate "Keep active" decision owning the packaging/shim/no-default-web-dependency criteria, and
  `pyproject.toml` still ships the `lense` extra as a deprecation alias. The lane is correct; the
  omission from NEXT_STEPS was the actual defect.
- A: Rejected moving it to `5_Completed/` or `4_Reference/` - that would have contradicted a recorded
  governance decision and silently dropped an open release obligation. The inbound-link check is what
  caught this before the move.
- F: `docs/2_Todo/0_NEXT_STEPS.md`

#### D2 - Drop the superpowers/specs housekeeping item as stale, keeping the why

- D: Remove the "remove the empty `superpowers/specs/`" item from doc-lifecycle Phase 2 in all four
  places it appeared, leaving an inline note recording why it was dropped.
- R: No `superpowers` directory exists in this worktree, in the primary working directory, or anywhere in
  git history. Since git does not track empty directories, the worktree alone was not sufficient evidence
  and the primary working directory was checked before removing the item.
- A: Kept an explanatory note rather than deleting the item outright, per Invariant #2 - the corpus should
  record why a tracked item disappeared, not silently lose it.
- F: `docs/2_Todo/0_NEXT_STEPS.md`, `docs/2_Todo/document-lifecycle-system-plan.md`, `docs/README.md`

#### D3 - Correct the Track C "blocking tails" terminology

- D: Reword Track C's priority clause from "below Track A's blocking tails" to "below Track A's open
  tails".
- R: Track A defines all three of its tails as non-blocking (item 1 "not a blocker", item 2 product-gated,
  item 3 "unblocked"), so "blocking" inverted the defining section. The priority ordering was intended and
  is preserved; only the word was wrong.
- F: `docs/2_Todo/0_NEXT_STEPS.md`
- T: `python -m pytest tests/` - 517 passed, 14 subtests passed

### Notes

- Checked and dismissed two suspected defects: the `2_Todo/completed/` count of 43 is accurate (a naive
  listing shows 44 only because `agent-templates/` is a subdirectory), and the React/B0b work's absence
  from `CHANGELOG.md` is correct by that file's convention - it records completed changes, and the plan
  never claims React parity is shipped.
- The 2026-07-17 trace-cache concurrency fix is not yet in `CHANGELOG.md`; it postdates the plan's last
  update and is left for the release-prep pass.

## 2026-07-17 01:17 - Add reusable full verification gates

```yaml
entry_id: mse_sp421s7cxtrbfqha
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: codex/feature/test-gates
topics:
  - release
  - memory-trace
  - memory-seed
related_entries:
  - mse_qgc90wxjngj6ss11
```

### Summary

- Added a reusable GitHub Actions verification workflow for pushes to `main`, pull requests, and release publishing.
- Expanded the release gate from a narrow unittest subset to the full root suite, session-link integrity, all Memory Trace tests, and the React typecheck/build and packaged-asset freshness checks.
- Renamed the stale `LenseCliTests` class to describe the current Memory Trace CLI and vanilla UI coverage; retained the real legacy CLI-shim tests during the documented compatibility window.
- Updated the functionality audit to describe the live verification contract.

### Decision

- **D:** Make `.github/workflows/verify.yml` the reusable source of truth for routine and release verification; `publish.yml` must call it before packaging.
- **R:** This closes the audit gap where release publishing ran only a small root subset and ordinary pushes and pull requests had no equivalent gate.
- **A:** The root suite runs before installing the Trace extra because the supported core-only `lense` compatibility-shim test would otherwise start the real Trace server. The initial sandboxed React build could not spawn esbuild, but the same build passed outside that restriction and left packaged assets unchanged.

### Files

- `.github/workflows/verify.yml`
- `.github/workflows/publish.yml`
- `memory-trace/tests/test_service.py`
- `docs/3_Spec/functionality-audit.md`

### Tests

- `python -m pytest tests` -> 517 passed, 1 skipped
- `python -m unittest discover -s memory-trace/tests -p 'test_*.py'` -> 146 passed
- `python -m memory_seed.cli links check` -> passed with 26 existing lifecycle-classification warnings
- `npm run typecheck` -> passed
- `npm run build` -> passed; `memory-trace/memory_trace/static/react/` unchanged

## 2026-07-17 01:20 - Backfill CHANGELOG for merged Trace concurrency and dual-launch work

```yaml
entry_id: mse_a20fkg47qz07nsc8
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/changelog-backfill
topics:
  - memory-trace
  - documentation
related_entries:
  - mse_d24ne1dvfa3j7sy2
  - mse_x3sp2m5r0qatmtcn
  - mse_qgc90wxjngj6ss11
evolves:
  - mse_qkvkd8qkza7kk4y8
```

### Summary

- Recorded the two completed-but-undocumented Trace changes in `CHANGELOG.md` "## Unreleased", closing the
  gap the prior soundness review identified and deferred.

### Decision

- D: Backfill both the concurrent-cache-rebuild fix and `memory-trace --open-both` into the root
  `CHANGELOG.md` now rather than at release prep, and keep both in the root file rather than
  `memory-trace/CHANGELOG.md`.
- R:
  - "## Unreleased" is the authority `0_NEXT_STEPS.md` defers to for what is shipped-but-unreleased, so a
    merged change missing from it makes the roadmap's own source of truth wrong.
  - `memory-trace/CHANGELOG.md` states that core behaviour changes are recorded in the root file, and both
    changes touch core-visible surfaces (`memory_seed/cli.py`, the service cache).
  - Deferring to release prep is what let the gap open; the entry cost is small and the drift cost compounds.
- A: Rejected splitting the entries across the two changelogs, which would contradict the trace changelog's
  own stated boundary. Rejected waiting for the 2.19 cut, since the cut is blocked on user approval and the
  backfill is not.
- F: `CHANGELOG.md`.
- T: `python -m unittest tests.test_memory_seed tests.test_processes` (287 pass) and
  `python -m unittest discover -s memory-trace/tests` with `PYTHONPATH` set to the repo and
  `memory-trace/` (146 pass). Trace tests need the package importable - pytest from the repo root alone
  cannot collect them, which is a test-invocation trap, not a regression.

## 2026-07-17 02:05 - Ship link add and block the P2 backfill on Invariant 2

```yaml
entry_id: mse_1cabc5sw1g4hkb25
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/link-add
topics:
  - memory-seed
  - graph
  - proposal-lifecycle
related_entries:
  - mse_a20fkg47qz07nsc8
```

### Decisions

#### D1 - Ship `link add`, refuse any non-newest source

- D: Implement `memory-seed link add <target> [--from]` writing one `related_entries` edge from the
  newest entry to an older one. Forward-only, idempotent, YAML-only, `links check`-gated. Any source
  that is not the newest entry is refused at the writer, not just at the CLI.
- R:
  - Adding to the entry being authored does not rewrite history, so it is clean under Invariant #2,
    and the plan itself calls this "the normal path because it does not rewrite history".
  - Enforcing in the library (not the CLI) means the guard cannot be bypassed by a future caller,
    which matters because the refused path is exactly the unratified backfill.
- A: Rejected implementing the plan whole. Rejected putting the newest-entry check only in the CLI.
- F: `memory_seed/semantic_cache.py`, `memory_seed/cli.py`, `tests/test_semantic_cache.py`,
  `CHANGELOG.md`, `docs/2_Todo/related-entries-p2-mutation-plan.md`.
- T: `pytest tests/` 526 pass; core CI invocation 287 pass; `memory-trace/tests` 146 pass. Eight new
  cases cover append, key-creation, idempotency, self/forward/unknown refusal, the Invariant #2
  refusal, file-ending preservation, and unterminated-block refusal.

#### D2 - The P2 historical backfill is blocked pending a constitutional ruling

- D: Do not build `link backfill`. Record it in the plan as an open user decision with three options:
  reject it in favour of the shipped evolution-edges seeding pass, amend Invariant #2 for a
  metadata-only carve-out, or record a reading that YAML curation was never "rewriting the past".
- R:
  - The plan was approved 2026-07-05; the Constitution was ratified 2026-07-14. The sign-off predates
    the invariant and cannot authorise it retroactively.
  - Backfill mutates existing entries' YAML, which collides with Invariant #2 ("never rewrite or
    delete"). Constitution §11: a proposal conflicting with a live invariant "is rejected or must
    first amend the invariant - it cannot silently override it".
  - The evolution-edges seeding pass already adds edges to history by writing *new* entries, so the
    capability may not need the invariant bent at all.
- A: Rejected building it on the 2026-07-05 approval. Rejected silently dropping it from the plan.
- F: `docs/2_Todo/related-entries-p2-mutation-plan.md`.

#### D3 - Fixed a crash found by reviewing the change, not by the tests

- D: Bound the closing-fence search to the source entry and refuse an unterminated yaml block.
- R: The first implementation scanned the whole file for the closing fence: an entry whose fence ran
  to EOF raised `IndexError`, and a stray fence in a later entry could have spliced metadata into
  another entry's prose. Every test passed while this was broken - the review caught it, so it is
  now pinned by a regression case.
- A: Also removed dead CRLF-preservation code: `read_text_file` uses universal newlines and
  `write_text_file` re-emits canonical LF, so the newline detection could never fire.
- F: `memory_seed/semantic_cache.py`, `tests/test_semantic_cache.py`.

## 2026-07-17 02:45 - Ship memory-quality metrics v0 and record the first baseline

```yaml
entry_id: mse_b1q6bjqv5w1zyn2k
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/quality-metrics-v0
topics:
  - memory-seed
  - documentation
related_entries:
  - mse_1cabc5sw1g4hkb25
```

### Decisions

#### D1 - Ship v0 with two metrics measured and three honestly unmeasured

- D: Implement `memory-seed quality report [--json]` measuring unlinked-entry rate (by age band) and
  DRAFT reason coverage. Report citation and provenance coverage as `unavailable`, and ranking-A/B
  regression as `not_applicable`, rather than delaying v0 until BG1 lands.
- R:
  - The proposal's own contract requires exactly this: no population -> `not_applicable`, "never 100
    percent". Shipping the two measurable metrics now produces the baseline that gates everything else.
  - `unavailable` and `not_applicable` are kept distinct on purpose: "there is no way to look yet" (BG1
    absent) is a different claim from "we looked and the population was empty" (no ranking-ab run
    supplied). Collapsing them is how a metric starts lying.
- A: Rejected blocking v0 on BG1 - it would stall the P1 baseline behind an unbuilt taxonomy. Rejected
  reimplementing ranking-ab scoring inside the report; it consumes that contract or reports nothing.
- F: `memory_seed/quality.py` (new), `memory_seed/cli.py`, `tests/test_quality.py` (new),
  `docs/4_Reference/memory-quality-v0-baseline.md` (new), `CHANGELOG.md`,
  `docs/2_Todo/memory-quality-metrics-v0-proposal.md`.
- T: `pytest tests/` 535 pass (9 new: empty corpus, fully-linked, mixed, exclusions, the
  decision-without-reason case, unavailable-vs-not_applicable, determinism, no-writes, and no-rate-when-
  unmeasured); core CI invocation 287 pass; `memory-trace/tests` 146 pass; `links check` clean.

#### D2 - Record the baseline as evidence and stop before targets

- D: Record the first real-corpus baseline at revision `bc9b174` in `docs/4_Reference/` and set the
  proposal to `blocked_by` a user usefulness review. Propose no targets.
- R:
  - The proposal's non-goals forbid targets before a baseline, and its promotion gate says not to
    graduate Constitution section 8 merely because the command exists. Step 6 is explicitly a review.
  - Baseline: unlinked 95/431 (22.0%; 0-30d: 15, 31-90d: 80, 90d+: 0), DRAFT reason coverage 403/403
    with 28 excluded. The 100% is the write-time DRAFT lint holding, not independent evidence that
    reasons are good - the metric measures structure only, and the entry says so.
- A: Rejected moving the proposal to `5_Completed`: what remains is a user review, and per the lane
  rules a blocked item stays in `2_Todo` with `blocked_by`.
- F: `docs/4_Reference/memory-quality-v0-baseline.md`, `docs/2_Todo/memory-quality-metrics-v0-proposal.md`.

#### D3 - Pinned the coupling to core's format parser with a test

- D: Match `check_entry_format`'s issue text for the missing-reason case rather than reimplementing the
  rule, and guard that coupling with a test that a `D:` with no `R:` counts as uncovered.
- R: Reimplementing the structural rule would fork the canonical parser. But matching on message text
  fails silently - a reworded message would make `missing` empty and the metric would report 100%
  coverage, which is exactly the dishonesty this report exists to avoid. The test converts that silent
  failure into a loud one. Review of my own diff also removed a dead `_DRAFT_LABELS` constant.
- F: `memory_seed/quality.py`, `tests/test_quality.py`.

## 2026-07-17 03:10 - Publish BG1 provenance authority crosswalk and surface the v1 contract break

```yaml
entry_id: mse_xje157km2m3rfzv0
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/bg1-provenance-crosswalk
topics:
  - memory-seed
  - documentation
  - memory-trace
related_entries:
  - mse_b1q6bjqv5w1zyn2k
```

### Decisions

#### D1 - Publish the crosswalk as a draft spec and stop at step 2

- D: Deliver BG1 steps 1-2 (inventory + canonical ownership + alias map) as
  `docs/3_Spec/draft/provenance-authority-crosswalk.md`. Change no code, no field, and no default.
- R:
  - The proposal's own next_action says "Produce an additive model/crosswalk proposal and fixtures; do
    not change ranking or actionability defaults in the taxonomy-definition step."
  - `3_Spec/draft/` is the correct lane: the lane README states only `3_Spec/` (parent) is normative and
    binding, so a candidate taxonomy belongs in draft until adopted.
- A: Rejected implementing the enum in the same change; step 3 is blocked (D2).
- F: `docs/3_Spec/draft/provenance-authority-crosswalk.md` (new), `docs/3_Spec/draft/README.md`,
  `docs/2_Todo/memory-provenance-and-authority-taxonomy-proposal.md`.
- T: `pytest tests/` 535 pass; `links check` clean. Every file:line reference in the crosswalk was
  verified against the source before publishing.

#### D2 - The inventory falsifies the proposal's premise; step 3 is blocked on a v1 contract decision

- D: Record that `authority_class` already exists and already disagrees with the proposal, and block
  step 3 on an explicit user decision rather than picking a resolution.
- R:
  - BG1 reads as though `authority_class` is unbuilt. It is not: `RendererGraphNode.authority_class`
    exists as a bare `str`, validated only as "non-empty" (`graph_projection.py:218`), emitting
    `canonical_memory` (L120) - a value absent from the proposal's seven-value vocabulary. The authority
    axis is already carrying an undocumented parallel vocabulary, the exact failure BG1 exists to prevent.
  - Fixing it is not additive, which step 3 requires: `openapi.v1.json:1045` publishes the field as
    `"type": "string"`, so enum-constraining it or renaming the value breaks `/api/v1`.
  - An agent should not break a frozen versioned contract unilaterally; the API is versioned precisely so
    this correction has a home.
- A: Four options recorded in crosswalk section 5 - rename+enum in v1, admit `canonical_memory` as a
  synonym, defer the enum to v2, or keep the wire value and map at the boundary. Deliberately not chosen.
- F: `docs/3_Spec/draft/provenance-authority-crosswalk.md`,
  `docs/2_Todo/memory-provenance-and-authority-taxonomy-proposal.md`.

#### D3 - Recorded two vocabulary hazards found while inventorying

- D: Document that the `Authored/Computed/External/Derived` shorthand is lossy across both axes and must
  never be the stored field, and that `processes.py:18` `Confidence` is process-detection confidence -
  unrelated to provenance confidence and not to be reused.
- R: The shorthand's `External` spans three provenance values while `Authored`/`Computed` share one, so
  storing it would destroy information. Reusing the `Confidence` literal would silently couple a UI trust
  signal to process detection - a name collision, not a shared concept.
- F: `docs/3_Spec/draft/provenance-authority-crosswalk.md`.

## 2026-07-17 03:40 - Ship the Track E dry-run worktree classifier

```yaml
entry_id: mse_1yk6gw5chvv6xks5
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/worktree-gc-classifier
topics:
  - memory-seed
  - git-workflow
related_entries:
  - mse_xje157km2m3rfzv0
```

### Decisions

#### D1 - Ship the classifier alone; the remover is a separate increment

- D: Implement `memory-seed worktree classify` (read-only, dry-run only) and explicitly do not implement
  `--apply` removal in this change.
- R:
  - Classification is the safe half and is independently useful; removal is destructive and deserves a
    supervised implementation rather than an autonomous one.
  - The plan's own Phase 1 says "Default to dry-run" - shipping dry-run as the only mode makes that the
    literal default rather than a flag someone can forget.
- A: Rejected shipping both halves together. Rejected a `--force` escape hatch: the plan forbids any raw
  recursive deletion fallback, and an escape hatch is how that rule gets bypassed later.
- F: `memory_seed/worktree_gc.py` (new), `memory_seed/cli.py`, `tests/test_worktree_gc.py` (new),
  `CHANGELOG.md`, `docs/2_Todo/agent-worktree-and-branch-hygiene-plan.md`.
- T: `pytest tests/` 547 pass (12 new, over real temp git repos rather than stubs - stubbed git would
  prove nothing about git-integration code).

#### D2 - Everything unanswerable fails closed, and `foreign` was added beyond the listed states

- D: A worktree is `removable` only when every safety question answers yes at once. Unreadable `git
  status`, detached HEAD, and indeterminate merge status all classify `unknown`, which refuses removal.
  Added a `foreign` state not in the plan's list.
- R:
  - The plan's rule is "never assume merge status implies a clean worktree"; the inverse matters just as
    much - an unanswerable question must never default to permitted.
  - The plan's state list omits ownership, but a clean, merged worktree in another agent's namespace is
    still not ours to remove. `foreign` records that distinction rather than reporting it `removable`.
- A: Rejected reporting a set of states per worktree; one state plus full evidence makes the reason a
  removal was refused unambiguous.
- F: `memory_seed/worktree_gc.py`, `tests/test_worktree_gc.py`.

#### D3 - Fixed a misclassification found by running it, and confirmed the plan's lock premise

- D: Take the main worktree from the first `git worktree list --porcelain` entry, not from `rev-parse
  --show-toplevel`.
- R: Inside a linked worktree, `--show-toplevel` returns *that worktree's* path, so the active worktree
  classified as `root` and every namespace would have resolved against the wrong base. Every test passed
  the module's own logic; running the command against this repo is what exposed it.
- A: Field note: `git worktree prune` here failed with `Permission denied` on four stale
  `.git/worktrees/*` admin dirs. The OneDrive lock case Phase 1 is designed around is real and routine,
  which is further reason the remover should not be built unsupervised.
- F: `memory_seed/worktree_gc.py`.

## 2026-07-17 04:20 - Retire the legacy completed archive into the Completed lane

```yaml
entry_id: mse_mr0dhy4s09wcsf9v
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/lifecycle-p2-migration
topics:
  - documentation
  - memory-seed
related_entries:
  - mse_1yk6gw5chvv6xks5
continuity:
  - kind: migration
    from: docs/2_Todo/completed/
    to: docs/5_Completed/
```

### Decisions

#### D1 - Migrate the archive and retire the folder; do not reclassify

- D: Move all 43 `2_Todo/completed/` docs plus the nested `agent-templates/` into `5_Completed/`, remove
  the folder, and repair every inbound reference. Skip the plan's optional `6_Rejected`/`7_Superseded`
  reclassification.
- R:
  - The source folder was literally named `completed`, so `5_Completed` is the faithful lane; the folder
    is the state, and a legacy archive sitting beside the lanes contradicted that rule.
  - Reclassifying 43 docs is 43 per-doc judgement calls, not a migration. Folding it into a mechanical
    move would smuggle judgement into a rename. Recorded in `7_Superseded/README.md` as still open.
- A: Rejected leaving `agent-templates/` behind: with the 43 docs gone, `2_Todo/completed/` would exist
  solely to hold it, keeping the contradiction alive for one nested folder.
- F: 49 files moved, `docs/README.md`, `docs/7_Superseded/README.md`,
  `docs/2_Todo/document-lifecycle-system-plan.md`, `docs/2_Todo/0_NEXT_STEPS.md`, `.memory-seed/index.md`,
  `tests/test_session_schema.py`, plus 27 docs with textual path references.
- T: `pytest tests/` 547 pass; core CI 287 pass; `memory-trace/tests` 146 pass; `links check` clean.
  **Docs went from 20 broken relative links to 0.**

#### D2 - Repaired links by resolving targets, not by pattern substitution

- D: Fix every broken link by looking up its real target file and computing the correct relative path,
  refusing any ambiguous basename, rather than running a path sed.
- R:
  - The rewrites are depth-sensitive in three different directions at once (links inside moved files,
    links into them from `2_Todo`, and links from other lanes). A sed would have silently mangled a
    subset.
  - The 20 pre-existing broken links were themselves depth errors left by the *previous* move into
    `completed/`; correct depth fixed them as a side effect, which is why the count went to 0 rather
    than back to 20.
- A: Verified with a throwaway link checker run before and after, since `docs check` (the real gate) is
  the remaining Phase 2 item and does not exist yet. Its absence is exactly why this was risky.
- F: `docs/**`.

#### D3 - Session logs keep their old paths; the move is a continuity event

- D: Do not rewrite the 8 session files that reference `docs/2_Todo/completed/...`. Record the migration
  as a `continuity:` block on this entry instead.
- R: Session logs are append-only history (Invariant #2). Their old paths are true statements about what
  was true then, and `continuity:` exists precisely to record renames/migrations without touching
  history - the Trail renders it as a lineage lane.
- A: Rejected updating them for tidiness; that is the rewrite the invariant forbids.

#### D4 - Two breaks the link checker structurally could not catch

- D: Fixed `tests/test_session_schema.py`, which hard-codes the doc's path and failed after the move, and
  27 docs carrying the path in YAML `related_to:`, JSON example payloads, and link *text*.
- R: The checker only validates markdown link *targets*. A hard-coded path in a test and a path inside
  YAML or link text are invisible to it - the test suite caught the first, and a plain grep the second.
  A green link check was not sufficient evidence that the migration was complete.
- A: One blanket replace then corrupted my own prose in the lifecycle plan (rewriting the very path the
  sentence explained session logs retain); caught on review and restored.
- F: `tests/test_session_schema.py`, 27 docs.

## 2026-07-17 05:00 - Ship docs check and repair the spec_binding drift it found

```yaml
entry_id: mse_5nr2pvjyrs92bvj2
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/feature/docs-check
topics:
  - documentation
  - memory-seed
related_entries:
  - mse_mr0dhy4s09wcsf9v
evolves:
  - mse_mr0dhy4s09wcsf9v
```

### Decisions

#### D1 - Ship `docs check`; the missing gate was proven by the migration that preceded it

- D: Implement `memory-seed docs check` (read-only) validating relative links, lifecycle pointers,
  `spec_binding`-vs-folder agreement, and the side-folder allowlist.
- R:
  - The 43-document lane migration an hour earlier rewrote ~85 links with **no gate**, forcing a
    throwaway checker to prove it worked. That is precisely the hole this closes, and the evidence that
    it was worth building before `docs index`.
  - It mirrors `links check`/`esr`, so it slots into the existing enforcement surface rather than
    inventing one.
- A: Rejected building `docs index` first: generating per-lane README tables is cosmetic next to a check
  that catches broken facts. Stdlib-only frontmatter parsing reuses `_parse_frontmatter_scalars`; the
  project stays dependency-light with no YAML library.
- F: `memory_seed/docs_check.py` (new), `memory_seed/cli.py`, `tests/test_docs_check.py` (new),
  `CHANGELOG.md`, `docs/2_Todo/document-lifecycle-system-plan.md`.
- T: `pytest tests/` 563 pass (16 new); core CI 287 pass. `docs check` on the real tree: 0 errors,
  29 warnings.

#### D2 - Errors are broken facts; missing YAML only warns

- D: Error on broken links, dangling pointers, `spec_binding` contradicting its folder, and off-allowlist
  folders. Warn on missing `priority`/`next_action`/`superseded_by`.
- R: Secondary-YAML backfill is explicitly still-open Phase 2 work. Failing on it would make the check red
  for a known-unfinished task, which trains people to ignore the check - worse than not having it. A dead
  link is wrong *now*; a missing `next_action` is merely incomplete.
- A: Rejected a single uniform severity in both directions.
- F: `memory_seed/docs_check.py`.

#### D3 - The check caught three real defects on its first run

- D: Repair `spec_binding` in three specs: `adr-lifecycle-sidecar-contract.md` held a *filename*, and the
  two B0a renderer specs held *prose*, in what is an enum field (`live|draft|candidate|deprecated`).
- R: None of the three values were valid bindings, so the field asserting each doc's normative status was
  meaningless in all three. The filename was a misfiled parent pointer; it now uses the `parent:` field
  the sibling specs already use. The descriptive prose moved to a new `role:` field rather than being
  discarded.
- A: Set each `spec_binding` to its folder-implied value rather than guessing intent - the folder is the
  state, so the folder decides.
- F: `docs/3_Spec/draft/adr-lifecycle-sidecar-contract.md`,
  `docs/3_Spec/memory-trace-renderer-benchmark-evidence.md`,
  `docs/3_Spec/memory-trace-renderer-neutral-graph-projection.md`.

## 2026-07-17 05:40 - Make live consent and constitutional conflict explicit stop triggers

```yaml
entry_id: mse_qsfdkttkwmypx9cz
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/consent-stop-triggers
topics:
  - control-plane
  - proposal-lifecycle
related_entries:
  - mse_5nr2pvjyrs92bvj2
```

### Decisions

#### D1 - Extend `risk_signaling.md` rather than add a new skill

- D: Encode the live-consent rule and a constitutional-conflict STOP category in the existing
  `risk_signaling.md` skill (+ seed twin), not in a new skill.
- R:
  - The skill already owns STOP categories and already said "unless the user has explicitly authorized
    that exact class of action **in the current context**". The rule existed; what was missing was that
    a *stale recorded approval* does not satisfy it. Sharpening the existing clause beats a parallel
    skill that would compete with it.
  - "Integrate, don't duplicate" - one canonical owner per concern. A second skill would fork the
    stop-trigger taxonomy.
- A: Rejected a new `consent_gating.md`. Rejected putting it only in `agent-rules.md`, which the seed
  ships but which is not where an agent looks when weighing a specific risky action.
- F: `.memory-seed/skills/risk_signaling.md`, `memory_seed/seed/.memory-seed/skills/risk_signaling.md`,
  and the scratchpad `autonomous-plan-execution-prompt.md`.
- T: `pytest tests/` 563 pass; `esr` reports live skills match seed twins.

#### D2 - "A recorded prior approval is not live consent"

- D: State that a proposal sign-off, a `status:`/`next_action:` field, or an approving session entry
  authorises the *work* but never a STOP-category action taken later while the user is absent. Test:
  *if the user is not present to say no right now, they have not said yes right now.*
- R:
  - This session produced the evidence. The Worker Context Contract was approved 2026-07-14 with scope
    explicitly naming `agent-rules.md` - a locked file. During the autonomous run I read the lock, then
    began reasoning my way past it on the strength of that three-day-old approval. The advisor stopped
    it. The failure mode is not ignorance of the rule; it is a plausible-sounding inference from a real
    approval, which is exactly what an unattended run is most prone to.
  - The user confirmed the intended rule on 2026-07-17 and asked for it to be institutionalised so
    future long-horizon runs inherit it.
- A: Rejected wording that would make every prior approval suspect - the rule is scoped to STOP
  categories only and explicitly says prior approval remains sufficient elsewhere.
- F: `.memory-seed/skills/risk_signaling.md` + twin.

#### D3 - Constitutional/architectural conflict is its own STOP category with no lower tier

- D: Add a STOP category for anything contradicting a ratified invariant or an established
  architectural decision, and state that no tier below Stop applies to it.
- R:
  - Constitution §11 already says such a change "is rejected or must first amend the invariant - it
    cannot silently override it". The skill had no category that carried this, so an agent could route
    a constitutional conflict through Proceed-and-flag by judging the invariant inapplicable.
  - Two real cases this session: the P2 backfill (approved pre-Constitution, conflicts with Invariant
    #2) and the BG1 `authority_class` v1 contract break. Both needed the user; neither was a
    conventional "destructive" or "irreversible" action, so no existing category caught them.
- A: Rejected folding it into "Shared / control-plane state" - a doc can conflict with an invariant
  without touching a control-plane file, and the remedy is different (amendment, not permission).
- F: `.memory-seed/skills/risk_signaling.md` + twin.

## 2026-07-17 06:00 - Adopt the Worker Context Contract for packeted subagents

```yaml
entry_id: mse_5ekvf2d0h5e2gw3y
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/worker-context-contract
topics:
  - control-plane
  - agent-collaboration
related_entries:
  - mse_qsfdkttkwmypx9cz
```

### Decisions

#### D1 - Workers load packet + one persona + triggered skills; safety rails stay

- D: Add the Worker Context Contract to `agent_collaboration.md` and two Task Packet fields (`persona:`,
  `context_load: packet|full`). A worker skips the full index read, the newest-session recency read, the
  whole skill registry, and load-all-active-personas. It still runs `base_sha`/preflight and the
  worktree guard.
- R:
  - Operating Mode Start is written for the primary agent, which must establish current state itself. A
    worker inherits that state from its packet, so re-deriving it is pure waste - four persona operating
    systems are irrelevant to "edit these two files and run these checks".
  - It is also a coordination risk, not just cost: a worker steeped in whole-project context starts
    reasoning about the project instead of executing a bounded objective, and strays outside
    `allowed_files`.
  - The exemption is scoped to *context volume*. A worker that skips the guard is not slim, it is
    unsafe - so the rails are named explicitly as non-exempt.
- A: Kept `persona:` (domain axis) separate from the existing `role:` (orchestration axis). Conflating
  "orchestrator/worker/reviewer" with "developer/copywriter" is the confusion the source evaluation
  identified; the win is trimming context, not renaming roles.
- F: `.memory-seed/skills/agent_collaboration.md`, `.memory-seed/agent-rules.md`, both seed twins.
- T: `pytest tests/` 563 pass; `esr` confirms twin parity.

#### D2 - The contract lives in the skill; agent-rules gets one clause

- D: Put the contract in `agent_collaboration.md` and add only a single clause to agent-rules step 10.
- R: A test enforces `agent-rules.md` <= 260 lines ("should stay compact enough for startup loading")
  and requires the phrase "Keep procedural details in skills". My first attempt added a 7-line block and
  pushed the file to 267 - a change about *reducing* worker startup context bloated the startup contract
  itself. The test caught it. agent-rules already points at `agent_collaboration.md`, so the clause only
  needs to stop a reader of step 10 from loading all personas as a worker.
- A: Rejected raising the 260-line limit; the limit is the rule working, and the file now sits at exactly
  260/260 - any future addition must earn its place by removing something.
- F: `.memory-seed/agent-rules.md` + twin.

#### D3 - Live consent for a locked file

- D: Made these edits under consent given in this conversation on 2026-07-17, not under the 2026-07-14
  proposal approval.
- R: `agent-rules.md` is on the Change Permission Model's locked list. The 07-14 approval named the file
  in its scope, which reads as authorisation - but that inference is exactly what `risk_signaling.md`
  now forbids for an unattended run. The proposal authorised the work; the user authorised the edit.
- F: `.memory-seed/agent-rules.md`.

## 2026-07-17 06:25 - Ratify Constitution 1.2 human-gated metadata curation

```yaml
entry_id: mse_a0mbycf08bfp7p8x
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude/docs/constitution-1.2-backfill-carve-out
topics:
  - proposal-lifecycle
  - documentation
  - memory-seed
related_entries:
  - mse_5ekvf2d0h5e2gw3y
evolves:
  - mse_1cabc5sw1g4hkb25
```

### Decisions

#### D1 - Amend Invariant #2 rather than reject the capability

- D: Ratify Constitution **1.2**. Invariant #2 now permits curating an existing entry's *untyped*
  `related_entries` metadata under four simultaneous conditions: one-off procedure and never core
  functionality; each edge approved by the user at the moment it is added; YAML metadata only, never
  prose; and never a typed lifecycle edge (`supersedes`/`evolves`/`continuity`) written into history.
  Any condition failing means the invariant applies unchanged.
- R:
  - JNL chose option (b) - amend rather than reject - on 2026-07-17, resolving the conflict that blocked
    the Related-entries P2 backfill (approved 2026-07-05, nine days before v1.0 existed).
  - §11 forbids both alternatives that were actually available to an agent: honouring a
    pre-constitutional sign-off, or silently overriding the invariant. Amendment was the only lawful
    route, and the amendment is written to the narrowest shape that permits the capability.
  - The carve-out is defensible because an *untyped* "these two relate" pointer is a navigational aid,
    not a claim about what was decided or why. It does not license editing the record of a decision -
    which is what Invariant #2 exists to protect.
- A: Versioned 1.2, not 2.0. §11 says "2.0 for a changed invariant", but v1.1 set the precedent by
  refining Invariant #6 under a 1.x bump; a narrowing exception follows that precedent.
- F: `docs/CONSTITUTION.md` (invariant + amendment log + header), `docs/README.md`,
  `docs/2_Todo/0_NEXT_STEPS.md`.
- T: `pytest tests/` 563 pass; `docs check` 0 errors.

#### D2 - No `link backfill` command, now or later

- D: Do not build a backfill command. Document a hand procedure in the plan instead, and mark the plan's
  original "Backfill Between Older Entries" section (flags, backups, idempotency) as superseded.
- R:
  - JNL's scope was explicit: *"this should not be part of the core functionality, it's a one-off
    procedure that needs to be done with explicit user permission and gating of each item."* A standing
    command is core functionality by definition, so building one would violate the amendment that
    permits the capability.
  - A command that asks per-edge permission is a worse version of doing it by hand - it adds a
    maintained surface, an automation temptation, and no safety the human step lacks.
  - The procedure points at the evolution-edges seeding pass first: it already adds edges to history by
    writing *new* entries, and read-time inverse edges make the old entry show the backlink anyway. The
    exception should rarely be needed.
- A: Rejected building it with a `--allow-history-edit` flag as the original plan specified; that is the
  shape the amendment excludes.
- F: `docs/2_Todo/related-entries-p2-mutation-plan.md`.

#### D3 - Repaired the refusal message the amendment made false

- D: Update `add_related_entry`'s error text: it said Invariant #2 "forbids" editing older entries, which
  became wrong the moment 1.2 ratified.
- R: The message is the main place a user meets this rule. Leaving it would have made the code assert a
  constitutional claim the Constitution no longer makes - drift of exactly the kind this session has been
  finding. It now states the real rule (permitted only as a one-off, per-edge-approved human procedure,
  which a standing command is not) and points at the procedure.
- F: `memory_seed/semantic_cache.py`.
