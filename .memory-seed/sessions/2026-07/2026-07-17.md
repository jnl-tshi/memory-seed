---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-17
---

## 2026-07-17 00:04 - Coordinate concurrent Memory Trace cache rebuilds

```yaml
entry_id: mse_d24ne1dvfa3j7sy2
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: codex/fix/trace-cache-concurrency
topics:
  - memory-trace
  - bugfix
evolves:
  - mse_8q7xccvk52812kve
  - mse_xcsvhpw3bncdxq20
```

### Summary

- Hardened Memory Trace's derived SQLite cache so two local server processes can rebuild the same workspace projection without producing a Windows file-lock 500.

### Decision

- D: Use a short-lived OS-level writer lease per primary cache path. A waiting process rechecks the winner's snapshot after it obtains the lease; when the lease remains unavailable after a bounded wait, it builds an isolated temporary projection instead of contending for the shared cache.
- R:
  - The observed failure was an `os.replace` denial during concurrent rebuild, not a Markdown or UI data error.
  - Markdown remains authoritative; both the shared SQLite cache and emergency temporary projections are disposable derived state.
- A: Rejected making unique temporary caches the normal default because it hides contention, duplicates warm caches, and leaves normal multi-process coordination unresolved. The session scaffolder could not create this new dated file in the OneDrive worktree, so this append uses the canonical generated entry ID.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/tests/test_service.py`, and `docs/3_Spec/functionality-audit.md`.
- T: `python -m unittest discover -s memory-trace/tests -p 'test_*.py'` passed 143 tests; a two-server shared-cache smoke test returned `200` from both `/api/runtime` endpoints with one shared SQLite file.

## 2026-07-17 00:25 - Add one-command dual Memory Trace launcher

```yaml
entry_id: mse_x3sp2m5r0qatmtcn
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: codex/feature/trace-dual-launch
topics:
  - memory-trace
  - process-management
related_entries:
  - mse_d24ne1dvfa3j7sy2
```

### Summary

- Added a single-command path to open both Memory Trace renderers without starting duplicate servers or manually constructing local import paths.

### Decision

- D: Add `memory-trace --open-both` for installed users and `scripts/launch-memory-trace.ps1` for this Windows source checkout. Both open vanilla `/` and React `/next` from one local Trace server.
- R:
  - The two renderers share the same API and server process, so separate launches add cache contention and port-management cost without adding capability.
  - The source wrapper makes the current repository runnable without a separately installed console script and reuses a healthy server already on the selected port.
- A: Rejected launching two Trace server processes, because it duplicates the derived cache and recreates the concurrency failure mode that the prior cache hardening just removed. The session scaffolder could not touch this existing worktree log under OneDrive, so this append uses the canonical generated entry ID.
- F: `memory-trace/memory_trace/cli.py`, `memory-trace/memory_trace/service.py`, `memory_seed/cli.py`, `scripts/launch-memory-trace.ps1`, `memory-trace/README.md`, `README.md`, `docs/3_Spec/functionality-audit.md`, `memory-trace/tests/test_service.py`, and `tests/test_memory_seed.py`.
- T: `python -m unittest memory-trace/tests/test_service.py` passed 55 tests; the focused legacy-shim test passed; `scripts/launch-memory-trace.ps1 -Port 8770 -NoOpen` reused the running Trace server and reported both URLs.
