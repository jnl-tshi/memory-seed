---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-10
---

## 2026-07-10 02:59 - Merge monthly session folder layout to main

```yaml
entry_id: mse_cne938zjaq5w6605
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - git-workflow
  - session-layout
  - merge
related_entries:
  - mse_k2188xq5evwmdny9
commits:
  - 5e1a2b6e1aa1ae9a30db200283a47350280af3ec
```

### Summary

- Merged `codex-feature-monthly-session-folders` back into `main` using a non-fast-forward merge.

### Decision

- D: Preserve the monthly-session feature branch in the Git graph with `git merge --no-ff`.
- R: The repository's branch-history workflow expects completed feature work to appear as discrete
  branch-and-merge topology rather than a linear fast-forward.
- F: `main`, merge commit `5e1a2b6`, `.memory-seed/sessions/2026-07/2026-07-10.md`.
- T: `git log --graph --decorate --oneline -n 8` shows the monthly-session branch merged into
  `main`; `git status --short --branch` shows `main...origin/main [ahead 7]` with only the
  pre-existing untracked `docs/1_Inbox/`.

## 2026-07-10 03:16 - Apply month-folder repair to main session tree

```yaml
entry_id: mse_pdbm4vfwavps92xk
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - session-layout
  - migration
  - memory-repair
related_entries:
  - mse_cne938zjaq5w6605
  - mse_k2188xq5evwmdny9
```

### Summary

- Applied the explicit month-layout migration to the repository's own historical session files and
  repaired the merged 2026-07-09 chronology.

### Decision

- D: Move historical root session files into month folders on `main`, leaving only months that have
  session Markdown at the session root.
- R: The current repository should model the canonical grouped layout, and keeping both flat and
  grouped 2026-07-09 files would create duplicate-read ambiguity.
- A: The migration created ignored timestamped backups under `.memory-seed/backups/20260710-031326/`
  before removing the flat sources.
- F: `.memory-seed/sessions/2026-05/`, `.memory-seed/sessions/2026-06/`,
  `.memory-seed/sessions/2026-07/`, `.memory-seed/sessions/2026-07/2026-07-09.md`,
  `.memory-seed/sessions/2026-07/2026-07-10.md`,
  `.memory-seed/sessions/diagrams/2026-07/2026-07-05.md`.
- T: `python -m memory_seed.cli migrate sessions-month-layout --dry-run` reported no old session
  files remaining; `python -m memory_seed.cli links check` passed for 31 files; `git diff --check`
  passed; `python -m memory_seed.cli doctor` passed with the known non-fatal encoding warning.

## 2026-07-10 03:28 - Tighten decision harvest and diagram sidecar logging

```yaml
entry_id: mse_m6amd5db4c7s8sfw
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - session-logging
  - decision-harvest
  - mermaid-sidecars
  - control-plane
related_entries:
  - mse_cne938zjaq5w6605
  - mse_k2188xq5evwmdny9
```

### Summary

- Tightened the session closeout rules so durable choices are harvested before choosing the entry
  shape, and topology/migration/workflow decisions are more likely to get diagram sidecars.

### Decisions

#### D1 - Add decision harvest

- D: Add an explicit Decision Harvest step to `session_logging.md` and route End Of Turn through it.
- R: The July 9 and July 10 implementation runs compressed several durable choices into single
  broad `D:` records, making later rationale harder to recover.
- F: `.memory-seed/skills/session_logging.md`, `.memory-seed/skills/end_of_turn.md`,
  `memory_seed/seed/.memory-seed/skills/session_logging.md`,
  `memory_seed/seed/.memory-seed/skills/end_of_turn.md`.

#### D2 - Strengthen sidecar triggers

- D: Change decision-diagram guidance from optional consideration to positive-trigger behavior for
  branch/merge topology, migrations, schema/layout compatibility flows, multi-agent concurrency,
  command lifecycles, and retrieval/data pipelines.
- R: The old wording correctly discouraged decorative diagrams but was too easy for agents to
  interpret as "skip diagrams unless forced."
- F: `.memory-seed/skills/session_logging.md`, `.memory-seed/skills/end_of_turn.md`,
  `.memory-seed/agent-rules.md`, `memory_seed/seed/.memory-seed/skills/session_logging.md`,
  `memory_seed/seed/.memory-seed/skills/end_of_turn.md`,
  `memory_seed/seed/.memory-seed/agent-rules.md`,
  `.memory-seed/sessions/diagrams/2026-07/2026-07-10.md`.

#### D3 - Keep live and seed runtime behavior aligned

- D: Mirror the changed control-plane and skill wording into the seed templates.
- R: Memory Seed dogfood behavior should match what new initialized projects receive.
- F: `memory_seed/seed/.memory-seed/agent-rules.md`,
  `memory_seed/seed/.memory-seed/skills/session_logging.md`,
  `memory_seed/seed/.memory-seed/skills/end_of_turn.md`.

### Validation

- `python -m memory_seed.cli links check` passed for 32 files; `python -m memory_seed.cli doctor`
  passed with the known non-fatal encoding warning; `git diff --check` passed; live and seed copies
  of `agent-rules.md`, `session_logging.md`, and `end_of_turn.md` matched after editing.

## 2026-07-10 03:34 - Record branch-decision gap for control-plane edit

```yaml
entry_id: mse_sngrjb8vqmh3ap8q
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - git-workflow
  - branch-history
  - control-plane
related_entries:
  - mse_m6amd5db4c7s8sfw
  - mse_y3gb93k466tcfdq8
```

### Summary

- Recorded that the prior decision-harvest and diagram-sidecar control-plane change was applied
  directly on `main` without a sufficiently explicit branch decision.

### Decision

- D: Treat the previous direct-main edit as a workflow miss to avoid repeating the same pattern.
- R: `agent_collaboration.md` says distinct feature, fix, refactor, test, or documentation tasks
  should use their own task branch unless direct-main work is explicitly chosen; the previous task was
  a distinct control-plane behavior change.
- A: The implicit rationale was proximity to an existing main-tree repair and low perceived blast
  radius, but that should have been stated up front or handled on a task branch.
- F: `.memory-seed/sessions/2026-07/2026-07-10.md`.
- T: `python -m memory_seed.cli links check` passed for 32 files.

## 2026-07-10 04:02 - Evaluate session fuse design before implementation

```yaml
entry_id: mse_9c151e4gbkkv1w5v
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - session-fuse
  - git-workflow
  - team-collaboration
  - design-evaluation
related_entries:
  - mse_sngrjb8vqmh3ap8q
  - mse_m6amd5db4c7s8sfw
  - mse_y3gb93k466tcfdq8
```

### Summary

- Evaluated the proposed `memory-seed session fuse` workflow before implementation, including Git
  merge primitives, existing session parsing code, and team-use constraints.

### Decision

- D: Treat session fuse as an explicit Memory Seed-aware integration command, not as a Git union
  merge driver or automatic notes mechanism.
- R: Git can merge text and preserve branch topology, but it does not validate `entry_id`,
  `branch:` provenance, append-only/session-order semantics, diagram sidecars, or existing-entry
  immutability.
- A: Recommended defaulting fuse to dry-run with an explicit apply mode, requiring source entries to
  carry a non-main `branch:` value matching the source branch, normalizing target paths into the
  current grouped layout, and blocking any existing-entry edits.
- F: `.memory-seed/sessions/2026-07/2026-07-10.md`.
- T: Pending final design questions before implementation.

## 2026-07-10 04:27 - Implement branch-session fuse MCP preview

```yaml
entry_id: mse_81v7vk4x5ys3k2n0
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: codex-feature-session-fuse
topics:
  - session-fuse
  - mcp-tools
  - agent-collaboration
  - diagram-sidecars
related_entries:
  - mse_9c151e4gbkkv1w5v
  - mse_sngrjb8vqmh3ap8q
  - mse_m6amd5db4c7s8sfw
```

### Summary

- Implemented branch-session fuse planning/application logic, a CLI command, read-only MCP preview
  tools, and lazy-loaded collaboration-skill guidance for LLM-managed branch-memory integration.

### Decisions

#### D1 - Make fuse Memory Seed-aware

- D: Add `session_fuse()` plus `memory-seed session fuse --branch <branch> [--base <ref>] [--apply]`.
- R: Git merge mechanics do not know session-entry immutability, `branch:` provenance, grouped target
  paths, sidecar parent requirements, or chronological session ordering.
- A: `--apply` stays gated to an in-progress `git merge --no-ff --no-commit <branch>`; dry-run is the
  default.
- F: `memory_seed/core.py`, `memory_seed/cli.py`, `tests/test_memory_seed.py`.

#### D2 - Require sidecar parent acceptance

- D: Import diagram sidecars only when the parent entry is already present on the base/main tree or
  the parent branch entry is accepted for promotion in the same fuse.
- R: A sidecar without a promoted parent creates an orphan reasoning artifact and breaks the
  entry-linked diagram contract.
- A: Malformed sidecar blocks without `entry_id`, orphan sidecars, modified existing sidecars, and
  date mismatches now block fuse planning.
- F: `memory_seed/core.py`, `tests/test_memory_seed.py`.

#### D3 - Add MCP and skill surfaces

- D: Expose `memory_branch_status` and `memory_session_fuse_preview` as read-only MCP tools, and route
  their usage through `agent_collaboration.md`.
- R: The intended operator is often an LLM; it needs structured branch/fuse state and skill guidance,
  while file-changing apply remains an explicit CLI action.
- A: `history_retrieval.md` now describes retrieval MCP tools only; `agent_collaboration.md` owns the
  branch/fuse MCP control surface.
- F: `memory_seed/mcp_server.py`, `tests/test_mcp_server.py`, `.memory-seed/skills/index.md`,
  `.memory-seed/skills/agent_collaboration.md`, `.memory-seed/skills/history_retrieval.md`,
  `memory_seed/seed/.memory-seed/skills/index.md`,
  `memory_seed/seed/.memory-seed/skills/agent_collaboration.md`,
  `memory_seed/seed/.memory-seed/skills/history_retrieval.md`.

#### D4 - Document the workflow as current architecture

- D: Update the public README, functionality audit, active index, and changelog with the fuse and MCP
  preview contract.
- R: Agents and users need the same source-of-truth wording for sidecar parent gating, dry-run-first
  behavior, and CLI-only apply.
- F: `README.md`, `docs/3_Spec/functionality-audit.md`, `.memory-seed/index.md`, `CHANGELOG.md`,
  `.memory-seed/sessions/2026-07/2026-07-10.md`,
  `.memory-seed/sessions/diagrams/2026-07/2026-07-10.md`.

### Validation

- `python -m unittest ...` targeted fuse and MCP tests passed for 13 tests.
- `python -m py_compile memory_seed\core.py memory_seed\cli.py memory_seed\mcp_server.py` passed.
- `python -m unittest discover -s tests` passed for 303 tests.
- `python -m memory_seed.cli links check` passed for 32 files.
- `python -m memory_seed.cli doctor` passed with the known non-fatal encoding warning.
- `git diff --check` passed.

## 2026-07-10 04:39 - Merge session fuse preview workflow to main

```yaml
entry_id: mse_9dkkddgb6zjxv4j9
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - session-fuse
  - git-workflow
  - merge
  - mcp-tools
related_entries:
  - mse_81v7vk4x5ys3k2n0
  - mse_9c151e4gbkkv1w5v
commits:
  - 878a3fdbe2a1e2b47ff8de89da8b5bde92e4c0b7
  - 16166e318c0c464f86188264a134f0e791b36753
```

### Summary

- Committed `codex-feature-session-fuse`, merged it back to `main` with a non-fast-forward merge,
  and validated the merged tree.

### Decision

- D: Merge the session-fuse CLI/MCP/skill workflow to `main` with an explicit merge commit.
- R: The user approved proceeding, the feature branch validated cleanly, and preserving branch
  topology keeps this feature visible in the Git graph.
- A: `memory-seed session fuse --branch codex-feature-session-fuse --base main` was dry-run before
  merge, but it blocked because this bootstrap branch also contained earlier main-tree session
  repairs and migrated legacy pre-schema entries that do not satisfy the future branch-local fuse
  contract. For this feature's own merge, direct Git merge plus full validation was the safer route.
- F: `main`, `codex-feature-session-fuse`, merge commit
  `16166e318c0c464f86188264a134f0e791b36753`,
  `.memory-seed/sessions/2026-07/2026-07-10.md`,
  `.memory-seed/sessions/diagrams/2026-07/2026-07-10.md`.
- T: `python -m unittest discover -s tests` passed for 303 tests;
  `python -m memory_seed.cli links check` passed for 32 files;
  `python -m memory_seed.cli doctor` passed with the known non-fatal encoding warning;
  `git diff --check` passed.

## 2026-07-10 05:10 - Add authoring-loop MCP tools (link suggest/show, session target)

```yaml
entry_id: mse_jpfs9yy19bznh18y
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-mcp-authoring-tools
topics:
  - mcp-tools
  - cli-audit
  - related-entries
  - session-authoring
related_entries:
  - mse_9dkkddgb6zjxv4j9
  - mse_81v7vk4x5ys3k2n0
  - mse_ay5tctkhx0218sn9
```

### Summary

- Audited the CLI and MCP surfaces, then closed the identified gap: the retrieval loop
  (`memory_search` -> `memory_get_chunk`) was complete but the authoring loop was not, so the LLM
  had to text-scrape the CLI to fill `related_entries` and find its session-log append target.

### Decisions

#### D1 - Add three read-only authoring-support MCP tools

- D: Add `memory_link_suggest`, `memory_link_show`, and `memory_session_target` to `mcp_server.py`,
  wrapping existing `suggest_related_entries` / `build_related_entry_graph` / `session_target`.
- R: These are read-only, reuse the same ranking/graph engine as search, and serve the highest-
  frequency LLM authoring task (append a linked entry every turn); the plumbing (`entry_id`,
  `related_entries` already flow through search results) was already there.
- A: Rejected a session-*write* MCP tool — append-only immutability and human-gated apply keep
  session writing on the CLI/direct-file path. `memory_session_target` is `create=False` and never
  writes (the user chose option a: a read-only target-discovery tool over relying on the hook).
- F: `memory_seed/mcp_server.py`, `tests/test_mcp_server.py`.

#### D2 - Home the tools in history_retrieval.md, not agent_collaboration.md

- D: Document the three tools under an "Authoring-Support Tools" section in `history_retrieval.md`
  (live + seed) and add a pointer from `session_logging.md` where `related_entries` is authored.
- R: `agent_collaboration.md` owns branch/fuse coordination; these are read-oriented memory tools,
  so they belong with `memory_search`/`memory_get_chunk`. The registry already routes retrieval MCP
  usage there.
- F: `.memory-seed/skills/history_retrieval.md`, `.memory-seed/skills/session_logging.md`,
  `memory_seed/seed/.memory-seed/skills/history_retrieval.md`,
  `memory_seed/seed/.memory-seed/skills/session_logging.md`.

#### D3 - Fix the CLI surface findings from the audit

- D: Relabel the `session` group help (it said "inspect session targets" but owns the write-capable
  `fuse` subcommand) and cross-reference the two easily-confused `migrate` subcommands.
- R: CLI-audit findings on their own terms, independent of the MCP work: a verb/scope mismatch and
  a naming-collision discoverability gap.
- F: `memory_seed/cli.py`.

### Validation

- `python -m unittest discover -s tests` passed for 310 tests (7 new MCP tests).
- `python -m py_compile memory_seed/mcp_server.py memory_seed/cli.py` passed.
- End-to-end `tools/list` over `--stdio` returned all 7 tools including the 3 new ones.
- `python -m memory_seed.cli links check` passed for 32 files;
  `python -m memory_seed.cli doctor` healthy with the known non-fatal encoding warning;
  `git diff --check` passed; live/seed parity confirmed for both edited skills.

## 2026-07-10 05:35 - Merge authoring-loop MCP tools to main

```yaml
entry_id: mse_6eek6qvf8cvcbjw6
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - git-workflow
  - merge
  - mcp-tools
related_entries:
  - mse_jpfs9yy19bznh18y
  - mse_9dkkddgb6zjxv4j9
commits:
  - 78cbd5d2a4d08e33635c10b7369881ccfdf4f80f
```

### Summary

- Merged `claude-feature-mcp-authoring-tools` (2 commits: feature + registry-routing fix) into
  `main` with a non-fast-forward merge, preserving visible branch topology.

### Decision

- D: Integrate with `git merge --no-ff` after user approval rather than fast-forwarding.
- R: The repository's branch-history workflow expects completed feature work to appear as a discrete
  branch-and-merge in the Git graph; the branch validated cleanly on its own before integration.
- A: Did not push — `main` is ahead of `origin/main` by 14 commits, left for the user to publish.
- F: `main`, feature branch `claude-feature-mcp-authoring-tools`, merge commit
  `78cbd5d2a4d08e33635c10b7369881ccfdf4f80f`, `.memory-seed/sessions/2026-07/2026-07-10.md`,
  `.memory-seed/sessions/diagrams/2026-07/2026-07-10.md`.
- T: Post-merge `python -m unittest discover -s tests` passed for 310 tests;
  `python -m memory_seed.cli links check` passed for 32 files;
  `python -m memory_seed.cli doctor` healthy with the known non-fatal encoding warning;
  `git diff --check` passed.

## 2026-07-10 05:52 - Correction: skipped fuse preview before merge; fuse unusable on legacy corpus

```yaml
entry_id: mse_dgrgwwf5n9pd082t
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - session-fuse
  - git-workflow
  - process-correction
  - windows-encoding
related_entries:
  - mse_6eek6qvf8cvcbjw6
  - mse_9dkkddgb6zjxv4j9
  - mse_jpfs9yy19bznh18y
```

### Summary

- Correction to the prior merge entry: the branch-local session entry and diagram sidecar were
  brought into `main` by a plain `git merge --no-ff`, and I did **not** run the recommended
  `session fuse` preview (MCP `memory_session_fuse_preview` or the CLI) before promotion. The new
  authoring-loop MCP tools were exercised only as in-process `call_tool` smoke tests, not through the
  MCP server to capture session memory.

### Decision

- D: Record the skipped fuse-preview step, and record what the retroactive dry-run actually revealed,
  so the "harden or not" decision rests on evidence rather than on the abstract workflow rule.
- R: `agent_collaboration.md` says to dry-run `session fuse --branch <branch>` when branch-local
  entries or sidecars exist; my branch had both, so the step was technically owed.
- A: The retroactive `session fuse --branch claude-feature-mcp-authoring-tools --base main` **blocked
  on unrelated legacy pre-schema entries** in `.memory-seed/sessions/2026-05/*.md` that carry no
  `entry_id` (migrated from `.AGENTS/`), not on the branch delta — the same reason the prior
  session-fuse feature merge (`mse_9dkkddgb6zjxv4j9`) fell back to a direct Git merge. So running the
  step first would not have changed the outcome: fuse cannot currently pass on this repo's own
  corpus, and I would have fallen back to `git merge` regardless.
- A: The dry-run also surfaced a **Windows correctness bug**: fuse's git-subprocess reader threads
  raise `UnicodeDecodeError: 'charmap' codec can't decode byte 0x81` (git output decoded as cp1252
  instead of utf-8). The command still printed a block result, but subprocess output may be silently
  lost, so fuse is effectively unreliable on Windows for any non-ASCII session content.
- A: No integrity harm from the skip: the append-only entry landed in the correct grouped path with
  no merge conflict, and `links check` (the deterministic post-merge gate) passed for 32 files.
- F: `.memory-seed/sessions/2026-07/2026-07-10.md`.
- T: `python -m memory_seed.cli session fuse --branch claude-feature-mcp-authoring-tools --base main`
  blocked on legacy 2026-05 no-`entry_id` entries (with cp1252 reader-thread tracebacks);
  `python -m memory_seed.cli links check` passed for 32 files.

## 2026-07-10 06:20 - Fix session fuse Windows encoding and corpus-scope bugs

```yaml
entry_id: mse_azn6bejpd9xpmh3f
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-fix-session-fuse
topics:
  - session-fuse
  - windows-encoding
  - git-workflow
  - bugfix
related_entries:
  - mse_dgrgwwf5n9pd082t
  - mse_9dkkddgb6zjxv4j9
  - mse_81v7vk4x5ys3k2n0
```

### Summary

- Fixed the two `session fuse` defects found in `mse_dgrgwwf5n9pd082t`: the Windows cp1252 decode
  crash and the whole-corpus validation that blocked on unchanged legacy entries. Researched with a
  read-only subagent, planned with advisor, implemented, and independently reviewed by a second
  subagent before merge.

### Decisions

#### D1 - Strict UTF-8 decode in the git subprocess helpers

- D: Add `encoding="utf-8"` to `_git_text`, `_git_show_text`, and `find_trailer_commits`, and catch
  `UnicodeDecodeError` alongside `OSError`/`TimeoutExpired`.
- R: `subprocess.run(text=True)` without `encoding=` uses the Windows locale (cp1252), which crashed
  on non-ASCII session content; strict UTF-8 matches the codebase's `CANONICAL_ENCODING` convention,
  and the catch degrades a genuinely-corrupt file to the existing "unreadable → skip" path instead
  of crashing (mirrors `_merge_head_commits`).
- A: Chose strict over `errors="replace"` because the repo reserves `replace` for console stdio only;
  silent substitution would corrupt text that fuse compares for immutability and writes back.
- F: `memory_seed/core.py`.

#### D2 - Scope branch-side validation to the branch delta

- D: Add `_changed_session_paths()` using three-dot `git diff --name-only <base>...<branch>`, an
  `only_paths=` filter on `_entry_records_from_ref`/`_sidecar_records_from_ref`, and apply it to the
  branch-side enumeration in `session_fuse`; base-side stays full.
- R: The branch commit contains the entire base corpus, so whole-tree enumeration flagged unchanged
  legacy pre-schema files (no `entry_id`) as branch problems. "Branch-local" means files the branch
  changed. Three-dot (not two-dot) avoids surfacing base's post-divergence appends to shared dated
  files. Base-side must stay full for already-present dedup and sidecar-parent-on-base lookups.
  (User chose the changed-files approach over per-entry already-present skipping.)
- A: `_changed_session_paths` returns `None` (not an empty set) on git failure, and `session_fuse`
  blocks with an issue — a review finding: an empty set would silently filter out every branch entry
  and report success importing nothing.
- F: `memory_seed/core.py`, `tests/test_memory_seed.py`.

### Known residual

- A genuinely non-UTF-8 branch session file is still silently skipped (`_git_show_text` → `None` →
  `continue`), matching the pre-existing "unreadable → skip" behavior and guarded by the UTF-8 write
  policy plus `encoding check`/`doctor`. Surfacing it as a fuse issue would require threading a signal
  through helpers shared by both enumeration sides; deferred as out of scope.

### Validation

- `python -m unittest discover -s tests` passed for 313 tests (3 new fuse regression tests:
  unchanged-legacy-not-blocking, non-ASCII body round-trip under apply, git-diff-failure surfaces
  an issue).
- **Windows before/after**: the same `session fuse --branch ... --base main` that produced cp1252
  tracebacks + a 2026-05 block in `mse_dgrgwwf5n9pd082t` now returns "No branch session entries or
  sidecars need fusing", exit 0, no tracebacks.
- `python -m memory_seed.cli links check` passed for 32 files;
  `python -m memory_seed.cli doctor` healthy with the known non-fatal encoding warning;
  `git diff --check` passed.
- Two subagents used: read-only research (sonnet) for the fix approach + convention scan, and an
  independent review (opus) of the diff whose findings D2's git-failure guard and a strengthened
  non-ASCII test address.

## 2026-07-10 06:40 - Merge session fuse fixes to main

```yaml
entry_id: mse_8c5e4m97szmfj8tp
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - session-fuse
  - git-workflow
  - merge
related_entries:
  - mse_azn6bejpd9xpmh3f
  - mse_dgrgwwf5n9pd082t
commits:
  - c52237946835c4b38badee6b93830489d1c7fc44
```

### Summary

- Merged `claude-fix-session-fuse` into `main` with a non-fast-forward merge after user approval,
  preserving branch topology.

### Decision

- D: Integrate with `git merge --no-ff` and validate the merged tree.
- R: Branch-history workflow expects discrete branch-and-merge topology; the fix branch validated
  cleanly and an independent review pass was addressed before merge.
- A: Did not push — `main` is ahead of `origin/main` by 18 commits, left for the user to publish.
- F: `main`, feature branch `claude-fix-session-fuse`, merge commit
  `c52237946835c4b38badee6b93830489d1c7fc44`.
- T: Post-merge `python -m unittest discover -s tests` passed for 313 tests;
  `python -m memory_seed.cli session fuse --branch claude-fix-session-fuse --base main` returned
  exit 0 with no branch entries to fuse (post-merge ancestor → empty delta) and no tracebacks;
  `python -m memory_seed.cli links check` passed for 32 files; `doctor` healthy;
  `git diff --check` passed.

## 2026-07-10 07:15 - Triage dev-tools reel: adopt Fontjoy, reference fffuel and 21st.dev

```yaml
entry_id: mse_wsp03dkgg693vhdm
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - proposal-lifecycle
  - memory-trace-ui
  - licensing
  - tooling-evaluation
```

### Summary

- Transcribed/reviewed an Instagram Reel (`docs/1_Inbox/dev-tools-reel-10of10-websites.md`) that
  featured 5 dev tools, then researched licensing + stack-fit for the three the user picked
  (21st.dev, fffuel, Fontjoy), drafted a proposal each, and placed them per the user's positioning
  decisions. Web research via WebSearch/WebFetch (no subagents — not requested).

### Decisions

#### D1 - Fontjoy: adopt (self-host fonts)

- D: Promote to `docs/2_Todo/fontjoy-typography-pairing.md`; choose a pairing and self-host the fonts
  in Memory Trace static assets.
- R: Legally clean (Google Fonts OFL 1.1 / Apache 2.0, free commercial), stack-agnostic (a
  `@font-face`, no framework needed), and fills Trace's missing typographic layer.
- A: Self-host rather than the Google Fonts CDN — Trace is a local/privacy tool and the CDN leaks the
  viewer IP (2022 German GDPR ruling); OFL/Apache permit bundling the files.
- F: `docs/2_Todo/fontjoy-typography-pairing.md`, `docs/2_Todo/0_NEXT_STEPS.md`.

#### D2 - fffuel: reference-only

- D: Move to `docs/4_Reference/fffuel-svg-css-generators.md`; use as inspiration, do not commit
  generated SVGs to the public repo.
- R: fffuel's license forbids redistributing "the Images"; committing a generated SVG into a public
  OSS repo is arguably redistribution. User chose reference-only over the CSS-only guardrail.
- F: `docs/4_Reference/fffuel-svg-css-generators.md`.

#### D3 - 21st.dev: reference-only

- D: Move to `docs/4_Reference/21st-dev-components.md`; browse for ideas, hand-implement in vanilla CSS.
- R: MIT (legally fine) but the components are React + Tailwind + Radix, while Memory Trace is a
  hand-written vanilla-JS static app — no clean drop-in without re-platforming Trace, which is
  disproportionate to its lean stack.
- F: `docs/4_Reference/21st-dev-components.md`.

### Notes

- Still un-triaged from the reel: 10x (idea→iOS app; likely out of scope) and Rive (interactive
  animations; needs a runtime-player call).
- Doc changes are placed but not yet committed; commit/push deferred to the user per this session's
  posture.
- T: `python -m memory_seed.cli links check` passed; `python -m memory_seed.cli doctor` healthy.

## 2026-07-10 07:45 - Audit 2-day decision capture in CHANGELOG/README/audit; fix 2 gaps

```yaml
entry_id: mse_0n9nwq9qj6dp493f
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-fix-changelog-audit-gaps
topics:
  - documentation
  - changelog
  - functionality-audit
  - audit
related_entries:
  - mse_y3gb93k466tcfdq8
  - mse_m6amd5db4c7s8sfw
  - mse_jpfs9yy19bznh18y
```

### Summary

- Cross-checked every decision from the 2026-07-09/2026-07-10 session logs (18 entries, 5 diagram
  sidecars) against CHANGELOG.md, README.md, and docs/3_Spec/functionality-audit.md. Found and fixed
  two real gaps; confirmed the rest landed correctly, including one entry (authoring-loop MCP tools,
  `mse_jpfs9yy19bznh18y`) whose own `F:` fields under-cited the doc work it actually did.

### Decisions

#### D1 - Fix: branch-history guardrail missing from CHANGELOG

- D: Add a CHANGELOG bullet for `memory-seed branch status [--json]`.
- R: The command shipped 07-09 (`mse_y3gb93k466tcfdq8`), is fully documented in README and the
  audit, but that entry's `F:` list never named CHANGELOG.md and no later entry added it either —
  confirmed by grep across the Unreleased section (zero hits for "branch status"/"branch-history").
- F: `CHANGELOG.md`.

#### D2 - Fix: Decision Harvest + sidecar positive-trigger tightening missing/stale

- D: Add a CHANGELOG bullet, and rewrite the audit's decision-diagram-sidecar bullet (docs/3_Spec/
  functionality-audit.md ~line 525) which still described the pre-tightening "optional consideration"
  framing, plus add a new Decision Harvest bullet.
- R: `mse_m6amd5db4c7s8sfw` (07-10 03:28) changed sidecar guidance from optional to a positive
  trigger for topology/migration/schema/concurrency/lifecycle/pipeline decisions and added the
  Decision Harvest step, but never claimed CHANGELOG/audit in its own `F:` fields. The audit wasn't
  merely silent — its existing sidecar bullet actively misdescribed current behavior.
- F: `CHANGELOG.md`, `docs/3_Spec/functionality-audit.md`.

### Notes (side-note: sidecar discoverability)

- Verified via code, not assumption: `memory_search` results (`ranked_to_dict`) carry no diagram
  flag, and `mcp_server.py` never passes `include_diagrams=True` to `get_chunk` — it's hardcoded off
  ("so the MCP tool contract is unchanged"). `history_retrieval.md` (the skill governing the
  search→fetch retrieval loop) never mentions diagrams/sidecars at all; all sidecar guidance lives in
  `session_logging.md`/`end_of_turn.md`, which govern *writing* entries, not reading them back.
- Consequence: an agent using only the documented MCP retrieval loop has no signal that an entry has
  a sidecar and no default path to fetch it. In this task I found the sidecars because I authored
  most of them earlier this session and the user's phrasing named them explicitly — not because the
  retrieval path surfaced them. A `has_diagram`/`diagram_count` field on `memory_search` results (or
  a documented "check `entry_diagram_sidecars()`" step in `history_retrieval.md`) would close this;
  not implemented here, flagged as a gap for a future decision.
- No sidecar for this observation despite touching the retrieval-pipeline positive trigger: no new
  flow was implemented, and the closely related existing diagram on `mse_jpfs9yy19bznh18y` already
  depicts the read/write MCP loop this gap sits inside.
- F: none (observation only, this turn).
- T: `python -m memory_seed.cli encoding check CHANGELOG.md` passed; `python -m memory_seed.cli
  encoding check docs/3_Spec/functionality-audit.md` passed; `python -m memory_seed.cli links check`
  passed for 32 files; `python -m memory_seed.cli doctor` healthy; `git diff --check` passed.

## 2026-07-10 07:50 - Backfill: place residual docs in inbox and push to origin

```yaml
entry_id: mse_pcc2sdkevarssc0x
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - session-logging
  - proposal-lifecycle
  - git-workflow
  - backfill
related_entries:
  - mse_8c5e4m97szmfj8tp
  - mse_dgrgwwf5n9pd082t
```

### Summary

- **Backfill** (written 2026-07-10 07:50 for a turn that happened between the 06:40 merge and the
  07:15 entry; no entry was written for it at the time). Per the user's request, placed the two
  cp1252/decode residual-work items as inbox markdown, then pushed `main` to `origin/main`.

### Decision

- D: Write `docs/1_Inbox/residual-fuse-non-utf8-silent-skip.md` and
  `docs/1_Inbox/residual-processes-cp1252-decode.md`, then run `git push origin main`.
- R: The user asked for residual work to be captured as inbox docs (rather than left only as
  in-conversation notes) and for the accumulated local commits to be published.
- A: The push initially succeeded (`b21394b..6d88638`), but `git status` afterward showed local
  `main` 1 commit ahead again — an unexpected commit `49b2159 "item added to inbox for review"`
  that I did not create, bundling the two residual `.md` files with the 80 MB
  `docs/1_Inbox/20260709_093402000_iOS.MP4`. Flagged this to the user rather than pushing it
  automatically, since an 80 MB binary in shared Git history is effectively permanent. User chose
  "don't push this commit" — it stayed local-only, unpushed.
- F: `docs/1_Inbox/residual-fuse-non-utf8-silent-skip.md`,
  `docs/1_Inbox/residual-processes-cp1252-decode.md`, `main`/`origin/main`.
- T: `git status -sb` confirmed `origin/main` at `6d88638` after push; `git show --stat 49b2159`
  confirmed its contents before deciding not to push it.

## 2026-07-10 07:52 - Backfill: transcribe and replace inbox video with notes

```yaml
entry_id: mse_b1as0fcx4hfq8hdj
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - document-ingestion
  - proposal-lifecycle
  - backfill
related_entries:
  - mse_pcc2sdkevarssc0x
  - mse_wsp03dkgg693vhdm
```

### Summary

- **Backfill** (written 2026-07-10 07:52 for the turn immediately preceding the 07:15 reel-triage
  entry; not logged at the time — only mentioned in passing as backstory in that later entry).
  Transcribed the inbox video and replaced it with an inbox markdown note per the user's request.

### Decision

- D: Extract/read video frames for tool identification (no local speech-to-text engine available,
  `ffmpeg` present but no `whisper`), accept the user's provided verbatim transcript as authoritative
  over my own frame OCR, write `docs/1_Inbox/dev-tools-reel-10of10-websites.md`, then delete the
  source video and remove it from Git history.
- R: The user asked to transcribe/review the video, then replace it with markdown docs covering the
  items it proposed; an 80 MB binary has no place staying in a documentation inbox once its content
  is captured as text.
- A: To delete the video cleanly, ran `git reset --mixed HEAD~1` to drop the unpushed `49b2159`
  commit (from the prior turn) while keeping its files in the working tree, then `rm` the MP4
  directly — leaving local `main` equal to `origin/main` again and the three inbox `.md` files
  (2 residuals + the new reel note) as local, uncommitted, untracked files.
- F: `docs/1_Inbox/dev-tools-reel-10of10-websites.md` (added), `docs/1_Inbox/20260709_093402000_iOS.MP4` (deleted).
- T: `git status --short` confirmed local `main` matched `origin/main` after the reset+delete;
  `ls docs/1_Inbox/` confirmed only the 3 markdown files remained.

## 2026-07-10 07:55 - Correction: two turns missed session logging

```yaml
entry_id: mse_g73xa2bdzpj41g83
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - session-logging
  - process-correction
related_entries:
  - mse_pcc2sdkevarssc0x
  - mse_b1as0fcx4hfq8hdj
```

### Summary

- User asked directly "have you been updating the session logs?" Verified against the actual file
  rather than answering from recollection, and found two turns with no entry at the time: the
  residual-docs-and-push turn and the video-transcription-and-replace turn (backfilled above as
  `mse_pcc2sdkevarssc0x` and `mse_b1as0fcx4hfq8hdj`).

### Decision

- D: Answer "no, not consistently" rather than a general "yes", back the claim with a grep/git
  verification the user can reproduce, and backfill both missing entries immediately instead of only
  promising to do better.
- R: `agent-rules.md` states deferring or batching session-log writes is a discipline failure; the
  push turn in particular was a consequential, externally-visible action (`git push` to shared
  `origin/main`) that had zero durable record beyond `git log` itself until this correction.
- A: Considered folding the missing work into the existing 07:15 entry's `related_entries` only
  (since it already mentions the video transcription in prose); rejected because that entry's own
  `D:`/`F:`/`T:` fields never covered the push, the flagged unexpected commit, or the video deletion
  — prose backstory in a later entry is not the same as a record of that turn's own decisions.
- F: `.memory-seed/sessions/2026-07/2026-07-10.md`.
- T: `python -m memory_seed.cli links check` passed; `python -m memory_seed.cli doctor` healthy.

## 2026-07-10 10:23 - Fix processes.py Windows encoding (same bug class as core.py)

```yaml
entry_id: mse_4yrmdvezk56ymaba
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-fix-processes-encoding
topics:
  - windows-encoding
  - bugfix
  - processes
related_entries:
  - mse_azn6bejpd9xpmh3f
  - mse_dgrgwwf5n9pd082t
```

### Summary

- Fixed the same Windows cp1252-locale decode bug class in `memory_seed/processes.py` that was
  already fixed for the git helpers in `core.py`: three `subprocess.run(..., text=True)` calls with
  no explicit `encoding=` (`_iter_windows_processes`, `_iter_posix_processes`,
  `_terminate_windows_process`). Advisor consulted before implementing; confirmed the three sites and
  flagged that a mocked test proves nothing about a real decode, and that PowerShell's own output
  encoding (not just the Python-side decode) needed empirical verification before trusting the fix.

### Decisions

#### D1 - Apply the core.py convention at all three sites

- D: Add `encoding="utf-8"` and catch `UnicodeDecodeError` alongside the existing
  `OSError`/`subprocess.TimeoutExpired` handling at all three sites.
- R: Matches the established convention from `mse_azn6bejpd9xpmh3f`; degrades a genuinely undecodable
  result to the existing "couldn't get a clean result → treat as empty/failed" path instead of an
  unhandled crash.
- F: `memory_seed/processes.py`.

#### D2 - Also force PowerShell's own output encoding to UTF-8

- D: Prepend `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8;` to the `_iter_windows_processes`
  PowerShell `-Command` script, in addition to the Python-side `encoding="utf-8"`.
- R: Empirically verified (real `powershell`/WMI calls, not mocked) that Windows PowerShell 5.1 does
  **not** emit UTF-8 on redirected stdout by default — `ConvertTo-Json` output for a non-ASCII name
  came back as lone OEM-codepage bytes (invalid UTF-8) for Latin-adjacent characters and literal `?`
  (irrecoverable data loss) for non-Latin script. Adding only `encoding="utf-8"` on the Python side
  would turn that into a `UnicodeDecodeError` for the *entire* JSON payload (all processes system-wide,
  not just the affected one) whenever any single running process anywhere on the system has a
  non-ASCII name/path — worse than the pre-fix mojibake-but-no-crash behavior. Setting
  `OutputEncoding` first makes PowerShell itself emit real UTF-8 bytes, which then round-trips
  correctly through `encoding="utf-8"`.
- A: Considered leaving this as a Python-side-only fix per the literal task instructions, but the
  verification showed that would be a net regression for this specific site (total data loss vs.
  partial mojibake), not a like-for-like port of the core.py convention — core.py's fix is complete
  only because git itself already guarantees UTF-8 output; PowerShell 5.1 does not, so it needed the
  producer-side fix too.
- F: `memory_seed/processes.py`.

#### D3 - Test with mocked wiring checks plus a real Windows integration test

- D: Add mock-based tests asserting `encoding="utf-8"` is passed and that a raised
  `UnicodeDecodeError` degrades gracefully (`[]`/`False`) at all three sites, plus a
  `sys.platform == "win32"`-gated integration test that spawns a real child process with a non-ASCII
  argv entry and reads it back through the real (non-mocked) `_iter_windows_processes()`.
- R: A mocked `str` stdout return never exercises the actual bytes→str decode, so it can't prove the
  encoding fix works — only a real subprocess round-trip can, and that's only possible on Windows.
  The mock tests still earn their keep by proving the wiring (the right kwarg is passed) and the new
  failure-handling branch.
- A: Left `taskkill`'s narrow residual risk undocumented-in-code but noted here: taskkill's own output
  isn't produced with a forced UTF-8 encoding (no equivalent `OutputEncoding` override applied), so a
  non-ASCII exe path or non-English Windows locale message could still raise `UnicodeDecodeError` and
  make `_terminate_windows_process` report `False` for an already-successful kill. Not fixed because
  the caller never reads the decoded text (only `returncode`), the risk is a misleading CLI message
  rather than a destructive or silent-failure outcome, and `terminate_processes()`'s own
  `remaining_processes()` re-check is the actual source of truth for whether a process is still alive.
- F: `tests/test_processes.py`.

### Validation

- `python -m unittest discover -s tests` passed for 319 tests (6 new: 5 mock-based wiring/failure
  tests + 1 real Windows integration test).
- Manual verification (not part of the automated suite): raw-byte capture of real `powershell |
  ConvertTo-Json` output for a `café-процесс.exe` name confirmed the pre-fix OEM-codepage mismatch,
  and confirmed `[Console]::OutputEncoding = UTF8` fixes it end-to-end (`json.loads` round-trip
  matched the original string exactly).
- `python -m py_compile memory_seed/processes.py tests/test_processes.py` passed.
- `python -m memory_seed.cli links check` passed for 32 files.
- `python -m memory_seed.cli doctor` healthy with the known non-fatal encoding warning (4 pre-existing
  CRLF issues in config files, unrelated to this change).
- `git diff --check` passed.

## 2026-07-10 10:33 - Merge processes.py encoding fix to main

```yaml
entry_id: mse_shsk32hbrd064ckp
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - git-workflow
  - merge
  - windows-encoding
related_entries:
  - mse_4yrmdvezk56ymaba
```

### Summary

- Merged `claude-fix-processes-encoding` into `main` with a non-fast-forward merge commit, created
  without a checkout because `main` was already checked out live in another worktree with its own
  uncommitted, unrelated changes.

### Decision

- D: Build the merge commit with `git merge-tree --write-tree` (compute the merge in-memory) +
  `git commit-tree` (two parents: `main` and the task branch) + `git update-ref refs/heads/main`,
  instead of `git checkout main && git merge --no-ff`.
- R: `git checkout main` failed here (`'main' is already used by worktree at ...`) because this repo
  had a second live worktree with `main` checked out and genuine uncommitted edits in unrelated files
  (`docs/2_Todo/0_NEXT_STEPS.md`, new inbox docs) — switching branches there or forcing a checkout
  in this worktree both risked disturbing that concurrent session's state. The plumbing route
  produces the same merge-commit shape (two parents, `--no-ff`-equivalent) without touching that
  worktree's working directory or index.
- A: A first attempt verified the merged tree in a deeply-nested scratch worktree under this
  session's own temp path and got 85 spurious test failures/errors; root cause was Windows MAX_PATH
  truncation during checkout (a seed file silently failed to materialize), not a real regression. Redid
  verification in a short-path worktree (`C:/ms-verify`) where all 319 tests, `links check`, `doctor`,
  and `git diff --check` passed cleanly — confirming the first run was a verification-environment
  artifact, not a code problem.
- A: `git merge-tree`'s automatic text merge of this append-only session log placed my branch's newest
  entry (10:23) *before* three chronologically-earlier entries main had independently appended
  (07:15-07:55), because both sides inserted at/near the same location and the merge has no
  timestamp awareness. Caught by inspecting the merged file directly (not by any automated check —
  neither `links check` nor `doctor` validates chronological order); manually reordered the merged
  session file so entries run in true ascending timestamp order before finalizing the merge commit,
  restoring the append-only chronology invariant `session_logging.md` requires.
- F: `main`, feature branch `claude-fix-processes-encoding`, merge commit
  `c6abdfe6b4c9454b74c323e5bd41f9160c3decdb` (session file content corrected in a follow-up commit
  on `main` immediately after, on top of the merge, rather than editing the merge commit itself).
- T: Full suite (319 tests), `links check` (32 files), `doctor`, and `git diff --check` all passed on
  the verified merge tree (see A above); did not push — left for the user to publish alongside the
  session's other unpushed local merges.

### Follow-up

- No automated tooling currently catches out-of-order session entries produced by a text-level git
  merge of this file; this was caught by manual inspection. Worth considering a `links check` addition
  that verifies ascending timestamp order across a whole session file, since two independent branches
  appending to the same day's file is an increasingly common pattern in this repo's own workflow.

## 2026-07-10 11:15 - Harden session-log-check.py: escalation state + language

```yaml
entry_id: mse_ynmnn99kgmqmec5r
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-session-log-hook-hardening
topics:
  - session-logging
  - hooks
  - windows-worktree-hazard
related_entries:
  - mse_g73xa2bdzpj41g83
  - mse_4yrmdvezk56ymaba
```

### Summary

- Followed up on the missed-logging correction by hardening `session-log-check.py` (live + seed):
  escalating repeated-miss state and tightened reminder language. Also caught and recovered from a
  shared-worktree desync with a concurrently running background task before it could have silently
  reverted that task's already-merged fix.

### Decisions

#### D1 - Add escalation state instead of a hard block

- D: Track consecutive stale checks (with no new entry appearing in between) in a gitignored
  `.memory-seed/.session-log-check-state`; escalate wording on the 2nd+ consecutive miss rather than
  repeating the same message forever.
- R: The plain 15-minute staleness check is anchored to the last logged entry's own timestamp, so
  it's immune to turn frequency and reliably fires once 15 real minutes pass - my earlier "many quick
  turns defeat it" theory to the user did not hold up under direct testing (see Correction below). The
  real gap is that a fired reminder has no way to know if it was ignored.
- A: Rejected converting `Stop` into a hard-blocking hook — that reverses a deliberate prior design
  choice ("no blocking Stop hook — evolution needs reasoning + approval") the user didn't ask to
  change; escalating language stays within the existing nudge-only contract while making a repeated
  miss much harder to silently pass over.
- F: `.memory-seed/hooks/session-log-check.py`, `memory_seed/seed/.memory-seed/hooks/session-log-check.py`,
  `.gitignore`.

#### D2 - Tighten reminder language

- D: Rewrite both the base and escalated reminder text: lead with the imperative, enumerate concrete
  triggers (file changes, `git push`/`merge`/`rebase`/delete, any decision), and state D/R as required
  on every entry rather than "for decisions, use DRAFT labels" (which read as if some entries don't
  need D/R, when the DRAFT record's D/R are the mandatory baseline shape for virtually all entries).
- R: The exact miss this hardens against was a `git push` to shared `origin/main` going unlogged; the
  old wording's soft "if you completed meaningful work" framing gave room to rationalize real work
  away, and never named git operations specifically despite them being the highest-stakes case.
- F: same as D1.

### Correction (self-correction, not a new miss)

- Earlier in this conversation I told the user a dense run of short turns could keep the 15-minute
  check "correctly silent turn after turn... because each new turn starts less than 15 minutes after
  the prior turn's entry." Directly testing the check's math (`latest < now() - 15min`, anchored to a
  fixed timestamp) shows this is wrong: elapsed time since the last entry grows regardless of turn
  count, so it always eventually crosses 15 minutes and fires. Said so to the user before finalizing
  the design, and scoped D1 to the failure mode the evidence actually supports (reminder fired,
  outcome unknown) rather than the incorrect one.

### Incident: shared-worktree desync recovered

- While creating this task branch, `git status` showed `memory_seed/processes.py`,
  `tests/test_processes.py`, and today's session log as staged-modified even though I had never
  touched them. Root cause: a background task (`task_56a17f30`, "Fix cp1252 decode risk in
  processes.py") was running concurrently in a sibling worktree
  (`.claude/worktrees/zealous-blackwell-63af13`, branch `claude-fix-processes-encoding`) and merged
  its own completed work into the shared `main` ref. My primary worktree's HEAD followed that ref
  update (`git switch -c` picked up the new `main` tip, `c6abdfe`), but the working tree/index files
  did not resync, leaving them stale relative to the ref they claimed to be on.
- D: Verify ground truth (`git show HEAD:<path>` vs. working tree) before any git operation, then run
  `git checkout HEAD -- <paths>` to resync the 3 affected files to my own branch's HEAD (which already
  had the background task's real, correctly-merged work) rather than editing/discarding anything.
- R: `HEAD` (`c6abdfe`) already contained the background task's completed, tested, merged fix
  (confirmed via `git show HEAD:memory_seed/processes.py`); the desync was purely a stale
  working-tree/index artifact of the concurrent ref update, not a real edit conflict — restoring to my
  own HEAD was safe and lost nothing.
- A: Had I not caught this and later committed the stale state, I would have silently reverted the
  background task's already-merged fix and corrupted its session log entry out of the shared file.
- F: `memory_seed/processes.py`, `tests/test_processes.py`, `.memory-seed/sessions/2026-07/2026-07-10.md`
  (all restored to HEAD, none edited).
- T: Post-restore `python -m unittest discover -s tests` passed 319 tests; `grep -c "encoding="
  memory_seed/processes.py` and the session-log heading count both matched HEAD's committed content.

### Follow-ups noted, not actioned this turn

- The background task's merge (`c6abdfe`) landed its own session entry ("Fix processes.py Windows
  encoding", timestamped 10:23) in a physical file position *before* several of my earlier same-day
  entries (07:15–07:55), which are chronologically earlier but appear later in the file — a real
  `SESSION LOG ORDER WARNING` condition in the shared file right now. Not fixed here: it belongs to
  the other task's merge, not mine to rewrite mid-flight without asking.
- `.identity-offer-stamp` and `.retrieval-reminder-stamp` are, like the new state file, not actually
  gitignore-managed by `init`/`update` for fresh projects — only present in this repo's own root
  `.gitignore` for dogfooding. Left unwired for all three; flagged as an optional follow-up rather than
  expanding this task's scope.

### Validation

- `python -m unittest tests.test_memory_seed.SessionLogOrderingHookTests -v` passed 14 tests (8
  existing + 6 new: base-vs-escalated wording, count climbs across repeats, reset on new entry, state
  file path/shape, fail-open on corrupt state).
- `python -m unittest discover -s tests` passed 325 tests.
- Manual escalation ladder verified in an isolated fixture: 3 consecutive stale runs produced tier-1
  then tier-2 (count=2) then tier-2 (count=3) wording; appending a fresh entry produced a silent,
  exit-0 run with `consecutive_misses` reset to 0 in the state file.
- `--codex` output shape verified (systemMessage + continue:true, correctly escalated).
- `python -m memory_seed.cli links check` passed for 32 files; `python -m memory_seed.cli doctor`
  healthy; `git diff --check` passed (pre-existing non-fatal `.gitignore` CRLF warning only); live/seed
  hook files byte-identical.

## 2026-07-10 12:03 - Add session merge-branch one-step integration command

```yaml
entry_id: mse_dr5eprnhrctqeeg3
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-session-merge-branch
topics:
  - session-fuse
  - git-workflow
  - session-logging
  - cli
related_entries:
  - mse_9c151e4gbkkv1w5v
  - mse_dgrgwwf5n9pd082t
  - mse_81v7vk4x5ys3k2n0
```

### Summary

- Implemented `memory-seed session merge-branch --branch <branch> [--dry-run]` per the approved
  plan: one command that dry-runs the fuse, merges `--no-ff --no-commit`, resets branch-touched
  session paths to base, applies the fuse, stages, and commits - so branch session entries always
  land in timestamp order without a separate manual fuse step to forget.

### Decisions

#### D1 - Wrapper command, not a merge driver or blocking hook

- D: Automate the ordering fix as an explicit CLI wrapper around the existing `session_fuse()`
  primitives, chosen by the user over a git merge driver and over an enforcement-only pre-commit
  gate.
- R: Twice today the manual dry-run/apply dance was skipped and raw git line-merges landed session
  entries out of chronological order; the machinery to sort deterministically already existed in
  `session_fuse()` - the gap was purely choreography. A merge driver would reverse the recorded
  design decision (mse_9c151e4gbkkv1w5v) that fuse stays an explicit Memory Seed-aware command.
- A: The user's initial framing was an asynchronous write job that deposits orchestrator/subagent
  entries programmatically by date; the wrapper achieves the same "agent never orders entries by
  hand" outcome at merge time while staying inside the explicit-command architecture.
- F: `memory_seed/core.py` (SessionMergeBranchResult, `session_merge_branch()`), `memory_seed/cli.py`.
- T: 7 new tests in `tests/test_memory_seed.py`; full suite 332 passing.

#### D2 - Fail-closed semantics and base-content reset

- D: Fuse issues abort before any merge state exists; non-session conflicts leave the merge in
  progress (never `merge --abort`); before the fuse apply, every branch-touched session path that
  exists on base is reset to base's committed content via `git checkout <base> -- <path>`; merge
  outcome is classified by MERGE_HEAD presence, not exit code; a clean working tree is required and
  refusals name the dirty paths.
- R: `session_fuse`'s working-tree read is a naive text parse with no conflict-marker awareness, so
  it must never see a conflicted file; the reset also undoes silent git auto-merges that land
  entries by physical position without any conflict - the exact bug that motivated this. Exit code
  1 from `git merge` is ambiguous between conflict and real failure.
- A: Rejected re-deriving conflicted-path correspondence against fuse target paths; classifying
  conflicted paths with the same `_session_doc_from_relative_path`/`_diagram_doc_from_relative_path`
  parsers fuse itself uses avoids a second path-normalization implementation.
- F: `memory_seed/core.py`, `.memory-seed/skills/agent_collaboration.md` (+ seed mirror,
  byte-identical), `README.md`, `docs/3_Spec/functionality-audit.md`, `.memory-seed/index.md`,
  `CHANGELOG.md`.
- T: covered end-to-end: clean merge commits with 2 parents; non-session conflict left in
  progress; divergent same-file appends land chronologically (09:00 < 09:30 < 10:00); modified
  existing entry blocks with MERGE_HEAD never created; dirty tree refused naming paths; CLI
  dry-run smoke test.

## 2026-07-10 12:34 - Draft evolution-edges proposal (evolves/evolved_by)

```yaml
entry_id: mse_t22wn997q5005zjp
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - graph
  - session-logging
  - proposal
related_entries:
  - mse_dr5eprnhrctqeeg3
  - mse_0n9nwq9qj6dp493f
```

### Summary

- User observed that the `session merge-branch` work evolved the fuse workflow yet the history
  carries no typed lifecycle signal, and asked for a way to identify entries that evolve or
  completely remove earlier features. Drafted `docs/2_Todo/evolution-edges-plan.md` (user chose
  proposal-first over immediate implementation).

### Decisions

#### D1 - Propose a typed `evolves` edge rather than stretching `supersedes`

- D: Draft a proposal for a fourth authored edge kind `evolves:` with computed `evolved_by`
  inverse, forward-only with the same four guards as `supersedes`, and explicitly **no** importance
  dampener on the target.
- R: `supersedes` means retired (x0.25 dampener, `exclude_superseded`), so using it for evolution
  would falsely deprioritize still-load-bearing decisions - which is precisely why
  `mse_dr5eprnhrctqeeg3` correctly avoided it and the evolution became invisible as untyped
  `related_entries`. The missing semantic is freshness-without-retirement.
- A: Rejected a fifth `removes:` edge - removal keeps `supersedes` semantics (identical retrieval
  meaning: do not rely on this), with the remover entry's D/R telling removal from replacement;
  keeps the contract at four kinds per the anti-edge-explosion lesson. Rejected qualifying
  `supersedes` with a mode field (complicates the flat-list schema and muddies the dampener rule).
- F: `docs/2_Todo/evolution-edges-plan.md`, `docs/2_Todo/0_NEXT_STEPS.md`.

#### D2 - Add a Decision Harvest lifecycle prompt

- D: The proposal includes one harvest-step question - does any harvested decision replace, remove,
  or evolve an earlier entry? - routing to `supersedes`/`evolves`/`related_entries` respectively.
- R: `supersedes` has zero uses in the corpus since shipping 2026-07-03; the mechanism exists but
  nothing triggers the question at write time, so the gap is behavioral as much as schematic.
- T: None yet - proposal only; implementation gated on user approval.

## 2026-07-10 12:38 - Constraint: evolved_by is read-time inferred only, never stored

```yaml
entry_id: mse_4670mpw532yec14w
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - graph
  - append-only
  - proposal
related_entries:
  - mse_t22wn997q5005zjp
```

### Decision

- D: Per user direction, the evolution-edges proposal now states as a hard constraint that
  `evolved_by` exists only in the derived read layer (canonical graph reader consumed by
  CLI/MCP/Trace) and is never written to any entry file by anyone - declaring `evolves:` on a new
  entry changes zero bytes of the target. Added an `authored-inverse-field` `links check` guard to
  the proposal scope so a stored `evolved_by:`/`superseded_by:` key becomes a named integrity
  error rather than being silently ignored.
- R: Entries must remain append-only; an inverse edge materialized into old files would be a
  retroactive edit of history. Enforcement belongs in the single validator, not in guidance prose
  alone - today a hand-written `superseded_by:` is merely ignored, which leaves the contract
  unguarded.
- F: `docs/2_Todo/evolution-edges-plan.md`.

## 2026-07-10 12:48 - F-file overlap as rarity-weighted lifecycle ranking signal

```yaml
entry_id: mse_rnjcg0d1k8a2a9gn
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - graph
  - retrieval
  - proposal
related_entries:
  - mse_t22wn997q5005zjp
  - mse_4670mpw532yec14w
```

### Decision

- D: Added D5 to the evolution-edges proposal per user direction: `F:` file-path overlap between
  entries is a first-class precision signal when ranking `evolves`/`supersedes` candidates in
  `link suggest`/`memory_link_suggest`, with four binding constraints - boost-never-gate (F is
  optional), rarity-weighted (hub files like CHANGELOG/README/session logs are stopword-like and
  contribute ~nothing), conservative backtick-only path extraction (plus an authoring nudge to
  backtick-quote F paths), and suggest-only with shared-file evidence shown, never machine-written
  edges.
- R: User's instinct - semantically similar decisions touching the same files are
  disproportionately likely to be lifecycle-linked - is correct as a precision signal (matches
  co-change/logical-coupling practice, and today's merge-branch/fuse F-overlap is the live
  example). Pushback accepted into the design: raw overlap without rarity weighting would be
  dominated by hub files; F is optional and free-text so it cannot gate; and refactors/renames
  break file continuity at exactly the biggest evolution moments, so semantic similarity stays the
  recall channel.
- A: Rejected making file overlap a filter or an auto-edge-writer - both would violate the
  expose-before-you-rank rule and the append-only authored-edge contract.
- F: `docs/2_Todo/evolution-edges-plan.md`.

## 2026-07-10 12:55 - Core continuity topics (rename/migration/removal) as D6

```yaml
entry_id: mse_hffa651fqxchm9hx
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - graph
  - topics
  - proposal
related_entries:
  - mse_rnjcg0d1k8a2a9gn
  - mse_t22wn997q5005zjp
```

### Decision

- D: Added D6 to the evolution-edges proposal per user exploration request: three core continuity
  topics (`rename`, `migration`, `removal`) seeded into every project's `topics.yaml`
  (delivery rides the accepted topics plan), applied only when the event occurs, marking the
  artifact-continuity breaks that blind both file-overlap and semantic traceability. The bridge is
  a convention, not an algorithm: rename/migration entries list both old and new paths
  backtick-quoted in `F:`, so plain D5 overlap routes through them (past shares old paths,
  future shares new paths) with zero extra code. Harvest prompt extends by one continuity clause.
  Cross-referenced from the topics plan's starter-vocabulary design detail.
- R: A rename breaks both traceability channels at once - paths change (D5 blind spot) and
  vocabulary drifts (this repo's Explorer -> Lense -> Memory Trace history is the live example) -
  so authored waypoint entries are the cheapest reliable bridge. Git rename detection cannot
  substitute: heuristic, banned from the hot read path, and blind to concept renames.
- A: Corrected the "always present" framing - core topics live always in the *vocabulary*, never
  on every entry (a topic on every entry is a stopword, same rarity lesson as D5 hub files).
  Fixed the set at three: `deprecation`/`revert` fold into removal/migration; `refactor` is
  too judgment-dependent to apply consistently. `removal` complements rather than duplicates the
  D3 supersedes edge: edge = per-target status, topic = browsable timeline. Explicit multi-hop
  bridging in link suggest deferred until fixtures show the convention insufficient.
- F: `docs/2_Todo/evolution-edges-plan.md`, `docs/2_Todo/memory-trace-topic-neighbourhoods-plan.md`.

## 2026-07-10 13:03 - Replace core topics with structured continuity field

```yaml
entry_id: mse_ft7874jfnxxdevrz
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - graph
  - schema
  - proposal
related_entries:
  - mse_hffa651fqxchm9hx
  - mse_rnjcg0d1k8a2a9gn
```

### Decision

- D: Rewrote the evolution-edges proposal's D6 per user review: rename/migration/removal events
  are recorded as a structured optional entry-YAML field `continuity:` (items of
  kind/from/to; `to` forbidden on removal) instead of seeded core topics plus an `F:`
  both-paths prose convention. Values are historical labels like `branch:` (never validated
  against live filesystem/git). `links check` gains `malformed-continuity`; `link suggest`
  builds a transitive old->new alias table from the mappings and applies it during D5 file-overlap
  ranking; Trace continuity chains become derived display axes; the topics-plan dependency is
  dropped and its cross-reference now warns against double-authoring membership.
- R: The user identified the flaw in the topic design: a topic records that a rename happened but
  not what became what, and the mapping is where the traceability value lives. Structured
  direction-preserving data upgrades the bridge from two-hop co-occurrence to genuine alias
  resolution (Explorer -> Lense -> Trace transitively), makes validation structural instead of
  conventional, and removes a cross-plan dependency.
- A: Kept the two-lineage-layers separation explicit - `supersedes`/`evolves` are decision
  lineage (entry -> entry), `continuity` is artifact lineage (name -> name); a feature removal
  typically writes both. Deferred term-level rename mappings feeding search-query expansion
  (retrieval behavior change; expose-before-you-rank). Alias table lives only in the suggest
  path, never default `memory_search` ranking.
- F: `docs/2_Todo/evolution-edges-plan.md`, `docs/2_Todo/memory-trace-topic-neighbourhoods-plan.md`,
  `docs/2_Todo/0_NEXT_STEPS.md`.

## 2026-07-10 13:36 - Opportunity triage: D7 freshness surfacing, seeding pass, Trace lineage pass

```yaml
entry_id: mse_xr6vav82parmbfh0
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - graph
  - retrieval
  - proposal
related_entries:
  - mse_ft7874jfnxxdevrz
  - mse_t22wn997q5005zjp
```

### Summary

- Explored adjacent opportunities to the evolution-edges proposal at user request; user accepted
  three of four via structured choice and rejected one. Also fixed a stale status line found
  during verification.

### Decisions

#### D1 - Fold three opportunities into the proposal

- D: Added D7 (computed `superseded_by`/`evolved_by` on `memory_search` result payloads -
  surfaces status, never re-ranks, reuses the search's extracted corpus via
  `build_related_entry_graph(chunks=...)` so the hot path never re-parses), a named post-P1
  user-reviewed lineage seeding pass (new clarification entries typing known history:
  merge-branch evolves fuse, Explorer -> Lense -> Trace renames, .AGENTS/layout migrations,
  reviewed supersedes candidates), and consolidated the Trace follow-up into one named "Trace
  lineage pass" deferred item (evolves edges + derived continuity chains).
- R: Verified gap: computed lifecycle status is exposed by `get_chunk` only, so a search
  consumer gets no retired-decision warning without a per-result round trip - the edges only pay
  off at the moment of consumption. The corpus has zero lifecycle edges, so without seeding every
  consumer launches inert.
- A: User rejected the fourth candidate (advisory git-rename reconciliation vs recorded
  continuity blocks) - recorded in Non-goals with the noise-risk rationale. Also rejected during
  exploration: more edge kinds, ranking changes from evolved_by, doc-lifecycle/entry-graph
  machinery, CHANGELOG generation from lifecycle metadata.
- F: `docs/2_Todo/evolution-edges-plan.md`.

#### D2 - Correct stale supersession-plan status

- D: Updated `docs/2_Todo/completed/supersession-edges-plan.md`'s status block: the "deferred
  P2 Lense UI surfacing" already shipped with the Memory Trace Arc 2 work (Trail view renders
  supersedes edges; verified against `memory-trace/` app.js/lense.py/tests).
- R: The plan's status line lagged `0_NEXT_STEPS.md`; two coordinating docs disagreed and
  verification against the code settled it.
- F: `docs/2_Todo/completed/supersession-edges-plan.md`.

## 2026-07-10 14:04 - Implement evolution edges P1 (evolves, continuity, D5-D7)

```yaml
entry_id: mse_h5trfwg2yjyjadpp
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-evolution-edges
topics:
  - graph
  - retrieval
  - session-logging
related_entries:
  - mse_xr6vav82parmbfh0
  - mse_t22wn997q5005zjp
  - mse_yjpvx6yp6ek50whk
evolves:
  - mse_mk82r25x7mvb1aqa
```

### Summary

- Implemented the approved evolution-edges plan end to end: the typed `evolves` edge with
  read-time-only `evolved_by` inverse, append-only inverse enforcement, the structured
  `continuity:` artifact-lineage field, rarity-weighted F-overlap ranking with a transitive
  rename alias table, computed lifecycle status on search results, and the harvest prompts. This
  entry's own `evolves:` ref is the first live use - the feature extends the typed-edge model
  the supersession P1 entry established, which remains fully valid.

### Decisions

#### D1 - Ship all seven plan decisions as one P1

- D: Implemented D1-D7 (edge + inverse, no-dampener rule, removal-under-supersedes convention,
  harvest prompts, F-overlap boost with shared-file evidence, continuity blocks + alias table,
  search freshness fields) in a single branch rather than splitting.
- R: The pieces interlock - the alias table consumes continuity, the harvest prompt leans on the
  boosted suggest output, and the search fields expose the inverse the graph computes - so a
  partial ship would document surfaces that do not exist yet.
- A: Return-shape change in `suggest_related_entries` handled with a `RelatedEntrySuggestion`
  wrapper carrying pass-through `chunk`/`final_score` properties, so existing consumers and
  tests kept working unchanged. `rank_session_memory` gained an optional `chunks=` corpus
  param so the search path builds the lifecycle graph without re-parsing session files.
- F: `memory_seed/semantic_cache.py`, `memory_seed/core.py`, `memory_seed/retrieval.py`,
  `memory_seed/mcp_server.py`, `memory_seed/cli.py`, `tests/test_semantic_cache.py`,
  `tests/test_memory_seed.py`, `tests/test_mcp_server.py`.
- T: 18 new tests; full suite 350 passing; `links check` clean on this corpus; live
  `link suggest` smoke ranked the supersession-P1 entry top for this very entry.

#### D2 - Documentation and skill surfaces in the same change

- D: Updated `session_logging.md`/`end_of_turn.md` (live + seed, byte-identical) with the
  evolves/continuity schema paragraphs, the three-way rule of thumb, the backtick-quoted F nudge,
  and harvest steps 7-8; updated `graph-edge-contract.md` (four edge kinds, per-kind guards,
  authored-inverse-field, continuity as a label family, read-surface rows), README, functionality
  audit, runtime index, and CHANGELOG.
- R: The edge contract's standing rule is that a new edge kind updates the spec in the same
  change, and the harvest prompts are the behavioral half without which the schema stays unused
  (supersedes sat at zero corpus uses for a week).
- F: `.memory-seed/skills/session_logging.md`, `.memory-seed/skills/end_of_turn.md`,
  `memory_seed/seed/.memory-seed/skills/session_logging.md`,
  `memory_seed/seed/.memory-seed/skills/end_of_turn.md`,
  `docs/3_Spec/graph-edge-contract.md`, `README.md`, `docs/3_Spec/functionality-audit.md`,
  `.memory-seed/index.md`, `CHANGELOG.md`, `docs/2_Todo/evolution-edges-plan.md`,
  `docs/2_Todo/0_NEXT_STEPS.md`.

## 2026-07-10 14:09 - Author goal directive for roadmap refinement and staged implementation

```yaml
entry_id: mse_m9h53bk0mj95g4q3
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - roadmap
  - goal
  - agent-collaboration
related_entries:
  - mse_h5trfwg2yjyjadpp
  - mse_xr6vav82parmbfh0
```

### Decision

- D: Authored `docs/2_Todo/goal-roadmap-refinement-and-staged-implementation.md` - a
  self-contained four-phase goal directive per user dictation: (1) plan refinement/alignment with
  batched user questions, blindspot surfacing, and sonnet-tier industry-standard research
  subagents; (2) plan + control-plane consolidation ending in a user-run /compact; (3) a
  read-only sonnet review swarm with frontier synthesis where nothing changes without user
  approval; (4) staged implementation - one feature per task branch, integrated via
  `session merge-branch`, one merge commit per stage for auditability/reversibility - closing
  with a docs-folder accuracy pass (completed-plan moves + doc refresh).
- R: The directive must survive the compaction it itself mandates, so it is written to be
  executable by a fresh session from the document alone, and it binds the repo's own mechanisms
  (Decision Harvest lifecycle prompts, merge-branch integration, proposal lifecycle, validation
  gates) rather than restating generic process.
- A: Normalized the user's duplicated phase numbering (two 3s) into phases 3 and 4; interpreted
  "the c docs folder" as the `docs/` lifecycle folders pass. Scope pinned to the Active Roadmap
  in `0_NEXT_STEPS.md` plus recorded residuals; the goal's Phase 1 confirms exact scope with the
  user before refinement starts.
- F: `docs/2_Todo/goal-roadmap-refinement-and-staged-implementation.md`.

## 2026-07-10 14:18 - Fix fuse rewriter entry spacing (blank line between entries)

```yaml
entry_id: mse_vm7trfd4dbd5yvnn
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-fix-session-entry-spacing
topics:
  - session-fuse
  - session-logging
related_entries:
  - mse_dr5eprnhrctqeeg3
  - mse_h5trfwg2yjyjadpp
```

### Decision

- D: Join rstripped entry records with a blank line ("\n\n") instead of a bare newline in
  `_write_chronological_session_file` and `_write_chronological_diagram_file`, per user report
  that fused files butted each `## ` heading against the previous entry's last line.
- R: Every `session fuse --apply` / `session merge-branch` rewrite today re-emitted the whole
  target file cramped (27/29 boundaries in 2026-07-10.md, 9/10 in its diagram sidecar), while
  hand-appended files like 2026-07-07.md kept the intended one-blank-line separation - the
  rewriter, not the appends, was the source.
- A: No repair pass needed for the session log itself: this branch's own integration re-fuses
  2026-07-10.md through the fixed writer, normalizing it in the same merge. The diagram sidecar
  file is not touched by this fuse (no new sidecar records), so its whitespace is repaired
  directly on main afterward as a user-requested formatting fix - whitespace only, no entry
  content or order changes.
- F: `memory_seed/core.py`, `tests/test_memory_seed.py`, `CHANGELOG.md`.
- T: New regression test pins one-blank-line separation after fuse apply; full suite 351 passing.

## 2026-07-10 14:26 - Push 18 commits to origin/main

```yaml
entry_id: mse_sbej12p5bq1n2ny8
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - git-workflow
  - release
related_entries:
  - mse_h5trfwg2yjyjadpp
  - mse_vm7trfd4dbd5yvnn
```

### Summary

- Pushed `6d88638..db4f190` to `origin/main` on explicit user direction: the full day's
  integrated work - session-log-check escalation hardening, the `session merge-branch` one-step
  integration command, evolution edges + continuity + lifecycle-aware ranking (D1-D7), the fuse
  rewriter spacing fix with whitespace repairs, and the docs/proposal landings (dev-tools triage,
  evolution-edges plan, goal directive).

### Decision

- D: Publish local main (18 commits, all merge-integrated and validated) to origin.
- R: Explicit user instruction "push to origin"; every commit had already passed the full suite
  (351), `links check`, and chronology verification at its own integration point.
- T: Push accepted by remote; `main...origin/main` in sync with no divergence.

## 2026-07-10 15:05 - Goal run Phases 1-2: alignment audit, decisions, consolidation

```yaml
entry_id: mse_dpf02qvaymr40nm7
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - roadmap
  - goal
  - release
related_entries:
  - mse_m9h53bk0mj95g4q3
  - mse_sbej12p5bq1n2ny8
```

### Summary

- Executed Phases 1-2 of the goal directive: three parallel sonnet plan-audits across the active
  roadmap, an alignment matrix, four user decisions via structured choice, and the consolidation
  edits across plans and the control plane. No plan contradicted a recorded design decision; the
  drift was status staleness, one live public-README defect, and one materialized blind spot.

### Decisions

#### D1 - Release strategy: cut core 2.17 now, trace 0.1.0 later

- D: This run prepares and cuts Memory Seed 2.17 (S4); memory-trace 0.1.0 publication is deferred
  until the user creates the PyPI project + trusted publisher for it (none exists), with a
  prepared publish workflow shipped in advance so it is one step later.
- R: The 2.17 branch-field floor is a hard prerequisite the distribution plan already records, the
  Unreleased pile (~15 features) is becoming a monster release (surfaced blind spot), and the
  trace PyPI side is a user-only dependency the plan had not called out as remaining work.
- A: Rejected publishing both in-run (blocks on user PyPI setup mid-run) and deferring all
  releases (accepts unbounded release growth).

#### D2 - Immediate README hotfix stage (S2)

- D: Fix the two live front-door defects ahead of the full refresh: the README instructs
  `pip install memory-trace` for a package not on PyPI, and line 16 renders mojibake.
- R: Both are live on the public GitHub front door today; the refresh plan framed the install
  claim only as a future risk rather than a present defect.

#### D3 - Topics starter vocabulary derived from existing slugs

- D: This repo's `.memory-seed/topics.yaml` is harvested from the ad-hoc `topics:` slugs
  already authored across 41 entries, de-duplicated, and user-approved before writing; seeded
  projects get a minimal generic starter. Recorded in the topics plan (its open decision closed).
- R: The plan's topic-sprawl blind spot already materialized - entries author unvalidated slugs -
  so the real vocabulary is the one in use, not an invented list.

#### D4 - Stage sequence approved and consolidated

- D: S1 doc repairs (executed as Phase 2), S2 README hotfix, S3 lineage seeding pass
  (user-reviewed), S4 core 2.17 cut (user approves PyPI gate), S5 topics P1, S6 README refresh;
  trace publication, diagram export packs, AI summarisation, fontjoy, and the Trace lineage pass
  deferred. One branch + one merge commit per stage.
- R: Dependency order (release before topics testing against released core; hotfix before more
  eyes land on the front door) with each stage independently revertable.

#### D5 - Alignment repairs applied (Phase 2 consolidation)

- D: Corrected the distribution plan's stale statuses (Phase 1 actually released in 2.16.0;
  Phase 2 merged/pushed, branch gone); split the README plan's live defects into the hotfix stage
  and retired its inapplicable search-URL criterion; added the reciprocal boundary notes between
  the P2 mutation plan and the evolution-edges seeding pass; bumped the graph-edge-contract
  "As of" line; removed the two self-declared stale 2_Todo leftovers (`README Improvements.md`,
  `memory-trail-graph-and-topic-neighbourhoods.md` - OneDrive permitted deletion this time);
  refreshed the 0_NEXT_STEPS run brief to the approved stages; updated the runtime index current-run
  line.
- R: Every repair traces to an audit finding classified as resolvable-from-record; open items went
  to the user instead (D1-D4).
- F: `docs/2_Todo/memory-trace-distribution-plan.md`, `docs/2_Todo/readme-front-door-refresh-plan.md`,
  `docs/2_Todo/related-entries-p2-mutation-plan.md`, `docs/2_Todo/evolution-edges-plan.md`,
  `docs/2_Todo/memory-trace-topic-neighbourhoods-plan.md`, `docs/2_Todo/0_NEXT_STEPS.md`,
  `docs/3_Spec/graph-edge-contract.md`, `.memory-seed/index.md`.
- T: `links check` pending in the commit gate below; no code changed in these phases.

## 2026-07-10 15:25 - Goal run Phase 3: review swarm verdicts and approved fixes

```yaml
entry_id: mse_903jqy9v4pr8g388
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - roadmap
  - goal
  - release
related_entries:
  - mse_dpf02qvaymr40nm7
```

### Summary

- Ran the Phase 3 assumption-review swarm (four parallel sonnet reviewers: plan consistency/doc
  drift, topics-P1 contract impact, 2.17 release readiness, migration-compat/test coverage; one
  stalled mid-stream and was resumed to re-emit). Synthesis: the S1-S6 stage set, cross-references,
  seeding-pass compatibility, topics parsing seams, version-bump guard coverage, CHANGELOG-fold
  convention, S2 hotfix scope, and publish.yml OIDC mechanics all confirmed sound. Four fixes
  proposed, all user-approved, all applied.

### Decisions

#### D1 - Release-history refresh (approved)

- D: Corrected the roadmap's stale history: 0_NEXT_STEPS now says releases run through 2.16.0,
  replaces the misclassified "Unreleased after 2.14.0" bucket with 2.15.0/2.16.0 summaries plus an
  Unreleased-queued-for-2.17 section deferring to CHANGELOG as authority; fixed the stale
  "unpushed tree" wording; corrected six index.md lifecycle markers to their verified ship
  versions (2.12.0/2.13.0/2.14.0/2.16.0, with sessions-month-layout split as still unreleased).
- R: Reviewer-verified drift - the bucket's contents shipped in 2.15/2.16; duplicated release
  bookkeeping was the root cause, so the refresh also delegates authority to CHANGELOG.
- A: Deeper finding during verification: **Memory Trace Phase-2 extraction and the skills-management
  CLI were never changelogged at all** (no memory-trace changelog exists either); both folded into
  CHANGELOG Unreleased so the 2.17 fold captures them.

#### D2 - Runbook fold/archive steps (approved)

- D: `release_publishing.md` (live + seed, byte-identical) procedure now includes the full
  version-bump surface (naming the root-routing-file trap and its guard test), the CHANGELOG fold
  with the standing empty Unreleased header, the operator-owned archive snapshot step, and the
  irreversible-PyPI-gate note.
- R: Preconditions mentioned fold/archive but no procedure step owned them - S4 would have relied
  on an incomplete runbook.

#### D3 - Topics slug contract pinned (approved)

- D: Topic slugs must match `^[a-z0-9][a-z0-9_-]{0,63}$` (user-slug pattern family, underscores
  permitted); recorded in the topics plan with the reviewer's novel-test-infra note for Trace
  chain rendering and `topics suggest`.
- R: The plan listed "malformed slugs" as a check category with no pattern defined - an
  implementation blocker; all 42 live slugs pass the chosen regex.

#### D4 - Stale worktree metadata removed (approved)

- D: Deleted the three phantom `.git/worktrees/` entries (ms-verify, ms-verify2, verify-main)
  via chmod-on-error rmtree after `git worktree prune` hit Windows read-only Permission denied.
- R: Verified safely prunable (gitdir files gone; `git worktree list` already excluded them);
  live agent worktrees untouched.
- F: `docs/2_Todo/0_NEXT_STEPS.md`, `.memory-seed/index.md`, `CHANGELOG.md`,
  `.memory-seed/skills/release_publishing.md`,
  `memory_seed/seed/.memory-seed/skills/release_publishing.md`,
  `docs/2_Todo/memory-trace-topic-neighbourhoods-plan.md`.

## 2026-07-10 15:28 - S2: README front-door hotfix (trace install truth + mojibake)

```yaml
entry_id: mse_x3807z3xnjdddzzd
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-fix-readme-front-door-defects
topics:
  - readme
  - release
related_entries:
  - mse_903jqy9v4pr8g388
```

### Decision

- D: Stage S2 of the goal run: the root README's Memory Trace section now states plainly that
  `memory-trace` is not yet on PyPI (publishes after core 2.17), with a working
  `pip install ./memory-trace` from-clone instruction until then; the same correction applied to
  `memory-trace/README.md`; and the line-16 demo link's mojibake ("â–¶") removed in favor of
  plain text.
- R: Both defects were live on the public front door: the install command failed for anyone who
  tried it, and the mojibake contradicted the project's own encoding policy. Approved as an
  immediate hotfix ahead of the full S6 refresh.
- A: Kept plain link text rather than re-adding a play symbol - the emoji-header blind spot in
  the refresh plan warns against decorative non-ASCII in this file.
- F: `README.md`, `memory-trace/README.md`.
- T: `memory-seed encoding check README.md` reports only the known non-fatal CRLF working-copy
  finding (normalized by .gitattributes on commit); mojibake finding gone.

## 2026-07-10 15:34 - Artifact lineage: naming and layout history

```yaml
entry_id: mse_srcvaz87r5wq244p
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-lineage-seeding
topics:
  - continuity
  - memory-trace
  - session-layout
related_entries:
  - mse_0842kjv76f4btrr8
  - mse_38mmhbyvxyta2wgd
  - mse_fcecj9hpq4qj16ay
  - mse_k2188xq5evwmdny9
continuity:
  - kind: rename
    from: Memory Explorer
    to: Memory Lense
  - kind: rename
    from: Memory Lense
    to: Memory Trace
  - kind: rename
    from: memory_seed/lense.py
    to: memory-trace/memory_trace/lense.py
  - kind: rename
    from: memory-seed lense
    to: memory-trace
  - kind: migration
    from: .AGENTS/
    to: .memory-seed/
  - kind: migration
    from: .memory-seed/sessions/YYYY-MM-DD.md
    to: .memory-seed/sessions/YYYY-MM/YYYY-MM-DD.md
```

### Summary

- Lineage seeding pass (goal-run stage S3, user-approved): records the project's artifact renames
  and migrations as structured continuity blocks so the transitive alias table can bridge
  file-overlap ranking and terminology across them. The referenced entries hold the original
  decisions; this entry adds only the machine-readable mappings the schema lacked at the time.

## 2026-07-10 15:35 - Lifecycle clarification: merge-branch evolves the fuse workflow

```yaml
entry_id: mse_5mv4dvxfp1tr8v16
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-lineage-seeding
topics:
  - session-fuse
  - git-workflow
related_entries:
  - mse_dr5eprnhrctqeeg3
evolves:
  - mse_81v7vk4x5ys3k2n0
```

### Summary

- Types the relationship the evolution-edges plan used as its motivating example: the
  `session merge-branch` command (mse_dr5eprnhrctqeeg3, 2026-07-10 12:03, which predates the
  `evolves` field) extends the branch-session fuse implementation (mse_81v7vk4x5ys3k2n0) - the
  fuse remains the fully valid primitive that merge-branch wraps; nothing is retired.

## 2026-07-10 15:36 - Retirement record: Explorer/Lense-era naming and packaging decisions

```yaml
entry_id: mse_mkxdvaxvw99dz4s0
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-lineage-seeding
topics:
  - memory-trace
  - supersession
related_entries:
  - mse_38mmhbyvxyta2wgd
  - mse_fcecj9hpq4qj16ay
supersedes:
  - ms-a4282580
  - mse_0842kjv76f4btrr8
```

### Summary

- Retirement record (user-directed, accepting the entry-level granularity trade-off): the
  Explorer-era packaging decision (ms-a4282580 D1: separate `memory-seed-explorer` companion
  install) and the Memory Lense naming reconciliation (mse_0842kjv76f4btrr8 D4) are superseded by
  the memory-trace naming (mse_38mmhbyvxyta2wgd) and standalone extraction (mse_fcecj9hpq4qj16ay).
  **Granularity caveat, on the record:** supersession is entry-level, so still-live sibling
  decisions inside those entries - notably ms-a4282580 D2 (80-bit `mse_` entry IDs, canonical
  entry-level `related_entries`) and mse_0842kjv76f4btrr8 D1-D3 (timeline/panel UI behaviors) -
  are dampened (importance x0.25) despite remaining valid. The user chose this knowingly during
  the 2026-07-10 goal-run S3 review; consult this entry before treating those entries' dampened
  rank as a signal their live decisions were retired.

## 2026-07-10 15:39 - S3 complete: lineage seeding pass (three entries)

```yaml
entry_id: mse_nzxacbsfes6q72be
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-lineage-seeding
topics:
  - goal
  - graph
related_entries:
  - mse_srcvaz87r5wq244p
  - mse_5mv4dvxfp1tr8v16
  - mse_mkxdvaxvw99dz4s0
```

### Decision

- D: Stage S3 executed with three user-reviewed seeded entries: E1 artifact lineage (six
  continuity blocks), E2 evolves clarification (merge-branch extends fuse), E3 retirement record
  (Explorer packaging + Lense naming superseded by the Trace decisions).
- R: The corpus carried zero lifecycle data, leaving the alias table, freshness fields, and
  dampening inert; seeding gives every consumer real data. Verified live post-write: fuse entry
  shows `evolved_by` with importance undampened (4.00), both retired entries show
  `superseded_by` with the x0.25 dampener, and `link show` renders all six continuity
  mappings.
- A: I recommended skipping supersedes seeding because both candidates sit inside multi-decision
  entries with live siblings (entry-level dampening falsifies their rank); the user chose to
  proceed anyway, so E3 carries an explicit granularity caveat naming the still-live sub-decisions
  it collaterally dampens.
- F: `.memory-seed/sessions/2026-07/2026-07-10.md`.
- T: `links check` clean (all guards pass on the seeded edges); live `link show` verification
  above.

## 2026-07-10 15:42 - S4: cut Memory Seed 2.17.0 (prep complete, release gate pending)

```yaml
entry_id: mse_1n3351qgep3xwa6y
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-release-2.17
topics:
  - release
  - goal
related_entries:
  - mse_903jqy9v4pr8g388
  - mse_nzxacbsfes6q72be
```

### Decision

- D: Prepared the 2.17.0 release per the repaired runbook: bumped every version surface
  (`pyproject.toml` 2.17.0, control-plane `VERSION` 2.17, `memory-system-version:` in 130
  markdown files including the root routing files, the version-pin test); folded the CHANGELOG
  Unreleased section into `## 2.17.0 - 2026-07-10` keeping the standing empty Unreleased header;
  restored the `.memory-seed/archive/<version>/` snapshot convention (dormant since 2.5) with
  `archive/2.17/`; shipped the inert `publish-memory-trace.yml` workflow (job-gated on
  `memory-trace-v*` release tags, `pypi-memory-trace` environment, OIDC trusted publishing -
  activates only after the user creates the PyPI project); and created
  `memory-trace/CHANGELOG.md` (the package had none).
- R: Goal-run S4 as approved: 2.17 is the hard floor for memory-trace 0.1.0 and the Unreleased
  pile had reached monster-release size. The GitHub Release itself (push + `gh release create
  v2.17.0`) is outward-facing and waits for the user's explicit go; publish.yml then pauses at
  the `pypi` environment for their approval before the irreversible PyPI push.
- F: `pyproject.toml`, `memory_seed/core.py`, `CHANGELOG.md`, 130 markdown version markers,
  `tests/test_memory_seed.py`, `.memory-seed/archive/2.17/`,
  `.github/workflows/publish-memory-trace.yml`, `memory-trace/CHANGELOG.md`,
  `docs/2_Todo/0_NEXT_STEPS.md`, `.memory-seed/index.md`.
- T: Full suite 351 passing on 2.17; `doctor` healthy (known non-fatal encoding warning);
  `links check` clean; `memory-seed version` prints 2.17.

## 2026-07-10 15:48 - v2.17.0 released: pushed, tagged, awaiting PyPI gate

```yaml
entry_id: mse_304mqvwvcz3rqq61
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - release
  - goal
related_entries:
  - mse_1n3351qgep3xwa6y
```

### Decision

- D: On explicit user approval, pushed the six release commits to origin
  (`b948495..20630fb`) and created GitHub Release v2.17.0 with CHANGELOG-derived notes;
  publish.yml run 29101112009 is at the `pypi` environment gate awaiting the maintainer's
  approval for the irreversible PyPI upload.
- R: Goal-run S4 release gate, taken per the user's "Push + create release" choice; every commit
  was suite-green and links-clean at its own integration point.
- T: `gh run list` shows the v2.17.0 run in waiting state; v2.16.0 precedent completed
  successfully via the same gate.

## 2026-07-10 16:13 - S5: implement indexed topics P1 (vocabulary, parsing, filter, CLI)

```yaml
entry_id: mse_ehm67mqpmsqm00md
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-topics-p1
topics:
  - graph
  - retrieval
  - control-plane
related_entries:
  - mse_903jqy9v4pr8g388
  - mse_dpf02qvaymr40nm7
evolves:
  - mse_srcvaz87r5wq244p
```

### Summary

- Implemented topics-plan Phases 0-3 (goal-run S5): `topics:` becomes a first-class controlled
  1-3-slug entry field resolved against the deploy-once project-local
  `.memory-seed/topics.yaml`. This entry's own `topics:` uses only canonical slugs and its
  `evolves` edge extends the artifact-lineage seeding record with vocabulary-level lineage.

### Decisions

#### D1 - Vocabulary derived from usage, aliases preserve every observed slug

- D: This repo's vocabulary is the user-approved 19-canonical consolidation of the 62 slugs found
  across 50 entries (every observed slug canonical or alias, aliases resolve at read time);
  seeded projects get a minimal 5-topic generic starter. Deploy-once via the existing
  runtime-local rule so `update` never overwrites curation.
- R: The plan's topic-sprawl blind spot had already materialized; deriving from real usage plus
  alias preservation makes the whole live corpus validate with zero errors on day one.
- F: `.memory-seed/topics.yaml`, `memory_seed/seed/.memory-seed/topics.yaml`,
  `memory_seed/core.py` (SEED_FILES), `pyproject.toml` (package data).

#### D2 - Separate topics module and validator

- D: New `memory_seed/topics.py` owns fail-open index loading, the slug contract
  (`^[a-z0-9][a-z0-9_-]{0,63}$`, user-slug pattern family), `memory-seed topics list`/`check`
  (unknown/malformed/duplicate/collision errors; deprecated-use and >3-count warnings;
  unused-topic info; missing-index error only when entries carry topics), and the alias-expanded
  filter set builder.
- R: Topics are membership, not a graph edge kind - the contract's validation-authority rule is
  scoped to edge kinds, so a separate validator keeps `links check` focused (Phase 3 review
  verdict).
- F: `memory_seed/topics.py`, `memory_seed/cli.py`.

#### D3 - Additive retrieval surface

- D: `MemoryChunk.topics` parsed at all three chunk build sites; `ranked_to_dict`/
  `chunk_to_dict` expose the stored slugs; `memory_search`/`rank_session_memory`/
  `_filter_chunks` gain an opt-in pre-ranking `topics` filter expanded through the alias table
  (fail-open on unknown names, structurally unable to affect default ranking when unused); the
  MCP `memory_search` schema gains the additive `topics` array param.
- R: Same additive pattern as evolves/continuity; the filter sits at the same seam as
  user/date_from/date_to per the Phase 3 review.
- F: `memory_seed/semantic_cache.py`, `memory_seed/retrieval.py`, `memory_seed/mcp_server.py`,
  `.memory-seed/skills/session_logging.md` (+ seed twin),
  `docs/3_Spec/graph-edge-contract.md`, `docs/3_Spec/functionality-audit.md`,
  `.memory-seed/index.md`, `README.md`, `CHANGELOG.md`,
  `docs/2_Todo/memory-trace-topic-neighbourhoods-plan.md` (status: Phases 0-3 implemented,
  Phase 4 deferred).
- T: 8 new tests in `tests/test_topics.py`; two packaging/inventory guard tests updated for the
  new seed file; full suite 359 passing; live `topics check`: 19 topics, 50 entries, zero
  errors (six soft count warnings); `links check` clean.

## 2026-07-10 16:17 - S6: README front-door refresh (shape, placeholders, links)

```yaml
entry_id: mse_ea9eea65snwrwrec
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-readme-refresh
topics:
  - documentation
related_entries:
  - mse_x3807z3xnjdddzzd
  - mse_dpf02qvaymr40nm7
```

### Decision

- D: Implemented the readme-refresh plan's front-door shape on the existing README rather than a
  ground-up rewrite: highlights block (local-first/vendor-neutral/inspectable/agent-native/safe
  collaboration), surfaced publishable-memory security callout linking Public Memory Hygiene,
  compact five-step "How It Works", explicit visual-proof placeholders (init + Trace screenshots),
  and a "Learn More" footer (CHANGELOG, roadmap, functionality audit, graph-edge contract, Trace
  README). Plan status set to IMPLEMENTED with the recorded residual: deep reference tail stays in
  the README for now; real captures replace placeholders when the user provides them.
- R: The existing top already satisfied the quickstart/agent-handoff/top-quarter criteria and the
  S2 hotfix fixed accuracy; the gap was highlights, security surfacing, orientation, visuals, and
  links - deliverable without destabilizing 600 lines of established reference content late in
  the run.
- F: `README.md`, `docs/2_Todo/readme-front-door-refresh-plan.md`.
- T: `memory-seed encoding check README.md` clean; anchor link verified against the existing
  Public Memory Hygiene heading.

## 2026-07-10 16:19 - Goal run complete: closing docs pass and stage map

```yaml
entry_id: mse_37jhdn135pbhv4nz
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - proposal-lifecycle
  - documentation
related_entries:
  - mse_dpf02qvaymr40nm7
  - mse_ea9eea65snwrwrec
  - mse_ehm67mqpmsqm00md
```

### Summary

- Closed the four-phase goal run: docs-folder accuracy pass done, every shipped feature traceable
  to one merge commit and one session entry. Stage map (all revertable via `git revert -m 1`):
  S1 docs repairs `aedd1c0`/`77c6c1b` (direct-main), S2 README hotfix `0d87ad9`, S3 lineage
  seeding `6ad3b33`, S4 v2.17.0 cut `20630fb` (GitHub Release published, PyPI gate user-side),
  S5 topics P1 `f6291d5`, S6 README refresh `1a09a3e`.

### Decision

- D: Moved `readme-front-door-refresh-plan.md` to completed/ with its residuals recorded
  (screenshot captures, optional tail-slimming); kept the evolution-edges, topics, and
  distribution plans in 2_Todo because each carries a named open follow-up (Trace lineage pass;
  plan Phase 4; trace 0.1.0 publication pending user PyPI setup). Refreshed the run brief to the
  completed stage map and the runtime index to the goal-complete state with the open follow-up
  list.
- R: Proposal-lifecycle rule 4 - only fully resolved plans move; partially complete ones stay
  active with explicit remaining work.
- F: `docs/2_Todo/0_NEXT_STEPS.md`, `docs/2_Todo/completed/readme-front-door-refresh-plan.md`,
  `.memory-seed/index.md`.
- T: Full suite 359 passing; `links check` clean; `topics check` clean. Commits since the
  release push remain local pending user direction.

## 2026-07-10 16:48 - Docs-folder organisation pass: inbox cleared, lifecycle moves

```yaml
entry_id: mse_am1w4rq6cv0nw4zy
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - proposal-lifecycle
  - documentation
related_entries:
  - mse_37jhdn135pbhv4nz
continuity:
  - kind: migration
    from: docs/2_Todo/3.0-plan.md
    to: docs/2_Todo/completed/3.0-plan.md
  - kind: migration
    from: docs/2_Todo/goal-roadmap-refinement-and-staged-implementation.md
    to: docs/2_Todo/completed/goal-roadmap-refinement-and-staged-implementation.md
```

### Decision

- D: Completed the docs-folder organisation the closing pass had only partially covered, prompted
  by the user's direct question. Inbox cleared to zero: the resolved cp1252-processes residual and
  the triaged dev-tools reel moved to completed/ with disposition headers; the still-open fuse
  non-UTF-8 silent-skip residual verified against current code, promoted to 2_Todo with a status
  header, and listed under deferred small fixes. Deleted three more self-declared OneDrive-blocked
  stale duplicates from 2_Todo (`graph recommendations.md`,
  `memory-seed-trace-upgrade-shutdown-plan.md`, `utf8-encoding-doctor-and-static-check-plan.md`
  - canonical copies confirmed in completed/). Moved the executed goal directive and the absorbed
  historical `3.0-plan.md` to completed/ with dispositions, and repaired every inbound link
  (0_NEXT_STEPS, distribution plan, P2 mutation plan, runtime index).
- R: The closing pass had handled run-generated artifacts but not the wider tree; an honest answer
  to "have you organised the docs folder" required finishing the job per the lifecycle rules
  (resolved items leave the inbox, superseded/executed plans move with dispositions, links never
  dangle).
- F: `docs/1_Inbox/` (now empty), `docs/2_Todo/` (5 moves/deletes),
  `docs/2_Todo/completed/` (+5 files), `docs/2_Todo/0_NEXT_STEPS.md`, `.memory-seed/index.md`,
  `docs/2_Todo/memory-trace-distribution-plan.md`, `docs/2_Todo/related-entries-p2-mutation-plan.md`.
- T: Full suite 359 passing; `links check` clean; inbox item count 0; moved-file reference sweep
  returned no dangling links.

## 2026-07-10 19:29 - Trace UI live session: first-load cache race found and fixed

```yaml
entry_id: mse_xcsvhpw3bncdxq20
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: main
topics:
  - memory-trace
  - bugfix
related_entries:
  - mse_am1w4rq6cv0nw4zy
```

### Decision

- D: Launched Memory Trace against this repo's live corpus for interactive user testing (preview
  server, port 8765, `.claude/launch.json` pinned to the interpreter that has fastapi/uvicorn).
  First load crashed with 500s and exposed a real product bug: `LenseCache.ensure_current()` had
  no lock, so the UI's concurrent first-load API calls raced the same rebuild into one
  pid-named tmp file ("table meta already exists", then Windows PermissionError on
  replace/unlink). Fixed in the working tree: double-checked `threading.Lock` around the
  rebuild, per-attempt uuid tmp names, retrying `os.replace` with tmp cleanup. Also killed two
  orphaned trace server processes (one from an earlier run on port 8799) that held the stale
  cache.
- R: Any Windows user would hit this on first cold-cache load - the UI fires several endpoints at
  once by design. Serializing the rebuild with double-checked locking keeps the hot path lock-free
  while losers reuse the winner's fresh cache.
- A: Fix is uncommitted on main deliberately - it becomes the first commit of the Trace UI
  development branch this testing session is kicking off. Observed targets for that branch: the
  sidebar TOPICS facet renders empty despite entries carrying indexed topics (plan Phase 4), and
  the Trail legend styles supersession but has no evolves-edge styling yet (Trace lineage pass).
- F: `memory-trace/memory_trace/lense.py`, `.claude/launch.json`.
- T: Server up after fix: 293 entries / 1084 chunks indexed, Trail renders 259 nodes / 344 edges,
  newest entry displays its `topics:` in the reader YAML.

## 2026-07-10 19:54 - Fix search focus loss; scope searchbox to Search view

```yaml
entry_id: mse_e9ftlqppckuo2ptw
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-trace-ui-pass
topics:
  - memory-trace
  - bugfix
related_entries:
  - mse_xcsvhpw3bncdxq20
```

### Decision

- D: Fixed the user-reported search bug in Memory Trace: `render()` rebuilds the whole DOM via
  `innerHTML`, destroying the input mid-typing (focus, caret, and keystrokes newer than the
  debounced state all lost). Added `captureFocusedInput()`/`restoreFocusedInput()` around the
  rebuild - captures id, live value, and selection; restores them after. Also scoped the topbar
  searchbox to the Search view only, replaced elsewhere by a same-width spacer: the query param
  only feeds `/api/search`, so on Timeline/Graph/Trail the box changed nothing onscreen. Verified
  live in the preview: typing "fuse" keeps focus (caret 4), filters to 30 results; spacer holds
  the tab cluster in place on Timeline.
- R: User direction: search "should be present only if i can actually use the text box to change
  what is onscreen". Preserving the live value (not just focus) matters because keystrokes landing
  during the in-flight reload are newer than `state.query` and would be visually reverted.
- A: The de-crowding research brief recommends one persistent global search across views; the
  user's explicit direction overrides it for now. If search later becomes cross-view (e.g. typing
  jumps to Search), revisit the conditional.
- F: `memory-trace/memory_trace/static/app.js`, `memory-trace/memory_trace/static/styles.css`,
  `memory-trace/memory_trace/static/index.html` (cache-bust bump).
- T: Preview eval: focusedIdAfterTyping=query, inputValue=fuse, resultCount=30,
  spacerOnTimeline=true.

## 2026-07-10 20:08 - Trail rebuilt as git-graph timeline; topics wired server-side

```yaml
entry_id: mse_37fpco0vvunilz5k
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-trace-ui-pass
topics:
  - memory-trace
  - graph
related_entries:
  - mse_e9ftlqppckuo2ptw
```

### Decision

- D: Rebuilt Memory Trace's Trail view as a vertical git-graph timeline, replacing the shared
  force-layout graph engine it borrowed before. Newest entry on top; one straight lane per branch
  via interval coloring (lowest free lane, freed when the branch's visible life ends); lifecycle
  arcs bow through a left gutter - supersedes dashed red "replaces", evolves dotted blue "refines"
  (new `--edge-evolves` token #4f98d9); branch chip on each branch's tip row only; day separators;
  fixed 30px row height keeps the SVG rail index-aligned with HTML rows. Default window is the
  newest 60 entries with "Load older" (+60, client-side - the full corpus is fetched once).
  Server side: graph nodes now carry `branch` and `datetime`, `_graph_edges` emits `evolves`, and
  `_topics()` includes indexed `chunk.topics` - which also fixed the empty sidebar TOPICS facet
  and the topic filter in one line. Trail's fixed edge set is now branch+supersedes+evolves;
  plain relatedness dropped from Trail (it swamped the lineage signal).
- R: User direction: "trail should be reminiscent of an interactive gitgraph timeline" + research
  brief consensus (GitKraken/Fork/Sublime Merge/VS Code Git Graph): vertical orientation, graph
  rail left + metadata right, straight lanes, bounded default window, dash+color dual encoding.
  User approved: recent window, minimal rows, chrome cuts, Space Grotesk+Inter.
- A: Two research briefs (git-graph conventions; dense-UI de-crowding) were produced by sonnet
  subagents this session and synthesized into the four approved design decisions; remaining
  stages: de-crowding pass, typography pass.
- F: `memory-trace/memory_trace/lense.py`, `memory-trace/memory_trace/static/app.js`,
  `memory-trace/memory_trace/static/styles.css`, `memory-trace/memory_trace/static/index.html`,
  `memory-trace/tests/test_lense.py`.
- T: 38 memory-trace tests pass (2 new: evolves edges + node lineage fields; indexed-topics
  facets/filter). Live preview: 60 of 261 entries, 40 lane segments, 2 lifecycle arcs, branch tip
  chips, day separators; row click opens reader; Load older 60->120; console clean.

## 2026-07-10 20:15 - De-crowding pass: minimal rows, chrome cuts, collapsible sidebar

```yaml
entry_id: mse_rqyj0ihl4whwqj19
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-trace-ui-pass
topics:
  - memory-trace
related_entries:
  - mse_37fpco0vvunilz5k
```

### Decision

- D: Applied the user-approved de-crowding bundle across Memory Trace. Rows are now
  overview-first: search cards show date + title only (score appears only under a query; excerpt,
  topics, agent, matched-section chips moved to the reader), timeline items collapsed to one
  time+title line, and the redundant timestamp prefix is stripped from all list titles via
  `stripTitleStamp()` (the date/time column already carries it). Chrome cuts: card/timeline/metric
  borders, backgrounds, and the floating graph legend removed - whitespace separates, accent
  appears only on hover/selection; graph edge-type chips now carry their edge color directly
  (dual-encoded control replaces the legend). Sidebar is collapsible (topbar toggle, persisted in
  localStorage) so Graph/Trail can claim the width. Facet hygiene: topics facet capped at 12 chips
  behind "+N more"; active filters render as removable chips in every viewbar. Dead code removed:
  graphLegend, matchedSectionChips, highlight, escapeRegExp.
- R: The dense-UI research brief's top levers (overview-first, data-ink, Hick's law, facet
  hygiene, muted-by-default) applied per the user's "too crowded - simplify" direction and
  explicit approval of the recommended options.
- F: `memory-trace/memory_trace/static/app.js`, `memory-trace/memory_trace/static/styles.css`,
  `memory-trace/memory_trace/static/index.html`.
- T: Live preview: cards have no excerpt, topics facet shows 12+"+50 more", topic filter renders
  "#graph x" chip and clears, sidebar collapse/restore round-trips, graph legend absent with 262
  nodes still rendering; console clean.

## 2026-07-10 20:24 - Typography: self-hosted Space Grotesk + Inter pairing

```yaml
entry_id: mse_ka0qpy31eq42cnf2
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-trace-ui-pass
topics:
  - memory-trace
related_entries:
  - mse_rqyj0ihl4whwqj19
```

### Decision

- D: Implemented the fontjoy typography plan with the user-selected pairing: Space Grotesk
  (display: brand, card titles, reader heading) + Inter (body). Variable latin-subset WOFF2s
  self-hosted under `static/fonts/` with both OFL license files retained; `@font-face` +
  `--display` token in styles.css; new whitelisted `/assets/fonts/{name}` route serving
  `font/woff2`; all six files added to package-data. No CDN call - Trace stays fully
  local/offline per the plan's GDPR positioning note. Also updated the two frontend-contract
  tests to pin the new design (no floating legend, edge chips carry edge colors,
  TRAIL_EDGE_TYPES=branch,supersedes,evolves, trail renderer internals) instead of the old one.
- R: User chose "Space Grotesk + Inter" from the pairing options; Inter was already first in the
  sans stack so body text shifts minimally while headings gain a distinct technical voice.
- F: `memory-trace/memory_trace/static/fonts/` (4 new files),
  `memory-trace/memory_trace/static/styles.css`, `memory-trace/memory_trace/lense.py`,
  `memory-trace/pyproject.toml`, `memory-trace/memory_trace/static/index.html`,
  `memory-trace/tests/test_lense.py`.
- T: 38 memory-trace tests pass; document.fonts reports both families loaded; font endpoint 200
  with font/woff2; Trail screenshot confirms Space Grotesk rendering offline.

## 2026-07-10 20:35 - Goal pass: scroll-preservation sweep + Trail gitgraph forking

```yaml
entry_id: mse_v3q8dwm2knzp61tr
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-trace-ui-pass
topics:
  - memory-trace
  - bugfix
related_entries:
  - mse_ka0qpy31eq42cnf2
```

### Decision

- D: First two units of the autonomous UI goal pass. (1) Scroll-reset bug class fixed:
  `captureCenterScroll` only preserved three center selectors, so left/right panes and the new
  Trail scroller reset on every re-render (the user-reported left-pane facet click bug). New
  `SCROLL_PRESERVE_SELECTORS` covers all six scrollable regions with a comment requiring new
  scroll containers to be registered; contract test pins the full list. (2) Trail now forks like
  a gitgraph: `trail-link` rounded curves connect each task branch's oldest visible row to the
  nearest older main row (fork) and its newest row to the nearest newer main row (merge); open
  branches deliberately dangle - no merge is fabricated. main is pinned to the leftmost lane
  (git-client convention) and no-branch legacy entries no longer allocate a lane.
- R: Goal directive: "when i'm selecting components on the left pane the scroll wheel is
  resetting... do a full check for this type of bug"; "straight lines and forking with nodes...
  much like a gitgraph". Fork/merge targets reflect the real workflow (task branches cut from and
  merged into main via session merge-branch), so no false edges are drawn.
- F: `memory-trace/memory_trace/static/app.js`, `memory-trace/memory_trace/static/index.html`,
  `memory-trace/tests/test_lense.py`.
- T: Live preview: left-pane scrollTop 249->249 across a facet click with filter applied; Trail
  shows 37 fork/merge connectors + 40 lane lines + 2 lifecycle arcs.

## 2026-07-10 20:48 - Goal pass: force-directed graph layout with sparse attraction

```yaml
entry_id: mse_h2xnk9wq5s0e7fmd
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-trace-ui-pass
topics:
  - memory-trace
  - graph
related_entries:
  - mse_v3q8dwm2knzp61tr
```

### Decision

- D: Replaced the Graph view's attraction-only hash-scatter layout with a hand-rolled
  Fruchterman-Reingold force simulation (d3-force recipe, dependency-free): all-pairs k^2/d
  repulsion, weighted link attraction, centering gravity, cooling schedule, then a 10-pass
  30px collision separation. Deterministic hash-seeded start; positions cached per dataset so
  pan/zoom/hover re-renders never recompute (427ms one-time for 265 nodes). Critical fix found
  during validation: the old all-pairs date-proximity link set collapsed the simulation into a
  hairball (minDist 1px) on burst-heavy corpora - `layoutSimilarityLinks` now emits sparse
  attraction only (related edges + top-3 topic neighbours per node, date proximity demoted to a
  tie-break boost): 370 links instead of thousands, minDist 23px. Labels are now progressive:
  hovered/selected/neighbourhood plus the top-15 connected nodes; tooltips carry the rest.
- R: Goal directive: "improve graph UI that spaces the nodes out more effectively". Force layout
  theory: FR attraction assumes a sparse edge set; dense similarity links invert the equilibrium.
- F: `memory-trace/memory_trace/static/app.js`, `memory-trace/tests/test_lense.py`.
- T: Layout metrics before/after: minPairDistance 1 -> 23.2, pairsUnder25 327 -> 1, links
  thousands -> 370; 19 labels on 265 nodes; layout 427ms cached thereafter.

## 2026-07-10 20:58 - Goal pass: sidebar dropdowns, right-pane collapse, modern polish

```yaml
entry_id: mse_p8s3vqe1m6yjt4hk
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-trace-ui-pass
topics:
  - memory-trace
related_entries:
  - mse_h2xnk9wq5s0e7fmd
```

### Decision

- D: Final two units of the autonomous UI goal pass. (1) Sidebar rework: Agent/User facet row
  lists became count-annotated dropdown selects (one row of real estate each); sidebar content
  grouped into collapsible sections (Saved Views / Filters / Topics) with per-section open state
  persisted in localStorage; right reader pane is now collapsible from the topbar and auto-expands
  when the user explicitly selects an entry (feedback principle: a click always produces a visible
  response). (2) Modern polish: :focus-visible accent outlines on all controls; 120ms background
  transitions on interactive surfaces + 180ms pane-collapse grid animation, all disabled under
  prefers-reduced-motion; the search empty state now explains and offers "Clear filters"; "/"
  focuses search from anywhere (switching to the Search view first), Esc leaves the box.
- R: Goal directive: "collapsible / potentially rearrangeable panes, dropdown filters for
  efficient screen real estate usage" + the modern-ui-ux-essentials guidance (motion explains
  state; empty states offer a next action; keyboard shortcuts; accessibility built in).
  Rearrangeable (drag-reorder) panes deliberately deferred - disproportionate to the vanilla
  stack for a first pass; collapsible + resizable covers the stated need.
- F: `memory-trace/memory_trace/static/app.js`, `memory-trace/memory_trace/static/styles.css`.
- T: 38 memory-trace tests pass. Live checks: agent select applies filter and renders removable
  chip; sections toggle and persist; right pane collapse round-trips and auto-expands on entry
  click; "/" focuses the query box.

## 2026-07-10 21:15 - Trail relationship lanes: related/evolves/replaces left of main

```yaml
entry_id: mse_c6wdkt2zr9x0p5ba
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: claude-feature-trace-ui-pass
topics:
  - memory-trace
  - graph
related_entries:
  - mse_p8s3vqe1m6yjt4hk
```

### Decision

- D: Reworked Trail's relationship rendering per user testing feedback. Three always-dotted
  relationship lanes now sit left of main - related | evolves | replaces (strongest lifecycle
  signal closest to the trunk) - while lanes right of main remain the solid spawned git branches.
  Relationship edges route orthogonally through their type's lane (out from the source dot, along
  the lane, back in to the target dot, rounded corners) instead of free-form bezier bows, at
  higher visibility (width 2, opacity 0.95). `related` edges joined Trail's fixed edge set
  (previously excluded); "refines" label renamed to "evolves" to match the entry field name
  ("replaces" kept for supersedes per user wording). Rail width reacts to both zones so the text
  column shifts with parallel-branch count; the existing interval coloring already allocates
  3rd/4th/nth lanes when branches genuinely overlap in time.
- R: User testing direction: relationship lanes left of main, always dotted; branch lanes right;
  evolves named like the entry field; related entries should be viewable in a lane too. User also
  noted Trail functionally captures Timeline "but in a better way" - possible future retirement
  of the Timeline tab, not acted on.
- F: `memory-trace/memory_trace/static/app.js`, `memory-trace/memory_trace/static/index.html`,
  `memory-trace/tests/test_lense.py`.
- T: 38 tests pass. Live: 101 relationship routes in the 60-entry window (99 related, 2 evolves),
  legend reads replaces/evolves/related, "refines" absent from the DOM, rail width 88 with the
  relationship zone.
