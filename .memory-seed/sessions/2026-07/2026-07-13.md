---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-13
---

## 2026-07-13 00:13 - Clean remaining Codex worktrees

```yaml
entry_id: mse_992a3jfxfpnecpst
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: main
topics:
  - git-workflow
  - agent-collaboration
related_entries:
  - mse_kq3ba0cy9nkpqkm0
```

### Decision

- D: Cleaned the remaining Codex worktrees from this repository by removing the three clean `.codex/worktrees/` directories after Git deregistered them: `codex-docs-agent-worktree-namespace-guard`, `codex-memory-trace-extra`, and `codex-docs-trace-release-strategy-cleanup`.
- R: The first two were fully merged stale candidates, and the cleanup branch was clean with its useful docs already reconciled onto `main`; removing the worktree directories reduces stale-agent workspace confusion without deleting branch refs.
- A: `git worktree remove` deregistered the Codex worktrees but Windows denied directory and metadata deletion, so the leftover `.codex/worktrees/*` and `.git/worktrees/*` Codex directories were removed after verifying each absolute path stayed inside this repository. Claude worktrees and Claude metadata were left untouched.
- F: `.codex/worktrees/codex-docs-agent-worktree-namespace-guard`, `.codex/worktrees/codex-memory-trace-extra`, `.codex/worktrees/codex-docs-trace-release-strategy-cleanup`, `.git/worktrees/codex-docs-agent-worktree-namespace-guard`, `.git/worktrees/codex-memory-trace-extra`, `.git/worktrees/codex-docs-trace-release-strategy-cleanup`.
- T: `git worktree list --porcelain` now shows only the primary `main` worktree and the locked Claude worktree; `.codex/worktrees` is empty; `git status --short --branch` remains clean on `main`.

## 2026-07-13 00:18 - CHANGELOG + next-steps brief caught up to the post-2.17 span

```yaml
entry_id: mse_7yxwmt61t2vgfpjm
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - documentation
  - release
related_entries:
  - mse_p4fdqkjw62gtqabj
```

### Decision

- D: Brought the release-facing docs current with the post-2.17 span. CHANGELOG `## Unreleased`
  gained ten consolidated bullets covering everything shipped 2026-07-11/12 that only three
  bullets previously represented: the Trace UI pass (Trail primary, search-as-function,
  relationship zone), the versioned `/api/v1` contract + Phase-0 harnesses, the worktree switcher,
  commit-accurate Trail merges (+ phantom trunk, reader split, estimated fallback), Trail lane
  readability (color packs, time-ordered lanes, lane-following indentation), lifecycle-edge link
  sidecars + `link audit` + the end-of-turn sweep, canonical entry-id tooling, `session append` +
  `session reorder`, `memory-seed esr`, the `prepare-commit-msg` trailer hook, and serve-time asset
  versioning + `--static-root` + the browserless golden-fixture harness.
  `docs/2_Todo/0_NEXT_STEPS.md` updated (2026-07-13): a "Shipped since the goal run" block mirrors
  the changelog and carries the three follow-ups forward explicitly - `/api/v1` port of
  merges/lifecycle edges, MCP-graph sidecar-edge parity, and the declined-but-revisitable
  historical lifecycle backfill.
- R: The changelog gap was the blocker for any 2.18 cut: a release-sized feature set was invisible
  to the release notes, and the next-steps brief still read "goal run in progress" from 2026-07-10.
  Consolidated to changelog granularity (feature-level bullets), not session-log granularity.
- A: Stale-worktree removals were dropped from this pass - Codex performed them. No release cut or
  push is made here; both remain explicit user decisions (PyPI gate).
- F: CHANGELOG.md (Unreleased), docs/2_Todo/0_NEXT_STEPS.md.
- T: doc tests 19 passed (Unreleased-header guard included); full core suite 413 passed.

## 2026-07-13 00:34 - Trail polish: adjacent lifecycle hops + reader prose reflow

```yaml
entry_id: mse_0djqrdfpjwfaa3d2
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - memory-trace
related_entries:
  - mse_7yxwmt61t2vgfpjm
```

### Decision

- D: Two Trail reader/rail polish fixes from user feedback. (1) Adjacent lifecycle de-cluttering:
  a `supersedes`/`evolves` edge whose source and target sit on adjacent node rows (a day separator
  between them still counts) renders as a short direct hop beside the dots instead of an
  out-and-back route through the relationship zone - daisy-chained same-session iteration (each
  entry refining the previous) was stacking the zone into a thicket carrying no information the
  proximity didn't already give. Routed lanes are reserved for edges that span distance; same
  color/dash/arrow/tooltip and focus-dimming rules apply to both forms. (2) Reader prose reflow:
  `markdown()` now joins hard-wrapped continuation lines back into their logical block before
  rendering (`unwrapLines`; a line continues unless it starts a heading/bullet/table/quote/fence,
  fenced code preserved verbatim), so sentences adapt to the pane width instead of breaking at the
  authoring column.
- R: The 2026-07-12 session produced exactly the stress case: ~10 chained adjacent lifecycle edges
  (8 of 11 arcs now render as hops, 3 genuinely distant ones still route) and long hard-wrapped
  D/R/A/F/T bullets that displayed as ~15 fragment paragraphs (now 5 - one flowing block per
  label). Verified live against the worktree via the new `--static-root` serving path: a
  `memory-trace-worktree` launch configuration (port 8766) replaced the temp-copy dance on its
  first outing, and serve-time asset hashing meant no cache-bust bump.
- F: `memory-trace/memory_trace/static/app.js` (adjacentRows + short-hop arcs in `trailView`;
  `unwrapLines` + `markdown()` wiring), `.claude/launch.json` (memory-trace-worktree config).
- T: memory-trace suite 92 passed (trailModel untouched - golden fixture unaffected); DOM checks:
  8/11 arcs short-hop, reader bullet count 15-fragments -> 5 blocks; screenshot confirms both.

## 2026-07-13 00:52 - Doc coherence pass: control plane, specs, READMEs aligned with post-2.17 state

```yaml
entry_id: mse_s1an2zwd5mzty8d5
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - documentation
related_entries:
  - mse_7yxwmt61t2vgfpjm
```

### Decision

- D: Doc-coherence pass across the control plane, specs, and READMEs after a three-agent audit
  (README/control-plane roots; all skills + seed-twin parity; specs/reference) flagged 5 STALE and
  7 MINOR-DRIFT documents against the post-2.17 feature state. Fixed: root README (version strings
  2.13 -> 2.17, `memory_entry_id` added to the MCP tool list, six new CLI commands added to the
  command inventory, `prepare-commit-msg.py` added to the Agent Hooks section / seed tree / init
  list, links-check bullet gained the sidecar validators, Memory Trace section gained the Trail
  feature summary and `--static-root`); `agent-rules.md` (hand-write-trailer guidance replaced with
  the auto-stamping hook contract, `session append` named the canonical append path, End Of Turn
  now leads with `memory-seed esr`); `project-bootstrap.md` (entry-id step points at the
  generator); `index.md` (hooks topology, MCP tool list, Trace UI feature line, new consolidated
  design-decision bullet for the 07-12/13 batch, Active State follow-ups refreshed);
  `memory-trace/README.md` (options + Trail features); `history_retrieval.md` (memory_entry_id in
  authoring tools; stale ms- id examples -> mse_ format); `agent_collaboration.md` (branch
  auto-capture note); `lifecycle-edge-linking-sidecars.md` (Status -> IMPLEMENTED with as-built
  deviations); `graph-edge-contract.md` (sidecar-union addendum + MCP scope boundary + legacy
  merges/branches/merged_by surface row); `3_Spec/README.md` (lists the lifecycle spec, explains
  why it stays); `memory-trace-vanilla-parity-checklist.md` (time-ordered lanes, commit-accurate
  connectors, phantom trunk, short-hops, reader split/reflow, worktree switcher, serving items);
  `functionality-audit.md` (dated addendum pointing at CHANGELOG Unreleased);
  phase0-baseline README (browserless regen procedure). Four seed twins re-synced
  (agent-rules, project-bootstrap, history_retrieval, agent_collaboration).
- R: User asked whether control-plane and spec docs are coherent with the new project state; the
  audit showed the README still claimed 2.13 and none of the new tooling was documented anywhere
  discoverable. AGENTS.md, policy.md, the trail-search UX spec, the provenance contract, and all
  other skills were confirmed CURRENT.
- A: The agent-rules startup-contract guard (<=260 lines) rejected the first draft of the
  routine-append expansion; compressed to fit rather than raising the limit. Fan-out audit ran as
  three read-only subagents against a shared fact sheet.
- F: README.md, memory-trace/README.md, .memory-seed/agent-rules.md, .memory-seed/index.md,
  .memory-seed/project-bootstrap.md, .memory-seed/skills/history_retrieval.md,
  .memory-seed/skills/agent_collaboration.md, docs/3_Spec/lifecycle-edge-linking-sidecars.md,
  docs/3_Spec/graph-edge-contract.md, docs/3_Spec/README.md,
  docs/3_Spec/memory-trace-vanilla-parity-checklist.md, docs/3_Spec/functionality-audit.md,
  docs/4_Reference/memory-trace-phase0-baseline/README.md, + four seed twins.
- T: core suite 413 passed (incl. the 260-line agent-rules guard and skill-twin schema checks),
  memory-trace 92 passed, `memory-seed esr` seed-twins section OK.

## 2026-07-13 00:59 - Release 2.18.0 cut: version surfaces bumped, CHANGELOG folded, snapshot archived

```yaml
entry_id: mse_4tdzdds5v9jxje0r
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - release
related_entries:
  - mse_s1an2zwd5mzty8d5
```

### Decision

- D: Cut release 2.18.0 per the release_publishing runbook. Bumped every version surface together:
  `pyproject.toml` (2.18.0), `memory_seed/core.py` VERSION (2.18), the `memory-system-version:`
  frontmatter across 67 live+seed files (line-anchored replace; historical mentions untouched),
  the `get_version()` test pin, README version strings/pins, and index.md's Active State version
  lines. Folded CHANGELOG `## Unreleased` (13 bullets - the 2026-07-11/13 feature span) into
  `## 2.18.0 - 2026-07-13`, keeping the standing empty Unreleased header. Archived the
  control-plane snapshot to `.memory-seed/archive/2.18/` (agent-rules + project-bootstrap).
- R: User instruction after the doc-coherence pass cleared the release path. Two residual doc bugs
  surfaced during the bump: the `.github/copilot-instructions.md` pair was skipped by a ".git"
  substring exclusion in the bump script (the classic root-routing-file bump trap, caught by the
  frontmatter sweep re-check), and `session_logging.md` still instructed hand-writing Memory-Entry
  trailers on ordinary commits (the doc-audit had skipped that file as freshly updated) - both
  fixed, seed twin re-synced, and the corresponding CHANGELOG clause aligned.
- A: Publishing follows the runbook: GitHub Release only (no direct workflow dispatch); the pypi
  GitHub Environment holds the upload until the maintainer approves - the PyPI push is irreversible
  and stays user-gated.
- F: pyproject.toml, memory_seed/core.py, tests/test_memory_seed.py, CHANGELOG.md, README.md,
  .memory-seed/index.md, .memory-seed/archive/2.18/, 67 frontmatter files (live + seed),
  .memory-seed/skills/session_logging.md + seed twin.
- T: core suite 413 passed, memory-trace 92 passed, `memory-seed doctor` healthy, no
  `memory-system-version: 2.17` remains outside `.memory-seed/archive/`.

## 2026-07-13 01:06 - Create Codex session worktree

```yaml
entry_id: mse_0d1dae0t39y2t9mn
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/session-2026-07-13
topics:
  - git-workflow
  - agent-collaboration
related_entries:
  - mse_992a3jfxfpnecpst
```

### Decision

- D: Created a Codex-owned session worktree at `.codex/worktrees/session-2026-07-13` on branch `codex/session-2026-07-13`.
- R: This keeps Codex write work isolated from the root checkout and the locked Claude worktree, matching the repo's branch/worktree collaboration guardrails.
- A: No diagram sidecar was added; the topology is only the primary `main` checkout, the existing locked Claude worktree, and this single Codex worktree, so prose plus `git worktree list` output is clearer than a separate diagram.
- F: `.codex/worktrees/session-2026-07-13`, branch `codex/session-2026-07-13`.
- T: `git worktree list --porcelain` shows the new Codex worktree; `memory-seed branch status --json` reports a clean non-integration task branch with no warnings.

## 2026-07-13 01:07 - Release 2.18.0 live on PyPI; origin reconciled with PR #1

```yaml
entry_id: mse_hvv1yxce344xex7k
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - release
related_entries:
  - mse_4tdzdds5v9jxje0r
```

### Decision

- D: Release 2.18.0 published end-to-end: 65 local commits pushed to origin, GitHub Release
  v2.18.0 created from the folded CHANGELOG section (runbook path - no workflow dispatch), and the
  Publish-to-PyPI workflow completed (build + publish both green); PyPI now serves 2.18.0
  (wheel + sdist) as latest. During the push, origin was found one commit ahead: GitHub PR #1 had
  merged `codex-feature-memory-trace-extra` remotely - the same content already merged locally on
  2026-07-12 (`c711116`). The reconciliation merge was content-empty (0 files changed; histories
  joined), after which the push fast-forwarded cleanly.
- R: Completes the "Cut 2.18" instruction. Recorded separately from the cut entry because the
  push-time PR #1 divergence and the live-on-PyPI confirmation postdate it.
- A: Same-branch dual-merge (local merge + remote PR of one branch) is what created the
  divergence; harmless this time because content was identical, but pushing main more regularly
  would surface such PRs sooner.
- T: `gh run` 29214581039 completed/success (Build + Publish); PyPI JSON API reports latest
  2.18.0 with `memory_seed-2.18.0-py3-none-any.whl` and `memory_seed-2.18.0.tar.gz`.

## 2026-07-13 01:16 - Paired goal-run prompts: Trace surface (Claude) + core parity (Codex)

```yaml
entry_id: mse_myqzs5nwbr4mzcgr
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - proposal-lifecycle
  - agent-collaboration
related_entries:
  - mse_hvv1yxce344xex7k
```

### Decision

- D: Authored two paired goal-run prompts splitting the next-steps backlog by package boundary
  (user-approved split, focused-run scope). `docs/2_Todo/goal-run-trace-surface-claude.md`:
  Claude in the memory-trace-trail worktree ports the Trail merge/lifecycle surface to `/api/v1`
  (polish condition met after a release cycle), builds topics Phase 4 Trace rendering, and
  designs-with-the-user then implements decision-diagram sidecar integration in Trace views
  (fontjoy typography as stretch). `docs/2_Todo/goal-run-core-parity-codex.md`: Codex in its own
  `.codex/worktrees/` namespace closes the MCP-graph sidecar-edge scope boundary, builds the
  topics Phase 4 MCP tools (write surface gated on a user ask), fixes the fuse non-UTF-8
  silent-skip, and implements the agent-worktree-namespace-guard P1 (verifying on the way whether
  the deferred post-commit-hook P2 is obsoleted by the prepare-commit-msg hook).
- R: User asked for two meaningful goal prompts over the next-steps brief with a shared
  escalation contract: STOP-and-ask for major blockers/design decisions (each prompt enumerates
  its known ones), autonomous-but-recorded resolution for smaller concerns. Package-boundary
  split minimizes collision; both prompts encode the 2.18 workflow tooling (session append, esr,
  auto-stamped trailers, session merge-branch continuous integration, second-merger owns
  conflicts, CHANGELOG union rule, no pushes/releases).
- A: User rulings: focused runs (not full-backlog sweeps); related-entries P2 mutation writers
  held for a dedicated run; historical lifecycle backfill stays declined; continuous merge over
  held branches.
- F: docs/2_Todo/goal-run-trace-surface-claude.md, docs/2_Todo/goal-run-core-parity-codex.md.

## 2026-07-13 01:19 - Update Codex session worktree from main

```yaml
entry_id: mse_8jv73n6rqy9f3rp1
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/session-2026-07-13
topics:
  - git-workflow
  - session-logging
  - agent-collaboration
related_entries:
  - mse_0d1dae0t39y2t9mn
  - mse_myqzs5nwbr4mzcgr
```

### Decision

- D: Updated the Codex session worktree by merging local `main` into `codex/session-2026-07-13`, producing merge commit `d703371`.
- R: `main` had advanced by four commits after this worktree was created, including release/publication notes and paired goal-run prompts that this Codex branch needs before further work.
- A: The merge produced one session-log append conflict in `.memory-seed/sessions/2026-07/2026-07-13.md`; resolved it by preserving the Codex `01:06` setup entry followed by main's `01:07` and `01:16` entries in chronological order. No diagram sidecar was added because the topology is a routine one-branch update and the resolution is fully captured by the ordered session entries.
- F: `.memory-seed/sessions/2026-07/2026-07-13.md`, `docs/2_Todo/goal-run-core-parity-codex.md`, `docs/2_Todo/goal-run-trace-surface-claude.md`.
- T: `python -m memory_seed.cli links check` passed; `python -m memory_seed.cli topics check` passed with only known older four-topic warnings; `git diff --check` passed before the merge commit.

## 2026-07-13 01:38 - Trail merge geometry promoted to /api/v1 (merges/branches/merged_by)

```yaml
entry_id: mse_35symm8vj8mp4npf
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - memory-trace
  - graph
related_entries:
  - mse_myqzs5nwbr4mzcgr
evolves:
  - mse_p4tj8rw2mzvq6k1e
```

### Decision

- D: Promoted the commit-accurate Trail merge geometry onto the versioned `/api/v1/*` surface,
  retiring the "vanilla only, polish first" scope boundary now that the feature has survived a full
  release cycle (2.18). `models.py` gains `MergeEvent` (sha/short/date/subject/entry_ids),
  `ForkPoint` (sha/short/date - no subject, a fork point is a plain trunk commit), and `BranchInfo`
  (merge: `CommitInfo`|null, fork: `ForkPoint`|null, estimated); `GraphResponse`/`TrailResponse`
  gain `merges`/`branches`, `ChunkResponse` gains `merged_by: CommitInfo`|null. The v1 routes
  already called the shared `service.graph()`/`chunk()`, so no route logic changed - only the
  response_models widened to stop stripping the keys. Regenerated `openapi.v1.json` (23 schemas,
  +3) and `types.ts` from it.
- R: Purely additive - old v1 clients ignore new keys - so this does not breach the additive-only
  v1 rule and no STOP was needed. The service emits these keys unconditionally, so the models make
  them required (matching the module's "formalize the exact dict shapes" philosophy) rather than
  optional-with-defaults. Sidecar-sourced lifecycle edges reach v1 graph edges automatically - they
  are ordinary `supersedes`/`evolves` entries the versioned surface never stripped - pinned with a
  test rather than assumed. Evolves (not supersedes) `mse_p4tj8rw2mzvq6k1e`: the vanilla merge
  feature stays valid; this extends it to the versioned surface.
- A: The old `test_v1_contract_does_not_expose_the_new_keys` (which pinned the retired boundary) was
  replaced by `test_v1_contract_exposes_the_merge_geometry` asserting v1==legacy shapes with real
  trailer data; added `test_v1_graph_inherits_sidecar_lifecycle_edges` and a v1 no-git-fallback
  assertion. Left `lifecycle-edge-linking-sidecars.md`'s MCP-graph scope boundary untouched - that
  is Codex's concurrent item, and weakening it here would fabricate MCP parity that does not exist
  yet and hand Codex a merge conflict. `export_openapi.py`'s docstring shows `PYTHONPATH=memory-trace`
  (assumes an installed `memory_seed`); in the dev worktree both paths are needed, so I ran
  `PYTHONPATH=".;memory-trace"` and left the docstring (correct for the installed-package case it
  documents).
- F: `memory-trace/memory_trace/models.py`, `memory-trace/memory_trace/lense.py` (comment only),
  `memory-trace/tests/test_trail_merge_accuracy.py`, `memory-trace/tests/test_v1_api_contract.py`,
  `memory-trace/tests/test_link_sidecars.py`, `memory-trace/tests/contract/openapi.v1.json`,
  `memory-trace/tests/contract/types.ts`, `docs/3_Spec/graph-edge-contract.md`,
  `docs/2_Todo/0_NEXT_STEPS.md`, `CHANGELOG.md`.
- T: memory-trace 94 passed (+2); core 413 passed. Live at the API level against this worktree's
  real trailers (`create_app` + `TestClient`, not the browser - the vanilla UI consumes legacy
  `/api/*`, so a screenshot proves nothing for a v1 change): `/api/v1/graph` returned 50 real merge
  events + 30 branches with real fork/merge SHAs (`estimated=False` for trailer-era branches),
  `/api/v1/trail` carried both, and `/api/v1/chunks/mse_myqzs5nwbr4mzcgr` showed `merged_by=9438818`
  distinct from authoring commit `4ce2198`.

## 2026-07-13 02:06 - Trace renders indexed topics: prefer-indexed + vocabulary-aware filter + reader chips

```yaml
entry_id: mse_cfatzzv84kmp4k7v
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - memory-trace
  - graph
related_entries:
  - mse_myqzs5nwbr4mzcgr
  - mse_35symm8vj8mp4npf
evolves:
  - mse_ehm67mqpmsqm00md
```

### Decision

- D: Memory Trace now renders the authored controlled-vocabulary `topics:` field as first-class UI
  (topic-neighbourhoods plan, Phase 4 Trace half - Codex owns the MCP half). The single `_topics()`
  chokepoint in `lense.py` flips from a union (`topics | tags | contexts`) to
  prefer-indexed-with-fallback: an entry carrying any authored topic shows exactly its authored
  slugs, and only pre-vocabulary entries fall back to hashtag/heading derivation. That effective set
  cascades to the topics facet, node topics, the reader's new clickable `.topic-chip` row
  (`readerTopics`), the graph's `topic` chronological chains (already chained via `chain()` - no
  full-mesh work needed), and the filter. The topic filter is now vocabulary-aware: `_filter_chunks`
  takes `cwd` and expands the requested slug via the core `expand_topic_filter` (canonical + aliases,
  fail-open), so filtering by a canonical topic matches alias-stored entries and vice versa. Wired at
  all four `_filter_chunks` call sites (search x2, timeline, graph); `/api/v1` inherits it through the
  shared service (no model change - the topics field is already on the models).
- R: The contract already anticipated this - graph-edge-contract.md called the existing topic chains
  "display-only ... fallback" pending Phase 4. Prefer-over-union means filtering by a hashtag no
  longer surfaces entries that have adopted indexed topics - the intended Phase-4 semantics, recorded
  here. Kept scope tight per advisor guidance: NO indexed-vs-derived chip styling, NO vocabulary
  labels on chips (the authored slug is honest), NO facet canonical-normalization - all deferred as
  gold-plating that would double the item. Evolves `mse_ehm67mqpmsqm00md` (topics P1 built the
  vocabulary/parsing/CLI; this renders it in Trace, and P1 stays valid).
- A: The one-line `_topics()` flip had ~zero blast radius (the suite stayed green immediately)
  because the 48-entry synthetic corpus carries only authored topics with no competing hashtags, so
  union == prefer there and `trail-golden-48.json` is unchanged. Live-verification finding worth
  recording: the `memory-trace-worktree` launch config serves the worktree's STATIC assets
  (`--static-root`) over the PRIMARY checkout's Python backend (its `sys.path` is primary-relative),
  so it verifies frontend changes but runs pre-change backend code. So I split the check: the reader
  chips were verified live in the browser (render, topic-colour styling, click-to-filter setting
  `state.topic`), while the backend prefer-indexed + vocab-filter was verified in-process (TestClient
  on the worktree code against the real corpus + real `topics.yaml`) - filtering by canonical `graph`
  pulled in all three alias-only entries (related-entries/continuity/supersession) and filtering by
  the alias `related-entries` returned the identical 25-node set. Browser screenshots timed out (a
  renderer-pane issue, not the code), so I relied on DOM + functional JS assertions for the visual.
- F: `memory-trace/memory_trace/lense.py` (`_topics` prefer-indexed, `_filter_chunks` cwd+expand,
  four call sites, `expand_topic_filter` import), `memory-trace/memory_trace/static/app.js`
  (`readerTopics` + reader wiring), `memory-trace/memory_trace/static/styles.css` (`.topic-chip`),
  `memory-trace/tests/test_lense.py` (+2 tests), `docs/3_Spec/graph-edge-contract.md`,
  `docs/2_Todo/memory-trace-topic-neighbourhoods-plan.md`, `CHANGELOG.md`.
- T: memory-trace 96 passed (+2: indexed-preferred-over-derived-tags, vocabulary-aware
  canonical<->alias filter); core 413 passed. Frontend live-verified in the browser; backend
  verified in-process against the real vocabulary.

## 2026-07-13 06:28 - Decision-diagram badges + popover in Trail/Graph views

```yaml
entry_id: mse_gyrdzvd2wj3qhj51
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - memory-trace
  - mermaid
related_entries:
  - mse_myqzs5nwbr4mzcgr
  - mse_cfatzzv84kmp4k7v
evolves:
  - mse_j4wn8rqk2t6x0vhs
```

### Decision

- D: Surfaced Class-2 decision-diagram sidecars in the Trail and Graph views (they previously
  rendered only in the reader). USER-CHOSEN design via the mandatory item-3 design gate - "badge +
  popover preview", picked over indicator-only and inline-Trail-expansion: a small diamond badge
  marks Trail rows and Graph nodes whose entry carries a sidecar, driven by a new `has_diagram`
  boolean on the graph node (`set(entry_diagram_sidecars(cwd))` membership, read fresh per request).
  Clicking the badge floats the diagram in a popover rendered by the same built-in Arc 2d renderer as
  the reader; the diagram source is lazy-fetched from `/api/chunks/<id>` on demand, so the ~300-node
  graph payload stays a boolean. The badge handler runs before row/node selection so engaging it
  never also selects the entry; the popover closes on the x button, an outside click, Escape, or
  re-clicking the badge.
- R: `has_diagram` ships on the legacy `/api/*` surface only (the v1 `GraphNode` model strips it)
  until the badge UI is polished - matching the item-1 merge-geometry vanilla-first precedent and
  keeping the estimate honest. Lazy-fetch (rather than inlining every node's mermaid source) was
  chosen because 300+ nodes each carrying full diagram source would bloat every graph load for a
  feature engaged rarely. Inline Trail expansion was rejected WITH the user as too costly against the
  Trail's fixed-row interpRow/occupancy/lane geometry; indicator-only (open-reader) as too thin.
  Evolves `mse_j4wn8rqk2t6x0vhs` (the 2026-07-11 deferral that recorded these candidate shapes with
  scope to be designed with the user - now designed and built; the deferral stays valid as history).
- A: Same launch-config split as item 2 (the `memory-trace-worktree` config serves worktree static
  over the PRIMARY backend), so the live server can't emit `has_diagram`. Verified in two halves per
  the item-2 pattern: the backend flag in-process (TestClient on worktree code against the real
  corpus - 14 real-sidecar entries, all 14 nodes flagged, nothing spurious) plus a unit test; the
  frontend by injecting `has_diagram` into the loaded graph data in the browser - the badge rendered
  on Trail rows (a diamond glyph) and Graph nodes (an SVG diamond), clicking either opened the
  popover which rendered the real 5-node Arc 2d flowchart for `mse_3n3mp35ekz08t4zb`, and all four
  close paths (toggle / outside-click / Escape / x) fired. Browser screenshots timed out again (a
  renderer-pane issue, not the code), so the visual rests on DOM + functional JS assertions.
- F: `memory-trace/memory_trace/lense.py` (`has_diagram` on `_graph_node` + diagram-id set in
  `graph()`), `memory-trace/memory_trace/static/app.js` (badge on Trail rows + Graph nodes,
  `openDiagramPopover`/close/position helpers, click-handler wiring), `memory-trace/memory_trace/
  static/styles.css` (`.trail-diagram-badge`, `.graph-diagram-badge`, `.diagram-popover`),
  `memory-trace/tests/test_lense.py` (+1 test), `docs/2_Todo/session-decision-diagrams-plan.md`,
  `docs/3_Spec/graph-edge-contract.md`, `CHANGELOG.md`.
- T: memory-trace 97 passed (+1: `has_diagram` flagged from a real sidecar fixture, false without);
  core 413 passed. Frontend live-verified in the browser (badge render + popover + all close paths on
  both surfaces); backend verified in-process against the 14 real diagram sidecars.

## 2026-07-13 07:46 - Trace UI polish: worktree-switch subway loader + zoomable diagram viewer

```yaml
entry_id: mse_1xexde5bhezse690
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - memory-trace
  - ui-design
  - mermaid
evolves:
  - mse_wnnth4y2f6wt997j
  - mse_gyrdzvd2wj3qhj51
```

### Summary

- Two user-requested Trace UI improvements (worktree-switch loader; zoomable diagram viewer), plus a
  renderer fix the larger viewer exposed. All pure-frontend.

### Decisions

#### D1 - Worktree-switch subway loader

- D: Worktree switching now shows a subway-map loading animation. `switchWorktree` renders a loader
  (`worktreeLoader`, via `centerPane`) BEFORE its awaits - it previously called `render()` only at the
  end - then advances `data-stage` 0->1->2 imperatively at the real fetch boundaries (leaving the
  platform -> reading branch memory -> arriving), so stations light up as genuine milestones. The
  train car slides station to station on a CSS transition, the travelled track lights up behind it,
  and a halo pulses between stops; a ~650ms minimum display keeps a warm-cache switch from flashing.
  Honours `prefers-reduced-motion`.
- R: User request. The old flow froze the previous view through the whole switch (`render()` only ran
  at the end), which read as a hang. Echoing the Trail's own vocabulary (vertical line, circular
  stations, accent lane colour) keeps it in the design language - "train of thought" made literal.
- F: `memory-trace/memory_trace/static/app.js` (`worktreeLoader` + `switchWorktree` loader wiring +
  `setWorktreeStage`), `memory-trace/memory_trace/static/styles.css` (`.worktree-loader` / `.wt-*`).
- T: Live-verified: a real worktree switch shows the loader mid-switch with `data-stage` advancing to
  the final station (track fully lit `dashoffset 0`, train at `translateY(210)`, halo pulsing), then
  clears with the trail rendered.

#### D2 - Zoomable diagram viewer (replaces the popover)

- D: The decision-diagram badge (and reader diagrams) now open a large centred, zoomable/pannable
  viewer (`openDiagramViewer` + `initDiagramPanZoom`) instead of the 420px anchored popover. Wheel
  zooms toward the cursor (preventDefault so the panel doesn't scroll), drag pans (pointer capture, so
  no leaked window listeners and no drag-release toggling it shut), and a minus/Fit/plus bar gives
  button control; closes on the x, a backdrop click, Escape, or re-trigger. Reader diagram figures are
  clickable into the same viewer. The chunk fetch is now worktree-scoped (via `api()`, cache keyed by
  worktree+entry).
- R: User request - the 420px preview was too small to inspect a real decision flow, and zooming
  inside a 420px box would not fix "too small on screen"; the root cause was the cap, so the fix is a
  genuinely larger surface plus zoom/pan. Worktree-scoping the fetch also corrects a latent bug (the
  old raw fetch always hit the server's default checkout).
- F: `memory-trace/memory_trace/static/app.js` (`openDiagramViewer`/`initDiagramPanZoom` replacing the
  popover; reader figures openable), `memory-trace/memory_trace/static/styles.css` (`.diagram-viewer*`
  replacing `.diagram-popover*`).
- T: Live-verified: opens at 880px vs the old 420px; wheel/button zoom + pointer pan + Fit all move the
  transform correctly; all four close paths fire; wheel `preventDefault` honoured. No console errors.

#### D3 - Arc 2d flowchart renderer honours `<br/>`

- D: Fixed `renderFlowchart` to split node labels on mermaid's `<br/>`, render multi-line `<tspan>`s,
  and size the uniform box to the widest line + tallest label.
- R: The larger viewer (D2) exposed a latent renderer gap: labels ignored `<br/>` and never wrapped,
  so long labels overflowed their box - invisible at thumbnail size, glaring full-size. Fixing it is
  what makes the zoomable inspector actually usable.
- F: `memory-trace/memory_trace/static/app.js` (`renderFlowchart` `<br/>` split + box sizing).
- T: All five nodes render 2-3 `<tspan>`s with no literal `<br/>`; a screenshot confirms labels sit
  cleanly inside their boxes.

### Validation

- memory-trace 97 + core 413 green (unchanged - all three changes are pure-frontend, no Python
  touched); everything else verified live in the browser as noted per decision.

## 2026-07-13 08:11 - Diagnose pytest stall and harden Windows hook validation

```yaml
entry_id: mse_nh1fykr5ae6kbp2y
user_initials: JNL
agent_type: codex
agent_name: developer
project_path: .
subproject_path: null
branch: codex/session-2026-07-13
topics:
  - bugfix
  - mcp-tools
  - hooks
related_entries:
  - mse_myqzs5nwbr4mzcgr
```

### Summary

- Diagnosed and fixed the stalled pytest path before continuing the MCP sidecar-edge work.
- Captured the developer-persona lesson that suspicious validation behavior must be diagnosed and fixed, not bypassed.

### Decisions

#### D1 - Disable pytest cacheprovider for project validation

- D: Added project pytest config disabling pytest's `cacheprovider` plugin by default.
- R: Bounded reproductions showed pytest itself worked outside the repo, and project tests passed under `unittest`, but pytest hung after printing a passing test marker whenever the repo root was selected and the cache provider was active. `-p no:cacheprovider` made the same project tests exit normally; `--cache-clear` did not.
- A: Plugin autoload and the `anyio` plugin were tested and rejected as causes; disabling all plugin autoload still hung while disabling `cacheprovider` fixed the project test path.
- F: `pyproject.toml`.
- T: `python -m pytest tests/test_mcp_server.py -q` passed; full core suite `python -m pytest tests -q` completed without hanging.

#### D2 - Use absolute-Python git hook shim on Windows

- D: Changed `install_git_hooks()` to write an absolute-Python `prepare-commit-msg` wrapper on Windows while retaining the shell shim on non-Windows platforms.
- R: Full-suite validation exposed real hook failures: Git for Windows failed to start `sh.exe` with `CreateFileMapping ... Win32 error 5`, so the shell shim never invoked the repo-tracked hook. A direct absolute-Python hook ran and stamped `Memory-Entry:` trailers correctly.
- A: `#!/usr/bin/env python` was tested and rejected because Git still invoked `env.exe`, which failed with the same Windows shared-memory error.
- F: `memory_seed/core.py`.
- T: `python -m pytest tests/test_git_hooks.py -q` passed; full core suite passed.

#### D3 - Canonical fixture IDs and persona hardening

- D: Replaced invented sidecar fixture IDs with generated `mse_` IDs and updated the developer persona to require diagnosing stalled validation and using canonical project generators for schema-bound IDs.
- R: The invalid `ms-oldcache0`-style IDs were not valid legacy refs, and recent Claude entries documented `memory_entry_id` / `session entry-id` specifically to prevent this drift.
- F: `tests/test_mcp_server.py`, `.agents/developer.md`, `.AGENTS/developer.md`.
- T: Targeted MCP sidecar tests passed under pytest; Memory Trace suite passed with `PYTHONPATH=.;memory-trace`.

### Validation

- `python -m pytest tests/test_mcp_server.py -q` -> 38 passed.
- `python -m pytest tests/test_git_hooks.py -q` -> 7 passed.
- `python -m pytest tests -q` -> 414 passed, 1 skipped, 8 subtests passed.
- `PYTHONPATH=.;memory-trace python -m pytest memory-trace/tests -q` via subprocess wrapper -> 92 passed.

## 2026-07-13 08:45 - Harden Memory-Entry hook status and repair

```yaml
entry_id: mse_x8t3msqrjns07210
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/session-2026-07-13
topics:
  - hooks
  - git-workflow
  - session-logging
```

### Decision

- D: Hardened Memory-Entry trailer hook management so status/repair can diagnose current, missing, stale, broken-python, and foreign states without overwriting user hooks.
- R: Commit trailers are core project provenance, and Windows Git shell startup can fail before the repo-tracked Python hook runs. The lower-friction path is to install a managed wrapper into the git common dir, use an absolute-Python wrapper on Windows, and expose status/repair so agents and users can verify it.
- A: Considered bypassing pytest and git hook failures after the code path worked under unittest/direct script execution; rejected because the hang/failure could affect package users and should be diagnosed as a product risk.
- F: memory_seed/core.py; memory_seed/cli.py; tests/test_git_hooks.py; pyproject.toml; README.md; docs/3_Spec/functionality-audit.md; CHANGELOG.md
- T: python -m pytest tests/test_git_hooks.py -q -> 10 passed; python -m pytest tests/test_mcp_server.py -q -> 38 passed; python -m pytest tests -q -> 417 passed, 1 skipped; PYTHONPATH=.;memory-trace python -m pytest memory-trace/tests -q -> 92 passed; python -m memory_seed.cli hooks status --json -> current; python -m memory_seed.cli doctor -> healthy; git diff --check -> clean.

## 2026-07-13 10:45 - Complete MCP sidecar-edge parity

```yaml
entry_id: mse_z9aqe69r95ezvc66
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/session-2026-07-13
topics:
  - mcp-tools
  - graph
  - documentation
```

### Decision

- D: Closed the MCP graph sidecar-edge parity gap for lifecycle link sidecars.
- R: `sessions/links/YYYY-MM/YYYY-MM-DD.md` sidecars are part of the effective graph contract, so MCP tools should not remain YAML-only while Trace and `links check` see union(YAML, sidecar). The shared merge point belongs in `memory_seed.retrieval`, which already depends on `semantic_cache`; putting it there avoids a reverse import from `semantic_cache` into retrieval.
- A: Reverted an out-of-scope Memory Trace cleanup edit so this Codex item stayed inside the core/control-plane package boundary assigned by the goal prompt. ESR produced lifecycle-link suggestions for current-day entries, but no sidecar was written because the sweep requires user approval.
- F: memory_seed/retrieval.py; memory_seed/mcp_server.py; tests/test_mcp_server.py; docs/3_Spec/lifecycle-edge-linking-sidecars.md; docs/3_Spec/graph-edge-contract.md
- T: python -m pytest tests/test_mcp_server.py -q -> 38 passed; python -m pytest tests/test_git_hooks.py -q -> 10 passed; python -m pytest tests -q -> 417 passed, 1 skipped; PYTHONPATH=.;memory-trace python -m pytest memory-trace/tests -q -> 92 passed; python -m memory_seed.cli links check -> OK; python -m memory_seed.cli topics check -> OK with existing topic-count warnings on older entries; python -m memory_seed.cli doctor -> healthy; python -m memory_seed.cli esr -> hard checks OK; git diff --check -> clean.

## 2026-07-13 10:58 - Add MCP topic-management tools

```yaml
entry_id: mse_a967rp2frx5tf4qp
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/session-2026-07-13
topics:
  - mcp-tools
  - session-logging
  - graph
```

### Decision

- D: Added read-only MCP topic-management tools for the controlled topic vocabulary.
- R: Phase 4 needed LLM-accessible topic operations after the CLI semantics stabilized. The MCP layer now mirrors the safe parts of `memory-seed topics list` and `topics check` while adding single-topic inspection for alias resolution and entry usage. No MCP write surface was added because `.memory-seed/topics.yaml` is deploy-once project-curated state that requires user approval to edit.
- A: Considered stopping at list/check parity only, but added `memory_topic_inspect` because the plan explicitly called for list/inspect behavior and agents need an easy way to resolve aliases before choosing entry topics. Deferred `topics suggest --from <file>` because it remains optional and needs its own scoring/design pass.
- F: memory_seed/mcp_server.py; tests/test_mcp_server.py; README.md; docs/2_Todo/memory-trace-topic-neighbourhoods-plan.md; docs/3_Spec/functionality-audit.md; .memory-seed/skills/index.md; memory_seed/seed/.memory-seed/skills/index.md; CHANGELOG.md
- T: python -m pytest tests/test_mcp_server.py -q -> 41 passed; python -m pytest tests/test_topics.py -q -> 8 passed; python -m pytest tests -q -> 420 passed, 1 skipped; python -m memory_seed.cli links check -> OK; python -m memory_seed.cli topics check -> OK with existing topic-count warnings on older entries; python -m memory_seed.cli doctor -> healthy; python -m memory_seed.cli esr -> hard checks OK and seed twins OK; git diff --check -> clean.

## 2026-07-13 11:09 - Block non-UTF-8 branch session fuse files

```yaml
entry_id: mse_004tgff7q4hxrjg6
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/session-2026-07-13
topics:
  - session-fuse
  - windows-encoding
  - bugfix
```

### Decision

- D: Fixed `session fuse` so changed branch session/diagram files that cannot decode as UTF-8 block with a named issue instead of being skipped.
- R: Fuse is a promotion gate; silently omitting a branch-local memory file is worse than failing closed. `_git_show_text` now captures raw git-show bytes and decodes explicitly, letting missing blobs remain non-blocking while invalid UTF-8 returns a sentinel that branch-delta enumeration turns into `could not decode <path> as UTF-8`.
- A: First implementation relied on `subprocess.run(text=True)` raising `UnicodeDecodeError`, but the regression test exposed a Windows reader-thread warning path where the decode failure did not reliably become the sentinel. Switched to byte capture plus explicit decode to make the failure deterministic.
- F: memory_seed/core.py; tests/test_memory_seed.py; docs/2_Todo/completed/residual-fuse-non-utf8-silent-skip.md; docs/2_Todo/0_NEXT_STEPS.md; docs/3_Spec/functionality-audit.md; CHANGELOG.md
- T: python -m pytest tests/test_memory_seed.py -q -k session_fuse -> 15 passed; python -m pytest tests -q -> 421 passed, 1 skipped; python -m memory_seed.cli links check -> OK; python -m memory_seed.cli topics check -> OK with existing topic-count warnings on older entries; python -m memory_seed.cli doctor -> healthy; python -m memory_seed.cli esr -> hard checks OK and seed twins OK; git diff --check -> clean.

## 2026-07-13 11:47 - OpenSSF credibility + configurable integration-mode proposals (approved)

```yaml
entry_id: mse_ex23fj34b49zn9ta
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: feature-integration-mode
topics:
  - proposal-lifecycle
  - git-workflow
  - control-plane
```

### Decision

- D: Designed and got user approval for two linked proposals, and started implementation on branch
  `feature-integration-mode`. (1) OpenSSF credibility (`openssf-credibility-proposals.md`): four
  tracks - Best Practices Badge (passing), Scorecard, Sigstore signed-provenance, SLSA Build L3 - on
  a shared `SECURITY.md` + CI foundation, adopting a PR/branch-protection workflow. Alpha-Omega and
  GUAC/package-analysis/Criticality-Score/S2C2F ruled out of scope (funding for already-critical
  projects; consumer tooling, not earned badges). (2) Configurable integration mode
  (`configurable-integration-mode-plan.md`): a declared project-local `integration_mode:
  local-merge | pr` in `project.yaml` (default `local-merge`), formalising the currently-implicit
  per-task `integration_artifact` choice into a project default. The OpenSSF PR-flow step (G2)
  consumes it - this repo declares `pr` and everything follows.
- R: User asked which OpenSSF programs improve credibility, then to propose all four and gate the
  implementation plan. User raised the workflow-choice idea (solo vs team) and I agreed it fits: the
  codebase already treats PR systems as "optional integration layers" and carries a per-task
  `integration_artifact`. Rulings: simple enum to start (`local-merge | pr`); declare, don't
  auto-detect (bootstrap *suggests* on team signals - branch protection, >1 collaborator, existing
  PRs - human confirms); default `local-merge` (backward-compatible, unset behaves as today); a
  declared `pr` mode IS the standing push authorization (force-push/destructive stay gated).
  Sequenced integration-mode first (Phases 1-3), then OpenSSF; land phase by phase with checkpoints.
- A: Key design risk recorded for `pr` mode: the fuse's chronology-repair + trailer-stamping happens
  DURING the local merge, but in `pr` mode the host performs the merge, so a naive push-and-PR risks
  interleaved/out-of-order session entries. Resolution: **prepare the branch** - lay entries into
  correct chronological order as commits on the branch, up to date with `main`, so the host merge is
  trivially clean; branch-prep is the main implementation risk, to prototype first. Rejected:
  post-merge CI repair on `main` (heavier, needs CI write access); auto-detecting solo-vs-team
  (fragile); a richer enum now (`patch`/`handoff` deferred).
- F: `docs/2_Todo/openssf-credibility-proposals.md`, `docs/2_Todo/configurable-integration-mode-plan.md`
  (both new). Implementation begins under Phase 0.1 (the `integration_mode` setting + reader).
- T: none (proposals/design only; implementation + tests follow per phase).

## 2026-07-13 11:54 - P0.1: integration_mode setting + core reader + esr surfacing

```yaml
entry_id: mse_t388apx6evvmmd1r
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: feature-integration-mode
topics:
  - control-plane
  - git-workflow
related_entries:
  - mse_ex23fj34b49zn9ta
```

### Decision

- D: Implemented P0.1 of the configurable integration-mode feature: the setting + core reader +
  `esr` surfacing. `memory_seed/core.py` gains `INTEGRATION_MODES = ("local-merge", "pr")`,
  `DEFAULT_INTEGRATION_MODE = "local-merge"`, and `read_integration_mode(target_root)` - a fail-open
  stdlib line-scanner (same style as `read_project_agents`/`read_project_participants`, no YAML dep)
  reading a top-level `integration_mode:` scalar from `.memory-seed/project.yaml`. `memory_seed/esr.py`
  carries the active mode on `EsrReport` (+ `to_dict`) and prints a new `## Integration mode` section.
- R: Fail-open to `local-merge` on an absent file/key, an unreadable file, or an unrecognised value
  keeps every legacy/unconfigured project behaving exactly as today - the field is purely additive.
  Surfacing the mode each preflight makes it visible which integration flow is in effect
  (local-merge = `session merge-branch` into local main, no push; pr = push + PR). This is the
  foundation the OpenSSF PR-flow step consumes (declares `pr`).
- F: `memory_seed/core.py` (constants + `read_integration_mode`), `memory_seed/esr.py`
  (`EsrReport.integration_mode` + `to_dict` + `## Integration mode` section + `read_integration_mode`
  wiring), `tests/test_memory_seed.py` (+1 reader test), `tests/test_esr.py` (+1 surfacing test,
  section-presence list extended).
- T: core 424 passed. Reader test covers absent-file/absent-key/pr/local-merge/garbage-fail-open/
  quoted; esr test covers the default and a declared `pr`. The `esr` CLI on this repo (no field set)
  prints "Integration mode: local-merge — integrate via session merge-branch … no push."

## 2026-07-13 14:07 - Deterministic entry DRAFT-format lint (enforced at write + audit)

```yaml
entry_id: mse_2b1bthj8x03nfe91
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: feature-integration-mode
topics:
  - control-plane
  - session-logging
related_entries:
  - mse_1xexde5bhezse690
```

### Decision

- D: Built a deterministic entry DRAFT-format lint and enforced it at write time and audit time.
  `core.entry_body_format_issues(body)` + `check_entry_format(text)` flag bare `D:`/`R:` labels that
  are not `- ` list items, DRAFT prose with no `### Decision`/`### Summary` heading, several decisions
  crammed under a singular `### Decision`, and a `D:` with no `R:`. `session append` now rejects a
  malformed body before writing (teach-at-failure message naming the fix); `links check` reports the
  same corpus-wide as `malformed-entry-format`, surfaced by `esr` and merge-blocking once CI runs it.
  It ships in core, so every downstream project inherits the guard. Reformatted the six historical
  malformed entries to the templates (four Codex bare-label entries, `mse_1xexde5bhezse690` and
  `ms-a939b6b4` multi-decision-under-singular).
- R: A correct template already lived in `session_logging.md`, but drift recurred because nothing
  enforced it - the write path took the body verbatim and no validator checked format. Enforcement
  must sit at the choke points every entry passes (the write, then commit/audit), not agent memory.
  Structural-only (it never judges one-vs-many decisions) keeps precision high: 6/348 real-corpus
  entries flag with no false positives once the readable `### Decisions` + `- D1:` + nested `- R:`
  style is excluded (that would have wrongly condemned `ms-0c929026`).
- A: A warning was considered and rejected - only six entries flag corpus-wide, all fixable, so a
  hard error is safe and genuinely "not overlooked." Gating in MCP was rejected: MCP is read-only by
  design and never writes session files, so the write-time gate belongs on the CLI (`session
  append`). Notable finding: the `session merge-branch`/fuse append-only guard correctly BLOCKS
  modifying an existing entry, so fixing malformed historical entries cannot go through the fuse -
  this branch lands via a plain `--no-ff` merge instead.
- F: `memory_seed/core.py` (`entry_body_format_issues`, `check_entry_format`, links-check wire,
  session-append gate); `tests/test_memory_seed.py`, `tests/test_session_append.py`,
  `tests/test_retrieval.py` (fixture given a reason); six reformatted session entries;
  `.memory-seed/skills/session_logging.md` (+ seed twin); `docs/3_Spec/functionality-audit.md`;
  `CHANGELOG.md`.
- T: core 427 passed (+ format-lint unit tests, links-check integration, session-append reject; two
  retrieval links-check fixtures repaired). Corpus format scan clean (0/348 malformed); `links check`
  OK; `esr` twins OK.
- Follow-up: a blocking git commit hook running the lint; `session append` decision-scaffolding flags
  (prevention by construction); a read-only `memory_entry_format_preview` MCP tool; the CI merge-gate
  rides the OpenSSF CI work.

## 2026-07-13 14:49 - Proposal: proactive consult-memory-for-why discipline (portable, all-agent)

```yaml
entry_id: mse_3114hmpcsyz9ftp1
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: feature-integration-mode
topics:
  - control-plane
  - retrieval
related_entries:
  - mse_hejpp9whj0nn8jyy
```

### Decision

- D: Created a proposal to make the "consult memory for the *why* before a design/change" discipline
  a portable, all-agent control-plane behavior
  (`docs/2_Todo/proactive-history-retrieval-discipline-proposal.md`). Layered design: sharpen
  `agent-rules.md` Working Principles into a *proactive* trigger (folded within the line budget), add
  the before-design/change trigger + a why-vs-current-state note to `history_retrieval.md`, sharpen the
  developer persona, sync all seed twins (the portability fix), and retire the Claude-personal feedback
  note. Not a new skill.
- R: User pointed out the behavior only lived in Claude-Code personal memory - invisible to other
  agents and not shipped in the seed. Dogfooding the very behavior (`memory_search` + reading
  `agent-rules.md`) showed the rule already *partly* exists (History Retrieval + Recency-vs-Topical:
  files = current authority, memory = reason) but is passive/reactive and under-heeded, and surfaced
  the settled decision `mse_hejpp9whj0nn8jyy` ("Working Principles over a new lazy skill"). So the
  proposal sharpens + makes portable rather than adds from scratch - a materially better shape than
  writing agent-rules from a blank page would have produced.
- A: A new lazy skill was rejected (superseded by the Working-Principles decision; skills are
  lazy-loaded and would be missed at the decision point). A hard tool-gate on agent reasoning was kept
  a non-goal (a search cannot be forced); enforcement is contract + trigger + each host's retrieval
  reminder, with an optional pre-risky-op hook nudge as a follow-on. Editing `agent-rules.md` (a locked
  control-plane file, within its line-budget test) is deferred to a separate approved implementation -
  this entry only creates the proposal.
- F: `docs/2_Todo/proactive-history-retrieval-discipline-proposal.md` (new).
- T: none (proposal only).
