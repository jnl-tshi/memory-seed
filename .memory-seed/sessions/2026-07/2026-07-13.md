## 2026-07-13 00:18 - CHANGELOG + next-steps brief caught up to the post-2.17 span

```yaml
entry_id: mse_7yxwmt61t2vgfpjm
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
branch: worktree-memory-trace-trail
topics:
  - documentation
  - release
related_entries:
  - mse_p4fdqkjw62gtqabj
```

### Decision

- D: Brought the release-facing docs current with the post-2.17 span. CHANGELOG `## Unreleased`
  gained ten consolidated bullets covering everything shipped 2026-07-11/12 that only three
  bullets previously represented: the Trace UI pass (Trail primary, search-as-function,
  relationship zone), the versioned `/api/v1` contract + Phase-0 harnesses, the worktree switcher,
  commit-accurate Trail merges (+ phantom trunk, reader split, estimated fallback), Trail lane
  readability (color packs, time-ordered lanes, lane-following indentation), lifecycle-edge link
  sidecars + `link audit` + the end-of-turn sweep, canonical entry-id tooling, `session append` +
  `session reorder`, `memory-seed esr`, the `prepare-commit-msg` trailer hook, and serve-time asset
  versioning + `--static-root` + the browserless golden-fixture harness.
  `docs/2_Todo/0_NEXT_STEPS.md` updated (2026-07-13): a "Shipped since the goal run" block mirrors
  the changelog and carries the three follow-ups forward explicitly - `/api/v1` port of
  merges/lifecycle edges, MCP-graph sidecar-edge parity, and the declined-but-revisitable
  historical lifecycle backfill.
- R: The changelog gap was the blocker for any 2.18 cut: a release-sized feature set was invisible
  to the release notes, and the next-steps brief still read "goal run in progress" from 2026-07-10.
  Consolidated to changelog granularity (feature-level bullets), not session-log granularity.
- A: Stale-worktree removals were dropped from this pass - Codex performed them. No release cut or
  push is made here; both remain explicit user decisions (PyPI gate).
- F: CHANGELOG.md (Unreleased), docs/2_Todo/0_NEXT_STEPS.md.
- T: doc tests 19 passed (Unreleased-header guard included); full core suite 413 passed.
