---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-31
---

## 2026-07-31 11:56 - Harden Trace project access and decision-sidecar recovery

```yaml
entry_id: mse_d06t9bccm3yykfqs
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/fix/review-remediation
evolves:
  - d1 -> mse_qbp1ndbnhezj34eb
  - d2 -> mse_rg1ghmczrxxjwxcv:d1
```

### Summary

- Remediated the two blocking review findings in the decision-sidecar writer and Memory Trace external-project routes.

### Decisions

#### D1 - Publish the parent before decision sidecars

- D: Stage the journal, append the canonical parent entry, then publish topic and lifecycle sidecars.
- R: An interruption can now leave only an incomplete enrichment, never a durable sidecar that references an absent entry and breaks link validation or readers.
- A: Keeping sidecars first and teaching every consumer to interpret pending journals was rejected because it multiplies recovery-state handling across the read path.
- F: `memory_seed/core.py`; `tests/test_session_append.py`.
- T: `python -m unittest discover -s tests -p "test_session_append.py"` (36 passed).

#### D2 - Limit filesystem project discovery to loopback Trace servers

- D: Default external-project access to disabled, enabling it only when the CLI host is loopback.
- R: The folder picker reads the server filesystem and registers new corpora for existing worktree-scoped routes; network-facing Trace instances must not expose that capability without authentication.
- A: Relying on the CLI default host was rejected because `--host 0.0.0.0` is supported and lacks authentication.
- F: `memory-trace/memory_trace/service.py`; `memory-trace/tests/test_project_open.py`; `memory-trace/tests/test_service.py`; `memory-trace/README.md`.
- T: `python -m unittest discover -s memory-trace/tests -p "test_service.py"` (60 passed); `python -m unittest discover -s memory-trace/tests -p "test_project_open.py"` (8 passed); `git diff --check` passed.
