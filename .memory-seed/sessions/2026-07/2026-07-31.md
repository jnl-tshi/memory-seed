---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-31
---

## 2026-07-31 11:56 - Harden Trace project access and decision-sidecar recovery

```yaml
entry_id: mse_d06t9bccm3yykfqs
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/fix/review-remediation
evolves:
  - d1 -> mse_qbp1ndbnhezj34eb
  - d2 -> mse_rg1ghmczrxxjwxcv:d1
```

### Summary

- Remediated the two blocking review findings in the decision-sidecar writer and Memory Trace external-project routes.

### Decisions

#### D1 - Publish the parent before decision sidecars

- D: Stage the journal, append the canonical parent entry, then publish topic and lifecycle sidecars.
- R: An interruption can now leave only an incomplete enrichment, never a durable sidecar that references an absent entry and breaks link validation or readers.
- A: Keeping sidecars first and teaching every consumer to interpret pending journals was rejected because it multiplies recovery-state handling across the read path.
- F: `memory_seed/core.py`; `tests/test_session_append.py`.
- T: `python -m unittest discover -s tests -p "test_session_append.py"` (36 passed).

#### D2 - Limit filesystem project discovery to loopback Trace servers

- D: Default external-project access to disabled, enabling it only when the CLI host is loopback.
- R: The folder picker reads the server filesystem and registers new corpora for existing worktree-scoped routes; network-facing Trace instances must not expose that capability without authentication.
- A: Relying on the CLI default host was rejected because `--host 0.0.0.0` is supported and lacks authentication.
- F: `memory-trace/memory_trace/service.py`; `memory-trace/tests/test_project_open.py`; `memory-trace/tests/test_service.py`; `memory-trace/README.md`.
- T: `python -m unittest discover -s memory-trace/tests -p "test_service.py"` (60 passed); `python -m unittest discover -s memory-trace/tests -p "test_project_open.py"` (8 passed); `git diff --check` passed.

## 2026-07-31 14:21 - Simplify the multi-decision Inspector

```yaml
entry_id: mse_r6tyarbkzz18st4w
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/fix/lightweight-inspector
evolves:
  - mse_qb6yh6m836ybvprf
```

### Summary

- Simplified the Memory Trace Inspector so a canonical entry reads as one calm, segmented document instead of a metadata-heavy active-decision card.

### Decisions

#### D1 - Keep information-state grammar off canonical DRAFT content

- D: Treat every authored DRAFT field as recorded by definition; apply recorded, derived, or suggested/inferred origin only to sidecar and projected information whose origin can vary.
- R: A large Recorded badge restated a property shared by every DRAFT entry and made the selected decision appear more authoritative than its siblings. Typed-link arrows and border patterns already communicate authored references versus backlinks, while technical provenance remains available under Entry details.
- A: Retaining the entry-wide Recorded/Derived/Suggested badge was rejected because it classified the whole selected decision from projection metadata and crowded the core reading surface.

#### D2 - Read multi-decision entries through one equal-weight decision window

- D: Present Summary and supporting entry sections once, place all decisions in one bounded window, and use an equal-weight D1/D2 selector to scroll to a decision without hiding its siblings. Expand the window into normal flow in bottom-docked and narrow layouts.
- R: The entry remains the context, every decision keeps equal visual weight, and the selector provides fast navigation without repeating the full session body. Exact Markdown stays one quiet action away and technical metadata is collapsed by default.
- A: Separate active-decision cards, a selector that swaps or hides sibling decisions, and metadata-first reading were rejected because each makes the Inspector denser or overstates the selected decision.
- F: `memory-trace/client/src/App.tsx`; `memory-trace/client/src/EntryReader.tsx`; `memory-trace/client/src/decisionReaderModel.ts`; `memory-trace/client/src/decisionReaderModel.test.ts`; `memory-trace/client/src/styles.css`; `docs/2_Todo/memory-trace-ux-m0-interaction-matrix.md`; `docs/2_Todo/memory-trace-ux-reference-model-implementation-plan.md`; `docs/2_Todo/memory-trace-next-generation-coverage-matrix.md`; `docs/README.md`; packaged output under `memory-trace/memory_trace/static/react/`.
- T: `npm test` passed 235 tests; `npm run typecheck` and `npm run build` passed; desktop and 760px browser checks passed with no console errors; `memory-seed links check` passed. `memory-seed docs check` retains two pre-existing lifecycle errors.

## 2026-07-31 14:56 - Reconcile the next-steps plan after Inspector refinement

```yaml
entry_id: mse_532mkqzsg993cxrb
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/docs/reconcile-next-steps-inspector
related_entries:
  - mse_r6tyarbkzz18st4w
```

### Summary

- Reconciled the active next-steps plan with the delivered Memory Trace M1/M2 work and the 2026-07-31 Inspector refinement. M3 bounded graph perspectives and controlled expansion is now explicit as the lead slice, and stale incremental-ingest and accessibility follow-up wording was corrected.

### Validation

- `python -m memory_seed.cli docs check` passed with 15 existing incomplete-metadata warnings.
- `python -m memory_seed.cli links check` passed with existing corpus warnings.
- `python -m memory_seed.cli docs index --check` and `git diff --check` passed after regenerating `docs/README.md`.

### Follow-up

- Implement M3 against the bounded-neighbourhood fixture, selection-continuity, reduced-motion, keyboard, and non-canvas-equivalent gates recorded in the plan.

## 2026-07-31 16:57 - Place Entry details before Inspector summary

```yaml
entry_id: mse_gs3m3m7wnp08dbzj
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/fix/entry-details-before-summary
related_entries:
  - mse_r6tyarbkzz18st4w
```

### Summary

- Moved the collapsed Entry details disclosure to the top of the Memory Trace Inspector, directly before Summary.

### Validation

- `npm test` (235 passing), `npm run typecheck`, and `npm run build` passed.
- Browser verification on the worktree preview confirmed Entry details appears before Summary and remains collapsed by default.

## 2026-07-31 17:49 - Coordinate Trace ontology filters, entry summaries, and Inspector decision navigation

```yaml
entry_id: mse_f81tn7z45hcv4n0q
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/feature/coordinated-ontology-and-summary
related_entries:
  - mse_gs3m3m7wnp08dbzj
  - mse_532mkqzsg993cxrb
evolves:
  - d3 -> mse_r6tyarbkzz18st4w:d2
```

### Summary

- Delivered coordinated Area and Activity filtering for Memory Trace, required Summary blocks for newly recorded entries, and a lighter Inspector interaction where decision selection stays inside the Decisions window.

### Decisions

#### D1 - Coordinate Area and Activity as independent, decision-level facets

- D: Preserve one Area and one Activity selection across Trail and Graph; filter an entry only when both selections occur on the same decision, and return self-excluding contextual counts for the other axis.
- R: A single scalar topic discarded the user's first choice and flattened multi-decision attribution, allowing false matches across different decisions.
- A: Client-side counts from rendered nodes were rejected because graph scope and hierarchy resolution belong to the server and viewport state must not change available options.
- F: `memory-trace/memory_trace/service.py`, `memory-trace/memory_trace/models.py`, `memory-trace/memory_trace/graph_projection.py`, `memory-trace/client/src/App.tsx`, `memory-trace/client/src/api.ts`, `memory-trace/tests/test_ontology_facets.py`, and generated contracts.
- T: `test_ontology_facets.py` (2 passing), `test_v1_api_contract.py` (13 passing), `test_openapi_contract_fixture.py` (7 passing), 235 frontend tests, typecheck, and production build passed.

#### D2 - Require a Summary on every newly recorded entry, while preserving legacy readability

- D: Enforce a `### Summary` section during session append, and show an explicit reader fallback for historic entries that predate the rule.
- R: The Inspector needs entry-level context even where there is one decision; retroactively failing historical records would turn valid history into a maintenance burden.
- A: Enforcing the rule in every corpus integrity check was rejected because those checks must retain historical compatibility.
- F: `memory_seed/core.py`, `tests/test_session_append.py`, `memory-trace/client/src/EntryReader.tsx`, and `.memory-seed/skills/session_logging.md`.
- T: `test_session_append.py` (37 passing).

#### D3 - Keep decision-selector navigation within the Decisions window

- D: Preserve the outer Inspector scroll position when a decision-selector click changes the exact Trail selection, while allowing the bounded Decisions window to scroll to the selected decision.
- R: The selector is local reader navigation; moving the whole Inspector loses the surrounding entry context.
- A: Suppressing all scroll was rejected because the selected decision still needs to be brought into view inside its own window.
- F: `memory-trace/client/src/App.tsx`.
- T: Live preview verification: selecting D2 moved the Decisions window from approximately 0 to 620px, while the Inspector remained at 0px.

### Implementation

- Added visible removable Area/Activity chips, per-axis clearing, contextual option reduction, and reset handling when the Trail is unfiltered.
- Updated the active UX plan and next-steps document to record the delivered M3a facet slice.

### Validation

- `npm test` (235 passing), `npm run typecheck`, and `npm run build` passed; the build retains its existing chunk-size warnings.
- `python -m memory_seed.cli docs index --check`, `docs check`, and `git diff --check` passed; docs check reports 15 pre-existing incomplete-metadata warnings.

### Follow-up

- Continue M3 with bounded graph perspectives and controlled expansion; no further work is required for the coordinated-facet or Inspector-scroll behavior delivered here.

## 2026-07-31 18:07 - Reveal Inspector decisions from Trail and Graph rows

```yaml
entry_id: mse_a5sw84qa1arrswhp
user_initials: JNL
agent_type: codex
agent_name: null
project_path: .
subproject_path: null
branch: codex/feature/coordinated-ontology-and-summary
topics:
  - memory-trace
  - ui-design
  - bugfix
evolves:
  - mse_f81tn7z45hcv4n0q:d3
```

### Summary

- Corrected decision-row navigation so Trail and Graph reveal the Inspector's Decisions section while the bounded decision window aligns the exact decision.

### Decision

- D: Treat a decision selected from either workspace as two coordinated scroll targets: the outer Inspector reveals the Decisions segment, and the nested decision window scrolls to the selected decision.
- R: A workspace row is external navigation, so preserving the entire Inspector position can hide the relevant section; scrolling the outer pane to the decision article instead makes the nested window drag its parent and loses the entry's section structure.
- A: Separate Trail and Graph scroll implementations were rejected because both views already resolve decision rows through the same selection handler. No decision diagram was added because this is a local UI interaction refinement without durable branch, data, or lifecycle topology.
- F: `memory-trace/client/src/App.tsx`; generated production assets under `memory-trace/memory_trace/static/react/`.
- T: `npm test` passed 235 tests; `npm run typecheck` and `npm run build` passed. Live preview checks on port 4179 confirmed both Trail D3 and Graph D2 put the Decisions segment 24px below the Inspector top, align the selected article at the top of the nested window, and keep it visible.
