---
tags:
  - session-log
  - memory-seed
session_date: 2026-07-19
---

## 2026-07-19 00:05 - Two-view workspace: drop List, grouped context panel, click-to-scroll Trail selection

```yaml
entry_id: mse_wvz8qtkgjcwfwzej
user_initials: JNL
agent_type: claude
agent_name: developer
project_path: .
subproject_path: memory-trace
branch: claude/feature/context-groups-no-list
topics:
  - memory-trace
  - ui-design
related_entries:
  - mse_y9ths7v3e9b0vz7k
evolves:
  - mse_y9ths7v3e9b0vz7k
```

### Decision

- D: Three refinements from JNL. (1) **List view removed** - the workspace is Graph | Trail (JNL: those
  two were agreed as the product's core); `GraphWorkspace` sheds its list branch and `viewMode` prop.
  (2) The context panel **groups by relationship** - one header per kind
  (related/replaces/evolves/same-commit/similar, coloured via the edge tokens) instead of a chip
  repeated on every row. (3) **Context click centres the Trail**: rows carry `data-entry`;
  `TrailWorkspace` watches the selection - if the entry is older than the loaded window it computes the
  entry's index in the full response and asks App to grow the window (`onEnsureVisible`), then scrolls
  the row to centre.
- R: Direct feedback round 4. The ensure-visible design keeps windowing in App (single owner) while
  the Trail owns geometry - no scroll state leaks upward.
- F: `memory-trace/client/src/{App.tsx,GraphWorkspace.tsx,TrailWorkspace.tsx,trailModel.ts,styles.css}`,
  rebuilt static/react.
- T: tsc+build clean; live - views ["Graph","Trail"]; group headers render with zero per-item chips;
  clicking a similar-item 254 rows deep grew the window 60->254, scrolled the selected row into the
  viewport, Inspector synced; no console errors.
