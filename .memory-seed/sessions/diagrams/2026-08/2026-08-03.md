---
tags:
  - session-log-diagrams
diagram_date: 2026-08-03
---

## 2026-08-03 15:40 - Parent-first recoverable sidecar transaction

```yaml
adr_id: adr_parent_first_sidecar_transaction
grounded_in:
  - mse_d06t9bccm3yykfqs:d1
  - mse_17d0qqh34a07qp5b:d2
```

```mermaid
flowchart TB
  S["stage the journal"] --> P["append the canonical PARENT entry"]
  P --> T["publish topic sidecar"]
  P --> L["publish lifecycle / link sidecar"]
  P --> A["publish ADR ledger events"]

  S -.->|"interrupted here"| R1["nothing durable<br/>(journal only)"]
  P -.->|"interrupted here"| R2["parent exists, enrichment pending<br/>= an INCOMPLETE ENRICHMENT"]
  T -.->|"interrupted here"| R2

  X["rejected: sidecars before the parent"] -.->|"leaves a durable sidecar<br/>referencing an absent entry"| BAD["breaks link validation<br/>and every reader"]
```

## 2026-08-03 16:40 - MCP decision envelope and mandatory ADR review

```yaml
adr_id: adr_mcp_decision_envelope_review
grounded_in:
  - mse_17d0qqh34a07qp5b:d1
  - mse_ddeat5w29spep3qw:d1
```

```mermaid
flowchart TB
  CALL["MCP session write call<br/>authors entry narrative +<br/>decision-keyed semantic sidecars<br/>(one shared transaction)"] --> MATCH{"Does the decision<br/>evolve or replace a<br/>living ADR lineage<br/>member?"}

  MATCH -- "no match" --> WRITE1["Transaction proceeds,<br/>publishes normally"]

  MATCH -- "matches one or more<br/>architectural concerns" --> GATE["Gate BEFORE mutation:<br/>content-bound review<br/>required per matched concern"]

  GATE --> FIRSTCALL["First call"]
  FIRSTCALL --> PAYLOAD["Return written: false<br/>+ complete accepted synopsis,<br/>history, pending work,<br/>source evidence, exact<br/>affected ADR state + draft"]
  PAYLOAD --> LLMREVIEW["LLM reviews the<br/>exact bound content"]
  LLMREVIEW --> RECEIPT["Retry with content-bound<br/>receipt + one explicit<br/>outcome per concern"]
  RECEIPT --> CHECK{"Receipt matches<br/>exact reviewed state?"}
  CHECK -- "yes" --> PUBLISH["Publish ADR proposal event(s)<br/>+ resume the transaction"]
  CHECK -- "no / stale" --> PAYLOAD

  PUBLISH --> LENS["ADR = high-signal design-<br/>thread lens: identifies current<br/>governing decision + lineage"]
  LENS --> BOUNDARY["Link sidecar remains the<br/>SOLE owner of evolves /<br/>replaces meaning &mdash;<br/>no second lifecycle ledger"]

  subgraph REJ["Rejected"]
    R1["Review only the current head<br/>&mdash; historical lineage can<br/>still change"]
    R2["Allow the first call to write<br/>&mdash; LLM would react<br/>after the fact"]
    R3["Duplicate ADR lineage as a<br/>second ledger &mdash; two<br/>authoritative homes for one<br/>relationship, Invariant #6"]
  end

  MATCH -.rejected design.-> R1
  GATE -.rejected design.-> R2
  BOUNDARY -.rejected design.-> R3
```

## 2026-08-03 16:40 - Shared write transaction binds lifecycle writes to mandatory ADR review

```yaml
adr_id: adr_session_decision_authority
grounded_in:
  - mse_17d0qqh34a07qp5b:d1
  - mse_ddeat5w29spep3qw:d1
```

```mermaid
flowchart TB
  W["Lifecycle-linked MCP/CLI write"] --> T["One shared transaction<br/>authors entry narrative + decision-keyed sidecars"]
  T --> M{"Matches an architectural concern?"}
  M -->|"No"| WR["Write resumes normally"]
  M -->|"Yes"| G{"Content-bound review receipt<br/>+ explicit outcome per concern?"}
  G -->|"No"| BLOCK["written: false -<br/>return synopsis, history, pending work, evidence"]
  BLOCK --> REVIEW["Agent reviews complete accepted context"]
  REVIEW --> RETRY["Resubmit with exact receipt"]
  RETRY --> G
  G -->|"Yes"| WR2["Write proceeds -<br/>session decision stays authoritative evidence"]
  WR2 --> ADR["Living ADR curates synopsis,<br/>governing decision, and lineage"]
  T -.-> LINKS["Link sidecar alone owns<br/>evolves / replaces"]
  REJ["Rejected: ADR as a second<br/>lifecycle ledger"] -.->|"two authoritative homes,<br/>violates Constitution Invariant #6"| ADR
  ALT["Rejected: review only current head,<br/>or let the first call write"] -.->|"historical lineage could still<br/>change after the fact"| G
```
