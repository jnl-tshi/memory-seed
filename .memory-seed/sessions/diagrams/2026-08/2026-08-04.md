---
tags:
  - session-log-diagrams
diagram_date: 2026-08-04
---

## 2026-08-04 10:36 - ESR orphan-worktree residue flow

```yaml
entry_id: mse_a0g1wacyk50jvsbd
```

```mermaid
flowchart TD
  Cleanup["Git cleanup"] --> Registered{"Directory still<br/>registered?"}
  Registered -->|yes| GitRemove["Use git<br/>worktree remove"]
  Registered -->|no| Compare["ESR compares physical namespaces<br/>with registered paths"]
  Compare --> Candidate["ORPHAN RESIDUE<br/>CANDIDATE"]
  Candidate --> Safe{"Merged or duplicated<br/>with no unique work?"}
  Safe -->|no or uncertain| Recover["Retain and recover<br/>unique content"]
  Safe -->|yes and live consent| Exact["Remove exact<br/>validated path"]
  Exact --> Verify["Prune Git metadata<br/>and rerun ESR"]
```

## 2026-08-04 18:07 - Optional external-tool skills ship in a profile, never core

```yaml
adr_id: adr_optional_skill_profiles
grounded_in:
  - mse_y7d6qtjgqdfhtmaj:d2
```

```mermaid
flowchart TB
  NEW["A new skill file lands"] --> Q{"Does it wrap an<br/>EXTERNAL tool?"}
  Q -->|"no - universal memory workflow"| CORE["CORE_SKILL_NAMES<br/>installed by a bare init"]
  Q -->|"yes (Semble, Graphify CLI)"| PROF["a named profile<br/>e.g. coding"]

  PROF --> R1["register in the profile"]
  R1 --> R2["add to SKILL_DESCRIPTIONS"]
  R2 --> R3["add to pyproject package-data"]
  R3 --> R4["add to the seed-file install-list test"]
  R4 --> OK["installs for projects that opt in"]

  R1 -.->|"miss ANY step"| GHOST["a skill that exists in the repo<br/>but never installs anywhere"]
  R2 -.-> GHOST
  R3 -.-> GHOST
  R4 -.->|"and nothing fails, so nobody notices"| GHOST
```
