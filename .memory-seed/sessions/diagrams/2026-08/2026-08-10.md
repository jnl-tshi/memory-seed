---
tags:
  - session-log-diagrams
diagram_date: 2026-08-10
---

## 2026-08-10 13:00 - Memory-grounded conclusions close the retrieval-to-write loop

```yaml
entry_id: mse_axkrkd339br970kw
grounded_in:
  - mse_axkrkd339br970kw:d1
  - mse_axkrkd339br970kw:d2
```

```mermaid
flowchart TB
  TASK["Consequential task"] --> CURRENT["Inspect current files"]
  TASK --> SEARCH["Search memory, then fetch<br/>the relevant full entries"]
  CURRENT --> CONCLUDE["Derive the conclusion"]
  SEARCH --> CONCLUDE
  CONCLUDE --> DISPOSE["Disposition for every fetched decision:<br/>replaces, typed evolves, related, or no-edge"]
  DISPOSE --> APPEND{"Append path"}
  APPEND -->|"unlinked decision"| SUGGEST["Return ranked link suggestions;<br/>author chooses the edge"]
  APPEND -->|"ADR-linked decision"| PREFLIGHT["Shared CLI/MCP zero-write preflight:<br/>full contexts plus bound receipt"]
  PREFLIGHT --> TX["Retry writes session, semantic sidecars,<br/>and reviewed ADR outcomes together"]
  APPEND -->|"review only, no new decision"| REVIEWED["adr reviewed / memory_adr_reviewed"]
  REVIEWED --> EVIDENCE["Require an existing evidence entry;<br/>record no-change without moving the head"]
```
