---
tags:
  - session-log-diagrams
diagram_date: 2026-07-12
---

## 2026-07-12 00:38 - Bundle Memory Trace as optional extra

```yaml
entry_id: mse_etm5m5682sseasgm
```

```mermaid
flowchart TD
  subgraph InstallTier["Install choices"]
    direction LR
    Core["memory-seed<br/>core install"]
    TraceExtra["memory-seed[trace]<br/>optional UI extra"]
  end

  subgraph PackageTier["Published package"]
    direction LR
    RootPkg["memory-seed<br/>PyPI distribution"]
    TracePkg["memory_trace<br/>bundled package"]
    TraceCmd["memory-trace<br/>console command"]
  end

  subgraph RuntimeTier["Runtime behavior"]
    direction LR
    CoreCli["memory-seed CLI<br/>MCP and control plane"]
    TraceUi["Trace UI<br/>FastAPI/Uvicorn"]
    Upgrade["memory-trace upgrade<br/>upgrades memory-seed"]
  end

  Core --> RootPkg --> CoreCli
  TraceExtra --> RootPkg
  RootPkg --> TracePkg --> TraceCmd --> TraceUi
  TraceCmd --> Upgrade
  InstallTier ~~~ PackageTier
  PackageTier ~~~ RuntimeTier
```
