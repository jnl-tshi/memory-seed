---
tags:
  - session-log-diagrams
diagram_date: 2026-07-12
---

## 2026-07-12 00:38 - Bundle Memory Trace as optional extra

```yaml
entry_id: mse_etm5m5682sseasgm
```

```mermaid
flowchart TD
  subgraph InstallTier["Install choices"]
    direction LR
    Core["memory-seed<br/>core install"]
    TraceExtra["memory-seed[trace]<br/>optional UI extra"]
  end

  subgraph PackageTier["Published package"]
    direction LR
    RootPkg["memory-seed<br/>PyPI distribution"]
    TracePkg["memory_trace<br/>bundled package"]
    TraceCmd["memory-trace<br/>console command"]
  end

  subgraph RuntimeTier["Runtime behavior"]
    direction LR
    CoreCli["memory-seed CLI<br/>MCP and control plane"]
    TraceUi["Trace UI<br/>FastAPI/Uvicorn"]
    Upgrade["memory-trace upgrade<br/>upgrades memory-seed"]
  end

  Core --> RootPkg --> CoreCli
  TraceExtra --> RootPkg
  RootPkg --> TracePkg --> TraceCmd --> TraceUi
  TraceCmd --> Upgrade
  InstallTier ~~~ PackageTier
  PackageTier ~~~ RuntimeTier
```

## 2026-07-12 12:15 - Fuse Codex branches and align Trace packaging docs

```yaml
entry_id: mse_kq3ba0cy9nkpqkm0
```

```mermaid
graph TD
  subgraph MergeTier["Merged to main"]
    direction LR
    Guard["namespace guard<br/>proposal branch"]
    Trace["Trace optional-extra<br/>implementation branch"]
  end

  subgraph ReconcileTier["Docs reconciliation"]
    direction LR
    Cleanup["stale cleanup branch<br/>not raw-merged"]
    Docs["current docs<br/>optional-extra strategy"]
  end

  subgraph CleanupTier["Deferred cleanup"]
    direction LR
    OldRefs["stale local refs"]
    Worktrees["worktree-backed<br/>branches"]
  end

  Guard --> Main["main"]
  Trace --> Main
  Cleanup --> Docs --> Main
  OldRefs -. approval blocked .-> Later["later cleanup pass"]
  Worktrees -. remove via worktree pass .-> Later

  MergeTier ~~~ ReconcileTier
  ReconcileTier ~~~ CleanupTier
```
