---
tags:
  - session-log-diagrams
diagram_date: 2026-07-22
---

## 2026-07-22 07:47 - Render diagram sidecars with Mermaid itself

```yaml
entry_id: mse_zwzdjn0m9e34gdth
```

The before/after of D1: one sidecar, three renderers, and which of them understood it. The subset
parser was the odd one out, which is what made it the defect rather than the specification.

```mermaid
flowchart TD
  Sidecar["Diagram sidecar<br/>Markdown, source of truth"]

  subgraph Before["Before - one renderer disagreed"]
    direction LR
    VsCodeB["VS Code<br/>full Mermaid"]
    GitHubB["GitHub<br/>full Mermaid"]
    ArcB["Trace / arc2d<br/>hand-written subset"]
  end

  subgraph After["After - Mermaid everywhere"]
    direction LR
    VsCodeA["VS Code"]
    GitHubA["GitHub"]
    TraceA["Trace<br/>Mermaid 11"]
  end

  Sidecar --> Before
  Sidecar --> After
  ArcB -->|"subgraph, direction, end<br/>render as stray boxes"| Broken["Corrupted diagram"]
  TraceA -->|"look and palette<br/>from Trace settings"| Correct["Diagram as authored"]
```

This one carries D2: the two Trace settings that reach Mermaid, and the direction colour travels.
`styles.css` stays the single source of truth because the values are read, never copied.

```mermaid
flowchart LR
  subgraph Settings["Trace settings"]
    direction TB
    Toggle["Drawn / Slick"]
    Theme["Light / Dark"]
  end

  subgraph Bridge["mermaidConfig.ts"]
    direction TB
    Look["look:<br/>handDrawn | classic"]
    Vars["themeVariables<br/>read from CSS vars"]
  end

  Css["styles.css<br/>palette tokens"] --> Vars
  Toggle --> Look
  Theme --> Vars
  Look --> Render["mermaid.render"]
  Vars --> Render
  Render -->|"16 paths Slick<br/>38 paths Drawn"| Svg["Rendered SVG"]
```

## 2026-07-22 19:17 - Name graph communities after authored topics

```yaml
entry_id: mse_3yvakpxdshc95e68
```

D2's rejected alternatives, with the measurement that killed each. The rule is the surviving path,
not the first one tried.

```mermaid
flowchart TD
  Node["Node with authored topics"]

  subgraph Rejected["Rejected - measured, not assumed"]
    direction TB
    Common["Most common topic"] --> CommonOut["46% in one community<br/>giant-component hairball"]
    First["First-listed topic"] --> FirstOut["Meaningless<br/>304 of 304 stored alphabetically"]
    Rare["Rarest, no floor"] --> RareOut["55 communities<br/>42 of them dust"]
  end

  subgraph Chosen["Chosen"]
    direction TB
    Floor["Rarest topic clearing<br/>corpus frequency 10"] --> Fallback["Fall back to next<br/>qualifying topic"]
    Fallback --> Result["15 communities<br/>largest 12%, 63% coloured"]
  end

  Node --> Rejected
  Node --> Chosen
```

Why §4.3's retention apparatus is not built: it exists to stabilise an unstable identity, and this
identity is stable to begin with.

```mermaid
stateDiagram-v2
  [*] --> Detected: Louvain / Leiden
  Detected --> Unstable: ids change per rebuild
  Unstable --> Fingerprint: generate fingerprint
  Fingerprint --> Overlap: compare member overlap
  Overlap --> Retained: retain colour above threshold
  Retained --> Persisted: community_previous_id
  Persisted --> [*]

  [*] --> Authored: topic slug
  Authored --> Stable: identity IS the slug
  Stable --> [*]
```

## 2026-07-22 19:35 - Topic becomes an opt-in graph edge

```yaml
entry_id: mse_0qycwt519qdggrpe
```

D1's core claim: the two edge families answer different questions. One is authored about a pair; the
other is an artefact of sorting entries that happen to share a tag.

```mermaid
flowchart LR
  subgraph Authored["Authored - a claim about this pair"]
    direction TB
    A1["Entry A"] -->|"supersedes"| A2["Entry B"]
    A3["Entry C"] -->|"evolves"| A4["Entry D"]
  end

  subgraph Derived["Derived - an artefact of sort order"]
    direction TB
    T1["Entry E<br/>tagged graph"] -->|"next tagged<br/>graph in time"| T2["Entry F<br/>tagged graph"]
  end

  Authored --> Keep["Default ON<br/>666 edges, under the cap"]
  Derived --> Chip["Opt-in chip<br/>membership already shown as colour"]
```

D2's correction. The first measurement used the wrong endpoint's defaults; the corrected one made
the case stronger, because the response with topic is truncated rather than merely large.

```mermaid
flowchart TD
  Q["Does topic earn a default edge?"]
  Q --> Wrong["First measurement<br/>server defaults<br/>related,topic,agent,day"]
  Q --> Right["Corrected<br/>client edge set<br/>related,supersedes,evolves,topic"]
  Wrong --> W1["topic 467 of 1000<br/>'spends budget'"]
  Right --> R1["1000 edges = CAP HIT<br/>authored edges truncated"]
  Right --> R2["without topic: 666<br/>every authored edge fits"]
  R1 --> Verdict["Crowding out, not just spending"]
  R2 --> Verdict
```

## 2026-07-22 19:50 - Topic community legend, and collision-free community colour

```yaml
entry_id: mse_d2669mmzd9se9cf1
```

D2: the legend did not introduce the collision, it made an existing one visible. Hashing was chosen
for stability, and stability was never the property it failed at.

```mermaid
flowchart TD
  Need["A community needs a colour"]

  subgraph Hash["Hash the fingerprint - what shipped"]
    direction TB
    H1["Stable across reloads"] --> H2["Subset-independent"]
    H2 --> H3["16 slots, 15 communities<br/>birthday bound"]
    H3 --> H4["10 colours for 15<br/>4 collision groups"]
  end

  subgraph Rank["Rank over qualifying topics - now"]
    direction TB
    R1["Corpus facet counts<br/>not the loaded subset"] --> R2["Alphabetical, so volume<br/>changes cannot swap"]
    R2 --> R3["Only topics above the floor<br/>can name a community"]
    R3 --> R4["15 colours for 15"]
  end

  Need --> Hash
  Need --> Rank
  H4 --> Seen["Two legend rows,<br/>identical swatches"]
```

D3: why one derivation. The failure mode of two is silent and reads as a rendering bug rather than
as a duplicated constant.

```mermaid
flowchart LR
  Corpus["facets.topics<br/>corpus-wide counts"] --> Scale["communityColourScale<br/>graphCommunities.ts"]
  Scale --> Nodes["Cytoscape node fill"]
  Scale --> Legend["Legend swatch"]
  Nodes --> Agree["Guaranteed to agree<br/>pinned by test"]
  Legend --> Agree
```

## 2026-07-22 20:09 - Measured community palette and inferred colour for topicless entries

```yaml
entry_id: mse_ftx8e1d5hwysajsz
```

D1: why the palette is generated rather than extended. Distinct hex values and distinguishable
colours are different properties, and only the second one matters on screen.

```mermaid
flowchart TD
  Start["Sixteen colours needed"]

  subgraph Byeye["Assembled by eye - what shipped"]
    direction TB
    E1["Six colours<br/>plus ten variants"] --> E2["Every hex distinct"]
    E2 --> E3["Closest pair 0.0351<br/>three blues, two ochres"]
  end

  subgraph Measured["Generated against a floor - now"]
    direction TB
    M1["Even OKLCh hues<br/>chroma 0.11"] --> M2["Lightness alternating<br/>0.62 / 0.74"]
    M2 --> M3["Closest pair 0.0831<br/>test-pinned floor 0.08"]
  end

  Start --> Byeye
  Start --> Measured
  E3 --> Problem["Distinct values,<br/>indistinguishable colours"]
```

D2: the inference, and the line it deliberately does not cross. One hop keeps this a reading of the
neighbourhood rather than a clustering algorithm.

```mermaid
stateDiagram-v2
  [*] --> HasTopic: entry carries a qualifying topic
  HasTopic --> FullColour: full community colour
  FullColour --> [*]

  [*] --> NoTopic: no qualifying topic
  NoTopic --> CheckNeighbours: look at DIRECT neighbours
  CheckNeighbours --> Faded: a classified neighbour exists
  CheckNeighbours --> Neutral: none classified
  Faded --> [*]: faded toward theme background
  Neutral --> [*]: stays neutral grey

  note right of Faded
    Never a source for another
    inference - one hop only,
    or this becomes label
    propagation
  end note
```

## 2026-07-22 20:28 - Community detection measured and rejected

```yaml
entry_id: mse_kdm34r508j3n1akg
```

D2: why the metric choice decided the answer. The same partition looks like a success or a failure
depending on which number you read, and only one of them is about meaning.

```mermaid
flowchart TD
  Part["One partition of the authored graph"]
  Part --> Q["Modularity 0.89"]
  Part --> A["ARI vs authored topics 0.11"]
  Q --> Ship["Reads as a clear success"]
  A --> Reject["Recovers almost none<br/>of the authored structure"]
  Ship -->|"would have shipped<br/>the worst-agreeing config"| Trap["Modularity measures<br/>separability, not meaning"]
  Reject --> Trap
```

D1 and D3: the decision path, with the control that bounds it. Matched-k is where it ends, and that
step does not depend on the ceiling argument.

```mermaid
flowchart TD
  Start["Can detection colour<br/>the topicless 36%?"]
  Start --> P1["Phase 1: authored-only<br/>2.24 deg/node, 107 isolated"]
  P1 -->|"too sparse"| Files["Admit file edges<br/>21.3 deg/node, 12 isolated"]
  Files --> Agree["Agreement vs authored topics"]
  Agree --> Ceiling["Control: truth as input<br/>scores only 0.272"]
  Agree --> Best["Best detection 0.12<br/>but needs k=43-145"]
  Best --> Matched["Force k to 15"]
  Matched --> Blob["ARI 0.000<br/>one community holds 93%"]
  Blob --> No["Not licensed"]
  Ceiling --> Read["Read 0.12 as 44% of achievable,<br/>not 12%"]
  Read --> No
```

## 2026-07-22 20:46 - Pastel inference with chain residue and collision-proof colour slots

```yaml
entry_id: mse_ajd2qn8gr0mvzvg6
```

D2 and D3 together: how a topicless entry gets its colour. Votes arrive from classified
neighbours and down topicless chains, then blend and scale - membership is never assigned.

```mermaid
flowchart TD
  subgraph Sources["Votes reaching a topicless entry"]
    direction TB
    Direct["Classified neighbour<br/>weight 1.0"]
    Hop2["Two hops down a<br/>topicless chain - 0.35"]
    Hop3["Three hops - 0.12"]
    Wall["Hop 4+<br/>nothing arrives"]
  end

  Direct --> Tally["Vote tally<br/>per community"]
  Hop2 --> Tally
  Hop3 --> Tally
  Tally --> Blend["Weighted OKLab blend<br/>disagreement pulls toward grey"]
  Blend --> Strength["Strength = votes / 5<br/>capped at 0.8"]
  Strength --> Render["Pastel -> full colour<br/>never equal to authored"]
```

D3's boundary: why this is not label propagation. The walk's corridor is topicless by
construction, and classified nodes are origins only.

```mermaid
flowchart LR
  G["graph entry<br/>(classified)"] -->|"seeds walk"| A["topicless A<br/>strong tint"]
  A -->|"decay 0.35"| B["topicless B<br/>faint tint"]
  B -->|"decay 0.12"| C["topicless C<br/>residue"]
  C -.->|"hop cap"| D["topicless D<br/>stays grey"]
  G2["release entry<br/>(classified)"] ---|"never relays<br/>graph's colour"| B
```

## 2026-07-22 20:58 - Authored-topic nodes wear a rim of their own colour

```yaml
entry_id: mse_2t1x5gtek6c8mhht
```

The provenance ladder a node's paint now encodes. Fill strength is continuous and can climb;
the rim is binary and cannot be borrowed.

```mermaid
flowchart LR
  Authored["Declared a topic<br/>full colour + darkened rim"]
  Strong["Borrowed, many votes<br/>near-full tint, NO rim"]
  Faint["Borrowed, chain residue<br/>pale tint, no rim"]
  None["No contact<br/>neutral grey, no rim"]
  Authored -->|"rim is the boundary<br/>tint can never cross"| Strong
  Strong --> Faint
  Faint --> None
```
