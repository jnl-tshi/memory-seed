---
tags:
  - session-log-diagrams
diagram_date: 2026-07-21
---

## 2026-07-21 19:47 - Trail D1 rows: the entry row anchors, every decision is a subheading

```yaml
entry_id: mse_t5bbdstgmqzagn2y
```

```mermaid
graph TD
  OLD["BEFORE - entry row doubles as D1"]
  NEW["AFTER - entry row is the ANCHOR<br/>no ordinal, count N, full colour, keeps the time"]
  OLD -->|"D1 had no row: heading 1 was skipped"| NEW
  NEW --> D1["D1 - pastel subheading"]
  NEW --> D2["D2 - pastel subheading"]
  NEW --> DN["DN - pastel subheading"]
  NEW --> P1["isDecisionRow - styling only"]
  NEW --> P2["inDecisionGroup - indent, search, selection"]
```

## 2026-07-21 22:50 - Score link-audit candidates on shared title terms

```yaml
entry_id: mse_p62d4y4df0d8pz4x
```

```mermaid
flowchart LR
  PAIR["candidate pair<br/>source and older entry"]
  PAIR -->|"lifecycle edge already declared"| SKIP["excluded by design<br/>not a miss"]
  PAIR -->|"shared files"| SCORE["score"]
  PAIR -->|"shared title terms - NEW"| SCORE
  PAIR -->|"shared topics, if unlinked"| SCORE
  SCORE -->|"idf weighted sum"| RANK["sort desc, keep top 5"]
  RANK --> STUB["classify_pending stub<br/>terms / files / topics evidence"]
```

## 2026-07-21 23:40 - Machine guard for entry-free branch merges

```yaml
entry_id: mse_27jbk0z5nkg4jabe
```

```mermaid
graph TD
  START["work on a feature branch"]
  START -->|"intended"| GOOD["append entry ON the branch"]
  GOOD --> MERGE["session merge-branch"]
  START -->|"dirty tree blocks the merge"| BLOCK["recover: merge first"]
  BLOCK --> LATE["append entry on main, branch gone"]
  LATE --> LOSS["trunk lane and no trailer<br/>unrecoverable once published"]
  MERGE -->|"trailers stamped = 0"| NOTE["guard prints the note"]
  MERGE -->|"trailers stamped > 0"| SILENT["silent - branch lane intact"]
```
