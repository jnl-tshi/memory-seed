---
tags:
  - session-log-diagrams
diagram_date: 2026-07-10
---

## 2026-07-10 03:28 - Tighten decision harvest and diagram sidecar logging

```yaml
entry_id: mse_m6amd5db4c7s8sfw
```

```mermaid
graph TD
  subgraph TopTier[" "]
    direction LR
    Start["End of turn"] --> Harvest["Decision<br>Harvest"]
  end

  subgraph MiddleTier[" "]
    direction LR
    One["Single<br>DRAFT"]
    Many["Multi-decision<br>D1 / D2"]
    Split["Separate<br>entries"]
  end

  subgraph BottomTier[" "]
    direction LR
    Gate["Topology / migration /<br>workflow trigger?"]
    Sidecar["Mermaid<br>sidecar"]
    Skip["Record skip<br>reason"]
  end

  subgraph FinalTier[" "]
    direction LR
    Log["Append<br>session log"]
    Verify["Links / doctor /<br>diff checks"]
  end

  TopTier ~~~ MiddleTier
  MiddleTier ~~~ BottomTier
  BottomTier ~~~ FinalTier

  Harvest --> One
  Harvest --> Many
  Harvest --> Split
  One --> Gate
  Many --> Gate
  Split --> Gate
  Gate -- yes --> Sidecar
  Gate -- no, positive trigger skipped --> Skip
  Sidecar --> Log
  Skip --> Log
  Log --> Verify
```

## 2026-07-10 04:27 - Implement branch-session fuse MCP preview

```yaml
entry_id: mse_81v7vk4x5ys3k2n0
```

```mermaid
graph TD
  subgraph TopTier[" "]
    direction LR
    Branch["Task branch<br>session entries"]
    Main["Base / main<br>accepted entries"]
    Preview["MCP preview<br>read-only"]
  end

  subgraph MiddleTier[" "]
    direction LR
    EntryGate["Entry gate<br>id + branch + time"]
    SidecarGate["Sidecar gate<br>parent accepted"]
    Issues["Issues<br>block merge"]
  end

  subgraph BottomTier[" "]
    direction LR
    Merge["git merge<br>--no-ff --no-commit"]
    Apply["CLI apply<br>session fuse"]
    Grouped["Grouped<br>session paths"]
  end

  TopTier ~~~ MiddleTier
  MiddleTier ~~~ BottomTier

  Branch --> Preview
  Main --> Preview
  Preview --> EntryGate
  Preview --> SidecarGate
  EntryGate --> Issues
  SidecarGate --> Issues
  EntryGate --> Merge
  SidecarGate --> Merge
  Merge --> Apply
  Apply --> Grouped
```

## 2026-07-10 04:39 - Merge session fuse preview workflow to main

```yaml
entry_id: mse_9dkkddgb6zjxv4j9
```

```mermaid
graph TD
  subgraph BranchTier[" "]
    direction LR
    Base["main<br>33f9cd3"]
    Feature["codex-feature-session-fuse<br>878a3fd"]
  end

  subgraph CheckTier[" "]
    direction LR
    Preview["Fuse dry-run<br>blocked bootstrap branch"]
    Merge["No-commit merge<br>clean"]
    Tests["Merged tree<br>validated"]
  end

  subgraph ResultTier[" "]
    direction LR
    Commit["Merge commit<br>16166e3"]
    Main["main<br>updated"]
  end

  BranchTier ~~~ CheckTier
  CheckTier ~~~ ResultTier

  Base --> Preview
  Feature --> Preview
  Base --> Merge
  Feature --> Merge
  Preview --> Merge
  Merge --> Tests
  Tests --> Commit
  Commit --> Main
```

## 2026-07-10 05:10 - Add authoring-loop MCP tools (link suggest/show, session target)

```yaml
entry_id: mse_jpfs9yy19bznh18y
```

```mermaid
graph TD
  subgraph ReadTier[" "]
    direction LR
    Search["memory_search<br>rank entries"]
    Get["memory_get_chunk<br>full text"]
  end

  subgraph StoreTier[" "]
    direction LR
    Store["Session memory<br>entries + graph"]
  end

  subgraph WriteTier[" "]
    direction LR
    Suggest["memory_link_suggest<br>related_entries"]
    Show["memory_link_show<br>graph node"]
    Target["memory_session_target<br>append path"]
  end

  subgraph AppendTier[" "]
    direction LR
    Append["Agent appends entry<br>CLI / direct file"]
  end

  ReadTier ~~~ StoreTier
  StoreTier ~~~ WriteTier
  WriteTier ~~~ AppendTier

  Search --> Get
  Get --> Store
  Store --> Search
  Store --> Suggest
  Store --> Show
  Store --> Target
  Suggest --> Append
  Target --> Append
  Append --> Store
```

## 2026-07-10 05:35 - Merge authoring-loop MCP tools to main

```yaml
entry_id: mse_6eek6qvf8cvcbjw6
```

```mermaid
graph TD
  subgraph BranchTier[" "]
    direction LR
    Base["main<br>fff8053"]
    Feat["claude-feature-mcp-authoring-tools<br>feat + registry fix"]
  end

  subgraph CheckTier[" "]
    direction LR
    Validate["Branch validated<br>310 tests, links, doctor"]
  end

  subgraph ResultTier[" "]
    direction LR
    Merge["Merge commit<br>78cbd5d (--no-ff)"]
    Main["main<br>ahead 14, unpushed"]
  end

  BranchTier ~~~ CheckTier
  CheckTier ~~~ ResultTier

  Base --> Merge
  Feat --> Validate
  Validate --> Merge
  Merge --> Main
```

## 2026-07-10 06:20 - Fix session fuse Windows encoding and corpus-scope bugs

```yaml
entry_id: mse_azn6bejpd9xpmh3f
```

```mermaid
graph TD
  subgraph SourceTier[" "]
    direction LR
    Base["base ref<br>FULL corpus"]
    Branch["branch ref"]
    Diff["git diff base...branch<br>changed files only"]
  end

  subgraph DecodeTier[" "]
    direction LR
    Show["git show<br>strict UTF-8 + catch"]
  end

  subgraph MergeTier[" "]
    direction LR
    BaseRec["base entries<br>dedup + parent lookup"]
    BranchRec["branch entries<br>validated (delta only)"]
    Plan["fuse plan<br>or issues"]
  end

  SourceTier ~~~ DecodeTier
  DecodeTier ~~~ MergeTier

  Branch --> Diff
  Diff -- "None = git fail -> block" --> Plan
  Base --> Show
  Diff --> Show
  Show --> BaseRec
  Show --> BranchRec
  BaseRec --> Plan
  BranchRec --> Plan
```

## 2026-07-10 06:40 - Merge session fuse fixes to main

```yaml
entry_id: mse_8c5e4m97szmfj8tp
```

```mermaid
graph TD
  subgraph BranchTier[" "]
    direction LR
    Base["main<br>e35bc3b"]
    Feat["claude-fix-session-fuse<br>644b6b0"]
  end

  subgraph CheckTier[" "]
    direction LR
    Validate["313 tests + clean<br>Windows fuse run"]
    Review["opus review<br>findings addressed"]
  end

  subgraph ResultTier[" "]
    direction LR
    Merge["Merge commit<br>c522379 (--no-ff)"]
    Main["main<br>ahead 18, unpushed"]
  end

  BranchTier ~~~ CheckTier
  CheckTier ~~~ ResultTier

  Base --> Merge
  Feat --> Review
  Review --> Validate
  Validate --> Merge
  Merge --> Main
```

## 2026-07-10 11:15 - Harden session-log-check.py: escalation state + language

```yaml
entry_id: mse_ynmnn99kgmqmec5r
```

```mermaid
graph TD
  subgraph WorktreeTier[" "]
    direction LR
    Primary["Primary worktree<br>my branch"]
    Sibling["Sibling worktree<br>background task"]
  end

  subgraph RaceTier[" "]
    direction LR
    SiblingMerge["Sibling merges<br>to shared main ref"]
    RefMoves["main ref advances<br>c6abdfe"]
    StaleFiles["Primary files/index<br>NOT resynced"]
  end

  subgraph DetectTier[" "]
    direction LR
    Detect["git status shows<br>unowned staged diffs"]
    GroundTruth["git show HEAD:path<br>vs working tree"]
  end

  subgraph RecoverTier[" "]
    direction LR
    Restore["git checkout HEAD --<br>path (resync only)"]
    Safe["No edits lost<br>fix preserved"]
  end

  WorktreeTier ~~~ RaceTier
  RaceTier ~~~ DetectTier
  DetectTier ~~~ RecoverTier

  Sibling --> SiblingMerge
  SiblingMerge --> RefMoves
  RefMoves --> StaleFiles
  Primary --> StaleFiles
  StaleFiles --> Detect
  Detect --> GroundTruth
  GroundTruth --> Restore
  Restore --> Safe
```

## 2026-07-10 12:03 - session merge-branch command lifecycle

```yaml
entry_id: mse_dr5eprnhrctqeeg3
```

```mermaid
flowchart TD
  Start([session merge-branch --branch B]) --> Preflight{clean tree,
no merge in progress?}
  Preflight -- no: name dirty paths --> Refuse([refuse, exit 1])
  Preflight -- yes --> DryRun{fuse dry-run
issues?}
  DryRun -- yes --> Abort([abort: no merge state created])
  DryRun -- no, --dry-run flag --> Plan([print plan, exit 0])
  DryRun -- no --> Merge[git merge --no-ff --no-commit B]
  Merge --> Head{MERGE_HEAD exists?}
  Head -- no, rc=0 --> Uptodate([already merged, nothing to do])
  Head -- no, rc!=0 --> GitErr([real git error, exit 1])
  Head -- yes --> Classify{conflicts outside
session files?}
  Classify -- yes --> Manual([leave merge in progress,
manual resolution])
  Classify -- no --> Reset[reset branch-touched session paths
to base content]
  Reset --> Apply[session fuse --apply:
timestamp-sorted rewrite]
  Apply --> Stage[git add sessions sweep]
  Stage --> Commit([git commit --no-edit:
2-parent merge, chronological log])
```

## 2026-07-10 14:04 - Lifecycle edge decision flow and read pipeline

```yaml
entry_id: mse_h5trfwg2yjyjadpp
```

```mermaid
flowchart TD
  Q{New entry relates to
an earlier decision?} -- old decision wrong or dead --> S[supersedes:]
  Q -- still right, incomplete alone --> E[evolves:]
  Q -- merely context --> R[related_entries:]
  S --> SI([superseded_by inferred read-time:
importance x0.25, excludable])
  E --> EI([evolved_by inferred read-time:
no dampener, never excluded])
  R --> RI([inbound backlink])
  A{Artifact renamed,
relocated, removed?} -- yes --> C[continuity: kind/from/to]
  C --> AT([transitive alias table:
old paths overlap new paths
in link suggest ranking])
  SI --> READ[link show / get_chunk /
memory_search results]
  EI --> READ
  AT --> SUGGEST[link suggest:
similarity + rarity-weighted
F-overlap + shared-file evidence]
```
