---
tags:
  - session-log-diagrams
diagram_date: 2026-07-07
---

## 2026-07-07 23:59 - Skills route through a deterministic registry, and seeded skills are written generic

```yaml
adr_id: adr_skill_registry_and_generic_skills
diagram_status: not_applicable
note: examined 2026-09-01 - this ADR bundles two rules: where trigger logic lives (a single deterministic registry file agents consult, instead of trigger prose scattered across every skill) and a content constraint on seeded skills (write them generic - no hardcoded paths, no machine-specific locations). Neither is an ordered process, state machine, or flow between components; the rejected agent-rules.md alternative is a design-time alternative that was never built, not a runtime branch. A diagram here would just re-box the location-of-truth sentence and the content rule as two boxes.
```

## 2026-07-07 23:59 - Skills route through a deterministic registry, and seeded skills are written generic

```yaml
adr_id: adr_skill_registry_and_generic_skills
diagram_status: not_applicable
note: examined 2026-09-01 - this ADR bundles two rules: where trigger logic lives (a single deterministic registry file agents consult, instead of trigger prose scattered across every skill) and a content constraint on seeded skills (write them generic - no hardcoded paths, no machine-specific locations). Neither is an ordered process, state machine, or flow between components; the rejected agent-rules.md alternative is a design-time alternative that was never built, not a runtime branch. A diagram here would just re-box the location-of-truth sentence and the content rule as two boxes.
```
