---
tags:
  - session-log-diagrams
diagram_date: 2026-09-19
---

## 2026-09-19 00:54 - Edition-specific memory authority

```yaml
entry_id: mse_kn36asmf96bf5tk1
```

```mermaid
flowchart TB
  Rule["Exactly one authority<br/>per edition"]
  subgraph Local["OSS / local edition"]
    direction LR
    LocalWrite["Agent or human<br/>writes Markdown"] --> LocalStore["Authoritative Markdown<br/>entries and sidecars"]
    LocalStore --> LocalViews["Derived indexes, cache<br/>and Memory Trace"]
  end
  subgraph Hosted["Hosted team edition"]
    direction LR
    HostedWrite["Authenticated MCP /<br/>Codex capture"] --> Sql["Authoritative SQL<br/>events, records, permissions,<br/>retention and audit"]
    Sql --> HostedViews["Permission-filtered<br/>retrieval and views"]
    Sql --> Export["Complete human-readable<br/>Markdown export"]
  end
  Rule --> LocalStore
  Rule --> Sql
  LocalStore -.->|"shared semantics<br/>no synchronization"| Sql
```

## 2026-09-19 00:55 - Markdown authority becomes edition-specific

```yaml
adr_id: adr_markdown_substrate
grounded_in:
  - mse_kn36asmf96bf5tk1:d1
```

```mermaid
flowchart LR
  Old["Before<br/>Markdown authoritative<br/>in every tier"] --> Amendment["Constitution v2.0<br/>one authority per edition"]
  Amendment --> Local["OSS / local<br/>Markdown authority"]
  Amendment --> Hosted["Hosted team<br/>SQL authority"]
  Hosted --> Export["Markdown export<br/>not a second writer"]
```

## 2026-09-19 01:11 - Guarded hosted-edition merge blocked by inbox captures

```yaml
entry_id: mse_30v60e8c3xa380yd
```

```mermaid
flowchart LR
  Branch["Clean committed branch<br/>a4b51292"] --> Guard["Guarded merge<br/>dry-run"]
  Guard -->|"three untracked<br/>inbox captures"| Stop["Refuse before<br/>changing main"]
  Stop --> Preserve["Primary files and<br/>task branch preserved"]
```
