---
tags:
  - session-log-diagrams
diagram_date: 2026-09-01
---

## 2026-09-01 20:00 - Orientation is a mandatory gate, not a numbered checklist

```yaml
adr_id: adr_orientation_completion_gate
diagram_status: not_applicable
note: examined 2026-09-01 - a wording fix strengthening how the orientation chain (AGENTS.md -> agent-rules.md) is phrased in the SessionStart hook and Operating Mode section, so it reads as a mandatory gate rather than a truncatable numbered checklist. The chain itself is two files already named in the title; the ADR's substance is enforcement language for an existing two-step read, not a process with branches or states to draw.
```

## 2026-09-01 20:01 - Hook installation is a per-agent merge, idempotent on script filename

```yaml
adr_id: adr_hook_merge_framework
grounded_in:
  - ms-5e366ef4:d2
```

```mermaid
flowchart TB
  A["init / update selects agent(s)"] --> B["Merge into that agent's OWN config file<br/>(never copy or clobber existing config)"]
  B --> C{"Existing hook entry matched<br/>by SCRIPT FILENAME<br/>(not full command line)?"}
  C -->|"no match found"| D["Append new hook entry"]
  C -->|"match found"| E["Upsert: replace command line in place<br/>(e.g. a later-version flag change)"]
  D --> F["Hooks install unconditionally<br/>per selected agent - seed stays model-agnostic"]
  E --> F
  F --> G["Contributor's own config content<br/>outside our entries stays untouched"]
```

## 2026-09-01 20:02 - Inbox promotion: conservative triage, one owner per workstream

```yaml
adr_id: adr_inbox_promotion_workflow
grounded_in:
  - mse_2geqfa8tg182a77p:d1
```

```mermaid
flowchart TB
  A["Inbox item"] --> B{"Evidence supports<br/>actionability?"}
  B -->|"no"| C["Stays in inbox,<br/>not promoted"]
  B -->|"yes"| D{"Overlaps with other<br/>live proposals?"}
  D -->|"no"| E["Promote directly to<br/>active roadmap"]
  D -->|"yes"| F["Distill overlapping proposals<br/>into one crosswalk"]
  F --> G{"Deltas confirmed or<br/>merged into an active owner?"}
  G -->|"not yet"| H["Crosswalk stands,<br/>individual sources remain"]
  G -->|"yes"| I["Retire the individual<br/>source documents"]
  I --> J["Crosswalk becomes the<br/>surviving record"]
  E --> K["Workstream assigned<br/>exactly one owner"]
  J --> K
```

## 2026-09-01 20:03 - MCP integration: per-agent config placement, upsert semantics, and a gated write surface

```yaml
adr_id: adr_mcp_integration_surface
grounded_in:
  - mse_vzsef0fmpsde2jh4:d1
```

```mermaid
flowchart TB
  INIT["memory-seed init runs"] --> AGENT{"Which agent?"}
  AGENT -->|"Claude"| CFILE["Write to project-root<br/>.mcp.json"]
  AGENT -->|"Codex"| XFILE["Write to<br/>.codex/config.toml"]

  CFILE --> UPSERT{"Matching command<br/>already present<br/>under our key?"}
  XFILE --> UPSERT
  UPSERT -->|"same command"| OVER["Overwrite -<br/>keep ours current"]
  UPSERT -->|"different command,<br/>same key"| LEAVE["Leave alone -<br/>protects the user's<br/>own server"]
  UPSERT -->|"key absent"| NEWW["Write new entry,<br/>unconditional (no PATH probe)"]

  subgraph GATE["Gated write surface"]
    APPEND["memory_session_append -<br/>the ONLY authoring path"] --> GUARD["Inherits the same<br/>write-time guards as the CLI"]
  end

  OVER -.-> GATE
  LEAVE -.-> GATE
  NEWW -.-> GATE
```

## 2026-09-01 20:04 - Trail: lifecycle visualization and decision-level row rendering

```yaml
adr_id: adr_trail_lifecycle_visualization
grounded_in:
  - mse_zm2h343r4shfre3j:d2
```

```mermaid
flowchart TB
  Edge["Lifecycle edge encountered<br/>(supersedes / evolves / related)"] --> Endpoints{"Edge endpoint kinds?"}
  Endpoints -->|"decision-to-decision<br/>or entry-to-decision"| Bold["Draw solid + bold"]
  Endpoints -->|"entry-to-entry"| Light["Draw dashed + light"]

  Entry["Entry with N decisions"] --> Multi{"Multiple decisions?"}
  Multi -->|"yes"| Anchor["Render one Trail row per decision,<br/>grouped under an anchor row"]
  Multi -->|"no"| Single["Single Trail row"]

  Anchor --> Click["User clicks any row<br/>in the group, or the single row"]
  Single --> Click
  Click --> Scope["Selection scoped by ENTRY identity"]
  Scope --> Lit["Entire entry's lineage lights up<br/>across the whole group"]
```

## 2026-09-01 20:05 - Encoding policy is owned by Memory Seed, with explicit check and repair tooling

```yaml
adr_id: adr_windows_encoding_policy
diagram_status: not_applicable
note: examined 2026-09-01 - rejected as a duplicate: this record and adr_encoding_policy both proposed the same head (mse_74ddxsena9nj2afk:d1) stating the same UTF-8/LF/NFC ownership rule, and adr_encoding_policy survives as the live record while this one was rejected. adr_encoding_policy is an ownership constraint with nothing to draw (states where a rule may live, not how anything flows); this rejected duplicate adds no mechanism that would change that verdict.
```

