---
tags:
  - session-log
  - memory-seed
session_date: 2026-05-29
default_user_initials: JN
---

# Session Log - 2026-05-29

## 2026-05-29 13:48 - Wire MCP server registration into memory-seed init

```yaml
entry_id: ms-4c8e2a17
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Added MCP server registration to `memory-seed init` and `update` for Claude Code, Cursor, and Gemini.
- Added PATH detection to `memory-retrieval-check.py`: if `memory-seed-mcp` is not on PATH (e.g. after `uvx memory-seed init`), the hook now outputs an actionable install message instead of instructing the agent to call a tool that isn't available.

### Decisions

#### D1 - Write MCP config unconditionally, no PATH check at init time

- D: `init_project` writes MCP server config for all three vendors unconditionally, without checking if `memory-seed-mcp` is on PATH first.
- R: If the user ran `memory-seed init`, the binary shipped in the same package and should already be on PATH. Checking at init time adds complexity for a failure case that can't happen through normal persistent installation.
- A: Check PATH at init and warn — rejected because the check is fragile (binary could disappear later) and doesn't cover the `uvx` case anyway.
- F: `memory_seed/core.py`.

#### D2 - Detect missing binary in the retrieval hook, not at init time

- D: `memory-retrieval-check.py` uses `shutil.which("memory-seed-mcp")` to detect whether the server is on PATH, and surfaces a `uv tool install memory-seed` instruction when it isn't.
- R: The hook fires at prompt time in the user's editor — it's the right moment to surface a fix. The `uvx` case (init ran ephemerally, binary not persisted) is the primary motivation.
- F: `memory_seed/seed/.memory-seed/hooks/memory-retrieval-check.py`, `.memory-seed/hooks/memory-retrieval-check.py` (byte-identical).

#### D3 - Cursor MCP uses separate .cursor/mcp.json, not hooks.json

- D: Cursor's MCP registration goes into `.cursor/mcp.json`; hooks stay in `.cursor/hooks.json`.
- R: Cursor treats hook config and MCP server config as separate files. Merging them into `hooks.json` would break Cursor's MCP discovery.
- F: `memory_seed/core.py` (`_merge_cursor_mcp`).

#### D4 - Claude MCP entry includes "type": "stdio"; Cursor and Gemini omit it

- D: Claude Code's `mcpServers` entry includes `"type": "stdio"`; Cursor and Gemini entries omit it.
- R: Claude Code's schema requires the `type` field. Cursor and Gemini infer stdio from the command shape and reject or ignore unknown fields.
- F: `memory_seed/core.py`.

### Implementation

- `memory_seed/core.py`: added `_MCP_SERVER_COMMAND`, `_MCP_SERVER_ARGS`, `_MCP_SERVER_KEY` constants; added `_merge_claude_mcp`, `_merge_cursor_mcp`, `_merge_gemini_mcp` functions; appended all three to the `hook_merges` tuple in both `init_project` and `update_project`.
- Both `memory-retrieval-check.py` copies: added `import shutil`; replaced single `reminder` assignment with `shutil.which` branch.

### Validation

- `python -m unittest discover -s tests` → 78 tests, OK (was 71; 7 new tests added).
- `init_project` in temp dir: Claude `mcpServers.memory-seed` present with `type: stdio`; Cursor `.cursor/mcp.json` present without `type`; Gemini `mcpServers.memory-seed` present.

### Follow-up

- Codex CLI has no documented MCP client support — skipped intentionally.
- Consider bumping package version and releasing once this is validated in a live editor.

## 2026-05-29 14:07 - Fix hook and MCP upgrade behavior (upsert) (upsert, not add-only)

```yaml
entry_id: ms-7b3f1e92
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Made all vendor config merge functions upsert (update-or-insert) instead of add-only.
- Previously: if a hook command changed between versions, `update` would append a new entry alongside the old one, causing both to fire.
- Now: merge functions identify their own entries by script filename (stable across command-string changes) and replace a stale command in place.

### Decisions

#### D1 - Script filename as the stable identifier for hook upsert

- D: Detect "our" hook entry by checking if the command string contains the script filename (e.g., `session-log-check.py`), not by exact command match.
- R: The script filename doesn't change between versions. Flags added to the command (the most likely version bump) would cause an exact-match check to miss, appending a duplicate. The filename check survives that.
- A: Version-stamp comments inside hook entries — rejected, would require non-standard comment fields in JSON.
- F: `_merge_grouped_hook`, `_merge_cursor_event_hook` (both now take `script_name` param).

#### D2 - Redirect four direct session-log functions to the helpers

- D: `_merge_claude_hook`, `_merge_codex_hook`, `_merge_gemini_hook`, `_merge_cursor_hook` previously duplicated the grouped/cursor-event logic inline. Replaced them with one-line delegations to the helpers.
- R: Eliminates four copies of the same upsert logic. All eight hook functions now share exactly two implementations.
- F: `memory_seed/core.py` — four functions reduced to thin wrappers.

#### D3 - MCP upsert: overwrite if command matches, skip if different command owns the key

- D: MCP merge functions compare the full expected dict. If the key exists with a different `command`, leave it alone (user may have a different server named `memory-seed`). If the command matches but args differ, overwrite.
- R: Protects user customization while still correcting stale args from our own prior installs.
- F: `_merge_claude_mcp`, `_merge_cursor_mcp`, `_merge_gemini_mcp`.

### Validation

- `python -m unittest discover -s tests` → 82 tests, OK (was 78; 4 new upsert tests added).

## 2026-05-29 14:23 - Enforce DRAFT format in session logs

```yaml
entry_id: ms-2e9c5f31
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Promoted DRAFT format documentation: "Reason Rules" section moved before "Entry Shapes" in `agent-rules.md` so agents read the format definition before seeing examples that use it.
- Added "**DRAFT** is the compact decision-record format" named introduction at the top of Reason Rules.
- Clarified T placement: T may appear inline as `- T:` or as a separate `### Validation` section — both acceptable.
- Extended `session-log-check.py` staleness reminder to include DRAFT labels inline, so agents see the format at the moment they're reminded to write.
- Extended `memory-retrieval-check.py` retrieval reminder to include a one-line DRAFT prompt at session start.
- All three files kept byte-identical between seed template and installed copy.

### Decisions

#### D1 - Move Reason Rules before Entry Shapes

- D: Swap section order in `agent-rules.md` so Reason Rules (DRAFT definition) precedes Entry Shapes (examples).
- R: A new agent reading sequentially encounters the DRAFT acronym in examples before it's ever defined. Reversing the order makes the format self-explaining.
- F: `memory_seed/seed/.memory-seed/agent-rules.md`, `.memory-seed/agent-rules.md`.

#### D2 - Embed DRAFT labels in the staleness hook reminder

- D: Add DRAFT label list to the `session-log-check.py` reminder text rather than adding a separate hook event.
- R: The staleness reminder fires at the exact moment agents are about to write a session entry — the highest-value point for a format nudge. No new infrastructure needed.
- F: `memory_seed/seed/.memory-seed/hooks/session-log-check.py`, `.memory-seed/hooks/session-log-check.py`.

#### D3 - Add DRAFT reminder to retrieval hook too

- D: Append a one-line DRAFT reminder to `memory-retrieval-check.py` so agents are primed for the format at session start, not just at write time.
- R: Priming at the start of a session means agents are thinking about DRAFT structure before they begin work, not just when the staleness check fires.
- F: `memory_seed/seed/.memory-seed/hooks/memory-retrieval-check.py`, `.memory-seed/hooks/memory-retrieval-check.py`.

### Validation

- `python -m unittest discover -s tests` → 82 tests, OK.
- Seed/installed parity confirmed for all three files.

## 2026-05-29 14:34 - Release 2.2.0

```yaml
entry_id: ms-1d7a4f88
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Bumped `pyproject.toml` to 2.2.0.
- Updated CHANGELOG.md: added DRAFT format bullets to 2.2.0 entry.
- Updated README.md: added auto-registration note to MCP Memory Search section.
- Updated `.memory-seed/index.md`: package version → 2.2.0.
- Committed 13 files, pushed to main, created GitHub Release v2.2.0.

### Validation

- 82 tests OK before commit.
- `gh release create v2.2.0` → https://github.com/jnl-tshi/memory-seed/releases/tag/v2.2.0
- PyPI publish triggered via GitHub Release creation.

## 2026-05-29 14:50 - Standardise DRAFT label required/optional tags and release 2.2.1

```yaml
entry_id: ms-5c3b8e12
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Fixed inconsistent required/optional tagging in the DRAFT label reminder text embedded in both hook scripts: previously `R` had `required` inside its parentheses, `A` and `F` had no qualifier, and `optional` appeared as a trailing word after `T` only.
- New consistent format: `D (Decision, required), R (Reason, required), A (Alternatives, optional), F (Files, optional), T (Tests, optional)` — each label self-contained.
- Bumped control-plane `memory-system-version` from `2.0` to `2.1` across all 26 seed and installed files (routing files, agent-rules.md, project-bootstrap.md, all skills) and updated `VERSION = "2.1"` in `core.py`.
- Updated version assertions in `tests/test_memory_seed.py` to match.
- Released package `2.2.1` — existing projects running `memory-seed update` will now receive the updated `agent-rules.md` (DRAFT format improvements) via version mismatch trigger.

### Decisions

#### D1 - Each label carries its own required/optional tag

- D: Change reminder text so every DRAFT label has its own `(required)` or `(optional)` qualifier, rather than grouping them with a trailing word.
- R: The previous format required parsing grouping to determine a label's status. Per-label tagging is unambiguous regardless of reading order or truncation.
- F: Both copies of `session-log-check.py` and `memory-retrieval-check.py` (seed + installed).

#### D2 - Patch release 2.2.1 for control-plane version bump

- D: Ship the control-plane version bump as a separate patch release (2.2.1) rather than rolling it into 2.2.0.
- R: 2.2.0 was already published. The version bump is a mechanical change with clear standalone value — it enables `memory-seed update` to propagate DRAFT improvements to existing projects.
- F: `core.py` VERSION, all 26 seed/installed `.md` frontmatter files, `pyproject.toml`, `tests/test_memory_seed.py`.

### Validation

- `python -m unittest discover -s tests` → 82 tests, OK.
- `memory-seed doctor` → healthy. `memory-seed version` → 2.1.
- `gh release create v2.2.1` → https://github.com/jnl-tshi/memory-seed/releases/tag/v2.2.1

## 2026-05-29 14:54 - Fix staleness check: use entry heading timestamp, not file mtime

```yaml
entry_id: ms-8f4d2c61
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- The session-log-check.py staleness check was using `f.stat().st_mtime` to detect recent writes. A `git commit` touching the session file updates mtime and silently satisfied the check, causing the hook to stay silent even when no new entry was written. Discovered because the hook failed to remind the agent after the 2.2.1 release commit.
- Fixed by reading entry heading timestamps from the file content instead. The most recent `## YYYY-MM-DD HH:MM` heading is parsed and compared to the 15-minute cutoff.
- As a side-effect, `heading_re` and `today_file` are now defined once and shared between both the staleness and chronology checks.
- Added 4 new tests covering: no session file, stale entry, recent entry, and the specific git-mtime defeat case.

### Decision

- D: Parse the most recent `## YYYY-MM-DD HH:MM` heading timestamp from the session file instead of checking `f.stat().st_mtime`.
- R: File mtime reflects the last OS-level write to the file (including git operations), not the last session entry written by an agent. Content-based checking is the only way to distinguish "agent wrote an entry" from "git touched the file".
- A: Keep mtime but exclude git processes — rejected because detecting which process modified a file is fragile and platform-specific.
- F: `memory_seed/seed/.memory-seed/hooks/session-log-check.py`, `.memory-seed/hooks/session-log-check.py`, `tests/test_memory_seed.py`.
- T: 86 tests OK. `test_staleness_not_defeated_by_file_mtime` specifically validates the fix.

## 2026-05-29 15:17 - Fix MCP server PATH resolution to use uvx

```yaml
entry_id: ms-a3f91c2b
user_initials: JN
agent_type: claude
project_path: .
subproject_path: null
```

### Decision

- D: Change `_MCP_SERVER_COMMAND` from `"memory-seed-mcp"` to `"uvx"` with `"memory-seed-mcp"` as the first arg.
- R: Claude Code (and other agents) spawn MCP servers without inheriting the user's full PATH. `~/.local/bin` — where `uv tool` installs scripts — is not on Claude Code's PATH, so `memory-seed-mcp` was silently not found. `uvx` is part of uv and is typically available at the system level.
- A: Hardcoding the absolute path was rejected — breaks portability across machines and users.
- F: `memory_seed/core.py` (`_MCP_SERVER_COMMAND`, `_MCP_SERVER_ARGS`); `.claude/settings.json`, `.cursor/mcp.json`, `.gemini/settings.json` updated to match.

## 2026-05-29 15:19 - Correct uvx invocation to use --from memory-seed flag

```yaml
entry_id: ms-b72e3d40
user_initials: JN
agent_type: claude
project_path: .
subproject_path: null
```

### Decision

- D: Change `_MCP_SERVER_ARGS` to `["--from", "memory-seed", "memory-seed-mcp", "--stdio"]`.
- R: `uvx memory-seed-mcp` treats `memory-seed-mcp` as a PyPI package name and fails with "not found in registry". The PyPI package is `memory-seed`; `--from memory-seed` tells uvx which package provides the script. Verified with live MCP handshake — server responded correctly.
- A: `uvx memory-seed-mcp` (no --from) — fails; bare `memory-seed-mcp` — PATH-dependent, fails in Claude Code.
- F: `memory_seed/core.py`, `.claude/settings.json`, `.cursor/mcp.json`, `.gemini/settings.json`.

## 2026-05-29 15:22 - Release 2.2.2: version bump, docs, and migration guard for uvx fix

```yaml
entry_id: ms-c94f7b51
user_initials: JN
agent_type: claude
project_path: .
subproject_path: null
```

### Summary

- Bumped package to 2.2.2 (control-plane stays 2.1 — no seed files changed).
- Updated CHANGELOG, README (auto-registration note, Agent Support table, Current Version section).
- Fixed upsert guard in `_merge_claude_mcp`, `_merge_cursor_mcp`, `_merge_gemini_mcp`: old `memory-seed-mcp` entries are now recognized as ours and migrated to the `uvx --from` form, rather than being treated as a foreign server and left stale.
- Added `_OWN_MCP_COMMANDS` set to `core.py` to centralize recognition of both old and new command forms.
- Updated 4 failing tests to assert the new command/args shape and the migration behavior.

### Decisions

#### D1 - Recognize old command as ours during upsert

- D: Check `existing.get("command") in _OWN_MCP_COMMANDS` rather than exact equality with the current command.
- R: Without this, `memory-seed update` on a project using the old `memory-seed-mcp` command would silently leave it stale — the guard treated it as a foreign server. The migration must be automatic.
- F: `memory_seed/core.py` (`_OWN_MCP_COMMANDS` constant, guard in all three `_merge_*_mcp` functions).

### Validation

- 86 tests OK.

## 2026-05-29 15:36 - Release 2.2.3: harden session-log append discipline

```yaml
entry_id: ms-d15e8a03
user_initials: JN
agent_type: claude
project_path: .
subproject_path: null
```

### Summary

- Added `>>` / append-mode rule to `agent-rules.md` Append-Only Chronology section: agents must use shell redirection or Python append mode, not editor replace/insert, to write session entries.
- Updated order-warning in `session-log-check.py` to include concrete repair instruction (use `>>` or `open(f, 'a')`).
- Bumped control-plane to `2.2`, package to `2.2.3`. All 13 seed `.md` files updated to `2.2`.
- Updated README "Current Version" to `2.2`.
- Fixed 5 test assertions to match new version strings.

### Decision

- D: Use `>>` / Python append mode as the required method for writing session entries.
- R: Editor replace/insert requires an anchor line that may not be at the true end of the file if a prior edit added content after it. Append mode writes unconditionally to the physical end with no anchor, making correct ordering mechanical rather than reliant on care.
- F: `.memory-seed/agent-rules.md`, `memory_seed/seed/.memory-seed/agent-rules.md`, both `session-log-check.py` copies, `core.py`, `pyproject.toml`, `CHANGELOG.md`, `README.md`, `tests/test_memory_seed.py`, all 13 seed `.md` frontmatter files.
- T: 86 tests OK.
