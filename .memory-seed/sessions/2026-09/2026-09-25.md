---
tags:
  - session-log
  - memory-seed
session_date: 2026-09-25
---

## 2026-09-25 00:02 - Make the broken-venv PATH test platform-honest

```yaml
entry_id: mse_bgq7zez6hveyxc8y
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-fix-windows-only-test
decision_origins:
  d1: agent
```

### Summary

After pushing `b633fcca`, CI Verify failed on Linux. The single failing test came from the 2026-09-24 merge-cleanup work (commit 8f0a255f), not from the Task Packet slice.

### Records

#### D1 - Decision: Test each platform's real branch instead of faking os.name
- D: `test_git_subprocess_env_drops_broken_windows_venv_scripts_from_path` no longer patches `memory_seed.core.os.name` to `"nt"`. On Windows it asserts the broken venv `Scripts` entry is dropped; elsewhere it asserts `PATH` passes through unchanged.
  - Scope: One test; no product code.
  - Disposition: Implemented to restore green CI on main.
- R: Faking `os.name` on Linux makes `pathlib.Path` construct a `WindowsPath`, which raises `NotImplementedError` there, so the test could never pass in CI. Each platform now exercises the branch it actually runs.
- F: `tests/test_session_fuse_and_merge.py`.
- T: The test passed locally on Windows. CI run 36070413938 showed the Linux failure; the fix is verified by the next CI run.
