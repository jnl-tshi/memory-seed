---
tags:
  - session-log
  - memory-seed
session_date: 2026-09-23
---

## 2026-09-23 08:50 - Harden Windows worktree lock recovery

```yaml
entry_id: mse_n8x2w1frxvkgwp9y
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/fix/windows-worktree-lock-recovery
decision_origins:
  d1: user
```

### Summary

- Hardened the reusable worktree-reconciliation workflow against Windows directory locks inherited by app-managed helper processes.

### Records

#### D1 - Decision: Recover exact Windows residue through a verified helper lifecycle

- D: Extend worktree reconciliation with a bounded Windows recovery path: diagnose exact lock owners read-only, require separate live approval for process termination, stop only freshly reverified helper descendants, preserve the owning app/server, revalidate and delete the exact residue once, then restart helpers through their owning tool surfaces with non-sensitive health checks.
  - Scope: Deregistered worktree residue already proven empty or disposable whose literal-path deletion fails because Windows reports an active handle.
  - Disposition: Implemented.
- R: The v2.22 release cleanup reproduced a gap the prior fail-closed rules handled safely but could not resolve: CUA, Semble, and Context Mode helpers inherited the removed worktree as their current directory, so waiting and retrying could never release it.
- R: Deletion authority and helper-interruption authority have different blast radii; keeping them separate prevents an approved directory cleanup from silently disrupting shared Codex services.
- A: Repeated blind retries, killing by executable name, terminating `codex.exe` or `ChatGPT.exe`, directly closing foreign handles, and raw helper relaunches were rejected as stale-PID, stability, or reconnection hazards.
- A: A stale globally installed Memory Seed MCP authoring helper was rejected after its dry run required the pre-v2.22 session schema; the checkout-local CLI was used instead.
- F: `.memory-seed/skills/worktree_reconciliation.md`, `memory_seed/seed/.memory-seed/skills/worktree_reconciliation.md`, `tests/test_session_schema.py`.
- T: The focused contract test passed; the broader session-schema and project-lifecycle suite passed 80 tests; `git diff --check` passed; live/seed hashes match; doctor reports healthy/bootstrap-complete; links integrity is OK with pre-existing warnings. Encoding check still reports six pre-existing issues outside the changed paths.
- S: Hardened runbook `.memory-seed/skills/worktree_reconciliation.md`

### Validation

- `python -X utf8 -m pytest tests/test_session_schema.py tests/test_project_lifecycle.py -q --tb=short` — changed skill, seed twin, and contract test; post-change; 80 passed; status: passed.
- `git diff --check` plus SHA-256 live/seed comparison — post-change; no whitespace errors and hashes match; status: passed.
- `python -X utf8 -m memory_seed.cli doctor` and `links check` — post-change; runtime healthy/bootstrap-complete and session integrity OK, with existing corpus warnings; status: passed.
- `python -X utf8 -m memory_seed.cli encoding check` — post-change; six pre-existing issues outside the changed paths remain; status: failed.

### Follow-up

- No release, tag, remote push, or package publication is part of this workstream.
