## 2026-09-06 12:00 - Implement progressive provenance public surfaces

```yaml
entry_id: mse_2cv0xm4mbgepa2d6
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/provenance-surfaces-boundaries-v2
related_entries:
  - mse_72c6knzvhwzss39j:d2
  - mse_6npe4ys5j6gfj89s:d1
```

### Decisions

#### D1 - Share provenance validation across CLI and MCP adapters

- D: Add `provenance show`, `bind`, and `check`, plus MCP decision-provenance read/write tools, through one append-only Markdown-event adapter over the shared provenance and Git-projection contracts.
- R: Public surfaces must neither persist code nor diverge in validation; a single adapter preserves owner checks, bounded projections, dry-run/apply behavior, and reference-only storage.
- F: `memory_seed/cli.py`, `memory_seed/mcp_server.py`, `tests/test_provenance_surfaces.py`.

#### D2 - Surface audit and temporal-lineage limits in ESR

- D: Add ESR provenance sidecar/reference audit and temporal-lineage reporting, publishing temporal cache only after its required ignore entry is already declared.
- R: Git proves reachable ordering, not wall-clock truth, and an ESR run must report a missing cache prerequisite rather than silently alter an unscoped ignore policy.
- F: `memory_seed/esr.py`, `tests/test_esr.py`.

### Tests

- T: Focused provenance surface, ESR, MCP, and engine-contract coverage passed before the packet acceptance suite.

### Summary

Implemented the packet-scoped progressive provenance public surfaces only. No Git derivation internals, hooks, lifecycle mutations, merge, push, or release action was performed.

## 2026-09-06 13:00 - Harden provenance runtime and append-only evidence

```yaml
entry_id: mse_7yrpnx76kdd71ts4
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/provenance-surfaces-boundaries-v2
related_entries:
  - mse_72c6knzvhwzss39j:d2
  - mse_6npe4ys5j6gfj89s:d1
```

### Decisions

#### D1 - Measure active pod authority from nested runtime topology

- D: Accept an active pod record only when its path resolves to a nested `.memory-seed` runtime, its id matches that path leaf, and a writer is executing in that exact runtime; roots may still explicitly inspect it.
- R: A caller-controlled runtime object cannot establish ownership. Binding authority must be derived from the filesystem topology before sidecar selection.
- F: `memory_seed/cli.py`, `tests/test_provenance_surfaces.py`.

#### D2 - Anchor sidecar events to Git history

- D: Report append-only status as verified only when the working sidecar retains the committed prefix and every reachable committed revision extends its predecessor; otherwise report violated or unverifiable.
- R: Schema-valid ledger reconstruction alone cannot detect event deletion or reordering. Git history provides a mechanical event-order anchor without inventing an external time claim.
- F: `memory_seed/cli.py`, `tests/test_provenance_surfaces.py`.

#### D3 - Publish ESR temporal classifications through the declared cache

- D: Declare the temporal cache ignore entry, refresh it during ESR, and emit each decision's relative-order, claim-relation, and calendar-evidence classification.
- R: The accepted ESR observable requires actual classifications, while cache publication must remain derived and ignored.
- F: `.gitignore`, `memory_seed/esr.py`, `tests/test_esr.py`.

### Tests

- T: Topology forgery, parent-write refusal, retired inspection, event deletion/reordering, missing decisions/Git, CLI descendant parity, and ESR temporal classifications pass with the packet covering suites.

### Summary

Addressed every round-one review finding without changing provenance derivation internals, hooks, lifecycle state, merge, push, or release behavior.
