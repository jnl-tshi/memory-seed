---
tags:
  - session-log
  - memory-seed
session_date: 2026-09-14
---

## 2026-09-14 02:45 - Implement DRAFTS sources and preferred keyword search

```yaml
entry_id: mse_mrrnd0wam54vjrpc
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/drafts-source-search
decision_origins:
  d1: user
  d2: user
```

### Summary

- Implemented the approved DRAFTS source-attribution and preferred-keyword retrieval plan.

### Decisions

#### D1 - Make sources part of the decision record

- D: Rename the current DRAFT mnemonic to DRAFTS and add repeatable `S:` items for repository-local artifacts that materially informed a decision; validate supplied references on append and expose their current resolution in retrieval.
- R: A decision derived from a proposal should carry a direct, machine-readable route back to that proposal so later recall does not have to infer the relationship.
- A: Requiring `S:` on every historical and new entry was rejected because many decisions are made directly in conversation and historical records remain append-only.
- F: `.memory-seed/skills/session_logging.md`, `memory_seed/core.py`, `memory_seed/semantic_cache.py`, `memory_seed/corpus_cache.py`, `memory_seed/retrieval.py`, `memory_seed/mcp_server.py`, `memory-trace/client/src/EntryReader.tsx`, and their documentation and tests.
- T: Focused Python validation passed: 351 tests and 11 subtests, with 5 unrelated integration tests deselected; Memory Trace typecheck and 238 tests passed.
- S: Governing policy `docs/CONSTITUTION.md#draft-format`

#### D2 - Add positive keyword steering without case-sensitive misses

- D: Add optional `preferred_keywords` to `memory_search`, normalize all lexical query and index text with Unicode NFKC plus case folding, and cap the positive BM25F preference bonus at the result's base relevance.
- R: Optional keywords give callers fine-grained control when a natural-language query blends nearby concepts, while a positive bounded nudge preserves recall and cannot inject a zero-match result.
- A: Excluded keywords and hard filtering were deferred because they can silently hide relevant history; lowercasing alone was rejected because Unicode case folding covers more capitalization-equivalent forms.
- F: `memory_seed/semantic_cache.py`, `memory_seed/retrieval.py`, `memory_seed/mcp_server.py`, `README.md`, and retrieval/cache/MCP tests.
- T: Case-equivalent query tests, preference diagnostics tests, source round-trip tests, and default-payload compatibility tests passed in the focused suite.

### Validation

- `python -m pytest tests/test_session_schema.py tests/test_core_misc.py tests/test_links_check.py tests/test_session_append.py tests/test_semantic_cache.py tests/test_mcp_server.py tests/test_corpus_cache.py -q -m "not integration"` — passed, 351 tests and 11 subtests; 5 integration tests deselected.
- `npm run typecheck` and `npm test` in `memory-trace/client` — passed, including 238 Node tests, using the existing dependency installation linked into the isolated worktree.
- `python -m memory_seed.cli adr check` and `python -m memory_seed.cli links check` — passed; existing warnings remain advisory.

### Follow-up

- After this tooling lands, append a new React decision that directly cites the frontend architecture proposal instead of rewriting the historical decision.
