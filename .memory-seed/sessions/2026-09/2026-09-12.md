---
tags:
  - session-log
  - memory-seed
session_date: 2026-09-12
---

## 2026-09-12 08:32 - Work through PR #4 CI for the first time this branch's history has run on GitHub Actions

```yaml
entry_id: mse_qgwse8k9dwtcysyh
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/repository-overview-42a0fd
```

### Summary

Opened [PR #4](https://github.com/jnl-tshi/memory-seed/pull/4) for the 2.21.0 release-prep branch and, with
Auto-fix pull requests watching it, worked through CI on GitHub Actions for the first time this branch's
415+ commit history has ever run there. Fixed six rounds of genuine bugs the run surfaced - none from this
session's own diff - plus triaged a flood of CodeQL findings across the huge diff. `Analyze (python)`,
`Analyze (javascript-typescript)`, `React workspace`, and `Python and Memory Trace` are all green as of
`28e3d7e2`; only the CodeQL alert-count gate remains, blocked on JNL dismissing false positives the
permission classifier would not let me dismiss via API. This entry should have been appended after the
first CI-fix round instead of being deferred across five more pushes - the Stop hook's repeated reminder
was missed, not heeded.

### Decisions

#### D1 - Fix the first wave of pre-existing CI defects this branch had never been tested against

- D: Extended `tests/test_corpus_read_path.py`'s allowlist for two undocumented raw-corpus-read call sites
  (`cli.py`'s `_provenance_decision` identity lookup by decision `chunk_id`; `esr.py`'s temporal-lineage
  tamper detection needing digest/timestamp fields, not the lifecycle graph). Synced
  `memory_seed/seed/.memory-seed/skills/topic_swarm.md` to its live twin (the live copy correctly pointed
  at `docs/8_Deferred/` after a roadmap-lane move; the seed twin still had the old `docs/2_Todo/` path).
  Fixed a Windows-only unit test comparing a hermes config path with the ambient `pathlib.Path` instead of
  `PureWindowsPath`. Made `tests/test_cli_help.py`'s `_git_repo_with_commit` persist git identity into the
  fixture repo's own config instead of a one-off `-c` override, since `git merge --no-ff --no-commit`
  validates committer identity up front even without committing, and `session_merge_branch`'s internal git
  calls carry no `-c` of their own - a dev machine's global git identity masked this; CI runners have none.
  Disabled `gc.auto` on `tests/test_mcp_read_parity.py`'s fixture repo, whose read-only-call tests snapshot
  the whole repo before/after: a `git commit`-triggered background `git gc --auto` can still be mid-run
  (stray `.git/objects/maintenance.lock`, rewritten refs/packs) when the second snapshot fires. Regenerated
  `memory-trace/tests/contract/types.ts` from the already-current `openapi.v1.json` per
  `memory-trace/tests/contract/README.md`'s documented step - the committed TypeScript bindings were stale,
  not the frontend code that read the `reason` field they lacked. Rebuilt the committed React bundle
  (`memory-trace/memory_trace/static/react/`) that `npm run build` + `git diff --exit-code` verifies.
- R: This branch (`4f4feca7` onward) had never run on GitHub Actions before - only local Windows `pytest`
  runs, which never exercised Linux-only git/CI-runner conditions or checked generated-artifact staleness.
  Every one of these was a real, independently-reproduced defect (confirmed locally under an
  identity-stripped git config for the `test_cli_help.py` case), not flakiness.
- F: `tests/test_corpus_read_path.py`, `memory_seed/seed/.memory-seed/skills/topic_swarm.md`,
  `tests/test_task_packet_calibration_harness.py`, `tests/test_cli_help.py`, `tests/test_mcp_read_parity.py`,
  `memory-trace/tests/contract/types.ts`, `memory-trace/memory_trace/static/react/**`.
- T: Each fix verified individually against its own test file before the combined push (`33de5f4d`,
  `8383ee90`); `memory-trace/client`'s `npm run typecheck` and `npm test` (238/238) passed after the
  contract regeneration and rebuild.

#### D2 - Fix Task Packet's self-referential token-estimate non-convergence

- D: `compile_task_packet`'s 20-iteration loop converges the packet's own serialized byte-length estimate
  against its embedded ledger, which itself is sized by that estimate. On CI (never locally - the estimate
  is sensitive to embedded absolute-path length, which differs by checkout) the loop can enter a true
  2-cycle with no fixed point. Added cycle detection that keeps the safest (largest `total_input_tokens`)
  ledger from the cycle, and - after a first push exposed a second, independent self-consistency check
  further down that no cycle member could satisfy by construction - track whether the fallback ran and, only
  then, heal `serialized_packet_input_tokens` to the packet's real final measured byte count rather than
  raising on drift that has no fixed point to find. The normal single-fixed-point path is untouched and
  still hard-fails on any unexplained drift.
- R: `estimate_tokens` is a plain ceiling-division byte-length heuristic with no external dependency, so the
  same deterministic code producing different results on two machines meant the *input* differed, not the
  logic - most likely the embedded absolute checkout path's length shifting which byte-length boundary the
  self-reference lands on.
- A: Considered relaxing the final consistency check's exactness broadly; rejected because that would also
  hide genuine future regressions on the normal, convergent path. Healing only in the detected-fallback
  branch preserves the check's value everywhere else.
- F: `memory_seed/task_packet.py`.
- T: `tests/test_task_packet.py` + `test_task_packet_surfaces.py` + `test_task_packet_calibration_harness.py`
  (73 tests, 153 subtests) pass after both fix commits (`e8a4ea63` then a correction). CI (`Python and
  Memory Trace`) went from failing on this exact test twice to passing.

#### D3 - Fix a real ReDoS in the identifier-extraction regex (CodeQL `py/polynomial-redos`)

- D: `IDENTIFIER_RE` had two adjacent `[A-Za-z0-9_.-]*` quantifiers wrapping a mandatory `[A-Za-z_]` middle
  character, all drawing from heavily-overlapping character classes - ambiguous backtracking by
  construction. Collapsed to one linear `[A-Za-z0-9_.-]+` and moved the "must contain a letter or
  underscore" requirement into a cheap Python check in `_extract_lexical_terms`.
- R: Verified this was a genuine vulnerability, not a CodeQL false positive, by confirming the ambiguous
  shape directly and timing a 5,000-character run of repeated `-` before and after. Confirmed identical
  accepted/rejected strings across representative inputs (paths, dates, dashed/underscored identifiers, IDs,
  pure-punctuation runs) between the old and new pattern before changing production code.
- F: `memory_seed/semantic_cache.py`.
- T: `tests/test_semantic_cache.py` + `test_retrieval.py` (80 tests) pass unchanged; the pathological input
  resolves instantly instead of hanging.

#### D4 - Fix `links check`'s duplicate-entry-id false positive, and the regression fixing it first caused

- D: The duplicate-entry-id detector scanned the whole file for `entry_id:` lines with no regard for
  context, so a Reflection Board receipt block's legitimate repeated citation of the entry it attaches to
  counted as N-1 duplicate declarations. Added `_declared_entry_ids`, which reads only the `entry_id:`
  inside each entry's own first metadata fence (mirroring `check_entry_metadata_fences`'s established
  anchor to the canonical, strict entry-heading grammar). That first version then silently dropped a
  legacy pre-convention entry (`## <date> - <title>`, no time component) that the strict heading regex
  doesn't recognize as a boundary, orphaning six topic-sidecar references to it - caught by re-running
  `links check` against the real corpus, not by the test suite. Fixed by splitting on a locally-scoped,
  more permissive heading pattern instead, safe here because this function only reads id/fence structure
  and never needs append/fuse/reorder's stricter refusal of a body line that merely looks like a heading.
- R: This was the FIRST CI run ever to exercise `links check` on this branch's full session-log history;
  `memory-seed doctor` had already surfaced "3 session integrity issues" locally before this session touched
  anything, and a prior session's own decision log explicitly named it: "ESR retains inherited duplicate
  September 8 IDs." Investigating rather than deferring or loosening the CI gate confirmed it was the
  checker at fault, not the corpus.
- A: Considered leaving `links check` non-blocking for this pre-existing issue instead of fixing the
  checker; rejected once the root cause turned out to be a real bug in the checker itself, not a real data
  defect requiring an append-only correction record.
- F: `memory_seed/core.py`.
- T: `tests/test_links_check.py` + `test_session_schema.py` + `test_session_fuse_and_merge.py` (227 tests, 5
  subtests) pass. `memory-seed links check` against the real repository corpus now reports "Session memory
  integrity OK (260 file(s) checked)" instead of 3 errors (or, after the first partial fix, 6 different
  ones).

#### D5 - Triage the CodeQL alert flood from the PR's 416-commit diff; hand dismissal to JNL where blocked

- D: The PR's huge diff (never scanned before) surfaced 21 CodeQL alerts across the session in total: 3+7
  `clear-text-logging/storage-sensitive-data` (all trace to `WorkstreamRebind.detail_digest`/
  `pre_ledger_digest` - SHA-256 ledger integrity digests - and one printed SHA-256 checksum in an unrelated
  experiments script, flagged purely for containing the word "digest"), 7 `path-injection` (three in
  `resolve_runtime(cwd)` - a CLI tool resolving its own invoker's filesystem path, no privilege boundary
  crossed - and four in `memory-trace/memory_trace/service.py`, each already gated behind
  `allow_external_project_access` being true only on a confirmed-loopback server, or validated against an
  explicit worktree allowlist in `service_for()` immediately after the flagged line), and 1
  `polynomial-redos` (real; see D3). Verified each false-positive claim by reading the actual code path,
  not by assuming the pattern repeated. Two inline-suppression syntaxes (`codeql[...]`, `lgtm[...]`) were
  tried on the digest findings and empirically failed to suppress re-filing on the next scan; dismissing via
  `gh api ... code-scanning/alerts` was blocked by the permission classifier for all but one attempt.
  Declined to add a `codeql-config.yml` disabling the two digest queries repo-wide when offered as an
  option - JNL chose manual dismissal instead, to keep those queries live for genuinely new future findings.
- R: A single false-positive pattern repeating across a huge diff still needs per-instance verification
  before dismissal, since the diff also contained one genuine finding (D3) that pattern-matching past
  without reading would have missed.
- A: Considered turning off CodeQL as a blocking check for this PR instead of triaging; JNL chose to triage
  properly. Considered batch-dismissing the whole digest+path-injection set without individual review;
  the ReDoS finding proved that assumption wrong for at least one alert type, so full triage went ahead.
- F: None (alert-state changes on GitHub, not repo files).
- T: `Analyze (python)` and `Analyze (javascript-typescript)` (the actual CodeQL scan jobs) pass on every
  push; the alert-count gate itself remains red pending JNL's dismissal of the alerts I could not dismiss.

### Follow-up

- 15 of 16 remaining open CodeQL alerts still need JNL's manual dismissal via the Security tab (one,
  `#13`, went through via API before the classifier began blocking further attempts): path-injection
  `#12,14,15,16,30,31`; clear-text-logging/storage `#17-#23,#27-#29`.
- Once CodeQL clears, PR #4 is green on every check. Remaining steps per JNL's original request: merge to
  `main`, watch CI there, then create the GitHub Release - stopping before the `pypi` environment's manual
  approval gate, which stays JNL's to approve.

## 2026-09-12 08:40 - Fix the Stop hook so its session-log reminder actually reaches Claude

```yaml
entry_id: mse_p0k7a94qwgmma5aw
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/repository-overview-42a0fd
```

### Summary

JNL asked why the Stop hook's repeated session-log reminders (visible to them as a warning banner)
never reached me across the prior CI-fix session. Investigated `.memory-seed/hooks/session-log-check.py`.

### Decisions

#### D1 - Fix the Stop hook's `claude` branch to actually reach the model, not just the user

- D: Changed `session-log-check.py`'s `claude` output branch from `{"systemMessage": reminder}` to
  `{"decision": "block", "reason": reminder}`, and synced the seed twin. Fix is scoped to that one
  branch of the existing agent-keyed `if/elif/else` chain; codex/cursor/gemini branches are untouched.
- R: `systemMessage` on a Stop hook is Claude Code's user-facing UI text field, never added to the
  model's context - confirmed both by the mismatch between the mobile screenshot (banner shown to JNL)
  and my own transcript (nothing ever appeared there across five reminder-worthy pushes), and by direct
  comparison with `memory-retrieval-check.py`'s own `claude` branch in the same folder, which already
  uses the correct field for its event type (`hookSpecificOutput.additionalContext` for
  UserPromptSubmit) and has been reaching me all session. `decision: "block"` + `reason` is the
  documented mechanism for a Stop hook to feed text back to Claude and keep it from stopping.
- A: JNL explicitly required the fix stay vendor-neutral - not weaken or change the codex/cursor/gemini
  branches. Scoping the change to the `claude` branch alone satisfies that: no other agent's output
  shape changed.
- F: `.memory-seed/hooks/session-log-check.py`, `memory_seed/seed/.memory-seed/hooks/session-log-check.py`.
- T: Manually invoked the hook in a scratch project and confirmed the new JSON shape.
  `tests/test_session_log_ordering_hook.py` + `test_hook_merge.py` + `test_session_schema.py` (64 tests,
  including the live/seed byte-identity check) pass unchanged - no test asserted on the wrapper key,
  only that the reminder text appears in stdout.

### Follow-up

- This was itself a live demonstration of the bug: this fix should have been the first thing the hook
  reminded me to log, and it's the first time in this session a Stop-hook reminder for this file will
  actually reach the model instead of only the UI.
