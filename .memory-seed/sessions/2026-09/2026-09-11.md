---
tags:
  - session-log
  - memory-seed
session_date: 2026-09-11
---

## 2026-09-11 22:40 - Cut memory-seed 2.21.0 and clean up merged integration worktree

```yaml
entry_id: mse_18dg77y2c81z098a
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/repository-overview-42a0fd
```

### Summary

Prepared the memory-seed 2.21.0 release (version bump + changelog fold) and, along the way, closed out
the merged `superpowers-delivery-uplift-integration` worktree and resynced the globally-installed
`memory-seed` uv tool to current source. Merge to `main`, push, and the actual GitHub Release are not
done yet.

### Decisions

#### D1 - Cut release 2.21.0: bump every version surface and fold the changelog

- D: Bumped `pyproject.toml`, `memory_seed/core.py`'s `VERSION`, and all 39 `SEED_FILES` markdown
  control-plane files (live + seed twins, 78 files) from 2.20 to 2.21. Folded `CHANGELOG.md`'s
  `## Unreleased` section into `## 2.21.0 - 2026-09-11`, leaving a fresh empty `## Unreleased` header.
- R: JNL asked to cut the next package release. `.memory-seed/skills/release_publishing.md` requires
  every version surface bumped together (root routing files are the historical miss a guard test now
  pins) and the Unreleased section folded before a version is published.
- A: Skipped creating a `.memory-seed/archive/2.21/` control-plane snapshot for this cut, matching the
  2.20.0 release, which also shipped without one; the archive step is an explicit, non-gated operator
  action and no test requires it per version. (A `.memory-seed/archive/2.21/adr-ledger-v1-preimage/`
  directory already exists from unrelated prior ADR-ledger work and was left untouched.)
- F: `pyproject.toml`, `memory_seed/core.py`, `CHANGELOG.md`, `tests/test_project_lifecycle.py` (pinned
  version literal), plus the 39 control-plane markdown files (live + seed twins).
- T: Targeted guard tests passed 16/16 (`pytest tests/test_project_lifecycle.py -k "version or
  control_plane or seed_files"`); `memory-seed doctor` reports the control plane healthy with no drift.
  The full `pytest tests` suite was still running in the background at commit time. Its first run
  aborted at 42% with exit 1 immediately after an unrelated `memory-seed shutdown --yes` call (see D2) -
  that looks like the shutdown killing the pytest process as collateral, not a real failure - and a clean
  rerun was started to get a trustworthy result before merging.

#### D2 - Clean up the merged integration worktree and resync the global CLI to match

- D: Removed the fully merged, clean `superpowers-delivery-uplift-integration` worktree: `git worktree
  remove` deregistered it but failed to delete the directory/admin metadata on Windows (permission
  denied), so the residual `.codex/worktrees/superpowers-delivery-uplift-integration/` directory and the
  now-empty `.git/worktrees/superpowers-delivery-uplift-integration/` were deleted by hand after
  confirming its HEAD matched `main`'s tip exactly and it was clean. Then reinstalled the
  globally-installed `memory-seed` uv tool (`uv tool install --from <this worktree> memory-seed --force
  --reinstall`) so it reports 2.21 instead of the stale 2.20.
- R: `main` was checked out in that stale worktree, blocking any fresh worktree checkout of `main` needed
  to land this release. Separately, JNL asked to update the global memory-seed install to the latest
  iteration; the uv-tool install other projects on this machine invoke was a version behind mid-release-
  prep.
- A: The first install attempt failed (`Access is denied` replacing the tool's venv) because 16 running
  `memory-seed-mcp` processes belonging to other concurrent sessions on this machine were holding its
  files open. Asked JNL before running `memory-seed shutdown --yes` rather than assuming it was safe to
  interrupt other sessions' MCP calls; JNL chose to shut them down now. Confirmed with `memory-seed
  processes` that none remained before retrying the install.
- F: none (worktree removal, process shutdown, and tool reinstall are outside the tracked repo diff).

### Follow-up

- Merge this branch to `main`, push to origin, watch CI, then create the GitHub Release - stopping before
  the `pypi` environment's manual approval gate, which is JNL's to approve.
- Confirm the rerun of the full `pytest tests` suite passes cleanly before merging.
- `memory-seed doctor` also reports pre-existing, unrelated warnings (stale prepare-commit-msg hook, 3
  session integrity issues, 83 encoding issues) - not addressed here, not new regressions from this work.
