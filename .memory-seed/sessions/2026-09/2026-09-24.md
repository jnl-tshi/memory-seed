---
tags:
  - session-log
  - memory-seed
session_date: 2026-09-24
---

## 2026-09-24 03:17 - Evaluate decision-to-chat alignment feasibility

```yaml
entry_id: mse_qrtbpakck8rs1zt4
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/decision-alignment-study
decision_origins:
  d1: user
```

### Summary

- Completed a reproducible, read-only experiment aligning 50 randomly sampled Memory Seed decisions to retained local Codex conversation windows.

### Records

#### D1 - Documentation: Evaluate retrospective decision-to-chat alignment

- D: Documented the alignment instrument, sanitized 50-row audit, quantitative outcome, failure analysis, dataset feasibility, and next experiment.
  - Scope: Retrospective provenance recovery from canonical Memory Seed Decision chunks to local Codex JSONL rollout windows.
- F: `experiments/decision-chat-alignment/DESIGN-NOTE.md`, `experiments/decision-chat-alignment/README.md`, `experiments/decision-chat-alignment/REPORT.md`, `experiments/decision-chat-alignment/align_decisions.py`, `experiments/decision-chat-alignment/test_align_decisions.py`, `experiments/decision-chat-alignment/results/alignments.json`, `experiments/decision-chat-alignment/results/alignments.csv`, `experiments/decision-chat-alignment/results/review.md`.
- T: Passed 7 unit tests; reproducible seeded run sampled 50 of 1,640 canonical decisions and produced 0 High, 3 Medium, 10 Low, and 37 No-match alignments; JSON and CSV each contain 50 rows; tracked outputs contain no raw conversation windows or local user-profile paths.
- S: Experiment report `experiments/decision-chat-alignment/REPORT.md`

### Implementation

- Normalized Codex rollout JSONL into session/turn/message records and combined TF-IDF, phrase, identifier, temporal, agent, branch, and commit signals.
- Excluded approval replays, copied agent-history wrappers, later turns naming the final entry ID, and full tool payloads after negative-control audit exposed provenance leakage.
- Kept raw bounded windows only in a named private local output directory; repository outputs retain hashes and source coordinates.

### Validation

- `python -m unittest experiments/decision-chat-alignment/test_align_decisions.py` — passed after the final privacy/output changes.
- `python -X utf8 experiments/decision-chat-alignment/align_decisions.py --repo . --output experiments/decision-chat-alignment/results --private-output <private-temp-dir>` — passed after the final matcher changes.
- Parsed `results/alignments.json` and `results/alignments.csv` — passed: 50 rows each and identical confidence totals.

### Follow-up

- The smallest next experiment is prospective capture of 30 first-hand Codex decision spans, followed by blind evaluation of a deterministic high-recall detector. No classifier was trained because the current labels are insufficient.
- No diagram sidecar was added: the research outcome is a quantitative feasibility finding, and a diagram would add no structure beyond the pipeline already recorded in the report.

## 2026-09-24 07:49 - Run Codex-only decision alignment cohort

```yaml
entry_id: mse_nah0gcne8gx0vnv8
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/decision-alignment-study
decision_origins:
  d1: user
```

### Summary

- Added a reproducible Codex-only cohort filter to the decision-to-chat alignment experiment and ran the requested 50-decision follow-up.

### Records

#### D1 - Decision: Filter the alignment population before sampling

- D: Added an exact case-insensitive `--decision-agent` filter and applied it to the canonical decision population before seeded sampling, while preserving the original mixed-cohort outputs.
  - Scope: The retrospective decision-to-chat alignment experiment and its Codex-only follow-up cohort.
  - Disposition: Implemented.
- R: The user requested the same experiment using only Codex-tagged decisions; pre-sampling filtering keeps the cohort definition explicit and reproducible without changing matcher behavior.
- A: Overwriting the original mixed-cohort outputs was rejected because separate outputs retain the baseline and make the follow-up auditable.
- F: `experiments/decision-chat-alignment/align_decisions.py`, `experiments/decision-chat-alignment/test_align_decisions.py`, `experiments/decision-chat-alignment/README.md`, `experiments/decision-chat-alignment/REPORT.md`, `experiments/decision-chat-alignment/REPORT-CODEX-ONLY.md`, `experiments/decision-chat-alignment/results-codex-only/alignments.json`, `experiments/decision-chat-alignment/results-codex-only/alignments.csv`, `experiments/decision-chat-alignment/results-codex-only/review.md`.
- T: Eight targeted unit tests passed; the seeded run sampled 50 of 533 Codex-tagged decisions and produced 0 High, 2 Medium, 21 Low, and 27 No-match alignments; output integrity and privacy checks passed.
- S: Follow-up report `experiments/decision-chat-alignment/REPORT-CODEX-ONLY.md`

### Implementation

- Kept the original matcher, temporal bounds, confidence thresholds, privacy exclusions, sample size, and seed unchanged; only the decision population filter changed.
- Wrote the follow-up artifacts to a separate tracked directory and retained raw conversation windows only in a named private temporary directory outside the repository.

### Validation

- `python -m unittest experiments/decision-chat-alignment/test_align_decisions.py` — passed 8 tests after the implementation and generated-output changes.
- Codex-only full run — passed with 50 JSON and 50 CSV rows, exact confidence totals, all sampled decisions tagged Codex, and no raw windows or user-profile paths in tracked outputs.

### Follow-up

- The Codex-only cohort still yielded only 4% Medium-or-better alignment, so retrospective matches remain too weak for classifier labels; prospective first-hand provenance capture remains the smallest useful next experiment.
- No diagram sidecar was added: this follow-up is a quantitative cohort comparison and adds no pipeline structure beyond the existing experiment report.

## 2026-09-24 08:25 - Prompt Design Discovery into native Plan mode

```yaml
entry_id: mse_6daesmtw2k7kr81h
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/design-discovery-plan-mode-gate
decision_origins:
  d1: user
```

### Summary

- Added a native Plan-mode entry gate to Design Discovery while retaining a bounded read-only fallback on surfaces that do not expose native Plan mode.

### Records

#### D1 - Decision: Require native Plan mode before Design Discovery

- D: When Design Discovery triggers on a Codex surface with native collaboration modes, continue only if Plan mode is active; otherwise prompt the user to switch with `/plan` and stop that turn. On surfaces without native Plan mode, disclose that limitation and keep discovery read-only until approval.
  - Scope: The live and seeded Design Discovery runbooks and their regression coverage.
  - Disposition: Implemented on JNL's direct instruction.
- R: Native Plan mode provides the explicit planning context and turn metadata the user wants for decision-dense discovery, while a skill can request but cannot itself change the host application's collaboration mode.
- A: Rejected silently emulating Plan mode because that would not set native mode metadata or enforce the requested turn boundary. Rejected blocking non-Codex clients entirely because Memory Seed's reusable skills remain portable across agents.
- F: Updated `.memory-seed/skills/design_discovery.md`, `memory_seed/seed/.memory-seed/skills/design_discovery.md`, and `tests/test_session_schema.py`.
- T: Test-first regression failed before the runbook change; after the change, `python -m pytest tests/test_session_schema.py tests/test_delivery_quality.py` passed 54 tests and `git diff --check` passed.

### Validation

- passed: after both runbook copies and the regression assertion changed, `python -m pytest tests/test_session_schema.py tests/test_delivery_quality.py` completed with 54 passed.
- passed: after the final edits, `git diff --check` reported no whitespace errors and the live/seed parity tests passed.

### Follow-up

- No roadmap item changed state. No diagram is needed because this is a single mode-entry guard with no data, schema, migration, or branch topology to clarify.

## 2026-09-24 12:43 - Make the Design Discovery Plan reminder one-shot

```yaml
entry_id: mse_1ef9qqx8yv2nt9bf
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/fix/design-discovery-one-shot
decision_origins:
  d1: user
```

### Summary

- Made the Design Discovery native Plan-mode reminder one-shot per conversation while preserving the first-turn pause and the fallback for surfaces without native collaboration modes.

### Records

#### D1 - Decision: Prompt for Plan mode only once per conversation

- D: Evaluate the native Plan-mode gate once when Design Discovery first triggers in a conversation. If a reminder is needed, pause that turn; on later turns, treat the visible reminder as satisfying the gate and continue without rechecking mode metadata or challenging the user's stated mode.
  - Scope: The live and seeded Design Discovery runbooks and their regression coverage.
  - Disposition: Implemented on JNL's direct instruction.
- R: Repeated mode checks disrupted development flow and could incorrectly contradict the user when native collaboration metadata was stale or unavailable. The initial reminder still communicates the preferred planning context without turning that context check into a recurring blocker.
- A: Removing the Plan-mode reminder entirely was rejected because one initial prompt still establishes the intended discovery context. Persisting gate state outside the conversation was rejected because visible conversation history is sufficient and avoids a new state mechanism.
- F: Updated `.memory-seed/skills/design_discovery.md`, `memory_seed/seed/.memory-seed/skills/design_discovery.md`, and `tests/test_session_schema.py`.
- T: Test-first regression failed against the former repeated-check contract, then passed after both runbook copies adopted the one-shot behavior.

### Follow-up

- No trigger-registry, policy, roadmap, or persistent-state change is needed. No diagram is warranted because this is a single conversational gate with no data, migration, or branching structure to clarify.

## 2026-09-24 13:55 - Revise Task Packet orientation-lite plan after independent review

```yaml
entry_id: mse_t7hc53kavrs8hx0m
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-lite-plan-rev
decision_origins:
  d1: agent
```

### Summary

An independent reviewer (opus, read-only) returned REVISE on the Task Packet plan. I confirmed its three blocking findings against the code. JNL approved revising the plan with all ten fixes.

### Records

#### D1 - Decision: Lite suffices for ordinary appends; the full rules load through a digest-verified path outside the budget reserve
- D: Revision 2 of `docs/2_Todo/task-packet-sources-and-orientation-lite-plan.md`:
  - Lite alone covers ordinary work, including a normal session append. The full `session_logging.md` is loaded only for repair or backfill, lifecycle edges, ADR-linked writes, or a refused append.
  - A new governance-load path returns full rules only on a pinned-digest match, and does not spend the supplemental reserve.
  - Source following is a key that is absent when off, or a new spec version, so the six v1 profiles recompile byte-identical. Followed files join the corpus revision.
  - Append itself refuses future timestamps, apart from the dry-run echo and labelled backfill.
  - The governance update (ADR evolution and formally replacing the Ada decision) now comes before the packet changes.
  - T2 and T8 are sequenced, with a measurement in between. Usage logging and the old plan's disposition are deferred.
  - Scope: The plan document and its Next Steps line; no code changed.
  - Disposition: Awaiting JNL approval of revision 2.
- R: The reviewer found three blockers, all confirmed at `task_packet.py:998-1004`, `retrieval_spec.py:309-315` and `cli.py:781`:
  - requiring the full session skill before every write would only move its tokens to runtime;
  - supplemental-fetch rules refuse re-reads of baseline sources and charge other reads to the reserve, which could starve a lazy load;
  - an always-written selector key would change every v1 fingerprint.
  The reviewer also found major gaps: the corpus revision isn't a git snapshot, a recorded decision was reversed unmarked, the ordering was wrong, and the append path accepted future timestamps.
- A: Measuring total tokens including a mandatory lazy load was rejected in favour of making lite sufficient for normal appends, given the T3 mechanical enforcement.
- F: `docs/2_Todo/task-packet-sources-and-orientation-lite-plan.md`; `docs/2_Todo/0_NEXT_STEPS.md`; regenerated the docs indexes.
- T: `docs check` passed (270 files); `docs index --check` current; `git diff --check` clean.

### Follow-up

- JNL to approve revision 2.
- The reviewer couldn't verify the pinned pilot fingerprints, or which registries "both trigger registries" covers. T8 and T9 now name them explicitly.

## 2026-09-24 14:02 - Measure Task Packet baseline, clear Reflection residue, and scope orientation for workers

```yaml
entry_id: mse_wgssyj0541btgke4
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-lite-impl
decision_origins:
  d1: agent
  d2: user
  d3: user
```

### Summary

Started the approved Task Packet sources and orientation-lite plan (revision 2) with T0, T1 and T2.

### Records

#### D1 - Documentation: T0 baseline packet measurement
- D: Built a reproducible harness that compiles the pilot fixture with production control-plane files. Measured with the rules embedded, read-only packets are 9,415 serialized tokens, of which `agent-rules.md` is 5,093 (54%). Session-writing packets are 21,161, of which the two baselines are 16,569 (78%).
  - Scope: `experiments/task-packet-sources-lite/`; the frozen Seed Pod dispatches aren't in the repository, so the pilot fixture is the reproducible set.
- F: `experiments/task-packet-sources-lite/measure.py`; `experiments/task-packet-sources-lite/RESULTS.md`; `experiments/task-packet-sources-lite/t0.json`.
- T: Token counts were identical across two runs; fingerprints vary with the fixture's commit time, as documented.

#### D2 - Decision: Workers complete an orientation-lite gate; primary sessions keep the full chain
- D: Scope the mandatory orientation chain by agent role. A primary session still completes AGENTS.md then agent-rules.md before any task action. A delegated worker or subagent completes the orientation-lite gate instead: its digest-pinned lite skill (embedded in a Task Packet, or read first when no packet is given), with the full rules loaded on demand through a digest-verified path.
  - Scope: The orientation gate ADR, the Worker Context Contract, and Task Packet v2.
  - Disposition: Approved by JNL with plan revision 2; mechanical enforcement (T3) lands before packets stop embedding the full rules (T8).
- R: Measured baselines are 54-78% of every packet. Workers already receive their state from the orchestrator under the Worker Context Contract. The Ada failure is addressed by making the append path enforce clock and structure, not by embedding more text.
- A: Keeping the full chain for workers was rejected because it keeps packets 2-5x larger than their task content. Delivering lite by reference only was rejected because there would be no digest proof.

#### D3 - Decision: Replace embedded full worker baselines with orientation lite plus digest-verified on-demand rules
- D: Task Packet v2 stops embedding the full `agent-rules.md` and `session_logging.md` and embeds orientation lite. The full files are referenced by path and digest, and their digests stay in the fingerprint.
  - Scope: New `task-packet` v2 only; v1 packets and their hook validation are unchanged.
  - Disposition: Approved; implementation follows T3-T8.
- R: The Ada incident's cause was missing session-writing rules plus an append path that accepted hand edits and explicit future timestamps. T3 moves that protection into the tooling, so embedding 16.5k tokens of prose is no longer the safeguard.

### Follow-up

- T3 mechanical session-write backstop next; T4 lite skill; T5 governance-load path.
- Empty untracked `.memory-seed/reflections/{active,trust}` folders remain in the primary checkout. Git does not track empty folders, so they need a local `rmdir` there.

## 2026-09-24 14:06 - Research atomic facts and decisions for curator items

```yaml
entry_id: mse_nxz1dsscxvyxsjqx
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/atomic-facts-decisions-554116
decision_origins:
  d1: user
```

### Summary

- Surveyed atomic-unit extraction research (proposition/claim decomposition, OpenIE, argument mining, dialogue decision detection, design rationale/ADRs) and proposed a unified FACT/DECISION atomic-item schema for the Decision Curator gate.

### Records

#### D1 - Documentation: Atomic facts and decisions research report
- D: Wrote a research survey and design proposal recommending molecular decision items (one issue, one chosen option, one scope, reasons attached), a new non-authoritative FACT kind with volatility and replacement, a `rests_on` decision-to-fact field instead of a new edge kind, an items sidecar, a grounded closed-world curator extraction prompt, retrieval rollup of fact hits to decisions with no new decay, a worked FAISS-deferral example, and nine open questions for JNL.
  - Scope: Research and proposal only; no code, schema, validator, or existing record changed.
- F: `docs/4_Reference/atomic-facts-and-decisions-report.md`, regenerated `docs/4_Reference/README.md` and `docs/README.md` lane indexes.
- T: `python -m memory_seed.cli docs check` passed (271 files) after moving the report out of the requested `docs/research/` path, which the docs lifecycle check rejects as an off-lane folder; JNL chose `4_Reference`.
- S: `docs/1_Inbox/decision-curator-orchestration-proposal.md`
- S: `docs/1_Inbox/semantic-compression-benchmark-proposal.md`

### Follow-up

- Open questions in the report (fact storage location, `:fN` ref grammar, edge kinds on facts, `rests_on` field vs edge, controlled Disposition vocabulary, curator input boundary, backfill, paired retrieval eval, `adr_retrieval_entry_granularity` revision) await JNL's decisions.
- No diagram added: the report is a research survey; the item schema is given as tables and YAML.

## 2026-09-24 14:12 - Refuse future-dated session entries at append and integration

```yaml
entry_id: mse_cwwg5k5dxy95940a
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-lite-impl
decision_origins:
  d1: agent
```

### Summary

T3 of the Task Packet orientation-lite plan: the mechanical backstop that must land before packets stop embedding the full session-logging rules.

### Records

#### D1 - Decision: Future-dated headings are refused for new writes; the corpus check stays advisory
- D: The new `entry_future_timestamp_issue` refuses a heading more than 10 minutes in the future. It runs at CLI and MCP `session append` (including dry run), and in the fuse planner used by `session fuse`, `merge-branch` and MCP integrate for newly imported branch entries.
  - Scope: New writes only. Past stamps (dry-run echo, labelled backfill) stay legal, and published history keeps the advisory check.
  - Disposition: Implemented. Hand-authored entry detection is a stated limit.
- R: The Ada incident wrote a future-dated entry through a hand edit. With lazy-loaded rules, safety must come from the tooling. A hand-authored entry has no unforgeable marker, so the enforceable symptom is the future stamp.
- A: Detecting hand-authored entries was rejected because it would be forgeable or would reject legitimate backfill. Refusing all explicit timestamps was rejected because it breaks the dry-run echo and backfill.
- F: `memory_seed/core.py`; `memory_seed/mcp_server.py`; `memory_seed/cli.py`; `tests/test_session_append.py`; `tests/test_session_fuse_and_merge.py`; `experiments/task-packet-sources-lite/RESULTS.md`.
- T: New future-timestamp tests passed (5). Append, fuse/merge, MCP and core tests passed (258 + 15 subtests); MCP and CLI help tests passed (74).

## 2026-09-24 14:14 - Add subagent orientation-lite skill with drift guards

```yaml
entry_id: mse_2cd4tv4zzzvs0nm6
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-lite-impl
decision_origins:
  d1: agent
```

### Summary

T4 of the Task Packet orientation-lite plan: the one-file worker orientation that replaces the full chain for delegated workers.

### Records

#### D1 - Decision: A 1,030-token lite skill restates the worker core and routes to the full rules on demand
- D: Added `subagent_orientation.md` (live and seed copies identical). It has:
  - seven always-apply rules: verify location, stay in scope, no shared state, writer-only sessions (clock-stamped, with a dry-run echo exception), stop on the eight STOP categories, no integration, evidence over recall;
  - the return contract;
  - an on-demand table routing to `session_logging.md`, `agent-rules.md`, `risk_signaling.md`, `systematic_debugging.md` and `memory_search`.
  - Scope: The skill and its drift tests. Registry, profile and delivery wiring is T9; packet embedding is T8.
  - Disposition: Implemented.
- R: Measured at 1,030 tokens, against 5,093 for `agent-rules.md` plus 11,476 for `session_logging.md`. The drift tests tie it to its sources without editing locked control files: every STOP category in `risk_signaling.md`, the writer named in `agent-rules.md`, the statuses in `agent_collaboration.md`, and route targets that must exist in both the live and seed trees.
- A: Adding section markers to `agent-rules.md` for the drift test was rejected; the phrase and category guards achieve the same without editing a locked file.
- F: `.memory-seed/skills/subagent_orientation.md`; `memory_seed/seed/.memory-seed/skills/subagent_orientation.md`; `tests/test_subagent_orientation.py`.
- T: `tests/test_subagent_orientation.py` passed (6 tests, 26 subtests); seed and skill registry suites passed (132).

## 2026-09-24 14:34 - Accept the worker-scoped orientation gate revision

```yaml
entry_id: mse_2948qnxwpxx87w7t
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-lite-t5
decision_origins:
  d1: user
```

### Summary

JNL reviewed and approved the proposed `adr_orientation_completion_gate` revision after an explanation of its scope, rationale and residual risk.

### Records

#### D1 - Decision: Accept the orientation gate revision for delegated workers
- D: Accept `mse_wgssyj0541btgke4:d2` as the authoritative head of `adr_orientation_completion_gate`. Primary sessions keep the mandatory AGENTS.md -> agent-rules chain. Delegated workers and subagents complete the orientation-lite gate.
  - Scope: The ADR head only. It unblocks T6 and T8 of the Task Packet orientation-lite plan.
  - Disposition: Accepted by JNL on 2026-09-24.
- R: JNL approved directly after reviewing:
  - what changes: workers only;
  - what doesn't: the primary-session gate, the hook and AGENTS.md wording;
  - the residual risk: a lite-only worker missing a full-rules-only rule, mitigated by drift tests, on-demand triggers, and T3's enforced future-timestamp refusal.

## 2026-09-24 14:40 - Add digest-verified governance load outside the supplemental reserve

```yaml
entry_id: mse_r0k9nzhaxqp31hwx
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-lite-t5
decision_origins:
  d1: agent
```

### Summary

T5 of the Task Packet orientation-lite plan: the on-demand path for full rules that can't be starved by the budget.

### Records

#### D1 - Decision: Governance loads verify a pinned digest and never debit the supplemental reserve
- D: Added two functions to `memory_seed/task_packet.py`:
  - `governance_reference`, which pins path, bytes, digest and token estimate without embedding content;
  - `load_task_packet_governance`, which returns a referenced file only when its live bytes match the pin. Otherwise it refuses with `stale_governance`, `missing_governance_reference`, `invalid_governance_reference`, or, for v1 packets, `governance_embedded`.
  - Supplemental gap reads now refuse referenced governance (`governance_reference`) and tolerate packets without `worker_baseline`.
  - Scope: Python API used by the v2 compiler (T8); the v1 behaviour is unchanged.
  - Disposition: Implemented.
- R: The review found that lazy rules routed through supplemental fetch would be refused or charged to the reserve, and could fail with `supplemental_budget_exceeded`. A separate digest-verified path keeps the required rules always loadable and provably the compiled bytes.
- F: `memory_seed/task_packet.py`; `tests/test_task_packet.py`.
- T: The new governance-load test passed (v1 refusal, pinned load with zero debit, missing reference, stale digest). The Task Packet, surfaces and pilot suites passed (63 other tests, 114 subtests). The supplemental refusal of referenced governance needs a canonical v2 packet and is deferred to T8.

## 2026-09-24 14:53 - Follow decision S: sources one hop in retrieval v2

```yaml
entry_id: mse_0nx78hh8rn8t4s6f
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-lite-t5
decision_origins:
  d1: user
```

### Summary

T6 of the Task Packet orientation-lite plan: packets now reach the documents their selected decisions cite, without changing any existing spec or profile.

### Records

#### D1 - Decision: Source following is an absent-when-off selector, defaulted on by new v2 profiles
- D: Added `selectors.source_references` to Retrieval Specification v2. It is written only when on, so every prior spec and all six v1 profiles keep their bytes and fingerprints. New `v2.yaml` profiles extend each v1 profile and turn it on; they're seeded and packaged.
  - The resolver's new `source_references` stage follows each selected decision's `S:` refs one hop, as optional evidence. An anchor selects that heading's section; a missing anchor falls back to the whole file with a warning.
  - Every unfollowed ref is reported by code: excluded governance files (`index.md`, `policy.md`, `agent-rules.md`), forbidden, moved, retired (`7_Replaced`, archived), non-Markdown, missing, unreadable, ADR paths, and sources over the 4,000-token cap.
  - Constitution refs are never followed as files. Their anchors go into `source_reference_constitution_anchors`, and `project_constitution` maps them to whole clauses by heading slug or clause-ref suffix, listing unmatched anchors.
  - When on, every Markdown file cited by an `S:` line joins the corpus revision.
  - Scope: `retrieval_spec`, `retrieval`, `task_packet` projection, profiles and seed registration; the resolver and pack versions are unchanged because the new fields appear only on the new path.
  - Disposition: Implemented.
- R: The reviewer found that an always-written key would change all v1 fingerprints, and that live-tree reads of followed files had to be revision-tracked. Routing Constitution anchors to clause projection keeps the 44-46% projection saving.
- A: Bumping resolver and pack versions for all packs was rejected because the new fields are conditional. Following ADR paths was rejected; ADRs keep their pinned and path selectors.
- F: `memory_seed/retrieval_spec.py`; `memory_seed/retrieval.py`; `memory_seed/task_packet.py`; `memory_seed/core.py`; `pyproject.toml`; `.memory-seed/retrieval-profiles/*/v2.yaml` and seed copies; `tests/test_source_references.py`; `tests/test_project_lifecycle.py`.
- T: `tests/test_source_references.py` passed (9 tests, 6 subtests), covering off-is-absent, v1 identity, sections, whole files, reporting, determinism, revision sensitivity, profiles and clause projection. Packet, retrieval, profile, hook and parity suites passed (120, 1 skipped, 139 subtests). The profile and lifecycle suites passed after adding the v2 seed destinations.

## 2026-09-24 14:55 - Find that no Task Packet compiles against this repository since Constitution v2.0

```yaml
entry_id: mse_x0pbst43x6rxsn33
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-lite-t5
decision_origins:
  d1: agent
```

### Summary

T7 (the before-measurement on the real corpus) is blocked by a pre-existing compiler refusal. This is likely a major cause of zero Task Packet usage.

### Records

#### D1 - Documentation: Constitution anchor namespace no longer matches its ratified major version
- D: Since Constitution v2.0 was ratified on 2026-09-19 (a4b51292), `_constitution_clauses` refuses every packet compiled against this repository with `invalid_constitution_projection`. It requires anchor prefixes to match the ratified major, but all 30 `docs/CONSTITUTION.md` anchors are `constitution:v1#...`. 54 ADRs, and 56 code and test references, bind v1 anchors. The pilot fixture is unaffected.
  - Scope: A finding recorded in `experiments/task-packet-sources-lite/RESULTS.md`; the T7 real-corpus harness (`measure.py --repo`) is committed and ready to rerun.
- F: `experiments/task-packet-sources-lite/measure.py`; `experiments/task-packet-sources-lite/RESULTS.md`.
- T: `measure.py --repo` failed identically on the unchanged v1 profile path. Grep counts confirmed 30 v1 and 0 v2 anchors in the Constitution.

### Follow-up

- JNL decision needed: either re-namespace the anchors to v2 (touching the Constitution, 54 ADR bindings and references), or change the compiler rule so anchor identity is a stable namespace independent of the ratified version.

## 2026-09-24 15:41 - Re-namespace Constitution anchors to v2 with legacy aliases and measure T7

```yaml
entry_id: mse_tq4k9kgs72x6qxsz
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-anchor-fix
decision_origins:
  d1: user
  d2: agent
```

### Summary

Fixed the blocker that stopped every Task Packet from compiling against this repository, then completed the T7 real-corpus measurement.

### Records

#### D1 - Decision: Constitution anchors take the ratified major; older names stay legacy aliases; new ADR events write the current name
- D: Renamed all 30 `constitution-ref` anchors from `constitution:v1#slug` to `constitution:v2#slug`, recorded as Constitution correction 2.3 (evolution-class: no clause text changes).
  - `constitution_legacy_aliases` lets ADR validation resolve every older `vK#slug` to the current anchor, so the 54 historical bindings still validate.
  - `adr promote` and `adr revise` refuse a legacy name in a new event and name the current one, so bindings migrate as ADRs are updated.
  - The packet compiler maps legacy dispatch refs and ADR bindings to the current clauses.
  - The index now reports v2.3.
  - Scope: The Constitution anchor markers and version log, `memory_seed/adr.py`, `memory_seed/task_packet.py`, and `.memory-seed/index.md`.
  - Disposition: Implemented on JNL's choice of v2 names with v1 aliases, with the condition that ADR updates move to the new id.
- R: Since v2.0 (2026-09-19), the compiler had refused every packet because anchor and ratified majors disagreed. Renaming with aliases keeps history valid without rewriting 54 append-only ADR ledgers.
- A: Keeping v1 as a permanent namespace was offered, but JNL wanted ADR updates to adopt the new id. Rewriting all bindings at once was rejected as invasive for append-only ledgers.
- F: `docs/CONSTITUTION.md`; `memory_seed/adr.py`; `memory_seed/task_packet.py`; `.memory-seed/index.md`; `tests/test_source_references.py`.
- T: `adr check` OK across all ADRs. The anchor-namespace and source-following tests passed (13, 6 subtests). ADR, context-derivation, planning, packet, ESR, docs and lifecycle suites passed (529, 126 subtests).

#### D2 - Documentation: T7 real-corpus source-following measurement
- D: With the implementation profile, v1 compiles to 17,773 serialized tokens and v2 to 25,604. v2 followed 3 cited sources (6,586 tokens) and reported 5 others: 2 over the 4,000-token cap and 3 archived.
  - Scope: `experiments/task-packet-sources-lite/RESULTS.md` and `t7.json`.
  - Open findings: resolution takes about 4.8 s against the 5 s deadline, so real-repo packets can time out by chance (the harness lifts the deadline for measurement only). Ranked clause projection selected 26 of 30 clauses for a generic dispatch. The cap cuts two of five worthwhile sources.
- F: `experiments/task-packet-sources-lite/measure.py`; `experiments/task-packet-sources-lite/RESULTS.md`; `experiments/task-packet-sources-lite/t7.json`.
- T: `measure.py --repo` completed both profiles.

### Follow-up

- Fix the 5 s resolution deadline on real corpora in a separate slice.
- T8 (task-packet v2 carrying lite) is next; T9 wiring and seeding; T10 after-measurement and review.

## 2026-09-24 15:57 - Bring real-corpus retrieval resolution inside its deadline

```yaml
entry_id: mse_qn8w99fnz2nh661a
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-retrieval-deadline-fix
decision_origins:
  d1: user
```

### Summary

JNL directed that errors found are fixed before building on them. Real-corpus Retrieval Specification resolution took about 4.8 s against its 5 s deadline, so Task Packets over this repository failed by chance.

### Records

#### D1 - Decision: Cut resolution cost without weakening confinement or revision identity
- D: Three changes:
  - `extract_memory_chunks(lexical_terms=False)` skips the per-chunk identifier scan, which only search ranking reads; resolution opts out, and the opt-out is scoped per call.
  - `_walk_confined_tree` replaces per-file `resolve()` with a scandir walk that resolves only symlinks and Windows reparse points (junctions), since a plain entry under a confined directory cannot escape it.
  - `_stable_retrieval_plan` hashes the corpus once per attempt and confirms stability with a stat signature (path, size, mtime), re-hashing only if it moved. Injected revision readers keep the two-read contract.
  - Scope: `memory_seed/retrieval.py`, `memory_seed/semantic_cache.py`; revision strings are unchanged.
  - Disposition: Implemented. Resolution now takes about 1.0-1.9 s, and T7 passed 3 of 3 runs under the production deadline.
- R: Profiling showed the time went to lexical scanning (about 3 s profiled), per-file path resolution (about 1.6 s) and a double full-corpus hash. Revision identity and escape refusal are security- and correctness-bearing, so both were verified rather than assumed.
- A: Raising the deadline or letting callers set it was rejected; it would hide the cost instead of removing it, and the measurement harness had already needed a workaround. That workaround is now removed.
- F: `memory_seed/retrieval.py`; `memory_seed/semantic_cache.py`; `tests/test_retrieval_deadline.py`; `experiments/task-packet-sources-lite/measure.py`; `experiments/task-packet-sources-lite/RESULTS.md`.
- T: New deadline tests passed (4): the lexical opt-out is scoped, a mid-resolution edit is detected by the default reader, a junction escape raises `forbidden_path` (created without privilege on this machine), and repository resolution fits the default deadline. Revisions matched main's implementation for both profiles. Retrieval, semantic, search, packet, source-reference, parity and corpus-cache suites passed (252, 1 skipped, 149 subtests).

## 2026-09-24 18:30 - Cap ranked Constitution clauses at the tier target

```yaml
entry_id: mse_5phfyb29x5mtbp7g
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-ranked-clause-cap
decision_origins:
  d1: user
```

### Summary

Fixed the third T7 finding. It was surfaced for decision first, because a 2026-09-05 test pinned the opposite rule.

### Records

#### D1 - Decision: Ranked clauses stop at the tier target; governing clauses keep full overage
- D: `project_constitution` now adds ranked clauses highest score first, whole clauses only, until the tier target (2k/4k/8k) would be exceeded, and always keeps the best clause. Explicit dispatch refs, ADR bindings and decision-cited `S:` anchors stay complete over target, with the overage reported.
  - Scope: The ranked-whole-clause mode of Constitution projection.
  - Disposition: Implemented on JNL's choice. It replaces the 2026-09-05 rule (commit e05156a9, test `test_constitution_projection_keeps_all_ranked_clauses_above_target`) that kept every ranked clause regardless of target.
- R: Ranked clauses are lexical relevance guesses, not governing bindings. Uncapped, one shared word admitted a clause, which gave 26 of 30 clauses (4,424 tokens, over the 4,000 balanced target) on a generic real-corpus dispatch and undid the projection saving.
- A: Keeping all ranked clauses was rejected because projection stays loose. Requiring two or more shared terms was rejected because it narrows without bounding size.
- F: `memory_seed/task_packet.py`; `tests/test_task_packet.py`; `experiments/task-packet-sources-lite/RESULTS.md`; `experiments/task-packet-sources-lite/t7.json`.
- T: The replacement test checks the ranked cap and that explicit refs still keep full overage. Packet, surfaces, pilot, source-reference and planning suites passed (200, 120 subtests). T7 re-measured: v1 went from 17,773 to 16,408 serialized tokens, with 20 clauses and 3,972 tokens.

## 2026-09-24 18:44 - Place atomic facts and decisions work in the hosted roadmap

```yaml
entry_id: mse_hj57qkwfgensbqpj
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/atomic-items-roadmap
decision_origins:
  d1: user
```

### Summary

- Placed the atomic facts and decisions report in the existing hosted roadmap instead of creating a new tranche, and parked its schema-changing core in `8_Deferred`.

### Records

#### D1 - Decision: Carry atomic-item elements into existing tranches and defer the Fact kind
- D: Carry the report's low-cost elements into existing tranche discoveries: agreement spans and user/agent attribution into P0.2; decision atomicity and 40–120 character grounding quotes as candidate curator acceptance rules into P0.4; record-kind, decision-split and premise-extraction tasks into the P1.5 tournament, with `supports` treated as a record field. Defer the Fact record kind, the `rests_on` field and premise-change flags until the P0.4 loop and P1.5 evidence exist.
  - Scope: Hosted roadmap sequencing and documentation only; no record kind, relationship kind, schema or code changed.
  - Disposition: Implemented as documentation on JNL's approval.
- R: The hosted programme is capture-first, keeps curated records to Decision and Documentation, and adds no relationship kinds. The 2026-09-21 precedent integrated a proposal pack under P1 instead of opening a parallel programme. The deferred core changes the hosted record model, so it needs evidence and its own design discovery.
- A: Rejected a dedicated tranche for atomic items because it would fragment the canonical hosted programme. Rejected adopting the Fact kind in P0.4 because P0 proves one decision round-trip, not a new record model.
- F: `docs/2_Todo/0_NEXT_STEPS.md`, `docs/2_Todo/hosted-memory-mvp-programme.md`, `docs/8_Deferred/fact-items-and-premise-tracking-proposal.md`, regenerated `docs/8_Deferred/README.md` and `docs/README.md`.
- T: `python -m memory_seed.cli docs check` passed (272 files) after `docs index` regeneration.
- S: `docs/4_Reference/atomic-facts-and-decisions-report.md`

### Follow-up

- Each carried element is weighed, not pre-decided, in its tranche's design discovery.
- No diagram added: this change only routes existing items between roadmap documents.

## 2026-09-24 18:50 - Ship Task Packet v2 carrying orientation lite

```yaml
entry_id: mse_8sx54czd3myrzr1v
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-v2
decision_origins:
  d1: user
```

### Summary

T8 of the Task Packet orientation-lite plan, started after JNL had the identified errors fixed first.

### Records

#### D1 - Decision: Task Packet v2 is opt-in per dispatch and replaces embedded full rules with lite plus digest pins
- D: A dispatch may set `"packet_version": 2`. The key is written only for v2, so every existing dispatch and packet keeps its bytes. A v2 packet:
  - drops `worker_baseline`;
  - embeds `worker_orientation` (the complete `subagent_orientation.md` with byte count, token estimate and digest);
  - carries `governance_references` pinning `agent-rules.md`, plus `session_logging.md` whenever session writes are delegated, by path, size and digest.
  - Activation validates v2 through `_validate_worker_orientation`, requires the dispatch and packet versions to agree, and keeps v1 validation unchanged. Core's cadence reader accepts both versions.
  - Both managed `prepare-commit-msg` copies (kept identical) accept v1 and v2. Under v2 they check the lite bytes and digest, and only the shape of each governance pin, since the hook does not re-read files.
  - The hook's exact dispatch-key check also refused any dispatch carrying `planning_evidence`, so such packets could never earn trailers. It now allows the optional `planning_evidence` and `packet_version` keys.
  - Scope: `memory_seed/task_packet.py`, `memory_seed/core.py`, both hook copies.
  - Disposition: Implemented.
- R: Measured on the pilot fixture with production control files, v2 reduces read-only packets from 9,415 to 5,330 serialized tokens (−43%) and session-writing packets from 21,161 to 5,473 (−74%). The full rules stay loadable on demand and digest-verified, and a rules change still changes the packet fingerprint.
- A: Making v2 the compile default was rejected because existing dispatches must recompile byte-identical. Validating referenced file bytes in the hook was rejected because the hook does not re-read files; `load_task_packet_governance` verifies them at load time.
- F: `memory_seed/task_packet.py`; `memory_seed/core.py`; `.memory-seed/hooks/prepare-commit-msg.py`; `memory_seed/seed/.memory-seed/hooks/prepare-commit-msg.py`; `tests/test_task_packet.py`; `tests/test_hooks.py`; `experiments/task-packet-sources-lite/`.
- T: New tests:
  - v2 compile embeds lite and pins rules; a rules change alters the fingerprint; a stale pin refuses load.
  - The supplemental path refuses referenced governance (the check deferred from T5).
  - A session-writing v2 packet pins session logging and activates; a tampered pin is refused.
  - `packet_version` must be 1 or 2.
  - The hook stamps from a v2 packet and refuses one with tampered orientation.
  Packet, hook, schema, source-reference, orientation, lifecycle, cadence and provenance suites passed (239, 168 subtests).

### Follow-up

- T9: wire the lite read-first line into `agent_collaboration.md` (live and seed), and register lite as a core skill.
- T10: final measurement and independent review.

## 2026-09-24 18:53 - Wire and seed the subagent orientation-lite skill

```yaml
entry_id: mse_v3tjy463a1yjekvh
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-v2
decision_origins:
  d1: user
```

### Summary

T9 of the Task Packet orientation-lite plan: delivery wiring and core seeding.

### Records

#### D1 - Decision: Orientation lite is a core seeded skill with two delivery paths in the Worker Context Contract
- D: The Worker Context Contract in `agent_collaboration.md` (live and seed, identical) now names orientation lite as the worker gate, with two delivery paths:
  - compile Task Packets with `"packet_version": 2`, which embeds lite and pins the full rules for digest-verified on-demand loading;
  - for plain subagents or spawned sessions, start the spawn prompt with a read-lite-first line.
  - `subagent_orientation.md` is added to `CORE_SKILL_NAMES`, the seed file inventory, package data, and both trigger registries (primary sessions are told not to load it), and it is advertised in `.memory-seed/index.md`.
  - Scope: Skill registration and collaboration guidance.
  - Disposition: Implemented.
- R: Plain subagents in some clients never receive the SessionStart hook, so the spawn-prompt line is their only route to the worker rules. Seeding it as a core skill follows JNL's choice that every project gets it. The skill protects runtime safety for delegated work, which meets the core-skill bar in `skill_architecture.md`.
- F: `.memory-seed/skills/agent_collaboration.md` and seed copy; `.memory-seed/skills/index.md` and seed copy; `memory_seed/core.py`; `pyproject.toml`; `.memory-seed/index.md`; `tests/test_project_lifecycle.py`.
- T: Lifecycle, schema, orientation drift, and seed/registry suites passed (138, 29 subtests). `doctor` reports a healthy control plane. Encoding issues are back to the 5 pre-existing ones after normalizing three files this work had written with CRLF.

## 2026-09-24 19:09 - Leave the worktree before cleanup; diagnose MCP merge commit failure

```yaml
entry_id: mse_nnr032x2nsrt394s
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/worktree-exit-before-cleanup
decision_origins:
  d1: user
  d2: agent
```

### Summary

- Found why `memory_session_integrate` kept failing with "git commit failed: (no output)", and made leaving the worktree before cleanup part of the agent collaboration skill.

### Records

#### D1 - Decision: Move the session to the primary checkout before removing its worktree
- D: When an agent is done with a worktree, it lands the branch from the primary checkout, moves the session to the primary checkout, verifies the branch is merged and the worktree is clean, then deregisters and deletes the worktree. A session that cannot leave its launch worktree reports `cleanup-pending` and hands the delete to the user instead of partially deleting it.
  - Scope: Branch Finish Contract step 8 and a new "Leave The Worktree Before Cleanup" section in the live and seeded `agent_collaboration.md`.
  - Disposition: Implemented on JNL's direct instruction.
- R: Cleanup that runs while the session's own processes live in the worktree destroys that session's tooling. The earlier merge half-deleted the launch worktree: the locked MCP-server `python.exe` stayed while `pyvenv.cfg` and `.memory-seed/` were removed.
- A: Leaving cleanup to a later manual sweep was rejected because the user wants the agent to finish the lifecycle. Retrying the failed MCP merge was rejected because the failure is deterministic while the broken interpreter stays first on `PATH`.
- F: `.memory-seed/skills/agent_collaboration.md`, `memory_seed/seed/.memory-seed/skills/agent_collaboration.md`, `tests/test_session_schema.py`.
- T: The new regression test failed before the skill change and passed after it; `python -m pytest tests/test_session_schema.py tests/test_delivery_quality.py tests/test_integration_mode.py tests/test_subagent_orientation.py` passed.

#### D2 - Documentation: Root cause of the MCP merge commit failure
- D: The MCP server was started by `uv run` from the session's launch worktree, so that worktree's `.venv\Scripts` was first on its `PATH`. The first successful integrate then cleaned up that same worktree. Because the running `python.exe` was locked, the cleanup deleted everything else, including `pyvenv.cfg`. Git for Windows looks up the `prepare-commit-msg` hook shebang's `python.exe` on `PATH`, so every later commit from the MCP server ran the broken interpreter. It failed with `No pyvenv.cfg file` (exit 106), and the merge aborted. `_git_text` captures only stdout, so the error surfaced as "(no output)". The same merge succeeded through the CLI from the primary checkout.
  - Scope: Diagnosis only; no code changed.
- T: Reproduced in a throwaway repository with the same hook shim: a commit with the broken `.venv\Scripts` first on `PATH` failed with `No pyvenv.cfg file`; the same commit with the normal `PATH` succeeded.
- S: `memory_seed/core.py`

### Follow-up

- Candidate code fixes, not yet made: make `_git_text` report stderr on failure; have merge cleanup refuse to delete the worktree that hosts the caller's process or its `.venv`; and use an interpreter lookup that does not depend on `PATH` in the hook shim.
- No diagram added: the failure is a linear causal chain, stated in D2.

## 2026-09-24 19:32 - Resolve independent review of the Task Packet lite slice

```yaml
entry_id: mse_yz25a19sdq9akxmh
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: worktree-task-packet-v2
decision_origins:
  d1: agent
  d2: agent
  d3: agent
```

### Summary

T10 of the Task Packet orientation-lite plan: an independent opus reviewer returned REVISE with 12 findings on T0-T9. I fixed ten real defects, recorded one as intended behaviour, and kept one as a by-design limit. The full suite then passed.

### Records

#### D1 - Decision: Governance loads are callable, packet-verified and pinned to fixed control files
- D: `load_task_packet_governance` now:
  - validates the packet's identity, fingerprint, orientation and pin shape first;
  - maps each name to exactly one control file;
  - resolves through the active runtime as the compiler did;
  - checks both digest and byte count.
  It is exposed as the read-only MCP tool `memory_task_packet_governance_load` and as the CLI command `task-packet governance-load`, and v2 packets declare it in `execution_defaults.governance_load`. The lite skill names the tool. Supplemental reads also refuse the embedded lite skill.
  - Scope: `memory_seed/task_packet.py`, `cli.py`, `mcp_server.py`, and the lite skill (live and seed).
  - Disposition: Implemented.
- R: The review found the load unreachable by workers, which is fail-open against the lazy-rules design. It also found the loader trusted the packet's own pins and resolved paths differently from the compiler.

#### D2 - Decision: Source following gates the resolved path and follows only for surviving decisions
- D: Source following changes:
  - Exclusion, forbidden-path, retired and non-Markdown gates now apply to both the authored and the resolved path.
  - Followed sources record their citing decisions and are dropped when none survives the limits; Constitution anchors are kept only for surviving decisions.
  - A covered section merges into its covering range, and a whole-file citation absorbs its sections.
  - Retired refs name their frontmatter successor.
  - Packs must carry the anchors key exactly when source following is on.
  - The heading-slug function is shared.
  Walker and change detection:
  - The confined walk matches Python 3.11 `rglob`: it descends junctions but not symlinked directories.
  - The corpus S: scan reuses the confined file list.
  - The stability signature adds git HEAD, ctime and inode.
  - Scope: `memory_seed/retrieval.py`.
  - Disposition: Implemented.
- R: An in-root link could steer following into excluded files, a section could appear twice, and the walker diverged from the revision it replaced. The review also found a mid-resolution commit or same-size edit could slip past the signature.

#### D3 - Decision: Hook and alias edge cases align with compiler output
- D: Both hook copies refuse an explicit `packet_version: 1` in a dispatch, matching compiler normalization. Legacy Constitution aliases resolve deterministically to the newest major, and a real anchor is never treated as an alias.
  - Scope: Both `prepare-commit-msg.py` copies (identical) and `memory_seed/adr.py`.
  - Disposition: Implemented.
- R: A dispatch the compiler would never emit must not earn trailers. The alias fixes remove latent non-determinism.
- A: Finding 1 (the ranked cap changes v1 bytes) was recorded as intended: JNL explicitly approved the cap, and the plan now scopes byte identity to the new switches. Finding 12 (a self-consistent tamper passes the hook) was kept as a by-design limit, shared with v1, because the hook never re-reads files.
- F: `memory_seed/task_packet.py`; `memory_seed/retrieval.py`; `memory_seed/adr.py`; `memory_seed/cli.py`; `memory_seed/mcp_server.py`; both hook copies; the lite skill and seed copy; `docs/2_Todo/task-packet-sources-and-orientation-lite-plan.md`; tests; `experiments/task-packet-sources-lite/`.
- T: The full suite passed: 1,992 passed, 1 skipped, 242 subtests. The new tests cover:
  - governance-load refusal of repinned, foreign and unverified packets;
  - CLI/MCP parity with no writes;
  - the junction-steering refusal;
  - whole-file absorption;
  - the successor pointer;
  - same-size touch detection;
  - explicit-v1 hook refusal.
  Encoding is back to the 5 pre-existing issues. Final T10 figures: v2 read-only packets are 5,433 tokens (−42% vs T0) and session-writing packets 5,576 (−74%).

## 2026-09-24 19:42 - Stop merge cleanup from gutting the caller's own worktree

```yaml
entry_id: mse_x8vgw4n4nhc1jhhv
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/fix/merge-cleanup-self-host
decision_origins:
  d1: user
```

### Summary

- Made post-merge worktree cleanup refuse to remove the worktree that hosts the calling process, made failed git calls in the merge path report their error text, and kept broken venv interpreters off the PATH git hooks resolve through.

### Records

#### D1 - Decision: Refuse to remove the worktree hosting the caller; surface git errors
- D: `_cleanup_merged_source_worktree` returns `cleanup-pending` without removal when the caller's working directory, interpreter, or `sys.prefix` lies inside the source worktree. `_git_text` returns stdout plus stderr on failure and a named exception on timeout or OS error. On Windows, `_git_subprocess_env` drops PATH entries for a venv `Scripts` directory whose parent has no `pyvenv.cfg`.
  - Scope: Post-merge cleanup and git subprocess handling in `memory_seed/core.py`, used by `session merge-branch` and `memory_session_integrate`.
  - Disposition: Implemented on JNL's direct instruction.
- R: On 2026-09-24 an MCP server running from its launch worktree merged a branch, then cleanup deleted that worktree around the locked interpreter. Git for Windows resolves the hook shebang's `python.exe` via PATH, so every later commit ran the broken venv and failed, and `_git_text` hid the stderr behind "(no output)".
- A: Rejected making the hook shim use an absolute interpreter path: tested backslash, forward-slash and MSYS absolute shebangs, and Git for Windows resolved `python.exe` through PATH for all three. Rejected returning to a `/bin/sh` shim, which does work, because a 2026-07-13 decision moved away from it after `sh.exe` failed to start on Windows.
- F: `memory_seed/core.py`, `tests/test_session_fuse_and_merge.py`, `.memory-seed/skills/agent_collaboration.md`, `memory_seed/seed/.memory-seed/skills/agent_collaboration.md`, `.memory-seed/sessions/links/2026-09/2026-09-24.md`.
- T: Three new tests failed before the fix and passed after it. The full suite shows 1761 passed and 1 failed; the failure, `tests/test_corpus_read_path.py::test_raw_corpus_reads_match_the_allowlist`, also fails on unmodified main.

### Follow-up

- `test_raw_corpus_reads_match_the_allowlist` is already failing on main (6 raw corpus reads against an allowlist of 5 in `retrieval.py`), so CI will be red regardless of this branch.
- This refines `mse_ghpefqfxx4n9cbnw:d1` (ADR `adr_merge_branch_primitive`), but the `evolves` edge was not written: the ADR review gate currently rejects every lifecycle link into this ADR. Its first event cites `constitution:v1#write-surface-parity` and `constitution:v1#append-only`, and neither resolves after today's v2 anchor re-namespace. It is linked as related instead.
- Added a late related-link sidecar from `mse_nnr032x2nsrt394s` to the 2026-09-10 cleanup-lifecycle entry, on JNL's instruction.
- No diagram added: two guard clauses and a helper.
