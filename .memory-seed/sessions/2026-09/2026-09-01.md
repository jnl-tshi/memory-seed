---
tags:
  - session-log
  - memory-seed
session_date: 2026-09-01
---

## 2026-09-01 00:45 - Diagnose and fix S4/S6's non-logging: orientation chain gap, not reminder wording

```yaml
entry_id: mse_936gt0xp5hqjv55y
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/orientation-gate
```

### Summary

- Diagnosed the specific, evidence-based mechanism behind S4/S6's persistent non-logging (open
  since Run 9): a targeted replay with full transcripts (not the 300-char summaries `dryrun.py`
  keeps) showed both sessions break the orientation chain (SessionStart hook pointer -> AGENTS.md
  -> agent-rules.md, where the actual "you must log" rule lives) rather than reasoning their way
  past it. S4 never opens `AGENTS.md` at all (no `Read`, no `cat` - two tool calls total: a
  file-existence check, then `Write`). S6 *does* read `AGENTS.md` in full (73 lines, nothing
  truncated) but never follows its own explicit "1. Read `.memory-seed/agent-rules.md`" instruction
  - stops at the entry point and goes straight to the task files. Both share one root cause: every
  step in the chain reads as a numbered checklist, and nothing states the chain must *complete*,
  not just be opened, before the task starts. Made both links of the chain state that explicitly.

### Decisions

#### D1 - State the orientation chain as a mandatory gate, not a numbered checklist

- D: `.memory-seed/hooks/session-start-context.py`'s injected `STARTUP ORIENTATION` block (live +
  seed twin) now leads with "mandatory, complete before any task action, however small the task
  looks" and states explicitly that reading `AGENTS.md` is the routing step, not the destination.
  `AGENTS.md`'s own "Operating Mode" section (root file, identical to `memory_seed/seed/AGENTS.md`
  - kept in sync) gained a parallel sentence: these steps must all complete before acting on the
  task, and reading this file does not itself satisfy step 1's instruction to continue into
  `agent-rules.md`.
- R: The diagnostic evidence names two distinct failure shapes at two different points in the same
  chain - a full skip (S4) and a stop-at-entry-point (S6) - and both share the same underlying gap:
  nothing in the existing wording asserts these steps are prerequisites rather than best-practice
  guidance a model judging its own task as simple can silently truncate. Fixing both links with the
  same gating language addresses the shared root cause rather than patching each session's specific
  behavior.
- A: Considered duplicating the logging mandate itself earlier in the chain (directly into
  `AGENTS.md` or the SessionStart injection) - rejected: `AGENTS.md` is deliberately a thin,
  tool-agnostic router by design (per `ms-5e366ef4`'s "SessionStart hook injects content, not a
  'go read it' nudge" precedent and the file's own stated purpose), and duplicating rules that live
  in `agent-rules.md` would blur that separation for a problem that is actually about completing
  the routing, not about where the rule itself lives.
- F: `.memory-seed/hooks/session-start-context.py`, `memory_seed/seed/.memory-seed/hooks/session-start-context.py`,
  `AGENTS.md`, `memory_seed/seed/AGENTS.md`.
- T: `pytest tests/test_session_start_hook.py tests/test_session_schema.py` - 47 passed, 4 subtests
  passed. `pytest tests/test_project_lifecycle.py tests/test_hook_merge.py tests/test_mcp_merge.py
  tests/test_agent_selection.py tests/test_cli_help.py` - 118 passed, 3 subtests passed (broader
  suite touching `AGENTS.md` content/merge behavior). `diff` confirms both file pairs stay
  byte-identical.

### Follow-up

- Not yet validated against the dry-run corpus - the fixture (`claude-L3-durstr`) is running a
  stale copy of both files (`memory-system-version: 2.19` vs. this repo's current `2.20`, and the
  older, weaker `STARTUP INSTRUCTIONS` wording), discovered mid-investigation. Syncing the fixture
  needs to bring it current *and* apply this fix in one step, or a validation run would conflate
  "catching up to 2.20" with "the gating clause worked."
- S3 flipped between compliant and non-compliant across three replay attempts with an identical
  brief in this same investigation - genuine session-to-session variance exists, so this fix should
  not be expected to make S4/S6 (or any session) deterministically compliant, only to remove the
  specific failure mode the transcripts showed.

## 2026-09-01 00:54 - JNL approves adr_orientation_completion_gate directly

```yaml
entry_id: mse_4793ykbj05kxeht3
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/accept-orientation-gate
```

### Summary

- JNL directly approved `adr_orientation_completion_gate` (the orientation-chain mandatory-gate
  fix diagnosed from S4/S6's transcripts) - accepted ahead of a validation run, on JNL's own
  authority as the project's ADR review gate.

### Decisions

#### D1 - Accept adr_orientation_completion_gate on JNL's direct approval

- D: `adr_orientation_completion_gate` transitions from Proposed to Accepted.
- R: JNL reviewed the ADR directly (opened it in the IDE) and approved it explicitly, rather than
  waiting on the fixture-validation run this project's recent pattern otherwise used (Run 8/Run 9
  gated acceptance on empirical results) - JNL is the standing authority this process already
  defers to, and a direct approval doesn't need to imitate the automated-evidence gate.
- F: `.memory-seed/decisions/adr_orientation_completion_gate.md`.

## 2026-09-01 01:41 - Ship reconstructable Task Packet compiler and clean-session pilot

```yaml
entry_id: mse_b7rhhtfx01cafwke
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/reconstructable-task-packet-compiler
related_entries:
  - mse_kcvkaad5y12h365s
  - mse_hypvaewymqy4khwj
  - mse_4jbfzs490zcy9q06
  - mse_3r4rwsad2ssn5fg8
```

### Summary

- Delivered and review-corrected the reconstructable Task Packet v1 integration on `codex/feature/reconstructable-task-packet-compiler`: six immutable core Retrieval Profile versions, strict install/update/doctor handling, live/seed collaboration parity, proposal/audit/reference/index/changelog updates, and an offline fixture-backed clean-session pilot.

### Decisions

#### D1 - Accept the bounded reconstructable Task Packet contract

- D: Accept the compiler-backed Task Packet v1 contract. The frontier-authored artifact is the semantic dispatch; an immutable profile version, measured runtime binding, and pinned corpus revision reconstruct the complete packet. Every worker-visible document counts toward input, but the compiler ledger is only the caller-supplied envelope: serialized packet, fixed instructions, tool/schema descriptions, and supplemental reserve. Resolver `token_estimate` is evidence-only; input, output/reasoning, and cost ledgers remain distinct. Required retrieval clauses can never be weakened. Task Dispatch `allowed_files` and `forbidden_files` govern execution edits, not memory reads, and `forbidden_files` does not bar task-scoped evidence materialization. The compiler does not load or intersect project policy. Keep materialized sources single-copy and preserve exported packets as derived/ephemeral artifacts.
- R: Deterministic reconstruction keeps semantic dispatch compact while making the complete caller-supplied worker envelope auditable and repeatable. The genuinely fresh clean balanced worker found the four-source Evidence Pack sufficient and reference-correct without registry, dispatch, authority, provider/pricing, worktree, policy, or network expansion. Its no-refetch and no-broad-discovery fields are worker self-report evidence because independent tool-call instrumentation was unavailable.
- A: Rejected hand-authoring the complete packet, overwriting immutable or custom profiles, allowing any required-clause downgrade, treating edit-scope lists as retrieval filters, claiming policy intersection, refetching materialized sources, or inferring provider telemetry and hidden platform overhead. The recorded `caller_supplied_harness_accounting` field contains the explicit 447-token fixed instruction contract and 124-token tool schema from the compiler ledger; `hidden_platform_overhead` is separately unavailable because the runtime did not surface it. Actual provider total input/usage, latency, and cost also remain unavailable.
- F: Added the six strict `schema_version: 1` profile versions and lifecycle/package handling; made malformed managed profiles unhealthy without traceback; fixed new flat-session frontmatter creation and enforced canonical `### Decisions` plus `#### D1`; corrected the opening session-logging example in the byte-identical live/seed runbooks; updated collaboration procedures and documentation truth; added the exact pilot worker environment, recorded clean-worker assessment, and focused doctor/session/pilot tests.
- T: Focused round-1 suites passed 223 tests before the clean rerun and 121 overlapping post-recording tests. Focused round-2 suites passed 224 tests across pilot, session schema/append/MCP, docs, lifecycle/doctor, MCP surface, and live/seed integration parity; 74 overlapping post-session confirmation tests also passed. One initial direct integration-mode import used the wrong invocation; its correct discovery-mode rerun passed all six tests. Docs check covered 225 files with 16 known incomplete-not-broken warnings; docs index was current; doctor and links checks passed; historical topic-sidecar errors remained; live/seed hashes and wheel contents matched; `git diff --check` passed. The earlier full discovery rerun remains 1,237 tests with zero failures, 12 known baseline/environment errors, and one skip; it was not repeated in either fix round as instructed.

### Key files

- `memory_seed/core.py`, `memory_seed/retrieval_profiles.py`, `memory_seed/task_packet.py`, `memory_seed/retrieval_adapters.py`, `memory_seed/mcp_server.py`, `pyproject.toml`
- `.memory-seed/retrieval-profiles/*/v1.yaml` and `memory_seed/seed/.memory-seed/retrieval-profiles/*/v1.yaml`
- `.memory-seed/skills/agent_collaboration.md`, `.memory-seed/skills/memory_doctor.md`, `.memory-seed/skills/session_logging.md`, and their seed copies
- `tests/test_project_lifecycle.py`, `tests/test_session_append.py`, `tests/test_task_packet_pilot.py`, `tests/pilot_task_packet_fixture.py`, `tests/fixtures/task_packet_pilot/`
- `docs/2_Todo/declarative-retrieval-specification-proposal.md`, `docs/3_Spec/functionality-audit.md`, `docs/4_Reference/clean-session-high-signal-task-packet-pilot.md`, `.memory-seed/index.md`, `CHANGELOG.md`

### Pilot result

- Genuine fresh clean worker provenance: `/root/m2_task4_impl/m2_task4_pilot_round2_clean`, `gpt-5.6-terra`, medium reasoning, `fork_turns: none`; it received only the exact compiled packet and `worker_environment` paths.
- It returned three source-linked conclusions, cited all four materialized evidence IDs correctly, and reported no missing questions, supplemental sources/calls, unsupported assertions, repeated fetches, or broad discovery. No-refetch/no-broad-discovery remain worker self-report because tool-call instrumentation was not independently available.
- Frontier semantic-dispatch estimate: 574 tokens. Resolver/materialized evidence: 352/352. Serialized packet: 3,347. Fixed instructions: 447. Tool schema: 124. Supplemental reserve: 1,000. Total caller-supplied input ledger: 4,918. Output/reasoning reserve: 2,500. Total context envelope: 7,418. Selected tier: balanced; soft cap: 48,000; status: within target. Packet fingerprint: `sha256:699016efbbc7c5ed95d0228238d29f7ce240403567f74b3337d31c29c2a5276a`.
- Actual hidden platform/system/tool overhead and provider total input/usage, latency, and cost were unavailable because the execution surface did not report them. Pricing was not supplied; no value was invented.

### Review and validation

- Review rounds found and fixed six truth/integrity defects: doctor had treated invalid profiles as warning-only and missed effective-spec validation errors; the original pilot omitted fixed/tool accounting and overclaimed instrumentation; its first corrected structured output still mislabeled caller accounting as platform overhead; retrieval docs overstated policy/edit-scope intersection and downgrade behavior; the newly created flat session lacked canonical file frontmatter and used the legacy singular decision shape; and the session-logging runbook's opening example still showed that legacy singular shape.
- Focused fix validation: 223 round-1 tests plus 121 overlapping post-recording checks, followed by 224 round-2 tests across pilot, session schema/append/MCP, docs, lifecycle/doctor, MCP surface, and live/seed integration parity. The pilot test was rerun against the exact genuinely fresh worker output, and 74 overlapping post-session confirmation tests passed.
- Docs check passed for 225 files with 16 pre-existing incomplete-frontmatter warnings. Docs index was current. Doctor reported healthy/bootstrap complete with eight encoding warnings including ignored derived pilot artifacts. Links check passed. Topics check retained pre-existing historical decision-suffixed topic errors. Live/seed skill and profile hashes matched. `git diff --check` passed.
- Wheel validation built `memory_seed-2.20.0-py3-none-any.whl` with SHA-256 `19c951d70b24eaf1c0395c99313a16c494c22785b96e208f2c19388b25f8fbf2`; archive inspection confirmed all six profile versions use `schema_version: 1`.

### Follow-up and residual risks

- Retrieval Specification M4/M5 remain future work; this delivery does not claim registry, dispatch engine, policy intersection, authority, or advanced selector completion.
- Frozen context-derivation hash drift, the Windows shallow-clone sandbox error, Memory Trace ADR `current.reason` mismatch, historical topic-sidecar errors, five baseline encoding warnings, and docs-frontmatter warnings predate this task or are outside its changed surface.
- Branch: `codex/feature/reconstructable-task-packet-compiler`.

## 2026-09-01 03:43 - Accept final integrated Task Packet hardening

```yaml
entry_id: mse_32tknck7w21xpf2h
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/reconstructable-task-packet-compiler
```

### Summary

- Completed the final whole-branch review of the reconstructable Task Packet compiler at `286cf7b760f8a758be2f91f7fbd0d49722367dce` and accepted the integrated retrieval, write-scope, and MCP error-envelope hardening after bounded cross-surface regression.

### Decisions

#### D1 - Accept the final integrated Task Packet hardening

- D: Accept the final M2 Task Packet branch after whole-branch review found and the branch fixed seven boundary defects: runtime symlink/junction read escape; nonmonotonic multi-parent profile defaults and required-pin downgrades; missing required-pin Evidence Pack validation; eager ADR scanning for decision-only pins; noncanonical decision IDs; write scopes that could escape the intended checkout; and an MCP surface that flattened Retrieval Specification failures instead of returning the structured spec envelope.
- R: Runtime evidence reads and execution write scopes must remain inside their separately declared boundaries even under filesystem aliases; required selectors and inherited profile constraints must stay monotonic; canonical evidence identity and lazy typed resolution keep bounded retrieval deterministic; and MCP callers need the same structured error contract as direct compilation so they can distinguish and repair input defects without parsing prose.
- A: Rejected accepting the pre-review branch on focused component tests alone. No additional production change was made during final integration because the combined affected suites exposed no regression. The 10-minute full suite was not repeated; its last run remains the known baseline/environment result recorded by the prior M2 milestone.
- F: Retrieval hardening is commit `5403ab028341a736a7ce08b28da958f6cf4787d5` in `memory_seed/retrieval.py`, `memory_seed/retrieval_profiles.py`, `memory_seed/retrieval_spec.py`, and `tests/test_retrieval_profiles.py`. Write-scope hardening is commit `a9661e9a3fc4d8d2c0565251832383f8abd50637` in `memory_seed/task_packet.py` and `tests/test_task_packet.py`. Structured MCP errors are commit `286cf7b760f8a758be2f91f7fbd0d49722367dce` in `memory_seed/mcp_server.py` and `tests/test_task_packet_surfaces.py`.
- T: The combined retrieval/spec/resolver/profile/retrieval, Task Packet, Task Packet surface/MCP parity, pilot, lifecycle, session-schema, docs, index, and grounded-fixture suite passed 248 tests in 76.628s with one Windows platform skip because symlink creation was unavailable. Session append, MCP session append, and integration-mode suites then passed 48, 34, and 6 tests in 10.725s, 5.709s, and 1.418s: 336 focused tests passed in total. Two non-failing `ResourceWarning`s remain in the model2vec dependency and retrieval-log fixture. `memory-seed doctor`, `links check`, `docs check`, and `docs index --check` passed; docs retained 16 incomplete-not-broken warnings, doctor retained eight encoding warnings, and links retained historical warnings. All nine Task 4 live/seed procedure/profile pairs matched byte-for-byte and `git diff --check` passed.

### Residual risks

- The symlink-escape regression is skipped on this Windows host because unprivileged symlink creation is unavailable; the platform-independent boundary code and other retrieval cases passed, and the branch's junction/reparse hardening remains covered by review evidence.
- The earlier full discovery result remains 1,237 tests with zero failures, 12 known baseline/environment errors, and one skip; it was not rerun by instruction. Historical topic/link/ADR/diagram advisories, the stale reconstructable corpus cache, eight encoding warnings, and 16 docs-frontmatter warnings remain outside this final-fix surface.
- No release/version bump, archive, publish, push, merge, cleanup, registry/dispatch, authority, provider/pricing, worktree, or network expansion occurred.

### Branch

- `codex/feature/reconstructable-task-packet-compiler`

## 2026-09-01 04:15 - Close residual Task Packet boundary findings

```yaml
entry_id: mse_sveand5qfj328g40
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/reconstructable-task-packet-compiler
```

### Summary

- Closed the residual final-review boundary findings on `codex/feature/reconstructable-task-packet-compiler` and verified the combined retrieval, Task Packet, MCP, lifecycle, session, docs, and parity surfaces without making another production change.

### Decisions

#### D1 - Accept the residual boundary closures

- D: Accept the residual hardening after re-review found and the branch fixed non-ADR corpus symlink/junction confinement, raw chained parent-list operations, omitted-default required-pin validation, and complete Windows device-alias rejection.
- R: Every runtime corpus read must remain under the active runtime regardless of evidence kind or filesystem indirection; inherited list operations must compose through every parent without bypassing monotonic constraints; required pins must be validated against effective defaults even when the child omits the field; and Windows device aliases must be rejected as a class so edit scopes cannot escape through alternate device syntax.
- A: No production code was changed during residual integration because the combined affected regression exposed no failure. The 10-minute full suite was not repeated; its last recorded baseline/environment result remains unchanged.
- F: Retrieval boundary and inheritance fixes are commit `a740865df618c3e5569d503761745148400d26ed` in `memory_seed/retrieval.py`, `memory_seed/retrieval_profiles.py`, and `tests/test_retrieval_profiles.py`. Windows device-alias rejection is commit `49443cda1e4be4b85969dc65a3d181ec7c9e0295` in `memory_seed/task_packet.py` and `tests/test_task_packet.py`.
- T: The combined retrieval/spec/resolver/profile/v1/path, Task Packet, Task Packet surface/MCP parity, pilot, lifecycle, session-schema, docs/index, and grounded-fixture suite passed 269 tests in 87.446s with one Windows real-symlink skip. Session append, MCP session append, and integration-mode suites passed 48, 34, and 6 tests in 10.956s, 5.395s, and 1.304s, for 357 focused passes total. Two non-failing `ResourceWarning`s remained in model2vec config loading and the retrieval-log fixture. Doctor, links, docs, docs-index, all nine Task 4 live/seed hash pairs, and `git diff --check` passed.

### Residual warnings

- The real-symlink escape test remains skipped on this Windows host because unprivileged symlink creation is unavailable; junction/device and platform-independent boundary cases passed. The earlier full discovery baseline remains 1,237 tests with zero failures, 12 known fixture/environment errors, and one skip and was not rerun by instruction.
- Existing eight encoding warnings, 16 incomplete docs-frontmatter warnings, and historical topic/link/ADR/diagram/cache/worktree advisories remain outside this residual-fix surface.

## 2026-09-01 04:31 - Align implicit-required retained-pin validation

```yaml
entry_id: mse_8mdqvsxykceb92fy
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/reconstructable-task-packet-compiler
```

### Summary

- Closed the final Retrieval Specification gate defect by aligning retained implicit-required pins with the default-true semantics already used by membership and deletion validation.

### Decisions

#### D1 - Accept consistent implicit-required pin validation

- D: Accept the final correction in commit `168aa92279f4c957749ca6616ae2422c0c189c9c`, so a retained pinned selector that omits `required` is validated as required by default instead of raising a raw `KeyError`.
- R: Pack validation must apply one default-true interpretation of omitted `required` fields across retained-pin comparison, required-pin membership, and deletion validation. A schema-valid implicit default must produce the same deterministic validation result on every path and must never leak an implementation exception.
- A: Rejected leaving the retained-pin comparison on direct dictionary indexing or treating omission differently in only that path. This milestone records the already-reviewed correction and adds no further production change.
- F: `memory_seed/retrieval.py` now uses `pin.get("required", True)` for retained-pin validation, with regression coverage in `tests/test_retrieval_profiles.py`.
- T: The focused retrieval/profile gate passed 71 tests with one host symlink-fixture skip, and `git diff --check` was clean.

### Residual warnings

- The real-symlink case remains skipped on this Windows host because unprivileged symlink creation is unavailable. Existing baseline documentation, encoding, history, cache, and environment warnings remain outside this correction.

## 2026-09-01 04:45 - Integrate reconstructable Task Packet compiler into main

```yaml
entry_id: mse_k7qfevcstmfjvkwj
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/reconstructable-task-packet-compiler
```

### Summary

- Integrated `codex/feature/reconstructable-task-packet-compiler` through the sanctioned session merge/fuse path in a temporary integration worktree, then fast-forwarded `main` to merge artifact `015aae242d5a591bb2aa72f35da1c295d89e134c`.
- The temporary integration worktree avoided touching the unrelated untracked `experiments/memory-index-dryrun-corpus2/diag-s4-s8/` directory in the shared primary checkout. Both task-owned physical worktree directories were removed after landing; the feature and integration branches were retained. Nothing was pushed, released, or published.

### Validation

- The absolute final independent gate returned `Ready for local merge`: retained implicit-required evidence validates, deleting it fails structurally, and no new Critical or Important breakage remained.
- Post-merge focused validation passed 179 tests with one host-limited real-symlink skip; `memory-seed task-packet --help` and `docs index --check` passed; session links remained structurally valid with historical warnings only.
- The earlier full branch run completed 1,237 tests with zero failures, 12 classified baseline/environment errors, and one skip. Subsequent integrated focused runs reached 357 passes with one host-limited symlink skip.

### Follow-up

- A redundant post-merge `doctor` invocation stalled during baseline semantic initialization and was stopped; doctor had already passed on the exact integrated commit before landing.
- Git deregistered both task-owned worktrees, but Windows left permission-denied metadata stubs under `.git/worktrees/`; their physical directories are gone. Existing historical docs, encoding, topic, link, ADR/diagram, cache, and worktree advisories remain outside this delivery.

## 2026-09-01 07:49 - Run 10: merge-cascade discounted, reconcile-immediately fix landed for Run 11

```yaml
entry_id: mse_tp6ry3nd3yn7x3hy
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/run10-merge-cascade-fix
related_entries:
  - mse_30q7cjwe46fxr3er
  - mse_936gt0xp5hqjv55y
```

### Summary

- Ran Run 10 to validate `adr_orientation_completion_gate` against the memory-index dry-run corpus.
  Result: 16/31 = 51.6, kill condition TRIGGERED - the worst of five runs, and discounted from
  consideration rather than read as a fix regression. Diagnosed directly against the seeded
  workspace's git history: seven sessions (S2, S4, S6, S7, S8, S9, S10) logged correctly - proper
  DRAFT structure, right MCP tool, the orientation-gate fix held - but on isolated worktree
  branches that never merged into `main`, which is all `dryrun.py` ever reads. This project's real
  merge discipline correctly stops and asks a human before merging through a conflict; seeding
  sessions are non-interactive `claude -p` calls with no human ever present to answer, so the first
  stranded branch cascaded into every later session touching the same file (S4/S6/S8 stacked three
  sessions' entries on one never-merged branch; S10 hit two independently-created, conflicting
  copies of RELEASE.md and stopped before writing anything). `false_memory` stayed 8/8 (zero
  fabrications) throughout, so Run 9's Q31 fix is unaffected by any of this.

### Decisions

#### D1 - Reconcile every seeding session's branch to main immediately, before the next session starts

- D: `dryrun.py` gained `reconcile_seed_branches()`, called right after each seeding session's
  `run_claude()` returns and before the next brief runs. For every branch left ahead of `main`, it
  attempts `git merge --no-ff --no-commit`; on conflict, falls back to `memory-seed session fuse
  --branch <branch> --apply` (the same session-aware fuse mechanism the compliant S1/S3/S5 sessions
  already invoke themselves) before committing; on success it removes the worktree
  (`git worktree remove --force`, falling back to `rmtree_force` + `prune` per the known Windows
  worktree-removal permission quirk) and deletes the branch. A session that leaves a genuinely
  unresolvable conflict is logged as `LEFT UNMERGED` in `seed-log.json`'s new `merge_notes` field
  rather than silently dropped.
- R: Reconciling after *every single* session - not batched, not left to the agent - keeps at most
  one branch ever ahead of `main`, so the staleness that produced Run 10's pile-up structurally
  cannot accumulate: each merge always lands against the exact `main` its branch was cut from, which
  is precisely the case `session fuse` already handles cleanly (confirmed by S1/S3/S5's own
  successful self-merges). Confirmed separately, against Run 10's actual stranded branches, that
  even `session fuse` cannot untangle the problem after the fact - replaying the merge for
  `worktree-docs+release-checklist-030` against the live 6-session-stale workspace still fails, on
  the same `existing entries are not chronological` guard S10 hit, because that guard is measuring a
  staleness symptom, not something `session fuse` can route around. That rules out "call fuse
  smarter" as the fix and points specifically at "call it sooner."
- A: Considered instructing each seeding brief that no human is present and it must resolve
  conflicts and complete the merge itself - rejected as the sole fix: this investigation's own prior
  rounds (Run 7's rationalization loophole, Run 9's partial file-truth fix) showed instruction-only
  fixes are fragile and partial, whereas a harness-level mechanical reconciliation is deterministic
  regardless of what the agent narrates in a given turn.
- F: `experiments/memory-index-dryrun-corpus2/dryrun.py` (harness-only - no seed twin, no ADR,
  matches how `facts.json`/`QUIZ_PREAMBLE` edits are handled), `docs/2_Todo/memory-index-dry-run-plan.md`
  (new Run 10 section), `docs/2_Todo/0_NEXT_STEPS.md` (open decision #13).
- T: Syntax-checked (`ast.parse`); `_no_merged_branches`/`_worktree_path_for_branch` sanity-checked
  live against Run 10's actual stranded-branch workspace (read-only, correct output for all 5
  branches). Full `reconcile_seed_branches` exercised for real only by Run 11 - not yet run.

### Follow-up

- Run 11 still needed: this branch lands the fix and the write-up, not a clean re-validation. Until
  Run 11 completes, S4/S6's status and the orientation-gate fix's actual effect remain unmeasured -
  Run 10's cascade masked both.
- Archived Run 10's raw evidence at
  `experiments/memory-index-dryrun-corpus2/runs-v3-run10-merge-cascade-anomaly/` (gitignored,
  matching the `runs-v1-*`/`runs-v2-*` naming) rather than deleting it, in case the merge-cascade
  diagnosis needs re-checking later.

## 2026-09-01 08:57 - Run 11: reconcile-immediately design confirmed, two implementation bugs fixed

```yaml
entry_id: mse_7vr38rzwn69ykjpa
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/run11-reconcile-bugfixes
related_entries:
  - mse_tp6ry3nd3yn7x3hy
```

### Summary

- Ran Run 11 to validate `reconcile_seed_branches()` (Run 10's fix). Seed-only - never reached
  quiz, also discounted, same reason as Run 10. The core design held: S1-S6 all merged cleanly,
  including S2, S4, and S6 - the exact three sessions stuck since Run 9/10. Two narrower
  implementation bugs in the reconcile step itself (not the design) stalled S7 onward; found by
  debugging a read-only copy of the live workspace mid-run rather than guessing, fixed, and
  confirmed via a post-hoc recovery attempt against the actual stranded branches (which itself
  surfaced the second bug).

### Decisions

#### D1 - Fix self-collision: commit a session's own dirty root state before merging its worktree branch

- D: Added `_commit_dirty_root()` to `dryrun.py`, called at the top of `reconcile_seed_branches()`
  before the branch loop. A session that works directly on `main` for part of its task and a
  worktree for another part leaves its own direct edits uncommitted; `git merge <its own branch>`
  then refuses with "local changes would be overwritten" - the session colliding with itself, not a
  real conflict. Committing the dirty root first (as that session's own legitimate work) clears it.
- R: Confirmed directly: reproducing S7's exact merge against a read-only copy of the live,
  mid-run workspace showed precisely this "local changes would be overwritten" error on
  `.memory-seed/index.md` and the session-log files, matching S5/S6's already-logged content sitting
  uncommitted in root rather than a genuine content conflict.
- F: `experiments/memory-index-dryrun-corpus2/dryrun.py`.
- T: Syntax-checked; exercised live via a post-hoc recovery pass against Run 11's actual stranded
  branches (`reconcile_seed_branches(WORKSPACE, "recovery")`) - the dirty-root commit succeeded,
  surfacing the second bug (D2) as the next blocker rather than this one recurring.

#### D2 - Fix foreign-package guard false positive against the nested fixture

- D: `reconcile_seed_branches()`'s `session fuse` subprocess call now sets
  `MEMORY_SEED_ALLOW_FOREIGN_PACKAGE=1` in its environment (the guard's own documented escape
  hatch).
- R: The fixture is a subdirectory of this checkout with no `pyproject.toml` of its own, so the
  CLI's checkout-root walk-up finds this repo's `pyproject.toml` and refuses to run against a path
  that doesn't match the loaded package - a false positive for a deliberately-nested standalone
  fixture (same version, 2.20, on both sides), not an actual version mismatch. The original blank
  `''` error message in Run 11's `merge_notes` masked this - also fixed error reporting to fall back
  through stdout then stderr, so a future failure is diagnosable from the log alone.
- A: Considered giving the fixture its own `pyproject.toml` stub to satisfy the walk-up cleanly -
  rejected as more invasive than the guard's own sanctioned override, and riskier (a stub could
  itself be picked up by something else expecting a real package).
- F: `experiments/memory-index-dryrun-corpus2/dryrun.py`.
- T: Same post-hoc recovery pass - `session fuse` ran without the guard refusal, though the two
  branches it targeted (stale since S7, four sessions old by the time recovery ran) still failed on
  the `existing entries are not chronological` staleness guard, consistent with Run 10's finding
  that this specific problem needs "reconcile immediately," not "reconcile better" - which is what
  Run 12 will now actually test end-to-end.

### Follow-up

- Run 12 needed with both fixes in place from S1 onward (not patched in mid-run) to get a clean
  measurement of `adr_orientation_completion_gate`.
- Archived Run 11's partial evidence at
  `experiments/memory-index-dryrun-corpus2/runs-v4-run11-two-residual-bugs/` (gitignored).

## 2026-09-01 13:10 - Run 12: clean sweep, 93.5/CLEAR - orientation-gate fix fully confirmed, recommend submitting

```yaml
entry_id: mse_ht4mgktz806d7m10
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/run12-clean-validation
related_entries:
  - mse_7vr38rzwn69ykjpa
  - mse_936gt0xp5hqjv55y
```

### Summary

- Ran Run 12 with both `reconcile_seed_branches()` bugs from Run 11 fixed, seeded from scratch.
  All 10 sessions logged and merged cleanly - zero `LEFT UNMERGED`, zero branches left unmerged at
  the end. S9/S10 hit the account's session rate limit on the first pass and were resumed (not
  reseeded) once it cleared; both merged cleanly on retry, same as every other session. Result:
  29/31 = 93.5, kill condition CLEAR, zero fabrications - the best of all seven runs on this
  corpus, and the first clean measurement of `adr_orientation_completion_gate`: S2, S4, and S6,
  stuck across every run since Run 6, all logged and merged this time. Two residual misses (Q1,
  Youssef misattribution; Q22, a retrieval-time miss on a correctly-captured fact) are both
  unrelated to logging/capture and out of scope for this investigation.

### Decisions

#### D1 - Recommend submitting on Run 12's result

- D: This investigation's recommendation for open decision #13 changes from "Run 9's 87.1 is strong
  enough to submit on" to "submit on Run 12's 93.5" - a clean, unmasked measurement with the
  merge-cascade harness bug (Run 10/11) fixed and confirmed working end-to-end.
- R: Run 9's 87.1 carried a known, unresolved gap (S4/S6 never logged, in any run, for reasons not
  yet diagnosed at the time). Run 12 closes that gap with a diagnosed, fixed, and now-validated
  mechanism (the orientation chain completing, per `adr_orientation_completion_gate`) rather than
  leaving it as an open unknown going into a submission.
- F: `docs/2_Todo/memory-index-dry-run-plan.md` (new Run 12 section), `docs/2_Todo/0_NEXT_STEPS.md`
  (open decision #13).
- T: `python dryrun.py seed && quiz && judge && score` under
  `experiments/memory-index-dryrun-corpus2/` - full pipeline, same invocation as every prior run.
  93.5/CLEAR, 0 fabrications, verified against `runs/verdicts.json` directly (not just the blended
  score) for both Q1 and Q22's specific failure notes.

### Follow-up

- Archived Run 12's evidence at
  `experiments/memory-index-dryrun-corpus2/runs-v5-run12-clean-935/` (gitignored) - kept as the
  reference-quality run, not discounted like Runs 10/11.
- Q1 (Youssef misattribution) and Q22 (long_term_retention retrieval miss) remain open, minor, and
  out of scope at this scale - not worth further engineering effort per the standing recommendation.

## 2026-09-01 16:06 - Q1/Q22 misses: leave as-is - recall is ideal, decision management and handoff are paramount

```yaml
entry_id: mse_wczrdd7hmyg848ka
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/q1-q22-scope-decision
related_entries:
  - mse_y9sr61rxfw1rcz9k
  - mse_ht4mgktz806d7m10
```

### Summary

- Diagnosed Run 12's two residual misses (Q1, Q22) precisely: both concern the same maintainer-
  roster fact but are two unrelated failure modes. Q1 is a write-time defect - S9's Youssef
  onboarding appended his Linux-ARM-CI-ownership fact directly onto `index.md`'s `Maintainers:`
  bullet instead of giving it its own line, a near-verbatim repeat of a documented 2026-08-05
  anti-pattern (`session_logging.md`'s "One claim per bullet" rule, with a "Windows CI" example).
  Q22 is an unrelated read-time defect - the quiz session located and cited the correct kickoff
  entry (`memory_get_chunk` was called) yet still reported the roster as unrecorded, when the
  entry's own D1 names it in full. Compared S5's (Oskar) and S9's (Youssef) briefs directly:
  structurally identical shape, no differing instruction - ruling out the test/harness as the
  cause and confirming this is an agent consistency gap over an *existing* rule, not a missing one.

### Decisions

#### D1 - Leave Q1/Q22 as-is; do not add mechanical enforcement for the repeated Active State anti-pattern

- D: No control-plane change for Q1's repeat occurrence of the "role fact appended to a membership
  bullet" anti-pattern, despite it now being a second measured instance of the identical shape
  (2026-08-05 "Windows CI"; 2026-09-01 "Linux-ARM CI"). No change for Q22 either - a retrieval-time
  comprehension issue, unrelated to capture.
- R: Recall accuracy is a genuine goal, not a discarded one - but decision management and context
  handoff are this project's paramount value, and take priority when they'd trade off against
  chasing a recall miss with new enforcement. The rule that would have prevented Q1 already exists,
  worded around this exact scenario; a mechanical hook (flagging a name on two Active State
  bullets, the rule's own stated "tell") was considered and explicitly declined rather than built
  reflexively on a second occurrence. Mirrors the project's established pattern (twice earlier in
  this same investigation) of not escalating to enforcement without deliberately choosing to.
- A: Considered a hook checking `index.md` diffs for a subject appearing on two Active State
  bullets - rejected for now, not on technical grounds but as a priority call: worth revisiting if
  the same shape recurs a third time, not before.
- F: None - no files changed by this decision; it closes out the memory-index dry-run investigation
  (Runs 6-12) without further action on Q1/Q22.

### Follow-up

- If this exact anti-pattern (a role/ownership fact appended to an existing membership bullet)
  recurs a third time, the enforcement option above is the one to revisit.

## 2026-09-01 16:31 - ADR diagram-review backlog: 19 never-reviewed answered; stale bucket found structurally unclearable

```yaml
entry_id: mse_yas629z4xsf567w5
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/docs/adr-diagram-review
related_entries:
  - mse_10m885gx6bery1ew
  - mse_kqna9hegj35dwsqj
```

### Summary

- Worked the `esr`-flagged ADR diagram-review backlog (38 of 60 ADRs: 19 never reviewed, 19 stale).
  Fanned out 8 parallel subagents to read each ADR's current content and judge diagram-worthiness
  against the project's existing high bar (most ADRs are constraints/schemas/naming rules, not
  genuine processes - `not_applicable` with a specific reason is the common, correct answer).
  Verified every subagent-proposed `grounded_in` ref exists before writing anything, and unescaped
  HTML entities the subagents introduced in their Mermaid output (`&lt;`/`&gt;` would have parsed
  as garbage - `links check` validates fence balance, not Mermaid syntax, so this needed a manual
  pass). Result: **all 19 never-reviewed ADRs now answered** (`esr` confirms 60/60... for that half
  - see D2). The 19 "stale" ADRs are untouched, for a structural reason discovered mid-task (D2),
  not for lack of trying.

### Decisions

#### D1 - File each never-reviewed ADR's diagram under its OWN decision's date, not today's

- D: The 19 never-reviewed answers are filed across 12 different dated diagram-sidecar files
  (`2026-05-29` through `2026-09-01`), each matching that ADR's authoritative decision's own session
  date - not uniformly dropped into today's file.
- R: `adr_head_entry_ids`' own docstring states the mechanism precisely: the diagram-review check
  compares the sidecar file's date against the ADR's current decision's date, and mismatches are
  what trip `needs-diagram-review`. Filing all 19 under 2026-09-01 would have "fixed" 19 gaps by
  creating up to 18 new ones on the next `esr` run - trading one backlog for another rather than
  closing it. Caught before writing (advisor flagged this as the blocking issue in the original
  plan); verified after writing (`esr` reports 0 of the 19 never-reviewed entries flagged).
- A: Six ADRs have no accepted decision yet (`adr_hook_merge_framework`, `adr_inbox_promotion_workflow`,
  `adr_mcp_integration_surface`, `adr_trail_lifecycle_visualization`, `adr_windows_encoding_policy` -
  proposed/rejected, no head) or a head dated today (`adr_orientation_completion_gate`) - these six
  correctly land in `2026-09-01.md`, the rest do not.
- F: New diagram-sidecar files at `.memory-seed/sessions/diagrams/2026-05/2026-05-29.md`,
  `2026-06/2026-06-14.md`, `2026-07/2026-07-03.md`, `2026-07/2026-07-04.md`, `2026-07/2026-07-07.md`,
  `2026-07/2026-07-25.md`, `2026-07/2026-07-29.md`, `2026-08/2026-08-09.md`, `2026-08/2026-08-31.md`,
  `2026-09/2026-09-01.md`; appended to existing `2026-07/2026-07-05.md` and `2026-07/2026-07-12.md`.
- T: `links check` - zero `orphan-diagram`/`malformed-diagram`/`dangling-diagram-ref` issues on the
  new content (237 files checked, only pre-existing unrelated warnings remain). `esr`: "ADR diagram
  review: 60 of 60 reviewed" for the never-reviewed count (0 remaining, was 19).

#### D2 - The 19 "stale" ADRs cannot be cleared by appending a fresh review, and none were touched

- D: No new diagram-sidecar entries were filed for any of the 19 ADRs `esr`/`links check` flagged as
  stale (`needs-diagram-review`). This is a deliberate stop, not an oversight.
- R: Verified directly (not assumed): the "latest" review for a given `adr_id` is selected by
  MAX(file_date, heading_ts, ...) - the review with the latest date wins, unconditionally. All 19
  stale ADRs' current authoritative decisions are dated BEFORE 2026-08-07 (the date the original
  bulk-review batch was filed under). That batch was filed under the day it was *performed*, not
  the day of each decision it answered - so for any of these 19, a fresh answer backdated to match
  its decision's true date can never become "latest" (it's chronologically earlier than the
  existing 2026-08-07 entry), and the append-only diagram-sidecar family has no mechanism to mark
  an older block superseded. The flag is real and correctly detects a genuine mismatch, but the
  mismatch is unresolvable by writing more content under the current design - a filing-convention
  gap in the original batch collided with the append-only + latest-by-date review-tick mechanism.
- A: Fresh re-reads did surface 4 ADRs where the ORIGINAL 2026-08-07 judgment looks substantively
  wrong on its own terms, independent of the staleness question: `adr_edge_confidence`'s Decision
  text explicitly states the opacity-fading mechanism (tiers, thresholds, hover override) the 2026-08-07
  note said was "a separate concern" belonging elsewhere - it doesn't, it's in the same paragraph.
  `adr_trail_derived_lanes`, `adr_experiment_isolation`, and `adr_archive_before_replace` show
  similar tension between the old verdict and the current text. Two subagents (assigned
  `adr_derived_precedence`/`adr_foreign_file_routing`/`adr_forward_only_edges`/`adr_link_retraction`/
  `adr_session_layout` and `adr_subproject_scoping`/`adr_topic_authority`/`adr_trace_boundary`)
  independently caught that their assigned "stale, previously not_applicable" premise was false -
  those 7 already carry live, correctly-grounded `diagram` verdicts against the exact same decision
  ref given as "new" - and refused to fabricate a false history rather than comply, flagging it back
  instead. That refusal was correct and is itself a good sign for this project's fabrication-resistance
  work this session has otherwise been focused on. None of these findings were acted on - filing a
  correction wouldn't clear the flag either (same append-only/date problem), and a genuine reversal
  needs a place to record *why* it reverses, which the current format doesn't have a field for.
- F: No files changed by this decision.

### Follow-up

- **For JNL:** the diagram-review mechanism has a structural gap for any ADR whose decision predates
  a bulk-review batch that files under review-date instead of decision-date - once that happens, the
  flag becomes permanently unclearable by content review alone. Two independent fixes are possible
  (not chosen here, this is a tooling/process question, not a content one): (a) change "latest" to
  prefer explicit supersession over raw date, or (b) accept the 2026-08-07 batch's tick as
  permanently spent and have the check compare against the LAST REVIEW's date instead of the
  decision's date, once a first review exists. Recommend deciding this before the next bulk pass.
- The 4 candidate corrections (`adr_edge_confidence`, `adr_trail_derived_lanes`,
  `adr_experiment_isolation`, `adr_archive_before_replace`) are flagged, not filed - worth a
  decision on whether recording a "this reverses the 2026-08-07 verdict, here's why" block is
  valuable even though it won't clear anything mechanically.

## 2026-09-01 17:31 - Fix: diagram-sidecar fuse dedup ignored adr_id, causing ADR-diagram double-import on merge

```yaml
entry_id: mse_hfdk6h79ye4jssep
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/bugfix/diagram-fuse-adr-dedup
related_entries:
  - mse_9szn9geevrgdntng
  - mse_m9kmrztj8kswee35
  - mse_ath81hrh9t3wzw6y
```

### Summary

- Root-caused and fixed the bug that duplicated every ADR diagram block during `session merge-branch`
  (discovered live: the ADR diagram-review work landed on `main` with every new diagram file doubled,
  worked around by manual deduplication at the time - see the merge-branch commit history from
  earlier today). Found the actual cause in `memory_seed/core.py`'s sidecar-apply step, fixed it,
  and added a regression test that reproduces the exact failure end-to-end through
  `session_merge_branch` rather than only the planning-side `session_fuse`.

### Decisions

#### D1 - Dedup diagram-sidecar blocks by (entry_id or adr_id, timestamp), not entry_id alone

- D: `_apply_session_fuse`'s diagram-write step built its "already present" lookup as
  `{record.entry_id: record for record in existing if record.entry_id}` - entry_id only. ADR-authored
  diagram blocks carry `adr_id`, not `entry_id` (the two are mutually exclusive per block), so every
  ADR block's `entry_id` is `None`, the dict comprehension's `if record.entry_id` guard drops it
  entirely, and the subsequent `by_id.get(record.entry_id)` lookup (also `None`) can never match -
  the block is unconditionally treated as new and appended, every fuse. Fixed by keying the dedup
  check on the same `(entry:<entry_id> | adr:<adr_id>, timestamp)` composite identity the planning
  side already uses (documented at `core.py`'s block-identity comment, ~line 6800) - the write side
  had drifted from that documented identity scheme without anyone noticing, because nothing exercised
  the apply path for a brand-new ADR-diagram file end-to-end.
- R: This explains precisely why the bug only showed up on ADR diagrams and only on brand-new files:
  entry-keyed blocks were never affected (entry_id is never `None` for those, so the old lookup
  worked). For a file that already existed on `main`, this fuse logic is the sole writer (git's own
  merge doesn't touch recognized session-family paths), so the block only gets written once
  regardless of the dedup bug. For a file introduced wholly new by the branch, git's raw merge adds
  it as an ordinary new file (no session-family exclusion applies - there's nothing on `main` to
  exclude it from), and this fuse step then independently re-appends the same content on top, blind
  to the fact it's already sitting in the working tree, because its dedup check can't recognize
  `adr_id`-keyed identity at all.
- A: Considered excluding brand-new session-family files from git's raw merge the same way existing
  ones are excluded, forcing every diagram file through the fuse-reconstruction path uniformly -
  rejected as a larger, riskier change to the merge/reset-guard boundary than the actual defect
  needs; a prior change to that exact boundary (`_is_recognized_session_tree_path`/ADR-path
  recognition, `mse_9szn9geevrgdntng`) was itself reverted after causing silent data loss, so this
  fix stays narrowly scoped to the dedup key that was actually wrong.
- F: `memory_seed/core.py`, `tests/test_session_fuse_and_merge.py`.
- T: `pytest tests/test_session_fuse_and_merge.py` - 67 passed, including the new
  `test_session_merge_branch_does_not_double_import_a_new_adr_diagram_file` (reproduces the exact
  failure: a brand-new diagram file with one ADR-keyed block, merged via `session_merge_branch`,
  asserts exactly one copy in the result). `pytest tests/test_mcp_session_integrate.py
  tests/test_merge_trigger.py tests/test_session_schema.py` - 52 passed, no regressions in adjacent
  session/merge/schema behavior.

### Follow-up

- The 10 already-duplicated diagram files from this morning's ADR-diagram-review work were fixed
  by hand at the time (deduplicated directly, verified via `links check`/`esr`, committed to `main`
  separately) - this entry is the root-cause fix so it can't recur, not a second pass over those
  files.

## 2026-09-01 20:02 - Fix 2 pre-existing test failures: frozen-fixture hash drift + corpus-read allowlist drift

```yaml
entry_id: mse_a2zzv3c1hkwzxf8c
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/bugfix/pre-existing-test-failures
related_entries:
  - mse_1b01mnzx5aybpbyg
```

### Summary

- Fixed both pre-existing test failures surfaced by the full-suite run after the diagram-fuse dedup
  fix (33 failures/errors total, unrelated to that fix - confirmed via `git diff --stat`). Full
  suite now clean: 1412 passed (was 1379), 1 skipped, 157 subtests passed, zero failed, zero
  errors.

### Decisions

#### D1 - Re-pin the 4 drifted frozen-fixture hashes against a specific commit, not "whatever's on disk"

- D: Updated `experiments/context-derivation/fixture_source/real-current.json`: the 4 drifted
  `sha256` values (3 ADRs rewritten by the 2026-08-31 `format: memory-seed-adr/1` -> `/2` migration;
  one session day-file whose whole-file hash changed because later, unrelated entries were appended
  after the pinned entry) and the top-level `base_sha` field, all pinned against `HEAD`
  (`81069fd0cdb46759b0cf1ca35ddb979e4003ca03` at pin time), verified via `git show HEAD:<path>`
  rather than a plain filesystem read. Added a `maintenance_note` field recording that no
  regeneration script exists and this has now drifted-and-been-hand-fixed twice (2026-08-12,
  2026-09-01).
- R: JNL's direct correction mid-plan: pin against an identifiable git version, not just current
  working-tree bytes, so the fixture's provenance stays traceable and reproducible - anyone can
  re-derive the same hash from the same commit later. Confirmed the working tree was clean for all
  4 paths before treating filesystem-read and `git show` hashes as equivalent. Confirmed via the
  ADR text diff that the v1->v2 migration changed prose/field names only (`### Why` -> `### Reason`
  etc.), not decision membership, and the session file's pinned entry
  (`mse_axkrkd339br970kw`) is byte-identical to what was already pinned - so no entry_id or
  decision-ref changes were needed alongside the hash update.
- A: Considered building actual regeneration automation - rejected as out of scope: the experiment
  is still an unapproved preregistration (`0_NEXT_STEPS.md` open decision #9), and this is the
  second hand-fix in three weeks with no clear trigger policy yet (should it run per ADR-affecting
  merge? periodically? only on failure, as both fixes so far have been?) - a real automation
  decision needs JNL's call on that policy first, not a script bolted on to make today's fix land.
  Left the `maintenance_note` as the interim trail instead.
- F: `experiments/context-derivation/fixture_source/real-current.json`.
- T: `pytest tests/test_context_derivation_fixtures.py tests/test_context_derivation_constitution_bindings.py
  tests/test_context_derivation_strong_context_fixture_v2.py tests/test_context_derivation_strong_context_sweep_v2.py
  tests/test_context_derivation_lightweight_topk_v1.py tests/test_corpus_read_path.py -v` - 60
  passed (D1+D2 combined run). Full suite - see Summary.

#### D2 - Switch the 3rd raw corpus read in retrieval.py to load_corpus, not the allowlist count

- D: `memory_seed/retrieval.py:2129`, inside `validate_evidence_pack`'s decision-evidence branch:
  `extract_memory_chunks(root, granularity="entry")` -> `load_corpus(root, granularity="entry")`.
  `tests/test_corpus_read_path.py`'s allowlist for `retrieval.py` stays at 2, unedited.
- R: Investigated via git and this project's own memory-seed tooling before choosing a fix, not
  just patching the assertion. `git log -S"session_chunks = extract_memory_chunks" --
  memory_seed/retrieval.py` found the exact originating commit: `45763fa9` ("Version Evidence Pack
  identities as typed IDs", 2026-08-31, `Memory-Entry: mse_vpqwta8c6m8yj6nk`). Read that entry in
  full - it documents the v2 typed-ID contract and says nothing about the corpus-read path; the
  gap looks unintentional, not a reviewed bypass. `memory_search` for related work found nothing
  that revisits this call site afterward either. Traced the allowlist test itself to its founding
  decision (`mse_1b01mnzx5aybpbyg`, 2026-08-05: "One canonical corpus read, after the ranking gate
  was found reading a partial one") - its own stated rationale is that this exact failure shape
  ("someone knew the augmenters existed and simply did not think of them at that call site") had
  already happened three times before this test existed; this is occurrence four. Confirmed the new
  site doesn't share the shape of the two legitimately-allowlisted `retrieval.py` sites (both need
  partial/no augmentation specifically because link-audit is measuring missing link edges - raising
  the count here would blur that documented exemption with an unrelated, unjustified one) and
  confirmed `load_corpus` is a safe drop-in: same `list[MemoryChunk]` return shape, no
  `CorpusSnapshot` being discarded (the caller had none to pass), same augmentation-neutral fields
  (`source_path`/`start_line`/`end_line`/`text`) the calling code actually reads.
- A: Considered raising the allowlist count to 3 with a reason - rejected: nothing in the commit,
  the Memory-Entry, or the surrounding code gives a reason augmentation would be wrong here (unlike
  the two real exemptions), so raising the count would document a rationale that doesn't exist
  rather than close the actual gap.
- F: `memory_seed/retrieval.py`.
- T: Same combined run as D1 (60 passed) plus the full-suite pass in Summary.

### Follow-up

- If the frozen-fixture drift recurs a third time, the `maintenance_note` in `real-current.json`
  names the exact re-hash procedure - no automation decision is owed unless JNL wants to revisit
  that scope call.

## 2026-09-01 21:03 - Parallelize the test suite with pytest-xdist: 10:15 -> ~4 minutes

```yaml
entry_id: mse_6jbrgzmc7d50ecjs
user_initials: JNL
agent_type: claude
project_path: .
subproject_path: null
branch: claude/feature/pytest-xdist-parallelization
related_entries:
  - mse_fh4sp1twn0w662jb
  - mse_a2zzv3c1hkwzxf8c
```

### Summary

- JNL flagged the 9-10 minute full test suite runtime as the priority complaint above everything
  else in this session. Profiled before touching anything: the `integration` marker (added
  2026-08-18-ish to let a fast loop run `pytest -m "not integration"`) barely helps - the
  1284-test non-integration subset alone still takes 7:21, and the top 30 slowest individual tests
  combined are only ~4.5 of the 10 minutes. The slowness is broadly distributed across ~1400 tests
  each doing real git-subprocess/tempdir I/O, not concentrated in a few fixable outliers - so
  parallelizing beats optimizing any one test. Measured `pytest-xdist -n auto` on 16 cores: 10:15 ->
  ~4 minutes, a ~2.5x wall-clock reduction with zero test-logic changes.

### Decisions

#### D1 - Adopt pytest-xdist as the standard way to run the suite, in CI and locally

- D: Added a `dev` extra (`pytest`, `pytest-xdist`) to `pyproject.toml`. Updated
  `.github/workflows/verify.yml`'s Python job to `pip install -e ".[dev]"` and
  `pytest tests -n auto` in place of the plain `pytest`/`pytest tests` invocation. This is the
  "later tiering review" that `mse_nttx32yw5ejypz5m:d2` (2026-08-18: bounded default pytest to
  maintained suites, noted the fast marker still exceeded five minutes and needed one) called for.
- R: Measured, not assumed - `-n auto` on this 16-core machine took the full suite from 10:15 to
  228.78s-247.87s across three runs (1411-1412 passed, 1-2 skipped, 157 subtests, no new failures
  once D2 landed). GitHub-hosted `ubuntu-latest` runners have fewer cores than this dev machine, so
  CI's speedup will be smaller than the measured local number, but still directionally the same fix
  for the same root cause (many small independent I/O-bound tests, not a few large CPU-bound ones).
- A: Considered fixing/removing individual slow tests instead - rejected, since no small set of
  tests accounts for enough of the runtime to matter (the slowest 30 are under half the total).
  Considered expanding the `integration` marker to cover more tests and running only `not
  integration` day-to-day - rejected as a partial, marker-maintenance-dependent fix for a problem
  parallelization solves completely and automatically, without anyone having to correctly tag every
  slow test going forward.
- F: `pyproject.toml`, `.github/workflows/verify.yml`.
- T: `pytest tests/ -q -n auto` - 1411 passed, 2 skipped, 157 subtests passed in 228.78s (3:48),
  zero failed. Three separate full-suite `-n auto` runs across this investigation, all clean once
  D2 landed.

#### D2 - Fix the one test that broke under xdist: a subTest label that couldn't cross the worker/controller boundary

- D: `tests/test_retrieval_profiles.py::test_public_retrieval_paths_fail_closed_before_guarded_topic_read`
  used `self.subTest(call=call)` where `call` was a bare lambda from a tuple of anonymous callables.
  Paired each lambda with a descriptive string name and changed the subTest label to the string.
- R: Reproduced the exact xdist failure in isolation before touching anything:
  `execnet.gateway_base.DumpError: can't serialize <class 'function'>`. The captured output showed
  `SUBPASSED(call=<function ...<lambda>...>)` - the test itself was passing; only pytest-xdist's
  cross-process reporting (worker process serializing the subtest result back to the controller via
  execnet) choked on the unpicklable lambda. Confirmed the fix by re-running the same test under
  `-n auto`: `SUBPASSED(call='preview_retrieval_spec')` etc., 1 passed, 4 subtests passed.
- A: Considered excluding this one test file from parallel distribution instead of fixing it -
  rejected: the actual fix is smaller than the workaround (one tuple restructure, no config
  exceptions to maintain) and removes the only known xdist incompatibility in the suite rather than
  carving out a permanent special case.
- F: `tests/test_retrieval_profiles.py`.
- T: Serial run unaffected (`pytest tests/test_retrieval_profiles.py -v` - 15 passed, 1 skipped, 10
  subtests passed, identical to before). Isolated xdist run - 1 passed, 4 subtests passed. Included
  in the full-suite D1 runs above.

### Follow-up

- Two context-derivation tests were flaky across full-suite runs during this investigation - a
  DIFFERENT test failed each time (`test_materialization_rejects_equally_stale_arms_after_corpus_mutation`
  once, `test_assembles_all_fixed_arm_packets_from_frozen_shards` with "retrieval shard is
  nondeterministic for CTX-01" another time), and neither failed in this run's clean pass. Looks
  like order/state-dependent nondeterminism in the experiment's sharding or corpus-mutation logic,
  unrelated to the runtime work here - flagging for a separate investigation, not touched.
- CI's actual speedup on `ubuntu-latest` (fewer cores than this dev machine) is unverified until the
  next push - the fix is directionally sound (same root cause, same mechanism) but the exact CI
  number should be checked against the run, not assumed to match the local 2.5x.

## 2026-09-01 21:07 - Build and smoke-test the Task Packet calibration harness

```yaml
entry_id: mse_a58avhtbcy9djpa0
user_initials: JNL
agent_type: codex
project_path: .
subproject_path: null
branch: codex/feature/task-packet-calibration-harness
```

### Summary

- Built the preregistered three-arm Task Packet calibration harness and completed its first local Qwen3.5 9B plumbing smoke without treating the single-run quality numbers as calibrated evidence.

### Decisions

#### D1 - Adopt the instrumented three-arm harness and pass only the M0 plumbing gate

- D: Use the frozen `no_memory`, `memory_tools`, and `compiled_packet` arms as the calibration baseline, with independently audited Memory Seed tool calls and separate compiler and provider token ledgers; mark M0 plumbing passed while leaving Retrieval Profile values, model tiers, and budget thresholds provisional.
- R: The three arms separately measure the value of project memory, the added value of a prepared reconstructable packet, and subject-model limits. The local smoke proved that the compiled arm reconstructed deterministically, avoided supplemental retrieval, and remained within the configured context while the tools arm made real audited retrieval calls.
- A: A 16K local probe failed because Hermes 0.20.5 enforces a 65,536-token minimum in one-shot mode. Direct Windows Proactor MCP transport failed on overlapped named pipes, so the harness selects the SDK's Selector/Popen fallback. Hermes progressive tool search added meta-tool turns for a four-tool surface and was disabled. Hermes's `untrusted` classifier misread MCP SDK v2 read-only annotations, so the isolated facade uses `full` trust while structurally exposing only four pinned read tools.
- F: `experiments/task-packet-calibration/contracts.py`, `experiments/task-packet-calibration/prepare.py`, `experiments/task-packet-calibration/run.py`, `experiments/task-packet-calibration/score.py`, `experiments/task-packet-calibration/mcp_wrapper.py`, `experiments/task-packet-calibration/hermes_bridge.py`, `experiments/task-packet-calibration/probe_mcp_transport.py`, `experiments/task-packet-calibration/tasks/development.json`, `experiments/task-packet-calibration/tasks/development.gold.json`, `experiments/task-packet-calibration/README.md`, `experiments/task-packet-calibration/PREREGISTRATION.md`, `experiments/task-packet-calibration/M0_SMOKE_FINDINGS.md`, `docs/2_Todo/task-packet-calibration-harness-plan.md`, and `tests/test_task_packet_calibration_harness.py`.
- T: M0 recorded 4/4 arm-specific abstentions with no tools for `no_memory`, two independently audited retrieval calls for `memory_tools`, and zero supplemental calls or repeated materialized fetches for `compiled_packet`. The final focused suite passed 112 tests plus 91 subtests; documentation checks and index checks passed, with only known baseline frontmatter warnings. The broader pre-sync run's substantive failures all passed after updating the branch to current `main`.

### Follow-up

- Freeze the representative development population and a genuinely sealed holdout across all six Retrieval Profiles before running more local models or manually launched Claude subscription trials. No decision diagram was added because the procedure and measured arm comparison are fully represented by the preregistration and result tables; a diagram would add no structural information.
