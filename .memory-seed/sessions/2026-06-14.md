# 2026-06-14

## 2026-06-14 08:33 - Publish 2.6.0 to PyPI (executed earlier this session)

```yaml
entry_id: ms-a8d0fc47
user_initials: JN
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Executed the 2.6.0 release the user approved: SessionStart hook, Copilot support, Gemini hook fix, agent-selective install. (Work done earlier in the session; logged now per append-with-current-time rule.)

### Decision

- D: Shipped 2.6.0 as three logical commits on `main` (`0249b0a` release product + control plane; `6a7efe9` docs/todo roadmap; `31bd1e6` dogfood configs + session logs), tagged `v2.6.0`, pushed, created the GitHub Release. `demo/` left untracked per convention. User approved the `pypi` manual-approval gate; PyPI went 2.5.0 → 2.6.0.
- R: User chose "2.6.0 (no bump)" + "split into logical commits". The publish.yml `pypi` environment gate is the user's to approve (irreversible OIDC push), which they did.
- A: Single squashed release commit (rejected — user wanted logical history).
- F: CHANGELOG.md (folded the Unreleased agent-selective bullet into 2.6.0, kept an empty `## Unreleased` placeholder for the public-docs test), plus the full 2.6.0 tree.
- T: 128 tests green; `doctor` healthy before tag.

### Follow-up

- First commit attempt put literal `@` lines in the message (used PowerShell here-string syntax in the bash tool); caught and fixed via `git reset --mixed` + clean `-F` message files before any push. A later `--amend` had also folded staged docs into the release commit — same reset resolved it. Pushed history is correct.

## 2026-06-14 08:33 - 2.7.0: orphan/dead-artifact ESR sweep + doctor backstop

```yaml
entry_id: ms-54f97b5d
user_initials: JN
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Specced (plan mode + web research + advisor), then implemented a dead-code/artifact review for the ESR routine to catch orphan features and files as we develop.

### Decision

- D: Added a diff-scoped **orphan & artifact sweep** as a new "End Of Turn" step in `agent-rules.md` (+ seed twin, mirrored in repo-local `.claude/commands/esr.md`): confirm additions are wired in, resolve references dangling from deletions/renames, flag scratch debris; optional declared dead-code tool (vulture/ruff, knip, ArchUnit, cppcheck) but never installs one. Backstopped by a deterministic `doctor` warning — any `.memory-seed/skills/*.md` not registered in `skills/index.md` is an orphan skill (matched on the `skill: <name>` registry token to avoid substring collisions).
- R: Two failure modes need different mechanisms (per research): orphan files/features an agent catches by reasoning over its own diff; whole-codebase dead code is a language-specific tool job, so it's optional/periodic, not mandated. The seed ships to arbitrary-language projects, so the routine must be language-agnostic and tool-free. A session-log dangling-reference doctor check was deliberately rejected (logs legitimately cite renamed/deleted/example paths → false positives that erode warnings-channel trust).
- A: Mandating vulture in the routine (rejected — not portable); doctor scanning host-project code (rejected — needs language detection + deps, conflicts with zero-dependency design).
- F: `agent-rules.md` (+ seed twin), `.claude/commands/esr.md`, `memory_seed/core.py` (`doctor()`), `memory_doctor.md` (+ seed twin), `index.md`, `CHANGELOG.md`, `README.md`, `NEXT_STEPS.md`, `tests/test_memory_seed.py`; version bump 2.6 → 2.7 / 2.6.0 → 2.7.0 across control-plane `.md` (incl. repo-root files — the version-bump trap), `core.py` VERSION, `pyproject.toml`, test pin.
- T: 129 tests green. `doctor` manual negative confirmed: clean → warns on a stray `skills/*.md` → clears once registered. Dogfooded the sweep on this diff (step renumber 7→8/8→9 left no dangling cross-refs; bulk `sed` frontmatter bump produced single-line diffs, no line-ending churn).

### Follow-up

- Implementation-complete, NOT released. 2.7.0 publish is deferred pending the user's go-ahead (same flow as 2.6.0).
- The `docs/cv-memory-seed.md` referenced by the 2026-06-13 16:49 entry does not exist on disk (a phantom reference — a different failure mode the new sweep intentionally does not claim to catch).

## 2026-06-14 09:52 - 2.7.0 seed promotions + full functionality audit

```yaml
entry_id: ms-a939b6b4
user_initials: JN
agent_type: claude
agent_name: developer
project_path: .
subproject_path: null
```

### Summary

- Verified (files + memory) that the three Tier-1 seed-promotion todos were unstarted, then ported them; then authored a full functionality audit with mermaid diagrams.

### Decision

- D: Bundled the three baseline seed promotions into 2.7.0 — `document_ingestion.md` and `office_document_editing.md` skill runbooks (live + byte-identical seed twins, trigger-registry entries, `SEED_FILES`, test list, README, index Lazy Skills) and a new **Working Principles** section in `agent-rules.md` (+ seed twin): POC-gate, verification-split, share-aware reads.
- R: User asked which todo items were worth bundling before publishing 2.7; confirmed via file checks + `memory_search` that all three were "specified but not started" (per ms-757053d4), and the genericized bodies already existed in `docs/todo/baseline-seed-promotions.md` (port, not author). Low-risk, additive.
- A: Defer to 2.8.0 (rejected — user chose to bundle); ship 2.7.0 as-is (rejected — same).
- D2: Authored `docs/functionality-audit.md` — every current feature (CLI, agent-selective install, runtime, routing, skills, personas, hooks, MCP retrieval + ranking, session model, versioning, doctor, orphan sweep, release) plus 7 mermaid diagrams (system map, init/update, MCP pipeline, session lifecycle, agent wiring, multi-user layout, roadmap) and an upcoming-features section (2.8.0 ESR generalization; 3.0 multi-user memory + Memory Explorer).
- R2: User requested a full audit with data-flow diagrams covering current + upcoming features. Grounded it in the actual code (surveyed cli/core/mcp_server/semantic_cache) rather than memory.
- F: `agent-rules.md` (+seed), `skills/document_ingestion.md` (+seed), `skills/office_document_editing.md` (+seed), `skills/index.md` (+seed), `core.py` (SEED_FILES), `tests/test_memory_seed.py` (list), `README.md`, `index.md`, `CHANGELOG.md`, `NEXT_STEPS.md`, `docs/functionality-audit.md` (new).
- T: 129 tests green; `doctor` healthy (new skills registered → no orphan-skill warning); mermaid fences balanced (7); referenced roadmap docs exist.

### Follow-up

- 2.7.0 remains uncommitted / unpublished — now includes orphan sweep + doctor backstop + 3 seed promotions. Awaiting user's release go-ahead.
- `docs/functionality-audit.md` is unreferenced from README/NEXT_STEPS (a standalone deliverable); link it if it should be discoverable.
