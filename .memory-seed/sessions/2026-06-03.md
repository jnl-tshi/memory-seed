---
tags:
  - session-log
  - memory-seed
session_date: 2026-06-03
default_user_initials: JN
---

# Session Log - 2026-06-03

## 2026-06-03 22:46 - Pin repo-root control-plane versions in test suite + refresh stale index Active State

```yaml
entry_id: ms-8308e577
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Addressed the recurring release trap (seen in 2.2.3 and 2.3.0): the frontmatter version-bump `sed` is scoped to `memory_seed/seed/` and `.memory-seed/`, so it keeps skipping this self-hosting repo's own root routing files. `doctor()` caught it at runtime each time, but nothing failed the test suite.
- Refreshed stale `.memory-seed/index.md` Active State body: control-plane `2.0` → `2.3`, package `2.2.0` → `2.3.0`.

### Decision

- D: Add `test_repo_root_control_plane_files_match_version`, which iterates `SEED_FILES` and asserts each live `.md` copy at the repo root reports `memory-system-version == get_version()`.
- R: The existing `test_control_plane_files_report_current_version` only checks `seed_file.source` (the templates), never this repo's own installed root files — exactly the gap the bump sed exploits. Pinning the live files to `get_version()` (not a hardcoded string) makes the test auto-track future bumps and fail CI when a root file is left behind.
- A: A standalone script or pre-commit check — rejected; the suite already encodes version invariants, so this belongs beside them with zero new infrastructure.
- F: `tests/test_memory_seed.py` (new test), `.memory-seed/index.md` (Active State body).
- T: 89 tests OK (was 88). `doctor` → healthy, bootstrap complete. Negative check: temporarily downgrading `CLAUDE.md` to 2.2 makes the new test fail as expected, then restored.

### Follow-up

- `index.md`/`policy.md` frontmatter still reads `memory-system-version: 2.0`. These are bootstrap-generated and not version-checked by `doctor` (only existence is), so left unchanged — not a health issue, but noted as cosmetic drift.
- Tracked-file working tree still shows EOL-only churn on the routing/control-plane files (empty `git diff`); no content change. Not committed.


## 2026-06-03 22:54 - Fix Claude Code MCP registration: write .mcp.json, strip dead settings.json block

```yaml
entry_id: ms-6a09aea8
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Acted on `memory-seed-claude-code-mcp-issue.md`: the 2.2.0-2.3.0 MCP auto-registration wrote the server into `.claude/settings.json > mcpServers`, which Claude Code never reads, so its `memory_search`/`memory_get_chunk` tools were never exposed and agents fell back to reading session files by hand. The feature never worked for Claude Code despite shipping across three releases; prior validation only confirmed the file was written, not consumed.
- Repointed Claude MCP registration to a project-root `.mcp.json` (the location Claude Code actually reads) and added a migration that strips the dead `mcpServers` block from `.claude/settings.json`.
- Applied the migration to this repo itself: created `.mcp.json`, removed the dead settings.json block (hooks preserved). Takes effect after a Claude Code restart + project-server trust prompt.

### Decisions

#### D1 - Claude MCP server belongs in project-root .mcp.json, not settings.json

- D: `_merge_claude_mcp` now writes `.mcp.json` at the project root (no `type` field); `.claude/settings.json` keeps hooks/permissions only.
- R: Claude Code discovers project-scope MCP servers from `.mcp.json`, `~/.claude.json`, or `claude mcp add` - the `mcpServers` key in `settings.json` is the Cursor/generic format and is silently ignored. The bare {command,args} form (no type) was verified working in the issue report.
- A: `claude mcp add` shell-out at init - rejected; fragile, needs the CLI on PATH and a live config, vs. a declarative checked-in file.
- F: `memory_seed/core.py` (`_merge_claude_mcp`).

#### D2 - Strip the legacy settings.json block on init/update, ours-only

- D: Added `_strip_claude_settings_mcp`; removes `mcpServers.memory-seed` from `.claude/settings.json` only when the entry is ours (command in `_OWN_MCP_COMMANDS` or args contain `memory-seed-mcp`), prunes an emptied `mcpServers`, and leaves a foreign server squatting the key untouched.
- R: Leaving the dead block invites confusion (looks configured but does nothing). Ours-only deletion protects user customisation. Wired into both `init_project` and `update_project` hook_merges so existing projects self-heal on `update`.
- F: `memory_seed/core.py` (`_strip_claude_settings_mcp`, both hook_merges tuples).

### Implementation

- `memory_seed/core.py`: rewrote `_merge_claude_mcp` (target `.mcp.json`, drop `type`), added `_strip_claude_settings_mcp`, wired `(_merge_claude_mcp, '.mcp.json')` + `(_strip_claude_settings_mcp, '.claude/settings.json')` into init and update.
- `tests/test_memory_seed.py`: updated `test_init_installs_mcp_for_claude`, `test_mcp_merge_updates_stale_args`, `test_mcp_merge_preserves_unrelated_mcp_server` to `.mcp.json`; added `test_strip_removes_legacy_claude_settings_mcp` and `test_strip_preserves_foreign_settings_mcp`.
- `README.md`: corrected MCP location to `.mcp.json` for Claude Code + added a callout about the 2.2.0-2.3.0 misregistration and the update-time migration.
- Applied `.mcp.json` + strip to this repo.

### Validation

- `python -m unittest discover -s tests` -> 91 tests OK (was 89; +2 strip tests).
- End-to-end: fresh `init` writes `.mcp.json` (no settings.json mcpServers, hooks intact); simulated legacy 2.3.0 project `update` creates `.mcp.json`, strips the dead block, preserves hooks.
- `memory-seed doctor` -> healthy, bootstrap complete.

### Follow-up

- Not released. Needs package version bump (control-plane unchanged - no seed `.md` edits) + CHANGELOG + commit + release when you are ready. This is a meaningful correctness fix worth a prompt patch release.
- Optional, deferred: have `doctor` warn when a Claude server exists only in `settings.json`/`.cursor/mcp.json` with no `.mcp.json`.
- User action: restart Claude Code and approve the project MCP server; verify with `claude mcp list` that `memory-seed` appears.


## 2026-06-03 23:23 - Document MCP search performance characteristics in README

```yaml
entry_id: ms-afd9de21
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Measured MCP search latency in-process (`rank_session_memory` over 81 chunks): ~30 ms/query, of which ~22 ms is re-reading and re-parsing every `sessions/*.md` (no persistent chunk/vector cache); embed+cosine+rank adds a few ms. Cold model load was negligible (~37 ms total) since Model2Vec `potion-base-8M` weights were already in the local HF cache.
- Established the felt "slowness" earlier was the `/btw` subagent reading source (~33 s), and that real-world MCP latency is dominated by one-time `uvx` server spawn, not per-query work.

### Decision

- D: Added a "Performance characteristics" subsection to the README MCP Memory Search section documenting the grep-vs-search tradeoff, ~30 ms warm per-query latency, the I/O-bound parse cost, cold-start (HF download + uvx spawn) behavior, and the future mtime-keyed cache option.
- R: User asked to record the latency findings so they are recognised. README is the public surface for MCP behavior; framing numbers as measured-on-this-repo avoids reading them as universal guarantees.
- F: `README.md` (new subsection after the ranking-engine paragraph).
- T: Documentation only; numbers sourced from in-process `python -c` timing run this session. No code or test change.

### Follow-up

- Deferred (no urgency at current scale): cache parsed chunks + embeddings keyed by file mtime to remove the ~22 ms/query parse cost as logs grow.


## 2026-06-03 23:43 - Add first-class Codex MCP auto-registration (.codex/config.toml)

```yaml
entry_id: ms-2cd452e4
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Closed the Codex gap in MCP auto-registration: Claude/Cursor/Gemini got their `memory-seed-mcp` server wired up on init/update, but Codex got only `.codex/hooks.json` hooks and no MCP, so its retrieval hook nagged the agent to use `memory_search` while the tool was never registered.
- Verified from OpenAI Codex docs that Codex reads project-scoped MCP from `.codex/config.toml` under `[mcp_servers.<name>]` (trusted projects only) - the TOML parallel to the other three vendors.

### Decisions

#### D1 - Codex MCP belongs in project .codex/config.toml, written via zero-dep text upsert

- D: Added `_merge_codex_mcp` writing `[mcp_servers.memory-seed]` to `.codex/config.toml`; wired into both init/update `hook_merges` tuples. Detection uses stdlib `tomllib` (read-only); writes are line-based (append our block when absent, replace just our table's lines when stale) so existing content and comments survive.
- R: Codex's project MCP config is TOML, not JSON, so the existing JSON mergers do not apply. Text-upsert preserves user comments/formatting and adds no dependency, matching the project's minimal-dep, local-first ethos.
- A: Add `tomli-w` and round-trip via dict - rejected; loses comments and adds a dependency. `codex mcp add` shell-out - rejected; fragile, needs CLI on PATH (same reasoning as the Claude `.mcp.json` fix).
- F: `memory_seed/core.py` (`_merge_codex_mcp`, `_render_codex_mcp_block`, `_CODEX_MCP_HEADER_RE`, `tomllib` import, both tuples).

#### D2 - Surface the trust requirement (Codex's silent-failure trap)

- D: Project `.codex/config.toml` loads only for *trusted* directories, so the trust step is surfaced in three places: a README caveat next to the Claude one, a one-line note appended in the `--codex` branch of `memory-retrieval-check.py` (live + seed copy), and a non-fatal `doctor` warning (new `DoctorResult.warnings`) when `.codex/hooks.json` exists but the MCP entry is missing.
- R: This is the exact class of bug just fixed for Claude (config written where the agent never reads/loads it). Trust is unavoidable and invisible; documenting + warning prevents a repeat silent failure.
- F: `README.md`, `.memory-seed/hooks/memory-retrieval-check.py` + seed copy, `memory_seed/core.py` (`doctor`, `_codex_mcp_registered`), `memory_seed/cli.py` (print warnings before the ok early-return).

### Validation

- `python -m unittest discover -s tests` -> 96 OK (was 91; +5: codex init, idempotency extension, stale-args, preserve-existing-config, foreign-key, doctor warning).
- End-to-end: init writes correct table; idempotent; round-trip preserved a comment + `model` + foreign `[mcp_servers.other]` with ours appended; doctor warning fires then clears after update with `control_plane_ok` staying True; `--codex` hook emits valid JSON with the trust note.

### Follow-up

- Not released. `core.py` behavior change with no seed `.md` content change, so control-plane stays `2.3`; needs a package version bump + CHANGELOG + commit/release, best bundled with the still-unreleased `.mcp.json` Claude fix (`ms-6a09aea8`).
- Known limitation (documented in docstring): in-place update of a stale entry relies on a standard `[mcp_servers.memory-seed]` header line; exotic dotted/inline forms no-op rather than risk corruption.
- Optional: the Claude `settings.json`-only doctor warning (other half of the deferred doctor idea) was left out of scope.


## 2026-06-03 23:55 - De-silence the Codex stale-MCP limitation via doctor status classification

```yaml
entry_id: ms-6eeb512f
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Follow-up to ms-2cd452e4. The documented `_merge_codex_mcp` limitation (stale ours-entry written in a non-standard TOML form -> safe no-op) was *silent*: `_codex_mcp_registered` returned True for any form, so `doctor` never warned, potentially leaving a user pointed at a stale command (e.g. pre-uvx `memory-seed-mcp` not on PATH) with no signal.

### Decision

- D: De-silence + document, keeping the safe no-op and zero-dep design. Replaced `_codex_mcp_registered` (bool) with `_codex_mcp_status` returning `absent | current | foreign | stale-fixable | stale-manual`; `doctor` now emits a distinct non-fatal warning per actionable state. Did NOT expand the update path to migrate exotic forms.
- R: The real hazard was silence, not the non-migration. A status classifier removes the silence cheaply; actually migrating dotted/inline forms needs either a TOML writer that loses comments or fragile multi-form text surgery (both previously rejected).
- A: Fully handle exotic forms - rejected (comment loss / fragility). Documentation-only - rejected (leaves the silent stale case).
- F: `memory_seed/core.py` (`_codex_expected`, `_codex_standard_header_index` shared by merge + status, `_codex_mcp_status`, `doctor` warning branches, expanded `_merge_codex_mcp` docstring); `README.md` (Codex callout sentence).

### Implementation

- Factored the header-line scan into `_codex_standard_header_index` so `_merge_codex_mcp` and `_codex_mcp_status` agree exactly on what "auto-fixable" means (a stale entry is `stale-fixable` iff the merge could actually rewrite it).
- `doctor`: absent+hooks -> register/trust warning (unchanged); `stale-fixable` -> "run update"; `stale-manual` -> "edit by hand to command=uvx, args=[...]"; `current`/`foreign` -> silent. All non-fatal.

### Validation

- `python -m unittest discover -s tests` -> 99 OK (was 96; +3: stale-manual, stale-fixable, current/foreign-quiet).
- E2E: printed `doctor().warnings` for dotted-key, header-stale, inline-subtable, and foreign configs - dotted + inline-subtable both classify `stale-manual` (de-silenced), header-stale is `stale-fixable`, foreign stays quiet.

### Follow-up

- Still unreleased; folds into the same pending package bump + CHANGELOG as ms-6a09aea8 / ms-2cd452e4.
