---
tags:
  - session-log
  - memory-seed
session_date: 2026-05-27
default_user_initials: JN
---

# Session Log - 2026-05-27

## 2026-05-27 00:30 - HyperFrames demo sub-project created

```yaml
entry_id: ms-demo-root-summary
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: demo
```

### Summary

- Created `demo/` sub-project: a HyperFrames (HeyGen) HTML-to-MP4 composition for the Memory Seed product demo.
- Registered `demo/` as a nested sub-project runtime in root `index.md` Topology.
- Added `demo/node_modules/` and `demo/*.mp4` to root `.gitignore`.

### Decisions

#### D1 - Use HyperFrames for the product demo

- D: Build the 30-second demo video using HeyGen's HyperFrames framework (HTML/CSS/JS → MP4 via headless Chrome + FFmpeg).
- R: HyperFrames is open-source, works with Claude Code via slash commands, and lets the composition be versioned as code.
- A: Screen recording or third-party video tools — rejected because they're not code-driven and hard to update.
- F: `demo/index.html`, `demo/.memory-seed/`.
- T: `npm run check` in `demo/` — 0 errors, 2 acceptable warnings.

#### D2 - Sub-project runtime created manually, not via memory-seed init

- D: Create `demo/.memory-seed/` by hand rather than running `memory-seed init` in the demo folder.
- R: HyperFrames generates its own AGENTS.md and CLAUDE.md with a required skill routing table. Running memory-seed init would overwrite them and lose that context. Per agent-rules, sub-projects opened non-independently don't need their own routing files.
- A: `memory-seed init --force` then restore HyperFrames CLAUDE.md — more steps, same result.
- F: `demo/.memory-seed/index.md`, `demo/.memory-seed/policy.md`, reusable control files copied from root.
- T: Runtime structure verified by file listing.

### Follow-up

- Preview composition: `cd demo && npm run dev` (long-running, use run_in_background)
- Render to MP4: `cd demo && npm run render`
- Consider splitting scenes into sub-compositions if `index.html` grows beyond manageable size.

## 2026-05-27 01:30 - Session log Stop hook added to Memory Seed seed

```yaml
entry_id: ms-9f3a2b1c
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Added session log enforcement to the Memory Seed seed so every `memory-seed init` project gets it automatically.
- Created `memory_seed/seed/.memory-seed/hooks/session-log-check.py` — cross-platform Python script that checks if any `.memory-seed/sessions/*.md` was modified in the last 15 minutes; if not, outputs a JSON `additionalContext` reminder to Claude.
- Created `memory_seed/seed/.claude/settings.json` — project-scoped Claude Code `Stop` hook that runs the script after every turn.
- Strengthened "End Of Turn" section in `memory_seed/seed/.memory-seed/agent-rules.md` — now explicitly states that all agents (Claude, Codex, Gemini) must write the session log entry before the current turn ends, not batched or deferred.
- Updated `memory_seed/core.py` `SEED_FILES` and `pyproject.toml` package-data to include both new files.

### Decisions

#### D1 - Project-scoped hook, not user-global

- D: Install the Stop hook as `.claude/settings.json` (committed project file) rather than modifying the user's `~/.claude/settings.json`.
- R: Project-scoped means every contributor using Claude Code on any memory-seed project gets the enforcement automatically. User-global only fixes one person's device.
- A: User-global hook in `~/.claude/settings.json` — rejected because it doesn't ship with the product.
- F: `memory_seed/seed/.claude/settings.json`.

#### D2 - Python for cross-platform compatibility

- D: Write the hook script in Python 3 rather than bash or PowerShell.
- R: Memory Seed already requires Python 3.11+, so it's available on every platform. Avoids platform-specific shell gymnastics.
- A: bash (Unix-only) or `powershell -NoProfile -Command "..."` (Windows-specific) — both rejected for portability.
- F: `memory_seed/seed/.memory-seed/hooks/session-log-check.py`.
- T: All three verification cases passed: no sessions dir → silent exit; stale sessions → reminder JSON printed; fresh session file → silent exit.

### Validation

- `python3 session-log-check.py` from repo root (stale sessions) → reminder JSON printed.
- `touch .memory-seed/sessions/2026-05-27.md && python3 session-log-check.py` → silent exit.
- `python3 -c "import json; ..."` → settings.json command field reads `'python3 .memory-seed/hooks/session-log-check.py'`.

## 2026-05-27 01:45 - Claude hook moved from SEED_FILES to merge logic

```yaml
entry_id: ms-7c4e1f9a
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Removed `.claude/settings.json` from `SEED_FILES` and `pyproject.toml` package-data.
- Added `_merge_claude_hook()` to `memory_seed/core.py` — reads existing `.claude/settings.json` (if any), adds the Stop hook without touching other content, then writes back. Idempotent.
- Called from both `init_project()` and `update_project()`.
- Applied the hook to this project's root `.claude/settings.json` and `.memory-seed/hooks/session-log-check.py`.

### Decisions

#### D1 - Merge, don't clobber

- D: Handle `.claude/settings.json` as a JSON merge rather than a seed file copy.
- R: Most projects already have a `.claude/settings.json` with permissions, model, etc. Adding it to SEED_FILES would cause `init` to fail on those projects (FileExistsError) or silently wipe their config with `--force`.
- A: Keep in SEED_FILES and document "merge manually" — rejected because it makes the hook opt-in work and defeats the purpose.
- F: `memory_seed/core.py` (`_merge_claude_hook`, calls in `init_project` and `update_project`), `pyproject.toml`.
- T: 3 smoke tests pass: fresh init, idempotent re-merge, merge preserves existing content.


## 2026-05-27 02:00 - Codex Stop hook added alongside Claude hook

```yaml
entry_id: ms-2d8b4c7e
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Added Codex CLI Stop hook support alongside the existing Claude Code hook.
- Updated `session-log-check.py` to accept `--codex` flag — outputs `{"systemMessage": ..., "continue": true}` for Codex, `{"hookSpecificOutput": ...}` for Claude.
- Added `_merge_codex_hook()` to `core.py` — merges the Stop hook into `.codex/hooks.json`, same safe-merge pattern as Claude.
- Both `init_project()` and `update_project()` now call both merge functions.
- Applied `.codex/hooks.json` to this project's root.

### Decisions

#### D1 - Shared script with --codex flag, not two separate scripts

- D: One `session-log-check.py` script handles both agents via `--codex` arg rather than two separate files.
- R: The session-staleness logic is identical; only the output JSON shape differs. Single file is easier to maintain.
- F: `memory_seed/seed/.memory-seed/hooks/session-log-check.py`, `.memory-seed/hooks/session-log-check.py`.

#### D2 - systemMessage for Codex (not additionalContext)

- D: Use `systemMessage` for Codex output, not an agent-context injection field.
- R: Codex's hook output has no `additionalContext` equivalent. `systemMessage` shows the reminder in the Codex UI — visible to the user who can then prompt the agent. Combined with the `agent-rules.md` instruction, this provides both enforcement layers.
- F: `session-log-check.py`.

### Validation

- `init_project()` in temp dir → both `.claude/settings.json` and `.codex/hooks.json` created.
- `_merge_codex_hook()` called twice → idempotent, no duplicate Stop groups.
- `python3 session-log-check.py --codex` → `{"systemMessage": "...", "continue": true}`.

## 2026-05-27 02:40 - Cursor and Gemini CLI hooks added; script generalised

```yaml
entry_id: ms-4f1e9a2d
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Generalised `session-log-check.py` to handle multiple agents via `--<agent>` flag: `--codex`, `--cursor`, `--gemini`, default (Claude).
- Added `_merge_cursor_hook()` → `.cursor/hooks.json` with `afterAgentResponse` event; outputs `{"agentMessage": "..."}`.
- Added `_merge_gemini_hook()` → `.gemini/settings.json` with `Stop` event; outputs `{"additionalContext": "..."}`.
- All four merge functions called unconditionally from `init_project()` and `update_project()` — model-agnostic by design.
- Applied all four hooks to this project's root.
- Skipped OpenCode (TypeScript plugin architecture, not shell commands) and Continue (no documented hook system).

### Decisions

#### D1 - All hooks installed unconditionally

- D: Install hooks for Claude, Codex, Cursor, and Gemini unconditionally on `memory-seed init`, regardless of which agents the project currently uses.
- R: Memory Seed's value proposition is model-agnosticism. A project may switch agents or use multiple. Installing all hooks upfront means the enforcement is always ready.
- A: Only install hooks for detected agents — rejected because agent detection is fragile and defeats the interoperability goal.
- F: `memory_seed/core.py` (all four merge calls unconditional).

### Validation

- `init_project()` in temp dir → all four hook files created with correct commands and formats.
- All four merge functions called twice → idempotent.
- `python3 session-log-check.py --cursor` → `{"agentMessage": "..."}`.
- `python3 session-log-check.py --gemini` → `{"additionalContext": "..."}`.

## 2026-05-27 02:45 - End-of-session runtime update

```yaml
entry_id: ms-6b3d8e1f
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Updated `.memory-seed/index.md` topology: added `.memory-seed/hooks/`, `.claude/settings.json`, `.codex/hooks.json`, `.cursor/hooks.json`, `.gemini/settings.json`; added design decisions for hook merge architecture.
- Updated `.memory-seed/policy.md`: added `.memory-seed/hooks/*.py` to File Ownership; renamed "End Of Work" to "End Of Turn" with tightened timing language.
- Fixed `doctor()` in `core.py` to skip version-check for non-`.md` seed files (Python scripts have no frontmatter).
- Synced installed `.memory-seed/agent-rules.md` from seed template (project copy was lagging the "End Of Turn" update).

### Validation

- `memory-seed doctor` → ok: True, control_plane_ok: True, bootstrap_complete: True.

## 2026-05-27 02:54 - Session log timestamp format corrected

```yaml
entry_id: ms-3a7c5f2b
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Added minute-level timestamps to all session entry headings in root and demo session files, per agent-rules format requirement ("prefer minute-level timestamps in entry headings").
- Earlier entries were written with date-only headings — corrected retroactively with approximate times.
- Demo sub-project entries corrected from out-of-range times (22:00–23:15) to 00:05, 00:20, 00:35.

### Decisions

#### D1 - Times are approximate for retroactively logged entries

- D: Use approximate timestamps for entries written after the fact rather than omitting times entirely.
- R: Approximate times preserve chronological ordering and satisfy the format spec; omitting times entirely loses the audit-trail value of the timestamp.

## 2026-05-27 14:10 - Fix Stop hook JSON output schema error

```yaml
entry_id: ms-8d2f4a1e
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Fixed Claude Code Stop hook JSON output validation error.
- `session-log-check.py` was outputting `hookSpecificOutput.hookEventName: "Stop"` which is not a valid `hookSpecificOutput` type in Claude Code's schema (only `PreToolUse`, `UserPromptSubmit`, `PostToolUse`, `PostToolBatch` are valid).
- Changed Claude default output branch to use top-level `systemMessage` — same field already used for the Codex output, and valid for Stop hooks.
- Fixed both the seed template and the installed project copy.

### Decisions

#### D1 - Use systemMessage for Stop hook output

- D: Output `{"systemMessage": "..."}` for Claude Code Stop hooks instead of `hookSpecificOutput`.
- R: `hookSpecificOutput` is only valid for tool-use hook events. `systemMessage` is the correct top-level field for injecting context in Stop hooks and is already consistent with the Codex format.
- F: `memory_seed/seed/.memory-seed/hooks/session-log-check.py`, `.memory-seed/hooks/session-log-check.py`.
- T: `python3 .memory-seed/hooks/session-log-check.py` → `{"systemMessage": "..."}`, no validation error in Claude Code.

## 2026-05-27 14:35 - Add help CLI command and clarify update-vs-upgrade docs

```yaml
entry_id: ms-1c7b9e3a
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Added a `memory-seed help` command (`memory_seed/cli.py`). Bare `memory-seed` now prints help instead of an argparse error (`subparsers` changed to `required=False`, command defaults to `help`).
- `_print_help()` leans on argparse's `parser.format_help()` (single source of truth) and appends a "Keeping Memory Seed current" note + a `<cmd> -h` pointer.
- Added README `## Updating` section distinguishing package upgrade (`uv tool upgrade` / `pip install --upgrade`) from project seed-file propagation (`memory-seed update`); clarified `update` sources files from the installed package, not PyPI.
- Documented `help` / `-h` / `<cmd> -h` discoverability in README Python CLI section.
- Added `## Unreleased` CHANGELOG section (also fixed a pre-existing failing doc test that requires it).
- Added `CliHelpTests` to `tests/test_memory_seed.py`.

### Decisions

#### D1 - help leans on argparse, not a hand-curated duplicate

- D: `help` prints `parser.format_help()` plus a short appended note, rather than a hand-maintained command/flag reference.
- R: A curated duplicate drifts out of sync when flags change; argparse is the source of truth and can't lie. Worked examples live in the README prose instead.
- A: Fully curated static reference — rejected for maintenance/drift cost (user feedback steered this).
- F: `memory_seed/cli.py`.
- T: `python -m memory_seed.cli help` and bare invocation both print help, exit 0; 65 tests pass.

### Validation

- `python -m memory_seed.cli help` / (no args) / `-h` / `version` → all correct.
- `python -m unittest discover -s tests` → 65 tests, OK.

## 2026-05-27 14:57 - Lock chronology, retrieval hook, clock-sourced recency (2.1.2)

```yaml
entry_id: ms-5e1a9d44
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Added append-only chronology rules to `agent-rules.md` (seed + installed, byte-identical): entries are appended at the end with the current clock time, never backdated or reordered.
- Extended `session-log-check.py` to warn when the day's entries are out of ascending time order, independent of the staleness check.
- Added `memory-retrieval-check.py` retrieval-reminder hook for all agents (Claude/Codex/Gemini via `UserPromptSubmit`, Cursor via `sessionStart`), gated by an 8-hour marker file; wired via new `_merge_*_retrieval_hook()` functions in `core.py`, added to `SEED_FILES` and `pyproject.toml`.
- Made MCP recency clock-sourced: `memory_search` reads `date.today()` at call time; removed the caller-supplied `today` override from the tool schema (kept a test-only `today` kwarg on `call_tool`).
- Repaired this file: the earlier 14:35/14:10 pair was inverted; reordered to ascending before appending this entry.

### Decisions

#### D1 - Cursor retrieval uses sessionStart, not beforeSubmitPrompt

- D: Wire Cursor's retrieval reminder to `sessionStart` with `additional_context`.
- R: Cursor's `beforeSubmitPrompt` can only allow/deny submission; it cannot inject context. `sessionStart`'s `additional_context` is the documented context-injection field.
- A: `beforeSubmitPrompt` — rejected, no context injection.
- F: `memory_seed/seed/.memory-seed/hooks/memory-retrieval-check.py`, `memory_seed/core.py`.

#### D2 - today override removed from agent surface, kept for tests

- D: `memory_search` no longer reads `today` from tool arguments; recency uses the system clock. A test-only `today` keyword on `call_tool` (unreachable via the MCP protocol) preserves deterministic tests.
- R: A stale agent-supplied date can skew recency; the clock is the trustworthy source. The protocol only passes `arguments`, so the kwarg cannot be set by an agent.
- F: `memory_seed/mcp_server.py`, `memory_seed/mcp_validate.py`, `tests/test_mcp_server.py`.

### Validation

- `python -m unittest discover -s tests` → 71 tests, OK.
- `memory-seed doctor` → healthy; agent-rules and hook seed/installed copies byte-identical.
- `memory-seed init` in a temp dir → all four agent configs carry both the log and retrieval hooks; merges idempotent across repeated `update`.


## 2026-05-27 15:29 - Rename rationale to reason in control-plane docs

```yaml
entry_id: ms-7f2a3c90
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: .
subproject_path: null
```

### Summary

- Renamed the term "rationale" to "reason" across the reusable control-plane docs (seed + byte-identical installed copies): agent-rules.md, project-bootstrap.md, skills/memory_consolidation.md. Case-preserving 1:1 token replacement.
- The DRAFT decision-record mnemonic is unchanged: R now stands for Reason instead of Rationale, keeping the acronym intact while making the slot plainer.
- Updated coupled assertions in tests/test_session_schema.py to match the new wording.
- Excluded by scope: generated index.md/policy.md, demo/ sub-project, archives, and append-only session history.

### Decisions

#### D1 - Reason over Rationale to strengthen the DRAFT mnemonic

- D: Rename Rationale to Reason in control-plane docs.
- R: "Rationale" was the most formal and least memorable of the five DRAFT terms; "Reason" keeps the R letter and is plainer.
- A: Leave as-is — rejected; user wanted the clearer term.
- F: agent-rules.md, project-bootstrap.md, skills/memory_consolidation.md (seed + installed), tests/test_session_schema.py.
- T: 71 tests pass; no remaining "rationale" in in-scope docs; seed/installed parity holds.
