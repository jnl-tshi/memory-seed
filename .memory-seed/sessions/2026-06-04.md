---
tags:
  - session-log
  - memory-seed
session_date: 2026-06-04
default_user_initials: JN
---

# Session Log - 2026-06-04

## 2026-06-04 00:34 - Make DRAFT the baseline session-entry format; route simpler/richer as alternatives

```yaml
entry_id: ms-d5f3155e
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Decision

- D: Reframed `## Session Log Format` in `.memory-seed/agent-rules.md` (and its byte-synced seed twin) so the single-decision DRAFT record is the baseline shape; the Small work and Multi-decision shapes are now framed as explicit down/up routes. Reordered Entry Shapes to Meaningful-decision -> Small-work -> Multi-decision, switched the headline example to a DRAFT entry, and annotated D/R (mandatory) and A/F/T (optional). (mandatory)
- R: User wanted agents to default to a reason-bearing decision record rather than reach for the lightest shape first, while still routing decision-less routine work down to the bare summary and never inventing a decision to fill the baseline. (mandatory)
- A: Considered leaving the headline example as the minimal Summary/Validation/Follow-up block; rejected - showing DRAFT up front reinforces the baseline. Plan was refined remotely via Ultraplan and approved before implementation. (optional)
- F: `.memory-seed/agent-rules.md` + `memory_seed/seed/.memory-seed/agent-rules.md` (identical edits; preserved the four-backtick ````markdown fence and nested ```yaml block, and all test-asserted substrings incl. the Multi-decision `### Summary` / "Summarize the coherent task"). (optional)
- T: `tests.test_session_schema` 12/12 pass (required substrings + live==seed normalized). Full suite shows 2 failures in `SessionLogOrderingHookTests` that are unrelated and environmental - see Follow-up. (optional)

### Follow-up

- Pre-existing latent test bug (not caused by this change): `test_staleness_fires_when_last_entry_is_old` and `test_staleness_not_defeated_by_file_mtime` hardcode a `01:00` entry and assert it reads as stale. They fail when the wall clock is between ~00:00 and 01:15 (it is currently 00:33), because a same-day `01:00` heading is then in the future. Worth making the test use a relative "now minus N minutes" timestamp. Offered to fix.
- Working-tree EOL skew remains: live `agent-rules.md` is CRLF, seed copy is LF. Byte-divergent but `read_text` normalization keeps the schema equality test green and git normalizes on commit. Cosmetic, pre-existing.
- Not released: control-plane frontmatter still `2.3`. This is a material workflow-wording change that likely warrants a control-plane version bump + CHANGELOG at release; deferred with the session's other unreleased changes (`ms-6a09aea8`, `ms-2cd452e4`, `ms-6eeb512f`).


## 2026-06-04 00:44 - Port DRAFT-baseline reframe to demo runtime; re-sync seed after manual ### Summary edits

```yaml
entry_id: ms-3a9a99a6
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Ported the DRAFT-baseline reframe (ms-d5f3155e) to `demo/.memory-seed/agent-rules.md`, and re-synced the seed copy after a manual edit diverged it from live.

### Decision

- D: Applied the reframe to the demo's existing v2.0 structure (headline -> DRAFT entry, baseline-first Entry Shapes intro, reorder to Meaningful/Small/Multi, (mandatory)/(optional) markers, baseline lead line) while keeping the demo's own "Rationale" terminology and its `### Rationale Rules` section placement. (mandatory)
- R: The request was to mirror "the new changes I made" - i.e. the reframe - not a v2.0->2.3 migration or the older Rationale->Reason rename (which was a prior commit, not part of these changes). "Rationale" appears throughout the demo file (lines 92/157/287/450+), so renaming only one section would make the demo internally inconsistent. (mandatory)
- A: Full sync of the demo section to root wording (incl. Rationale->Reason) - rejected as scope creep that would leave the rest of the demo file inconsistent. (optional)
- F: `demo/.memory-seed/agent-rules.md` (headline + Entry Shapes + Rationale Rules); `memory_seed/seed/.memory-seed/agent-rules.md` (re-synced to live). (optional)
- T: `tests.test_session_schema` 12/12 (seed==live restored). Demo fences balanced (20). Full suite 2 failures = the same unrelated date-flaky staleness tests (now 00:44, still inside the <01:15 window). (optional)

### Implementation

- Mirrored the user's two manual `### Summary` additions (headline example + Meaningful-decision template) from live root into both the seed copy and the demo, so all three agree.
- Seed re-sync done by copying live's text normalized to the seed's LF convention, which also guarantees byte-equality for the schema test regardless of surgical-edit precision.

### Follow-up

- Demo left at `memory-system-version: 2.0`; a full 2.0->2.3 demo migration remains out of scope unless requested.
- Same deferred items stand: the date-flaky staleness tests (offer to fix with a relative timestamp) and the eventual control-plane version bump + CHANGELOG at release.


## 2026-06-04 01:08 - Release 2.4.0: package + control-plane bump, committed and tagged; publish paused at pypi gate

```yaml
entry_id: ms-9066d377
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Cut the 2.4.0 release bundling this session's unreleased work (Codex MCP auto-registration ms-2cd452e4, doctor de-silence ms-6eeb512f, Claude .mcp.json fix ms-6a09aea8, DRAFT-baseline reframe ms-d5f3155e). Commit `395ae60` pushed to main; GitHub Release `v2.4.0` created, which triggered `publish.yml`.

### Decisions

#### D1 - 2.4.0 minor + control-plane 2.3 -> 2.4

- D: Package 2.3.0 -> 2.4.0 (minor; Codex MCP is a new feature) and control-plane 2.3 -> 2.4. (mandatory)
- R: Bumping the control-plane is what makes existing projects receive the reframed agent-rules.md + Codex changes via `memory-seed update` (forward-only skip is keyed on control-plane version). (mandatory)
- F: pyproject, core.py VERSION, all live+seed control-plane .md frontmatter (excluded archive/tests/demo), CHANGELOG, index.md Active State. (optional)

#### D2 - Convert version test fixtures to get_version() and de-flake staleness tests

- D: Replaced hardcoded "2.3" in test fixtures with `get_version()` (except the one canonical pin -> "2.4"); rewrote the two SessionLogOrderingHookTests to stamp `now - 30 min` instead of a fixed `01:00`. (mandatory)
- R: The hardcoded-version fixtures are the test-side of the version-bump trap; get_version() makes them self-tracking. The `01:00` staleness tests fail in the <01:15 post-midnight window (a fixed early time reads as future) - and `publish.yml` CI runs `tests.test_memory_seed`, so a flaky failure could have blocked the build. (mandatory)
- T: Full suite 99 OK locally; CI Build job passed in 14s (confirms the de-flake held on the runner). (optional)

### Implementation

- Excluded `demo/` and the scratch `memory-seed-claude-code-mcp-issue.md` from the release commit; committed `.mcp.json` (dogfood), archive/2.2 + archive/2.3 snapshots, and the new session logs.
- Stripped the dead `mcpServers` block from this repo's `.claude/settings.json` (part of the .mcp.json migration).

### Validation

- `python -m unittest discover -s tests` -> 99 OK; `memory-seed doctor` healthy (one expected non-fatal Codex MCP warning - this repo has not registered its own .codex/config.toml yet).
- GitHub Release v2.4.0 Build job: success. Publish job: PAUSED at the `pypi` environment manual-approval gate.

### Follow-up

- PUBLISH NOT YET LIVE. The `pypi` environment requires manual approval (reviewer jnl-tshi). User chose to approve it themselves: run page -> "Review deployments" -> approve, or `gh api -X POST repos/jnl-tshi/memory-seed/actions/runs/26920903634/pending_deployments -f environment_ids[]=15455640661 -f state=approved -f comment=""`. PyPI push is irreversible once approved.
- Optional dogfard: register this repo's own Codex MCP (write .codex/config.toml) to clear the doctor warning.
- Demo 2.0 control-plane migration: still not done (re-read of the request suggests "do both" meant version-bump + publish, not the demo migration). Confirm if a full demo migration is wanted.


## 2026-06-04 01:11 - 2.4.0 published to PyPI (publish gate approved)

```yaml
entry_id: ms-5587c5b2
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Follow-up to ms-9066d377: user approved the `pypi` environment deployment. Publish job succeeded (18s, with digital attestations); `memory-seed 2.4.0` is live on PyPI (https://pypi.org/pypi/memory-seed/2.4.0/ -> 200). Release complete.

### Follow-up

- Still optional/open: register this repo's own `.codex/config.toml` to clear the doctor warning; demo 2.0->2.4 control-plane migration (pending scope confirmation); fold the post-release session entries into a follow-up commit if desired.


## 2026-06-04 01:14 - Post-release cleanup: dogfood repo Codex MCP; flagged demo-migration hazard

```yaml
entry_id: ms-d3518e28
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Registered this repo's own Codex MCP (`.codex/config.toml`) so it dogfoods the 2.4.0 feature; `doctor` now fully clean (no warnings). Committed + pushed as `774dbdb` along with the post-release session entries.

### Follow-up

- Demo migration hazard found: `demo/` root `CLAUDE.md`/`AGENTS.md`/`GEMINI.md` are HyperFrames-specific (no memory-system-version frontmatter), so `memory-seed update` run in `demo/` would CLOBBER them with memory-seed seed routing files. A safe demo migration must touch only `demo/.memory-seed/` control-plane files (agent-rules.md, project-bootstrap.md, skills/*) + frontmatter, never the HyperFrames root files. Awaiting user go before proceeding.


## 2026-06-04 01:17 - Refresh demo/.memory-seed control-plane to 2.4 (safe, HyperFrames root untouched)

```yaml
entry_id: ms-6e1aadec
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: demo
```

### Decision

- D: Refreshed only `demo/.memory-seed/` reusable control-plane (agent-rules.md, project-bootstrap.md, skills/index.md) to the current 2.4 seed versions, bumped demo-local index.md/policy.md frontmatter 2.0->2.4 (body preserved), and archived the old 2.0 snapshot under `demo/.memory-seed/archive/2.0/`. (mandatory)
- R: Option A (adopt current). Did NOT run `memory-seed update` in demo/ because the demo's root CLAUDE.md/AGENTS.md/GEMINI.md are HyperFrames files and update would clobber them; surgical copy of just the `.memory-seed/` reusable files avoids that. This supersedes the earlier "keep Rationale voice" choice (that was for a minimal alignment, not a migration). (mandatory)
- A: Run `memory-seed update` in demo (rejected: clobbers HyperFrames root files); keep Rationale voice (rejected: user chose full refresh). (optional)
- F: demo/.memory-seed/{agent-rules.md, project-bootstrap.md, skills/index.md} replaced; index.md/policy.md frontmatter bumped; archive/2.0/ snapshot added. (optional)
- T: Verified all 5 demo control-plane files report 2.4; agent-rules now has Reason Rules + baseline-first; demo/CLAUDE.md still starts with HyperFrames header; demo session log preserved. (optional)

### Follow-up

- demo/ remains untracked local housekeeping (not committed; consistent with prior). The recent root session entries (ms-d3518e28, this one) are also uncommitted. Offer to commit if a tidy tree is wanted.


## 2026-06-04 08:00 - End-of-session consolidation: promote 2.4.0 facts into index.md

```yaml
entry_id: ms-96e2ada7
user_initials: JN
agent_type: claude-opus-4-8
project_path: .
subproject_path: null
```

### Summary

- Ran the consolidation routine after the 2.4.0 release (triggers: release completed + >3 meaningful entries). `memory-seed compact` reviewed; full suite 99 OK; `doctor` healthy.

### Implementation

- Promoted durable facts into `.memory-seed/index.md`: Topology now lists the MCP config registration targets (.mcp.json / .cursor/mcp.json / .gemini/settings.json / .codex/config.toml); Design Decisions gained the Claude .mcp.json location gotcha, the Codex .codex/config.toml registration, the doctor warnings channel, the DRAFT-baseline session-entry shape, and the GitHub-Release-triggers-publish + pypi manual-approval-gate release fact.
- Reviewed `policy.md` (no new durable behavioral constraint) and skills (no runbook changed) - left unchanged.
- Updated agent auto-memory: refreshed the control-plane-version-bump-trap note and added a release-pypi-manual-approval-gate memory.

### Validation

- `python -m unittest discover -s tests` -> 99 OK; `python -m memory_seed.cli doctor` -> healthy, bootstrap complete, no warnings.

### Follow-up

- None open from the release. demo assets/renders and the scratch `memory-seed-claude-code-mcp-issue.md` remain intentionally untracked.
