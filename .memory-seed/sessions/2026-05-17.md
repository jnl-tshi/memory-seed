## 2026-05-17 - Memory system evaluation

- Evaluated the local `.AGENTS` memory system by following `AGENTS.md` and `.AGENTS/agent-rules.md`.
- Finding: the repository claims operating mode, but required operating files `.AGENTS/index.md`, `.AGENTS/context.md`, and `.AGENTS/style.md` are missing.
- Finding: `.AGENTS/sessions/` exists but had no prior session logs before this note, so there were no last two session entries to summarize.
- Follow-up: repair/bootstrap should create the missing required files using `.AGENTS/project-bootstrap.md`, then validate that operating mode no longer requires bootstrap guidance.

## 2026-05-17 - Operating memory repair

- User clarified the project identity: Memory Seed is a reusable local memory-system seed that can be planted into other projects and grow project-specific memory across AI sessions without vendor lock-in.
- User approved creating the missing operating files `.AGENTS/index.md`, `.AGENTS/context.md`, and `.AGENTS/style.md`.
- Decision: version archiving applies only to reusable seed/control-plane artifacts, not generated operating files such as `index.md`, `context.md`, `style.md`, or dated session logs.
- Decision: generated operating-file changes should be recorded in this project's `.AGENTS/sessions/` history.
- Decision: `style.md` should act as a project-type-aware style router rather than a single rigid style guide.

## 2026-05-17 - Bootstrap owns style selection

- User clarified that project-type-aware style selection must happen during bootstrap because a virgin target project does not yet have a generated `.AGENTS/style.md`.
- This supersedes the earlier same-day note that described `.AGENTS/style.md` itself as the style router.
- Archived reusable version `1.0` artifacts under `.AGENTS/archive/1.0/`.
- Updated reusable control-plane version to `1.1` for `AGENTS.md`, `CLAUDE.md`, `.AGENTS/agent-rules.md`, and `.AGENTS/project-bootstrap.md`.
- Updated `.AGENTS/project-bootstrap.md` so Step 7 owns style-profile selection and target-specific `style.md` generation.
- Revised this project's generated `.AGENTS/style.md` so it guides Memory Seed development rather than acting as the reusable style router.

## 2026-05-17 - File-reading agent compatibility and seed routing

- Archived reusable version `1.1` artifacts under `.AGENTS/archive/1.1/` before updating the control plane.
- Updated reusable control-plane version to `1.2`.
- Added `GEMINI.md` as a Gemini CLI routing file that points agents to `AGENTS.md`.
- Updated seed file/folder lists to include `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.AGENTS/agent-rules.md`, and `.AGENTS/project-bootstrap.md`.
- Added the portability rule that Memory Seed targets file-reading AI coding agents through plain Markdown, predictable paths, explicit read order, and minimal vendor-specific assumptions.
- Added the explicit rule that reusable control-plane files must be archived under `.AGENTS/archive/<version>/` before replacement.

## 2026-05-17 - Public GitHub repository preparation

- User asked to publish Memory Seed as a public GitHub repository.
- Added `README.md` as a public-facing project overview.
- Initialized a local Git repository on `main` and created the initial commit.
- Blocker: `gh auth status` reports the saved GitHub token for account `jnl-tshi` is invalid, so repository creation and push require re-authentication before publishing can complete.
- User re-authenticated GitHub CLI.
- Published the project as the public GitHub repository `jnl-tshi/memory-seed`.

## 2026-05-17 - Python CLI start

- User requested a Python tool rather than a Node tool.
- Added a Python CLI with `init`, `doctor`, and `version` commands.
- `init` copies only bundled reusable control-plane files into the target folder: `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.AGENTS/agent-rules.md`, and `.AGENTS/project-bootstrap.md`.
- `init --dry-run` previews planned copies without requiring `--force` and without changing files.
- `init --force` backs up existing seed files under `.AGENTS/backups/<timestamp>/` before replacement.
- Added unit tests for copy scope, overwrite safety, backups, version reporting, and doctor checks.

## 2026-05-17 - PyPI publishing setup

- User configured a PyPI pending trusted publisher for project `memory-seed`.
- Added `.github/workflows/publish.yml` using GitHub Actions, `uv build`, and `pypa/gh-action-pypi-publish@release/v1`.
- Workflow uses GitHub environment `pypi`, matching the intended PyPI publisher configuration.
- Added `LICENSE` and PyPI classifiers/keywords to package metadata.

## 2026-05-17 - Security hardening

- `memory-seed init --force` now adds `.AGENTS/backups/` to the target project's `.gitignore` when backups are created.
- CLI prints a warning that backups were ignored to reduce accidental leaks.
- Added public-memory hygiene rules to agent rules, bootstrap guidance, and README.
- Updated tests for backup `.gitignore` behavior.

## 2026-05-17 - Author metadata correction

- Corrected package author metadata and license copyright holder from `John Tshibuyi` to `Jean Tshibuyi`.
- Reason: PyPI displayed the incorrect author name in package metadata.
- Updated full-name metadata to `Jean Nathan Tshibuyi` where a full name is required.
- Bumped Python package release version from `1.2.0` to `1.2.1` so PyPI can publish corrected metadata; control-plane `memory-system-version` remains `1.2`.

## 2026-05-17 - Bootstrap mode check fix

- User identified that newly initialized projects were confused because planted `agent-rules.md` asserted operating mode before generated files existed.
- Updated `AGENTS.md` and `.AGENTS/agent-rules.md` to require checking for `.AGENTS/index.md`, `.AGENTS/context.md`, `.AGENTS/style.md`, and `.AGENTS/sessions/` before entering operating mode.
- Bootstrap mode now applies to seed-only projects as well as missing or incomplete `.AGENTS` folders.
- Updated the CLI copy implementation to write Markdown contents rather than preserving filesystem metadata, avoiding local OneDrive reparse-point metadata being copied into throwaway or target projects.
- Bumped control-plane `memory-system-version` to `1.3` and Python package version to `1.3.0`.

## 2026-05-17 - Local uv installation

- Installed `uv` using Astral's official Windows installer.
- Installer placed `uv.exe`, `uvx.exe`, and `uvw.exe` in `C:\Users\johnn\.local\bin`.
- Verified `uv 0.11.14` and `uvx 0.11.14` in the active shell after prepending that directory to `PATH`.
- Note: new PowerShell terminals may need to be restarted before the updated user PATH is available.
