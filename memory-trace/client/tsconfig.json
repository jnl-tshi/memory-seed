{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ES2020"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    // node:test loads app modules directly, and its ESM loader will not resolve
    // an extensionless VALUE import between two source files - only type-only
    // imports, which are erased before it ever looks. Vite resolves either
    // form. Safe under noEmit: nothing is emitted for the extension to be
    // wrong in.
    "allowImportingTsExtensions": true,
    "jsx": "react-jsx"
  },
  "include": ["src", "vite.config.ts"],
  // Unit tests run on node:test with native type stripping, so they import
  // with explicit .ts extensions and pull in node builtins. Keeping them out
  // of the app compile keeps `npm run typecheck` and `vite build` clean.
  "exclude": ["**/*.test.ts"]
}
