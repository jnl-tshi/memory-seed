---
tags:
  - session-log
  - memory-seed
  - demo
  - hyperframes
session_date: 2026-05-27
default_user_initials: JN
---

# Session Log - 2026-05-27

## 2026-05-27 00:05 - HyperFrames demo sub-project initialized

```yaml
entry_id: ms-demo-init
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: demo
subproject_path: null
```

### Summary

- Initialized `demo/` as a HyperFrames video project using `npx hyperframes init .`
- Installed FFmpeg (v8.1.1) via winget as a prerequisite for rendering.
- Wrote 30-second product demo composition in `demo/index.html` — 5 scenes, GSAP animations, 1920×1080.
- Created sub-project Memory Seed runtime at `demo/.memory-seed/` manually (did not use `memory-seed init` to preserve HyperFrames-generated AGENTS.md and CLAUDE.md).
- Bootstrapped `demo/.memory-seed/index.md` and `demo/.memory-seed/policy.md`.

### Decisions

#### D1 - Keep HyperFrames routing files intact

- D: Do not overwrite `demo/AGENTS.md` or `demo/CLAUDE.md` with memory-seed init output.
- R: HyperFrames generates a detailed CLAUDE.md with the skill routing table that is required before editing compositions. Overwriting it with the generic memory-seed routing file would lose that context.
- A: Could merge both files, but that adds complexity and `demo/` is not opened independently as a repo.
- F: `demo/.memory-seed/policy.md` records this as a local constraint.
- T: `npm run check` passed with 0 errors.

#### D2 - Single-file composition for first version

- D: Use one `index.html` for all 5 scenes rather than splitting into sub-compositions.
- R: The timeline_track_too_dense warning is cosmetic for a 5-scene, 30-second clip. Sub-compositions add complexity before the scene content is finalized.
- A: Split into `compositions/s1.html` through `compositions/s5.html` — deferred until scenes need independent iteration.
- F: `demo/index.html`.
- T: `npm run check` — 0 errors, 2 acceptable warnings.

### Validation

- `npm run check` → 0 errors, 2 warnings (`google_fonts_import`, `timeline_track_too_dense`), 0 layout issues.
- 0 WCAG errors (contrast warnings are for intentionally dim decorative labels and emoji elements in hidden clips).

### Follow-up

- Run `npm run dev` to preview in browser and dial in timing and visual polish.
- Run `npm run render --output memory-seed-demo.mp4` to produce the final MP4.
- Add `demo/node_modules/` and `demo/memory-seed-demo.mp4` to root `.gitignore`.
- Update root `index.md` Topology to record this sub-project runtime.

## 2026-05-27 00:20 - Kokoro TTS narration added; CTA timing split

```yaml
entry_id: ms-demo-narration
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: demo
subproject_path: null
```

### Summary

- Added open-source Kokoro TTS narration (af_heart voice) to the composition.
- Split CTA audio into two clips to create a precise 0.5 s pause between "Get started." and "pip install memory-seed."
- Synced `#s5-cmd` visual fade-in to match the audio at 28.5 s.
- Extended video to 32 s (was 30 s) to give the CTA scene breathing room.

### Decisions

#### D1 - Split CTA narration into two audio files

- D: Generate "Get started." and "pip install memory-seed." as separate WAV files and stagger them in the composition with explicit `data-start` and `data-duration`.
- R: SSML `<break>` tag support in kokoro-onnx is unreliable. Separate files give frame-accurate control over the 0.5 s gap.
- A: SSML markup in a single file — rejected due to uncertain support.
- F: `assets/narration.txt` (body only), `assets/narration-cta-1.wav`, `assets/narration-cta-2.wav`, `index.html`.
- T: `npx hyperframes lint` → 0 errors, 2 pre-existing warnings.

### Audio timing

| Clip | data-start | data-duration | Track |
|------|-----------|---------------|-------|
| narration.wav (body) | 0 | 26.6 | 2 |
| narration-cta-1.wav ("Get started.") | 27.0 | 1.0 | 3 |
| narration-cta-2.wav ("pip install…") | 28.5 | 1.5 | 4 |

### Prereqs installed

- `kokoro-onnx` + `soundfile` Python packages (pip install).
- Kokoro voice data (~27 MB) auto-downloaded to `~/.cache/hyperframes/` on first run.

## 2026-05-27 00:35 - Video duration extended; FFmpeg PATH resolved

```yaml
entry_id: ms-demo-duration-ffmpeg
user_initials: JN
agent_type: claude-sonnet-4-6
project_path: demo
subproject_path: null
```

### Summary

- Extended video from 30 s to 32 s to give the CTA scene breathing room (Scene 5: 3 s → 5 s).
- Diagnosed FFmpeg PATH issue: winget installed FFmpeg to `%LOCALAPPDATA%\Microsoft\WinGet\Packages\Gyan.FFmpeg_...\ffmpeg-8.1.1-full_build\bin` but the PATH update only applies to new shell sessions.

### Validation

- `npx hyperframes lint` → 0 errors, 2 pre-existing warnings after all changes.

### Follow-up

- User needs to either open a new terminal or set `$env:PATH` manually before running `npx hyperframes render`.
- Permanent fix: add the FFmpeg bin path to the Machine PATH via `[System.Environment]::SetEnvironmentVariable(...)` as Administrator.
