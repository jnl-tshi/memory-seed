name: Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "42 6 * * 2"
  workflow_dispatch:

permissions: read-all

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    # Scorecard analyses PUBLIC repositories; on a private one its checkout fails with git exit
    # 128 before analysis starts, which is how this job went red on every scheduled run from
    # 2026-08-04. Skipped rather than deleted: this project is open-core and intended to go
    # public, so the workflow stays ready and starts running the day visibility flips. A skipped
    # job is green; a permanently failing one just teaches everyone to ignore red CI.
    if: github.event.repository.visibility == 'public'
    permissions:
      security-events: write # upload SARIF results
      id-token: write # publish results to the OpenSSF REST API

    steps:
      - name: Check out repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Run analysis
        uses: ossf/scorecard-action@4eaacf0543bb3f2c246792bd56e8cdeffafb205a # v2.4.3
        with:
          results_file: results.sarif
          results_format: sarif
          # Publishing to the Scorecard API is what makes the README badge live.
          publish_results: true

      - name: Upload SARIF to code scanning
        uses: github/codeql-action/upload-sarif@7188fc363630916deb702c7fdcf4e481b751f97a # v4.37.1
        with:
          sarif_file: results.sarif
