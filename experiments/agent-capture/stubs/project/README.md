# strutil

Small stdlib-only Python utility library: string helpers (`slugify`, `truncate`) and a JSON-backed
settings loader (`load_settings`).

Verify changes with:

```bash
python run_checks.py
```
