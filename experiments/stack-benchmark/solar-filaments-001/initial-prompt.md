# Engineering task

Build the strongest reproducible solution you can for the supplied frozen Solar
Filament Segmentation Challenge workspace. Use only the supplied data, dependencies,
pretrained assets, tools, and compute contract. Keep the validation split and local
evaluator unchanged. Iterate from evidence, preserve failed approaches, and finish
with every canonical deliverable plus a locally validated Kaggle submission file.

Colab is a remote compute worker. Continue useful local engineering while jobs run
where possible. Do not treat GPU speed or assigned GPU model as solution quality.
Do not submit to Kaggle or use credentials unless explicitly instructed.
