[
 {
  "adr_id": "adr_semantic_provider",
  "title": "Model2Vec default provider; silent lexical degrade; model2vec the only required dep",
  "concern": "founding:.memory-seed/index.md#L85",
  "founding_head": "founding:.memory-seed/index.md#L85",
  "candidates": [
   {
    "ref": "mse_9r00krq403dsmdnq:d1",
    "title": "D1 - The lightweight install is `--no-deps`, not an extra, and the invariant that makes it work is now a test",
    "body": "- D: Keep `model2vec` required. Document `python -m pip install --no-deps memory-seed` as the supported install without semantic ranking, and pin with `test_model2vec_is_the_only_required_dependency` that `project.dependencies` contains exactly one entry and it is model2vec. - R: JNL asked for \"required, but a clear path to a lightweight version\". Python packaging cannot express that directly: extras only ADD depende",
    "claimed_by": []
   },
   {
    "ref": "mse_9r00krq403dsmdnq:d2",
    "title": "D2 - `esr` reports semantic availability, because the degradation was invisible for an unknown period",
    "body": "- D: `esr` probes the embedding provider exactly as search does and prints a \"Semantic ranking\" section on every run: `OK - <provider>` or `DEGRADED - ranking is lexical only` with the reason and the fix. - R: Found by JNL asking whether model2vec should be used for semantic similarity. It turned out it is ALREADY the package's only required dependency - and that it is absent from the interpreter this project runs on",
    "claimed_by": []
   },
   {
    "ref": "mse_trt2tdzxw9jx6h2a:d2",
    "title": "D2 - Keep semantic ranking default-on and add `--no-semantic`, rather than flipping it to opt-in",
    "body": "- D: `link audit` keeps ranking semantically by default; `--no-semantic` (and `semantic_enabled=False`) ranks lexically and resolves no provider at all, so no model is loaded. Confirmed the laziness the task required is already structural: `Model2VecEmbeddingProvider.__init__` loads nothing and `_load_model2vec_model` imports `model2vec` inside the function body, so importing `memory_seed.retrieval` never touches the",
    "claimed_by": []
   },
   {
    "ref": "mse_xfbcr3n4vet62hmh:d1",
    "title": "D1 - Semantic similarity ranks link-audit candidates; it never decides membership",
    "body": "- D: `audit_link_gaps` adds `SEMANTIC_OVERLAP_BOOST * cosine(source, candidate)` to the existing lexical score, using model2vec embeddings of each entry's full text. The lexical gate (shared files, shared title terms, or shared topics when unlinked) still decides which pairs are candidates at all; semantic similarity only reorders what got through. - R: Measured against the 104 supersedes/evolves edges authors declar",
    "claimed_by": []
   },
   {
    "ref": "mse_trt2tdzxw9jx6h2a:d3",
    "title": "D3 - Expose the semantic term and report its absence, instead of folding it into the total",
    "body": "- D: `LinkGapCandidate` gains `lexical_score` and `semantic_score` (raw cosine in `[0,1]` *before* the ×160 boost, `None` when semantic was off or unavailable); `file_overlap_score` keeps its meaning as the total and its misleading name, now documented. `_embed_entries_for_link_audit` returns the provider name and fallback reason instead of swallowing them, `audit_link_gaps` fills a `semantic_status` out-dict, and `l",
    "claimed_by": []
   },
   {
    "ref": "mse_1b01mnzx5aybpbyg:d1",
    "title": "D1 - `load_corpus` is the corpus read; bypassing it must be declared",
    "body": "- D: Add `retrieval.load_corpus(cwd, granularity)` composing `extract_memory_chunks` with both sidecar augmentations, route `search_memory` and `ranking_ab.run_ab` through it, and add `tests/test_corpus_read_path.py`, which pins the count of unaugmented reads per module against an allowlist carrying a written reason for each. - R: Sidecar augmentation was opt-in convention. Of roughly fifteen call sites only `search_",
    "claimed_by": []
   },
   {
    "ref": "mse_wh13f4kfxwr083jc:d3",
    "title": "D3 - Record what the guard cannot do rather than write it up as closed",
    "body": "- D: `CONTRIBUTING.md` and the CHANGELOG both state that a **pre-guard** global build has no such check compiled in, so the specific binary that caused the incident stays silent until the global install is refreshed. The remedy named there is `uv tool upgrade memory-seed` or `pip install -e .` into the worktree venv. - R: A guard written in `memory_seed/` only executes when `memory_seed/` is what got loaded — which i",
    "claimed_by": []
   },
   {
    "ref": "mse_hqasfw7847m20vz4:d1",
    "title": "D1 - Embed the same surface the lexical side scores",
    "body": "- D: `semantic_text(chunk)` composes `heading_path + tags + contexts + text`, and `_semantic_scores` embeds that instead of `chunk.text` alone. - R: JNL asked whether the two sides were being measured on the same content. They were not. `_lexical_score` reads tags at weight 12, contexts at 8, heading_path at 6, lexical_terms at 4 and the body at 1 per term, while the embedding saw the body only - so every blend weigh",
    "claimed_by": []
   },
   {
    "ref": "mse_57e1ek08qq9smwv3:d1",
    "title": "D1 - Dogfood the local build through `uv run`, and pin the config against a silent revert",
    "body": "- D: `.mcp.json` becomes `uv run --no-sync python -m memory_seed.mcp_server --stdio`. The published `uvx --from memory-seed` invocation stays as the constant `init` writes into user projects, so nothing changes for anyone downstream. `tests/test_mcp_local_build.py` pins that an update leaves a local-build entry alone. - R: The attention layer, its MCP instrumentation and its A/B gate all shipped today against a serve",
    "claimed_by": []
   },
   {
    "ref": "mse_ss97qw0m8makbng2:d3",
    "title": "D3 - The ranking-ab gate fails closed when its evidence base is empty",
    "body": "- D: Add `requires_affected_hits` to `Signal` and `ABResult`; the `attention` signal opts in, so an empty retrieval log now yields exit 1 and a REFUSED line rather than a pass. - R: As shipped this morning the gate returned PASS whenever `--query` was supplied against an empty log: nothing was affected, every query fell into the control bucket, both arms were trivially identical, and the pass certified nothing - the ",
    "claimed_by": []
   },
   {
    "ref": "mse_wz1aec7859rk0h67:d1",
    "title": "D1 - Decision",
    "body": "- D: Revert `semantic_text` to embedding `chunk.text` alone. Keep it as a named function with the measurement recorded in its docstring rather than inlining it back into `_semantic_scores`. - R: The argument for composing the surface was that lexical scores tags, contexts and heading_path while the embedding saw only the body, so the two sides were not being compared like for like and any blend weight partly fitted t",
    "claimed_by": []
   },
   {
    "ref": "mse_nf7re4qekngvnnbx:d1",
    "title": "D1 - Pillar B ships as a separate distribution; the decision rests on cadence + product-line, not dep isolation",
    "body": "- D: Wrote `docs/todo/memory-seed-explorer-distribution-plan.md` as the canonical two-phase plan. Phase 1 freezes a public retrieval service **in-package** (extract `call_tool`, `format_search_results`, `_ranked_to_dict`, `_chunk_to_dict`, `_semantic_provider` from `mcp_server.py` to join the already-shared `semantic_cache.py` parser/ranker; MCP becomes a thin wrapper and the in-package Lense consumes the same servic",
    "claimed_by": []
   },
   {
    "ref": "mse_8kfwn08qf59nnbtn:d1",
    "title": "D1 - Event log as substrate, derived counter as interface, fetches score and impressions do not",
    "body": "- D: `memory_seed/attention.py` records one JSON line per MCP retrieval event into gitignored `.memory-seed/.retrieval-log.jsonl`; a decayed per-entry score (half-life 30d) plus a plain `fetch_count` are derived on read, compacting into `.retrieval-attention.json` past 5,000 lines. Only `memory_get_chunk` fetches score; `memory_search` impressions are logged at weight zero. - R: JNL proposed a per-entry counter as th",
    "claimed_by": []
   },
   {
    "ref": "mse_emv963patckeftx8:d2",
    "title": "D2 - Filter the server's score-0 filler before anything cycles",
    "body": "- D: New `searchResults.ts` keeps a result when `score > 0 || matched_terms.length > 0`, and every full-text consumer reads the filtered array - the counter, both step functions, and the Enter guard. Search `limit` rose 12 -> 100 (`SEARCH_LIMIT`), and the counter renders `100+` when the filtered set fills the page. - R: `rank_memory_chunks` never drops zero-score chunks, so `granularity=entry` returns the entire corp",
    "claimed_by": []
   },
   {
    "ref": "mse_hexq9x671dww29te:d1",
    "title": "D1 - Ship a fail-closed full-corpus ranking gate",
    "body": "- D: Ship `memory-seed ranking-ab --signal <name> [--query <q> ...] [--json]` as the reusable branch-time gate for default ranking changes. The supersession signal derives every live lineage query, compares off/on at full corpus size, requires each replacement to out-rank the decision it retires, and requires a real no-affected-hit control to remain identical. - R: Fixtures prove that a mechanism fires, but only the ",
    "claimed_by": []
   },
   {
    "ref": "mse_22pckkvdm0a4jgfh:d1",
    "title": "D1 - Decision",
    "body": "- D: Turned the freshness supersession rank-dampener ON by default. `search_memory` and the MCP `memory_search` tool now default `supersession_damping=True`; the primitive `rank_session_memory` stays default-False because `search_memory` passes the flag explicitly (the ranker stays neutral). Reframed the docstring, MCP schema, and `graph-edge-contract.md` to on-by-default with `supersession_damping=False` as the opt-",
    "claimed_by": []
   },
   {
    "ref": "mse_v26pem9hsvsbjbge:d1",
    "title": "D1 - Accept the Wave 1 implementation set",
    "body": "- D: Accept and integrate deterministic topic suggestions, timeline Evidence Pack Phase 1, Trail continuity lanes, `superseding_head` plus the bounded successor boost, configurable integration mode, and inert lifecycle-link scaffolding steps 1-3. - R: Each feature preserved Markdown authority and derived/read-only boundaries, passed its focused worker checks, then passed review on the combined integration head. - A: ",
    "claimed_by": []
   },
   {
    "ref": "mse_9ca5mpekppn3b1y1:d1",
    "title": "D1 - Decision",
    "body": "- D: Overview now ranks by connectivity across the whole corpus by default, with a \"N of Y entries shown\" indicator and a \"Show more\" control — JNL agreed to this pairing (approach #2 + #4) after a design discussion about why Overview looked sparse relative to the 567-entry corpus. - R: Investigated the discussion's approach #2 (\"rank by connectivity/importance instead of recency, across all dates\") before building a",
    "claimed_by": []
   },
   {
    "ref": "mse_dka59wfne2be5fr0:d1",
    "title": "D1 - Order the fixes by dependency, and specify what each one must not break",
    "body": "- D: F0 (re-derive lifecycle labels from `evolves`) runs before F2 (recency); F3 (the band) is fitted last; each fix carries named non-regression guards that can fail, and `ranking-ab` gates none of them. - R: Two orderings are load-bearing rather than tidy. Recency is partly doing supersession's job by accident, so weakening it may surface retired decisions above their replacements - and with 4 `replaced_by` edges a",
    "claimed_by": []
   },
   {
    "ref": "mse_fcecj9hpq4qj16ay:d1",
    "title": "D1 - Decision",
    "body": "- D: Create `memory-trace/` as a separate distribution (own `pyproject.toml`, `memory_trace` package, `memory-trace` console command, `static/` assets). `git mv`'d `memory_seed/lense.py` → `memory-trace/memory_trace/lense.py`, `memory_seed/lense_static/*` → `memory_trace/static/*`, and `tests/test_lense.py` → `memory-trace/tests/`. Repointed the module's imports to the public `memory_seed` API, and its static-asset/h",
    "claimed_by": []
   },
   {
    "ref": "mse_26e49psx6rcdpee5:d1",
    "title": "D1 - Replace binary-per-field lexical scoring with BM25F, and score topics",
    "body": "- D: `_bm25f_score` with per-field weights (topics 8, heading 6, identifiers 3, body 1), k1 1.2 and per-field length normalisation. `tags` is folded into the `topics` field rather than keeping its own weight; `contexts` is dropped. - R: JNL identified that tags were the original home of topics, that topics had since moved to the sidecar, and that the scorer never followed. Measured: `tags` on 16 of 1259 chunks at wei",
    "claimed_by": []
   },
   {
    "ref": "mse_44fh1qcq29bwnbt6:d2",
    "title": "D2 - The recall findings outrank the band, and both causes are single constants",
    "body": "- D: Record two candidate fixes ahead of the band in priority: raise the semantic blend weight (`semantic_component = cosine * 3.0`, `semantic_cache.py:927`), and re-examine the recency multiplier. Neither is applied in this branch - both need their own held-out fit. - R: Semantic ranking is on by default and changes paraphrase recall by exactly zero - 63/120 in the top 8 with it on and off. The reason is arithmetic:",
    "claimed_by": []
   }
  ]
 },
 {
  "adr_id": "adr_session_layout",
  "title": "Month-grouped session layout; per-user gated on 2+ participants",
  "concern": "founding:.memory-seed/index.md#L147",
  "founding_head": "founding:.memory-seed/index.md#L147",
  "candidates": [
   {
    "ref": "ms-3cad2a35:d2",
    "title": "D2 - 2.9.0 read-only dual discovery",
    "body": "- D: Added `SessionDocument` / `iter_session_documents()` and routed package readers (`extract_memory_chunks()` / MCP and `compact_sessions()`) through it so both `sessions/YYYY-MM-DD.md` and `sessions/YYYY-MM-DD/<user>.md` are read. Per-user filenames use the bare slug form (for example `jean.md`). Writes, hooks, active-user resolution, migration, and `session_path()` remain deferred. - R: This is the safe Phase 1 f",
    "claimed_by": []
   },
   {
    "ref": "mse_q9vz4hj9xqk8b0ed:d1",
    "title": "D1 - Decision",
    "body": "- D: Extended the worktree naming in the lifecycle spec to include the user in multi-user workflows: `.<agent>/worktrees/<user>/<session>` when 2+ human participants are registered, gated on the SAME participant-count gate (`session_target()`) that flips session logs from flat to per-user. A solo repo stays flat `.<agent>/worktrees/<session>` (unchanged). No new config knob - the `<user>` segment follows the existing",
    "claimed_by": []
   },
   {
    "ref": "mse_ed8zaf52k3eyaxqv:d1",
    "title": "D1 - Decision",
    "body": "- D: Added a conservative migration API and CLI command. The migration parses each legacy `sessions/YYYY-MM-DD.md` entry, maps entry `user_initials` through `.memory-seed/project.yaml` `participants:`, writes/appends to `sessions/YYYY-MM-DD/<user>.md`, preserves existing `entry_id` values, creates per-user file frontmatter with one `hash_id`, backs up each migrated flat file, then removes the flat source so dual-read",
    "claimed_by": []
   },
   {
    "ref": "mse_k2188xq5evwmdny9:d1",
    "title": "D1 - Decision",
    "body": "- D: Make `.memory-seed/sessions/YYYY-MM/YYYY-MM-DD.md`, `.memory-seed/sessions/YYYY-MM/YYYY-MM-DD/<user>.md`, and `.memory-seed/sessions/diagrams/YYYY-MM/YYYY-MM-DD.md` the new canonical write targets. - R: Month folders keep session discovery manageable as logs grow without breaking old projects or forcing automatic migrations. - A: Historical files are not moved by init, update, hooks, MCP startup, or Memory Trace",
    "claimed_by": []
   },
   {
    "ref": "mse_j4s82bgb07vb1vvz:d1",
    "title": "D1 - Decision",
    "body": "- D: Confirmed scope with the user first (ship as a real product feature; one-time offer, not a per-session nag; implement directly rather than spec-first). Then, per the advisor's flag, first repaired the immediate inconsistency it caught: this session's own `sessions/2026-07-02/jean.md` was folded back into the flat file (previous entry) before writing any new behavior, so the repo wasn't left violating the rule be",
    "claimed_by": []
   },
   {
    "ref": "mse_31m31wsrwe77jh78:d3",
    "title": "D3 - Topic sidecars file under the ENTRY date, which reshapes the write and the correction path",
    "body": "- D: The skill states as its sharpest divergence from `link_swarm` that a batch writes one file per distinct ENTRY date under `sessions/topics/YYYY-MM/YYYY-MM-DD.md` — a 40-entry batch touches ~40 files spread across every month the corpus covers — and that a wrong batch is corrected by appending a block with a strictly later heading time to that SAME file, never by editing the published block. - R: `topic-sidecar-da",
    "claimed_by": []
   },
   {
    "ref": "mse_0vbj4zdcc11s3ggj:d1",
    "title": "D1 - One dated file per day, multiple diagram blocks appended, each keyed by entry_id inside",
    "body": "- D: Sidecars now live at `.memory-seed/sessions/diagrams/YYYY-MM-DD.md` - one file per date. Each diagram is a heading block shaped exactly like a session entry: `## <timestamp> - <title>` + fenced ```yaml block naming `entry_id` + fenced ```mermaid block(s). Multiple diagrams logged the same day append to the same file, ascending time order, exactly like session logs. `check_session_links()` reuses the existing `_E",
    "claimed_by": [
     "adr_diagram_sidecar_structure"
    ]
   },
   {
    "ref": "mse_m6dgqw1dbdmq1hpk:d1",
    "title": "D1 - Decision",
    "body": "- D: In `check_session_links()` (`memory_seed/core.py`), moved the entry-level `related_entries` scan (each entry's fenced ```` ```yaml ```` block) out of the `if doc.layout != \"per-user-day\": continue` gate so it runs for both legacy-flat and per-user files. Removed the now-duplicate copy of the same scan that used to sit inside the gate. The genuinely per-user-file-specific checks (user-slug validation, file-level ",
    "claimed_by": []
   },
   {
    "ref": "mse_4av5twf24m6xy7br:d1",
    "title": "D1 - Decision",
    "body": "- D: Made link/diagram sidecars **first-class inputs to the projection freshness signal**, then memoized the sidecar-derived read structures. The git freshness check already caught sidecar changes (they live under the `.memory-seed/sessions` pathspec), but the no-git mtime scan skipped them (`iter_session_documents` only recurses into `YYYY-MM`/`YYYY-MM-DD` dirs). Added `_tracked_document_paths` (session docs + link ",
    "claimed_by": []
   },
   {
    "ref": "mse_gtn504wfjt3c34p6:d2",
    "title": "D2 - Incremental reconciliation with fail-toward-full-rebuild, gated on the G2 equivalence test",
    "body": "- D: `ensure_current` now tries `_reconcile_locked()` before a full rebuild: forward HEAD/trunk moves reparse only changed session documents (committed delta + dirty-now + dirty-at-last-build), advance entry attribution over the new range only, re-emit trailer merges from the persisted fork table, and advance the file index only if it was already built. Every ambiguity - rewritten history (`merge-base --is-ancestor` ",
    "claimed_by": []
   },
   {
    "ref": "mse_pdbm4vfwavps92xk:d1",
    "title": "D1 - Decision",
    "body": "- D: Move historical root session files into month folders on `main`, leaving only months that have session Markdown at the session root. - R: The current repository should model the canonical grouped layout, and keeping both flat and grouped 2026-07-09 files would create duplicate-read ambiguity. - A: The migration created ignored timestamped backups under `.memory-seed/backups/20260710-031326/` before removing the ",
    "claimed_by": []
   },
   {
    "ref": "mse_zm2h343r4shfre3j:d1",
    "title": "D1 - Topics get a third sidecar family, modelled on diagrams, not links",
    "body": "- D: Backfilled topics live in `sessions/topics/YYYY-MM/YYYY-MM-DD.md`, a new sidecar family alongside diagrams and links. `links check` validates vocabulary membership (alias-canonicalizing), a 3-slug cap, orphan/date-mismatch/duplicate-block, and warns (never errors) when a sidecar restates a topic the author already wrote. - R: Append-only forbids reopening a published entry, so a topic learned later has nowhere e",
    "claimed_by": []
   },
   {
    "ref": "mse_ktw9r3nq5bvx2plm:d1",
    "title": "D1 - Decision",
    "body": "- D: Built Phases 1-2 of the lifecycle-edge linking hardening (see `docs/3_Spec/lifecycle-edge-linking-sidecars.md`). Root problem: `supersedes`/`evolves` are opt-in typed edges declared in an entry's own YAML at write time, and in practice almost nobody stamps them (across 312 entries they appear in ONE file); genuine supersessions get logged as generic `related_entries`, so the Trail's replaces/evolves lines nearly",
    "claimed_by": []
   },
   {
    "ref": "mse_6pmwp3gm2ebz2hsz:d1",
    "title": "D1 - Decision",
    "body": "- D: Ran `memory-seed user set jean` (writes gitignored `.memory-seed/local.yaml`) and created `.memory-seed/project.yaml` with a `participants:` entry (`slug: jean`, `initials: JNL`, `display_name: Jean Nathan Tshibuyi`). Backfilled `user_initials: USER` → `JNL` in all five entries already logged today in this flat `sessions/2026-07-02.md` file (now committed in `3086122`), and fixed the one line in `docs/functional",
    "claimed_by": []
   },
   {
    "ref": "mse_bqc8am1yq8sv5s92:d1",
    "title": "D1 - Decision",
    "body": "- D: Moved `NEXT_STEPS.md` to `docs/todo/NEXT_STEPS.md` (`git mv`), fixed the two live-file references (`.memory-seed/index.md` topology line, `docs/functionality-audit.md`) and all six now-relative internal links inside the file itself. Wrote five new plan docs — `git-commit-entry-linking-plan.md`, `supersession-edges-plan.md`, `interaction-frequency-ranking-plan.md`, `mermaid-usage-guidance-plan.md`, `failed-approa",
    "claimed_by": []
   },
   {
    "ref": "mse_5p94m2c3rwy7kbtj:d1",
    "title": "D1 - `memory-seed situate`: network-free orientation preflight with full cross-agent parity",
    "body": "- D: New `memory-seed situate` CLI (`situate.py`, mirroring `esr.py`) — a read-only report of LOCAL authoritative facts: git posture (branch / dirty / ahead-of-origin), `integration_mode`, the newest session entry (via the layout-aware `iter_session_documents` reader, not a re-derivation), worktree posture (reusing esr's helpers), and the local `pyproject` version + whether the CHANGELOG has a non-empty `## Unrelease",
    "claimed_by": []
   },
   {
    "ref": "ms-38098d7a:d1",
    "title": "D1 - Decision",
    "body": "- D: Added opt-in local user identity, per-user session target creation, and user-aware hooks while keeping no-user installs on the legacy flat session file. - R: This advances the multi-user roadmap without silently moving existing users' write targets; a clone must opt in through `.memory-seed/local.yaml`, `MEMORY_SEED_USER`, or an explicit CLI/user argument. - A: Automatic user inference was not added; guessing wo",
    "claimed_by": [
     "adr_session_layout_participant_gating"
    ]
   },
   {
    "ref": "mse_z9aqe69r95ezvc66:d1",
    "title": "D1 - Decision",
    "body": "- D: Closed the MCP graph sidecar-edge parity gap for lifecycle link sidecars. - R: `sessions/links/YYYY-MM/YYYY-MM-DD.md` sidecars are part of the effective graph contract, so MCP tools should not remain YAML-only while Trace and `links check` see union(YAML, sidecar). The shared merge point belongs in `memory_seed.retrieval`, which already depends on `semantic_cache`; putting it there avoids a reverse import from `",
    "claimed_by": []
   },
   {
    "ref": "mse_8wq2vnr5tkxm3jhc:d1",
    "title": "D1 - Decision",
    "body": "- D: Completed Phases 3-4 of the lifecycle-edge hardening (`docs/3_Spec/lifecycle-edge-linking-sidecars.md`). Phase 3: `memory-seed link audit` finds entry pairs sharing `F:` files or topics with no recorded edge - WITHOUT an all-pairs semantic scan (user's efficiency design: candidates = older entries sharing >=1 file OR >=1 topic; matching files override the lack of a topic link). File overlap surfaces a candidate ",
    "claimed_by": []
   },
   {
    "ref": "ms-a4282580:d2",
    "title": "D2 - Entry IDs and related links",
    "body": "- D: New generated entry IDs are planned as `mse_` plus 80 bits encoded in 16 lower-case Base32 characters; legacy `ms-` IDs remain valid forever. Entry-level `related_entries` become canonical graph edges, while file-frontmatter related refs remain coarse session/file relationships. - R: 80 bits is the practical middle ground: far safer than the current 32-bit visible IDs at team scale, materially shorter than 128-b",
    "claimed_by": [
     "adr_entry_id_scheme"
    ]
   },
   {
    "ref": "ms-8f4d2c61:d1",
    "title": "D1 - Decision",
    "body": "- D: Parse the most recent `## YYYY-MM-DD HH:MM` heading timestamp from the session file instead of checking `f.stat().st_mtime`. - R: File mtime reflects the last OS-level write to the file (including git operations), not the last session entry written by an agent. Content-based checking is the only way to distinguish \"agent wrote an entry\" from \"git touched the file\". - A: Keep mtime but exclude git processes — rej",
    "claimed_by": []
   },
   {
    "ref": "mse_3e63pfckz4rzkw25:d4",
    "title": "D4 - The ADR corpus table is re-derived from a committed script, and disagrees with both prior counts",
    "body": "- D: Ship `scripts/count_decision_shapes.py` and amend the ADR table in place to 138 numbered / 383 singular / 1 inline / 42 no-decision = 564 entries, 521 with an addressable decision, 115 multi-decision, 710 addressable decisions in total. - R: The ADR's 2026-07-20 hand count (612 entries) and the 2026-07-21 recount (580) were both unreproducible, and the honest finding is that the new numbers match neither. The me",
    "claimed_by": []
   },
   {
    "ref": "mse_m677wfhvq7s8b1x8:d1",
    "title": "D1 - Deterministic sidecar validation inside the single validator",
    "body": "- D: `check_session_links()` now scans `sessions/diagrams/*.md` after the entry-ID pass: no/invalid frontmatter or missing `entry_id` or no ```mermaid block or unbalanced fence -> `malformed-diagram`; filename stem != frontmatter `entry_id` -> `diagram-filename-mismatch`; `entry_id` resolving to no known entry -> `orphan-diagram`. Sidecars stay optional (no issue for entries without one), the fence check is purely le",
    "claimed_by": []
   }
  ]
 },
 {
  "adr_id": "adr_subproject_scoping",
  "title": "Sub-project runtime scoping and inheritance defaults",
  "concern": "founding:.memory-seed/policy.md#L78",
  "founding_head": "founding:.memory-seed/policy.md#L78",
  "candidates": [
   {
    "ref": "ms-0bd3d8b2:d2",
    "title": "D2 - Keep sub-project logs local but summarize parent-visible changes",
    "body": "- D: Detailed session logs remain in the nearest active runtime; parent/root memory gets only coordination summaries for root-visible topology, shared design, release behavior, inheritance, dependencies, risks, or priorities. - R: This preserves localized context while keeping the root aware of cross-project consequences. - A: Rejected mirroring sub-project sessions into root memory because that would duplicate noise",
    "claimed_by": []
   },
   {
    "ref": "mse_gbc4m5m71dqmen75:d1",
    "title": "D1 - Decision",
    "body": "- D: Generate each fixture by running the real `init_project()` and applying documented per-level strips inside a standalone git repo, pin the MCP server to the local working tree via absolute interpreter plus PYTHONPATH, keep hand-written `index.md`/`policy.md`/vocabulary byte-identical across levels, and treat each fixture's own `.memory-seed/sessions/` as the experiment readout, with `templates/` and `runs/` gener",
    "claimed_by": []
   },
   {
    "ref": "mse_j23dvr3zehz8g7xh:d1",
    "title": "D1 - Decision",
    "body": "- D: Document the `--branch`/`--no-branch` workaround (A) in `.memory-seed/skills/session_logging.md`, appended to the prose that already defines the `branch` field, and cross-reference it from `README.md`'s `session append` command entry; record the standing convention (D) — a harness passes `--branch` unconditionally, sourced from the Task Packet's `working_branch` — as a bullet under \"Branch And Worktree Defaults\"",
    "claimed_by": []
   },
   {
    "ref": "mse_is0a3iw426g6v2zb:d1",
    "title": "D1 - Decision",
    "body": "- D: Treated the repository as initialized operating mode (all reusable control files present, `index.md`/`policy.md` present - not seed-only), not bootstrap or repair mode. No lazy skill runbook loaded yet since no concrete task was specified beyond orientation. - R: `.memory-seed/index.md` confirms this is the root runtime with no parent to inherit policy from. Four personas are `status: active` in the registry wit",
    "claimed_by": []
   },
   {
    "ref": "ms-demo-root-summary:d2",
    "title": "D2 - Sub-project runtime created manually, not via memory-seed init",
    "body": "- D: Create `demo/.memory-seed/` by hand rather than running `memory-seed init` in the demo folder. - R: HyperFrames generates its own AGENTS.md and CLAUDE.md with a required skill routing table. Running memory-seed init would overwrite them and lose that context. Per agent-rules, sub-projects opened non-independently don't need their own routing files. - A: `memory-seed init --force` then restore HyperFrames CLAUDE.",
    "claimed_by": []
   },
   {
    "ref": "mse_j4s82bgb07vb1vvz:d1",
    "title": "D1 - Decision",
    "body": "- D: Confirmed scope with the user first (ship as a real product feature; one-time offer, not a per-session nag; implement directly rather than spec-first). Then, per the advisor's flag, first repaired the immediate inconsistency it caught: this session's own `sessions/2026-07-02/jean.md` was folded back into the flat file (previous entry) before writing any new behavior, so the repo wasn't left violating the rule be",
    "claimed_by": []
   },
   {
    "ref": "mse_azhh0f71css624jc:d2",
    "title": "D2 - A skill names the ADR that governs it, and ESR audits the link",
    "body": "- D: Skill frontmatter gains optional `governing_adr: <adr_id>`. ESR gains a `Skill governance` section reporting skills with no ref and skills whose ref does not resolve. Reported, never enforced. `topic_swarm.md`, `link_swarm.md` and `session_logging.md` are routed first (live and seed); 24 skills remain unrouted. - R: JNL asked for this after watching the failure. A skill says what to DO; an ADR says what is curre",
    "claimed_by": []
   },
   {
    "ref": "mse_5p94m2c3rwy7kbtj:d1",
    "title": "D1 - `memory-seed situate`: network-free orientation preflight with full cross-agent parity",
    "body": "- D: New `memory-seed situate` CLI (`situate.py`, mirroring `esr.py`) — a read-only report of LOCAL authoritative facts: git posture (branch / dirty / ahead-of-origin), `integration_mode`, the newest session entry (via the layout-aware `iter_session_documents` reader, not a re-derivation), worktree posture (reusing esr's helpers), and the local `pyproject` version + whether the CHANGELOG has a non-empty `## Unrelease",
    "claimed_by": []
   },
   {
    "ref": "mse_498jee2br14bp2mp:d2",
    "title": "D2 - Root cause: persona loading is a buried, conditional checklist step with no automatic backstop, unlike session-state recall",
    "body": "- D: No code change - a diagnosis, offered because JNL asked for one. - R: Traced the actual load path rather than guessing. `.agents/README.md` documents the intended behaviour (\"when an agent starts a session, it reads `_registry.yaml`... and applies those rules\"), and `.memory-seed/agent-rules.md` really does encode it - **step 10 of 10** in the startup checklist: \"If `.agents/_registry.yaml` exists... load all pe",
    "claimed_by": []
   },
   {
    "ref": "mse_vy5yzac5jqw1vd0e:d2",
    "title": "D2 - `skills/index.md` is the sole exemption; `.agents/` is excluded structurally, not by rule",
    "body": "- D: `.memory-seed/skills/index.md` is skipped and the skip is asserted to be the only one. `.agents/` persona templates are not mentioned in the filter at all — they simply never match the `.memory-seed/skills/` prefix. - R: `index.md` is runtime-local: projects extend the trigger registry with persona entries, so exact equality is the wrong assertion, and `test_skill_trigger_registry_is_deterministic_and_seeded` al",
    "claimed_by": []
   },
   {
    "ref": "mse_hqzcxnkehb7mbcma:d2",
    "title": "D2 - Route a skill to its ADR only where one genuinely governs it",
    "body": "- D: 12 more skills carry `governing_adr` (agent_collaboration -> worktree_convention, release_publishing -> release_topology, subproject_runtime -> subproject_scoping, and so on). 12 remain unrouted and ESR keeps naming them. - R: The unrouted 12 are persona and tooling skills - `code_search`, `security_triage`, `office_document_editing`, `graphify_analysis` - with no architectural concern in the ADR corpus to point",
    "claimed_by": []
   },
   {
    "ref": "mse_s3t523yvtr39978k:d1",
    "title": "D1 - Decision",
    "body": "- D: Updated the control-plane skills to match the new folder-primary document lifecycle, with seed twins. Rewrote `.memory-seed/skills/proposal_lifecycle.md`: the folder IS the lifecycle state; distinct terminal lanes (completed/rejected/superseded/deferred) replace the old single mixed \"completed\"; rejected/superseded are kept, never `git rm`'d; the project's `docs/README.md` front door is the path authority; secon",
    "claimed_by": []
   },
   {
    "ref": "mse_52cw7g10wmaha5h4:d1",
    "title": "D1 - Decision",
    "body": "- D: Updated `index.md` — folded the participant-count session-layout gate and the doctor local-user/participant-mismatch warning into the existing \"Participant registry parsing (current unreleased worktree)\" Design Decision bullet; added a new adjacent bullet for the one-time identity-offer hook (`.memory-seed/.identity-offer-stamp`); added a Topology bullet documenting the `docs/todo/` / `docs/todo/completed/` / `d",
    "claimed_by": []
   },
   {
    "ref": "mse_5ekvf2d0h5e2gw3y:d1",
    "title": "D1 - Workers load packet + one persona + triggered skills; safety rails stay",
    "body": "- D: Add the Worker Context Contract to `agent_collaboration.md` and two Task Packet fields (`persona:`, `context_load: packet|full`). A worker skips the full index read, the newest-session recency read, the whole skill registry, and load-all-active-personas. It still runs `base_sha`/preflight and the worktree guard. - R: - Operating Mode Start is written for the primary agent, which must establish current state itse",
    "claimed_by": []
   },
   {
    "ref": "mse_aqsfm061kmekm87m:d1",
    "title": "D1 - Adopt four Codex harness constants, each forced by an observed failure",
    "body": "- D: The Codex runner passes `-c projects={\"<run dir>\"={trust_level=\"trusted\"}}`, `--dangerously-bypass-approvals-and-sandbox`, `-c features.apps=false`, and pins the MCP server's `cwd` to the run directory per run rather than in the template. - R: None is cosmetic. Codex loads project-scoped `.codex/config.toml` and `.codex/hooks.json` only for trusted paths and every run directory is a fresh untrusted path, so with",
    "claimed_by": []
   },
   {
    "ref": "mse_rg1ghmczrxxjwxcv:d1",
    "title": "D1 - Opened folders join the SAME allowlist git worktrees use, so every existing endpoint serves them for free",
    "body": "- D: `create_app()` gained a process-lifetime `external_projects: dict[Path, dict]` registry alongside the existing `worktree_services`/`worktree_lock`. `worktree_entries()` now returns git worktrees plus `external_projects.values()`; `service_for()`'s allowlist (`known = {...for entry in worktree_entries()}`) was untouched, because it already derives from `worktree_entries()`. Registering a folder there is therefore",
    "claimed_by": []
   },
   {
    "ref": "mse_5trzymn43k92ky7z:d3",
    "title": "D3 - The Skill Loading section's per-skill description list is cut; `skills/index.md` already does that job with more precision",
    "body": "- D: The 17-bullet, one-line-per-skill description list (23 lines) is replaced by a 3-sentence paragraph: a pointer to `skills/index.md` as the authoritative `load_when`/`do_not_load_when` map, naming only the two skills needed almost every turn (`session_logging.md`, `end_of_turn.md`), and a closing line that everything else routes through the registry. Net effect on the file: -18 lines, more than paying for D2's tw",
    "claimed_by": []
   },
   {
    "ref": "mse_rg1ghmczrxxjwxcv:d2",
    "title": "D2 - The picker is a modal folder browser reusing the worktree-switch choreography, not a new switching mechanism",
    "body": "- D: New `FolderPicker.tsx` (modelled on `DiagramViewer`'s backdrop/Escape/outside-click pattern): browses via `GET /api/v1/browse`, marks folders with a `.memory-seed/` with a `FolderCheck` icon (a hint, not a promise - opening can still be refused), and either row has its own \"Open\" or the current folder has \"Open this folder\". A refusal shows the actual `doctor()` issues inline rather than just failing. On success",
    "claimed_by": []
   },
   {
    "ref": "mse_66xwhhr3thp18d4p:d1",
    "title": "D1 - Decision",
    "body": "- D: Record Memory Lense V1 as a current unreleased local UI/service path in the runtime index, including its backend module, static assets, source-of-truth boundary, and verified graph/timeline behavior. - R: The recent Memory Lense work changed durable project topology and active state; leaving it only in session logs would make future agents treat first-class files as an untracked experiment. - F: `.memory-seed/in",
    "claimed_by": []
   },
   {
    "ref": "mse_7p3q9r2t5v8w1x6z:d1",
    "title": "D1 - Decision",
    "body": "- D: Keep `.memory-seed/agent-rules.md` as a compact startup-safe operating contract and route procedural detail through indexed lazy skills. - R: This preserves always-on runtime discovery, authority, file ownership, and skill-loading rules while reducing the chance that agents carry bulky task-specific runbooks in every context. - F: Added live/seed skill twins for `history_retrieval.md`, `session_logging.md`, `end",
    "claimed_by": []
   },
   {
    "ref": "ms-38098d7a:d1",
    "title": "D1 - Decision",
    "body": "- D: Added opt-in local user identity, per-user session target creation, and user-aware hooks while keeping no-user installs on the legacy flat session file. - R: This advances the multi-user roadmap without silently moving existing users' write targets; a clone must opt in through `.memory-seed/local.yaml`, `MEMORY_SEED_USER`, or an explicit CLI/user argument. - A: Automatic user inference was not added; guessing wo",
    "claimed_by": [
     "adr_session_layout_participant_gating"
    ]
   },
   {
    "ref": "mse_25zzy3cmdjgrsf69:d1",
    "title": "D1 - Declare the axis on roots only and let children inherit it",
    "body": "- D: `axis: area` / `axis: activity` is declared on the 23 root slugs and on nothing else. Every child leaves the field empty and resolves its axis through `TopicIndex.axis_of()`, which walks to the nearest declaring ancestor. The assignment is the one already measured and published in `topic-vocabulary-concentration-review.md` - 13 areas (`memory-trace`, `memory-seed`, `graph`, `session-logging`, `control-plane`, `m",
    "claimed_by": [
     "adr_topic_vocabulary"
    ]
   }
  ]
 }
]