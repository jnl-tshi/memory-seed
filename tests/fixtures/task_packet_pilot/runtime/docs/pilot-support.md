# Pilot support evidence

The frontier-authored artifact is the semantic dispatch. The exact immutable profile version, measured runtime binding, and resolver-pinned corpus revision reconstruct the complete Task Packet. Compilation is local and adds no packet registry, worker dispatch, worktree creation, authority, pricing lookup, provider lookup, or network access.
