{
  "fixed_instructions": [
    "Assess only the compiled Task Packet at the exact repository-relative path `.superpowers/sdd/retrieval-m2-task-packet-compiler/task-4-pilot-round2/task-packet.json`. Treat that packet as the complete initial context: do not inspect the inherited conversation, project index, project policy, repository, or broad filesystem. Reading that one supplied packet is initial context, not a supplemental call. Evaluate whether its Evidence Pack supports the bounded conclusions in the dispatch output contract. Materialized evidence is already supplied and must not be refetched. If a material gap remains, use only a task-scoped Memory Seed read named in the supplied tool manifest and record the exact supplemental source and call; otherwise make no supplemental tool call. Return only one JSON object matching the recorded-assessment fields: evidence_status, conclusions, missing_questions, supplemental_sources, supplemental_calls, unsupported_assertions, evidence_id_correctness, repeated_fetches, broad_discovery, selected_tier, soft_cap_tokens, tool_call_instrumentation, caller_supplied_harness_accounting, hidden_platform_overhead, actual_provider_input, actual_provider_usage, actual_provider_latency, and actual_provider_cost. `caller_supplied_harness_accounting` must be available and report `fixed_instruction_tokens` plus `tool_schema_tokens` from the packet compiler ledger. `hidden_platform_overhead` must be unavailable with the reason that the runtime did not surface it. Actual provider input, usage, latency, and cost remain unavailable unless post-run provider telemetry explicitly supplies them. Conclusions must cite only evidence IDs present in the packet or reported supplemental sources. This output is worker self-report evidence, not mechanically instrumented proof."
  ],
  "tool_schemas": [
    {
      "name": "memory_task_scoped_read",
      "description": "Read one specifically identified Memory Seed source only when the supplied packet has a material evidence gap; broad discovery is forbidden.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "source",
          "gap"
        ],
        "properties": {
          "source": {
            "type": "string",
            "description": "Exact task-scoped Memory Seed source or evidence identifier."
          },
          "gap": {
            "type": "string",
            "description": "Material question the supplied packet cannot answer."
          }
        }
      }
    }
  ]
}
